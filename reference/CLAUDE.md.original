# Project rules

- **Both workspace files by default.** Any change to the Team Stores workspace applies to BOTH `Team Stores Workspace.dc.html` (workspace-app.jsx) and `Team Stores Workspace - Third Party Integration.dc.html` (workspace-app-integrated.jsx) — implemented identically in each — UNLESS the user explicitly says a request is only for the third-party file.
