[{"type":"range","start":"// One contact channel (email or phone) with Service / Salesperson / Custom radio rows.\nfunction ContactChannel({ kind, mode, onMode, sp, onSp, custom, onCustom, accent }) {","end":"function StorePointOfContact","replace":"// One contact channel (email or phone) with Team Manager / Salesperson / Custom\n// radio rows. Team Manager (the default) inherits the Team Manager record live —\n// it reads through to that record rather than copying the value.\nfunction ContactChannel({ kind, mode, onMode, sp, onSp, custom, onCustom, accent, inherited, onEditManager }) {\n  const isEmail = kind === \"email\";\n  const Icon = isEmail ? IconMail : IconPhone;\n  const m = (!mode || mode === \"service\") ? \"teammanager\" : mode; // legacy \"service\" reads as Team Manager\n  const labels = isEmail\n    ? { tm: \"Team Manager Email\", sp: \"Salesperson Email\", custom: \"Custom Email\" }\n    : { tm: \"Team Manager Phone\", sp: \"Salesperson Phone\", custom: \"Custom Phone\" };\n  const placeholder = isEmail ? \"example@email.com\" : \"000-000-0000\";\n  const spField = isEmail ? \"email\" : \"phone\";\n\n  const Row = ({ val, label, children }) => (\n    <button type=\"button\" onClick={() => onMode(val)} style={{\n      textAlign: \"left\", width: \"100%\", background: \"transparent\", border: 0, padding: 0,\n      cursor: \"pointer\", display: \"block\",\n    }}>\n      <div style={{ display: \"flex\", gap: 12, alignItems: \"flex-start\" }}>\n        <RadioDot on={m === val} accent={accent} />\n        <div style={{ flex: 1, minWidth: 0 }}>\n          <div style={{ fontSize: 14, fontWeight: 700, color: \"#0e0e10\", lineHeight: 1.3 }}>{label}</div>\n          {children}\n        </div>\n      </div>\n    </button>\n  );\n\n  const inputStyle = {\n    width: \"100%\", border: \"1px solid #d4d4d8\", borderRadius: 5, padding: \"12px 12px\",\n    fontSize: 13.5, color: \"#0e0e10\", outline: \"none\", marginTop: 8, fontFamily: \"inherit\",\n  };\n\n  return (\n    <div style={{ flex: 1, minWidth: 0 }}>\n      <div style={{ display: \"flex\", alignItems: \"center\", gap: 8, marginBottom: 16 }}>\n        <Icon size={16} style={{ color: accent }} />\n        <span style={{ fontSize: 12, fontWeight: 800, letterSpacing: \".08em\", color: \"#0e0e10\", textTransform: \"uppercase\" }}>\n          {isEmail ? \"Email\" : \"Phone\"}\n        </span>\n      </div>\n      <div style={{ display: \"flex\", flexDirection: \"column\", gap: 16 }}>\n        <Row val=\"teammanager\" label={labels.tm}>\n          {/* Inherits — read-only here; edited on the Team Manager record itself */}\n          <div style={{ marginTop: 2 }}>\n            <div style={{ fontSize: 13, color: inherited ? \"#0e0e10\" : \"#b06f1e\", fontWeight: 600, fontFamily: (isEmail || !inherited) ? \"inherit\" : \"JetBrains Mono, monospace\" }}>\n              {inherited || (\"No \" + (isEmail ? \"email\" : \"phone number\") + \" on the Team Manager record yet\")}\n            </div>\n            {m === \"teammanager\" && (\n              <div style={{ fontSize: 11.5, color: \"#9a9aa1\", marginTop: 3 }}>\n                Follows the Team Manager record.{\" \"}\n                {onEditManager && <span role=\"button\" onClick={(e) => { e.stopPropagation(); onEditManager(); }} style={{ color: \"#0e0e10\", fontWeight: 600, textDecoration: \"underline\", textUnderlineOffset: 2, cursor: \"pointer\" }}>Edit it there</span>}\n              </div>\n            )}\n          </div>\n        </Row>\n        <Row val=\"salesperson\" label={labels.sp}>\n          {m === \"salesperson\" && (\n            <div onClick={(e) => e.stopPropagation()} style={{ marginTop: 8 }}>\n              <SelectField label=\"Select a Salesperson\" value={sp}\n                onChange={onSp}\n                options={SALESPEOPLE.map((s) => ({ value: s.value, label: s.label + \" · \" + s[spField] }))} />\n            </div>\n          )}\n        </Row>\n        <Row val=\"custom\" label={labels.custom}>\n          {m === \"custom\" && (\n            <input onClick={(e) => e.stopPropagation()} value={custom || \"\"} placeholder={placeholder}\n              type={isEmail ? \"email\" : \"tel\"}\n              onChange={(e) => onCustom(isEmail ? e.target.value : formatPhone(e.target.value))} style={inputStyle} />\n          )}\n        </Row>\n      </div>\n    </div>\n  );\n}\n\nfunction StorePointOfContact"},{"type":"once","find":"        <ContactChannel kind=\"email\" accent={accent}\n          mode={data.contactEmailMode} onMode={(v) => set({ contactEmailMode: v })}\n          sp={data.contactEmailSalesperson} onSp={(v) => set({ contactEmailSalesperson: v })}\n          custom={data.contactEmailCustom} onCustom={(v) => set({ contactEmailCustom: v })} />\n        <ContactChannel kind=\"phone\" accent={accent}\n          mode={data.contactPhoneMode} onMode={(v) => set({ contactPhoneMode: v })}\n          sp={data.contactPhoneSalesperson} onSp={(v) => set({ contactPhoneSalesperson: v })}\n          custom={data.contactPhoneCustom} onCustom={(v) => set({ contactPhoneCustom: v })} />","replace":"        <ContactChannel kind=\"email\" accent={accent}\n          inherited={data.email} onEditManager={jumpToManagerPanel}\n          mode={data.contactEmailMode} onMode={(v) => set({ contactEmailMode: v })}\n          sp={data.contactEmailSalesperson} onSp={(v) => set({ contactEmailSalesperson: v })}\n          custom={data.contactEmailCustom} onCustom={(v) => set({ contactEmailCustom: v })} />\n        <ContactChannel kind=\"phone\" accent={accent}\n          inherited={data.phone} onEditManager={jumpToManagerPanel}\n          mode={data.contactPhoneMode} onMode={(v) => set({ contactPhoneMode: v })}\n          sp={data.contactPhoneSalesperson} onSp={(v) => set({ contactPhoneSalesperson: v })}\n          custom={data.contactPhoneCustom} onCustom={(v) => set({ contactPhoneCustom: v })} />"},{"type":"once","find":"function StorePointOfContact({ data, set, accent }) {","replace":"function StorePointOfContact({ data, set, accent }) {\n  // Scroll the wizard to the Team Manager record (no-op where the panel isn't on screen).\n  const jumpToManagerPanel = () => {\n    const el = document.getElementById(\"tm-panel\");\n    if (el) window.scrollTo({ top: window.scrollY + el.getBoundingClientRect().top - 100, behavior: \"smooth\" });\n  };"},{"type":"once","find":"      <WizardPanel label={data.storeType === \"league\" ? \"League Manager\" : \"Team Manager\"} hint={data.storeType === \"league\" ? \"League director\" : \"Team coach\"}>\n        <div style={{ display: \"grid\", gridTemplateColumns: \"1fr 1fr\", gap: 16, marginBottom: 16 }}>","replace":"      <WizardPanel label={data.storeType === \"league\" ? \"League Manager\" : \"Team Manager\"} hint={data.storeType === \"league\" ? \"League director\" : \"Team coach\"}>\n        <span id=\"tm-panel\" style={{ display: \"block\", height: 0 }}></span>\n        <div style={{ display: \"grid\", gridTemplateColumns: \"1fr 1fr\", gap: 16, marginBottom: 16 }}>"},{"type":"once","find":"            <input type=\"email\" value={data.email || \"\"} onChange={(e) => set({ email: e.target.value })} placeholder=\"name@example.com\" style={managerInput} />","replace":"            <input type=\"email\" value={data.email || \"\"} onChange={(e) => set({ email: e.target.value })} placeholder=\"name@example.com\" style={{ ...managerInput, ...(errors.email ? { borderColor: \"#b81d14\" } : {}) }} />\n            {errors.email && <div style={{ color: \"#b81d14\", fontSize: 12, marginTop: 6 }}>{errors.email}</div>}"},{"type":"once","find":"            <input type=\"tel\" value={data.phone || \"\"} onChange={(e) => set({ phone: formatPhone(e.target.value) })} placeholder=\"111-000-3243\" style={{ ...managerInput, fontFamily: \"JetBrains Mono, monospace\" }} />","replace":"            <input type=\"tel\" value={data.phone || \"\"} onChange={(e) => set({ phone: formatPhone(e.target.value) })} placeholder=\"111-000-3243\" style={{ ...managerInput, fontFamily: \"JetBrains Mono, monospace\", ...(errors.phone ? { borderColor: \"#b81d14\" } : {}) }} />\n            {errors.phone && <div style={{ color: \"#b81d14\", fontSize: 12, marginTop: 6 }}>{errors.phone}</div>}"},{"type":"once","find":"      if (!data.storeSlug || !data.storeSlug.trim()) e.storeSlug = \"Web address is required\";","replace":"      if (!data.storeSlug || !data.storeSlug.trim()) e.storeSlug = \"Web address is required\";\n      // Team Manager is the default point-of-contact source — any channel left on\n      // that mode makes the corresponding record field required (error lands HERE).\n      const emMode = data.contactEmailMode === \"service\" ? \"teammanager\" : (data.contactEmailMode || \"teammanager\");\n      const phMode = data.contactPhoneMode === \"service\" ? \"teammanager\" : (data.contactPhoneMode || \"teammanager\");\n      if (emMode === \"teammanager\" && !isValidEmail(data.email)) e.email = \"Required — the store's point of contact follows this record\";\n      if (phMode === \"teammanager\" && !isValidPhone(data.phone)) e.phone = \"Enter a 10-digit phone — the store's point of contact follows this record\";"},{"type":"once","find":"    const derivedEmail = data.contactEmailMode === \"service\" ? \"service@prolookstores.com\" :\n    data.contactEmailMode === \"salesperson\" ? findSp(data.contactEmailSalesperson).email || data.email :\n    data.contactEmailCustom || data.email;\n    const derivedPhone = data.contactPhoneMode === \"service\" ? \"888-776-5665\" :\n    data.contactPhoneMode === \"salesperson\" ? findSp(data.contactPhoneSalesperson).phone || data.phone :\n    data.contactPhoneCustom || data.phone;","replace":"    // Team Manager mode inherits the manager record; salesperson/custom override.\n    const derivedEmail = data.contactEmailMode === \"salesperson\" ? findSp(data.contactEmailSalesperson).email || data.email :\n    data.contactEmailMode === \"custom\" ? data.contactEmailCustom || data.email :\n    data.email;\n    const derivedPhone = data.contactPhoneMode === \"salesperson\" ? findSp(data.contactPhoneSalesperson).phone || data.phone :\n    data.contactPhoneMode === \"custom\" ? data.contactPhoneCustom || data.phone :\n    data.phone;"},{"type":"once","find":"    contactEmailMode: p.contactEmailMode || \"service\",","replace":"    contactEmailMode: p.contactEmailMode || \"teammanager\","},{"type":"once","find":"    contactPhoneMode: p.contactPhoneMode || \"service\",","replace":"    contactPhoneMode: p.contactPhoneMode || \"teammanager\","},{"type":"once","find":"    contactEmailMode: \"service\",\n    contactPhoneMode: \"service\",","replace":"    contactEmailMode: \"teammanager\",\n    contactPhoneMode: \"teammanager\","},{"type":"once","find":"      contactEmailMode: store.contactEmailMode || \"service\",","replace":"      contactEmailMode: store.contactEmailMode || \"teammanager\","},{"type":"once","find":"      contactPhoneMode: store.contactPhoneMode || \"service\",","replace":"      contactPhoneMode: store.contactPhoneMode || \"teammanager\","},{"type":"once","find":"  const emailVal = data.contactEmailMode === \"service\" ? SERVICE_CONTACT.email\n    : data.contactEmailMode === \"salesperson\" ? ((SALESPEOPLE.find((s) => s.value === data.contactEmailSalesperson) || {}).email || \"Salesperson\")\n    : (data.contactEmailCustom || \"Custom email\");","replace":"  const emailVal = data.contactEmailMode === \"salesperson\" ? ((SALESPEOPLE.find((s) => s.value === data.contactEmailSalesperson) || {}).email || \"Salesperson\")\n    : data.contactEmailMode === \"custom\" ? (data.contactEmailCustom || \"Custom email\")\n    : (data.email || \"Team Manager email\");"},{"type":"opt","find":": \"Service Email\";","replace":": \"Team Manager Email\";"},{"type":"opt","find":": SERVICE_CONTACT.email;","replace":": (store.email || \"—\");"},{"type":"opt","find":": \"Service Phone\";","replace":": \"Team Manager Phone\";"},{"type":"opt","find":": SERVICE_CONTACT.phone;","replace":": (store.phone || \"—\");"},{"type":"once","find":"const EG_COUNTRIES = [\"United States\", \"Canada\", \"Mexico\"];","replace":"const EG_COUNTRIES = [\"United States\", \"Canada\", \"Mexico\"];\n// Two-letter postal abbreviations for type-ahead state search (Epic G).\nconst TAX_STATE_ABBR = { Alabama: \"AL\", Alaska: \"AK\", Arizona: \"AZ\", Arkansas: \"AR\", California: \"CA\", Colorado: \"CO\", Connecticut: \"CT\", Delaware: \"DE\", Florida: \"FL\", Georgia: \"GA\", Hawaii: \"HI\", Idaho: \"ID\", Illinois: \"IL\", Indiana: \"IN\", Iowa: \"IA\", Kansas: \"KS\", Kentucky: \"KY\", Louisiana: \"LA\", Maine: \"ME\", Maryland: \"MD\", Massachusetts: \"MA\", Michigan: \"MI\", Minnesota: \"MN\", Mississippi: \"MS\", Missouri: \"MO\", Montana: \"MT\", Nebraska: \"NE\", Nevada: \"NV\", \"New Hampshire\": \"NH\", \"New Jersey\": \"NJ\", \"New Mexico\": \"NM\", \"New York\": \"NY\", \"North Carolina\": \"NC\", \"North Dakota\": \"ND\", Ohio: \"OH\", Oklahoma: \"OK\", Oregon: \"OR\", Pennsylvania: \"PA\", \"Rhode Island\": \"RI\", \"South Carolina\": \"SC\", \"South Dakota\": \"SD\", Tennessee: \"TN\", Texas: \"TX\", Utah: \"UT\", Vermont: \"VT\", Virginia: \"VA\", Washington: \"WA\", \"West Virginia\": \"WV\", Wisconsin: \"WI\", Wyoming: \"WY\" };\n// Type-ahead single-select over EG_STATES; filters by name or abbreviation and\n// stays browsable when nothing is typed. Selection auto-fills the tax rate.\nfunction StateSearchSelect({ label = \"State\", value, onChange }) {\n  const [q, setQ] = React.useState(\"\");\n  const [open, setOpen] = React.useState(false);\n  const [hi, setHi] = React.useState(0);\n  const wrapRef = React.useRef(null);\n  React.useEffect(() => {\n    const h = (e) => { if (wrapRef.current && !wrapRef.current.contains(e.target)) setOpen(false); };\n    document.addEventListener(\"mousedown\", h);\n    return () => document.removeEventListener(\"mousedown\", h);\n  }, []);\n  const ql = q.trim().toLowerCase();\n  const list = EG_STATES.filter((s) => !ql || s.toLowerCase().includes(ql) || (TAX_STATE_ABBR[s] || \"\").toLowerCase().startsWith(ql));\n  const pick = (s) => { onChange(s); setQ(\"\"); setOpen(false); };\n  return (\n    <div ref={wrapRef} style={{ position: \"relative\" }}>\n      <div style={{ fontSize: 12.5, fontWeight: 600, color: \"#3f3f46\", marginBottom: 7 }}>{label}</div>\n      <input\n        value={open ? q : (value || \"\")}\n        placeholder={value || \"Search by state or abbreviation\"}\n        onFocus={() => { setOpen(true); setQ(\"\"); setHi(0); }}\n        onChange={(e) => { setQ(e.target.value); setOpen(true); setHi(0); }}\n        onKeyDown={(e) => {\n          if (e.key === \"ArrowDown\") { e.preventDefault(); setHi((h) => Math.min(h + 1, list.length - 1)); }\n          else if (e.key === \"ArrowUp\") { e.preventDefault(); setHi((h) => Math.max(h - 1, 0)); }\n          else if (e.key === \"Enter\") { e.preventDefault(); if (list[hi]) pick(list[hi]); }\n          else if (e.key === \"Escape\") setOpen(false);\n        }}\n        style={{ width: \"100%\", height: 44, boxSizing: \"border-box\", border: \"1px solid #d4d4d8\", borderRadius: 5, padding: \"0 12px\", fontSize: 13.5, fontFamily: \"inherit\", outline: \"none\", background: \"#fff\", color: \"#0e0e10\" }} />\n      {open && (\n        <div style={{ position: \"absolute\", left: 0, right: 0, top: \"calc(100% + 4px)\", zIndex: 60, background: \"#fff\", border: \"1px solid #e4e4e7\", borderRadius: 8, boxShadow: \"0 12px 28px rgba(20,20,30,.14)\", maxHeight: 240, overflowY: \"auto\", padding: 4 }}>\n          {list.length === 0 && <div style={{ padding: \"12px 12px\", fontSize: 12.5, color: \"#9a9aa1\" }}>No state matches \"{q}\"</div>}\n          {list.map((s, i) => (\n            <button key={s} type=\"button\" onMouseEnter={() => setHi(i)} onClick={() => pick(s)} style={{ display: \"flex\", alignItems: \"center\", justifyContent: \"space-between\", gap: 10, width: \"100%\", textAlign: \"left\", background: i === hi ? \"#f4f4f5\" : \"#fff\", border: 0, borderRadius: 6, padding: \"9px 10px\", fontSize: 13, fontWeight: value === s ? 700 : 500, color: \"#0e0e10\", cursor: \"pointer\", fontFamily: \"inherit\" }}>\n              <span>{s}</span>\n              <span style={{ fontSize: 11, fontWeight: 700, color: \"#9a9aa1\" }}>{TAX_STATE_ABBR[s] || \"\"}</span>\n            </button>\n          ))}\n        </div>\n      )}\n    </div>\n  );\n}"},{"type":"once","find":"              <SelectField label=\"State\" placeholder=\"Select a state\" value={data.taxState} options={EG_STATES}\n                onChange={(v) => set({ taxState: v, taxRate: STATE_TAX_RATES[v] != null ? String(STATE_TAX_RATES[v]) : data.taxRate })} />","replace":"              <StateSearchSelect label=\"State\" value={data.taxState}\n                onChange={(v) => set({ taxState: v, taxRate: STATE_TAX_RATES[v] != null ? String(STATE_TAX_RATES[v]) : data.taxRate })} />"},{"type":"once","find":"              <SelectField label=\"State\" placeholder=\"Select a state\" value={f.taxState} options={EG_STATES}\n                onChange={(v) => setMany({ taxState: v, taxRate: STATE_TAX_RATES[v] != null ? String(STATE_TAX_RATES[v]) : f.taxRate })} />","replace":"              <StateSearchSelect label=\"State\" value={f.taxState}\n                onChange={(v) => setMany({ taxState: v, taxRate: STATE_TAX_RATES[v] != null ? String(STATE_TAX_RATES[v]) : f.taxRate })} />"},{"type":"once","find":"Object.assign(window, { OnboardingChecklist, CHECKLIST_STEPS });","replace":"// ─────────── Store Setup Guide — informational modal ───────────\n// Plain-language orientation, not a checklist: no progress tracking, never a\n// gate. Auto-shows once per rep; reopenable from the workspace header.\nfunction StoreGuideModal({ open, onClose }) {\n  if (!open) return null;\n  const sec = (title, body) => (\n    <div key={title} style={{ display: \"flex\", gap: 14, alignItems: \"flex-start\" }}>\n      <span style={{ width: 6, height: 6, borderRadius: 999, background: \"#e1251b\", marginTop: 7, flexShrink: 0 }} />\n      <div>\n        <div style={{ fontSize: 13.5, fontWeight: 800, color: \"#0e0e10\", marginBottom: 3 }}>{title}</div>\n        <div style={{ fontSize: 12.5, color: \"#52525b\", lineHeight: 1.6 }}>{body}</div>\n      </div>\n    </div>\n  );\n  return (\n    <Modal open onClose={onClose} width={640}>\n      <div style={{ padding: \"28px 32px 28px\" }}>\n        <div style={{ display: \"flex\", alignItems: \"flex-start\", justifyContent: \"space-between\", gap: 12 }}>\n          <div>\n            <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: \".12em\", color: \"#9a9aa1\", textTransform: \"uppercase\", marginBottom: 6 }}>Store Setup Guide</div>\n            <h2 style={{ margin: 0, fontSize: 22, fontWeight: 800, color: \"#0e0e10\", fontFamily: \"Archivo, sans-serif\" }}>How your store comes together</h2>\n          </div>\n          <button onClick={onClose} aria-label=\"Close\" style={{ background: \"none\", border: 0, cursor: \"pointer\", color: \"#6b6b72\", padding: 4 }}><IconX size={20} /></button>\n        </div>\n        <div style={{ fontSize: 13, color: \"#6b6b72\", lineHeight: 1.55, margin: \"10px 0 20px\" }}>\n          A quick orientation — nothing here is required, and you can reopen it anytime from the workspace header.\n        </div>\n        <div style={{ display: \"flex\", flexDirection: \"column\", gap: 16, marginBottom: 24 }}>\n          {sec(\"The tabs\", \"Products holds your catalog. Orders tracks buyer purchases. My Team(s) manages rosters and team codes. Reports covers sales and fundraising. Settings edits everything you set at creation.\")}\n          {sec(\"How a product reaches the storefront\", \"Add it (Add Products), configure it (sizes, category, pricing), turn its Visibility on, then launch the store. Hidden items stay in your catalog but never show to buyers.\")}\n          {sec(\"Team Required vs. Team Supplied\", \"Team Required gear must be bought by rostered players at their own cost. Team Supplied gear is already paid for — players pick sizes and pay $0. Both file under the Team Gear category.\")}\n          {sec(\"Product Vault vs. bundles\", \"The Vault is your private library — Pro Stock, Sideline Stock, and reusable packages you can drop into any store. A bundle is buyer-facing: several catalog items sold together as one storefront line.\")}\n          {sec(\"Launching, closing, re-opening\", \"Launch controls live in the workspace on the Settings tab. A store needs at least one visible product to launch; you can close and re-open it anytime.\")}\n        </div>\n        <div style={{ display: \"flex\", justifyContent: \"flex-end\" }}>\n          <button onClick={onClose} style={{ background: \"#0e0e10\", color: \"#fff\", border: 0, borderRadius: 6, padding: \"12px 24px\", fontSize: 13, fontWeight: 700, cursor: \"pointer\", fontFamily: \"inherit\" }}>Got it</button>\n        </div>\n      </div>\n    </Modal>\n  );\n}\n\nObject.assign(window, { OnboardingChecklist, CHECKLIST_STEPS, StoreGuideModal });"},{"type":"once","find":"  const [tourRun, setTourRun] = React.useState(false);","replace":"  const [tourRun, setTourRun] = React.useState(false);\n  // Store Setup Guide — informational modal; dismissal is per rep, not per store.\n  const [guideOpen, setGuideOpen] = React.useState(false);\n  const dismissGuide = () => { setGuideOpen(false); try { localStorage.setItem(\"ts_setup_guide_seen_v1\", \"1\"); } catch (e) {} };\n  React.useEffect(() => {\n    if (screen !== \"dashboard\" || isFrameRoute) return;\n    let seen = false;\n    try { seen = localStorage.getItem(\"ts_setup_guide_seen_v1\") === \"1\"; } catch (e) {}\n    if (!seen) setGuideOpen(true);\n  }, [screen]);\n  React.useEffect(() => {\n    window.__openStoreGuide = () => setGuideOpen(true);\n    return () => { delete window.__openStoreGuide; };\n  }, []);"},{"type":"range","start":"      <OnboardingChecklist\n        run={tourRun && !isFrameRoute && !tweaks.overviewMode}\n        store={activeStore}\n        screen={screen}\n        accent={tweaks.checklistAccent}\n        position={tweaks.checklistPosition}\n        onStepAction={handleChecklistStep}\n        onDismiss={endTour}\n        ","end":"onComplete={() => persistFirstStore(false)}","replace":"      {/* Store Setup Guide — informational modal, replaces the corner checklist */}\n      <StoreGuideModal\n        open={guideOpen && !isFrameRoute && !tweaks.overviewMode}\n        onClose={dismissGuide}\n        "},{"type":"once","find":"          <TweakButton label=\"▶  Start onboarding demo\" onClick={startOnboardingDemo} />","replace":"          <TweakButton label=\"Open Store Setup Guide\" onClick={() => setGuideOpen(true)} />\n          <TweakButton label=\"Reset guide (auto-shows again)\" secondary onClick={() => { try { localStorage.removeItem(\"ts_setup_guide_seen_v1\"); } catch (e) {} setToast(\"Guide reset — it will auto-open on the next workspace visit\"); }} />"},{"type":"once","find":"      {/* Right — preview store + actions menu */}\n      <div style={{ justifySelf: \"end\", display: \"flex\", alignItems: \"center\", gap: 12 }}>","replace":"      {/* Right — store guide + preview store + actions menu */}\n      <div style={{ justifySelf: \"end\", display: \"flex\", alignItems: \"center\", gap: 12 }}>\n        <button onClick={() => window.__openStoreGuide && window.__openStoreGuide()} title=\"Store Setup Guide — how the workspace fits together\" style={{\n          display: \"inline-flex\", alignItems: \"center\", gap: 8, background: \"transparent\",\n          color: \"rgba(255,255,255,.85)\", border: 0, padding: \"12px 8px\", cursor: \"pointer\",\n          fontSize: 13.5, fontWeight: 600, fontFamily: \"inherit\",\n        }}\n          onMouseEnter={(e) => (e.currentTarget.style.color = \"#fff\")}\n          onMouseLeave={(e) => (e.currentTarget.style.color = \"rgba(255,255,255,.85)\")}>\n          <IconInfo size={16} /> Store Guide\n        </button>"}]