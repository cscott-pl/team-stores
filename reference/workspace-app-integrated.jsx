

/* ===== ASSET 0 : 7989a353-667b-4392-962f-4d2a39bb3307 ===== */

// tweaks-panel.jsx
// Reusable Tweaks shell + form-control helpers.
//
// Owns the host protocol (listens for __activate_edit_mode / __deactivate_edit_mode,
// posts __edit_mode_available / __edit_mode_set_keys / __edit_mode_dismissed) so
// individual prototypes don't re-roll it. Ships a consistent set of controls so you
// don't hand-draw <input type="range">, segmented radios, steppers, etc.
//
// Usage (in an HTML file that loads React + Babel):
//
//   const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
//     "primaryColor": "#D97757",
//     "palette": ["#D97757", "#29261b", "#f6f4ef"],
//     "fontSize": 16,
//     "density": "regular",
//     "dark": false
//   }/*EDITMODE-END*/;
//
//   function App() {
//     const [t, setTweak] = useTweaks(TWEAK_DEFAULTS);
//     return (
//       <div style={{ fontSize: t.fontSize, color: t.primaryColor }}>
//         Hello
//         <TweaksPanel>
//           <TweakSection label="Typography" />
//           <TweakSlider label="Font size" value={t.fontSize} min={10} max={32} unit="px"
//                        onChange={(v) => setTweak('fontSize', v)} />
//           <TweakRadio  label="Density" value={t.density}
//                        options={['compact', 'regular', 'comfy']}
//                        onChange={(v) => setTweak('density', v)} />
//           <TweakSection label="Theme" />
//           <TweakColor  label="Primary" value={t.primaryColor}
//                        options={['#D97757', '#2A6FDB', '#1F8A5B', '#7A5AE0']}
//                        onChange={(v) => setTweak('primaryColor', v)} />
//           <TweakColor  label="Palette" value={t.palette}
//                        options={[['#D97757', '#29261b', '#f6f4ef'],
//                                  ['#475569', '#0f172a', '#f1f5f9']]}
//                        onChange={(v) => setTweak('palette', v)} />
//           <TweakToggle label="Dark mode" value={t.dark}
//                        onChange={(v) => setTweak('dark', v)} />
//         </TweaksPanel>
//       </div>
//     );
//   }
//
// ─────────────────────────────────────────────────────────────────────────────

const __TWEAKS_STYLE = `
  .twk-panel{position:fixed;right:16px;bottom:16px;z-index:2147483646;width:280px;
    max-height:calc(100vh - 32px);display:flex;flex-direction:column;
    transform:scale(var(--dc-inv-zoom,1));transform-origin:bottom right;
    background:rgba(250,249,247,.78);color:#29261b;
    -webkit-backdrop-filter:blur(24px) saturate(160%);backdrop-filter:blur(24px) saturate(160%);
    border:.5px solid rgba(255,255,255,.6);border-radius:14px;
    box-shadow:0 1px 0 rgba(255,255,255,.5) inset,0 12px 40px rgba(0,0,0,.18);
    font:11.5px/1.4 ui-sans-serif,system-ui,-apple-system,sans-serif;overflow:hidden}
  .twk-hd{display:flex;align-items:center;justify-content:space-between;
    padding:12px 8px 12px 16px;cursor:move;user-select:none}
  .twk-hd b{font-size:12px;font-weight:600;letter-spacing:.01em}
  .twk-x{appearance:none;border:0;background:transparent;color:rgba(41,38,27,.55);
    width:24px;height:24px;border-radius:6px;cursor:default;font-size:13px;line-height:1}
  .twk-x:hover{background:rgba(0,0,0,.06);color:#29261b}
  .twk-body{padding:2px 16px 16px;display:flex;flex-direction:column;gap:12px;
    overflow-y:auto;overflow-x:hidden;min-height:0;
    scrollbar-width:thin;scrollbar-color:rgba(0,0,0,.15) transparent}
  .twk-body::-webkit-scrollbar{width:8px}
  .twk-body::-webkit-scrollbar-track{background:transparent;margin:2px}
  .twk-body::-webkit-scrollbar-thumb{background:rgba(0,0,0,.15);border-radius:4px;
    border:2px solid transparent;background-clip:content-box}
  .twk-body::-webkit-scrollbar-thumb:hover{background:rgba(0,0,0,.25);
    border:2px solid transparent;background-clip:content-box}
  .twk-row{display:flex;flex-direction:column;gap:4px}
  .twk-row-h{flex-direction:row;align-items:center;justify-content:space-between;gap:12px}
  .twk-lbl{display:flex;justify-content:space-between;align-items:baseline;
    color:rgba(41,38,27,.72)}
  .twk-lbl>span:first-child{font-weight:500}
  .twk-val{color:rgba(41,38,27,.5);font-variant-numeric:tabular-nums}

  .twk-sect{font-size:10px;font-weight:600;letter-spacing:.06em;text-transform:uppercase;
    color:rgba(41,38,27,.45);padding:12px 0 0}
  .twk-sect:first-child{padding-top:0}

  .twk-field{appearance:none;box-sizing:border-box;width:100%;min-width:0;height:28px;padding:0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;
    background:rgba(255,255,255,.6);color:inherit;font:inherit;outline:none}
  .twk-field:focus{border-color:rgba(0,0,0,.25);background:rgba(255,255,255,.85)}
  select.twk-field{padding-right:24px;
    background-image:url("data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='10' height='6' viewBox='0 0 10 6'><path fill='rgba(0,0,0,.5)' d='M0 0h10L5 6z'/></svg>");
    background-repeat:no-repeat;background-position:right 8px center}

  .twk-slider{appearance:none;-webkit-appearance:none;width:100%;height:4px;margin:8px 0;
    border-radius:999px;background:rgba(0,0,0,.12);outline:none}
  .twk-slider::-webkit-slider-thumb{-webkit-appearance:none;appearance:none;
    width:16px;height:16px;border-radius:50%;background:#fff;
    border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}
  .twk-slider::-moz-range-thumb{width:16px;height:16px;border-radius:50%;
    background:#fff;border:.5px solid rgba(0,0,0,.12);box-shadow:0 1px 3px rgba(0,0,0,.2);cursor:default}

  .twk-seg{position:relative;display:flex;padding:2px;border-radius:8px;
    background:rgba(0,0,0,.06);user-select:none}
  .twk-seg-thumb{position:absolute;top:2px;bottom:2px;border-radius:6px;
    background:rgba(255,255,255,.9);box-shadow:0 1px 2px rgba(0,0,0,.12);
    transition:left .15s cubic-bezier(.3,.7,.4,1),width .15s}
  .twk-seg.dragging .twk-seg-thumb{transition:none}
  .twk-seg button{appearance:none;position:relative;z-index:1;flex:1;border:0;
    background:transparent;color:inherit;font:inherit;font-weight:500;min-height:24px;
    border-radius:6px;cursor:default;padding:4px 8px;line-height:1.2;
    overflow-wrap:anywhere}

  .twk-toggle{position:relative;width:32px;height:16px;border:0;border-radius:999px;
    background:rgba(0,0,0,.15);transition:background .15s;cursor:default;padding:0}
  .twk-toggle[data-on="1"]{background:#34c759}
  .twk-toggle i{position:absolute;top:2px;left:2px;width:16px;height:16px;border-radius:50%;
    background:#fff;box-shadow:0 1px 2px rgba(0,0,0,.25);transition:transform .15s}
  .twk-toggle[data-on="1"] i{transform:translateX(16px)}

  .twk-num{display:flex;align-items:center;box-sizing:border-box;min-width:0;height:28px;padding:0 0 0 8px;
    border:.5px solid rgba(0,0,0,.1);border-radius:7px;background:rgba(255,255,255,.6)}
  .twk-num-lbl{font-weight:500;color:rgba(41,38,27,.6);cursor:ew-resize;
    user-select:none;padding-right:8px}
  .twk-num input{flex:1;min-width:0;height:100%;border:0;background:transparent;
    font:inherit;font-variant-numeric:tabular-nums;text-align:right;padding:0 8px 0 0;
    outline:none;color:inherit;-moz-appearance:textfield}
  .twk-num input::-webkit-inner-spin-button,.twk-num input::-webkit-outer-spin-button{
    -webkit-appearance:none;margin:0}
  .twk-num-unit{padding-right:8px;color:rgba(41,38,27,.45)}

  .twk-btn{appearance:none;height:28px;padding:0 12px;border:0;border-radius:7px;
    background:rgba(0,0,0,.78);color:#fff;font:inherit;font-weight:500;cursor:default}
  .twk-btn:hover{background:rgba(0,0,0,.88)}
  .twk-btn.secondary{background:rgba(0,0,0,.06);color:inherit}
  .twk-btn.secondary:hover{background:rgba(0,0,0,.1)}

  .twk-swatch{appearance:none;-webkit-appearance:none;width:56px;height:24px;
    border:.5px solid rgba(0,0,0,.1);border-radius:6px;padding:0;cursor:default;
    background:transparent;flex-shrink:0}
  .twk-swatch::-webkit-color-swatch-wrapper{padding:0}
  .twk-swatch::-webkit-color-swatch{border:0;border-radius:5.5px}
  .twk-swatch::-moz-color-swatch{border:0;border-radius:5.5px}

  .twk-chips{display:flex;gap:8px}
  .twk-chip{position:relative;appearance:none;flex:1;min-width:0;height:48px;
    padding:0;border:0;border-radius:6px;overflow:hidden;cursor:default;
    box-shadow:0 0 0 .5px rgba(0,0,0,.12),0 1px 2px rgba(0,0,0,.06);
    transition:transform .12s cubic-bezier(.3,.7,.4,1),box-shadow .12s}
  .twk-chip:hover{transform:translateY(-1px);
    box-shadow:0 0 0 .5px rgba(0,0,0,.18),0 4px 10px rgba(0,0,0,.12)}
  .twk-chip[data-on="1"]{box-shadow:0 0 0 1.5px rgba(0,0,0,.85),
    0 2px 6px rgba(0,0,0,.15)}
  .twk-chip>span{position:absolute;top:0;bottom:0;right:0;width:34%;
    display:flex;flex-direction:column;box-shadow:-1px 0 0 rgba(0,0,0,.1)}
  .twk-chip>span>i{flex:1;box-shadow:0 -1px 0 rgba(0,0,0,.1)}
  .twk-chip>span>i:first-child{box-shadow:none}
  .twk-chip svg{position:absolute;top:8px;left:8px;width:12px;height:12px;
    filter:drop-shadow(0 1px 1px rgba(0,0,0,.3))}
`;

// ── useTweaks ───────────────────────────────────────────────────────────────
// Single source of truth for tweak values. setTweak persists via the host
// (__edit_mode_set_keys → host rewrites the EDITMODE block on disk).
function useTweaks(defaults) {
  const [values, setValues] = React.useState(defaults);
  // Accepts either setTweak('key', value) or setTweak({ key: value, ... }) so a
  // useState-style call doesn't write a "[object Object]" key into the persisted
  // JSON block.
  const setTweak = React.useCallback((keyOrEdits, val) => {
    const edits = typeof keyOrEdits === 'object' && keyOrEdits !== null
      ? keyOrEdits : { [keyOrEdits]: val };
    setValues((prev) => ({ ...prev, ...edits }));
    window.parent.postMessage({ type: '__edit_mode_set_keys', edits }, '*');
    // Same-window signal so in-page listeners (deck-stage rail thumbnails)
    // can react — the parent message only reaches the host, not peers.
    window.dispatchEvent(new CustomEvent('tweakchange', { detail: edits }));
  }, []);
  return [values, setTweak];
}

// ── TweaksPanel ─────────────────────────────────────────────────────────────
// Floating shell. Registers the protocol listener BEFORE announcing
// availability — if the announce ran first, the host's activate could land
// before our handler exists and the toolbar toggle would silently no-op.
// The close button posts __edit_mode_dismissed so the host's toolbar toggle
// flips off in lockstep; the host echoes __deactivate_edit_mode back which
// is what actually hides the panel.
function TweaksPanel({ title = 'Tweaks', children }) {
  const [open, setOpen] = React.useState(false);
  const dragRef = React.useRef(null);
  const offsetRef = React.useRef({ x: 16, y: 16 });
  const PAD = 16;

  const clampToViewport = React.useCallback(() => {
    const panel = dragRef.current;
    if (!panel) return;
    const w = panel.offsetWidth, h = panel.offsetHeight;
    const maxRight = Math.max(PAD, window.innerWidth - w - PAD);
    const maxBottom = Math.max(PAD, window.innerHeight - h - PAD);
    offsetRef.current = {
      x: Math.min(maxRight, Math.max(PAD, offsetRef.current.x)),
      y: Math.min(maxBottom, Math.max(PAD, offsetRef.current.y)),
    };
    panel.style.right = offsetRef.current.x + 'px';
    panel.style.bottom = offsetRef.current.y + 'px';
  }, []);

  React.useEffect(() => {
    if (!open) return;
    clampToViewport();
    if (typeof ResizeObserver === 'undefined') {
      window.addEventListener('resize', clampToViewport);
      return () => window.removeEventListener('resize', clampToViewport);
    }
    const ro = new ResizeObserver(clampToViewport);
    ro.observe(document.documentElement);
    return () => ro.disconnect();
  }, [open, clampToViewport]);

  React.useEffect(() => {
    const onMsg = (e) => {
      const t = e?.data?.type;
      if (t === '__activate_edit_mode') setOpen(true);
      else if (t === '__deactivate_edit_mode') setOpen(false);
    };
    window.addEventListener('message', onMsg);
    window.parent.postMessage({ type: '__edit_mode_available' }, '*');
    return () => window.removeEventListener('message', onMsg);
  }, []);

  const dismiss = () => {
    setOpen(false);
    window.parent.postMessage({ type: '__edit_mode_dismissed' }, '*');
  };

  const onDragStart = (e) => {
    const panel = dragRef.current;
    if (!panel) return;
    const r = panel.getBoundingClientRect();
    const sx = e.clientX, sy = e.clientY;
    const startRight = window.innerWidth - r.right;
    const startBottom = window.innerHeight - r.bottom;
    const move = (ev) => {
      offsetRef.current = {
        x: startRight - (ev.clientX - sx),
        y: startBottom - (ev.clientY - sy),
      };
      clampToViewport();
    };
    const up = () => {
      window.removeEventListener('mousemove', move);
      window.removeEventListener('mouseup', up);
    };
    window.addEventListener('mousemove', move);
    window.addEventListener('mouseup', up);
  };

  if (!open) return null;
  return (
    <>
      <style>{__TWEAKS_STYLE}</style>
      <div ref={dragRef} className="twk-panel" data-omelette-chrome=""
           style={{ right: offsetRef.current.x, bottom: offsetRef.current.y }}>
        <div className="twk-hd" onMouseDown={onDragStart}>
          <b>{title}</b>
          <button className="twk-x" aria-label="Close tweaks"
                  onMouseDown={(e) => e.stopPropagation()}
                  onClick={dismiss}>✕</button>
        </div>
        <div className="twk-body">
          {children}
        </div>
      </div>
    </>
  );
}

// ── Layout helpers ──────────────────────────────────────────────────────────

function TweakSection({ label, children }) {
  return (
    <>
      <div className="twk-sect">{label}</div>
      {children}
    </>
  );
}

function TweakRow({ label, value, children, inline = false }) {
  return (
    <div className={inline ? 'twk-row twk-row-h' : 'twk-row'}>
      <div className="twk-lbl">
        <span>{label}</span>
        {value != null && <span className="twk-val">{value}</span>}
      </div>
      {children}
    </div>
  );
}

// ── Controls ────────────────────────────────────────────────────────────────

function TweakSlider({ label, value, min = 0, max = 100, step = 1, unit = '', onChange }) {
  return (
    <TweakRow label={label} value={`${value}${unit}`}>
      <input type="range" className="twk-slider" min={min} max={max} step={step}
             value={value} onChange={(e) => onChange(Number(e.target.value))} />
    </TweakRow>
  );
}

function TweakToggle({ label, value, onChange }) {
  return (
    <div className="twk-row twk-row-h">
      <div className="twk-lbl"><span>{label}</span></div>
      <button type="button" className="twk-toggle" data-on={value ? '1' : '0'}
              role="switch" aria-checked={!!value}
              onClick={() => onChange(!value)}><i /></button>
    </div>
  );
}

function TweakRadio({ label, value, options, onChange }) {
  const trackRef = React.useRef(null);
  const [dragging, setDragging] = React.useState(false);
  // The active value is read by pointer-move handlers attached for the lifetime
  // of a drag — ref it so a stale closure doesn't fire onChange for every move.
  const valueRef = React.useRef(value);
  valueRef.current = value;

  // Segments wrap mid-word once per-segment width runs out. The track is
  // ~248px (280 panel − 28 body pad − 4 seg pad), each button loses 12px
  // to its own padding, and 11.5px system-ui averages ~6.3px/char — so 2
  // options fit ~16 chars each, 3 fit ~10. Past that (or >3 options), fall
  // back to a dropdown rather than wrap.
  const labelLen = (o) => String(typeof o === 'object' ? o.label : o).length;
  const maxLen = options.reduce((m, o) => Math.max(m, labelLen(o)), 0);
  const fitsAsSegments = maxLen <= ({ 2: 16, 3: 10 }[options.length] ?? 0);
  if (!fitsAsSegments) {
    // <select> emits strings — map back to the original option value so the
    // fallback stays type-preserving (numbers, booleans) like the segment path.
    const resolve = (s) => {
      const m = options.find((o) => String(typeof o === 'object' ? o.value : o) === s);
      return m === undefined ? s : typeof m === 'object' ? m.value : m;
    };
    return <TweakSelect label={label} value={value} options={options}
                        onChange={(s) => onChange(resolve(s))} />;
  }
  const opts = options.map((o) => (typeof o === 'object' ? o : { value: o, label: o }));
  const idx = Math.max(0, opts.findIndex((o) => o.value === value));
  const n = opts.length;

  const segAt = (clientX) => {
    const r = trackRef.current.getBoundingClientRect();
    const inner = r.width - 4;
    const i = Math.floor(((clientX - r.left - 2) / inner) * n);
    return opts[Math.max(0, Math.min(n - 1, i))].value;
  };

  const onPointerDown = (e) => {
    setDragging(true);
    const v0 = segAt(e.clientX);
    if (v0 !== valueRef.current) onChange(v0);
    const move = (ev) => {
      if (!trackRef.current) return;
      const v = segAt(ev.clientX);
      if (v !== valueRef.current) onChange(v);
    };
    const up = () => {
      setDragging(false);
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };

  return (
    <TweakRow label={label}>
      <div ref={trackRef} role="radiogroup" onPointerDown={onPointerDown}
           className={dragging ? 'twk-seg dragging' : 'twk-seg'}>
        <div className="twk-seg-thumb"
             style={{ left: `calc(2px + ${idx} * (100% - 4px) / ${n})`,
                      width: `calc((100% - 4px) / ${n})` }} />
        {opts.map((o) => (
          <button key={o.value} type="button" role="radio" aria-checked={o.value === value}>
            {o.label}
          </button>
        ))}
      </div>
    </TweakRow>
  );
}

function TweakSelect({ label, value, options, onChange }) {
  return (
    <TweakRow label={label}>
      <select className="twk-field" value={value} onChange={(e) => onChange(e.target.value)}>
        {options.map((o) => {
          const v = typeof o === 'object' ? o.value : o;
          const l = typeof o === 'object' ? o.label : o;
          return <option key={v} value={v}>{l}</option>;
        })}
      </select>
    </TweakRow>
  );
}

function TweakText({ label, value, placeholder, onChange }) {
  return (
    <TweakRow label={label}>
      <input className="twk-field" type="text" value={value} placeholder={placeholder}
             onChange={(e) => onChange(e.target.value)} />
    </TweakRow>
  );
}

function TweakNumber({ label, value, min, max, step = 1, unit = '', onChange }) {
  const clamp = (n) => {
    if (min != null && n < min) return min;
    if (max != null && n > max) return max;
    return n;
  };
  const startRef = React.useRef({ x: 0, val: 0 });
  const onScrubStart = (e) => {
    e.preventDefault();
    startRef.current = { x: e.clientX, val: value };
    const decimals = (String(step).split('.')[1] || '').length;
    const move = (ev) => {
      const dx = ev.clientX - startRef.current.x;
      const raw = startRef.current.val + dx * step;
      const snapped = Math.round(raw / step) * step;
      onChange(clamp(Number(snapped.toFixed(decimals))));
    };
    const up = () => {
      window.removeEventListener('pointermove', move);
      window.removeEventListener('pointerup', up);
    };
    window.addEventListener('pointermove', move);
    window.addEventListener('pointerup', up);
  };
  return (
    <div className="twk-num">
      <span className="twk-num-lbl" onPointerDown={onScrubStart}>{label}</span>
      <input type="number" value={value} min={min} max={max} step={step}
             onChange={(e) => onChange(clamp(Number(e.target.value)))} />
      {unit && <span className="twk-num-unit">{unit}</span>}
    </div>
  );
}

// Relative-luminance contrast pick — checkmarks drawn over a swatch need to
// read on both #111 and #fafafa without per-option configuration. Hex input
// only (#rgb / #rrggbb); named or rgb()/hsl() colors fall through to "light".
function __twkIsLight(hex) {
  const h = String(hex).replace('#', '');
  const x = h.length === 3 ? h.replace(/./g, (c) => c + c) : h.padEnd(6, '0');
  const n = parseInt(x.slice(0, 6), 16);
  if (Number.isNaN(n)) return true;
  const r = (n >> 16) & 255, g = (n >> 8) & 255, b = n & 255;
  return r * 299 + g * 587 + b * 114 > 148000;
}

const __TwkCheck = ({ light }) => (
  <svg viewBox="0 0 14 14" aria-hidden="true">
    <path d="M3 7.2 5.8 10 11 4.2" fill="none" strokeWidth="2.2"
          strokeLinecap="round" strokeLinejoin="round"
          stroke={light ? 'rgba(0,0,0,.78)' : '#fff'} />
  </svg>
);

// TweakColor — curated color/palette picker. Each option is either a single
// hex string or an array of 1-5 hex strings; the card adapts — a lone color
// renders solid, a palette renders colors[0] as the hero (left ~2/3) with the
// rest stacked in a sharp column on the right. onChange emits the
// option in the shape it was passed (string stays string, array stays array).
// Without options it falls back to the native color input for back-compat.
function TweakColor({ label, value, options, onChange }) {
  if (!options || !options.length) {
    return (
      <div className="twk-row twk-row-h">
        <div className="twk-lbl"><span>{label}</span></div>
        <input type="color" className="twk-swatch" value={value}
               onChange={(e) => onChange(e.target.value)} />
      </div>
    );
  }
  // Native <input type=color> emits lowercase hex per the HTML spec, so
  // compare case-insensitively. String() guards JSON.stringify(undefined),
  // which returns the primitive undefined (no .toLowerCase).
  const key = (o) => String(JSON.stringify(o)).toLowerCase();
  const cur = key(value);
  return (
    <TweakRow label={label}>
      <div className="twk-chips" role="radiogroup">
        {options.map((o, i) => {
          const colors = Array.isArray(o) ? o : [o];
          const [hero, ...rest] = colors;
          const sup = rest.slice(0, 4);
          const on = key(o) === cur;
          return (
            <button key={i} type="button" className="twk-chip" role="radio"
                    aria-checked={on} data-on={on ? '1' : '0'}
                    aria-label={colors.join(', ')} title={colors.join(' · ')}
                    style={{ background: hero }}
                    onClick={() => onChange(o)}>
              {sup.length > 0 && (
                <span>
                  {sup.map((c, j) => <i key={j} style={{ background: c }} />)}
                </span>
              )}
              {on && <__TwkCheck light={__twkIsLight(hero)} />}
            </button>
          );
        })}
      </div>
    </TweakRow>
  );
}

function TweakButton({ label, onClick, secondary = false }) {
  return (
    <button type="button" className={secondary ? 'twk-btn secondary' : 'twk-btn'}
            onClick={onClick}>{label}</button>
  );
}

Object.assign(window, {
  useTweaks, TweaksPanel, TweakSection, TweakRow,
  TweakSlider, TweakToggle, TweakRadio, TweakSelect,
  TweakText, TweakNumber, TweakColor, TweakButton,
});


/* ===== ASSET 1 : 8648ec71-d243-4097-9126-b7b0772e0c43 ===== */
// Small SVG icon set — line icons matching the PROLOOK admin UI.
const Icon = ({ d, size = 18, stroke = 1.6, fill = "none", style }) => (
  <svg width={size} height={size} viewBox="0 0 24 24" fill={fill} stroke="currentColor" strokeWidth={stroke} strokeLinecap="round" strokeLinejoin="round" style={style}>
    {Array.isArray(d) ? d.map((p, i) => <path key={i} d={p} />) : <path d={d} />}
  </svg>
);

const IconUser = (p) => <Icon {...p} d={["M20 21a8 8 0 1 0-16 0", "M12 13a5 5 0 1 0 0-10 5 5 0 0 0 0 10z"]} />;
const IconUserSolid = (p) => <Icon {...p} fill="currentColor" stroke="none" d="M12 12c2.7 0 4.9-2.2 4.9-4.9S14.7 2.2 12 2.2 7.1 4.4 7.1 7.1 9.3 12 12 12zm0 2.5c-3.3 0-9.9 1.7-9.9 4.9V22h19.8v-2.5c0-3.3-6.6-5-9.9-5z" />;
const IconPin = (p) => <Icon {...p} d={["M20 10c0 6-8 12-8 12s-8-6-8-12a8 8 0 0 1 16 0z", "M12 12a2 2 0 1 0 0-4 2 2 0 0 0 0 4z"]} />;
const IconImage = (p) => <Icon {...p} d={["M3 5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v14a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5z","M3 16l5-5 4 4 3-3 6 6","M9 10a1.5 1.5 0 1 0 0-3 1.5 1.5 0 0 0 0 3z"]} />;
const IconBriefcase = (p) => <Icon {...p} d={["M3 8a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v10a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V8z","M9 6V5a2 2 0 0 1 2-2h2a2 2 0 0 1 2 2v1","M3 12h18"]} />;
const IconCart = (p) => <Icon {...p} d={["M3 4h2l2.4 11.2a2 2 0 0 0 2 1.6h7.9a2 2 0 0 0 2-1.5L21 8H6","M10 20a1 1 0 1 0 0-2 1 1 0 0 0 0 2z","M17 20a1 1 0 1 0 0-2 1 1 0 0 0 0 2z"]} />;
const IconBag = (p) => <Icon {...p} d={["M6 7a6 6 0 1 1 12 0","M4 7h16l-1.4 12.2a2 2 0 0 1-2 1.8H7.4a2 2 0 0 1-2-1.8L4 7z"]} />;
const IconStore = (p) => <Icon {...p} d={["M3 9l1.5-5h15L21 9","M4 9v10a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1V9","M9 20v-5h6v5","M3 9h18"]} />;
const IconDollar = (p) => <Icon {...p} d={["M12 1v22","M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"]} />;
const IconSearch = (p) => <Icon {...p} d={["M11 4a7 7 0 1 0 0 14 7 7 0 0 0 0-14z","M16 16l4 4"]} />;
const IconSliders = (p) => <Icon {...p} d={["M4 6h10","M18 6h2","M4 12h2","M10 12h10","M4 18h12","M16 18h4","M14 4v4","M8 10v4","M14 16v4"]} />;
const IconChevron = (p) => <Icon {...p} d="M6 9l6 6 6-6" />;
const IconChevronRight = (p) => <Icon {...p} d="M9 6l6 6-6 6" />;
const IconColumns = (p) => <Icon {...p} d={["M4 5a1 1 0 0 1 1-1h14a1 1 0 0 1 1 1v14a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V5z","M10 4v16","M15 4v16"]} />;
const IconPlus = (p) => <Icon {...p} d={["M12 5v14","M5 12h14"]} />;
const IconX = (p) => <Icon {...p} d={["M5 5l14 14","M19 5L5 19"]} />;
const IconCheck = (p) => <Icon {...p} d="M4 12l5 5L20 6" />;
const IconExternal = (p) => <Icon {...p} d={["M14 4h6v6","M20 4l-9 9","M19 14v5a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1V6a1 1 0 0 1 1-1h5"]} />;
const IconCircleCheck = (p) => <Icon {...p} d={["M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18z","M8.5 12l2.5 2.5L16 9"]} />;
const IconClipboard = (p) => <Icon {...p} d={["M9 4h6a1 1 0 0 1 1 1v1H8V5a1 1 0 0 1 1-1z","M8 6H6a1 1 0 0 0-1 1v13a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V7a1 1 0 0 0-1-1h-2"]} />;
const IconEdit = (p) => <Icon {...p} d={["M4 20h4l10.5-10.5a2.1 2.1 0 0 0-3-3L5 17v3z","M14 6l4 4"]} />;
const IconUpload = (p) => <Icon {...p} d={["M12 16V4","M6 10l6-6 6 6","M4 20h16"]} />;
const IconTrash = (p) => <Icon {...p} d={["M3 6h18","M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2","M6 6l1 14a2 2 0 0 0 2 2h6a2 2 0 0 0 2-2l1-14"]} />;
const IconAlert = (p) => <Icon {...p} d={["M12 2L1 22h22L12 2z","M12 9v5","M12 18h.01"]} />;
const IconShield = (p) => <Icon {...p} d={["M12 2l9 4v6c0 5-3.6 9.4-9 10-5.4-.6-9-5-9-10V6l9-4z","M9 12l2 2 4-4"]} />;
const IconLock = (p) => <Icon {...p} d={["M5 11h14a1 1 0 0 1 1 1v8a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1v-8a1 1 0 0 1 1-1z","M8 11V7a4 4 0 0 1 8 0v4"]} />;
const IconUnlock = (p) => <Icon {...p} d={["M5 11h14a1 1 0 0 1 1 1v8a1 1 0 0 1-1 1H5a1 1 0 0 1-1-1v-8a1 1 0 0 1 1-1z","M8 11V7a4 4 0 0 1 7.5-1.9"]} />;
const IconArrowRight = (p) => <Icon {...p} d={["M4 12h16","M14 6l6 6-6 6"]} />;
const IconClock = (p) => <Icon {...p} d={["M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18z","M12 7v5l3 2"]} />;
const IconCalendar = (p) => <Icon {...p} d={["M4 6a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v13a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V6z","M4 9h16","M8 3v4","M16 3v4"]} />;
const IconTrophy = (p) => <Icon {...p} d={["M6 4h12v4a6 6 0 0 1-12 0V4z","M6 6H3.5a2.5 2.5 0 0 0 4 2","M18 6h2.5a2.5 2.5 0 0 1-4 2","M9 14.5V18","M15 14.5V18","M8 21h8","M9 21v-1.5a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1V21"]} />;
const IconUsers = (p) => <Icon {...p} d={["M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2","M9 11a4 4 0 1 0 0-8 4 4 0 0 0 0 8z","M22 21v-2a4 4 0 0 0-3-3.87","M16 3.13a4 4 0 0 1 0 7.75"]} />;
const IconLayers = (p) => <Icon {...p} d={["M12 2l9 5-9 5-9-5 9-5z","M3 12l9 5 9-5","M3 17l9 5 9-5"]} />;
const IconGrid = (p) => <Icon {...p} d={["M4 4h7v7H4z","M13 4h7v7h-7z","M4 13h7v7H4z","M13 13h7v7h-7z"]} />;
const IconGroupAdd = (p) => <Icon {...p} d={["M3.5 3.5h6v6h-6z","M14.5 3.5h6v6h-6z","M3.5 14.5h6v6h-6z","M17.5 14.5v6","M14.5 17.5h6"]} />;
const IconChevronLeft = (p) => <Icon {...p} d="M15 18l-6-6 6-6" />;
const IconSettings = (p) => <Icon {...p} d={["M12 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6z","M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 1 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09a1.65 1.65 0 0 0-1-1.51 1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 1 1-2.83-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09a1.65 1.65 0 0 0 1.51-1 1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 1 1 2.83-2.83l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 1 1 2.83 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"]} />;
const IconShare = (p) => <Icon {...p} d={["M18 8a3 3 0 1 0 0-6 3 3 0 0 0 0 6z","M6 15a3 3 0 1 0 0-6 3 3 0 0 0 0 6z","M18 22a3 3 0 1 0 0-6 3 3 0 0 0 0 6z","M8.6 13.5l6.8 3.9","M15.4 6.6L8.6 10.5"]} />;
const IconLink = (p) => <Icon {...p} d={["M10 13a5 5 0 0 0 7.07 0l3-3a5 5 0 0 0-7.07-7.07l-1.5 1.5","M14 11a5 5 0 0 0-7.07 0l-3 3a5 5 0 0 0 7.07 7.07l1.5-1.5"]} />;
const IconMail = (p) => <Icon {...p} d={["M3 6a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V6z","M3 7l9 6 9-6"]} />;
const IconPhone = (p) => <Icon {...p} d={["M4 4h4l2 5-2.5 1.5a11 11 0 0 0 5 5L14 13l5 2v4a2 2 0 0 1-2.2 2A16 16 0 0 1 2.2 6.2 2 2 0 0 1 4 4z"]} />;
const IconDownload = (p) => <Icon {...p} d={["M12 4v12","M6 10l6 6 6-6","M4 20h16"]} />;
const IconPaperclip = (p) => <Icon {...p} d={["M21.44 11.05l-8.49 8.49a5.5 5.5 0 0 1-7.78-7.78l8.49-8.49a3.67 3.67 0 0 1 5.19 5.19l-8.5 8.49a1.83 1.83 0 0 1-2.59-2.59l7.84-7.84"]} />;
// Folder (Leagues) + nested sub-folder (League Groups)
const IconFolder = (p) => <Icon {...p} d={["M3 7a2 2 0 0 1 2-2h4l2 2.2h8a2 2 0 0 1 2 2V18a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V7z"]} />;
const IconSubfolder = (p) => <Icon {...p} d={["M5 8a1.6 1.6 0 0 1 1.6-1.6h3.2l1.6 1.7h6.4A1.6 1.6 0 0 1 19.4 9.4V17a1.6 1.6 0 0 1-1.6 1.6H6.6A1.6 1.6 0 0 1 5 17V8z","M9 19.2h9.4a1.6 1.6 0 0 0 1.6-1.6V11"]} />;
// ── Workspace side-nav + toolbar icons ──
const IconInfo = (p) => <Icon {...p} d={["M12 21a9 9 0 1 0 0-18 9 9 0 0 0 0 18z","M12 11v5","M12 8h.01"]} />;
// Bar-graph-in-box — Reports (figma bar-graph-box, Size=Large Style=Outline)
const IconBarGraphBox = (p) => <Icon {...p} d={["M6 3h12a3 3 0 0 1 3 3v12a3 3 0 0 1-3 3H6a3 3 0 0 1-3-3V6a3 3 0 0 1 3-3z","M7.5 16.5v-2.25","M10.5 16.5V12","M13.5 16.5V9.75","M16.5 16.5v-9"]} />;
const IconReceipt = (p) => <Icon {...p} d={["M6 3h12a1 1 0 0 1 1 1v17l-3-2-2 2-2-2-2 2-2-2-3 2V4a1 1 0 0 1 1-1z","M9 8h6","M9 12h6"]} />;
const IconShirt = (p) => <Icon {...p} d={["M8 3l4 3 4-3 4.5 3.2-2.3 4-2.2-1.1V21H8V9.1L5.8 10.2 3.5 6.2 8 3z"]} />;
const IconList = (p) => <Icon {...p} d={["M8 6h13","M8 12h13","M8 18h13","M3.5 6h.01","M3.5 12h.01","M3.5 18h.01"]} />;
const IconFilter = (p) => <Icon {...p} d={["M3 5h18l-7 8v6l-4-2v-4L3 5z"]} />;
const IconSort = (p) => <Icon {...p} d={["M7 4v16","M4 8l3-4 3 4","M17 20V4","M14 16l3 4 3-4"]} />;
const IconTrendUp = (p) => <Icon {...p} d={["M3 17l6-6 4 4 8-8","M15 7h6v6"]} />;
const IconTrendDown = (p) => <Icon {...p} d={["M3 7l6 6 4-4 8 8","M15 17h6v-6"]} />;
const IconWallet = (p) => <Icon {...p} d={["M3 7a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v0H5a2 2 0 0 0-2 2z","M3 9a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2v9a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V9z","M16 13h.01"]} />;

// PROLOOK logo placeholder — angled red "P" badge.
const ProlookLogo = ({ size = 32 }) => (
  <svg width={size} height={size * 0.85} viewBox="0 0 40 34" aria-label="PROLOOK">
    <path d="M3 2 H22 A12 12 0 0 1 22 22 H14 V32 H3 Z" fill="#e1251b"/>
    <path d="M13 9 H21 A3 3 0 0 1 21 15 H13 Z" fill="#ffffff"/>
    <text x="2" y="33" fontFamily="Inter, sans-serif" fontSize="5.5" fontWeight="800" letterSpacing="0.5" fill="#0e0e10">PROLOOK</text>
  </svg>
);

// ─────────── Agnostic Customizer branding ───────────
// Team Stores is brand-agnostic: whichever Customizer the rep signed in from
// brands the platform. The landing page hands the choice off via ?brand=<id>.
// PROLOOK is the default when no brand is specified. Non-PROLOOK brands render
// as text wordmark placeholders — swap in real logo assets when available.
const CUSTOMIZER_BRANDS = {
  prolook: { name: "PROLOOK", logoInk: "assets/prolook-mark-ink.png", logoBlack: "assets/prolook-mark-black.png", logoWhite: "assets/prolook-mark.png" },
  alli: { name: "Alli Sports", wordmark: "ALLI SPORTS" },
  riddell: { name: "Riddell", wordmark: "Riddell" },
  "game-one": { name: "Game One", wordmark: "GAME ONE" },
  varsity: { name: "Varsity", wordmark: "VARSITY" },
  squadlocker: { name: "SquadLocker", wordmark: "SquadLocker" },
  "bsn-sports": { name: "BSN Sports", wordmark: "BSN SPORTS" },
  augusta: { name: "Augusta", wordmark: "AUGUSTA" },
};
function getActiveBrand() {
  try {
    const id = new URLSearchParams(window.location.search).get("brand");
    return CUSTOMIZER_BRANDS[id] || CUSTOMIZER_BRANDS.prolook;
  } catch (e) { return CUSTOMIZER_BRANDS.prolook; }
}
// Team Stores is a brand-agnostic platform: the header always shows the neutral
// QX platform mark rather than the signed-in Customizer's brand.
// variant: "ink" (dark on light) | "black" | "white" (on dark)
function BrandMark({ variant = "ink", height = 38 }) {
  return <img src="assets/qx-logo.png" alt="Team Stores" style={{ height, width: "auto", display: "block" }} />;
}

// Small team-emblem placeholders (used in dashboard tab).
const TeamCrest = ({ kind = "shield", color = "#e1251b" }) => {
  if (kind === "shield") return (
    <svg width="22" height="22" viewBox="0 0 24 24"><path d="M12 2l9 3v7c0 5-4 8-9 10-5-2-9-5-9-10V5l9-3z" fill={color}/><circle cx="12" cy="12" r="3" fill="#fff"/></svg>
  );
  if (kind === "ball") return (
    <svg width="22" height="22" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" fill={color}/><path d="M5 8 Q12 14 19 8 M5 16 Q12 10 19 16" stroke="#fff" strokeWidth="1.4" fill="none"/></svg>
  );
  return null;
};

Object.assign(window, {
  Icon, IconUser, IconUserSolid, IconPin, IconImage, IconBriefcase, IconCart, IconBag,
  IconStore, IconDollar, IconSearch, IconSliders, IconChevron, IconChevronRight,
  IconPlus, IconX, IconCheck, IconExternal, IconCircleCheck, IconClipboard, IconEdit, IconUpload, IconTrash, IconAlert, IconShield,
  IconLock, IconUnlock, IconArrowRight, IconClock,
  IconTrophy, IconUsers, IconLayers, IconGrid, IconGroupAdd, IconChevronLeft, IconSettings,
  IconShare, IconLink, IconMail, IconPhone, IconDownload, IconPaperclip, IconFolder, IconSubfolder,
  IconInfo, IconReceipt, IconShirt, IconList, IconFilter, IconSort, IconTrendUp, IconTrendDown, IconWallet,
  ProlookLogo, TeamCrest, BrandMark, getActiveBrand,
});


/* ===== ASSET 2 : 34c54ddf-5248-4773-ad63-30328e38b01c ===== */
// Shared UI primitives — form fields, buttons, modal scaffolding.
const { useState, useRef, useEffect, useId, useCallback } = React;

// ─────────── Button ───────────
function Button({ variant = "primary", size = "md", children, disabled, onClick, type = "button", style, ...rest }) {
  const [pressed, setPressed] = useState(false);
  const base = {
    display: "inline-flex", alignItems: "center", justifyContent: "center",
    gap: 8, fontWeight: 700, letterSpacing: ".01em",
    border: "1px solid transparent", borderRadius: 4,
    padding: size === "sm" ? "8px 14px" : size === "lg" ? "14px 28px" : "11px 22px",
    fontSize: size === "sm" ? 12 : 13,
    transition: "background .12s ease, border-color .12s ease, color .12s ease, box-shadow .12s ease, transform .1s ease",
    cursor: disabled ? "not-allowed" : "pointer",
    whiteSpace: "nowrap",
    transform: (pressed && !disabled) ? "scale(.972)" : "scale(1)",
  };
  const styles = {
    primary: { background: disabled ? "#d4d4d8" : "#0e0e10", color: "#fff", borderColor: disabled ? "#d4d4d8" : "#0e0e10" },
    secondary: { background: "#fff", color: "#0e0e10", borderColor: "#0e0e10" },
    ghost: { background: "transparent", color: disabled ? "#9a9aa1" : "#0e0e10", borderColor: "transparent" },
    danger: { background: "#fff", color: "#b81d14", borderColor: "#b81d14" },
  }[variant];
  return (
    <button type={type} disabled={disabled} onClick={onClick}
      onMouseDown={() => !disabled && setPressed(true)}
      onMouseUp={() => setPressed(false)}
      onMouseLeave={() => setPressed(false)}
      style={{ ...base, ...styles, ...style }} {...rest}>
      {children}
    </button>
  );
}

// ─────────── Floating-label text input ───────────
function TextField({ label, value, onChange, error, prefix, suffix, type = "text", placeholder, autoFocus, name, onBlur, readOnly, style }) {
  const id = useId();
  const [focus, setFocus] = useState(false);
  const filled = value != null && String(value).length > 0;
  const float = focus || filled || placeholder;
  return (
    <div style={{ position: "relative", flex: 1, ...style }}>
      <div style={{
        position: "relative",
        border: `1px solid ${error ? "#b81d14" : focus ? "#0e0e10" : "#d4d4d8"}`,
        borderRadius: 4,
        background: readOnly ? "#fafafb" : "#fff",
        transition: "border-color .15s",
      }}>
        {prefix && <div style={{ position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)" }}>{prefix}</div>}
        <label htmlFor={id} style={{
          position: "absolute",
          left: prefix ? 56 : 12,
          top: float ? -7 : 14,
          fontSize: float ? 11 : 13,
          color: error ? "#b81d14" : focus ? "#0e0e10" : "#6b6b72",
          background: "#fff", padding: float ? "0 4px" : 0,
          pointerEvents: "none",
          transition: "all .12s ease",
          fontWeight: float ? 500 : 400,
        }}>{label}</label>
        <input
          id={id}
          type={type}
          name={name}
          value={value || ""}
          placeholder={focus ? placeholder : ""}
          autoFocus={autoFocus}
          readOnly={readOnly}
          onChange={(e) => onChange && onChange(e.target.value)}
          onFocus={() => setFocus(true)}
          onBlur={(e) => { setFocus(false); onBlur && onBlur(e); }}
          style={{
            width: "100%", border: 0, outline: 0, background: "transparent",
            padding: prefix ? "14px 12px 14px 56px" : suffix ? "14px 40px 14px 12px" : "14px 12px",
            fontSize: 14, color: "#0e0e10",
          }}
        />
        {suffix && <div style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)" }}>{suffix}</div>}
      </div>
      {error && <div style={{ color: "#b81d14", fontSize: 11, marginTop: 4, marginLeft: 2 }}>{error}</div>}
    </div>
  );
}

// ─────────── Select / dropdown ───────────
function SelectField({ label, value, onChange, options, error, placeholder, style }) {
  const [open, setOpen] = useState(false);
  const [focus, setFocus] = useState(false);
  const ref = useRef();
  useEffect(() => {
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const filled = !!value;
  const float = focus || filled || open;
  const selected = options?.find((o) => (o.value || o) === value);
  return (
    <div ref={ref} style={{ position: "relative", flex: 1, ...style }}>
      <div
        tabIndex={0}
        onFocus={() => setFocus(true)}
        onBlur={() => setFocus(false)}
        onClick={() => setOpen((v) => !v)}
        style={{
          position: "relative",
          border: `1px solid ${error ? "#b81d14" : open || focus ? "#0e0e10" : "#d4d4d8"}`,
          borderRadius: 4, background: "#fff",
          padding: "16px 40px 16px 12px",
          cursor: "pointer", userSelect: "none", minHeight: 48,
          fontSize: 14,
        }}>
        <label style={{
          position: "absolute", left: 12, top: float ? -7 : 14,
          fontSize: float ? 11 : 13,
          color: error ? "#b81d14" : open || focus ? "#0e0e10" : "#6b6b72",
          background: "#fff", padding: float ? "0 4px" : 0,
          pointerEvents: "none", transition: "all .12s ease",
          fontWeight: float ? 500 : 400,
        }}>{label}</label>
        <div style={{ color: filled ? "#0e0e10" : "transparent" }}>{selected?.label || selected?.toString() || "—"}</div>
        <IconChevron size={18} style={{ position: "absolute", right: 12, top: "50%", transform: `translateY(-50%) rotate(${open ? 180 : 0}deg)`, transition: "transform .15s", color: "#6b6b72" }} />
      </div>
      {open && (
        <div style={{
          position: "absolute", top: "calc(100% + 4px)", left: 0, right: 0,
          background: "#fff", border: "1px solid #e5e5e9", borderRadius: 6,
          boxShadow: "0 8px 24px rgba(20,20,30,.10)", zIndex: 30,
          maxHeight: 280, overflowY: "auto",
        }}>
          {options.map((o) => {
            const v = o.value || o;
            const l = o.label || o;
            const active = v === value;
            return (
              <div key={v}
                onMouseDown={(e) => { e.preventDefault(); onChange(v); setOpen(false); }}
                style={{
                  padding: "12px 16px", cursor: "pointer", fontSize: 14,
                  background: active ? "#f7f7f8" : "transparent",
                  display: "flex", alignItems: "center", justifyContent: "space-between",
                }}
                onMouseEnter={(e) => { if (!active) e.currentTarget.style.background = "#fafafb"; }}
                onMouseLeave={(e) => { if (!active) e.currentTarget.style.background = "transparent"; }}>
                <span>{l}</span>
                {active && <IconCheck size={16} />}
              </div>
            );
          })}
        </div>
      )}
      {error && <div style={{ color: "#b81d14", fontSize: 11, marginTop: 4, marginLeft: 2 }}>{error}</div>}
    </div>
  );
}

// ─────────── Phone field with country prefix ───────────
function PhoneField({ value, onChange, error }) {
  return (
    <div style={{ position: "relative", flex: 1, display: "flex", border: `1px solid ${error ? "#b81d14" : "#d4d4d8"}`, borderRadius: 4, background: "#fff", overflow: "hidden" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 8, padding: "0 12px", borderRight: "1px solid #e5e5e9", background: "#fafafb", cursor: "pointer" }}>
        <span style={{ width: 24, height: 16, background: "linear-gradient(to bottom, #b22 33%, #fff 33% 66%, #2a4 66%)", borderRadius: 2, display: "inline-block", position: "relative", overflow: "hidden" }}>
          <span style={{ position: "absolute", inset: 0, background: "linear-gradient(to bottom, #c8102e 33%, #fff 33%, #fff 66%, #c8102e 66%)" }} />
          <span style={{ position: "absolute", inset: 0, width: 12, background: "#3c3b6e" }} />
        </span>
        <span style={{ fontSize: 13, color: "#0e0e10" }}>+1</span>
        <IconChevron size={14} style={{ color: "#6b6b72" }} />
      </div>
      <input
        type="tel"
        placeholder="Phone Number"
        value={value || ""}
        onChange={(e) => onChange(formatPhone(e.target.value))}
        style={{ flex: 1, border: 0, outline: 0, background: "transparent", padding: "16px 12px", fontSize: 14 }}
      />
    </div>
  );
}

// ─────────── Checkbox ───────────
function Checkbox({ checked, onChange, label, size = 16, style }) {
  return (
    <label style={{ display: "inline-flex", alignItems: "flex-start", gap: 12, cursor: "pointer", ...style }}>
      <span
        onClick={(e) => { e.preventDefault(); onChange(!checked); }}
        style={{
          width: size, height: size, flexShrink: 0,
          border: `1.5px solid ${checked ? "#0e0e10" : "#9a9aa1"}`,
          background: checked ? "#0e0e10" : "#fff",
          borderRadius: 3, display: "inline-flex", alignItems: "center", justifyContent: "center",
          transition: "all .12s ease", marginTop: 1,
        }}>
        {checked && <IconCheck size={size - 4} style={{ color: "#fff", strokeWidth: 3 }} />}
      </span>
      {label && <span style={{ fontSize: 13, color: "#0e0e10", lineHeight: 1.4 }}>{label}</span>}
    </label>
  );
}

// ─────────── Modal scaffold ───────────
function Modal({ open, onClose, children, width = 480 }) {
  const [visible, setVisible] = useState(open);
  const [closing, setClosing] = useState(false);
  useEffect(() => {
    if (open) { setVisible(true); setClosing(false); }
    else if (visible) {
      setClosing(true);
      const t = setTimeout(() => { setVisible(false); setClosing(false); }, 160);
      return () => clearTimeout(t);
    }
  }, [open]);
  useEffect(() => {
    if (!visible) return;
    const h = (e) => { if (e.key === "Escape") onClose(); };
    window.addEventListener("keydown", h);
    document.body.style.overflow = "hidden";
    return () => { window.removeEventListener("keydown", h); document.body.style.overflow = ""; };
  }, [visible, onClose]);
  if (!visible) return null;
  return (
    <div style={{
      position: "fixed", inset: 0, zIndex: 100,
      background: "rgba(20,20,30,.45)", backdropFilter: "blur(2px)",
      display: "flex", alignItems: "center", justifyContent: "center",
      animation: closing ? "fadeOut .16s ease forwards" : "fadeIn .18s ease-out",
    }} onClick={onClose}>
      <div onClick={(e) => e.stopPropagation()} style={{
        background: "#fff", borderRadius: 8, width, maxWidth: "calc(100vw - 32px)",
        boxShadow: "0 24px 48px rgba(20,20,30,.20)",
        animation: closing ? "popOut .16s cubic-bezier(.4,0,1,1) forwards" : "popIn .2s cubic-bezier(.16,.9,.3,1)",
      }}>
        {children}
      </div>
      <style>{`
        @keyframes fadeIn  { from{opacity:0}                                       to{opacity:1} }
        @keyframes fadeOut { from{opacity:1}                                       to{opacity:0} }
        @keyframes popIn   { from{opacity:0;transform:scale(.95) translateY(8px)} to{opacity:1;transform:scale(1) translateY(0)} }
        @keyframes popOut  { from{opacity:1;transform:scale(1) translateY(0)}     to{opacity:0;transform:scale(.96) translateY(4px)} }
        @media (prefers-reduced-motion:reduce){*,*::before,*::after{animation-duration:1ms!important;transition-duration:1ms!important}}
      `}</style>
    </div>
  );
}

// ─────────── Toast ───────────
function Toast({ message, onDone }) {
  useEffect(() => {
    if (!message) return;
    const t = setTimeout(() => onDone(), 2400);
    return () => clearTimeout(t);
  }, [message, onDone]);
  if (!message) return null;
  return (
    <div style={{
      position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)",
      background: "#0e0e10", color: "#fff", padding: "12px 20px", borderRadius: 6,
      fontSize: 13, fontWeight: 500, zIndex: 200,
      boxShadow: "0 8px 24px rgba(20,20,30,.30)",
      display: "flex", alignItems: "center", gap: 12,
      animation: "toastIn .25s ease",
    }}>
      <IconCheck size={16} style={{ color: "#3acc7a" }} />
      {message}
      <style>{`@keyframes toastIn{from{opacity:0;transform:translate(-50%, 8px)}to{opacity:1;transform:translate(-50%, 0)}}`}</style>
    </div>
  );
}

Object.assign(window, { Button, TextField, SelectField, PhoneField, Checkbox, Modal, Toast });


/* ===== ASSET 3 : 6f3bef04-d1f6-4130-892e-526713997088 ===== */
// Global navigation shell — top utility strip + persistent three-tab header.
// This header stays mounted across Overview / Stores / Leagues and the
// contextual "Manage Store" workspace.

// ─────────── Top utility strip ───────────
function UtilityBar() {
  const links = ["Cobrowse", "Catalog", "Contact"];
  return (
    <div style={{
      background: "#ededef", borderBottom: "1px solid #e2e2e6",
      height: 36, display: "flex", alignItems: "center", justifyContent: "flex-end",
      padding: "0 32px",
    }}>
      <nav style={{ display: "flex", alignItems: "center", gap: 0 }}>
        {links.map((l, i) => (
          <React.Fragment key={l}>
            {i > 0 && <span style={{ width: 1, height: 12, background: "#c8c8ce", margin: "0 16px" }} />}
            <a href="#" onClick={(e) => e.preventDefault()} style={{
              textDecoration: "none", color: "#3a3a40", fontSize: 13.5, fontWeight: 500,
              transition: "color .12s",
            }}
              onMouseEnter={(e) => (e.currentTarget.style.color = "#0e0e10")}
              onMouseLeave={(e) => (e.currentTarget.style.color = "#3a3a40")}>
              {l}
            </a>
          </React.Fragment>
        ))}
      </nav>
    </div>
  );
}

// ─────────── prolook.com primary header: logo · storefront nav · account/cart ───────────
// Persistent across the whole platform EXCEPT inside the store editor.
// `active` is one of "discover" | "build-mens" | "build-womens" | "team-stores" | "none".
function GlobalHeader({ active = "none", onNavigate, cartCount = 3, hideAccountCart = false }) {
  const tabs = [
    { id: "discover", label: "DISCOVER" },
    { id: "build-mens", label: "BUILD MEN'S" },
    { id: "build-womens", label: "BUILD WOMEN'S" },
    { id: "team-stores", label: "TEAM STORES" },
  ];
  return (
    <header style={{
      height: 80, background: "#fff", borderBottom: "1px solid #e8e8ec",
      display: "flex", alignItems: "center", justifyContent: "space-between",
      padding: "0 32px", position: "sticky", top: 0, zIndex: 50,
    }}>
      <div style={{ display: "flex", alignItems: "center", gap: 52 }}>
        <a href="#" onClick={(e) => { e.preventDefault(); onNavigate && onNavigate("discover"); }}
          title="PROLOOK home"
          style={{ display: "inline-flex", alignItems: "center", textDecoration: "none" }}>
          <BrandMark variant="black" height={34} />
        </a>
        <nav style={{ display: "flex", alignItems: "stretch", gap: 36, height: 80 }}>
          {tabs.map((t) => {
            const on = active === t.id;
            return (
              <a key={t.id} href="#" onClick={(e) => { e.preventDefault(); onNavigate && onNavigate(t.id); }}
                style={{
                  position: "relative",
                  display: "inline-flex", alignItems: "center", textDecoration: "none",
                  color: on ? "#0e0e10" : "#1a1a1d",
                  fontWeight: 800, fontSize: 15, letterSpacing: ".035em", whiteSpace: "nowrap",
                  transition: "opacity .12s",
                }}
                onMouseEnter={(e) => { if (!on) e.currentTarget.style.opacity = ".6"; }}
                onMouseLeave={(e) => { if (!on) e.currentTarget.style.opacity = "1"; }}>
                <span>{t.label}</span>
                {on && (
                  <span aria-hidden="true" style={{
                    position: "absolute", left: 0, right: 0, bottom: 24, height: 2.5,
                    background: "#0e0e10",
                  }} />
                )}
              </a>
            );
          })}
        </nav>
      </div>


    </header>
  );
}

// Left-aligned section nav, matching the Team Manager Portal treatment: the
// active label reads red and its 3px rule sits flush with the header’s bottom
// border (no floating pill).
function NavTabs({ tabs, active, onChange }) {
  return (
    <nav style={{ display: "flex", alignItems: "stretch", gap: 4, marginLeft: 44, alignSelf: "stretch" }}>
      {tabs.map((t) => {
        const on = active === t.id;
        return (
          <a key={t.id} href="#"
            onClick={(e) => { e.preventDefault(); onChange && onChange(t.id); }}
            style={{
              display: "inline-flex", alignItems: "center", padding: "0 18px",
              textDecoration: "none", color: on ? "#e1251b" : "#3f3f46",
              fontSize: 15, fontWeight: 800, letterSpacing: ".08em", whiteSpace: "nowrap",
              borderBottom: "3px solid " + (on ? "#e1251b" : "transparent"),
              transition: "color .16s, border-color .16s",
            }}
            onMouseEnter={(e) => { if (!on) e.currentTarget.style.color = "#0e0e10"; }}
            onMouseLeave={(e) => { if (!on) e.currentTarget.style.color = "#3f3f46"; }}>
            <span>{t.label}</span>
          </a>
        );
      })}
    </nav>
  );
}

// ─────────── Header account menu ───────────
const IconLogout = (p) => <Icon {...p} d={["M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4","M16 17l5-5-5-5","M21 12H9"]} />;

// Customizer row glyph — the rep's PRIMARY brand logo, boxed to the same 19px
// optical square as the other item icons. Neutral monogram tile as fallback
// when no logo asset loads.
function CustomizerBrandGlyph() {
  const [failed, setFailed] = React.useState(false);
  return (
    <span style={{ width: 19, height: 19, display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
      {failed ? (
        <span style={{ width: 19, height: 19, borderRadius: 4, background: "#3f3f46", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 10.5, fontWeight: 800 }}>C</span>
      ) : (
        <img src="assets/prolook-mark-ink.png" alt="" onError={() => setFailed(true)} style={{ maxWidth: 19, maxHeight: 17, objectFit: "contain", display: "block" }} />
      )}
    </span>
  );
}

// Account dropdown — one menu, two ways in: CLICK toggles it, HOVER opens it.
// Closes on click-outside, Escape, or focus leaving. Never navigates on open.
function AccountMenu({ name, onSelect }) {
  const [open, setOpen] = React.useState(false);
  const [vis, setVis] = React.useState(false); // drives the open/close transition
  const rootRef = React.useRef(null);
  const btnRef = React.useRef(null);
  const timerRef = React.useRef(null);
  const openMenu = () => { clearTimeout(timerRef.current); setOpen(true); requestAnimationFrame(() => requestAnimationFrame(() => setVis(true))); };
  const closeMenu = () => { clearTimeout(timerRef.current); setVis(false); timerRef.current = setTimeout(() => setOpen(false), 150); };
  React.useEffect(() => {
    if (!open) return;
    const onDoc = (e) => { if (rootRef.current && !rootRef.current.contains(e.target)) closeMenu(); };
    const onKey = (e) => { if (e.key === "Escape") { closeMenu(); btnRef.current && btnRef.current.focus(); } };
    document.addEventListener("mousedown", onDoc);
    window.addEventListener("keydown", onKey);
    return () => { document.removeEventListener("mousedown", onDoc); window.removeEventListener("keydown", onKey); };
  }, [open]);
  React.useEffect(() => () => clearTimeout(timerRef.current), []);
  const pick = (id) => { closeMenu(); onSelect && onSelect(id); };
  const items = [
    { id: "account", label: "My Account", icon: IconUser },
    { id: "customizer", label: "Customizer", glyph: CustomizerBrandGlyph },
    { id: "orders", label: "Orders", icon: IconBag },
    { id: "wsettings", label: "Workspace Settings", icon: IconSettings },
    { id: "logout", label: "Logout", icon: IconLogout },
  ];
  return (
    <div ref={rootRef}
      onMouseEnter={() => openMenu()}
      onMouseLeave={() => { clearTimeout(timerRef.current); timerRef.current = setTimeout(() => { setVis(false); setTimeout(() => setOpen(false), 150); }, 120); }}
      onBlur={(e) => { if (rootRef.current && !rootRef.current.contains(e.relatedTarget)) closeMenu(); }}
      style={{ marginLeft: "auto", position: "relative", display: "flex", alignItems: "center", alignSelf: "stretch" }}>
      <button ref={btnRef}
        onClick={() => (open && vis ? closeMenu() : openMenu())}
        aria-haspopup="menu" aria-expanded={open && vis} title="Account menu"
        onMouseDown={(e) => { e.currentTarget.style.opacity = ".55"; }}
        onMouseUp={(e) => { e.currentTarget.style.opacity = "1"; }}
        style={{
          display: "inline-flex", alignItems: "center", gap: 12, background: "transparent", border: 0,
          cursor: "pointer", color: "#0e0e10", padding: "8px 2px", borderRadius: 6,
          outlineOffset: 4, transition: "opacity .1s ease", fontFamily: "inherit",
        }}>
        <span style={{ fontSize: 15, fontWeight: 800, letterSpacing: ".08em" }}>{name || "MY ACCOUNT"}</span>
        <IconUser size={22} stroke={1.7} />
      </button>
      {open && (
        <div role="menu" aria-label="Account" style={{
          position: "absolute", top: "100%", right: 0, width: 252, zIndex: 80, marginTop: -6,
          background: "#fff", border: "1px solid #e8e8ec", borderRadius: 10,
          boxShadow: "0 16px 40px rgba(20,20,30,.16)", overflow: "hidden",
          opacity: vis ? 1 : 0, transform: vis ? "translateY(0)" : "translateY(-6px)",
          transition: "opacity .14s ease, transform .14s ease",
        }}>
          {items.map((it, i) => {
            const Glyph = it.glyph;
            const IconC = it.icon;
            return (
              <button key={it.id} role="menuitem" onClick={() => pick(it.id)} style={{
                display: "flex", alignItems: "center", gap: 14, width: "100%",
                background: "transparent", border: 0, borderTop: i > 0 ? "1px solid #f1f1f3" : 0,
                padding: "15px 20px", cursor: "pointer", textAlign: "left",
                fontSize: 15, fontWeight: 600, color: "#0e0e10", fontFamily: "inherit",
                transition: "background .1s ease",
              }}
                onMouseEnter={(e) => (e.currentTarget.style.background = "#f7f7f8")}
                onMouseLeave={(e) => (e.currentTarget.style.background = "transparent")}>
                {Glyph ? <Glyph /> : <IconC size={19} stroke={1.8} style={{ flexShrink: 0 }} />}
                <span>{it.label}</span>
              </button>
            );
          })}
        </div>
      )}
    </div>
  );
}

// ─────────── Team Stores workspace header (Overview / Stores) ───────────
// Single unified bar: brand mark + "TEAM STORES" wordmark (→ landing page),
// centered OVERVIEW / STORES / STOCK VAULT nav, and the account menu on the right.
function TeamStoresHeader({ active = "overview", onChange, onGoLanding, onMenuSelect, accountName }) {
  const tabs = [
    { id: "overview", label: "OVERVIEW" },
    { id: "stores", label: "STORES" },
    { id: "vault", label: "STOCK VAULT" },
  ];
  const goLanding = (e) => { e && e.preventDefault(); onGoLanding && onGoLanding(); };
  return (
    <header style={{
      position: "sticky", top: 0, zIndex: 50,
      height: 92, background: "#fff", borderBottom: "1px solid #e8e8ec",
      display: "flex", alignItems: "stretch", padding: "0 40px",
    }}>
      {/* Left — brand mark + wordmark → landing */}
      <a href="#" onClick={goLanding} title="Team Stores home" style={{
        display: "inline-flex", alignItems: "center", gap: 20,
        textDecoration: "none", color: "#0e0e10", flexShrink: 0,
      }}>
        <BrandMark variant="ink" height={38} />
        <span style={{ width: 1, height: 28, background: "#e4e4e7" }} />
        <span style={{ fontSize: 17, fontWeight: 800, letterSpacing: ".14em", whiteSpace: "nowrap" }}>TEAM STORES</span>
      </a>

      {/* Center — section nav */}
      <NavTabs tabs={tabs} active={active} onChange={onChange} />

      {/* Right — account menu (click or hover opens; no direct navigation) */}
      <AccountMenu name={accountName} onSelect={onMenuSelect} />
    </header>
  );
}

Object.assign(window, { UtilityBar, GlobalHeader, TeamStoresHeader });


/* ===== ASSET 4 : 5b45417a-f183-427a-ac73-ad2a71e7bc8c ===== */
// prolook.com storefront landing + the Team Stores section sub-tab bar.

// ─────────── prolook.com home — full-bleed brand hero ───────────
function ProlookHome({ onShopTeamStores }) {
  return (
    <div style={{ flex: 1, minWidth: 0, position: "relative", background: "#0e0e10", overflow: "hidden" }}>
      <img src="assets/hero-baseball.jpg" alt="PROLOOK team apparel" style={{
        position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", objectPosition: "50% 38%",
      }} />
    </div>
  );
}

// ─────────── Team Stores section sub-tabs (Overview / Stores / Leagues) ───────────
// The right side hosts the section-wide global search + filter/sort tools.
function TeamStoresTabs({ active = "overview", onChange, storeCount = 0, leagueCount = 0 }) {
  const tabs = [
    { id: "overview", label: "OVERVIEW", icon: IconGrid, count: null },
    { id: "stores", label: "STORES", icon: IconStore, count: storeCount },
  ];
  return (
    <div style={{
      display: "flex", alignItems: "center", justifyContent: "space-between",
      padding: "16px 32px", background: "#fff", borderBottom: "1px solid #eceef0",
    }}>
      <div style={{ display: "inline-flex", alignItems: "center", gap: 4, background: "#f3f3f5", borderRadius: 11, padding: 4 }}>
        {tabs.map((t) => {
          const on = active === t.id;
          const IconC = t.icon;
          return (
            <button key={t.id} onClick={() => onChange && onChange(t.id)} style={{
              display: "inline-flex", alignItems: "center", gap: 8,
              background: on ? "#fff" : "transparent",
              border: on ? "1px solid #e6e6ea" : "1px solid transparent",
              boxShadow: on ? "0 1px 3px rgba(20,20,30,.10)" : "none",
              borderRadius: 8, padding: "8px 16px", cursor: "pointer",
              color: on ? "#0e0e10" : "#6b6b72", fontSize: 13.5, fontWeight: 800, letterSpacing: ".045em",
              transition: "background .15s ease, border-color .15s ease, box-shadow .15s ease, color .12s ease",
            }}
              onMouseEnter={(e) => { if (!on) e.currentTarget.style.color = "#0e0e10"; }}
              onMouseLeave={(e) => { if (!on) e.currentTarget.style.color = "#6b6b72"; }}>
              <IconC size={17} stroke={1.8} />
              <span>{t.label}</span>
              {t.count != null && (
                <span style={{
                  minWidth: 20, height: 20, padding: "0 8px", borderRadius: 999,
                  background: on ? "#eef0f2" : "#e5e5e9", color: "#52525a",
                  fontSize: 11.5, fontWeight: 700, display: "inline-flex", alignItems: "center", justifyContent: "center",
                  letterSpacing: 0,
                }}>{t.count}</span>
              )}
            </button>
          );
        })}
      </div>
      <a href="#" onClick={(e) => e.preventDefault()} title="Account & profile" style={{
        marginLeft: 24, flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 12,
        textDecoration: "none", color: "#0e0e10",
      }}>
        <span style={{ fontSize: 15.5, fontWeight: 600 }}>Hi, Jane</span>
        <IconUser size={21} stroke={1.7} />
      </a>
    </div>
  );
}

Object.assign(window, { ProlookHome, TeamStoresTabs });


/* ===== ASSET 5 : 0647e24c-9706-425c-b671-823c7458e94e ===== */
// Team Stores Portal — the branded intermediate landing that sits between the
// public storefront ("TEAM STORES" header click) and the dense Overview
// dashboard. Mirrors the prolook.com consumer aesthetic, then hands off to the
// Store Manager via one deliberate gateway CTA.

const TSP_INK = "#0e0e10";
const TSP_RED = "#e1251b";
const TSP_MUTED = "#6b6b72";
const TSP_LINE = "#e5e5e9";

// Headline copy presets (exposed as a Tweak)
const TSP_HEADLINES = {
  fulfillment: ["ONE PORTAL.", "TOTAL TEAM", "FULFILLMENT."],
  teamstores: ["PROLOOK", "TEAM", "STORES."],
  geared: ["GET YOUR", "WHOLE TEAM", "GEARED UP."],
};

function TspEyebrow({ label, light }) {
  return (
    <div style={{ display: "inline-flex", alignItems: "center", gap: 12 }}>
      <span style={{ width: 12, height: 12, background: TSP_RED, flexShrink: 0 }} />
      <span style={{
        fontFamily: "'JetBrains Mono', monospace", fontSize: 12.5, fontWeight: 500,
        letterSpacing: ".34em", textTransform: "uppercase",
        color: light ? "rgba(255,255,255,.82)" : TSP_MUTED,
      }}>{label}</span>
    </div>
  );
}

// ─────────── HERO ───────────
// Reveal hook — visible state is the BASE/resting style; we start hidden and
// flip to visible via a timer (fires regardless of paint/animation timeline).
// If the transition can't tick, the committed style is still visible — content
// is never left invisible (critical for frame-boot / export / non-paint).
function useTspReveal() {
  const reduce = typeof window !== "undefined" && window.matchMedia
    && window.matchMedia("(prefers-reduced-motion: reduce)").matches;
  const [on, setOn] = React.useState(reduce); // reduced motion → already visible, no motion
  React.useEffect(() => {
    if (reduce) return;
    const t = setTimeout(() => setOn(true), 40);
    return () => clearTimeout(t);
  }, [reduce]);
  // i = stagger index
  return (i = 0) => {
    if (reduce) return null;
    return {
      opacity: on ? 1 : 0,
      transform: on ? "none" : "translateY(18px)",
      transition: `opacity .72s cubic-bezier(.2,.7,.2,1) ${i * 0.07}s, transform .72s cubic-bezier(.2,.7,.2,1) ${i * 0.07}s`,
      willChange: "opacity, transform",
    };
  };
}

function TspHero({ variant, headlineKey, onExplore }) {
  const lines = TSP_HEADLINES[headlineKey] || TSP_HEADLINES.fulfillment;
  const rise = useTspReveal();

  const Display = ({ base = 1 }) => (
    <h1 style={{
      margin: 0, fontFamily: "Archivo, sans-serif", fontWeight: 900,
      fontSize: 88, lineHeight: 0.9, letterSpacing: "-.025em",
      color: "#fff", textTransform: "uppercase",
    }}>
      {lines.map((ln, i) => (
        <span key={i} style={{ display: "block", ...rise(base + i) }}>
          {i === lines.length - 1 ? (
            <span>{ln.replace(/\.$/, "")}<span style={{ color: TSP_RED }}>.</span></span>
          ) : ln}
        </span>
      ))}
    </h1>
  );

  const sub = "The single command center for every team store you run — branding, products, pricing, rosters, and orders, all in one place.";

  const exploreBtn = (style) => (
    <button onClick={onExplore} style={{
      alignSelf: "flex-start",
      display: "inline-flex", alignItems: "center", gap: 12,
      background: "transparent", color: "#fff", border: "1.5px solid rgba(255,255,255,.55)",
      borderRadius: 4, padding: "16px 28px", cursor: "pointer",
      fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: 13.5, letterSpacing: ".14em", textTransform: "uppercase",
      transition: "background .16s ease, color .16s ease, border-color .16s ease",
      ...style,
    }}
      onMouseEnter={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = TSP_INK; e.currentTarget.style.borderColor = "#fff"; }}
      onMouseLeave={(e) => { e.currentTarget.style.background = "transparent"; e.currentTarget.style.color = "#fff"; e.currentTarget.style.borderColor = "rgba(255,255,255,.55)"; }}>
      See how it works <span style={{ fontSize: 16, lineHeight: 1 }}>↓</span>
    </button>
  );

  if (variant === "split") {
    return (
      <section style={{ display: "flex", minHeight: 600, background: TSP_INK }}>
        {/* Left dark editorial panel */}
        <div style={{
          flex: "0 0 47%", display: "flex", flexDirection: "column", justifyContent: "center",
          padding: "64px 56px 64px 64px", borderRight: `5px solid ${TSP_RED}`,
        }}>
          <div style={rise(0)}><TspEyebrow label="PROLOOK · Team Stores" light /></div>
          <div style={{ height: 32 }} />
          <Display base={1} />
          <div style={{ height: 28 }} />
          <p style={{ margin: 0, maxWidth: 460, color: "rgba(255,255,255,.7)", fontSize: 16.5, lineHeight: 1.6, ...rise(lines.length + 1) }}>{sub}</p>
          <div style={{ height: 40 }} />
          <div style={rise(lines.length + 2)}>{exploreBtn({ padding: "16px 28px" })}</div>
        </div>
        {/* Right photo panel */}
        <div style={{ flex: 1, position: "relative", overflow: "hidden", background: "#0a0a0b" }}>
          <img src="assets/hero-baseball.jpg" alt="PROLOOK team in custom uniforms" style={{
            position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", objectPosition: "50% 30%",
          }} />
          <div style={{ position: "absolute", inset: 0, background: "linear-gradient(90deg, rgba(14,14,16,.55) 0%, rgba(14,14,16,0) 30%)" }} />
        </div>
      </section>
    );
  }

  // Cinematic full-bleed lineup (default)
  return (
    <section style={{ position: "relative", minHeight: 620, background: TSP_INK, overflow: "hidden" }}>
      <img src="assets/hero-baseball.jpg" alt="PROLOOK team in custom uniforms" style={{
        position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", objectPosition: "62% 28%",
      }} />
      {/* Left-anchored scrim for legible block lettering */}
      <div style={{ position: "absolute", inset: 0, background: "linear-gradient(95deg, rgba(8,8,9,.97) 0%, rgba(8,8,9,.86) 30%, rgba(8,8,9,.45) 58%, rgba(8,8,9,.04) 100%)" }} />
      <div style={{ position: "absolute", left: 0, right: 0, bottom: 0, height: 160, background: "linear-gradient(0deg, rgba(8,8,9,.6) 0%, rgba(8,8,9,0) 100%)" }} />
      <div style={{ position: "relative", maxWidth: 1240, margin: "0 auto", padding: "0 64px", minHeight: 620, display: "flex", flexDirection: "column", justifyContent: "center" }}>
        <div style={rise(0)}><TspEyebrow label="PROLOOK · Team Stores" light /></div>
        <div style={{ height: 32 }} />
        <Display base={1} />
        <div style={{ height: 32 }} />
        <p style={{ margin: 0, maxWidth: 540, color: "rgba(255,255,255,.74)", fontSize: 17, lineHeight: 1.6, ...rise(lines.length + 1) }}>{sub}</p>
        <div style={{ height: 40 }} />
        <div style={rise(lines.length + 2)}>{exploreBtn()}</div>
      </div>
    </section>
  );
}

// ─────────── PITCH + TRANSITION GATEWAY ───────────
function TspGateway({ ctaLabel, ctaColor, onEnter, gwRef }) {
  const [hover, setHover] = React.useState(false);
  const solid = ctaColor === "red" ? TSP_RED : TSP_INK;
  const solidHover = ctaColor === "red" ? "#b81d14" : "#000";
  return (
    <section ref={gwRef} style={{ background: "#fff", borderTop: `1px solid ${TSP_LINE}` }}>
      <div style={{
        maxWidth: 1240, margin: "0 auto", padding: "84px 64px",
        display: "flex", alignItems: "center", justifyContent: "space-between", gap: 64,
      }}>
        {/* Left text stack */}
        <div style={{ maxWidth: 640 }}>
          <TspEyebrow label="The Transition" />
          <div style={{ height: 24 }} />
          <h2 style={{
            margin: 0, fontFamily: "Archivo, sans-serif", fontWeight: 800,
            fontSize: 50, lineHeight: 1.0, letterSpacing: "-.02em", color: TSP_INK, textTransform: "uppercase",
          }}>Get geared up<br />for the season.</h2>
          <div style={{ height: 24 }} />
          <p style={{ margin: 0, maxWidth: 560, color: TSP_MUTED, fontSize: 16.5, lineHeight: 1.65 }}>
            Our online team stores simplify the way you distribute elite, 100% custom gear to your
            players, coaches, and fans. Manage roster tracking and configure team-required packages
            seamlessly — from one dashboard.
          </p>
        </div>

        {/* Right gateway CTA */}
        <div style={{ flexShrink: 0, display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 16 }}>
          <button onClick={onEnter}
            onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}
            style={{
              display: "inline-flex", alignItems: "center", gap: 16,
              background: hover ? solidHover : solid, color: "#fff", border: 0,
              borderRadius: 5, padding: "24px 40px", cursor: "pointer",
              fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: 18,
              letterSpacing: ".05em", textTransform: "uppercase", whiteSpace: "nowrap",
              boxShadow: hover ? "0 14px 34px rgba(14,14,16,.22)" : "0 6px 18px rgba(14,14,16,.12)",
              transition: "all .18s ease", transform: hover ? "translateY(-2px)" : "none",
            }}>
            {ctaLabel}
            <span style={{ display: "inline-flex", transition: "transform .18s ease", transform: hover ? "translateX(5px)" : "none" }}>
              <IconArrowRight size={24} stroke={2.4} />
            </span>
          </button>
          <span style={{ fontSize: 13, color: TSP_MUTED, display: "inline-flex", alignItems: "center", gap: 8 }}>
            <IconLock size={13} /> Opens your store performance dashboard
          </span>
        </div>
      </div>
    </section>
  );
}

// ─────────── THREE-PILLAR FEATURE GRID ───────────
function TspFeatureCard({ icon, title, body }) {
  const [hover, setHover] = React.useState(false);
  return (
    <div onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)} style={{
      position: "relative", background: "#fff",
      border: `1px solid ${hover ? TSP_INK : TSP_LINE}`, borderRadius: 6,
      padding: "36px 32px 36px", display: "flex", flexDirection: "column",
      transition: "border-color .16s ease, transform .16s ease, box-shadow .16s ease",
      transform: hover ? "translateY(-3px)" : "none",
      boxShadow: hover ? "0 16px 38px rgba(20,20,30,.08)" : "none",
    }}>
      {/* top row: icon + arrow indicator */}
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 28 }}>
        <span style={{
          width: 52, height: 52, border: `1px solid ${hover ? TSP_INK : TSP_LINE}`, borderRadius: 6,
          display: "inline-flex", alignItems: "center", justifyContent: "center", color: TSP_INK,
          transition: "border-color .16s ease",
        }}>
          {icon}
        </span>
        <span style={{ color: hover ? TSP_RED : "#c2c2c8", transition: "color .16s ease", marginTop: 4 }}>
          <IconExternal size={20} stroke={2} />
        </span>
      </div>
      <h3 style={{
        margin: "0 0 12px", fontFamily: "Archivo, sans-serif", fontWeight: 800,
        fontSize: 19, lineHeight: 1.12, letterSpacing: ".005em", color: TSP_INK, textTransform: "uppercase",
      }}>{title}</h3>
      <p style={{ margin: 0, color: TSP_MUTED, fontSize: 14.5, lineHeight: 1.62 }}>{body}</p>
    </div>
  );
}

function TspFeatures() {
  const cards = [
    {
      icon: <IconStore size={26} stroke={1.7} />,
      title: "An experience to fit your needs",
      body: "Custom team stores engineered entirely around your roster's sizing run, division levels, and operational schedule.",
    },
    {
      icon: <IconDollar size={26} stroke={1.7} />,
      title: "Flexible pricing templates",
      body: "Complete financial transparency. Instantly adjust dealer profit margins, coach incentives, and end-user retail values per item.",
    },
    {
      icon: <IconBag size={26} stroke={1.7} />,
      title: "Required player packs",
      body: "Group essential uniforms into mandatory bundles to ensure your athletes are completely equipped and ready by deadline day.",
    },
  ];
  return (
    <section style={{ background: "#f7f7f8", borderTop: `1px solid ${TSP_LINE}` }}>
      <div style={{ maxWidth: 1240, margin: "0 auto", padding: "72px 64px 88px" }}>
        <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", marginBottom: 36 }}>
          <div>
            <TspEyebrow label="What you get" />
            <h2 style={{ margin: "16px 0 0", fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: 32, letterSpacing: "-.01em", color: TSP_INK, textTransform: "uppercase" }}>
              Built for the way teams gear up
            </h2>
          </div>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 20 }}>
          {cards.map((c) => <TspFeatureCard key={c.title} {...c} />)}
        </div>
      </div>
    </section>
  );
}

// ─────────── PORTAL (composed) ───────────
function TeamStoresPortal({ onEnter, headlineKey = "fulfillment", heroVariant = "cinematic", ctaLabel = "Enter Store Manager", ctaColor = "black" }) {
  const gwRef = React.useRef(null);
  const scrollToGateway = () => {
    const el = gwRef.current;
    if (!el) return;
    const top = el.getBoundingClientRect().top + window.scrollY - 90;
    window.scrollTo({ top, behavior: "smooth" });
  };
  return (
    <div style={{ flex: 1, minWidth: 0, background: "#fff" }}>
      <TspHero variant={heroVariant} headlineKey={headlineKey} onExplore={scrollToGateway} />
      <TspGateway ctaLabel={ctaLabel} ctaColor={ctaColor} onEnter={onEnter} gwRef={gwRef} />
      <TspFeatures />
    </div>
  );
}

Object.assign(window, { TeamStoresPortal });


/* ===== ASSET 6 : fb048e9f-323b-449c-b640-fdc8eb182b57 ===== */
// Contextual "Manage Store" workspace — the bar + icon side-rail that replace
// the global chrome once you drill into a single store. The actual tab content
// is still rendered by <Dashboard embedded>, which owns the side-rail.

// Small round team emblem reused in the store bar + switcher.
function TeamEmblem({ store, size = 30 }) {
  if (store?.teamLogo?.url) {
    return <img src={store.teamLogo.url} alt="" style={{ width: size, height: size, borderRadius: "50%", objectFit: "cover", flexShrink: 0 }} />;
  }
  const bg = store?.teamLogo?.color || store?.teamColors?.[0] || "#1d3a8a";
  const mono = store?.teamLogo?.mono || (store?.teamName || store?.storeTitle || "T").slice(0, 2).toUpperCase();
  return (
    <span style={{
      width: size, height: size, borderRadius: "50%", background: bg, flexShrink: 0,
      color: (bg || "").toLowerCase() === "#ffffff" ? "#0e0e10" : "#fff",
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      fontWeight: 800, fontSize: size * 0.36,
      border: (bg || "").toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none",
    }}>{mono}</span>
  );
}

// ─────────── Status pill (Active / On hold) ───────────
function StatusDropdown({ status = "active" }) {
  const active = status !== "hold";
  return (
    <span style={{
      display: "inline-flex", alignItems: "center", gap: 8,
      background: active ? "#d8f5e3" : "#fdeecb", color: active ? "#1f7a44" : "#92670b",
      borderRadius: 999, padding: "8px 16px",
      fontSize: 14, fontWeight: 700, userSelect: "none",
    }}>
      <span style={{ width: 8, height: 8, borderRadius: "50%", background: active ? "#1f9d55" : "#e0a92b", flexShrink: 0 }} />
      {active ? "Active" : "On Hold"}
    </span>
  );
}

// ─────────── Store switcher ───────────
function StoreSwitcher({ store, stores = [], onSwitchStore }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef();
  React.useEffect(() => {
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const list = stores.length ? stores : [store];
  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button onClick={() => setOpen((v) => !v)} style={{
        display: "inline-flex", alignItems: "center", gap: 12,
        background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8,
        padding: "8px 12px 8px 8px", cursor: "pointer", minWidth: 212, justifyContent: "space-between",
      }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 8, minWidth: 0 }}>
          <TeamEmblem store={store} size={24} />
          <span style={{ fontSize: 14.5, fontWeight: 600, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
            {store?.teamName || store?.storeTitle || "[Team Name]"}
          </span>
        </span>
        <IconChevron size={16} style={{ color: "#6b6b72", flexShrink: 0, transform: open ? "rotate(180deg)" : "none", transition: "transform .15s" }} />
      </button>
      {open && (
        <div style={{
          position: "absolute", top: "calc(100% + 8px)", right: 0, minWidth: 252, zIndex: 60,
          background: "#fff", border: "1px solid #e8e8ec", borderRadius: 8,
          boxShadow: "0 12px 28px rgba(20,20,30,.12)", padding: 4, maxHeight: 340, overflowY: "auto",
        }}>
          <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", padding: "8px 12px 8px" }}>SWITCH STORE</div>
          {list.map((s) => {
            const on = s.id === store?.id;
            return (
              <div key={s.id} onClick={() => { setOpen(false); if (!on) onSwitchStore && onSwitchStore(s.id); }}
                style={{ padding: "8px 12px", borderRadius: 5, fontSize: 14, cursor: "pointer", display: "flex", alignItems: "center", gap: 12, background: on ? "#f7f7f8" : "transparent" }}
                onMouseEnter={(e) => { if (!on) e.currentTarget.style.background = "#fafafb"; }}
                onMouseLeave={(e) => { if (!on) e.currentTarget.style.background = "transparent"; }}>
                <TeamEmblem store={s} size={24} />
                <span style={{ flex: 1, fontWeight: on ? 700 : 500, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s.teamName || s.storeTitle}</span>
                {on && <IconCheck size={15} />}
              </div>
            );
          })}
        </div>
      )}
    </div>
  );
}

// ─────────── Store workspace bar ───────────
function StoreWorkspaceBar({ store, liveStores = [], onViewStore, onSetStatus, onSwitchStore, onBack, readOnly }) {
  const previewStore = () => { window.location.href = encodeURI("Team Store Storefront.dc.html"); };
  return (
    <div style={{
      display: "grid", gridTemplateColumns: "1fr auto 1fr", alignItems: "center",
      padding: "16px 32px", background: "#18181B",
    }}>
      {/* Left — back to stores */}
      <div style={{ justifySelf: "start" }}>
        <button onClick={() => onBack && onBack()} style={{
          display: "inline-flex", alignItems: "center", gap: 8, background: "transparent", border: 0,
          cursor: "pointer", color: "#fff", fontSize: 15, fontWeight: 600, padding: "8px 4px",
        }}
          onMouseEnter={(e) => (e.currentTarget.style.opacity = ".62")}
          onMouseLeave={(e) => (e.currentTarget.style.opacity = "1")}>
          <IconChevronLeft size={20} /> Back to Stores
        </button>
      </div>

      {/* Center — team identity, emblem stacked above the name */}
      <div style={{ justifySelf: "center", display: "flex", flexDirection: "column", alignItems: "center", gap: 8 }}>
        <TeamEmblem store={store} size={34} />
        <h1 style={{ fontSize: 14, fontWeight: 800, letterSpacing: ".14em", margin: 0, color: "#fff", textTransform: "uppercase", textAlign: "center" }}>
          {store.teamName || store.storeTitle || "[TEAM NAME]"}
        </h1>
      </div>

      {/* Right — store guide + preview store + actions menu */}
      <div style={{ justifySelf: "end", display: "flex", alignItems: "center", gap: 12 }}>
        <button onClick={() => window.dispatchEvent(new CustomEvent("setupguide:open"))} title="Reopen the store setup guide" style={{
          display: "inline-flex", alignItems: "center", gap: 8, background: "transparent",
          color: "rgba(255,255,255,.85)", border: 0, padding: "12px 8px", cursor: "pointer",
          fontSize: 13.5, fontWeight: 600, fontFamily: "inherit",
        }}
          onMouseEnter={(e) => (e.currentTarget.style.color = "#fff")}
          onMouseLeave={(e) => (e.currentTarget.style.color = "rgba(255,255,255,.85)")}>
          <IconInfo size={16} /> Setup guide
        </button>
        <button onClick={readOnly ? undefined : previewStore} disabled={readOnly} title={readOnly ? "Storefront is closed for archived stores" : "Preview the customer storefront"} style={{
          display: "inline-flex", alignItems: "center", gap: 12, background: "transparent",
          color: "#fff", border: "1px solid rgba(255,255,255,.4)", borderRadius: 8,
          padding: "12px 20px", cursor: readOnly ? "not-allowed" : "pointer", fontSize: 14, fontWeight: 700, fontFamily: "inherit",
          opacity: readOnly ? 0.4 : 1,
          transition: "background .14s, border-color .14s",
        }}
          onMouseEnter={(e) => { if (readOnly) return; e.currentTarget.style.background = "rgba(255,255,255,.1)"; e.currentTarget.style.borderColor = "rgba(255,255,255,.7)"; }}
          onMouseLeave={(e) => { if (readOnly) return; e.currentTarget.style.background = "transparent"; e.currentTarget.style.borderColor = "rgba(255,255,255,.4)"; }}>
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <circle cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="1.8" />
            <path d="M10 8.5l5 3.5-5 3.5z" fill="currentColor" />
          </svg>
          {readOnly ? "Storefront Closed" : "Preview Store"}
        </button>
      </div>
    </div>
  );
}

// ─────────── Workspace icon side-rail ───────────
// Rendered by <Dashboard embedded>. Items map to the dashboard's internal tab
// ids (general/orders/products/roster/payouts) under refreshed labels.
function IconHomeGlyph({ size = 24, stroke = 1.7 }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={stroke} strokeLinecap="round" strokeLinejoin="round">
      <path d="M3 11l9-8 9 8" /><path d="M5 9.5V20h14V9.5" />
    </svg>
  );
}
const WORKSPACE_NAV = [
  { id: "home", label: "Home", icon: IconHomeGlyph },
  { id: "roster", label: "Roster", icon: IconUsers },
  { id: "products", label: "Products", icon: IconShirt },
  { id: "orders", label: "Orders", icon: IconReceipt },
  { id: "payouts", label: "Reports", icon: IconBarGraphBox },
  { id: "general", label: "Settings", icon: IconSettings },
];

function StoreSideNav({ tab, onChange, badges = {}, store }) {
  const labelFor = (it) => it.id === "roster" ? ((store && store.storeType === "league") ? "My Teams" : "My Team") : it.label;
  return (
    <nav style={{
      width: 116, flexShrink: 0, background: "#fff", borderRight: "1px solid #eceef0",
      padding: 16, display: "flex", flexDirection: "column", alignItems: "stretch", gap: 8,
      position: "sticky", top: 80, alignSelf: "flex-start", height: "calc(100vh - 80px)",
    }}>
      {WORKSPACE_NAV.map((it) => {
        const on = tab === it.id;
        const IconC = it.icon;
        const label = labelFor(it);
        return (
          <button key={it.id} onClick={() => onChange && onChange(it.id)} title={label} style={{
            background: on ? "#ececf0" : "transparent", border: 0, cursor: "pointer", borderRadius: 20,
            padding: "16px 0", display: "flex", flexDirection: "column", alignItems: "center", gap: 8, width: "100%",
            transition: "background .12s",
          }}
            onMouseEnter={(e) => { if (!on) e.currentTarget.style.background = "#f6f6f8"; }}
            onMouseLeave={(e) => { if (!on) e.currentTarget.style.background = "transparent"; }}>
            <span style={{
              position: "relative", width: 44, height: 44, borderRadius: "50%",
              background: on ? "#0e0e10" : "transparent",
              display: "inline-flex", alignItems: "center", justifyContent: "center",
              color: on ? "#fff" : "#0e0e10", transition: "background .12s",
            }}>
              <IconC size={on ? 22 : 24} stroke={on ? 2 : 1.7} />
              {badges[it.id] && (
                <span style={{ position: "absolute", top: 4, right: 4, width: 8, height: 8, borderRadius: "50%", background: "#e1251b", border: "1.5px solid #fff" }} />
              )}
            </span>
            <span style={{ fontSize: 13, fontWeight: on ? 700 : 600, color: on ? "#0e0e10" : "#52525a", letterSpacing: ".01em" }}>
              {label}
            </span>
          </button>
        );
      })}
    </nav>
  );
}

// ─────────── Wrapper ───────────
function ManageStoreWorkspace(props) {
  const { store, liveStores, onViewStore, onSetStatus, onSwitchStore, onBack, onCreateTemplate, readOnly } = props;
  return (
    <div style={{ flex: 1, display: "flex", flexDirection: "column", background: "#fbfbfc", minWidth: 0, height: "100vh", overflow: "hidden" }}>
      <StoreWorkspaceBar
        store={store}
        liveStores={liveStores}
        onViewStore={onViewStore}
        onSetStatus={onSetStatus}
        onSwitchStore={onSwitchStore}
        onBack={onBack}
        onCreateTemplate={onCreateTemplate}
        readOnly={readOnly}
      />
      {readOnly && (
        <div style={{
          display: "flex", alignItems: "center", gap: 14, padding: "14px 32px",
          background: "#fdf6e3", borderBottom: "1px solid #f0e2b8", color: "#7a5b0b",
        }}>
          <span style={{ width: 34, height: 34, borderRadius: 9, background: "#f6e2a8", color: "#92670b", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.1" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
              <rect x="3" y="4" width="18" height="4" rx="1" /><path d="M5 8v11a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V8" /><path d="M10 12h4" />
            </svg>
          </span>
          <div style={{ minWidth: 0, flex: 1 }}>
            <div style={{ fontSize: 14, fontWeight: 800, color: "#5c4408" }}>This store is archived — read-only</div>
            <div style={{ fontSize: 12.5, color: "#8a6a17", marginTop: 1 }}>
              Order history and reports are retained as a record. Restore this store from My Account to make changes again — restoring is a managing-rep action.
            </div>
          </div>
        </div>
      )}
      <Dashboard embedded {...props} />
    </div>
  );
}

Object.assign(window, { TeamEmblem, StoreWorkspaceBar, StoreSideNav, ManageStoreWorkspace, WORKSPACE_NAV });


/* ===== ASSET 7 : efc49966-9ed4-4998-967b-14fbe5b310e1 ===== */
// Empty state — matches screenshot 1.
function EmptyState({ onCreate }) {
  return (
    <div style={{ flex: 1, padding: "28px 36px" }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 28 }}>
        <div>
          <h1 style={{ fontSize: 16, fontWeight: 800, letterSpacing: ".06em", margin: 0, marginBottom: 12 }}>TEAM STORES</h1>
          <div style={{ display: "flex", gap: 20, alignItems: "center", color: "#6b6b72" }}>
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
              <IconStore size={16} /> 0 Stores
            </span>
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
              <IconDollar size={16} style={{ color: "#1f9d55" }} />
              <span style={{ color: "#1f9d55", fontWeight: 600 }}>$0.00</span>
            </span>
          </div>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
          <div style={{
            position: "relative", border: "1px solid #d4d4d8", borderRadius: 4,
            background: "#fff", minWidth: 360, display: "flex", alignItems: "center",
          }}>
            <IconSearch size={16} style={{ position: "absolute", left: 12, color: "#9a9aa1" }} />
            <input placeholder="Search by name, email, or order number" style={{
              width: "100%", border: 0, outline: 0, background: "transparent",
              padding: "12px 12px 12px 36px", fontSize: 13,
            }} />
          </div>
          <button style={{
            width: 40, height: 40, border: "1px solid #d4d4d8", background: "#fff",
            borderRadius: 4, display: "inline-flex", alignItems: "center", justifyContent: "center",
            cursor: "pointer",
          }}>
            <IconSliders size={18} />
          </button>
        </div>
      </div>

      <div style={{
        flex: 1, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
        padding: "120px 20px",
      }}>
        <div style={{ fontSize: 17, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>
          There are currently no available team stores yet.
        </div>
        <div style={{ fontSize: 14, color: "#6b6b72", marginBottom: 32 }}>
          Get started by creating a new store!
        </div>
        <Button variant="primary" size="lg" onClick={onCreate}>Create Store</Button>
      </div>
    </div>
  );
}

Object.assign(window, { EmptyState });


/* ===== ASSET 8 : 4f715cfb-29a8-402f-a856-ebf4478c743f ===== */
// Full-viewport cinematic splash / feature-gate for Team Stores.
// Renders OUTSIDE the app shell (no sidebar, no global header) — a standalone
// introductory portal that sells the feature and captures verification intent.
// 100vw × 100vh, no scroll. Athletic editorial aesthetic: full-bleed action
// photo, deep dark wash, Prolook-red accents + crosshair framing.

function FeatureGate({ onApply, onBypass, onBack, embedded }) {
  const RED = "#e1251b";
  const INK = "#0e0e10";

  const pillars = [
    {
      icon: <IconClipboard size={22} />,
      title: "Automated Roster Tracking",
      body: "Monitor exactly which players have ordered required uniforms — and who is still incomplete.",
    },
    {
      icon: <IconDollar size={22} />,
      title: "Flexible Profit Margins",
      body: "Easily adjust dealer, coach, and organization payouts inside the configuration tool.",
    },
    {
      icon: <IconLayers size={22} />,
      title: "Batched Window Ordering",
      body: "Open and close ordering campaigns on a timeline aligned with production schedules.",
    },
  ];

  return (
    <div style={{
      position: embedded ? "relative" : "fixed", inset: embedded ? "auto" : 0,
      flex: embedded ? 1 : "none", minWidth: 0,
      minHeight: embedded ? "calc(100vh - 114px)" : "auto",
      overflow: "hidden", background: INK, color: "#fff",
      fontFamily: '"Inter", system-ui, sans-serif',
      display: "flex", flexDirection: "column",
    }}>
      {/* ── Full-bleed hero photo ───────────────────────────────── */}
      <img src="assets/hero-baseball.jpg" alt="" aria-hidden style={{
        position: "absolute", inset: 0, width: "100%", height: "100%",
        objectFit: "cover", objectPosition: "72% 32%",
      }} />

      {/* dark cinematic wash: deep on the left for text legibility, revealing the team on the right */}
      <div aria-hidden style={{
        position: "absolute", inset: 0, pointerEvents: "none",
        background:
          "linear-gradient(100deg, rgba(8,8,10,.95) 0%, rgba(8,8,10,.86) 26%, rgba(8,8,10,.5) 48%, rgba(8,8,10,.12) 70%, rgba(8,8,10,.42) 100%)",
      }} />
      {/* bottom + top vignette to anchor pillars and header */}
      <div aria-hidden style={{
        position: "absolute", inset: 0, pointerEvents: "none",
        background: "linear-gradient(180deg, rgba(8,8,10,.6) 0%, transparent 16%, transparent 60%, rgba(8,8,10,.82) 100%)",
      }} />
      {/* warm red edge bloom, BRUTL-style, kept subtle */}
      <div aria-hidden style={{
        position: "absolute", inset: 0, pointerEvents: "none", mixBlendMode: "soft-light",
        background: "radial-gradient(110% 70% at 88% 8%, rgba(225,37,27,.45), transparent 50%)",
      }} />

      {/* ── Olympiq-style crosshair frame ───────────────────────── */}
      <Crosshairs color={RED} />

      {/* ── Micro-header (standalone only — hidden when embedded under the prolook.com header) ── */}
      {!embedded && (
      <header style={{
        position: "relative", zIndex: 3, flexShrink: 0,
        display: "flex", alignItems: "center", justifyContent: "space-between",
        padding: "16px 40px",
      }}>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <img src="assets/prolook-mark.png" alt="PROLOOK" style={{ height: 32, width: "auto", display: "block" }} />
          <span style={{ width: 1, height: 28, background: "rgba(255,255,255,.22)" }} />
          <span style={{
            fontFamily: '"JetBrains Mono", monospace', fontSize: 11.5, fontWeight: 500,
            letterSpacing: ".22em", textTransform: "uppercase", color: "rgba(255,255,255,.92)",
            border: "1px solid rgba(255,255,255,.3)", padding: "4px 12px",
          }}>Team Stores</span>
        </div>
        <button onClick={onBack} title="Return to prolook.com" style={{
          display: "inline-flex", alignItems: "center", gap: 8, background: "none",
          border: "none", color: "rgba(255,255,255,.82)", padding: "8px 2px", cursor: "pointer",
          fontSize: 13, fontWeight: 600, letterSpacing: ".01em", whiteSpace: "nowrap",
          borderBottom: "1px solid transparent", transition: "color .14s ease, border-color .14s ease",
        }}
          onMouseEnter={(e) => { e.currentTarget.style.color = "#fff"; e.currentTarget.style.borderBottomColor = "rgba(255,255,255,.5)"; }}
          onMouseLeave={(e) => { e.currentTarget.style.color = "rgba(255,255,255,.82)"; e.currentTarget.style.borderBottomColor = "transparent"; }}>
          <span style={{ fontSize: 15, lineHeight: 1 }}>&larr;</span> Back to PROLOOK
        </button>
      </header>
      )}

      {/* ── Hero content (left-anchored) ────────────────────────── */}
      <div style={{
        position: "relative", zIndex: 3, flex: 1, minHeight: 0,
        display: "flex", flexDirection: "column", justifyContent: "center",
        padding: "clamp(8px, 2vh, 28px) 40px", maxWidth: 980,
      }}>
        <div style={{
          display: "inline-flex", alignItems: "center", gap: 12, marginBottom: "clamp(12px, 1.8vh, 20px)",
          fontFamily: '"JetBrains Mono", monospace', fontSize: 11.5, fontWeight: 500,
          letterSpacing: ".26em", textTransform: "uppercase", color: "rgba(255,255,255,.72)",
        }}>
          <span style={{ width: 32, height: 2, background: RED, display: "inline-block" }} />
          Team Apparel Logistics
        </div>

        <h1 style={{
          margin: 0, fontFamily: '"Archivo", sans-serif', fontWeight: 900,
          fontSize: "clamp(34px, 5.4vh, 70px)", lineHeight: 0.94, letterSpacing: "-0.02em",
          textTransform: "uppercase", textShadow: "0 2px 30px rgba(0,0,0,.5)", maxWidth: "16ch",
        }}>
          Take Control of Your Team's{" "}
          <span style={{ color: RED }}>Apparel Logistics</span>
        </h1>

        <p style={{
          margin: "clamp(12px, 2vh, 24px) 0 0", maxWidth: 560, color: "rgba(255,255,255,.82)",
          fontSize: "clamp(13px, 1.7vh, 17px)", lineHeight: 1.5, textWrap: "pretty",
          textShadow: "0 1px 12px rgba(0,0,0,.5)",
        }}>
          Launch custom online shops for leagues, schools, and clubs. Handle roster tracking,
          required player gear runs, and custom profit margins in one centralized spot.
        </p>

        {/* compliance line */}
        <div style={{
          display: "inline-flex", alignItems: "center", gap: 12, marginTop: "clamp(16px, 2.4vh, 28px)",
          alignSelf: "flex-start", maxWidth: 600,
          background: "rgba(255,255,255,.05)", border: "1px solid rgba(255,255,255,.14)",
          backdropFilter: "blur(6px)", WebkitBackdropFilter: "blur(6px)",
          padding: "8px 16px",
        }}>
          <IconLock size={16} style={{ color: RED, flexShrink: 0 }} />
          <span style={{ fontSize: 12.5, lineHeight: 1.45, color: "rgba(255,255,255,.78)" }}>
            <strong style={{ color: "#fff", fontWeight: 700 }}>Verification required.</strong>{" "}
            Business entity, EIN, and banking details are reviewed by a PROLOOK
            admin &mdash; typical turnaround <strong style={{ color: "#fff", fontWeight: 700 }}>24&ndash;48 hours</strong>.
          </span>
        </div>

        {/* CTA */}
        <div style={{ display: "flex", alignItems: "center", gap: 24, marginTop: "clamp(16px, 2.6vh, 32px)" }}>
          <button onClick={onApply} style={{
            display: "inline-flex", alignItems: "center", gap: 12, background: RED, color: "#fff",
            border: "none", padding: "16px 28px", fontSize: 14, fontWeight: 800,
            letterSpacing: ".02em", cursor: "pointer", whiteSpace: "nowrap",
            boxShadow: "0 14px 40px rgba(225,37,27,.4)", transition: "transform .14s ease, box-shadow .14s ease, background .14s ease",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = INK; e.currentTarget.style.transform = "translateY(-2px)"; }}
            onMouseLeave={(e) => { e.currentTarget.style.background = RED; e.currentTarget.style.color = "#fff"; e.currentTarget.style.transform = "none"; }}>
            <IconLock size={17} />
            Apply for Access
            <IconArrowRight size={17} />
          </button>
          <button onClick={onBypass} style={{
            background: "none", border: "none", cursor: "pointer", color: "rgba(255,255,255,.82)",
            fontSize: 12.5, fontWeight: 600, letterSpacing: ".04em", padding: "4px 0", whiteSpace: "nowrap",
            borderBottom: "1px solid transparent", textShadow: "0 1px 10px rgba(0,0,0,.85)",
            display: "inline-flex", alignItems: "center", gap: 8,
          }}
            onMouseEnter={(e) => { e.currentTarget.style.color = "#fff"; e.currentTarget.style.borderBottomColor = "rgba(255,255,255,.6)"; }}
            onMouseLeave={(e) => { e.currentTarget.style.color = "rgba(255,255,255,.7)"; e.currentTarget.style.borderBottomColor = "transparent"; }}>
            Already verified? Enter <span style={{ fontSize: 14, lineHeight: 1 }}>&rarr;</span>
          </button>
        </div>
      </div>

      {/* ── Pillar grid (bottom third) ──────────────────────────── */}
      <div style={{
        position: "relative", zIndex: 3, flexShrink: 0,
        display: "grid", gridTemplateColumns: "repeat(3, 1fr)",
        borderTop: "1px solid rgba(255,255,255,.14)",
      }}>
        {pillars.map((p, i) => (
          <div key={p.title} style={{
            display: "flex", alignItems: "flex-start", gap: 16,
            padding: "clamp(12px, 2.2vh, 24px) clamp(20px, 2vw, 40px)",
            borderLeft: i === 0 ? "none" : "1px solid rgba(255,255,255,.14)",
            background: "rgba(10,10,12,.4)", backdropFilter: "blur(8px)", WebkitBackdropFilter: "blur(8px)",
          }}>
            <span style={{
              flexShrink: 0, width: 44, height: 44, border: "1px solid rgba(255,255,255,.22)",
              display: "inline-flex", alignItems: "center", justifyContent: "center", color: RED,
            }}>{p.icon}</span>
            <div>
              <div style={{
                fontSize: 13, fontWeight: 800, textTransform: "uppercase", letterSpacing: ".04em",
                marginBottom: 8, lineHeight: 1.2, color: "#fff",
              }}>{p.title}</div>
              <div style={{ fontSize: 12, lineHeight: 1.5, color: "rgba(255,255,255,.62)", textWrap: "pretty" }}>{p.body}</div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

// Olympiq-style thin grid + neon corner crosshairs framing the canvas.
function Crosshairs({ color }) {
  const tick = (style) => (
    <span style={{ position: "absolute", background: color, ...style }} />
  );
  const corner = (pos) => (
    <div style={{ position: "absolute", width: 16, height: 16, ...pos }}>
      <span style={{ position: "absolute", left: 8, top: 0, width: 2, height: 16, background: color }} />
      <span style={{ position: "absolute", top: 8, left: 0, height: 2, width: 16, background: color }} />
    </div>
  );
  return (
    <div aria-hidden style={{ position: "absolute", inset: 28, pointerEvents: "none", zIndex: 2 }}>
      {/* faint frame border */}
      <div style={{ position: "absolute", inset: 0, border: "1px solid rgba(255,255,255,.08)" }} />
      {corner({ left: -8, top: -8 })}
      {corner({ right: -8, top: -8 })}
      {corner({ left: -8, bottom: -8 })}
      {corner({ right: -8, bottom: -8 })}
    </div>
  );
}

Object.assign(window, { FeatureGate });


/* ===== Access intake form — business-entity application (no-access flow). =====
// Standalone full-viewport page. Collects business identity + tax/banking,
// validates required fields, then routes to the manual-review pending state. */

function AccessApplyForm({ onSubmit, onCancel }) {
  const RED = "#e1251b";
  const INK = "#0e0e10";
  const [vals, setVals] = React.useState({
    legalName: "", dba: "", entityType: "", address: "",
    ein: "", routing: "", account: "",
  });
  const [errors, setErrors] = React.useState({});
  const set = (k) => (e) => setVals((v) => ({ ...v, [k]: e.target.value }));

  const REQUIRED = ["legalName", "entityType", "address", "ein", "routing", "account"];
  const submit = () => {
    const errs = {};
    REQUIRED.forEach((k) => { if (!vals[k].trim()) errs[k] = true; });
    setErrors(errs);
    if (Object.keys(errs).length === 0) onSubmit(vals);
  };

  const baseField = {
    width: "100%", padding: "12px 16px", fontSize: 14, color: INK,
    background: "#fff", border: "1px solid #d9d9de", borderRadius: 6,
    outline: "none", transition: "border-color .14s ease, box-shadow .14s ease",
  };
  const field = (key, placeholder, opts = {}) => {
    const invalid = errors[key];
    return (
      <input
        value={vals[key]}
        onChange={set(key)}
        placeholder={placeholder}
        style={{
          ...baseField,
          ...(opts.mono ? { fontFamily: '"JetBrains Mono", monospace', letterSpacing: ".02em" } : null),
          borderColor: invalid ? RED : "#d9d9de",
          boxShadow: invalid ? "0 0 0 3px rgba(225,37,27,.12)" : "none",
        }}
        onFocus={(e) => { if (!invalid) { e.target.style.borderColor = RED; e.target.style.boxShadow = "0 0 0 3px rgba(225,37,27,.12)"; } }}
        onBlur={(e) => { if (!invalid) { e.target.style.borderColor = "#d9d9de"; e.target.style.boxShadow = "none"; } }}
      />
    );
  };
  const sectionLabel = (txt) => (
    <div style={{
      fontFamily: '"JetBrains Mono", monospace', fontSize: 11.5, fontWeight: 500,
      letterSpacing: ".2em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 16,
    }}>{txt}</div>
  );

  return (
    <div style={{
      position: "fixed", inset: 0, overflowY: "auto", background: "#f3f3f5",
      fontFamily: '"Inter", system-ui, sans-serif', color: INK,
    }}>
      <div style={{ maxWidth: 900, margin: "0 auto", padding: "64px 32px 80px" }}>
        {/* header */}
        <div style={{ display: "inline-flex", alignItems: "center", gap: 16, marginBottom: 24 }}>
          <BrandMark variant="black" height={26} />
          <span style={{ width: 1, height: 24, background: "#d4d4d8" }} />
          <span style={{
            fontFamily: '"JetBrains Mono", monospace', fontSize: 11.5, fontWeight: 500,
            letterSpacing: ".22em", textTransform: "uppercase", color: "#6b6b72",
          }}>Business Verification</span>
        </div>

        <h1 style={{
          margin: "0 0 16px", fontFamily: '"Archivo", sans-serif', fontWeight: 900,
          fontSize: "clamp(30px, 4vw, 46px)", lineHeight: 1.02, letterSpacing: "-0.02em",
          textTransform: "uppercase",
        }}>Apply for Team Stores Access</h1>
        <p style={{ margin: "0 0 36px", maxWidth: 640, color: "#6b6b72", fontSize: 15.5, lineHeight: 1.5 }}>
          Provide your business-entity details. We verify your business instantly through
          <strong style={{ color: INK, fontWeight: 700 }}> Stripe</strong> &mdash; there&apos;s no waiting period.
          Once it&apos;s confirmed you go straight into your dashboard.
        </p>

        {/* card */}
        <div style={{
          background: "#fff", border: "1px solid #e5e5e9", borderRadius: 10,
          boxShadow: "0 1px 2px rgba(20,20,30,.04)", padding: "36px 40px 40px",
        }}>
          {sectionLabel("Business Identity")}
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            {field("legalName", "Legal business name")}
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
              {field("dba", "DBA / trade name (optional)")}
              {field("entityType", "Entity type (LLC, S-Corp…)")}
            </div>
            {field("address", "Registered business address")}
          </div>

          <div style={{ height: 1, background: "#ececf0", margin: "32px 0 28px" }} />

          {sectionLabel("Tax & Banking")}
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
              {field("ein", "EIN (XX-XXXXXXX)", { mono: true })}
              {field("routing", "Bank routing number", { mono: true })}
            </div>
            {field("account", "Bank account number", { mono: true })}
          </div>

          <div style={{
            display: "flex", alignItems: "center", gap: 12, marginTop: 24,
            background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 6, padding: "12px 16px",
          }}>
            <IconLock size={16} style={{ color: "#1f9d55", flexShrink: 0 }} />
            <span style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.4 }}>
              Banking &amp; EIN details are encrypted and used only for payout verification.
            </span>
          </div>

          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: 32 }}>
            <button onClick={onCancel} style={{
              background: "#fff", border: "1px solid #d4d4d8", color: INK,
              padding: "12px 28px", fontSize: 14, fontWeight: 700, borderRadius: 6, cursor: "pointer",
              transition: "background .14s ease, border-color .14s ease",
            }}
              onMouseEnter={(e) => { e.currentTarget.style.background = "#f7f7f8"; e.currentTarget.style.borderColor = "#b8b8be"; }}
              onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.borderColor = "#d4d4d8"; }}>
              Cancel
            </button>
            <button onClick={submit} style={{
              display: "inline-flex", alignItems: "center", gap: 12, background: INK, color: "#fff",
              border: "none", padding: "12px 28px", fontSize: 14, fontWeight: 700, borderRadius: 6,
              cursor: "pointer", transition: "background .14s ease",
            }}
              onMouseEnter={(e) => { e.currentTarget.style.background = "#000"; }}
              onMouseLeave={(e) => { e.currentTarget.style.background = INK; }}>
              Submit &amp; Verify
              <IconArrowRight size={16} />
            </button>
          </div>
        </div>
      </div>
    </div>
  );
}

/* ===== Access pending — manual review waiting state (no-access flow). =====
// Shown for the 24–48 hr admin review window after an application is submitted.
// A returning unverified user lands here until a decision is made. */

function AccessPending({ onApprove, onBack, email }) {
  const RED = "#e1251b";
  return (
    <div style={{
      position: "fixed", inset: 0, overflowY: "auto", background: "#0c0c0e", color: "#fff",
      fontFamily: '"Inter", system-ui, sans-serif',
      display: "flex", alignItems: "center", justifyContent: "center", padding: 40,
    }}>
      <div style={{ textAlign: "center", maxWidth: 620 }}>
        {/* spinner + clock */}
        <div style={{ position: "relative", width: 88, height: 88, margin: "0 auto 40px" }}>
          <div style={{
            position: "absolute", inset: 0, borderRadius: "50%",
            border: "2px solid rgba(255,255,255,.10)",
          }} />
          <div style={{
            position: "absolute", inset: 0, borderRadius: "50%",
            border: "2px solid transparent", borderTopColor: RED, borderRightColor: RED,
            animation: "accSpin 1.1s linear infinite",
          }} />
          <div style={{
            position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center",
            color: RED,
          }}>
            <IconClock size={26} />
          </div>
        </div>

        <div style={{
          fontFamily: '"JetBrains Mono", monospace', fontSize: 12, fontWeight: 500,
          letterSpacing: ".26em", textTransform: "uppercase", color: "rgba(255,255,255,.5)", marginBottom: 20,
        }}>Application Received</div>

        <h1 style={{
          margin: "0 0 24px", fontFamily: '"Archivo", sans-serif', fontWeight: 900,
          fontSize: "clamp(36px, 5vw, 58px)", lineHeight: 0.98, letterSpacing: "-0.01em",
          textTransform: "uppercase",
        }}>Your Access Is<br />Under Review</h1>

        <p style={{
          margin: "0 auto 36px", maxWidth: 520, color: "rgba(255,255,255,.62)",
          fontSize: 16, lineHeight: 1.55, textWrap: "pretty",
        }}>
          A PROLOOK admin is manually verifying your business entity, EIN, and banking
          details. This typically takes <strong style={{ color: "#fff", fontWeight: 700 }}>24&ndash;48 hours</strong>.
          We&apos;ll email <strong style={{ color: "#fff", fontWeight: 700 }}>{email || "jane@prolook.com"}</strong> the moment a decision is made.
        </p>

        <div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: 16 }}>
          <button onClick={onBack} style={{
            background: "rgba(255,255,255,.04)", border: "1px solid rgba(255,255,255,.22)", color: "#fff",
            padding: "12px 28px", fontSize: 14, fontWeight: 700, borderRadius: 6, cursor: "pointer",
            transition: "background .14s ease, border-color .14s ease",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.background = "rgba(255,255,255,.1)"; e.currentTarget.style.borderColor = "rgba(255,255,255,.4)"; }}
            onMouseLeave={(e) => { e.currentTarget.style.background = "rgba(255,255,255,.04)"; e.currentTarget.style.borderColor = "rgba(255,255,255,.22)"; }}>
            Back to PROLOOK
          </button>
          <button onClick={onApprove} style={{
            display: "inline-flex", alignItems: "center", gap: 12, background: RED, color: "#fff",
            border: "none", padding: "12px 28px", fontSize: 14, fontWeight: 700, borderRadius: 6,
            cursor: "pointer", transition: "transform .14s ease, background .14s ease",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.background = "#c41d14"; e.currentTarget.style.transform = "translateY(-1px)"; }}
            onMouseLeave={(e) => { e.currentTarget.style.background = RED; e.currentTarget.style.transform = "none"; }}>
            Simulate admin approval
            <IconCheck size={16} style={{ strokeWidth: 3 }} />
          </button>
        </div>

        <div style={{
          fontFamily: '"JetBrains Mono", monospace', fontSize: 11, letterSpacing: ".14em",
          textTransform: "uppercase", color: "rgba(255,255,255,.32)", marginTop: 32,
        }}>Prototype &mdash; approval simulated for demo</div>
      </div>
      <style>{`@keyframes accSpin { to { transform: rotate(360deg) } }`}</style>
    </div>
  );
}

/* ===== Stripe verification — automated approval (no-access flow). =====
// Replaces the old manual-review waiting state. Business details are verified
// instantly via Stripe; this brief step animates the check, then hands straight
// off to the dashboard. No 24–48 hr wait. */
function StripeVerifying({ onDone }) {
  const STRIPE = "#635bff";
  const [done, setDone] = React.useState(false);
  React.useEffect(() => {
    const t1 = setTimeout(() => setDone(true), 1500);
    const t2 = setTimeout(() => { onDone && onDone(); }, 2400);
    return () => { clearTimeout(t1); clearTimeout(t2); };
  }, []);
  return (
    <div style={{
      position: "fixed", inset: 0, overflowY: "auto", background: "#0c0c0e", color: "#fff",
      fontFamily: '"Inter", system-ui, sans-serif',
      display: "flex", alignItems: "center", justifyContent: "center", padding: 40,
    }}>
      <div style={{ textAlign: "center", maxWidth: 560 }}>
        <div style={{ position: "relative", width: 88, height: 88, margin: "0 auto 40px" }}>
          {!done && (
            <React.Fragment>
              <div style={{ position: "absolute", inset: 0, borderRadius: "50%", border: "2px solid rgba(255,255,255,.10)" }} />
              <div style={{ position: "absolute", inset: 0, borderRadius: "50%", border: "2px solid transparent", borderTopColor: STRIPE, borderRightColor: STRIPE, animation: "accSpin 0.9s linear infinite" }} />
            </React.Fragment>
          )}
          {done && (
            <span style={{ position: "absolute", inset: 0, borderRadius: "50%", background: "#1f9d55", display: "flex", alignItems: "center", justifyContent: "center", animation: "accPop .35s cubic-bezier(.2,1.2,.4,1) both" }}>
              <IconCheck size={38} style={{ strokeWidth: 3 }} />
            </span>
          )}
          {!done && (
            <div style={{ position: "absolute", inset: 0, display: "flex", alignItems: "center", justifyContent: "center", color: STRIPE }}>
              <IconLock size={26} />
            </div>
          )}
        </div>

        <div style={{
          fontFamily: '"JetBrains Mono", monospace', fontSize: 12, fontWeight: 500,
          letterSpacing: ".26em", textTransform: "uppercase", color: "rgba(255,255,255,.5)", marginBottom: 20,
        }}>{done ? "Verified" : "Verifying with Stripe"}</div>

        <h1 style={{
          margin: "0 0 24px", fontFamily: '"Archivo", sans-serif', fontWeight: 900,
          fontSize: "clamp(32px, 4.5vw, 52px)", lineHeight: 0.98, letterSpacing: "-0.01em",
          textTransform: "uppercase",
        }}>{done ? "You're All Set" : "Verifying Your Business"}</h1>

        <p style={{ margin: "0 auto", maxWidth: 460, color: "rgba(255,255,255,.62)", fontSize: 16, lineHeight: 1.55, textWrap: "pretty" }}>
          {done
            ? "Your business is verified. Opening your Team Stores dashboard\u2026"
            : "We\u2019re confirming your business entity and banking details through Stripe. This only takes a moment \u2014 no manual review, no waiting."}
        </p>
      </div>
      <style>{`@keyframes accSpin { to { transform: rotate(360deg) } } @keyframes accPop { from { transform: scale(.3); opacity: 0 } to { transform: scale(1); opacity: 1 } }`}</style>
    </div>
  );
}

Object.assign(window, { AccessApplyForm, AccessPending, StripeVerifying });


/* ===== ASSET 9 : ed813c4c-4804-4e3e-8d11-c83d3eaa34de ===== */
// League feature — Create New League 3-step modal + league filter pills.
// Lets a rep group multiple team stores into an organizational "league".

const LEAGUE_GREEN = "#1f9d55";
const LEAGUE_GREEN_SOFT = "#e7f5ed";

// ─────────── Step indicator ───────────
function LeagueStepper({ step }) {
  const steps = ["Basic Info", "Stores & Groups", "Access"];
  return (
    <div style={{ display: "flex", alignItems: "center", padding: "16px 28px 0" }}>
      {steps.map((label, i) => {
        const n = i + 1;
        const done = step > n;
        const current = step === n;
        const active = done || current;
        return (
          <React.Fragment key={label}>
            <div style={{ display: "flex", alignItems: "center", gap: 12, flexShrink: 0 }}>
              <div style={{
                width: 28, height: 28, borderRadius: "50%", flexShrink: 0,
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                background: active ? LEAGUE_GREEN : "#fff",
                border: `1.5px solid ${active ? LEAGUE_GREEN : "#d4d4d8"}`,
                color: active ? "#fff" : "#9a9aa1",
                fontSize: 13, fontWeight: 700,
                boxShadow: current ? `0 0 0 4px ${LEAGUE_GREEN_SOFT}` : "none",
                transition: "all .15s ease"
              }}>
                {done ? <IconCheck size={15} style={{ strokeWidth: 3 }} /> : n}
              </div>
              <span style={{
                fontSize: 14, fontWeight: 600,
                color: active ? "#0e0e10" : "#9a9aa1", whiteSpace: "nowrap"
              }}>{label}</span>
            </div>
            {i < steps.length - 1 &&
            <div style={{
              flex: 1, height: 1.5, margin: "0 16px",
              background: step > n ? LEAGUE_GREEN : "#e5e5e9",
              transition: "background .2s ease"
            }} />
            }
          </React.Fragment>);

      })}
    </div>);

}

// ─────────── Store row (selectable) ───────────
function StoreSelectRow({ store, checked, onToggle }) {
  const STATE_ABBR = { California: "CA", Texas: "TX", "New York": "NY", Florida: "FL", Washington: "WA", Oregon: "OR", Nevada: "NV", Arizona: "AZ", Colorado: "CO" };
  const city = store.shipCity || "Carlsbad";
  const stateAbbr = STATE_ABBR[store.shipState] || (store.shipState || "CA").slice(0, 2).toUpperCase();
  const name = store.storeTitle || store.teamName || "Team Store";
  const color = store.teamColors?.[0] || "#1d3a8a";
  const initial = name[0]?.toUpperCase() || "T";
  return (
    <label style={{
      display: "flex", alignItems: "center", gap: 12, padding: "12px 16px",
      borderBottom: "1px solid #f1f1f3", cursor: "pointer",
      background: checked ? "#fafafb" : "#fff", transition: "background .12s"
    }}
    onMouseEnter={(e) => {if (!checked) e.currentTarget.style.background = "#fcfcfd";}}
    onMouseLeave={(e) => {if (!checked) e.currentTarget.style.background = "#fff";}}>
      <Checkbox checked={checked} onChange={onToggle} size={18} />
      <div style={{
        width: 36, height: 36, borderRadius: "50%", flexShrink: 0,
        background: color, color: "#fff", fontWeight: 700, fontSize: 14,
        display: "inline-flex", alignItems: "center", justifyContent: "center"
      }}>{initial}</div>
      <div style={{ minWidth: 0 }}>
        <div style={{ fontSize: 14, fontWeight: 600, color: "#0e0e10" }}>{name}</div>
        <div style={{ fontSize: 12, color: "#9a9aa1" }}>{city}, {stateAbbr}</div>
      </div>
    </label>);

}

// ─────────── The modal ───────────
function CreateLeagueModal({ open, stores, onClose, onCreate }) {
  const [step, setStep] = React.useState(1);
  // step 1
  const [name, setName] = React.useState("");
  const [logo, setLogo] = React.useState(null); // {url}
  // step 2
  const [query, setQuery] = React.useState("");
  const [selected, setSelected] = React.useState({}); // id -> true
  const [groups, setGroups] = React.useState([]);
  const [groupDraft, setGroupDraft] = React.useState("");
  // step 3
  const [invites, setInvites] = React.useState("");
  const [role, setRole] = React.useState("manager");

  // Reset everything when (re)opened
  React.useEffect(() => {
    if (open) {
      setStep(1);setName("");setLogo(null);setQuery("");setSelected({});
      setGroups([]);setGroupDraft("");setInvites("");setRole("manager");
    }
  }, [open]);

  const selectedIds = Object.keys(selected).filter((id) => selected[id]);
  const selectedCount = selectedIds.length;

  const filteredStores = stores.filter((s) => {
    const n = (s.storeTitle || s.teamName || "").toLowerCase();
    return n.includes(query.toLowerCase());
  });

  const fileRef = React.useRef();
  const onFile = (file) => {
    if (!file) return;
    setLogo({ url: URL.createObjectURL(file), name: file.name });
  };

  const addGroup = () => {
    const v = groupDraft.trim();
    if (!v) return;
    setGroups((g) => [...g, v]);
    setGroupDraft("");
  };

  const canContinue1 = name.trim().length > 0;
  const canContinue2 = selectedCount > 0;

  const submit = () => {
    const managerEmails = invites.split(",").map((e) => e.trim()).filter(Boolean);
    onCreate({
      id: "league-" + Date.now(),
      name: name.trim(),
      logo,
      storeIds: selectedIds,
      groups,
      managers: managerEmails,
      role
    });
  };

  if (!open) return null;

  const ROLE_LABEL = {
    manager: "Manager — manage stores & groups",
    admin: "Admin — full league control",
    viewer: "Viewer — read-only access"
  };

  return (
    <Modal open={open} onClose={onClose} width={720}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "86vh" }}>
        {/* Header */}
        <div style={{ padding: "28px 28px 0", position: "relative" }}>
          <button onClick={onClose} style={{
            position: "absolute", top: 24, right: 24, width: 32, height: 32,
            border: 0, background: "transparent", borderRadius: 6, cursor: "pointer",
            display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#6b6b72"
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#f1f1f3"}
          onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
            <IconX size={18} />
          </button>
          <h2 style={{ fontSize: 22, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Create New League</h2>
          <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 0" }}>
            Set up an organizational container to manage multiple team stores together.
          </p>
        </div>

        <LeagueStepper step={step} />

        <div style={{ height: 1, background: "#ececf0", margin: "16px 0 0" }} />

        {/* Body (scrollable) */}
        <div style={{ padding: "24px 28px", overflowY: "auto", flex: 1 }}>
          {step === 1 &&
          <StepBasic
            name={name} setName={setName}
            logo={logo} fileRef={fileRef} onFile={onFile} setLogo={setLogo} />

          }
          {step === 2 &&
          <StepStores
            query={query} setQuery={setQuery}
            filteredStores={filteredStores} selected={selected}
            onToggle={(id) => setSelected((s) => ({ ...s, [id]: !s[id] }))}
            selectedCount={selectedCount}
            groups={groups} setGroups={setGroups}
            groupDraft={groupDraft} setGroupDraft={setGroupDraft} addGroup={addGroup} />

          }
          {step === 3 &&
          <StepAccess
            invites={invites} setInvites={setInvites}
            role={role} setRole={setRole} roleLabel={ROLE_LABEL}
            summary={{ name: name.trim() || "Untitled League", stores: selectedCount, groups: groups.length }} />

          }
        </div>

        {/* Footer */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          padding: "16px 28px", borderTop: "1px solid #ececf0", gap: 12
        }}>
          <div>
            {step > 1 &&
            <button onClick={() => setStep((s) => s - 1)} style={{
              border: 0, background: "transparent", cursor: "pointer",
              fontSize: 14, fontWeight: 600, color: "#0e0e10",
              display: "inline-flex", alignItems: "center", gap: 8, padding: "8px 4px"
            }}>
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M19 12H5" /><path d="M12 19l-7-7 7-7" /></svg>
                Back
              </button>
            }
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <button onClick={onClose} style={{
              border: "1px solid #d4d4d8", background: "#fff", color: "#0e0e10",
              padding: "12px 24px", borderRadius: 6, cursor: "pointer",
              fontSize: 14, fontWeight: 600
            }}>Cancel</button>
            {step < 3 ?
            <GreenButton
              disabled={step === 1 ? !canContinue1 : !canContinue2}
              onClick={() => setStep((s) => s + 1)}>
                Continue
              </GreenButton> :

            <GreenButton onClick={submit}>Create League</GreenButton>
            }
          </div>
        </div>
      </div>
    </Modal>);

}

// Green primary action button (league accent)
function GreenButton({ children, onClick, disabled }) {
  return (
    <button onClick={disabled ? undefined : onClick} disabled={disabled} style={{
      border: 0, borderRadius: 6, padding: "12px 28px",
      background: disabled ? "#a8d8bd" : LEAGUE_GREEN, color: "#fff",
      fontSize: 14, fontWeight: 700, cursor: disabled ? "not-allowed" : "pointer",
      boxShadow: disabled ? "none" : "0 1px 2px rgba(31,157,85,.4)",
      transition: "background .15s"
    }}
    onMouseEnter={(e) => {if (!disabled) e.currentTarget.style.background = "#1b8a4a";}}
    onMouseLeave={(e) => {if (!disabled) e.currentTarget.style.background = LEAGUE_GREEN;}}>
      {children}
    </button>);

}

// ─────────── Step 1: Basic Info ───────────
function StepBasic({ name, setName, logo, fileRef, onFile, setLogo }) {
  const [drag, setDrag] = React.useState(false);
  return (
    <div>
      <FieldLabel>League Name</FieldLabel>
      <input
        autoFocus
        value={name}
        onChange={(e) => setName(e.target.value)}
        placeholder="e.g. Cougars Athletic Association"
        style={{
          width: "100%", padding: "12px 16px", fontSize: 15, borderRadius: 6,
          border: `1.5px solid ${name ? LEAGUE_GREEN : "#d4d4d8"}`, outline: "none",
          boxShadow: name ? `0 0 0 3px ${LEAGUE_GREEN_SOFT}` : "none", transition: "all .12s"
        }}
        onFocus={(e) => {e.target.style.borderColor = LEAGUE_GREEN;e.target.style.boxShadow = `0 0 0 3px ${LEAGUE_GREEN_SOFT}`;}}
        onBlur={(e) => {if (!name) {e.target.style.borderColor = "#d4d4d8";e.target.style.boxShadow = "none";}}} />
      
      <HelpText>This is how your league appears across all dashboards.</HelpText>

      <FieldLabel style={{ marginTop: 24 }}>League Logo</FieldLabel>
      <input ref={fileRef} type="file" accept="image/*" style={{ display: "none" }}
      onChange={(e) => onFile(e.target.files?.[0])} />
      <div
        onClick={() => fileRef.current?.click()}
        onDragOver={(e) => {e.preventDefault();setDrag(true);}}
        onDragLeave={() => setDrag(false)}
        onDrop={(e) => {e.preventDefault();setDrag(false);onFile(e.dataTransfer.files?.[0]);}}
        style={{
          border: `1.5px dashed ${drag ? LEAGUE_GREEN : "#d4d4d8"}`, borderRadius: 8,
          padding: "32px 20px", textAlign: "center", cursor: "pointer",
          background: drag ? LEAGUE_GREEN_SOFT : "#fafafb", transition: "all .12s"
        }}>
        {logo ?
        <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 12 }}>
            <img src={logo.url} alt="" style={{ width: 72, height: 72, objectFit: "contain", borderRadius: 8 }} />
            <div style={{ fontSize: 13, color: "#0e0e10", fontWeight: 600 }}>{logo.name}</div>
            <button onClick={(e) => {e.stopPropagation();setLogo(null);}} style={{
            border: 0, background: "transparent", color: "#b81d14", fontSize: 13, fontWeight: 600, cursor: "pointer",
            display: "inline-flex", alignItems: "center", gap: 8
          }}><IconTrash size={14} /> Remove</button>
          </div> :

        <React.Fragment>
            <div style={{
            width: 48, height: 48, borderRadius: "50%", background: "#ececf0", margin: "0 auto 16px",
            display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#6b6b72"
          }}>
              <IconUpload size={22} />
            </div>
            <div style={{ fontSize: 14, color: "#0e0e10", fontWeight: 500 }}>Drop your logo here, or click to browse</div>
            <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 4 }}>PNG, JPG or SVG — recommended 512×512px</div>
          </React.Fragment>
        }
      </div>
    </div>);

}

// ─────────── Step 2: Stores & Groups ───────────
function StepStores({ query, setQuery, filteredStores, selected, onToggle, selectedCount, groups, setGroups, groupDraft, setGroupDraft, addGroup }) {
  return (
    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 28 }}>
      {/* Left: stores */}
      <div>
        <SectionHead title="Add Team Stores" sub="Select stores to include in this league." />
        <div style={{
          position: "relative", border: "1px solid #d4d4d8", borderRadius: 6,
          display: "flex", alignItems: "center", marginBottom: 12
        }}>
          <IconSearch size={16} style={{ position: "absolute", left: 12, color: "#9a9aa1" }} />
          <input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search stores..." style={{
            width: "100%", border: 0, outline: 0, background: "transparent", padding: "12px 12px 12px 36px", fontSize: 14
          }} />
        </div>
        <div style={{ border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden", maxHeight: 280, overflowY: "auto" }}>
          {filteredStores.length === 0 ?
          <div style={{ padding: 24, textAlign: "center", color: "#9a9aa1", fontSize: 13 }}>No stores match your search.</div> :
          filteredStores.map((s) =>
          <StoreSelectRow key={s.id} store={s} checked={!!selected[s.id]} onToggle={() => onToggle(s.id)} />
          )}
        </div>
        <div style={{ fontSize: 12, color: "#6b6b72", marginTop: 12 }}>{selectedCount} selected</div>
      </div>

      {/* Right: groups */}
      <div>
        <SectionHead title="Create Groups" sub="Organize stores by sport or season. Optional." />
        <div style={{ display: "flex", gap: 12, marginBottom: 12 }}>
          <input
            value={groupDraft}
            onChange={(e) => setGroupDraft(e.target.value)}
            onKeyDown={(e) => {if (e.key === "Enter") {e.preventDefault();addGroup();}}}
            placeholder="e.g. Boys Basketball"
            style={{ flex: 1, border: "1px solid #d4d4d8", borderRadius: 6, outline: 0, padding: "12px 12px", fontSize: 14 }} />
          
          <button onClick={addGroup} style={{
            border: "1px solid #d4d4d8", background: "#fff", borderRadius: 6, padding: "0 16px",
            fontSize: 14, fontWeight: 600, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, color: "#0e0e10"
          }}><IconPlus size={15} /> Add</button>
        </div>
        {groups.length === 0 ?
        <div style={{
          border: "1.5px dashed #d4d4d8", borderRadius: 8, padding: "28px 16px",
          textAlign: "center", color: "#9a9aa1", fontSize: 13
        }}>No groups yet — they're optional.</div> :

        <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
            {groups.map((g, i) =>
          <div key={i} style={{
            display: "flex", alignItems: "center", justifyContent: "space-between",
            border: "1px solid #ececf0", borderRadius: 6, padding: "12px 12px", background: "#fff"
          }}>
                <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 14, color: "#0e0e10" }}>
                  <IconLayers size={15} style={{ color: LEAGUE_GREEN }} /> {g}
                </span>
                <button onClick={() => setGroups((arr) => arr.filter((_, idx) => idx !== i))} style={{
              border: 0, background: "transparent", cursor: "pointer", color: "#9a9aa1", padding: 4,
              display: "inline-flex"
            }}><IconX size={15} /></button>
              </div>
          )}
          </div>
        }
      </div>
    </div>);

}

// ─────────── Step 3: Access ───────────
function StepAccess({ invites, setInvites, role, setRole, roleLabel, summary }) {
  return (
    <div>
      <FieldLabel>Invite League Managers</FieldLabel>
      <textarea
        value={invites}
        onChange={(e) => setInvites(e.target.value)}
        placeholder="jane@example.com, john@example.com"
        rows={3}
        style={{
          width: "100%", border: "1px solid #d4d4d8", borderRadius: 6, outline: 0,
          padding: "12px 16px", fontSize: 14, resize: "vertical", fontFamily: "inherit", lineHeight: 1.5
        }} />
      
      <HelpText>Separate multiple emails with commas. Invitations are sent immediately.</HelpText>

      <FieldLabel style={{ marginTop: 24 }}>Default Role for Invitees</FieldLabel>
      <div style={{ position: "relative" }}>
        <select value={role} onChange={(e) => setRole(e.target.value)} style={{
          width: "100%", border: "1px solid #d4d4d8", borderRadius: 6, outline: 0,
          padding: "12px 40px 12px 16px", fontSize: 14, background: "#fff",
          appearance: "none", cursor: "pointer", color: "#0e0e10"
        }}>
          <option value="manager">{roleLabel.manager}</option>
          <option value="admin">{roleLabel.admin}</option>
          <option value="viewer">{roleLabel.viewer}</option>
        </select>
        <IconChevron size={18} style={{ position: "absolute", right: 16, top: "50%", transform: "translateY(-50%)", color: "#6b6b72", pointerEvents: "none" }} />
      </div>
      <HelpText>You can change individual roles later in League Settings.</HelpText>

      {/* Summary */}
      <div style={{
        marginTop: 24, border: "1px solid #ececf0", borderRadius: 8,
        background: "#fafafb", padding: "16px 16px"
      }}>
        <div style={{ display: "inline-flex", alignItems: "center", gap: 8, fontWeight: 700, fontSize: 14, color: "#0e0e10", marginBottom: 12 }}>
          <IconTrophy size={17} style={{ color: LEAGUE_GREEN }} /> Summary
        </div>
        <SummaryRow label="League:" value={summary.name} />
        <SummaryRow label="Team stores:" value={summary.stores} />
        <SummaryRow label="Groups:" value={summary.groups} />
      </div>
    </div>);

}

function SummaryRow({ label, value }) {
  return (
    <div style={{ fontSize: 14, color: "#6b6b72", marginTop: 4 }}>
      {label} <span style={{ color: "#0e0e10", fontWeight: 600 }}>{value}</span>
    </div>);

}

// ─────────── small helpers ───────────
function FieldLabel({ children, style }) {
  return <div style={{ fontSize: 14, fontWeight: 600, color: "#0e0e10", marginBottom: 8, ...style }}>{children}</div>;
}
function HelpText({ children }) {
  return <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 8 }}>{children}</div>;
}
function SectionHead({ title, sub }) {
  return (
    <div style={{ marginBottom: 16 }}>
      <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>{title}</div>
      <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 2 }}>{sub}</div>
    </div>);

}

// ─────────── League filter pills (shown on the overview once leagues exist) ───────────
function LeaguePills({ leagues, activeLeagueId, onSelect }) {
  if (!leagues.length) return null;
  const pill = (key, label, active, onClick) =>
  <button key={key} onClick={onClick} style={{
    border: `1px solid ${active ? "#0e0e10" : "#d4d4d8"}`,
    background: active ? "#0e0e10" : "#fff",
    color: active ? "#fff" : "#0e0e10",
    borderRadius: 999, padding: "8px 24px", fontSize: 13, fontWeight: 700,
    letterSpacing: ".02em", cursor: "pointer", whiteSpace: "nowrap",
    transition: "all .12s"
  }}>{label}</button>;

  return (
    <div style={{ display: "flex", gap: 12, flexWrap: "wrap", marginBottom: 24 }}>
      {pill("all", "All Stores", !activeLeagueId, () => onSelect(null))}
      {leagues.map((lg) => pill(lg.id, lg.name, activeLeagueId === lg.id, () => onSelect(lg.id)))}
    </div>);

}

Object.assign(window, { CreateLeagueModal, LeaguePills, CreateGroupModal });

// ═══════════════ Create New League Group modal ═══════════════
// Opened from within a league tab. Lets the rep name a group and pick which
// of the league's still-ungrouped team stores belong to it.

function GroupStoreSelectRow({ store, checked, onToggle }) {
  const STATE_ABBR = { California: "CA", Texas: "TX", "New York": "NY", Florida: "FL", Washington: "WA", Oregon: "OR", Nevada: "NV", Arizona: "AZ", Colorado: "CO" };
  const name = store.storeTitle || store.teamName || "Team Store";
  const sport = store.sport || "Sport";
  const color = store.teamColors?.[0] || "#c9c9cf";
  const initial = (store.teamName || store.storeTitle || "T").slice(0, 2).toUpperCase();
  return (
    <button onClick={onToggle} style={{
      display: "flex", alignItems: "center", gap: 12, width: "100%", textAlign: "left",
      padding: "12px 12px", borderRadius: 8, cursor: "pointer",
      border: `1.5px solid ${checked ? LEAGUE_GREEN : "#e5e5e9"}`,
      background: checked ? LEAGUE_GREEN_SOFT : "#fff",
      transition: "border-color .12s, background .12s",
    }}
      onMouseEnter={(e) => { if (!checked) e.currentTarget.style.borderColor = "#c9c9cf"; }}
      onMouseLeave={(e) => { if (!checked) e.currentTarget.style.borderColor = "#e5e5e9"; }}>
      <div style={{
        width: 40, height: 40, borderRadius: "50%", flexShrink: 0,
        background: color, color: "#fff", fontWeight: 700, fontSize: 13,
        display: "inline-flex", alignItems: "center", justifyContent: "center",
      }}>{initial}</div>
      <div style={{ minWidth: 0, flex: 1 }}>
        <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{name}</div>
        <div style={{ fontSize: 12.5, color: "#9a9aa1" }}>{sport}</div>
      </div>
      <span style={{
        width: 24, height: 24, borderRadius: "50%", flexShrink: 0,
        border: checked ? "none" : "1.5px solid #d4d4d8",
        background: checked ? LEAGUE_GREEN : "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center",
        transition: "all .12s",
      }}>
        {checked && <IconCheck size={13} style={{ color: "#fff", strokeWidth: 3.5 }} />}
      </span>
    </button>
  );
}

function CreateGroupModal({ open, league, stores, onClose, onCreate }) {
  const [name, setName] = React.useState("");
  const [selected, setSelected] = React.useState({});

  React.useEffect(() => {
    if (open) { setName(""); setSelected({}); }
  }, [open]);

  if (!open || !league) return null;

  const assignedIds = new Set((league.groups || []).flatMap((g) => g.storeIds || []));
  const available = stores.filter((s) => league.storeIds.includes(s.id) && !assignedIds.has(s.id));

  const selectedIds = Object.keys(selected).filter((id) => selected[id]);
  const count = selectedIds.length;
  const canCreate = name.trim().length > 0 && count > 0;

  const submit = () => {
    if (!canCreate) return;
    onCreate(league.id, { id: "group-" + Date.now(), name: name.trim(), storeIds: selectedIds });
  };

  return (
    <Modal open={open} onClose={onClose} width={760}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "86vh" }}>
        {/* Header */}
        <div style={{ padding: "28px 32px 20px", position: "relative" }}>
          <button onClick={onClose} style={{
            position: "absolute", top: 24, right: 24, width: 32, height: 32,
            border: 0, background: "transparent", borderRadius: 6, cursor: "pointer",
            display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#6b6b72",
          }}
            onMouseEnter={(e) => e.currentTarget.style.background = "#f1f1f3"}
            onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
            <IconX size={18} />
          </button>
          <h2 style={{ fontSize: 22, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Create New League Group</h2>
          <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 0", maxWidth: 560, lineHeight: 1.5 }}>
            Groups allow you to organize individual team stores by sport or season within your league.
          </p>
        </div>

        {/* Body */}
        <div style={{ padding: "0 32px 8px", overflowY: "auto", flex: 1 }}>
          <FieldLabel>Group Name</FieldLabel>
          <input
            autoFocus
            value={name}
            onChange={(e) => setName(e.target.value)}
            placeholder="e.g. Boys Basketball"
            style={{
              width: "100%", padding: "12px 16px", fontSize: 15, borderRadius: 6,
              border: `1.5px solid ${name ? LEAGUE_GREEN : "#d4d4d8"}`, outline: "none",
              boxShadow: name ? `0 0 0 3px ${LEAGUE_GREEN_SOFT}` : "none", transition: "all .12s",
            }}
            onFocus={(e) => { e.target.style.borderColor = LEAGUE_GREEN; e.target.style.boxShadow = `0 0 0 3px ${LEAGUE_GREEN_SOFT}`; }}
            onBlur={(e) => { if (!name) { e.target.style.borderColor = "#d4d4d8"; e.target.style.boxShadow = "none"; } }}
          />

          <div style={{ marginTop: 28 }}>
            <div style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10" }}>Add team stores</div>
            <div style={{ fontSize: 13.5, color: "#9a9aa1", marginTop: 4 }}>
              These stores are currently in <b style={{ color: "#6b6b72" }}>{league.name}</b> but not assigned to any group.
            </div>
          </div>

          <div style={{
            marginTop: 16, display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12,
          }}>
            {available.length === 0 ? (
              <div style={{
                gridColumn: "1 / -1", padding: "32px 16px", textAlign: "center",
                color: "#9a9aa1", fontSize: 13.5, border: "1.5px dashed #e5e5e9", borderRadius: 8,
              }}>
                Every store in this league is already assigned to a group.
              </div>
            ) : available.map((s) => (
              <GroupStoreSelectRow
                key={s.id}
                store={s}
                checked={!!selected[s.id]}
                onToggle={() => setSelected((prev) => ({ ...prev, [s.id]: !prev[s.id] }))}
              />
            ))}
          </div>
        </div>

        {/* Footer */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          padding: "16px 32px", borderTop: "1px solid #ececf0", gap: 12, marginTop: 16,
        }}>
          <div style={{ fontSize: 14, color: "#6b6b72" }}>
            <b style={{ color: "#0e0e10", fontWeight: 700 }}>{count}</b> selected
          </div>
          <button onClick={submit} disabled={!canCreate} style={{
            border: 0, borderRadius: 6, padding: "12px 28px",
            background: canCreate ? "#0e0e10" : "#e5e5e9",
            color: canCreate ? "#fff" : "#9a9aa1",
            fontSize: 14, fontWeight: 700, cursor: canCreate ? "pointer" : "not-allowed",
            transition: "background .15s",
          }}>Create Group</button>
        </div>
      </div>
    </Modal>
  );
}

/* ===== ASSET 10 : eaddf260-0dfd-4f79-832f-6d421c1464dc ===== */
// League Settings — full-page editor reached from the "League Settings" button
// on a league tab. Edit profile, manage members/roles, and league lifecycle.

const LS_GREEN = "#1f9d55";

// Floating-label outlined input (Material-style notch).
function FloatingField({ label, value, onChange, placeholder }) {
  const [focus, setFocus] = React.useState(false);
  return (
    <div style={{ position: "relative" }}>
      <label style={{
        position: "absolute", top: -8, left: 12, padding: "0 8px",
        background: "#fff", fontSize: 12.5, fontWeight: 500,
        color: focus ? "#0e0e10" : "#6b6b72", letterSpacing: ".01em",
      }}>{label}</label>
      <input
        value={value}
        placeholder={placeholder}
        onChange={(e) => onChange(e.target.value)}
        onFocus={() => setFocus(true)}
        onBlur={() => setFocus(false)}
        style={{
          width: "100%", padding: "16px 16px", fontSize: 15, borderRadius: 8,
          border: `1.5px solid ${focus ? "#0e0e10" : "#d4d4d8"}`, outline: "none",
          color: "#0e0e10", background: "#fff", transition: "border-color .12s",
        }}
      />
    </div>
  );
}

// Avatar disc with initials (or uploaded image).
function LeagueAvatar({ logo, name, color = "#1d3a8a", size = 110 }) {
  const initials = (name || "League").trim().split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase() || "LG";
  if (logo?.url) {
    return <img src={logo.url} alt="" style={{ width: size, height: size, borderRadius: "50%", objectFit: "cover" }} />;
  }
  return (
    <div style={{
      width: size, height: size, borderRadius: "50%", background: color,
      color: "#fff", fontWeight: 700, fontSize: size * 0.3,
      display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0,
    }}>{initials}</div>
  );
}

// ─── Role badge ───
function RoleBadge({ role }) {
  const map = {
    Owner: { bg: LS_GREEN, color: "#fff" },
    Admin: { bg: "#f1f1f3", color: "#3a3a40" },
    Manager: { bg: "#f1f1f3", color: "#3a3a40" },
    Viewer: { bg: "#f1f1f3", color: "#3a3a40" },
  };
  const s = map[role] || map.Manager;
  return (
    <span style={{
      display: "inline-block", padding: "4px 16px", borderRadius: 999,
      fontSize: 13, fontWeight: 600, background: s.bg, color: s.color,
    }}>{role}</span>
  );
}

// ─── Member row action menu ───
function MemberMenu({ member, onRole, onRemove }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef();
  React.useEffect(() => {
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const isOwner = member.role === "Owner";
  return (
    <div ref={ref} style={{ position: "relative", display: "inline-block" }}>
      <button onClick={() => setOpen((v) => !v)} style={{
        border: 0, background: "transparent", cursor: "pointer", color: "#6b6b72",
        width: 32, height: 32, borderRadius: 6, display: "inline-flex", alignItems: "center", justifyContent: "center",
      }}
        onMouseEnter={(e) => e.currentTarget.style.background = "#f1f1f3"}
        onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
        <span style={{ display: "inline-flex", gap: 4 }}>
          {[0, 1, 2].map((i) => <span key={i} style={{ width: 4, height: 4, borderRadius: "50%", background: "currentColor" }} />)}
        </span>
      </button>
      {open && (
        <div style={{
          position: "absolute", top: 40, right: 0, zIndex: 10,
          background: "#fff", border: "1px solid #ececf0", borderRadius: 8,
          boxShadow: "0 8px 24px rgba(20,20,30,.12)", padding: 4, minWidth: 168,
        }}>
          <div style={{ fontSize: 11, fontWeight: 700, color: "#9a9aa1", padding: "8px 12px 4px", letterSpacing: ".04em" }}>SET ROLE</div>
          {["Admin", "Manager", "Viewer"].map((r) => (
            <div key={r} onClick={() => { onRole(r); setOpen(false); }} style={{
              padding: "8px 12px", fontSize: 13, cursor: "pointer", borderRadius: 5,
              display: "flex", alignItems: "center", justifyContent: "space-between", color: "#0e0e10",
            }}
              onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
              onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
              {r}
              {member.role === r && <IconCheck size={14} style={{ color: LS_GREEN, strokeWidth: 3 }} />}
            </div>
          ))}
          {!isOwner && (
            <React.Fragment>
              <div style={{ height: 1, background: "#f1f1f3", margin: "4px 0" }} />
              <div onClick={() => { onRemove(); setOpen(false); }} style={{
                padding: "8px 12px", fontSize: 13, cursor: "pointer", borderRadius: 5, color: "#b81d14",
                display: "inline-flex", alignItems: "center", gap: 8, width: "100%",
              }}
                onMouseEnter={(e) => e.currentTarget.style.background = "#fdf2f1"}
                onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
                <IconTrash size={14} /> Remove
              </div>
            </React.Fragment>
          )}
        </div>
      )}
    </div>
  );
}

// ─── Add Manager modal ───
function AddManagerModal({ open, onClose, onAdd }) {
  const [name, setName] = React.useState("");
  const [email, setEmail] = React.useState("");
  const [role, setRole] = React.useState("Manager");
  React.useEffect(() => { if (open) { setName(""); setEmail(""); setRole("Manager"); } }, [open]);
  const canAdd = name.trim() && /\S+@\S+\.\S+/.test(email);
  return (
    <Modal open={open} onClose={onClose} width={460}>
      <div style={{ padding: "28px 28px" }}>
        <h2 style={{ fontSize: 20, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Add Manager</h2>
        <p style={{ fontSize: 13.5, color: "#6b6b72", margin: "8px 0 24px" }}>Invite someone to help manage this league.</p>
        <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
          <FloatingField label="Full Name" value={name} onChange={setName} placeholder="Jane Smith" />
          <FloatingField label="Email" value={email} onChange={setEmail} placeholder="jane@example.com" />
          <div style={{ position: "relative" }}>
            <label style={{ position: "absolute", top: -8, left: 12, padding: "0 8px", background: "#fff", fontSize: 12.5, color: "#6b6b72" }}>Role</label>
            <select value={role} onChange={(e) => setRole(e.target.value)} style={{
              width: "100%", padding: "16px 40px 16px 16px", fontSize: 15, borderRadius: 8,
              border: "1.5px solid #d4d4d8", outline: "none", appearance: "none", background: "#fff", cursor: "pointer",
            }}>
              <option>Admin</option>
              <option>Manager</option>
              <option>Viewer</option>
            </select>
            <IconChevron size={18} style={{ position: "absolute", right: 16, top: "50%", transform: "translateY(-50%)", color: "#6b6b72", pointerEvents: "none" }} />
          </div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
          <button onClick={onClose} style={{
            border: "1px solid #d4d4d8", background: "#fff", color: "#0e0e10",
            padding: "12px 24px", borderRadius: 6, cursor: "pointer", fontSize: 14, fontWeight: 600,
          }}>Cancel</button>
          <button onClick={() => canAdd && onAdd({ name: name.trim(), email: email.trim(), role })} disabled={!canAdd} style={{
            border: 0, borderRadius: 6, padding: "12px 24px",
            background: canAdd ? "#0e0e10" : "#e5e5e9", color: canAdd ? "#fff" : "#9a9aa1",
            fontSize: 14, fontWeight: 700, cursor: canAdd ? "pointer" : "not-allowed",
          }}>Send Invite</button>
        </div>
      </div>
    </Modal>
  );
}

// ─── Card shell ───
function SettingsCard({ children, danger, style }) {
  return (
    <div style={{
      background: "#fff", border: `1px solid ${danger ? "#f3c9c5" : "#ececf0"}`, borderRadius: 14,
      padding: "32px 36px", boxShadow: "0 1px 2px rgba(20,20,30,.03)", ...style,
    }}>{children}</div>
  );
}

// ─────────── Main page ───────────
function LeagueSettings({ league, onBack, onSave, onDelete, onToast }) {
  const [name, setName] = React.useState(league?.name || "");
  const [location, setLocation] = React.useState(league?.location || "Perrisville, CA");
  const [logo, setLogo] = React.useState(league?.logo || null);
  const [members, setMembers] = React.useState(league?.members || []);
  const [showAdd, setShowAdd] = React.useState(false);
  const [confirmDelete, setConfirmDelete] = React.useState(false);
  const fileRef = React.useRef();

  React.useEffect(() => {
    setName(league?.name || "");
    setLocation(league?.location || "Perrisville, CA");
    setLogo(league?.logo || null);
    setMembers(league?.members || []);
  }, [league?.id]);

  if (!league) return null;

  const dirty = name !== league.name || location !== (league.location || "Perrisville, CA") || logo !== league.logo;

  const persistMembers = (next) => { setMembers(next); onSave(league.id, { members: next }, true); };
  const setRole = (id, role) => persistMembers(members.map((m) => m.id === id ? { ...m, role } : m));
  const removeMember = (id) => persistMembers(members.filter((m) => m.id !== id));
  const addMember = (m) => {
    persistMembers([...members, { id: "m-" + Date.now(), ...m }]);
    setShowAdd(false);
    onToast(`Invite sent to ${m.email}`);
  };

  const save = () => { onSave(league.id, { name: name.trim() || league.name, location, logo }); onToast("League profile updated"); };
  const cancel = () => { setName(league.name); setLocation(league.location || "Perrisville, CA"); setLogo(league.logo || null); };

  return (
    <div style={{ flex: 1, padding: "28px 40px 120px", background: "#fafafb" }}>
      {/* Back link */}
      <button onClick={onBack} style={{
        border: 0, background: "transparent", cursor: "pointer", color: "#6b6b72",
        display: "inline-flex", alignItems: "center", gap: 8, fontSize: 14, fontWeight: 500, padding: 0, marginBottom: 16,
      }}
        onMouseEnter={(e) => e.currentTarget.style.color = "#0e0e10"}
        onMouseLeave={(e) => e.currentTarget.style.color = "#6b6b72"}>
        <IconChevronLeft size={18} /> Back to League Page
      </button>

      <h1 style={{ fontSize: 30, fontWeight: 800, margin: 0, color: "#0e0e10" }}>League Settings</h1>
      <p style={{ fontSize: 15, color: "#6b6b72", margin: "8px 0 0" }}>
        Manage {league.name} preferences, members, and lifecycle.
      </p>

      <div style={{ maxWidth: 1180, margin: "32px auto 0", display: "flex", flexDirection: "column", gap: 28 }}>
        {/* ─── League Profile ─── */}
        <SettingsCard>
          <h2 style={{ fontSize: 21, fontWeight: 800, margin: 0, color: "#0e0e10" }}>League Profile</h2>
          <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 28px" }}>Update your league's public identity.</p>

          <div style={{ display: "grid", gridTemplateColumns: "auto 1fr", gap: 36, alignItems: "start" }}>
            <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 16 }}>
              <LeagueAvatar logo={logo} name={name} color={league.avatarColor || "#1d3a8a"} />
              <input ref={fileRef} type="file" accept="image/*" style={{ display: "none" }}
                onChange={(e) => { const f = e.target.files?.[0]; if (f) setLogo({ url: URL.createObjectURL(f), name: f.name }); }} />
              <button onClick={() => fileRef.current?.click()} style={{
                border: "1px solid #d4d4d8", background: "#fff", borderRadius: 8, padding: "8px 16px",
                fontSize: 14, fontWeight: 600, cursor: "pointer", color: "#0e0e10",
                display: "inline-flex", alignItems: "center", gap: 8,
              }}>
                <IconUpload size={15} /> Change
              </button>
              {logo && (
                <button onClick={() => setLogo(null)} style={{
                  border: 0, background: "transparent", color: "#b81d14", fontSize: 14, fontWeight: 600, cursor: "pointer", padding: 0,
                }}>Remove</button>
              )}
            </div>

            <div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
              <FloatingField label="League Name" value={name} onChange={setName} placeholder="League name" />
              <FloatingField label="Location" value={location} onChange={setLocation} placeholder="City, State" />
              <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 4 }}>
                <button onClick={cancel} disabled={!dirty} style={{
                  border: "1px solid #d4d4d8", background: "#fff", color: dirty ? "#0e0e10" : "#9a9aa1",
                  padding: "12px 28px", borderRadius: 8, cursor: dirty ? "pointer" : "default", fontSize: 14, fontWeight: 600,
                }}>Cancel</button>
                <button onClick={save} disabled={!dirty} style={{
                  border: 0, borderRadius: 8, padding: "12px 28px",
                  background: dirty ? "#0e0e10" : "#e5e5e9", color: dirty ? "#fff" : "#9a9aa1",
                  fontSize: 14, fontWeight: 700, cursor: dirty ? "pointer" : "default",
                }}>Save Changes</button>
              </div>
            </div>
          </div>
        </SettingsCard>

        {/* ─── Member Management ─── */}
        <SettingsCard>
          <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 24 }}>
            <div>
              <h2 style={{ fontSize: 21, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Member Management</h2>
              <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 0" }}>League managers can configure stores and invite others.</p>
            </div>
            <button onClick={() => setShowAdd(true)} style={{
              border: 0, background: "#0e0e10", color: "#fff", borderRadius: 8, padding: "12px 20px",
              fontSize: 14, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, flexShrink: 0,
            }}>
              <IconPlus size={16} style={{ strokeWidth: 2.4 }} /> Add Manager
            </button>
          </div>

          <div style={{ border: "1px solid #ececf0", borderRadius: 12, overflow: "hidden" }}>
            {/* Table header */}
            <div style={{
              display: "grid", gridTemplateColumns: "1.4fr 1.6fr .8fr 80px", gap: 12,
              padding: "16px 24px", background: "#fafafb", borderBottom: "1px solid #ececf0",
              fontSize: 14, fontWeight: 600, color: "#6b6b72",
            }}>
              <div>User</div><div>Email</div><div>Role</div><div style={{ textAlign: "right" }}>Actions</div>
            </div>
            {members.length === 0 ? (
              <div style={{ padding: "28px", textAlign: "center", color: "#9a9aa1", fontSize: 14 }}>No members yet.</div>
            ) : members.map((m, i) => (
              <div key={m.id} style={{
                display: "grid", gridTemplateColumns: "1.4fr 1.6fr .8fr 80px", gap: 12, alignItems: "center",
                padding: "16px 24px", borderBottom: i < members.length - 1 ? "1px solid #f1f1f3" : "none",
              }}>
                <div style={{ display: "inline-flex", alignItems: "center", gap: 16, minWidth: 0 }}>
                  <div style={{
                    width: 44, height: 44, borderRadius: "50%", flexShrink: 0,
                    background: m.color || "#1d3a8a", color: "#fff", fontWeight: 700, fontSize: 14,
                    display: "inline-flex", alignItems: "center", justifyContent: "center",
                  }}>{m.name.split(/\s+/).map((w) => w[0]).slice(0, 2).join("").toUpperCase()}</div>
                  <span style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>{m.name}</span>
                </div>
                <div style={{ fontSize: 14.5, color: "#6b6b72", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{m.email}</div>
                <div><RoleBadge role={m.role} /></div>
                <div style={{ textAlign: "right" }}>
                  <MemberMenu member={m} onRole={(r) => setRole(m.id, r)} onRemove={() => removeMember(m.id)} />
                </div>
              </div>
            ))}
          </div>
        </SettingsCard>

        {/* ─── Danger Zone ─── */}
        <SettingsCard danger>
          <h2 style={{ fontSize: 21, fontWeight: 800, margin: 0, color: "#b81d14" }}>Danger Zone</h2>
          <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 0" }}>
            Deleting a league is permanent. Its team stores are kept but moved back to All Stores.
          </p>
          <div style={{
            display: "flex", alignItems: "center", justifyContent: "space-between",
            marginTop: 20, padding: "16px 24px", border: "1px solid #f3c9c5", borderRadius: 10, background: "#fdf6f5",
          }}>
            <div>
              <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>Delete this league</div>
              <div style={{ fontSize: 13.5, color: "#6b6b72", marginTop: 2 }}>Removes {league.name} and all of its groups.</div>
            </div>
            {confirmDelete ? (
              <div style={{ display: "inline-flex", gap: 12, alignItems: "center" }}>
                <span style={{ fontSize: 13.5, color: "#6b6b72" }}>Are you sure?</span>
                <button onClick={() => setConfirmDelete(false)} style={{
                  border: "1px solid #d4d4d8", background: "#fff", color: "#0e0e10", borderRadius: 8, padding: "12px 16px",
                  fontSize: 14, fontWeight: 600, cursor: "pointer",
                }}>Cancel</button>
                <button onClick={() => onDelete(league.id)} style={{
                  border: 0, background: "#b81d14", color: "#fff", borderRadius: 8, padding: "12px 16px",
                  fontSize: 14, fontWeight: 700, cursor: "pointer",
                }}>Yes, delete</button>
              </div>
            ) : (
              <button onClick={() => setConfirmDelete(true)} style={{
                border: "1px solid #e1251b", background: "#fff", color: "#b81d14", borderRadius: 8, padding: "12px 24px",
                fontSize: 14, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, flexShrink: 0,
              }}>
                <IconTrash size={15} /> Delete League
              </button>
            )}
          </div>
        </SettingsCard>
      </div>

      <AddManagerModal open={showAdd} onClose={() => setShowAdd(false)} onAdd={addMember} />
    </div>
  );
}

Object.assign(window, { LeagueSettings });


/* ===== ASSET 11 : 2ce2edef-0aeb-49d8-987b-1358931f03ea ===== */
// Stores — high-density card grid of every team store the admin manages.
// Doubles as the drill-down view when a single league is opened from Leagues.

const STATE_ABBR = { California: "CA", Texas: "TX", "New York": "NY", Florida: "FL", Washington: "WA", Oregon: "OR", Nevada: "NV", Arizona: "AZ", Colorado: "CO", Illinois: "IL", Massachusetts: "MA", Georgia: "GA", Ohio: "OH", Michigan: "MI", "North Carolina": "NC", "South Carolina": "SC", Virginia: "VA", Pennsylvania: "PA" };

function StoreList({ stores, onAdd, onAddLeague, onAddGroup, onManage, onArchive, onCreateTemplate, leagues = [], activeLeagueId, onSelectLeague, activeGroupId, onOpenGroup, onCloseGroup, onOpenSettings, onBackToLeagues, zeroState = false }) {
  // Zero-state usability test: strip the top-level stores landing to a true
  // first-run milestone — no cards, pills, org labels or ownership counts.
  const zeroTop = zeroState && !activeLeagueId;
  const [ownership, setOwnership] = React.useState("all");
  const [sort, setSort] = React.useState("Name (A-Z)");
  const [filter, setFilter] = React.useState("All");
  const [search, setSearch] = React.useState("");

  const activeLeague = leagues.find((l) => l.id === activeLeagueId);
  const groups = activeLeague?.groups || [];
  const activeGroup = activeGroupId ? groups.find((g) => g.id === activeGroupId) : null;

  const leagueStores = activeLeague ? stores.filter((s) => activeLeague.storeIds.includes(s.id)) : stores;

  let visibleStores;
  if (activeGroup) {
    visibleStores = leagueStores.filter((s) => activeGroup.storeIds.includes(s.id));
  } else if (activeLeague) {
    const grouped = new Set(groups.flatMap((g) => g.storeIds || []));
    visibleStores = leagueStores.filter((s) => !grouped.has(s.id));
  } else {
    visibleStores =
      ownership === "mine" ? stores.filter((s) => s.ownership === "mine") :
      ownership === "joined" ? stores.filter((s) => s.ownership === "joined") :
      stores; // "all" — every storefront accessible to the logged-in user
  }

  // ─── Toolbar sort + filter ───
  // Options are derived from the stores currently in scope so a choice never
  // points at data that isn't present.
  const scopePool = activeLeague ? leagueStores : stores;
  const nameOf = (s) => (s.storeTitle || s.teamName || "").toLowerCase();
  const sportsPresent = [...new Set(scopePool.map((s) => s.sport).filter(Boolean))].sort();
  const citiesPresent = [...new Set(scopePool.map((s) => s.shipCity).filter(Boolean))].sort();
  const hasHold = scopePool.some((s) => (s.status || "active") === "hold");

  const storeSortOptions = ["Name (A-Z)", "Name (Z-A)", "Revenue (high to low)", "Revenue (low to high)"];
  const storeFilterOptions = [
    "All",
    { label: "Status", sub: hasHold ? ["Active", "On Hold"] : ["Active"] },
    ...(sportsPresent.length ? [{ label: "Sport", sub: sportsPresent }] : []),
    ...(citiesPresent.length ? [{ label: "Location", sub: citiesPresent }] : []),
  ];
  const matchesFilter = (s) => {
    switch (filter) {
      case "All": return true;
      case "Active": return (s.status || "active") !== "hold";
      case "On Hold": return (s.status || "active") === "hold";
      default:
        if (sportsPresent.includes(filter)) return (s.sport || "") === filter;
        if (citiesPresent.includes(filter)) return (s.shipCity || "") === filter;
        return true;
    }
  };
  const applySort = (arr) => {
    const a = [...arr];
    if (sort === "Name (A-Z)") a.sort((x, y) => nameOf(x).localeCompare(nameOf(y)));
    else if (sort === "Name (Z-A)") a.sort((x, y) => nameOf(y).localeCompare(nameOf(x)));
    else if (sort === "Revenue (high to low)") a.sort((x, y) => (y.revenue || 0) - (x.revenue || 0));
    else if (sort === "Revenue (low to high)") a.sort((x, y) => (x.revenue || 0) - (y.revenue || 0));
    return a;
  };
  const matchesSearch = (s) => {
    const q = search.trim().toLowerCase();
    if (!q) return true;
    return [s.storeTitle, s.teamName, s.sport, s.shipCity].some((v) => (v || "").toLowerCase().includes(q));
  };
  const finalStores = zeroTop ? [] : applySort(visibleStores.filter(matchesFilter).filter(matchesSearch));

  const storeToolbar = (
    <ListToolbar
      searchPlaceholder="Search Team Stores"
      searchValue={search}
      onSearch={setSearch}
      filter={{ value: filter, options: storeFilterOptions, onChange: setFilter, width: 232 }}
      sort={{ value: sort, options: storeSortOptions, onChange: setSort, width: 224 }} />
  );

  const scopeStores = activeGroup ? visibleStores : leagueStores;
  const totalRevenue = (activeLeague ? scopeStores : finalStores).reduce((sum, s) => sum + (s.revenue || 0), 0);
  const storeCount = activeLeague ? scopeStores.length : finalStores.length;

  return (
    <div style={{ flex: 1, minWidth: 0, padding: "28px max(32px, calc((100% - 1376px)/2)) 120px", background: "#fbfbfc", position: "relative" }}>
      {/* Header */}
      {activeLeague ? (
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 24, gap: 16, flexWrap: "wrap" }}>
          <div>
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8, fontSize: 13 }}>
              <button onClick={onBackToLeagues} style={{ border: 0, background: "transparent", padding: 0, cursor: "pointer", color: "#9a9aa1", fontWeight: 700, letterSpacing: ".04em" }}>LEAGUES</button>
              <IconChevronRight size={14} style={{ color: "#c9c9cf" }} />
              {activeGroup ? (
                <React.Fragment>
                  <button onClick={onCloseGroup} style={{ border: 0, background: "transparent", padding: 0, cursor: "pointer", color: "#9a9aa1", fontWeight: 700, letterSpacing: ".04em" }}>{(activeLeague.name || "LEAGUE").toUpperCase()}</button>
                  <IconChevronRight size={14} style={{ color: "#c9c9cf" }} />
                  <span style={{ fontWeight: 800, color: "#0e0e10" }}>{activeGroup.name.toUpperCase()}</span>
                </React.Fragment>
              ) : (
                <span style={{ fontWeight: 800, color: "#0e0e10" }}>{activeLeague.name.toUpperCase()}</span>
              )}
            </div>
            <h1 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, margin: 0, color: "#0e0e10", letterSpacing: "-.3px" }}>{activeGroup ? activeGroup.name : activeLeague.name}</h1>
            <div style={{ display: "flex", gap: 24, alignItems: "center", color: "#6b6b72", fontSize: 13.5, marginTop: 8 }}>
              <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}><IconStore size={16} /> {storeCount} {storeCount === 1 ? "Store" : "Stores"}</span>
              <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}><IconDollar size={16} style={{ color: "#1f9d55" }} /><span style={{ color: "#1f9d55", fontWeight: 700 }}>${totalRevenue.toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/g, ",")}</span></span>
            </div>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            {!activeGroup && (
              <button onClick={onOpenSettings} style={{ border: "1px solid #d8d8de", background: "#fff", color: "#0e0e10", borderRadius: 8, padding: "12px 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 600, display: "inline-flex", alignItems: "center", gap: 8 }}>
                <IconSettings size={17} /> League Settings
              </button>
            )}
            {storeToolbar}
          </div>
        </div>
      ) : (
        <ListPageHeader title="Stores" subtitle={(() => { const inLeague = new Set(leagues.flatMap((l) => l.storeIds || [])); const teamN = stores.filter((s) => !inLeague.has(s.id)).length; return `${teamN} Team Stores \u2022 ${stores.length - teamN} League Stores`; })()}>
          <button onClick={onAdd} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", border: 0, borderRadius: 8, padding: "12px 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 700, color: "#fff", whiteSpace: "nowrap" }}>
            <IconPlus size={16} style={{ strokeWidth: 2.4 }} /> Add Store
          </button>
          {storeToolbar}
        </ListPageHeader>
      )}

      {/* Ownership filter — only at the top-level stores view */}
      {!activeLeague && !zeroTop && (
        <div style={{ display: "inline-flex", background: "#f1f1f3", borderRadius: 999, padding: 4, marginBottom: 24, gap: 4 }}>
          {[
            { id: "all", label: "All Stores" },
            { id: "mine", label: "My Stores" },
            { id: "joined", label: "Joined Stores" },
          ].map((t) => {
            const on = ownership === t.id;
            const count =
              t.id === "all" ? stores.length :
              stores.filter((s) => s.ownership === t.id).length;
            return (
              <button key={t.id} onClick={() => setOwnership(t.id)} style={{
                background: on ? "#0e0e10" : "transparent", color: on ? "#fff" : "#52525a",
                border: 0, borderRadius: 999, padding: "8px 20px", cursor: "pointer", fontSize: 13, fontWeight: 700,
                whiteSpace: "nowrap", transition: "all .12s",
              }}>
                {t.label}
                <span style={{
                  marginLeft: 8, display: "inline-flex", alignItems: "center", justifyContent: "center",
                  minWidth: 20, height: 20, padding: "0 8px", borderRadius: 999, fontSize: 11.5, fontWeight: 700,
                  background: on ? "rgba(255,255,255,.18)" : "#e4e4e7",
                  color: on ? "#fff" : "#52525a", transition: "all .12s",
                }}>{count}</span>
              </button>
            );
          })}
        </div>
      )}

      {/* Cards grid */}
      {zeroTop ? (
        <div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", textAlign: "center", padding: "92px 20px 40px" }}>
          <div style={{ width: 152, height: 152, borderRadius: "50%", background: "#f5f5f7", border: "1px solid #ececf0", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 32 }}>
            <svg width="78" height="78" viewBox="0 0 64 64" fill="none" stroke="#a1a1aa" strokeWidth="2.4" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
              {/* Storefront awning + shutter — empty retail rack */}
              <path d="M8 24 L12 12 H52 L56 24" />
              <path d="M8 24 v28 a2 2 0 0 0 2 2 h44 a2 2 0 0 0 2 -2 V24" />
              <path d="M8 24 h48" />
              <path d="M20 24 v-3 M32 24 v-3 M44 24 v-3" opacity="0.55" />
              <path d="M24 54 V38 a8 8 0 0 1 16 0 V54" />
            </svg>
          </div>
          <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, color: "#0e0e10", margin: "0 0 12px", letterSpacing: "-.01em" }}>You don&rsquo;t have any Team Stores yet.</h2>
          <p style={{ fontSize: 15, color: "#6b6b72", lineHeight: 1.6, maxWidth: 460, margin: "0 0 28px" }}>Create an always-on locker or a temporary pop-up store to start assigning customized gear collections to your teams.</p>
          <button onClick={onAdd} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", color: "#fff", border: 0, borderRadius: 8, padding: "16px 28px", fontSize: 15, fontWeight: 700, cursor: "pointer" }}
            onMouseEnter={(e) => (e.currentTarget.style.background = "#1f1f22")}
            onMouseLeave={(e) => (e.currentTarget.style.background = "#0e0e10")}>
            <IconPlus size={18} style={{ strokeWidth: 2.6 }} /> Create Your First Store
          </button>
        </div>
      ) : (
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(248px, 1fr))", gap: 16 }}>
        {activeLeague && !activeGroup && groups.map((g) => <GroupCard key={g.id} group={g} onOpen={() => onOpenGroup(g.id)} />)}
        {finalStores.map((s) => <StoreCard key={s.id} store={s} onManage={() => onManage(s.id)} onArchive={() => onArchive && onArchive(s.id)} onCreateTemplate={onCreateTemplate} />)}
        {finalStores.length === 0 && !(activeLeague && !activeGroup && groups.length) && (
          <div style={{ gridColumn: "1 / -1", padding: "92px 20px", textAlign: "center", color: "#9a9aa1", fontSize: 14 }}>
            {filter !== "All" ? `No stores match “${filter}”.` :
             ownership === "mine" ? "You haven’t created any stores yet." : ownership === "joined" ? "No stores have been shared with you yet." : "No stores here yet."}
          </div>
        )}
      </div>
      )}

      {/* Floating actions — Add Group only (Add Store moved to toolbar) */}
      {activeLeague && (
        <div style={{ position: "fixed", bottom: 28, right: 36, zIndex: 40, display: "flex", alignItems: "center", gap: 12 }}>
          <button onClick={onAddGroup} style={{ background: "#fff", color: "#0e0e10", border: "1px solid #d4d4d8", padding: "16px 24px", borderRadius: 8, cursor: "pointer", fontSize: 13, fontWeight: 800, letterSpacing: ".06em", display: "inline-flex", alignItems: "center", gap: 8, boxShadow: "0 8px 20px rgba(20,20,30,.12)" }}>
            <IconGroupAdd size={17} /> Add Group
          </button>
        </div>
      )}
    </div>
  );
}

// Group card — sits at the front of a league grid; opens the group's stores.
function GroupCard({ group, onOpen }) {
  const count = (group.storeIds || []).length;
  return (
    <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden", boxShadow: "0 1px 2px rgba(20,20,30,.03)", display: "flex", flexDirection: "column", transition: "box-shadow .15s ease, border-color .15s ease" }}
      onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 8px 22px rgba(20,20,30,.09)"; e.currentTarget.style.borderColor = "#d4d4d8"; }}
      onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "0 1px 2px rgba(20,20,30,.03)"; e.currentTarget.style.borderColor = "#ececf0"; }}>
      <div style={{ position: "relative", background: "#fafafb", padding: "20px", minHeight: 172, display: "flex", alignItems: "center", justifyContent: "center" }}>
        <div style={{ position: "absolute", top: 16, left: 16, background: "#e3edff", color: "#2657c4", padding: "4px 12px", borderRadius: 999, fontSize: 11, fontWeight: 700 }}>Group</div>
        <div style={{ width: 92, height: 92, borderRadius: "50%", background: "#ececf0", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
          <IconGrid size={32} stroke={1.7} />
        </div>
      </div>
      <div style={{ padding: "16px 16px 16px", textAlign: "center" }}>
        <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 4, color: "#0e0e10" }}>{group.name}</div>
        <div style={{ fontSize: 12.5, color: "#9a9aa1" }}>{count} {count === 1 ? "team" : "teams"}</div>
      </div>
      <div style={{ borderTop: "1px solid #ececf0", padding: 16 }}>
        <button onClick={onOpen} style={{ width: "100%", background: "#fff", border: "1px solid #0e0e10", color: "#0e0e10", padding: "12px 16px", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", transition: "all .12s" }}
          onMouseEnter={(e) => { e.currentTarget.style.background = "#0e0e10"; e.currentTarget.style.color = "#fff"; }}
          onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = "#0e0e10"; }}>
          Open Group
        </button>
      </div>
    </div>
  );
}

function ArchiveStoreModal({ open, store, onClose, onConfirm }) {
  if (!store) return null;
  return (
    <Modal open={open} onClose={onClose} width={480}>
      <div style={{ padding: "28px 28px 8px" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 12 }}>
          <span style={{ width: 40, height: 40, borderRadius: "50%", background: "#fbe3e1", color: "#b81d14", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
            <IconAlert size={19} style={{ strokeWidth: 2.2 }} />
          </span>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Archive {store.teamName || store.storeTitle}?</h2>
        </div>
        <p style={{ margin: "0 0 12px", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>Archiving a store will:</p>
        <ul style={{ margin: "0 0 4px", paddingLeft: 20, fontSize: 13.5, color: "#0e0e10", lineHeight: 1.85 }}>
          <li>Hide the storefront from all buyers immediately</li>
          <li>Stop all new orders and close any active ordering window</li>
          <li>Remove it from your active stores list</li>
        </ul>
        <p style={{ margin: "12px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.6 }}>Order history and reports are retained. This can be undone by support.</p>
      </div>
      <div style={{ padding: "20px 28px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
        <Button variant="secondary" onClick={onClose}>Cancel</Button>
        <button onClick={onConfirm} style={{
          display: "inline-flex", alignItems: "center", gap: 8, background: "#e1251b", border: 0,
          borderRadius: 6, padding: "12px 20px", fontSize: 14, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit",
        }}>
          <IconTrash size={14} style={{ strokeWidth: 2.2 }} /> Archive Store
        </button>
      </div>
    </Modal>
  );
}

function RestoreStoreModal({ open, store, onClose, onConfirm }) {
  if (!store) return null;
  return (
    <Modal open={open} onClose={onClose} width={480}>
      <div style={{ padding: "28px 28px 8px" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 12 }}>
          <span style={{ width: 40, height: 40, borderRadius: "50%", background: "#e7f5ed", color: "#1f7a44", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
            <IconCheck size={19} style={{ strokeWidth: 2.4 }} />
          </span>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Restore {store.teamName || store.storeTitle}?</h2>
        </div>
        <p style={{ margin: "0 0 12px", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>Restoring this store will:</p>
        <ul style={{ margin: "0 0 4px", paddingLeft: 20, fontSize: 13.5, color: "#0e0e10", lineHeight: 1.85 }}>
          <li>Move it back into your active stores list</li>
          <li>Return it as a draft so you can review before re-opening</li>
          <li>Keep all retained order history and reports intact</li>
        </ul>
        <p style={{ margin: "12px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.6 }}>The storefront stays closed to buyers until you re-open it.</p>
      </div>
      <div style={{ padding: "20px 28px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
        <Button variant="secondary" onClick={onClose}>Cancel</Button>
        <button onClick={onConfirm} style={{
          display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", border: 0,
          borderRadius: 6, padding: "12px 20px", fontSize: 14, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit",
        }}>
          <IconCheck size={15} style={{ strokeWidth: 2.4 }} /> Restore Store
        </button>
      </div>
    </Modal>
  );
}

function StoreCard({ store, onManage, onArchive, onCreateTemplate, archived = false, onView, onUnarchive, condensed = false, context, contextIcon }) {
  const [menuOpen, setMenuOpen] = React.useState(false);
  const menuRef = React.useRef();
  React.useEffect(() => {
    const h = (e) => { if (menuRef.current && !menuRef.current.contains(e.target)) setMenuOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const city = store.shipCity || "Carlsbad";
  const stateAbbr = STATE_ABBR[store.shipState] || (store.shipState || "CA").slice(0, 2).toUpperCase();
  const isDraft = !store.live;
  const isHold = (store.status || "active") === "hold";
  const tagBg = archived ? "#e4e4e7" : isDraft ? "#ebebed" : isHold ? "#fdeecb" : "#d8f5e3";
  const tagColor = archived ? "#52525b" : isDraft ? "#6b6b72" : isHold ? "#92670b" : "#1f7a44";
  const tagLabel = archived ? "Archived" : isDraft ? "Draft" : isHold ? "On Hold" : "Active";
  const isLeague = store.storeType === "league";
  const typeLabel = isLeague ? "League Store" : "Team Store";

  // Condensed variant — same anatomy and styling, reduced content. Used by the
  // Overview's Frequently Visited shelf, where the row must stay ~200px tall.
  if (condensed) {
    return (
      <div onClick={onManage} title={"Open " + (store.storeTitle || store.teamName || "store")}
        style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden", boxShadow: "0 1px 2px rgba(20,20,30,.03)", display: "flex", flexDirection: "column", height: "100%", cursor: "pointer", transition: "box-shadow .15s ease, border-color .15s ease" }}
        onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 8px 22px rgba(20,20,30,.09)"; e.currentTarget.style.borderColor = "#d4d4d8"; }}
        onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "0 1px 2px rgba(20,20,30,.03)"; e.currentTarget.style.borderColor = "#ececf0"; }}>
        <div style={{ position: "relative", background: "#fafafb", padding: 12, minHeight: 78, display: "flex", alignItems: "center", justifyContent: "center" }}>
          <div style={{ position: "absolute", top: 9, left: 9, background: tagBg, color: tagColor, padding: "3px 8px", borderRadius: 999, fontSize: 10, fontWeight: 700 }}>{tagLabel}</div>
          {store.teamLogo?.url ? (
            <img src={store.teamLogo.url} alt="" style={{ maxWidth: 64, maxHeight: 54, objectFit: "contain" }} />
          ) : (
            <div style={{ width: 42, height: 42, borderRadius: "50%", background: store.teamLogo?.color || store.teamColors?.[0] || "#1d3a8a", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#fff", fontWeight: 800, fontSize: 13.5, fontFamily: "Archivo, sans-serif", border: (store.teamColors?.[0] || "").toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none" }}>
              {store.teamLogo?.mono || (store.teamName || store.storeTitle || "T").slice(0, 2).toUpperCase()}
            </div>
          )}
        </div>
        <div style={{ padding: "10px 12px 9px", textAlign: "center", flex: 1, minWidth: 0 }}>
          <div style={{ fontSize: 13, fontWeight: 800, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{store.storeTitle || store.teamName || "Team Store"}</div>
          <div style={{ fontSize: 11, color: "#9a9aa1", marginTop: 3, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{typeLabel} · {store.sport || "Sport"}</div>
          {context && (
            <div style={{ marginTop: 7, display: "flex", justifyContent: "center" }}>
              <span style={{ display: "inline-flex", alignItems: "center", gap: 5, maxWidth: "100%", fontSize: 10.5, fontWeight: 600, color: "#6b6b72", background: "#f5f5f7", borderRadius: 999, padding: "3px 9px", whiteSpace: "nowrap", overflow: "hidden" }}>
                {contextIcon}
                <span style={{ overflow: "hidden", textOverflow: "ellipsis" }}>{context}</span>
              </span>
            </div>
          )}
        </div>
        <div style={{ borderTop: "1px solid #ececf0", padding: "9px 10px" }}>
          <button onClick={(e) => { e.stopPropagation(); onManage && onManage(); }} style={{ width: "100%", background: "#fff", border: "1px solid #0e0e10", color: "#0e0e10", padding: "8px 12px", borderRadius: 6, fontSize: 11.5, fontWeight: 700, cursor: "pointer", transition: "all .12s" }}
            onMouseEnter={(e) => { e.currentTarget.style.background = "#0e0e10"; e.currentTarget.style.color = "#fff"; }}
            onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = "#0e0e10"; }}>
            Manage Store
          </button>
        </div>
      </div>
    );
  }

  return (
    <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden", boxShadow: "0 1px 2px rgba(20,20,30,.03)", display: "flex", flexDirection: "column", transition: "box-shadow .15s ease, border-color .15s ease" }}
      onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 8px 22px rgba(20,20,30,.09)"; e.currentTarget.style.borderColor = "#d4d4d8"; }}
      onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "0 1px 2px rgba(20,20,30,.03)"; e.currentTarget.style.borderColor = "#ececf0"; }}>
      <div style={{ position: "relative", background: "#fafafb", padding: "20px", minHeight: 172, display: "flex", alignItems: "center", justifyContent: "center" }}>
        <div style={{ position: "absolute", top: 16, left: 16, display: "flex", flexDirection: "column", alignItems: "flex-start", gap: 8 }}>
          <div style={{ background: tagBg, color: tagColor, padding: "4px 12px", borderRadius: 999, fontSize: 11, fontWeight: 700, display: "inline-flex", alignItems: "center", gap: 5 }}>
            {archived && (
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
                <rect x="3" y="4" width="18" height="4" rx="1" /><path d="M5 8v11a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V8" /><path d="M10 12h4" />
              </svg>
            )}
            {tagLabel}
          </div>
          <div style={{
            background: isLeague ? "#0e0e10" : "#fff", color: isLeague ? "#fff" : "#0e0e10",
            border: isLeague ? "1px solid #0e0e10" : "1px solid #d4d4d8",
            padding: "4px 12px", borderRadius: 999, fontSize: 11, fontWeight: 700,
            display: "inline-flex", alignItems: "center", gap: 4
          }}>
            {isLeague ? <IconLayers size={12} style={{ strokeWidth: 2.2 }} /> : <IconUsers size={12} style={{ strokeWidth: 2.2 }} />}
            {typeLabel}
          </div>
        </div>
        <div ref={menuRef} style={{ position: "absolute", top: 12, right: 12 }}>
        <button onClick={() => setMenuOpen((v) => !v)} style={{ background: "transparent", border: 0, display: "inline-flex", alignItems: "center", justifyContent: "center", cursor: "pointer", padding: 8, borderRadius: 6 }} title="Store actions"
          onMouseEnter={(e) => (e.currentTarget.style.background = "#ececf0")}
          onMouseLeave={(e) => (e.currentTarget.style.background = "transparent")}>
          <span style={{ display: "inline-flex", gap: 4 }}>
            <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#1a1a1d" }} />
            <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#1a1a1d" }} />
            <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#1a1a1d" }} />
          </span>
        </button>
        {menuOpen && (
          <div style={{ position: "absolute", top: 36, right: 0, zIndex: 5, background: "#fff", border: "1px solid #ececf0", borderRadius: 6, boxShadow: "0 8px 24px rgba(20,20,30,.10)", padding: 4, minWidth: 140 }}>
            {(archived ? [
              { label: "View", onClick: () => { setMenuOpen(false); (onView || onManage) && (onView || onManage)(); } },
              { label: "Unarchive", onClick: () => { setMenuOpen(false); onUnarchive && onUnarchive(); } },
            ] : [
              { label: "Edit", onClick: () => { setMenuOpen(false); onManage && onManage(); } },
              { label: "Visit Store", onClick: () => { setMenuOpen(false); } },
              { label: "Archive", onClick: () => { setMenuOpen(false); onArchive && onArchive(); } },
            ]).map(({ label, onClick }) => (
              <div key={label} onClick={onClick} style={{ padding: "8px 12px", fontSize: 13, cursor: "pointer", borderRadius: 4 }}
                onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")}
                onMouseLeave={(e) => (e.currentTarget.style.background = "transparent")}>{label}</div>
            ))}
          </div>
        )}
        </div>
        {store.teamLogo?.url ? (
          <img src={store.teamLogo.url} alt="" style={{ maxWidth: 120, maxHeight: 104, objectFit: "contain", filter: archived ? "grayscale(1)" : "none", opacity: archived ? 0.5 : 1 }} />
        ) : (
          <div style={{ width: 92, height: 92, borderRadius: "50%", background: store.teamLogo?.color || store.teamColors?.[0] || "#1d3a8a", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#fff", fontWeight: 800, fontSize: 28, fontFamily: "Archivo, sans-serif", border: (store.teamColors?.[0] || "").toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none", filter: archived ? "grayscale(1)" : "none", opacity: archived ? 0.5 : 1 }}>
            {store.teamLogo?.mono || (store.teamName || store.storeTitle || "T").slice(0, 2).toUpperCase()}
          </div>
        )}
      </div>

      <div style={{ padding: "16px 16px 16px", textAlign: "center", flex: 1 }}>
        <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 4, color: "#0e0e10" }}>{store.storeTitle || store.teamName || "Team Store"}</div>
        <div style={{ fontSize: 12.5, color: "#9a9aa1", marginBottom: 4 }}>{store.sport || "Sport"}</div>
        <div style={{ fontSize: 12.5, color: "#6b6b72" }}>Created by {store.createdByName || store.managerName || store.contactName || "—"}</div>
      </div>

      <div style={{ borderTop: "1px solid #ececf0", padding: 16 }}>
        <button onClick={onManage} style={{ width: "100%", background: "#fff", border: "1px solid #0e0e10", color: "#0e0e10", padding: "12px 16px", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", transition: "all .12s" }}
          onMouseEnter={(e) => { e.currentTarget.style.background = "#0e0e10"; e.currentTarget.style.color = "#fff"; }}
          onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = "#0e0e10"; }}>
          Manage Store
        </button>
      </div>
    </div>
  );
}

Object.assign(window, { StoreList });


/* ===== ASSET 12 : ac768a0e-c89d-41a8-8f2c-1178ee038c34 ===== */
// Leagues — high-level card grid mirroring the Stores tab, one tier up.

// ─────────── Shared sort/filter dropdown menu ───────────
// White card list matching the product/roster reference menus. Options are
// strings, or { label, sub:[...] } for an inline-expanding sub-list.
function ToolbarMenu({ options, value, onSelect, onClose, align = "left", width = 232 }) {
  const ref = React.useRef();
  const [openSub, setOpenSub] = React.useState(null);
  React.useEffect(() => {
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) onClose(); };
    document.addEventListener("mousedown", h);
    const k = (e) => { if (e.key === "Escape") onClose(); };
    document.addEventListener("keydown", k);
    return () => { document.removeEventListener("mousedown", h); document.removeEventListener("keydown", k); };
  }, [onClose]);

  const rowBase = {
    display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12,
    padding: "16px 16px", fontSize: 13.5, fontWeight: 600, color: "#0e0e10",
    cursor: "pointer", background: "#fff", transition: "background .1s",
  };

  return (
    <div ref={ref} style={{
      position: "absolute", top: "calc(100% + 8px)", [align]: 0, zIndex: 70,
      background: "#fff", border: "1px solid #ececf0", borderRadius: 12,
      boxShadow: "0 16px 38px rgba(20,20,30,.14)", width, overflow: "hidden",
    }}>
      {options.map((opt, i) => {
        const label = typeof opt === "string" ? opt : opt.label;
        const sub = typeof opt === "object" ? opt.sub : null;
        const selected = value === label;
        const subOpen = openSub === label;
        const divider = i > 0 ? { borderTop: "1px solid #f1f1f3" } : {};
        return (
          <div key={label}>
            <div
              role="menuitem"
              onClick={() => { if (sub) setOpenSub(subOpen ? null : label); else { onSelect(label); onClose(); } }}
              style={{ ...rowBase, ...divider, background: selected && !sub ? "#fafafb" : "#fff" }}
              onMouseEnter={(e) => (e.currentTarget.style.background = "#f6f6f8")}
              onMouseLeave={(e) => (e.currentTarget.style.background = selected && !sub ? "#fafafb" : "#fff")}>
              <span>{label}</span>
              {sub
                ? <IconChevron size={15} style={{ color: "#9a9aa1", transform: subOpen ? "rotate(180deg)" : "none", transition: "transform .15s" }} />
                : selected ? <IconCheck size={15} style={{ color: "#0e0e10" }} /> : null}
            </div>
            {sub && subOpen && (
              <div style={{ background: "#fafafb", borderTop: "1px solid #f1f1f3" }}>
                {sub.map((s) => {
                  const sLabel = typeof s === "string" ? s : s.label;
                  const sSel = value === sLabel;
                  return (
                    <div key={sLabel}
                      onClick={() => { onSelect(sLabel); onClose(); }}
                      style={{ ...rowBase, background: sSel ? "#f1f1f3" : "#fafafb", padding: "12px 16px 12px 32px", fontSize: 13, fontWeight: 500, color: sSel ? "#0e0e10" : "#4a4a52" }}
                      onMouseEnter={(e) => (e.currentTarget.style.background = "#eeeef1")}
                      onMouseLeave={(e) => (e.currentTarget.style.background = sSel ? "#f1f1f3" : "#fafafb")}>
                      <span>{sLabel}</span>
                      {sSel ? <IconCheck size={14} style={{ color: "#0e0e10" }} /> : null}
                    </div>
                  );
                })}
              </div>
            )}
          </div>
        );
      })}
    </div>
  );
}

// Shared list-page toolbar (Filters · Sort By · Search) used by Stores, Leagues,
// Roster, and Products. Pass `sort`/`filter` ({ value, options, onChange }) to make
// the buttons functional dropdowns; omit them for the legacy static toolbar.
function ListToolbar({ searchPlaceholder = "Search Team Stores", sort, filter, showSearch = true, searchValue, onSearch }) {
  const [open, setOpen] = React.useState(null); // 'sort' | 'filter' | null
  const legacy = !sort && !filter;
  const btn = (active) => ({
    display: "inline-flex", alignItems: "center", gap: 8,
    background: active ? "#fafafb" : "#fff",
    border: `1px solid ${active ? "#c9c9d0" : "#d8d8de"}`, borderRadius: 8, padding: "12px 16px",
    cursor: "pointer", fontSize: 13.5, fontWeight: 600, color: "#0e0e10", whiteSpace: "nowrap",
  });

  if (legacy) {
    return (
      <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <button style={btn(false)} onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")} onMouseLeave={(e) => (e.currentTarget.style.background = "#fff")}>
          <IconFilter size={16} /> Show Filters
        </button>
        <button style={btn(false)} onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")} onMouseLeave={(e) => (e.currentTarget.style.background = "#fff")}>
          <IconSort size={16} /> Sort By: Default
        </button>
        <div style={{ position: "relative", display: "flex", alignItems: "center", minWidth: 240, border: "1px solid #d8d8de", borderRadius: 8, background: "#fff" }}>
          <input placeholder={searchPlaceholder} style={{ width: "100%", border: 0, outline: 0, background: "transparent", padding: "12px 40px 12px 16px", fontSize: 13.5 }} />
          <IconSearch size={16} style={{ position: "absolute", right: 12, color: "#9a9aa1" }} />
        </div>
      </div>
    );
  }

  const filterLabel = filter && filter.value && filter.value !== "All" ? `Filter: ${filter.value}` : "Show Filters";
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
      {filter && (
        <div style={{ position: "relative" }}>
          <button style={btn(open === "filter")} onClick={() => setOpen(open === "filter" ? null : "filter")}>
            <IconFilter size={16} /> {filterLabel}
          </button>
          {open === "filter" && (
            <ToolbarMenu options={filter.options} value={filter.value} width={filter.width || 232}
              onSelect={filter.onChange} onClose={() => setOpen(null)} />
          )}
        </div>
      )}
      {sort && (
        <div style={{ position: "relative" }}>
          <button style={btn(open === "sort")} onClick={() => setOpen(open === "sort" ? null : "sort")}>
            <IconSort size={16} /> Sort By: {sort.value}
          </button>
          {open === "sort" && (
            <ToolbarMenu options={sort.options} value={sort.value} width={sort.width || 232}
              align="left" onSelect={sort.onChange} onClose={() => setOpen(null)} />
          )}
        </div>
      )}
      {showSearch && (
        <div style={{ position: "relative", display: "flex", alignItems: "center", minWidth: 240, border: "1px solid #d8d8de", borderRadius: 8, background: "#fff" }}>
          <input value={searchValue || ""} onChange={(e) => onSearch && onSearch(e.target.value)} placeholder={searchPlaceholder} style={{ width: "100%", border: 0, outline: 0, background: "transparent", padding: "12px 40px 12px 16px", fontSize: 13.5 }} />
          <IconSearch size={16} style={{ position: "absolute", right: 12, color: "#9a9aa1" }} />
        </div>
      )}
    </div>
  );
}

function ListPageHeader({ title, storeCount, revenue, subtitle, children }) {
  return (
    <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", marginBottom: 24, flexWrap: "wrap", gap: 16 }}>
      <div>
        <h1 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, margin: "0 0 8px", color: "#0e0e10", letterSpacing: "-.3px" }}>{title}</h1>
        {subtitle ? (
          <div style={{ color: "#6b6b72", fontSize: 14 }}>{subtitle}</div>
        ) : (
        <div style={{ display: "flex", gap: 24, alignItems: "center", color: "#6b6b72", fontSize: 13.5 }}>
          <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
            <IconStore size={16} /> {storeCount} {storeCount === 1 ? "Store" : "Stores"}
          </span>
          <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
            <IconDollar size={16} style={{ color: "#1f9d55" }} />
            <span style={{ color: "#1f9d55", fontWeight: 700 }}>${revenue.toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/g, ",")}</span>
          </span>
        </div>
        )}
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 12 }}>{children}</div>
    </div>
  );
}

function LeagueCard({ league, storeCount, onOpen }) {
  const color = league.avatarColor || "#1d3a8a";
  const mono = (league.name || "L").split(" ").map((w) => w[0]).join("").slice(0, 2).toUpperCase();
  return (
    <div style={{
      background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden",
      boxShadow: "0 1px 2px rgba(20,20,30,.03)", display: "flex", flexDirection: "column", transition: "box-shadow .15s ease, border-color .15s ease",
    }}
      onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 8px 22px rgba(20,20,30,.09)"; e.currentTarget.style.borderColor = "#d4d4d8"; }}
      onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "0 1px 2px rgba(20,20,30,.03)"; e.currentTarget.style.borderColor = "#ececf0"; }}>
      <div style={{ position: "relative", background: "#fafafb", padding: "20px", minHeight: 172, display: "flex", alignItems: "center", justifyContent: "center" }}>
        <div style={{ position: "absolute", top: 16, left: 16, background: "#e3edff", color: "#2657c4", padding: "4px 12px", borderRadius: 999, fontSize: 11, fontWeight: 700, display: "inline-flex", alignItems: "center", gap: 4 }}>
          <IconTrophy size={12} /> League
        </div>
        <button style={{ position: "absolute", top: 12, right: 16, background: "transparent", border: 0, display: "inline-flex", alignItems: "center", justifyContent: "center", cursor: "pointer", padding: 8, borderRadius: 6 }} title="League actions"
          onMouseEnter={(e) => (e.currentTarget.style.background = "#ececf0")}
          onMouseLeave={(e) => (e.currentTarget.style.background = "transparent")}>
          <span style={{ display: "inline-flex", gap: 4 }}>
            <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#1a1a1d" }} />
            <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#1a1a1d" }} />
            <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#1a1a1d" }} />
          </span>
        </button>
        <div style={{ width: 92, height: 92, borderRadius: "50%", background: color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: 30 }}>{mono}</div>
      </div>
      <div style={{ padding: "16px 16px 16px", textAlign: "center" }}>
        <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 4, color: "#0e0e10" }}>{league.name}</div>
        <div style={{ fontSize: 12.5, color: "#6b6b72" }}>
          {league.location || "—"} · {storeCount} {storeCount === 1 ? "team" : "teams"}
        </div>
      </div>
      <div style={{ borderTop: "1px solid #ececf0", padding: 16 }}>
        <button onClick={onOpen} style={{
          width: "100%", background: "#fff", border: "1px solid #0e0e10", color: "#0e0e10",
          padding: "12px 16px", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", transition: "all .12s",
        }}
          onMouseEnter={(e) => { e.currentTarget.style.background = "#0e0e10"; e.currentTarget.style.color = "#fff"; }}
          onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = "#0e0e10"; }}>
          Manage League
        </button>
      </div>
    </div>
  );
}

function LeaguesGrid({ leagues = [], stores = [], onOpenLeague, onAddLeague }) {
  const [sort, setSort] = React.useState("Name (A-Z)");
  const [filter, setFilter] = React.useState("All");

  const storesById = {};
  stores.forEach((s) => { storesById[s.id] = s; });
  const leagueStoreCount = (l) => (l.storeIds || []).length;
  const leagueRevenue = (l) => (l.storeIds || []).reduce((a, id) => a + (storesById[id]?.revenue || 0), 0);
  const nameOf = (l) => (l.name || "").toLowerCase();

  const locationsPresent = [...new Set(leagues.map((l) => l.location).filter(Boolean))].sort();
  const leagueSortOptions = ["Name (A-Z)", "Name (Z-A)", "Teams (most to fewest)", "Revenue (high to low)"];
  const leagueFilterOptions = [
    "All",
    ...(locationsPresent.length ? [{ label: "Location", sub: locationsPresent }] : []),
  ];
  const matchesFilter = (l) => filter === "All" || l.location === filter;
  const applySort = (arr) => {
    const a = [...arr];
    if (sort === "Name (A-Z)") a.sort((x, y) => nameOf(x).localeCompare(nameOf(y)));
    else if (sort === "Name (Z-A)") a.sort((x, y) => nameOf(y).localeCompare(nameOf(x)));
    else if (sort === "Teams (most to fewest)") a.sort((x, y) => leagueStoreCount(y) - leagueStoreCount(x));
    else if (sort === "Revenue (high to low)") a.sort((x, y) => leagueRevenue(y) - leagueRevenue(x));
    return a;
  };
  const finalLeagues = applySort(leagues.filter(matchesFilter));

  const totalStores = finalLeagues.reduce((n, l) => n + leagueStoreCount(l), 0);
  const totalRevenue = finalLeagues.reduce((sum, l) => sum + leagueRevenue(l), 0);

  return (
    <div style={{ flex: 1, minWidth: 0, padding: "28px 32px 120px", background: "#fbfbfc", position: "relative" }}>
      <ListPageHeader title="Leagues" storeCount={totalStores} revenue={totalRevenue}>
        {leagues.length > 0 && (
          <ListToolbar
            showSearch={false}
            filter={{ value: filter, options: leagueFilterOptions, onChange: setFilter, width: 240 }}
            sort={{ value: sort, options: leagueSortOptions, onChange: setSort, width: 232 }} />
        )}
      </ListPageHeader>

      {leagues.length === 0 ? (
        <div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", padding: "120px 20px", textAlign: "center" }}>
          <div style={{ width: 64, height: 64, borderRadius: "50%", background: "#f1f1f3", color: "#9a9aa1", display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}>
            <IconTrophy size={28} />
          </div>
          <div style={{ fontSize: 17, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>No leagues yet</div>
          <div style={{ fontSize: 14, color: "#6b6b72", marginBottom: 28, maxWidth: 420 }}>
            Group multiple team stores under a league to manage them together and roll up reporting.
          </div>
          <Button variant="primary" size="lg" onClick={onAddLeague}>Create League</Button>
        </div>
      ) : finalLeagues.length === 0 ? (
        <div style={{ gridColumn: "1 / -1", padding: "92px 20px", textAlign: "center", color: "#9a9aa1", fontSize: 14 }}>
          No leagues match “{filter}”.
        </div>
      ) : (
        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(248px, 1fr))", gap: 16 }}>
          {finalLeagues.map((l) => (
            <LeagueCard key={l.id} league={l} storeCount={leagueStoreCount(l)} onOpen={() => onOpenLeague && onOpenLeague(l.id)} />
          ))}
        </div>
      )}

      {leagues.length > 0 && (
        <div style={{ position: "fixed", bottom: 28, right: 36, zIndex: 40 }}>
          <button onClick={onAddLeague} style={{
            background: "#0e0e10", color: "#fff", border: 0, padding: "16px 24px", borderRadius: 8, cursor: "pointer",
            fontSize: 13, fontWeight: 800, letterSpacing: ".06em", display: "inline-flex", alignItems: "center", gap: 12,
            boxShadow: "0 8px 20px rgba(20,20,30,.18)",
          }}>
            <IconTrophy size={17} /> Add League
          </button>
        </div>
      )}
    </div>
  );
}

Object.assign(window, { LeaguesGrid, ListToolbar, ListPageHeader, LeagueCard, ToolbarMenu });


/* ===== ASSET 13 : 596b9d68-ecc3-4075-beeb-987e64e4e28d ===== */
// Overview — aggregate executive dashboard across every store & league.
// Brand-true PROLOOK admin styling: Archivo numerals, Inter body, red accent.

const OV_INK = "#0e0e10";
const OV_MUTED = "#6b6b72";
const OV_LINE = "#ececf0";
const OV_RED = "#e1251b";
const OV_GREEN = "#1f9d55";

function ovFmt(n) {
  return "$" + Math.round(n).toLocaleString("en-US");
}
function ovFmtK(n) {
  if (n >= 1000) return "$" + (n / 1000).toFixed(n >= 10000 ? 0 : 1) + "K";
  return "$" + Math.round(n);
}

// Deterministic revenue series near `total` for a given time scale:
// "day" (last 14 days), "month" (trailing 9 months), or "year" (last 4 years).
function buildRevenueSeries(total, scale) {
  if (scale === "day") {
    const days = 14;
    const now = new Date();
    const daily = total / 30; // approx daily run-rate
    const out = [];
    for (let i = days - 1; i >= 0; i--) {
      const d = new Date(now);
      d.setDate(now.getDate() - i);
      const w = 0.55 + 0.9 * Math.abs(Math.sin((d.getDate() + d.getMonth() * 3) * 1.3));
      out.push({
        label: d.toLocaleDateString("en-US", { month: "numeric", day: "numeric" }),
        revenue: Math.round(daily * w),
      });
    }
    return out;
  }
  if (scale === "year") {
    const nowY = new Date().getFullYear();
    const factors = [0.42, 0.61, 0.78, 1.0]; // growth curve into the current year
    const annual = total * 12; // annualized run-rate
    return factors.map((f, i) => ({
      label: String(nowY - (factors.length - 1) + i),
      revenue: Math.round(annual * f),
      current: i === factors.length - 1,
    }));
  }
  // month (default) — trailing 12 months ending in the current month.
  const weights = [0.6, 0.8, 1.1, 0.7, 0.9, 1.2, 1.35, 1.5, 1.0, 1.15, 1.3, 1.05];
  const wsum = weights.reduce((a, b) => a + b, 0);
  const now = new Date();
  return weights.map((w, i) => {
    const d = new Date(now.getFullYear(), now.getMonth() - (11 - i), 1);
    return { label: d.toLocaleDateString("en-US", { month: "short" }), revenue: Math.round((total * w) / wsum), current: i === 11 };
  });
}

function StatCard({ label, value, icon, delta, deltaDir = "up", sub, onClick }) {
  const Trend = deltaDir === "up" ? IconTrendUp : IconTrendDown;
  const trendColor = deltaDir === "up" ? OV_GREEN : OV_RED;
  return (
    <div
      onClick={onClick}
      role={onClick ? "button" : undefined}
      title={onClick ? "View all orders" : undefined}
      style={{
        background: "#fff", border: `1px solid ${OV_LINE}`, borderRadius: 12, padding: "20px 24px",
        display: "flex", flexDirection: "column", gap: 16,
        cursor: onClick ? "pointer" : "default",
        transition: "box-shadow .15s, border-color .15s, transform .15s",
      }}
      onMouseEnter={onClick ? (e) => { e.currentTarget.style.borderColor = "#c4c4c8"; e.currentTarget.style.boxShadow = "0 6px 18px rgba(20,20,30,.07)"; e.currentTarget.style.transform = "translateY(-1px)"; } : undefined}
      onMouseLeave={onClick ? (e) => { e.currentTarget.style.borderColor = OV_LINE; e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.transform = "none"; } : undefined}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, fontWeight: 700, letterSpacing: ".07em", color: OV_MUTED, textTransform: "uppercase" }}>
          {label}{onClick && <IconChevronRight size={13} style={{ color: "#c4c4c8" }} />}
        </span>
        <span style={{ width: 36, height: 36, borderRadius: 9, background: "#f5f5f7", color: OV_INK, display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{icon}</span>
      </div>
      <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 32, fontWeight: 800, color: OV_INK, lineHeight: 1, letterSpacing: "-.01em" }}>{value}</div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12.5 }}>
        {delta != null && (
          <span style={{ display: "inline-flex", alignItems: "center", gap: 4, color: trendColor, fontWeight: 700 }}>
            <Trend size={15} /> {delta}
          </span>
        )}
        <span style={{ color: OV_MUTED }}>{sub}</span>
      </div>
    </div>
  );
}

function RevenueScaleToggle({ scale, onChange }) {
  const scales = [
    { key: "day", label: "Day" },
    { key: "month", label: "Month" },
    { key: "year", label: "Year" },
  ];
  return (
    <div style={{ display: "inline-flex", background: "#f5f5f7", border: `1px solid ${OV_LINE}`, borderRadius: 9, padding: 4, gap: 2 }}>
      {scales.map((s) => {
        const active = scale === s.key;
        return (
          <button
            key={s.key}
            onClick={() => onChange(s.key)}
            style={{
              border: 0, cursor: "pointer", borderRadius: 6, padding: "8px 16px",
              fontSize: 12.5, fontWeight: 700, fontFamily: "inherit",
              background: active ? "#fff" : "transparent",
              color: active ? OV_INK : OV_MUTED,
              boxShadow: active ? "0 1px 2px rgba(20,20,30,.10)" : "none",
              transition: "background .12s, color .12s",
            }}>
            {s.label}
          </button>
        );
      })}
    </div>
  );
}

function RevenueChart({ totalRevenue = 0, scale = "month" }) {
  const data = React.useMemo(() => buildRevenueSeries(totalRevenue, scale), [totalRevenue, scale]);
  const [hover, setHover] = React.useState(null);
  const maxVal = Math.max(...data.map((d) => d.revenue), 1);
  // $200 increments by default; widen (still in $200 multiples) when a scale's
  // values are large enough that a flat $200 step would flood the axis.
  const step = 200 * Math.max(1, Math.ceil(maxVal / 200 / 6));
  const axisMax = Math.max(step, Math.ceil(maxVal / step) * step);
  const ticks = axisMax / step;
  const denseX = data.length > 12; // daily view — thin the bars/labels
  return (
    <div>
      <div style={{ position: "relative", height: 340, paddingLeft: 52, paddingBottom: 28 }}>
        {/* Y axis — every $200 */}
        <div style={{ position: "absolute", left: 0, top: 0, bottom: 28, width: 48, display: "flex", flexDirection: "column", justifyContent: "space-between", alignItems: "flex-end", fontSize: 11, color: "#a4a4ab", fontFamily: "JetBrains Mono, monospace" }}>
          {Array.from({ length: ticks + 1 }).map((_, i) => (
            <span key={i}>{ovFmt(axisMax - i * step)}</span>
          ))}
        </div>
        {/* Plot */}
        <div style={{ position: "relative", height: "100%", borderLeft: `1px solid ${OV_LINE}`, borderBottom: `1px solid ${OV_LINE}` }}>
          {/* grid */}
          {Array.from({ length: ticks }).map((_, i) => (
            <div key={i} style={{ position: "absolute", left: 0, right: 0, top: `${(i / ticks) * 100}%`, borderTop: "1px dashed #f1f1f3" }} />
          ))}
          <div style={{ position: "absolute", inset: "8px 8px 0", display: "flex", alignItems: "flex-end", justifyContent: "space-between", gap: denseX ? 6 : 8 }}>
            {data.map((d, i) => {
              const totalH = (d.revenue / axisMax) * 100;
              return (
                <div key={d.label + i} onMouseEnter={() => setHover(i)} onMouseLeave={() => setHover(null)}
                  style={{ position: "relative", flex: 1, display: "flex", flexDirection: "column", alignItems: "center", gap: 8, height: "100%", justifyContent: "flex-end", cursor: "default" }}>
                  {hover === i && (
                    <div style={{ position: "absolute", bottom: `calc(${totalH}% + 10px)`, left: "50%", transform: "translateX(-50%)", background: OV_INK, color: "#fff", borderRadius: 8, padding: "8px 12px", whiteSpace: "nowrap", zIndex: 5, boxShadow: "0 6px 18px rgba(20,20,30,.22)", pointerEvents: "none" }}>
                      <div style={{ fontSize: 11, color: "#a4a4ab", marginBottom: 2 }}>{d.label}{d.current ? " · current" : ""}</div>
                      <div style={{ fontSize: 13.5, fontWeight: 700, fontFamily: "JetBrains Mono, monospace" }}>{"$" + d.revenue.toLocaleString("en-US")}</div>
                    </div>
                  )}
                  <div style={{ width: "100%", maxWidth: denseX ? 22 : 48, height: `${totalH}%`, borderRadius: 5, background: d.current ? OV_RED : OV_INK, opacity: hover === i ? 0.82 : 1, transition: "opacity .12s" }} />
                  <span style={{ fontSize: denseX ? 9.5 : 11, color: d.current ? OV_INK : OV_MUTED, fontWeight: d.current ? 700 : 400, position: "absolute", bottom: -24, whiteSpace: "nowrap" }}>{d.label}</span>
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  );
}


// Combined Revenue & Dealer Profit chart (exploration 10c) — KPI header + dual-line
// chart. Revenue = violet (left axis), Dealer Profit = amber (right axis).
const RPC_REV = "#7c3aed";
const RPC_PROF = "#d97706";
function RevenueProfitChart({ totalRevenue = 0, scale = "month" }) {
  const rev = React.useMemo(() => buildRevenueSeries(totalRevenue, scale), [totalRevenue, scale]);
  // Per-period margin varies ~30–38% (deterministic) so the profit line carries
  // its own shape instead of tracing revenue exactly.
  const pts = React.useMemo(() => rev.map((d, i) => {
    const margin = 0.3 + 0.08 * (((i * 37) % 10) / 10);
    return { ...d, profit: Math.round(d.revenue * margin), margin };
  }), [rev]);
  const [hover, setHover] = React.useState(null);
  const wrapRef = React.useRef(null);
  const [w, setW] = React.useState(900);
  React.useEffect(() => {
    const el = wrapRef.current; if (!el) return;
    const ro = new ResizeObserver(() => setW(el.clientWidth || 900));
    ro.observe(el); setW(el.clientWidth || 900);
    return () => ro.disconnect();
  }, []);
  const tickStep = (v) => { const raw = Math.max(v, 1) / 5; const p = Math.pow(10, Math.floor(Math.log10(raw))); const m = raw / p; const s = m <= 1 ? 1 : m <= 2 ? 2 : m <= 2.5 ? 2.5 : m <= 5 ? 5 : 10; return s * p; };
  const revTop = Math.max(...pts.map((d) => d.revenue), 1);
  const profTop = Math.max(...pts.map((d) => d.profit), 1);
  const revStep = tickStep(revTop), profStep = tickStep(profTop);
  const revMax = Math.ceil(revTop / revStep) * revStep;
  const profMax = Math.ceil(profTop / profStep) * profStep;
  const revTicks = Array.from({ length: Math.round(revMax / revStep) + 1 }, (_, i) => i * revStep);
  const profTicks = Array.from({ length: Math.round(profMax / profStep) + 1 }, (_, i) => i * profStep);
  const X0 = 56, X1 = w - 56, Y0 = 16, Y1 = 336;
  const n = pts.length;
  const x = (i) => X0 + (i * (X1 - X0)) / (n - 1);
  const yR = (v) => Y1 - (v / revMax) * (Y1 - Y0);
  const yP = (v) => Y1 - (v / profMax) * (Y1 - Y0);
  const smooth = (arr) => arr.map((p, i) => {
    if (i === 0) return "M" + p[0] + "," + p[1];
    const q = arr[i - 1], c = (p[0] - q[0]) / 2;
    return "C" + (q[0] + c) + "," + q[1] + " " + (p[0] - c) + "," + p[1] + " " + p[0] + "," + p[1];
  }).join(" ");
  const revPath = smooth(pts.map((d, i) => [x(i), yR(d.revenue)]));
  const profPath = smooth(pts.map((d, i) => [x(i), yP(d.profit)]));
  const last = pts[n - 1], prev = pts[n - 2] || last;
  const pctd = (a, b) => (b ? Math.round((Math.abs(a - b) / b) * 100) : 0);
  const dollars = (v) => "$" + Math.round(v).toLocaleString("en-US");
  const dense = n > 12;
  const stepPx = (X1 - X0) / (n - 1);
  const marginPts = Math.round((last.margin - prev.margin) * 100);
  const kpis = [
    { label: "Revenue · " + last.label, value: dollars(last.revenue), up: last.revenue >= prev.revenue, delta: (last.revenue >= prev.revenue ? "+" : "-") + pctd(last.revenue, prev.revenue) + "%" },
    { label: "Dealer Profit · " + last.label, value: dollars(last.profit), up: last.profit >= prev.profit, delta: (last.profit >= prev.profit ? "+" : "-") + pctd(last.profit, prev.profit) + "%" },
    { label: "Margin · " + last.label, value: Math.round(last.margin * 100) + "%", up: marginPts >= 0, delta: (marginPts >= 0 ? "+" : "") + marginPts + " pts" },
  ];
  return (
    <div>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 12 }}>
        {kpis.map((k) => (
          <div key={k.label} style={{ minWidth: 200, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 8, padding: 16, background: "#fff", display: "flex", flexDirection: "column", gap: 3.5, alignItems: "flex-start" }}>
            <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".03em", lineHeight: "16px", color: "#71717a" }}>{k.label}</div>
            <div style={{ display: "flex", alignItems: "baseline", gap: 8 }}>
              <span style={{ fontSize: 24, fontWeight: 700, lineHeight: "32px", letterSpacing: "-.01em", color: OV_INK }}>{k.value}</span>
              <span style={{ fontSize: 14, fontWeight: 700, lineHeight: "20px", letterSpacing: ".01em", color: k.up ? OV_GREEN : OV_RED }}>{k.delta}</span>
            </div>
          </div>
        ))}
      </div>
      <div style={{ display: "flex", justifyContent: "flex-end", alignItems: "center", gap: 20, padding: "14px 0 10px", fontSize: 12.5, fontWeight: 600, color: OV_INK }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 7 }}><span style={{ width: 9, height: 9, borderRadius: 999, background: RPC_REV }} />Revenue <span style={{ color: OV_MUTED, fontWeight: 400 }}>(left axis)</span></span>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 7 }}><span style={{ width: 9, height: 9, borderRadius: 999, background: RPC_PROF }} />Dealer Profit <span style={{ color: OV_MUTED, fontWeight: 400 }}>(right axis)</span></span>
      </div>
      <div ref={wrapRef} style={{ position: "relative" }}>
        <svg viewBox={`0 0 ${w} 380`} style={{ width: "100%", height: 380, display: "block" }}>
          <g stroke="#f1f1f3" strokeDasharray="3 4">
            {revTicks.slice(1).map((t) => <line key={t} x1={X0} y1={yR(t)} x2={X1} y2={yR(t)} />)}
          </g>
          <line x1={X0} y1={Y1} x2={X1} y2={Y1} stroke={OV_LINE} />
          <g fontSize="11" fill="#a4a4ab" textAnchor="end" fontFamily="JetBrains Mono, monospace">
            {revTicks.map((t) => <text key={t} x={X0 - 7} y={yR(t) + 3.5}>{t === 0 ? "$0" : ovFmt(t)}</text>)}
          </g>
          <g fontSize="11" fill="#b45309" textAnchor="start" fontFamily="JetBrains Mono, monospace">
            {profTicks.map((t) => <text key={t} x={X1 + 7} y={yP(t) + 3.5}>{t === 0 ? "$0" : ovFmt(t)}</text>)}
          </g>
          {hover != null && <line x1={x(hover)} y1={Y0} x2={x(hover)} y2={Y1} stroke="#d4d4d8" strokeDasharray="3 3" />}
          <path d={revPath} fill="none" stroke={RPC_REV} strokeWidth="2.5" strokeLinecap="round" />
          <path d={profPath} fill="none" stroke={RPC_PROF} strokeWidth="2.5" strokeLinecap="round" />
          {hover != null && (
            <g>
              <circle cx={x(hover)} cy={yR(pts[hover].revenue)} r="4" fill="#fff" stroke={RPC_REV} strokeWidth="2.5" />
              <circle cx={x(hover)} cy={yP(pts[hover].profit)} r="4" fill="#fff" stroke={RPC_PROF} strokeWidth="2.5" />
            </g>
          )}
          {hover == null && (
            <g>
              <circle cx={X1} cy={yR(last.revenue)} r="4" fill="#fff" stroke={RPC_REV} strokeWidth="2.5" />
              <circle cx={X1} cy={yP(last.profit)} r="4" fill="#fff" stroke={RPC_PROF} strokeWidth="2.5" />
            </g>
          )}
          <g fontSize={dense ? 9.5 : 11} fill="#9a9aa1" textAnchor="middle">
            {pts.map((d, i) => (
              <text key={d.label + i} x={x(i)} y={364} fontWeight={d.current || i === n - 1 ? 700 : 400} fill={d.current || i === n - 1 ? OV_INK : "#9a9aa1"}>{d.label}</text>
            ))}
          </g>
        </svg>
        {pts.map((d, i) => (
          <div key={"h" + i} onMouseEnter={() => setHover(i)} onMouseLeave={() => setHover(null)}
            style={{ position: "absolute", top: 0, bottom: 44, left: x(i) - stepPx / 2, width: stepPx }} />
        ))}
        {hover != null && (
          <div style={{ position: "absolute", top: -6, left: x(hover), transform: "translateX(-50%)", background: OV_INK, color: "#fff", borderRadius: 8, padding: "8px 12px", whiteSpace: "nowrap", zIndex: 5, boxShadow: "0 6px 18px rgba(20,20,30,.22)", pointerEvents: "none" }}>
            <div style={{ fontSize: 11, color: "#a4a4ab", marginBottom: 3 }}>{pts[hover].label}{pts[hover].current ? " · current" : ""}</div>
            <div style={{ fontSize: 12.5, fontWeight: 700, fontFamily: "JetBrains Mono, monospace", color: "#c4b5fd" }}>{dollars(pts[hover].revenue)} <span style={{ color: "#a4a4ab", fontWeight: 400 }}>rev</span></div>
            <div style={{ fontSize: 12.5, fontWeight: 700, fontFamily: "JetBrains Mono, monospace", color: "#fcd34d" }}>{dollars(pts[hover].profit)} <span style={{ color: "#a4a4ab", fontWeight: 400 }}>profit · {Math.round(pts[hover].margin * 100)}%</span></div>
          </div>
        )}
      </div>
    </div>
  );
}

function fmtWindowDate(d) {
  if (!d) return "";
  return new Date(d).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
}

function OrderingWindowRow({ name, color, mono, daysLeft, pct, raised, startDate, endDate }) {
  const startLabel = fmtWindowDate(startDate);
  const endLabel = fmtWindowDate(endDate);
  const dayCount = Math.max(Math.round((endDate - startDate) / 86400000), 1);
  return (
    <div style={{ padding: "20px 0", borderBottom: `1px solid ${OV_LINE}` }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}>
        <span style={{ width: 32, height: 32, borderRadius: "50%", background: color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11, flexShrink: 0 }}>{mono}</span>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ fontSize: 13.5, fontWeight: 700, color: OV_INK, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{name}</div>
          <div style={{ fontSize: 12, color: OV_MUTED, marginTop: 1 }}>{dayCount} day window</div>
        </div>
        <span style={{
          flexShrink: 0, fontSize: 13, fontWeight: 700, borderRadius: 999, padding: "8px 16px",
          background: "#dcf6e3", color: "#1f9d55",
        }}>{ovFmt(raised)} raised</span>
      </div>
      <div style={{ height: 8, borderRadius: 999, background: "#ececef", overflow: "hidden" }}>
        <div style={{ width: `${pct}%`, height: "100%", borderRadius: 999, background: "#5cb924" }} />
      </div>
      <div style={{ display: "flex", justifyContent: "space-between", marginTop: 12 }}>
        <span style={{ fontSize: 12.5, fontWeight: 600, color: "#9a9aa1" }}>{startLabel}</span>
        <span style={{ fontSize: 12.5, fontWeight: 600, color: "#9a9aa1" }}>{endLabel}</span>
      </div>
    </div>
  );
}

// Sortable column header for the Top Performing Stores leaderboard.
function TopSortHead({ label, sortKey, sort, onSort, align, pad }) {
  const active = sort.key === sortKey;
  return (
    <button
      onClick={() => onSort(sortKey)}
      title={active ? `Sorted ${sort.dir === "desc" ? "descending" : "ascending"}` : "Sort by " + label}
      style={{
        display: "inline-flex", alignItems: "center", gap: 5,
        justifyContent: align === "right" ? "flex-end" : "flex-start",
        paddingRight: pad || 0, background: "transparent", border: 0, cursor: "pointer", padding: `0 ${pad || 0}px 0 0`,
        font: "inherit", fontSize: 11, fontWeight: 700, letterSpacing: ".05em", textTransform: "uppercase",
        color: active ? OV_INK : OV_MUTED, whiteSpace: "nowrap", minWidth: 0,
      }}>
      <span style={{ overflow: "hidden", textOverflow: "ellipsis" }}>{label}</span>
      <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.5" strokeLinecap="round" strokeLinejoin="round" style={{ flexShrink: 0, opacity: active ? 1 : 0.45 }}>
        {active
          ? (sort.dir === "desc"
              ? <><path d="M12 5v14" /><path d="M6 13l6 6 6-6" /></>
              : <><path d="M12 19V5" /><path d="M6 11l6-6 6 6" /></>)
          : <><path d="M8 9l4-4 4 4" /><path d="M8 15l4 4 4-4" /></>}
      </svg>
    </button>
  );
}

// Overview "Roster Bank" row — mirrors the OrderingWindowRow treatment: one row
// per store with proposed players waiting in its bank zone.
function RosterBankRow({ name, color, mono, team, proposed, captures, onReview }) {
  const [hover, setHover] = React.useState(false);
  return (
    <div onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}
      style={{ display: "flex", alignItems: "center", gap: 12, padding: "16px 0", borderBottom: "1px solid #f4f4f6" }}>
      <span style={{ width: 32, height: 32, borderRadius: "50%", background: color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11, flexShrink: 0 }}>{mono}</span>
      <span style={{ flex: 1, minWidth: 0 }}>
        <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: OV_INK, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{name}</span>
        <span style={{ display: "block", fontSize: 12, color: OV_MUTED, marginTop: 1, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{team} · {captures} capture{captures === 1 ? "" : "s"}</span>
      </span>
      <span style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 6, fontSize: 11, fontWeight: 800, color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "4px 10px" }}><IconBankInbox size={12} /> {proposed} proposed</span>
      <button type="button" onClick={onReview} style={{ flexShrink: 0, background: hover ? "#fff" : "transparent", border: `1px solid ${hover ? "#d4d4d8" : "transparent"}`, color: "#0e0e10", borderRadius: 6, padding: "7px 14px", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Review</button>
    </div>);
}

function Panel({ title, subtitle, action, onAction, right, children, style }) {
  return (
    <div style={{ background: "#fff", border: `1px solid ${OV_LINE}`, borderRadius: 12, padding: "20px 24px", ...style }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
        <div>
          <h3 style={{ fontSize: 16, fontWeight: 800, margin: 0, color: OV_INK, letterSpacing: ".01em" }}>{title}</h3>
          {subtitle && <div style={{ fontSize: 12, color: OV_MUTED, marginTop: 4, lineHeight: 1.45 }}>{subtitle}</div>}
        </div>
        {right}
        {action && <button onClick={onAction} style={{ background: "transparent", border: 0, cursor: "pointer", color: "#2563eb", fontSize: 13, fontWeight: 600, padding: 0 }}>{action}</button>}
      </div>
      {children}
    </div>
  );
}

// #9 — Store-scope multi-select for the Overview. Defaults to none selected
// (= all stores). Picking stores filters every KPI and panel above.
function StoreScopeSelector({ stores = [], scope, onChange }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, [open]);
  const toggle = (id) => {
    const next = new Set(scope);
    next.has(id) ? next.delete(id) : next.add(id);
    onChange(next);
  };
  const label = scope.size === 0 ? "All Stores" : scope.size === 1
    ? (stores.find((s) => scope.has(s.id))?.teamName || stores.find((s) => scope.has(s.id))?.storeTitle || "1 store")
    : `${scope.size} stores`;
  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button onClick={() => setOpen((o) => !o)} style={{
        display: "inline-flex", alignItems: "center", gap: 8,
        background: scope.size ? "#f1f1f3" : "#fff", border: `1px solid ${scope.size ? "#c4c4c8" : "#d4d4d8"}`,
        borderRadius: 8, padding: "12px 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 600, color: "#0e0e10",
      }}>
        <IconStore size={16} /> {label} <IconChevron size={15} style={{ color: "#6b6b72" }} />
      </button>
      {open &&
      <div style={{
        position: "absolute", top: "calc(100% + 8px)", right: 0, zIndex: 60,
        width: 280, background: "#fff", border: "1px solid #e5e5e9", borderRadius: 10,
        boxShadow: "0 12px 32px rgba(20,20,30,.14)", overflow: "hidden",
      }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "12px 16px", borderBottom: "1px solid #ececf0" }}>
          <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".04em", color: "#6b6b72", textTransform: "uppercase" }}>Filter by store</span>
          {scope.size > 0 &&
          <button onClick={() => onChange(new Set())} style={{ background: "none", border: 0, padding: 0, cursor: "pointer", fontSize: 12.5, fontWeight: 600, color: "#e1251b" }}>Clear</button>
          }
        </div>
        <div style={{ maxHeight: 280, overflowY: "auto", padding: "8px 0" }}>
          {stores.map((s) => {
            const on = scope.has(s.id);
            const nm = s.teamName || s.storeTitle || "Store";
            return (
              <button key={s.id} onClick={() => toggle(s.id)} style={{
                width: "100%", textAlign: "left", display: "flex", alignItems: "center", gap: 12,
                background: on ? "#fafafb" : "transparent", border: 0, cursor: "pointer", padding: "8px 16px",
              }}>
                <span style={{
                  width: 16, height: 16, borderRadius: 4, flexShrink: 0,
                  background: on ? "#0e0e10" : "#fff", border: `1.5px solid ${on ? "#0e0e10" : "#c4c4c8"}`,
                  display: "inline-flex", alignItems: "center", justifyContent: "center",
                }}>
                  {on && <svg width={11} height={11} viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round"><path d="M4 12l5 5L20 6" /></svg>}
                </span>
                <span style={{ width: 24, height: 24, borderRadius: "50%", background: s.teamColors?.[0] || "#1d3a8a", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 9.5, flexShrink: 0 }}>{nm.slice(0, 2).toUpperCase()}</span>
                <span style={{ fontSize: 13.5, fontWeight: 600, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{nm}</span>
              </button>
            );
          })}
        </div>
      </div>
      }
    </div>
  );
}

// ─────────── Frequently Visited (Overview quick-access shelf) ───────────
// One-click shortcuts to the stores this rep actually opens, ranked by visit
// count with recency breaking ties. Recorded in localStorage on every store
// open — no rep-side curation, nothing to set up. Cards are the Stores-page
// StoreCard in its condensed variant so the shelf stays ~200px tall.
const VISITS_KEY = "prolook_store_visits_v1";
function readStoreVisits() {
  try { return JSON.parse(localStorage.getItem(VISITS_KEY) || "{}") || {}; } catch (e) { return {}; }
}
function recordStoreVisit(id) {
  if (!id) return;
  try {
    const v = readStoreVisits();
    const cur = v[id] || { n: 0, ts: 0 };
    v[id] = { n: cur.n + 1, ts: Date.now() };
    localStorage.setItem(VISITS_KEY, JSON.stringify(v));
  } catch (e) {}
}
// Demo history, so the shelf reflects a rep mid-season on first load. Real
// visits accumulate on top of these counts.
const SEED_VISITS = [24, 19, 15, 9, 6, 4];
function rankFrequentStores(stores, visits) {
  return stores
    .map((s, i) => {
      const rec = visits[s.id];
      const seeded = s.seeded ? SEED_VISITS[i % SEED_VISITS.length] : 0;
      return { store: s, n: seeded + (rec ? rec.n : 0), ts: rec ? rec.ts : 0 };
    })
    .filter((r) => r.n > 0)
    .sort((a, b) => (b.n - a.n) || (b.ts - a.ts))
    .slice(0, 5);
}
function visitLabel(r) {
  if (!r.ts) return r.n + " visits";
  const days = Math.floor((Date.now() - r.ts) / 86400000);
  const when = days <= 0 ? "today" : days === 1 ? "yesterday" : new Date(r.ts).toLocaleDateString("en-US", { month: "short", day: "numeric" });
  return r.n + " visits · " + when;
}

function FrequentlyVisitedShelf({ stores = [], onManageStore, onGoStores }) {
  const [visits, setVisits] = React.useState(readStoreVisits);
  React.useEffect(() => {
    const h = () => setVisits(readStoreVisits());
    window.addEventListener("focus", h);
    return () => window.removeEventListener("focus", h);
  }, []);
  const ranked = rankFrequentStores(stores, visits);
  const open = (id) => { onManageStore && onManageStore(id); };
  return (
    <div style={{ marginBottom: 20 }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 10 }}>
        <div style={{ display: "flex", alignItems: "baseline", gap: 10 }}>
          <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", color: OV_MUTED }}>Frequently Visited</span>
          <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>Updated automatically from your activity</span>
        </div>
        {ranked.length > 0 && (
          <button onClick={onGoStores} style={{ background: "transparent", border: 0, padding: 0, cursor: "pointer", color: "#2563eb", fontSize: 13, fontWeight: 600 }}>All stores →</button>
        )}
      </div>
      {ranked.length > 0 ? (
        <div style={{ display: "flex", gap: 12, alignItems: "stretch" }}>
          {ranked.map((r) => (
            <div key={r.store.id} style={{ flex: "1 1 0", minWidth: 0 }}>
              <StoreCard
                store={r.store}
                condensed
                context={visitLabel(r)}
                contextIcon={<IconClock size={11} style={{ color: "#9a9aa1", strokeWidth: 2 }} />}
                onManage={() => open(r.store.id)}
              />
            </div>
          ))}
        </div>
      ) : (
        <div style={{ border: "1px dashed #d8d8de", borderRadius: 10, background: "#fff", padding: "18px 22px", display: "flex", alignItems: "center", gap: 16 }}>
          <span style={{ width: 38, height: 38, borderRadius: 9, background: "#f5f5f7", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
            <IconClock size={18} style={{ color: "#9a9aa1" }} />
          </span>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ fontSize: 13.5, fontWeight: 800, color: OV_INK }}>Nothing to shortcut yet</div>
            <div style={{ fontSize: 12.5, color: OV_MUTED, marginTop: 3 }}>Open a store and it earns its place here — the stores you work most land in this row.</div>
          </div>
          <button onClick={onGoStores} style={{ flexShrink: 0, background: "#fff", border: "1px solid " + OV_INK, borderRadius: 7, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, color: OV_INK, cursor: "pointer" }}>Browse stores →</button>
        </div>
      )}
    </div>
  );
}

function OverviewDashboard({ stores = [], leagues = [], onManageStore, onGoStores, onViewOrders, onAdd, isEmpty = false }) {
  // #9 — multi-select store scope. Empty = all stores (default). Selecting a
  // subset recomputes every KPI / panel below against just those stores.
  const [search, setSearch] = React.useState("");
  const [scope, setScope] = React.useState(new Set());
  const [moneyScale, setMoneyScale] = React.useState("month");
  const [topSort, setTopSort] = React.useState({ key: "revenue", dir: "desc" });
  const toggleTopSort = (key) => setTopSort((s) => (s.key === key ? { key, dir: s.dir === "desc" ? "asc" : "desc" } : { key, dir: "desc" }));
  const scoped = scope.size ? stores.filter((s) => scope.has(s.id)) : stores;
  const totalRevenue = scoped.reduce((s, x) => s + (x.revenue || 0), 0) || 18420;
  // Profit = gross revenue minus COGS, platform fee, sales tax, and team
  // fundraising (when applicable). Modeled here at a blended ~34% net margin.
  const totalProfit = Math.round(totalRevenue * 0.34);
  const liveCount = scoped.filter((s) => s.live || s.status === "active").length || scoped.length;
  const orders = Math.max(Math.round(totalRevenue / 46), scoped.length * 8);
  const aov = orders ? totalRevenue / orders : 0;

  // Active ordering windows (set-window stores not yet closed).
  const today = new Date("2026-06-04");
  // Staggered seed windows — distinct start dates per team, spans of 30 / 60 / 90 days.
  const seedWinStart = ["2026-05-20", "2026-05-01", "2026-04-10", "2026-05-25"];
  const seedWinSpan = [30, 60, 90, 60];
  const windows = scoped
    .map((s, i) => {
      const startISO = s.storeOpenStart || seedWinStart[i % 4];
      const start = new Date(startISO);
      const end = s.storeOpenEnd
        ? new Date(s.storeOpenEnd)
        : new Date(new Date(startISO).getTime() + seedWinSpan[i % 4] * 86400000);
      const total = Math.max((end - start) / 86400000, 1);
      const elapsed = Math.min(Math.max((today - start) / 86400000, 0), total);
      const daysLeft = Math.max(Math.round((end - today) / 86400000), 0);
      return {
        id: s.id,
        name: s.teamName || s.storeTitle,
        color: s.teamColors?.[0] || "#1d3a8a",
        mono: (s.teamName || s.storeTitle || "T").slice(0, 2).toUpperCase(),
        pct: Math.round((elapsed / total) * 100),
        daysLeft,
        raised: s.revenue || 0,
        startDate: start,
        endDate: end,
      };
    })
    .sort((a, b) => a.daysLeft - b.daysLeft)
    .slice(0, 4);

  // Roster Bank rollup — proposed players waiting in each store's bank zone
  // (seeded captures + live storefront captures from the consent-gated gate).
  let liveCaps = 0;
  try { liveCaps = (JSON.parse(localStorage.getItem("prolook_bank_captures_v1") || "[]") || []).length; } catch (e) {}
  const bankRows = scoped.map((s) => {
    const prof = s.rosterProfile;
    const bulk = prof === "teams-only" ? { ...bankSeedCounts(WASATCH_BANK_SEED), team: "18 teams mapped · no rosters yet · " + (s.sport || "—") }
      : prof === "unstructured" ? { ...bankSeedCounts(BAYSHORE_BANK_SEED), team: "No teams set up · team gear locked" } : null;
    return {
      id: s.id,
      name: s.teamName || s.storeTitle,
      color: s.teamColors?.[0] || "#1d3a8a",
      mono: (s.teamName || s.storeTitle || "T").slice(0, 2).toUpperCase(),
      team: bulk ? bulk.team : ((s.storeType || "league") === "league" ? "Lone Peak 8U" : (s.teamName || "Team Roster")) + " · " + (s.sport || "—"),
      proposed: bulk ? bulk.proposed : 4 + liveCaps,
      captures: bulk ? bulk.captures : 6 + liveCaps,
    };
  }).sort((a, b) => b.proposed - a.proposed).slice(0, 4);
  const bankTotal = bankRows.reduce((n, b) => n + b.proposed, 0);

  // Top stores leaderboard
  const top = [...scoped]
    .sort((a, b) => (b.revenue || 0) - (a.revenue || 0))
    .slice(0, 5)
    .map((s, i) => {
      const rev = s.revenue || 0;
      const ords = Math.round(rev / 44) + 6;
      const rosterSize = [58, 55, 62, 48, 40][i] ?? 50;
      const ordered = [92, 85, 78, 71, 64][i] ?? 60;
      const seedCreated = ["2026-01-14", "2026-02-03", "2026-02-27", "2026-03-19", "2026-04-08"];
      const createdISO = s.createdDate || s.storeOpenStart || seedCreated[i % 5];
      const created = new Date(createdISO).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" });
      const createdTs = new Date(createdISO).getTime();
      return {
        id: s.id,
        name: s.teamName || s.storeTitle,
        color: s.teamColors?.[0] || "#1d3a8a",
        mono: (s.teamName || s.storeTitle || "T").slice(0, 2).toUpperCase(),
        sport: s.sport || "—",
        created,
        createdTs,
        orders: ords,
        revenue: rev,
        aov: ords ? rev / ords : 0,
        pctOfTotal: totalRevenue ? (rev / totalRevenue) * 100 : 0,
        rosterSize,
        rosterOrdered: Math.round((ordered / 100) * rosterSize),
        completion: ordered,
        trend: [12.4, 8.1, -3.2, 5.6, 2.1][i] ?? 1.0,
      };
    });

  const sortedTop = React.useMemo(() => {
    const { key, dir } = topSort;
    const mul = dir === "asc" ? 1 : -1;
    return [...top].sort((a, b) => {
      if (key === "name") return a.name.localeCompare(b.name) * mul;
      const ka = key === "created" ? a.createdTs : a[key];
      const kb = key === "created" ? b.createdTs : b[key];
      return ((ka || 0) - (kb || 0)) * mul;
    });
  }, [top, topSort]);

  if (isEmpty) {
    const GHOST_BARS = [32, 48, 68, 42, 58, 78, 88, 92, 55];
    const GHOST_MONTHS = ["Oct","Nov","Dec","Jan","Feb","Mar","Apr","May","Jun"];
    const GHOST_KPI = [
      { label: "Total Revenue",    icon: React.createElement(IconDollar,  { size: 18 }), hint: "Your total cumulative payout revenue and performance trends will track here in real time." },
      { label: "Orders",           icon: React.createElement(IconReceipt, { size: 18 }), hint: "Order volume across all active stores will accumulate and display here as your athletes shop." },
      { label: "Active Stores",    icon: React.createElement(IconStore,   { size: 18 }), hint: "The count of live, order-accepting stores in your portfolio will reflect here automatically." },
      { label: "Avg. Order Value", icon: React.createElement(IconWallet,  { size: 18 }), hint: "Average dollars spent per order — a key indicator of athlete basket size — will populate here." },
    ];
    return (
      <div style={{ flex: 1, minWidth: 0, padding: "28px 32px 80px", background: "#fbfbfc" }}>
        <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", marginBottom: 24 }}>
          <div>
            <h1 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, margin: 0, color: OV_INK, letterSpacing: "-.3px" }}>Overview</h1>
            <div style={{ fontSize: 14, color: OV_MUTED, marginTop: 4 }}>Launch your first store to start tracking performance</div>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <button onClick={onAdd} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: OV_INK, border: 0, borderRadius: 8, padding: "12px 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 700, color: "#fff" }}>
              <IconPlus size={16} style={{ strokeWidth: 2.4 }} /> Add Store
            </button>
            <button disabled style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#f5f5f7", border: "1px solid #ececf0", borderRadius: 8, padding: "12px 16px", cursor: "not-allowed", fontSize: 13.5, fontWeight: 600, color: "#c4c4c8" }}>
              <IconStore size={16} /> All Stores <IconChevron size={15} />
            </button>
          </div>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 16, marginBottom: 16 }}>
          {GHOST_KPI.map((c) => (
            <div key={c.label} style={{ background: "#fff", border: `1px solid ${OV_LINE}`, borderRadius: 12, padding: "20px 24px", display: "flex", flexDirection: "column", gap: 16 }}>
              <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
                <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".07em", color: "#c4c4c8", textTransform: "uppercase" }}>{c.label}</span>
                <span style={{ width: 36, height: 36, borderRadius: 9, background: "#f5f5f7", color: "#d4d4d8", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{c.icon}</span>
              </div>
              <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 32, fontWeight: 800, color: "#d8d8dc", lineHeight: 1, letterSpacing: "-.01em" }}>—</div>
              <div style={{ fontSize: 12, color: "#c4c4c8" }}>{c.hint}</div>
            </div>
          ))}
        </div>
        <div style={{ background: "#fff", border: `1px solid ${OV_LINE}`, borderRadius: 12, padding: "20px 24px", marginBottom: 16, display: "flex", alignItems: "center", justifyContent: "space-between", gap: 24 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
            <div style={{ width: 44, height: 44, borderRadius: 10, background: "#fff0ef", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
              <IconStore size={22} style={{ color: OV_RED }} />
            </div>
            <div>
              <div style={{ fontSize: 15, fontWeight: 800, color: OV_INK, marginBottom: 4 }}>Your dashboard is ready — no stores are live yet</div>
              <div style={{ fontSize: 13.5, color: OV_MUTED }}>Revenue, orders, and analytics populate the moment a store goes live and receives its first order.</div>
            </div>
          </div>
          <button onClick={onGoStores} style={{ flexShrink: 0, background: OV_INK, color: "#fff", border: 0, borderRadius: 8, padding: "12px 24px", fontSize: 13.5, fontWeight: 700, cursor: "pointer", whiteSpace: "nowrap" }}>
            Create a Store →
          </button>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "minmax(0,1fr) 380px", gap: 16, alignItems: "start" }}>
          <div style={{ display: "flex", flexDirection: "column", gap: 16, minWidth: 0 }}>
            <Panel title="Dealer Profit">
              <div style={{ position: "relative", height: 260, paddingLeft: 48, paddingBottom: 28 }}>
                <div style={{ position: "absolute", left: 0, top: 0, bottom: 28, width: 44, display: "flex", flexDirection: "column", justifyContent: "space-between", alignItems: "flex-end" }}>
                  {[0,1,2,3,4].map((i) => <div key={i} style={{ width: 28, height: 12, background: "#ebebed", borderRadius: 4 }} />)}
                </div>
                <div style={{ position: "relative", height: "100%", borderLeft: `1px solid ${OV_LINE}`, borderBottom: `1px solid ${OV_LINE}` }}>
                  {[0,1,2,3].map((i) => <div key={i} style={{ position: "absolute", left: 0, right: 0, top: `${(i/4)*100}%`, borderTop: "1px dashed #f1f1f3" }} />)}
                  <div style={{ position: "absolute", inset: "8px 8px 0", display: "flex", alignItems: "flex-end", justifyContent: "space-between", gap: 12 }}>
                    {GHOST_BARS.map((h, i) => (
                      <div key={i} style={{ flex: 1, display: "flex", flexDirection: "column", alignItems: "center", height: "100%", justifyContent: "flex-end" }}>
                        <div style={{ width: "100%", maxWidth: 32, height: `${h}%`, borderRadius: 5, background: "#ebebed" }} />
                        <span style={{ fontSize: 11, color: "#d4d4d8", position: "absolute", bottom: -24 }}>{GHOST_MONTHS[i]}</span>
                      </div>
                    ))}
                  </div>
                  <div style={{ position: "absolute", inset: 0, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 12 }}>
                    <div style={{ width: 40, height: 40, borderRadius: 10, background: "#f5f5f7", display: "flex", alignItems: "center", justifyContent: "center" }}>
                      <IconTrendUp size={20} style={{ color: "#c4c4c8" }} />
                    </div>
                    <div style={{ textAlign: "center" }}>
                      <div style={{ fontSize: 14, fontWeight: 700, color: OV_INK, marginBottom: 8 }}>Performance Charting</div>
                      <div style={{ fontSize: 12.5, color: OV_MUTED, maxWidth: 280, lineHeight: 1.55 }}>This space will evolve into a dynamic snapshot of your monthly team store profit margins as orders roll in.</div>
                    </div>
                  </div>
                </div>
              </div>
            </Panel>
            <Panel title="Top Performing Stores" action="All stores →" onAction={onGoStores}>
              <div>
                <div style={{ display: "grid", gridTemplateColumns: "2fr 1fr 1fr 0.8fr", gap: 12, padding: "0 0 12px", borderBottom: `1px solid ${OV_LINE}`, fontSize: 11.5, fontWeight: 700, letterSpacing: ".05em", color: "#c4c4c8", textTransform: "uppercase" }}>
                  <span>Store</span><span style={{ textAlign: "right" }}>Orders</span><span style={{ textAlign: "right" }}>Revenue</span><span style={{ textAlign: "right" }}>Trend</span>
                </div>
                {[80,65,55,45,35].map((w, i) => (
                  <div key={i} style={{ display: "grid", gridTemplateColumns: "2fr 1fr 1fr 0.8fr", gap: 12, alignItems: "center", padding: "12px 0", borderBottom: "1px solid #f4f4f6" }}>
                    <div style={{ display: "inline-flex", alignItems: "center", gap: 12 }}>
                      <div style={{ width: 32, height: 32, borderRadius: "50%", background: "#ebebed", flexShrink: 0 }} />
                      <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
                        <div style={{ width: w, height: 12, background: "#ebebed", borderRadius: 4 }} />
                        <div style={{ width: Math.round(w * 0.6), height: 8, background: "#f1f1f3", borderRadius: 4 }} />
                      </div>
                    </div>
                    <div style={{ display: "flex", justifyContent: "flex-end" }}><div style={{ width: 28, height: 12, background: "#ebebed", borderRadius: 4 }} /></div>
                    <div style={{ display: "flex", justifyContent: "flex-end" }}><div style={{ width: 48, height: 12, background: "#ebebed", borderRadius: 4 }} /></div>
                    <div style={{ display: "flex", justifyContent: "flex-end" }}><div style={{ width: 32, height: 12, background: "#ebebed", borderRadius: 4 }} /></div>
                  </div>
                ))}
                <div style={{ padding: "24px 0 8px", textAlign: "center" }}>
                  <div style={{ fontSize: 13.5, color: OV_MUTED, marginBottom: 12 }}>Store rankings will appear once sales begin</div>
                  <button onClick={onGoStores} style={{ background: "transparent", border: `1px solid ${OV_LINE}`, borderRadius: 7, padding: "8px 16px", fontSize: 13, fontWeight: 700, color: OV_INK, cursor: "pointer" }}>Create a Store →</button>
                </div>
              </div>
            </Panel>
          </div>
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            <Panel title="Active Ordering Windows">
              {[0,1,2].map((i) => (
                <div key={i} style={{ padding: "16px 0", borderBottom: `1px solid ${OV_LINE}` }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 12 }}>
                    <div style={{ width: 32, height: 32, borderRadius: "50%", background: "#ebebed", flexShrink: 0 }} />
                    <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: 4 }}>
                      <div style={{ width: "70%", height: 12, background: "#ebebed", borderRadius: 4 }} />
                      <div style={{ width: "45%", height: 8, background: "#f1f1f3", borderRadius: 4 }} />
                    </div>
                    <div style={{ width: 36, height: 24, background: "#f1f1f3", borderRadius: 999 }} />
                  </div>
                  <div style={{ height: 8, borderRadius: 999, background: "#f1f1f3" }} />
                </div>
              ))}
              <div style={{ padding: "16px 0 4px", fontSize: 12.5, color: OV_MUTED, lineHeight: 1.6 }}>Active ordering window cutoffs will organize and countdown automatically here once your stores go live.</div>
            </Panel>
            <Panel title="Roster Bank">
              {[0, 1].map((i) => (
                <div key={i} style={{ display: "flex", alignItems: "center", gap: 12, padding: "16px 0", borderBottom: `1px solid ${OV_LINE}` }}>
                  <div style={{ width: 32, height: 32, borderRadius: "50%", background: "#ebebed", flexShrink: 0 }} />
                  <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: 4 }}>
                    <div style={{ width: "60%", height: 12, background: "#ebebed", borderRadius: 4 }} />
                    <div style={{ width: "40%", height: 8, background: "#f1f1f3", borderRadius: 4 }} />
                  </div>
                  <div style={{ width: 84, height: 24, background: "#f1f1f3", borderRadius: 999 }} />
                </div>
              ))}
              <div style={{ padding: "16px 0 4px", fontSize: 12.5, color: OV_MUTED, lineHeight: 1.6 }}>Team-gear checkout captures will queue here as proposed players once buyers opt in — commit them to rosters in one click.</div>
            </Panel>
            <Panel title="Payouts">
              <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", marginBottom: 16 }}>
                <div>
                  <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 26, fontWeight: 800, color: "#d8d8dc", lineHeight: 1 }}>$—</div>
                  <div style={{ fontSize: 12, color: "#c4c4c8", marginTop: 4 }}>Available to pay out</div>
                </div>
                <div style={{ width: 64, height: 24, background: "#f1f1f3", borderRadius: 999 }} />
              </div>
              {["Pending clearance", "Paid this season"].map((label) => (
                <div key={label} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "8px 0", borderBottom: "1px solid #f4f4f6", fontSize: 13 }}>
                  <span style={{ display: "inline-flex", alignItems: "center", gap: 8, color: "#c4c4c8" }}>
                    <span style={{ width: 8, height: 8, borderRadius: "50%", background: "#d8d8dc" }} /> {label}
                  </span>
                  <span style={{ fontWeight: 700, color: "#d8d8dc" }}>$—</span>
                </div>
              ))}
              <div style={{ marginTop: 8, paddingTop: 16, borderTop: `1px solid ${OV_LINE}`, fontSize: 12.5, color: "#9a9aa1", lineHeight: 1.6 }}>Payout status, pending banking clearances, and active ordering window cutoffs will organize themselves automatically right here.</div>
            </Panel>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div style={{ flex: 1, minWidth: 0, padding: "28px max(32px, calc((100% - 1376px)/2)) 80px", background: "#fbfbfc" }}>
      {/* Page heading */}
      <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", marginBottom: 24 }}>
        <div>
          <h1 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, margin: "0 0 8px", color: OV_INK, letterSpacing: "-.3px" }}>Overview</h1>
          <div style={{ fontSize: 14, color: OV_MUTED }}>Your stores at a glance: activity, performance, and what needs attention.</div>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
          <button onClick={onAdd} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: OV_INK, border: 0, borderRadius: 8, padding: "12px 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 700, color: "#fff" }}>
            <IconPlus size={16} style={{ strokeWidth: 2.4 }} /> Add Store
          </button>
          <StoreScopeSelector stores={stores} scope={scope} onChange={setScope} />
          <div style={{ position: "relative", display: "flex", alignItems: "center", minWidth: 240, border: "1px solid #d8d8de", borderRadius: 8, background: "#fff" }}>
            <input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Search Team Stores" style={{ width: "100%", border: 0, outline: 0, background: "transparent", padding: "12px 40px 12px 16px", fontSize: 13.5 }} />
            <IconSearch size={16} style={{ position: "absolute", right: 12, color: "#9a9aa1" }} />
          </div>
        </div>
      </div>

      {/* Quick access — frequently visited stores */}
      <FrequentlyVisitedShelf stores={stores} onManageStore={onManageStore} onGoStores={onGoStores} />

      {/* KPI row */}
      <div style={{ paddingTop: 8, display: "flex", alignItems: "baseline", gap: 10, marginBottom: 10 }}>
        <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", color: OV_MUTED }}>Performance summary</span>
        <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>
          {scope.size
            ? `Performance across ${scope.size} selected store${scope.size > 1 ? "s" : ""}`
            : `Performance across ${stores.length} stores${leagues.length ? ` and ${leagues.length} league${leagues.length > 1 ? "s" : ""}` : ""}`}
        </span>
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 16, marginBottom: 16 }}>
        <StatCard label="Total Revenue" value={ovFmt(totalRevenue)} icon={<IconDollar size={18} />} delta="12.4%" deltaDir="up" sub="vs last month" />
        <StatCard label="Orders" value={orders.toLocaleString()} icon={<IconReceipt size={18} />} delta="8.1%" deltaDir="up" sub="vs last month" onClick={onViewOrders} />
        <StatCard label="Active Stores" value={String(liveCount)} icon={<IconStore size={18} />} sub={`${stores.length} total`} />
        <StatCard label="Avg. Order Value" value={ovFmt(aov)} icon={<IconWallet size={18} />} delta="3.2%" deltaDir="down" sub="vs last month" />
      </div>

      {/* Main content — full-width stacked panels */}
      <div style={{ display: "flex", flexDirection: "column", gap: 16, minWidth: 0 }}>
        {/* Dealer-side money only — team revenue & fundraising live on each store's Reports tab. */}
        <Panel title="Revenue & Dealer Profit" subtitle="Gross revenue across all stores vs. dealer profit — revenue minus manufacturing cost (COGS), platform fee, sales tax, and team fundraising." right={<RevenueScaleToggle scale={moneyScale} onChange={setMoneyScale} />}>
          <RevenueProfitChart totalRevenue={totalRevenue} scale={moneyScale} />
        </Panel>

        <Panel title="Top Performing Stores" action="All stores →" onAction={onGoStores}>
          <div>
            {/* head */}
            <div style={{ display: "grid", gridTemplateColumns: "minmax(190px,1.8fr) repeat(7, 1fr)", gap: 12, padding: "0 0 12px", borderBottom: `1px solid ${OV_LINE}`, fontSize: 11, fontWeight: 700, letterSpacing: ".05em", color: OV_MUTED, textTransform: "uppercase" }}>
              <TopSortHead label="Store" sortKey="name" sort={topSort} onSort={toggleTopSort} />
              <TopSortHead label="Store Created" sortKey="created" sort={topSort} onSort={toggleTopSort} />
              <TopSortHead label="Orders" sortKey="orders" sort={topSort} onSort={toggleTopSort} align="right" pad={24} />
              <TopSortHead label="Revenue" sortKey="revenue" sort={topSort} onSort={toggleTopSort} align="right" pad={24} />
              <TopSortHead label="Avg. Order Value" sortKey="aov" sort={topSort} onSort={toggleTopSort} align="right" pad={24} />
              <TopSortHead label="% of Total Rev." sortKey="pctOfTotal" sort={topSort} onSort={toggleTopSort} align="right" />
              <TopSortHead label="Completion" sortKey="completion" sort={topSort} onSort={toggleTopSort} align="right" />
              <TopSortHead label="Trend" sortKey="trend" sort={topSort} onSort={toggleTopSort} align="right" />
            </div>
            {sortedTop.map((s) => (
              <button key={s.id} onClick={() => onManageStore && onManageStore(s.id)} style={{
                width: "100%", textAlign: "left", background: "transparent", border: 0, cursor: "pointer",
                display: "grid", gridTemplateColumns: "minmax(190px,1.8fr) repeat(7, 1fr)", gap: 12, alignItems: "center",
                padding: "16px 0", borderBottom: `1px solid #f4f4f6`,
              }}
                onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")}
                onMouseLeave={(e) => (e.currentTarget.style.background = "transparent")}>
                <span style={{ display: "inline-flex", alignItems: "center", gap: 12, minWidth: 0 }}>
                  <span style={{ width: 32, height: 32, borderRadius: "50%", background: s.color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11, flexShrink: 0 }}>{s.mono}</span>
                  <span style={{ minWidth: 0 }}>
                    <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: OV_INK, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s.name}</span>
                    <span style={{ display: "block", fontSize: 12, color: OV_MUTED }}>{s.sport}</span>
                  </span>
                </span>
                <span style={{ fontSize: 13, color: OV_MUTED, fontFamily: "JetBrains Mono, monospace" }}>{s.created}</span>
                <span style={{ textAlign: "right", paddingRight: 24, fontSize: 13.5, color: OV_INK, fontFamily: "JetBrains Mono, monospace" }}>{s.orders}</span>
                <span style={{ textAlign: "right", paddingRight: 24, fontSize: 13.5, fontWeight: 700, color: OV_INK, fontFamily: "JetBrains Mono, monospace" }}>{ovFmt(s.revenue)}</span>
                <span style={{ textAlign: "right", paddingRight: 24, fontSize: 13.5, color: OV_INK, fontFamily: "JetBrains Mono, monospace" }}>{ovFmt(s.aov)}</span>
                <span style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 4 }}>
                  <span style={{ fontSize: 13, color: OV_INK, fontFamily: "JetBrains Mono, monospace" }}>{s.pctOfTotal.toFixed(1)}%</span>
                  <span style={{ width: "70%", height: 4, borderRadius: 999, background: "#f1f1f3", overflow: "hidden" }}>
                    <span style={{ display: "block", width: `${Math.min(s.pctOfTotal, 100)}%`, height: "100%", borderRadius: 999, background: OV_INK }} />
                  </span>
                </span>
                <span style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 4 }}>
                  <span style={{ fontSize: 13, color: OV_INK, fontFamily: "JetBrains Mono, monospace" }}>
                    {s.completion}% <span style={{ color: OV_MUTED, fontFamily: "inherit", fontSize: 11.5 }}>· {s.rosterOrdered}/{s.rosterSize}</span>
                  </span>
                  <span style={{ width: "70%", height: 4, borderRadius: 999, background: "#f1f1f3", overflow: "hidden" }}>
                    <span style={{ display: "block", width: `${Math.min(s.completion, 100)}%`, height: "100%", borderRadius: 999, background: s.completion >= 80 ? OV_GREEN : "#e0a92b" }} />
                  </span>
                </span>
                <span style={{ textAlign: "right", fontSize: 12.5, fontWeight: 700, color: s.trend >= 0 ? OV_GREEN : OV_RED, display: "inline-flex", alignItems: "center", justifyContent: "flex-end", gap: 4 }}>
                  {s.trend >= 0 ? <IconTrendUp size={14} /> : <IconTrendDown size={14} />}{Math.abs(s.trend)}%
                </span>
              </button>
            ))}
            {top.length === 0 && <div style={{ padding: "32px 0", textAlign: "center", color: OV_MUTED, fontSize: 13 }}>No stores yet.</div>}
          </div>
        </Panel>

        <Panel title="Active Ordering Windows">
          {windows.length ? windows.map((w) => <OrderingWindowRow key={w.id} {...w} />) : (
            <div style={{ padding: "24px 0", textAlign: "center", color: OV_MUTED, fontSize: 13 }}>No open windows.</div>
          )}
          <div style={{ paddingTop: 16, fontSize: 12.5, color: OV_MUTED, display: "flex", alignItems: "center", gap: 8 }}>
            <IconClock size={14} /> {windows.length} window{windows.length === 1 ? "" : "s"} open now
          </div>
        </Panel>

        <Panel title="Roster Bank" subtitle="Checkout captures awaiting roster review" style={{ marginTop: 16 }}>
          {bankRows.length ? bankRows.map((b) => (
            <RosterBankRow key={b.id} {...b} onReview={() => onManageStore && onManageStore(b.id, "roster")} />
          )) : (
            <div style={{ padding: "24px 0", textAlign: "center", color: OV_MUTED, fontSize: 13 }}>No captures waiting — team-gear orders with roster opt-in land here.</div>
          )}
          <div style={{ paddingTop: 16, fontSize: 12.5, color: OV_MUTED, display: "flex", alignItems: "center", gap: 8 }}>
            <IconBankInbox size={14} /> {bankTotal} proposed player{bankTotal === 1 ? "" : "s"} across {bankRows.length} store{bankRows.length === 1 ? "" : "s"} — commit them in My Teams
          </div>
        </Panel>
      </div>
    </div>
  );
}

Object.assign(window, { OverviewDashboard });


/* ===== Cross-store Orders (branches from the Overview "Orders" KPI) ===== */
// Aggregates orders across EVERY store, so each order row and each pending
// group carries a Store tag identifying which store it belongs to. The user
// stays inside the Overview environment (header + OVERVIEW sub-tab stay lit) —
// this is a drill-down off the Orders KPI, not a separate section.

// Store identity pill — colored initials avatar + store name.
function StoreTag({ store, size = "sm" }) {
  const color = store?.teamColors?.[0] || "#1d3a8a";
  const name = store?.teamName || store?.storeTitle || "Store";
  const initials = name.replace(/[^A-Za-z0-9 ]/g, "").split(/\s+/).map((w) => w[0]).join("").slice(0, 2).toUpperCase() || name.slice(0, 2).toUpperCase();
  const big = size === "lg";
  return (
    <span style={{
      display: "inline-flex", alignItems: "center", gap: big ? 9 : 7, maxWidth: "100%",
      background: "#fff", border: "1px solid #e5e5e9", borderRadius: 999,
      padding: big ? "5px 14px 5px 5px" : "3px 11px 3px 4px",
    }}>
      <span style={{
        width: big ? 24 : 20, height: big ? 24 : 20, borderRadius: "50%", background: color, color: "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center",
        fontWeight: 800, fontSize: big ? 10 : 9, flexShrink: 0,
      }}>{initials}</span>
      <span style={{ fontSize: big ? 13 : 12, fontWeight: 700, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{name}</span>
    </span>
  );
}

// Flatten the per-store demo datasets into a single cross-store view.
function buildCrossStoreOrders(stores) {
  const seeded = stores.filter((s) => s.seeded);
  const all = [];
  const pendingGroups = [];
  seeded.forEach((store, si) => {
    const ds = makeOrdersDataset(store);
    const merged = [
      ...(ds.myOrders || []).map((o) => ({ ...o, orderType: "my" })),
      ...(ds.fanOrders || []).map((o) => ({ ...o, orderType: "fan" })),
    ].slice(0, 4);
    merged.forEach((o) => all.push({ ...o, __store: store, organization: store.teamName || store.storeTitle }));
    // One pending group per store (rotate PMP / BLB for variety).
    const grp = (ds.pending || [])[si % Math.max((ds.pending || []).length, 1)];
    if (grp) pendingGroups.push({ ...grp, __store: store });
  });
  return { all, pendingGroups };
}

// Page-level toolbar matching the Stores/Leagues toolbar (Filters · Sort · Search).
function OrdersOverviewToolbar({ search, onSearch }) {
  const btn = {
    display: "inline-flex", alignItems: "center", gap: 8, height: 44,
    background: "#fff", border: "1px solid #d8d8de", borderRadius: 8,
    padding: "0 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 600, color: "#0e0e10",
  };
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
      <button style={btn} onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")} onMouseLeave={(e) => (e.currentTarget.style.background = "#fff")}>
        <IconFilter size={16} /> Show Filters
      </button>
      <button style={btn} onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")} onMouseLeave={(e) => (e.currentTarget.style.background = "#fff")}>
        <IconSort size={16} /> Sort By: Default
      </button>
      <div style={{ position: "relative", display: "flex", alignItems: "center", minWidth: 260, height: 44, border: "1px solid #d8d8de", borderRadius: 8, background: "#fff" }}>
        <input value={search} onChange={(e) => onSearch(e.target.value)} placeholder="Search Orders" style={{ width: "100%", border: 0, outline: 0, background: "transparent", padding: "0 40px 0 16px", fontSize: 13.5, height: "100%" }} />
        <IconSearch size={16} style={{ position: "absolute", right: 12, color: "#9a9aa1" }} />
      </div>
    </div>
  );
}

// Cross-store "Pending Orders" — same construction as the per-store
// PendingOrdersList, but each shipping group is headed by a Store tag.
function CrossStorePending({ groups, onOpen, onSubmit }) {
  const cols = [
    { key: "check", label: "", w: "24px" },
    { key: "item", label: "Item", w: "1.5fr" },
    { key: "orderDate", label: "Order Date", w: "1fr" },
    { key: "customer", label: "Customer", w: "1.3fr" },
    { key: "size", label: "Size", w: ".6fr" },
    { key: "factory", label: "Factory", w: ".8fr" },
    { key: "orderNum", label: "Order #", w: "1fr" },
  ];
  const gridTemplate = cols.map((c) => c.w || "1fr").join(" ");
  const [checked, setChecked] = React.useState({});
  const selectedCount = Object.values(checked).filter(Boolean).length;
  const noop = () => {};

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 24, paddingBottom: 80 }}>
      {groups.map((g, gi) => {
        const allChecked = g.rows.every((r) => checked[r.id]);
        return (
          <div key={(g.__store?.id || gi) + "-" + g.shippingGroup} style={{
            background: "#fafafb", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden",
          }}>
            {/* group header — Store tag is the cross-store identifier */}
            <div style={{ padding: "16px 24px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 24, flexWrap: "wrap" }}>
              <div style={{ display: "flex", flexDirection: "column", gap: 12, minWidth: 240 }}>
                <div style={{ display: "flex", alignItems: "center", gap: 12, flexWrap: "wrap" }}>
                  <StoreTag store={g.__store} size="lg" />
                  <span style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10" }}>
                    SHIPPING GROUP: {g.shippingGroup}
                  </span>
                </div>
                <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
                  <MinQuantityRing pct={g.pct} />
                  <div style={{ display: "flex", flexDirection: "column", gap: 2 }}>
                    <div style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, color: "#e1251b", fontWeight: 600, whiteSpace: "nowrap" }}>
                      Minimum Quantity
                      <span title="Below the factory minimum — a fee applies." style={{
                        width: 16, height: 16, borderRadius: "50%", background: "#fff",
                        border: "1px solid #e1251b", color: "#e1251b",
                        display: "inline-flex", alignItems: "center", justifyContent: "center",
                        fontSize: 9, fontWeight: 800,
                      }}>i</span>
                    </div>
                    <div style={{ fontSize: 12, color: "#e1251b" }}>{g.pct}%</div>
                  </div>
                </div>
              </div>
              <div style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 12, minWidth: 200 }}>
                <div style={{ textAlign: "right" }}>
                  <div style={{ fontSize: 13, fontWeight: 600, color: "#e1251b", marginBottom: 4, whiteSpace: "nowrap" }}>Minimum Quantity Fee</div>
                  <div style={{ fontSize: 22, fontWeight: 700, color: "#e1251b", fontVariantNumeric: "tabular-nums" }}>${(g.fee || 0).toFixed(2)}</div>
                </div>
                <button onClick={() => { onSubmit && onSubmit([{ shippingGroup: g.shippingGroup, fee: g.fee, pct: g.pct, store: g.__store, rows: g.rows.map((r) => ({ id: r.id, product: r.product, size: r.size, qty: r.quantity || 1, customer: r.customer })) }]); }} style={{
                  background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
                  padding: "12px 20px", fontSize: 12.5, fontWeight: 700, letterSpacing: ".05em",
                  cursor: "pointer", whiteSpace: "nowrap", transition: "background .12s",
                }}
                  onMouseEnter={(e) => e.currentTarget.style.background = "#e1251b"}
                  onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>Submit Group</button>
              </div>
            </div>

            {/* column headers */}
            <div style={{
              display: "grid", gridTemplateColumns: gridTemplate, gap: 12,
              padding: "12px 24px", borderTop: "1px solid #ececf0", borderBottom: "1px solid #ececf0",
              background: "#fff", fontSize: 11, fontWeight: 700, color: "#0e0e10",
              letterSpacing: ".06em", textTransform: "uppercase", alignItems: "center",
            }}>
              <Checkbox checked={allChecked} onChange={(v) => setChecked((c) => { const n = { ...c }; g.rows.forEach((r) => { n[r.id] = v; }); return n; })} />
              {cols.slice(1).map((c) => <div key={c.key} style={{ textAlign: c.align || "left" }}>{c.label}</div>)}
            </div>

            {/* rows */}
            <div style={{ background: "#fff" }}>
              {g.rows.map((r, i) => (
                <div key={r.id}
                  onClick={() => onOpen && onOpen({ ...r, __store: g.__store })}
                  style={{
                    display: "grid", gridTemplateColumns: gridTemplate, gap: 12,
                    padding: "16px 24px", alignItems: "center",
                    borderTop: i === 0 ? "none" : "1px solid #f1f1f3",
                    cursor: "pointer", transition: "background .12s",
                  }}
                  onMouseEnter={(e) => (e.currentTarget.style.background = "#fafafb")}
                  onMouseLeave={(e) => (e.currentTarget.style.background = "#fff")}>
                  <div onClick={(e) => e.stopPropagation()}>
                    <Checkbox checked={!!checked[r.id]} onChange={(v) => setChecked((c) => ({ ...c, [r.id]: v }))} />
                  </div>
                  <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
                    <ProductThumbPair product={r.product} store={g.__store} kind={r.product?.kind || "jersey"} />
                    <div style={{ fontSize: 10, color: "#9a9aa1", fontWeight: 600, letterSpacing: ".02em", maxWidth: 140, lineHeight: 1.3 }}>
                      {(r.product?.title || "").toUpperCase()}
                    </div>
                  </div>
                  <div style={{ fontSize: 13, color: "#6b6b72" }}>{r.orderDate}</div>
                  <div style={{ fontSize: 13 }}>
                    <div style={{ fontWeight: 600, color: "#0e0e10" }}>{r.customer}</div>
                    <div style={{ fontSize: 12, color: "#6b6b72" }}>{r.customerEmail}</div>
                  </div>
                  <div style={{ fontSize: 13 }}>{r.size}</div>
                  <div style={{ fontSize: 13 }}>{r.factory}</div>
                  <div onClick={(e) => e.stopPropagation()}><BlueLink>{r.orderNum}</BlueLink></div>
                </div>
              ))}
            </div>
          </div>
        );
      })}

      <FloatingActionBar>
        {selectedCount > 0 && (
          <button onClick={() => {
            const grps = groups.map((g) => ({ shippingGroup: g.shippingGroup, fee: g.fee, pct: g.pct, store: g.__store, rows: g.rows.filter((r) => checked[r.id]).map((r) => ({ id: r.id, product: r.product, size: r.size, qty: r.quantity || 1, customer: r.customer })) })).filter((g) => g.rows.length);
            if (grps.length) onSubmit && onSubmit(grps);
          }} style={{
            background: "#fff", color: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", cursor: "pointer", whiteSpace: "nowrap",
          }}>Submit Selected ({selectedCount})</button>
        )}
        <button onClick={() => {
          const grps = groups.map((g) => ({ shippingGroup: g.shippingGroup, fee: g.fee, pct: g.pct, store: g.__store, rows: g.rows.map((r) => ({ id: r.id, product: r.product, size: r.size, qty: r.quantity || 1, customer: r.customer })) }));
          onSubmit && onSubmit(grps);
        }} style={{
          background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
          padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", cursor: "pointer", whiteSpace: "nowrap",
        }}>Submit All</button>
      </FloatingActionBar>
    </div>
  );
}

function OrdersOverview({ stores = [], onBack, origin = "overview" }) {
  const [search, setSearch] = React.useState("");
  const [openOrder, setOpenOrder] = React.useState(null);
  const [toast, setToast] = React.useState(null);

  const { all } = React.useMemo(() => buildCrossStoreOrders(stores), [stores]);

  const matches = (s, o) => {
    if (!s) return true;
    const q = s.toLowerCase();
    return [o.__store?.teamName, o.__store?.storeTitle, o.organization, o.customer, o.rep, o.webOrderNum, o.factoryOrderNum, o.cartName, o.orderNum, o.product?.title]
      .filter(Boolean).some((v) => String(v).toLowerCase().includes(q));
  };

  const filteredAll = all.filter((o) => matches(search, o));

  // All Orders columns — Store tag is inserted right after the product.
  const cols = [
    { key: "product", label: "Product", w: "1.4fr", alwaysOn: true, render: (o) => <ProductCell product={o.product} store={o.__store} />, csv: (o) => o.product?.title || "" },
    { key: "store", label: "Store", w: "1.1fr", noClip: true, render: (o) => <StoreTag store={o.__store} />, csv: (o) => o.__store?.teamName || o.__store?.storeTitle || "" },
    { key: "organization", label: "Organization", w: "1fr", render: (o) => (
      <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
        <span>{o.organization}</span>
      </div>
    ) },
    { key: "factoryOrderNum", label: "Factory Order #", w: "1fr", render: (o) => <BlueLink>{o.factoryOrderNum}</BlueLink> },
    { key: "webOrderNum", label: "Web Order #", w: "1fr", render: (o) => <BlueLink>{o.webOrderNum}</BlueLink> },
    { key: "cartName", label: "Cart Name", w: "1fr" },
    { key: "customer", label: "Customer", w: ".9fr", render: (o) => o.customer || o.rep || "—" },
    { key: "sport", label: "Sport", w: ".8fr" },
    { key: "quantity", label: "Qty", w: ".5fr", align: "left", render: (o) => <span style={{ fontVariantNumeric: "tabular-nums" }}>{o.quantity}</span> },
    { key: "shippingGroup", label: "Shipping Group", w: ".9fr" },
    { key: "dueDate", label: "Due/Ship Date", w: "1.1fr", render: (o) => <DueDateCell date={o.dueDate} status={o.status} /> },
    { key: "status", label: "Status", w: "1.2fr", render: (o) => <StatusBadge status={o.status} tracking={o.tracking} /> },
  ];

  const noData = all.length === 0;

  return (
    <div style={{ flex: 1, minWidth: 0, padding: "24px 32px 80px", background: "#fbfbfc" }}>
      {/* heading row */}
      <div style={{ display: "flex", alignItems: "flex-end", justifyContent: "space-between", gap: 24, marginBottom: 24, flexWrap: "wrap" }}>
        <div>
          <button onClick={onBack} style={{
            display: "inline-flex", alignItems: "center", gap: 4, background: "transparent", border: 0,
            cursor: "pointer", color: "#6b6b72", fontSize: 13, fontWeight: 600, padding: 0, marginBottom: 8,
          }}
            onMouseEnter={(e) => (e.currentTarget.style.color = "#0e0e10")}
            onMouseLeave={(e) => (e.currentTarget.style.color = "#6b6b72")}>
            <IconChevronLeft size={16} /> {origin === "stores" ? "Back to stores" : origin === "vault" ? "Back to stock vault" : "Back to overview"}
          </button>
          <h1 style={{ fontFamily: "Archivo, sans-serif", fontSize: 30, fontWeight: 800, margin: 0, color: "#0e0e10", letterSpacing: "-.01em" }}>Orders</h1>
          <div style={{ fontSize: 13.5, color: "#6b6b72", marginTop: 4 }}>
            Orders across all {stores.filter((s) => s.seeded).length || stores.length} stores
          </div>
        </div>
      </div>

      <AllOrdersExperience
        orders={all}
        columns={cols}
        storeFor={(o) => o.__store}
        onOpen={(ord) => setOpenOrder({ ...ord, kind: ord.orderType })} />

      {openOrder && <OrderDetailDrawer order={openOrder} store={openOrder.__store} onClose={() => setOpenOrder(null)} />}
      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
}

Object.assign(window, { OrdersOverview, StoreTag });


/* ===== ASSET 14 : 097f8365-819e-4542-8200-88135ba71a8a ===== */
// "Create a Team Store" form — single page, matches screenshot 2.
const { useState: useStateCF, useRef: useRefCF, useEffect: useEffectCF } = React;

const SPORTS = ["Baseball", "Basketball", "Football", "Hockey", "Lacrosse", "Soccer", "Softball", "Track & Field", "Volleyball", "Wrestling"];
const TEAMS_TO_CONNECT = [
{ value: "carlsbad-padres", label: "Carlsbad Padres" },
{ value: "valley-hoyas", label: "Valley Hoyas" },
{ value: "north-rams", label: "North Rams" },
{ value: "honey-badgers", label: "Honey Badgers" },
{ value: "general-fc", label: "General FC" },
{ value: "create-new", label: "+ Create new team…" }];

const CONTACT_PREFS = ["Email Only", "Phone Only", "Email & Phone", "Text Message"];
const SHIP_OPTIONS = ["Ship to Coach", "Ship to each Player", "Ship to Team Address", "Local Pickup"];
const COUNTRIES = ["United States", "Canada", "Mexico"];
const STATES = ["Alabama", "Alaska", "Arizona", "Arkansas", "California", "Colorado", "Connecticut", "Delaware", "Florida", "Georgia", "Hawaii", "Idaho", "Illinois", "Indiana", "Iowa", "Kansas", "Kentucky", "Louisiana", "Maine", "Maryland", "Massachusetts", "Michigan", "Minnesota", "Mississippi", "Missouri", "Montana", "Nebraska", "Nevada", "New Hampshire", "New Jersey", "New Mexico", "New York", "North Carolina", "North Dakota", "Ohio", "Oklahoma", "Oregon", "Pennsylvania", "Rhode Island", "South Carolina", "South Dakota", "Tennessee", "Texas", "Utah", "Vermont", "Virginia", "Washington", "West Virginia", "Wisconsin", "Wyoming"];

// Color picker swatch palette.
const COLOR_PRESETS = [
"#e1e2dd", "#e1251b", "#1d3a8a", "#0e0e10", "#ffffff", "#f5b400",
"#1f9d55", "#7c3aed", "#ec4899", "#0891b2", "#fb923c", "#65a30d"];


// PROLOOK standard team-color palette — code + hex, laid out in rows like the
// "Select Team Color" picker.
const PROLOOK_COLORS = [
{ code: "BK", name: "Black", hex: "#0e0e10" },
{ code: "WH", name: "White", hex: "#ffffff" },
{ code: "M", name: "Maroon", hex: "#4a1416" },
{ code: "SE", name: "Sangria", hex: "#6e1423" },
{ code: "C", name: "Cardinal", hex: "#9b1b30" },
{ code: "BRR", name: "Brick", hex: "#b3202a" },
{ code: "R", name: "Red", hex: "#e1251b" },
{ code: "UR", name: "Ultra Red", hex: "#e23744" },
{ code: "TX", name: "Texas Orange", hex: "#bf5700" },
{ code: "O", name: "Orange", hex: "#e8590c" },
{ code: "TO", name: "Tangerine", hex: "#f7941d" },

{ code: "G", name: "Gold", hex: "#f5a800" },
{ code: "Y", name: "Yellow", hex: "#ffd400" },
{ code: "OP", name: "Olive Pop", hex: "#9aab2e" },
{ code: "S", name: "Slime", hex: "#b5e61d" },
{ code: "DF", name: "Dark Forest", hex: "#14532d" },
{ code: "FG", name: "Forest Green", hex: "#2e7d32" },
{ code: "RG", name: "Rifle Green", hex: "#3a431e" },
{ code: "KG", name: "Kelly Green", hex: "#1f9d55" },
{ code: "MO", name: "Moss", hex: "#6b7a3a" },
{ code: "LG", name: "Light Green", hex: "#5cb85c" },
{ code: "MB", name: "Mint Blue", hex: "#18b39c" },

{ code: "T", name: "Teal", hex: "#2b8a8a" },
{ code: "IB", name: "Ice Blue", hex: "#6f93ad" },
{ code: "AB", name: "Azure Blue", hex: "#3f8edc" },
{ code: "CA", name: "Carolina", hex: "#6fb0e6" },
{ code: "CB", name: "Columbia Blue", hex: "#a9cce8" },
{ code: "DN", name: "Dark Navy", hex: "#16203f" },
{ code: "NB", name: "Navy Blue", hex: "#1c2a5e" },
{ code: "UN", name: "Union", hex: "#1f357a" },
{ code: "DR", name: "Deep Royal", hex: "#1746a2" },
{ code: "RB", name: "Royal Blue", hex: "#1f53c4" },
{ code: "AR", name: "Azure Royal", hex: "#2563eb" },

{ code: "DP", name: "Deep Purple", hex: "#371a6e" },
{ code: "P", name: "Purple", hex: "#4a148c" },
{ code: "LV", name: "Lavender", hex: "#b39ddb" },
{ code: "NP", name: "Neon Pink", hex: "#ff4da6" },
{ code: "PK", name: "Pink", hex: "#ffb6d5" },
{ code: "BR", name: "Brown", hex: "#3e2723" },
{ code: "YB", name: "Yukon Brown", hex: "#5d4037" },
{ code: "BG", name: "Bog", hex: "#7c6f2c" },
{ code: "OG", name: "Old Gold", hex: "#c39b1e" },
{ code: "VG", name: "Vegas Gold", hex: "#c5b358" },
{ code: "DS", name: "Desert Sand", hex: "#d6c7a1" },

{ code: "CR", name: "Cream", hex: "#f4efe1" },
{ code: "CG", name: "Charcoal Gray", hex: "#44474a" },
{ code: "GR", name: "Gray", hex: "#8b8d90" },
{ code: "CC", name: "Carbon", hex: "#54605a" },
{ code: "AG", name: "Ash Gray", hex: "#b2b6b8" },
{ code: "SG", name: "Silver Gray", hex: "#ccd0d2" }];


// Pick black/white text for legibility on a given swatch hex.
function readableInk(hex) {
  const h = (hex || "").replace("#", "");
  if (h.length < 6) return "#0e0e10";
  const r = parseInt(h.slice(0, 2), 16),g = parseInt(h.slice(2, 4), 16),b = parseInt(h.slice(4, 6), 16);
  const lum = (0.299 * r + 0.587 * g + 0.114 * b) / 255;
  return lum > 0.62 ? "#0e0e10" : "#ffffff";
}

function SelectTeamColorModal({ open, initial, onClose, onSave }) {
  const [picked, setPicked] = useStateCF(initial || []);
  useEffectCF(() => {if (open) setPicked(initial || []);}, [open]);
  useEffectCF(() => {
    if (!open) return;
    const h = (e) => {if (e.key === "Escape") onClose();};
    window.addEventListener("keydown", h);
    document.body.style.overflow = "hidden";
    return () => {window.removeEventListener("keydown", h);document.body.style.overflow = "";};
  }, [open, onClose]);
  if (!open) return null;

  const isOn = (hex) => picked.some((c) => c.toLowerCase() === hex.toLowerCase());
  const toggle = (hex) => setPicked((p) =>
  p.some((c) => c.toLowerCase() === hex.toLowerCase()) ?
  p.filter((c) => c.toLowerCase() !== hex.toLowerCase()) :
  [...p, hex]
  );

  return (
    <div onClick={onClose} style={{
      position: "fixed", inset: 0, zIndex: 130, background: "rgba(20,20,30,.55)",
      display: "flex", alignItems: "center", justifyContent: "center", padding: 24
    }}>
      <div onClick={(e) => e.stopPropagation()} style={{
        width: "min(820px, 100%)", maxHeight: "calc(100vh - 48px)", background: "#fff",
        borderRadius: 10, boxShadow: "0 32px 64px rgba(20,20,30,.34)", overflow: "hidden",
        display: "flex", flexDirection: "column"
      }}>
        {/* Header */}
        <div style={{ padding: "28px 36px 0", flexShrink: 0 }}>
          <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between" }}>
            <h2 style={{ fontSize: 30, fontWeight: 800, letterSpacing: ".01em", margin: 0, color: "#0e0e10" }}>SELECT TEAM COLOR</h2>
            <button onClick={onClose} title="Close" style={{
              width: 36, height: 36, borderRadius: "50%", border: "none", background: "transparent",
              color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0
            }}
            onMouseEnter={(e) => {e.currentTarget.style.background = "#f1f1f3";}}
            onMouseLeave={(e) => {e.currentTarget.style.background = "transparent";}}>
              <IconX size={22} />
            </button>
          </div>
          <div style={{ borderTop: "1px solid #ececf0", marginTop: 20 }} />
          <p style={{ textAlign: "center", color: "#e1251b", fontSize: 14, fontWeight: 500, margin: "24px 0 4px" }}>
            *If a color isn't available, black or white will be used instead.
          </p>
        </div>

        {/* Swatch grid */}
        <div style={{ padding: "16px 36px 8px", overflowY: "auto" }}>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(11, 1fr)", gap: 12, maxWidth: 700, margin: "0 auto" }}>
            {PROLOOK_COLORS.map((c) => {
              const on = isOn(c.hex);
              const ink = readableInk(c.hex);
              return (
                <button key={c.code} onClick={() => toggle(c.hex)} title={c.name}
                style={{
                  aspectRatio: "1 / 1", borderRadius: 6, background: c.hex,
                  border: c.hex.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "1px solid rgba(0,0,0,.06)",
                  color: ink, display: "inline-flex", alignItems: "center", justifyContent: "center",
                  fontSize: 13, fontWeight: 800, letterSpacing: ".02em",
                  outline: on ? "2.5px solid #0e0e10" : "none", outlineOffset: 2
                }}>
                  {on ? <IconCheck size={18} style={{ strokeWidth: 3 }} /> : c.code}
                </button>);

            })}
          </div>
        </div>

        {/* Save */}
        <div style={{ padding: "20px 36px 28px", flexShrink: 0 }}>
          <button onClick={() => onSave(picked)} style={{
            width: "100%", height: 56, border: "none", borderRadius: 6, background: "#e1251b",
            color: "#fff", fontSize: 16, fontWeight: 800, letterSpacing: ".01em", cursor: "pointer"
          }}
          onMouseEnter={(e) => {e.currentTarget.style.background = "#c41d14";}}
          onMouseLeave={(e) => {e.currentTarget.style.background = "#e1251b";}}>
            Save Colors
          </button>
        </div>
      </div>
    </div>);

}

function ColorPickerPopup({ value, onChange, onClose }) {
  const [hex, setHex] = useStateCF(value || "#000000");
  return (
    <div style={{
      position: "absolute", top: "calc(100% + 8px)", left: 0, zIndex: 40,
      background: "#fff", border: "1px solid #e5e5e9", borderRadius: 8,
      boxShadow: "0 8px 24px rgba(20,20,30,.12)", padding: 16, width: 240
    }}>
      <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#6b6b72", marginBottom: 8 }}>PRESETS</div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(6, 1fr)", gap: 8, marginBottom: 16 }}>
        {COLOR_PRESETS.map((c) =>
        <button key={c}
        onClick={() => {onChange(c);onClose();}}
        style={{
          width: 32, height: 32, borderRadius: 4, padding: 0,
          border: c === "#ffffff" ? "1px solid #d4d4d8" : "1px solid transparent",
          background: c, cursor: "pointer", outline: hex === c ? "2px solid #0e0e10" : "none", outlineOffset: 1
        }} />
        )}
      </div>
      <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
        <input
          type="color"
          value={hex}
          onChange={(e) => setHex(e.target.value)}
          style={{ width: 36, height: 36, border: "1px solid #d4d4d8", borderRadius: 4, padding: 2, cursor: "pointer" }} />
        
        <input
          type="text"
          value={hex.toUpperCase()}
          onChange={(e) => setHex(e.target.value)}
          style={{ flex: 1, border: "1px solid #d4d4d8", borderRadius: 4, padding: "8px 12px", fontSize: 13, fontFamily: "JetBrains Mono, monospace" }} />
        
        <Button size="sm" variant="primary" onClick={() => {onChange(hex);onClose();}}>ADD</Button>
      </div>
    </div>);

}

function ColorSwatches({ colors, onChange }) {
  const [adding, setAdding] = useStateCF(false);
  const ref = useRefCF();
  useEffectCF(() => {
    const h = (e) => {if (ref.current && !ref.current.contains(e.target)) setAdding(false);};
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  return (
    <div style={{ position: "relative" }} ref={ref}>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center" }}>
        {colors.map((c, i) =>
        <div key={i} style={{ position: "relative" }} onMouseEnter={(e) => {
          const x = e.currentTarget.querySelector(".rm");
          if (x) x.style.opacity = "1";
        }} onMouseLeave={(e) => {
          const x = e.currentTarget.querySelector(".rm");
          if (x) x.style.opacity = "0";
        }}>
            <div style={{
            width: 40, height: 40, borderRadius: 4,
            border: c.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "1px solid transparent",
            background: c
          }} />
            <button
            className="rm"
            onClick={() => onChange(colors.filter((_, j) => j !== i))}
            title="Remove"
            style={{
              position: "absolute", top: -8, right: -8, width: 16, height: 16,
              borderRadius: "50%", background: "#0e0e10", color: "#fff",
              border: "1.5px solid #fff", padding: 0,
              display: "inline-flex", alignItems: "center", justifyContent: "center",
              cursor: "pointer", opacity: 0, transition: "opacity .12s"
            }}>
              <IconX size={10} />
            </button>
          </div>
        )}
        <button onClick={() => setAdding(true)} style={{
          width: 40, height: 40, borderRadius: 4, border: "1px dashed #9a9aa1",
          background: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center",
          cursor: "pointer", color: "#6b6b72"
        }}>
          <IconPlus size={18} />
        </button>
      </div>
      <SelectTeamColorModal
        open={adding}
        initial={colors}
        onClose={() => setAdding(false)}
        onSave={(next) => {onChange(next);setAdding(false);}} />
      
    </div>);

}

// Reps' saved-logo library — seeded mock data for the My Logos modal.
const SAVED_LOGOS_SEED = [
{ id: "l-padres", name: "Carlsbad Padres", mono: "CP", color: "#1d3a8a", favorite: true, recent: true },
{ id: "l-eazy-1", name: "Final Eazy Clean Utah", mono: "EC", color: "#b3a24c", favorite: false, recent: true },
{ id: "l-eazy-2", name: "Eazy Clean Utah", mono: "EU", color: "#8f8748", favorite: false, recent: false },
{ id: "l-broncos", name: "Broncos", mono: "D", color: "#e8590c", favorite: true, recent: false },
{ id: "l-hoyas", name: "Valley Hoyas", mono: "VH", color: "#0e0e10", favorite: false, recent: true },
{ id: "l-rams", name: "North Rams", mono: "NR", color: "#1f7a44", favorite: false, recent: false }];


// Renders a saved logo: real artwork if uploaded, otherwise a monogram disc.
function LogoArt({ logo, size = 92 }) {
  if (logo.url) {
    return <img src={logo.url} alt="" style={{ maxWidth: "82%", maxHeight: "82%", objectFit: "contain" }} />;
  }
  return (
    <div style={{
      width: size, height: size, borderRadius: "50%", background: logo.color,
      color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center",
      fontSize: size * 0.36, fontWeight: 800, letterSpacing: ".02em"
    }}>{logo.mono}</div>);

}

function HeartIcon({ filled, size = 16 }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24"
    fill={filled ? "#e1251b" : "none"} stroke={filled ? "#e1251b" : "#9a9aa1"} strokeWidth="1.8"
    strokeLinecap="round" strokeLinejoin="round">
      <path d="M12 21s-7.5-4.8-10-9.3C.4 8.3 2 4.6 5.4 4.6c2 0 3.4 1.2 4.6 2.7 1.2-1.5 2.6-2.7 4.6-2.7 3.4 0 5 3.7 3.4 7.1C19.5 16.2 12 21 12 21z" />
    </svg>);

}

function MyLogosModal({ open, onClose, onSelect }) {
  const fileRef = useRefCF();
  const [logos, setLogos] = useStateCF(SAVED_LOGOS_SEED);
  const [search, setSearch] = useStateCF("");
  const [filter, setFilter] = useStateCF("all"); // "all" | "favorites" | "recent"

  useEffectCF(() => {
    if (!open) return;
    const h = (e) => {if (e.key === "Escape") onClose();};
    window.addEventListener("keydown", h);
    document.body.style.overflow = "hidden";
    return () => {window.removeEventListener("keydown", h);document.body.style.overflow = "";};
  }, [open, onClose]);

  if (!open) return null;

  const handleFile = (file) => {
    if (!file) return;
    const url = URL.createObjectURL(file);
    const next = { id: "up-" + Date.now(), name: file.name.replace(/\.[^.]+$/, ""), url, favorite: false, recent: true };
    setLogos((l) => [next, ...l]);
    onSelect({ name: next.name, url });
  };

  const toggleFav = (id) => setLogos((l) => l.map((x) => x.id === id ? { ...x, favorite: !x.favorite } : x));

  const q = search.trim().toLowerCase();
  const visible = logos.filter((l) => {
    if (q && !l.name.toLowerCase().includes(q)) return false;
    if (filter === "favorites") return l.favorite;
    if (filter === "recent") return l.recent;
    return true;
  });

  const FILTERS = [
  { id: "all", label: "All" },
  { id: "favorites", label: "Favorites" },
  { id: "recent", label: "Most Recent" }];


  return (
    <div onClick={onClose} style={{
      position: "fixed", inset: 0, zIndex: 130, background: "rgba(20,20,30,.55)",
      display: "flex", alignItems: "center", justifyContent: "center", padding: 24
    }}>
    <div onClick={(e) => e.stopPropagation()} style={{
        width: "min(1480px, 100%)", height: "min(940px, 100%)", background: "#fff",
        borderRadius: 10, boxShadow: "0 32px 64px rgba(20,20,30,.34)", overflow: "hidden",
        display: "flex", flexDirection: "column"
      }}>
      {/* Header */}
      <div style={{
          display: "grid", gridTemplateColumns: "1fr auto 1fr", alignItems: "center",
          padding: "24px 40px 16px", borderBottom: "1px solid #ececf0"
        }}>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <img src="assets/prolook-logo.png" alt="PROLOOK" style={{ height: 40, width: "auto", display: "block" }} />
          <div style={{ lineHeight: 1.05 }}>
            <div style={{ fontSize: 15, fontWeight: 800, letterSpacing: ".02em", color: "#0e0e10" }}>PROLOOK</div>
            <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>Design Studio</div>
          </div>
        </div>
        <div style={{ fontSize: 18, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textAlign: "center" }}>MY LOGOS</div>
        <button onClick={onClose} title="Close" style={{
            justifySelf: "end", width: 36, height: 36, borderRadius: "50%", border: "none",
            background: "transparent", color: "#6b6b72", display: "inline-flex",
            alignItems: "center", justifyContent: "center"
          }}
          onMouseEnter={(e) => {e.currentTarget.style.background = "#f1f1f3";}}
          onMouseLeave={(e) => {e.currentTarget.style.background = "transparent";}}>
          <IconX size={22} />
        </button>
      </div>

      {/* Body */}
      <div style={{ flex: 1, minHeight: 0, display: "flex", overflow: "hidden" }}>
        {/* Sidebar */}
        <aside style={{ width: 320, flexShrink: 0, padding: "28px 24px" }}>
          <div style={{
              background: "#f1f1f3", borderRadius: 8, padding: "20px 20px 24px"
            }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
              <span style={{ fontSize: 15, fontWeight: 800, letterSpacing: ".04em", color: "#0e0e10" }}>MY LOGOS</span>
              <IconChevron size={18} style={{ color: "#6b6b72" }} />
            </div>
            <div style={{ position: "relative", marginBottom: 16 }}>
              <input
                  value={search}
                  onChange={(e) => setSearch(e.target.value)}
                  placeholder="SEARCH BY KEYWORD"
                  style={{
                    width: "100%", height: 44, borderRadius: 6, border: "1px solid #d4d4d8",
                    background: "#fff", padding: "0 40px 0 16px", fontSize: 12, letterSpacing: ".04em",
                    color: "#0e0e10", outline: "none"
                  }} />
              <IconSearch size={18} style={{ position: "absolute", right: 12, top: 12, color: "#9a9aa1" }} />
            </div>
            <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
              {FILTERS.map((f) =>
                <label key={f.id} style={{ display: "flex", alignItems: "center", gap: 12, cursor: "pointer" }}>
                  <span style={{
                    width: 16, height: 16, borderRadius: 3, flexShrink: 0,
                    border: `1.5px solid ${filter === f.id ? "#0e0e10" : "#9a9aa1"}`,
                    background: filter === f.id ? "#0e0e10" : "#fff",
                    display: "inline-flex", alignItems: "center", justifyContent: "center"
                  }}>
                    {filter === f.id && <IconCheck size={13} style={{ color: "#fff", strokeWidth: 3 }} />}
                  </span>
                  <input type="checkbox" checked={filter === f.id} onChange={() => setFilter(f.id)} style={{ display: "none" }} />
                  <span style={{ fontSize: 15, color: "#0e0e10" }}>{f.label}</span>
                </label>
                )}
            </div>
          </div>
        </aside>

        {/* Logo grid */}
        <div style={{ flex: 1, minWidth: 0, overflowY: "auto", padding: "28px 40px 40px" }}>
          <input ref={fileRef} type="file" accept="image/png,image/jpeg,image/svg+xml,application/pdf,.ai,.eps"
            onChange={(e) => handleFile(e.target.files[0])} style={{ display: "none" }} />
          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(200px, 1fr))", gap: 24 }}>
            {/* Add logo card */}
            <div>
              <button onClick={() => fileRef.current?.click()} style={{
                  width: "100%", aspectRatio: "1 / 1.08", borderRadius: 10, border: "1.5px solid #d4d4d8",
                  background: "#fff", display: "flex", flexDirection: "column", alignItems: "center",
                  justifyContent: "center", gap: 16, color: "#0e0e10", transition: "border-color .12s, background .12s"
                }}
                onMouseEnter={(e) => {e.currentTarget.style.borderColor = "#0e0e10";e.currentTarget.style.background = "#fafafb";}}
                onMouseLeave={(e) => {e.currentTarget.style.borderColor = "#d4d4d8";e.currentTarget.style.background = "#fff";}}>
                <IconPlus size={40} style={{ color: "#0e0e10", strokeWidth: 1.4 }} />
                <span style={{ fontSize: 15, fontWeight: 700, letterSpacing: ".04em" }}>ADD LOGO</span>
              </button>
              <div style={{ display: "flex", justifyContent: "center", marginTop: 16 }}>
                <span style={{
                    display: "inline-flex", alignItems: "center", gap: 8, background: "#fbf0c9",
                    color: "#8a6d12", padding: "8px 16px", borderRadius: 999, fontSize: 13, fontWeight: 600
                  }}>
                  <span style={{
                      width: 16, height: 16, borderRadius: "50%", background: "#8a6d12", color: "#fbf0c9",
                      fontSize: 11, fontWeight: 800, fontStyle: "italic", display: "inline-flex",
                      alignItems: "center", justifyContent: "center"
                    }}>i</span>
                  Supported File types
                </span>
              </div>
            </div>

            {/* Saved logos */}
            {visible.map((l) =>
              <div key={l.id}>
                <button onClick={() => onSelect({ name: l.name, url: l.url || null, mono: l.mono, color: l.color })}
                style={{
                  width: "100%", aspectRatio: "1 / 1.08", borderRadius: 10, border: "1px solid #ececf0",
                  background: "#fff", display: "flex", alignItems: "center", justifyContent: "center",
                  position: "relative", overflow: "hidden", transition: "box-shadow .12s, border-color .12s"
                }}
                onMouseEnter={(e) => {e.currentTarget.style.boxShadow = "0 8px 22px rgba(20,20,30,.10)";e.currentTarget.style.borderColor = "#d4d4d8";}}
                onMouseLeave={(e) => {e.currentTarget.style.boxShadow = "none";e.currentTarget.style.borderColor = "#ececf0";}}>
                  <LogoArt logo={l} />
                  <span onClick={(e) => {e.stopPropagation();toggleFav(l.id);}} title="Favorite" style={{
                    position: "absolute", top: 12, right: 12, width: 32, height: 32, borderRadius: "50%",
                    background: "#fff", boxShadow: "0 1px 4px rgba(20,20,30,.12)", display: "inline-flex",
                    alignItems: "center", justifyContent: "center"
                  }}>
                    <HeartIcon filled={l.favorite} />
                  </span>
                </button>
                <div style={{
                  marginTop: 12, textAlign: "center", fontSize: 15, fontWeight: 800, letterSpacing: ".02em",
                  color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis",
                  textTransform: "uppercase"
                }}>{l.name}</div>
              </div>
              )}
          </div>

          {visible.length === 0 ?
            <div style={{ textAlign: "center", color: "#9a9aa1", fontSize: 15, fontWeight: 600, marginTop: 60 }}>
              No logos match your search
            </div> :

            <div style={{ textAlign: "center", color: "#9a9aa1", fontSize: 15, fontWeight: 600, marginTop: 56 }}>
              All logos are loaded
            </div>
            }
        </div>
      </div>
    </div>
    </div>);

}

function LogoDropZone({ logo, onChange }) {
  const [drag, setDrag] = useStateCF(false);
  const [libOpen, setLibOpen] = useStateCF(false);
  const handleFile = (file) => {
    if (!file) return;
    const url = URL.createObjectURL(file);
    onChange({ name: file.name, url });
  };
  return (
    <>
    <div
        onClick={!logo ? () => setLibOpen(true) : undefined}
        onDragOver={(e) => {e.preventDefault();setDrag(true);}}
        onDragLeave={() => setDrag(false)}
        onDrop={(e) => {e.preventDefault();setDrag(false);handleFile(e.dataTransfer.files[0]);}}
        style={{
          width: 56, height: 56, borderRadius: 4,
          border: `1px ${logo ? "solid" : "dashed"} ${drag ? "#0e0e10" : logo ? "#d4d4d8" : "#9a9aa1"}`,
          background: drag ? "#fafafb" : logo ? "#fff" : "#fff",
          display: "inline-flex", alignItems: "center", justifyContent: "center",
          cursor: "pointer", position: "relative", overflow: "hidden"
        }}>
      {logo ?
        <>
          {logo.url ?
          <img src={logo.url} alt="" style={{ width: "100%", height: "100%", objectFit: "cover" }} /> :

          <div style={{
            width: "100%", height: "100%", background: logo.color || "#0e0e10", color: "#fff",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            fontSize: 18, fontWeight: 800
          }}>{logo.mono || "?"}</div>
          }
          <button onClick={(e) => {e.stopPropagation();onChange(null);}} title="Remove logo" style={{
            position: "absolute", top: -8, right: -8, width: 16, height: 16,
            borderRadius: "50%", background: "#0e0e10", color: "#fff",
            border: "1.5px solid #fff", padding: 0,
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            cursor: "pointer"
          }}>
            <IconX size={10} />
          </button>
        </> :

        <IconPlus size={18} style={{ color: "#6b6b72" }} />
        }
    </div>
    <MyLogosModal
        open={libOpen}
        onClose={() => setLibOpen(false)}
        onSelect={(l) => {onChange(l);setLibOpen(false);}} />
    </>);

}


// ───────────────────────── 6-step wizard shell ─────────────────────────
// Reusable card wrapper (kept for back-compat with any callers).
function Section({ title, intro, children, action }) {
  return (
    <div style={{
      background: "#fff", border: "1px solid #ececf0", borderRadius: 8,
      padding: 28, marginBottom: 24, boxShadow: "0 1px 2px rgba(20,20,30,.03)"
    }}>
      <div style={{ marginBottom: 24, display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 24 }}>
        <div style={{ flex: 1, minWidth: 0 }}>
          <h2 style={{ fontSize: 16, fontWeight: 700, margin: 0, marginBottom: 8, color: "#0e0e10" }}>{title}</h2>
          {intro && <p style={{ fontSize: 13, color: "#6b6b72", margin: 0, lineHeight: 1.55 }}>{intro}</p>}
        </div>
        {action && <div style={{ flexShrink: 0 }}>{action}</div>}
      </div>
      {children}
    </div>);

}

function buildWizardData(presetData) {
  const p = presetData || {};
  return {
    // Core branching flag — "league" (Multi-Team Hub) unlocks the division →
    // team canvas, product-to-team assignment, and team-manager mapping. "single"
    // (Single-Team Locker) enforces one roster and the streamlined flow.
    storeType: p.storeType || "",
    rosterAssignments: p.rosterAssignments || {},
    fundraisingEnabled: p.fundraisingEnabled ?? false,
    // existing store fields (carried through to finalize / dashboard)
    storeTitle: p.storeTitle || "",
    teamName: p.teamName || "",
    teamColors: p.teamColors || [],
    teamLogo: p.teamLogo || null,
    sport: p.sport || "",
    contactPref: p.contactPref || "Email & Phone",
    email: p.email || "",
    phone: p.phone || "",
    storeOpenMode: p.storeOpenMode || "set-window",
    storeOpenStart: p.storeOpenStart || "",
    storeOpenEnd: p.storeOpenEnd || "",
    // team + sport
    connectedTeam: p.connectedTeam || "",
    sports: p.sports || [],
    // store point of contact
    contactEmailMode: p.contactEmailMode || "",
    contactEmailSalesperson: p.contactEmailSalesperson || "",
    contactEmailCustom: p.contactEmailCustom || "",
    contactPhoneMode: p.contactPhoneMode || "",
    contactPhoneSalesperson: p.contactPhoneSalesperson || "",
    contactPhoneCustom: p.contactPhoneCustom || "",
    shipName: p.shipName || "",
    shipStreet: p.shipStreet || "",
    shipCity: p.shipCity || "",
    shipState: p.shipState || "",
    shipZip: p.shipZip || "",
    shipCountry: p.shipCountry || "United States",
    shipOption: p.shipOption || "",
    collectTax: p.collectTax !== false,
    taxRate: p.taxRate || "",
    // new wizard fields
    distributionFormat: p.distributionFormat || "",
    storeSlug: p.storeSlug || slugify(p.storeTitle || ""),
    _slugTouched: !!p.storeSlug,
    managerName: p.managerName || p.shipName || "",
    urgencyTimers: p.urgencyTimers != null ? p.urgencyTimers : true,
    distributionModel: p.distributionModel || "",
    curatedProducts: p.curatedProducts || [],
    // Step 1 additions — none are required; all have working defaults.
    allowDuplicateNumbers: p.allowDuplicateNumbers ?? false,
    numberSelection: p.numberSelection || "manager",
    additionalManagers: p.additionalManagers || [],
    shipApt: p.shipApt || "",
    // League Step 2 — Divisions & Teams (same shape as the workspace org).
    leagueStructure: p.leagueStructure || { groups: [], loose: [] },
    collectPlayerName: p.collectPlayerName != null ? p.collectPlayerName : true,
    collectRosterNumber: p.collectRosterNumber != null ? p.collectRosterNumber : true,
    taxMode: p.taxMode || (p.collectTax === false ? "exempt" : "zip"),
    flatTaxRate: p.flatTaxRate || "",
    taxState: p.taxState || "",
    taxLiabilityAgreed: p.taxLiabilityAgreed != null ? p.taxLiabilityAgreed : false,
    heroTheme: p.heroTheme || "pitch",
    primaryColor: p.primaryColor || p.teamColors && p.teamColors[1] || "#e1251b",
    announcement: p.announcement || "",
    // User-generated-template flow markers (Path B)
    _fromUserTemplate: !!p._fromUserTemplate,
    _templateName: p._templateName || "",
    _templateBlocks: p._templateBlocks || null,
    // Vault-launch flow marker (Stock Vault → "Start store with selected")
    _fromVault: !!p._fromVault
  };
}

function CreateForm({ onCancel, onSubmit, presetData, accent = "#e1251b", athletes = 30, templateMode = false, liveStores }) {
  const [data, setData] = useStateCF(() => buildWizardData(presetData));
  // One source of truth for every step number, badge, and label: the list is
  // store-type dependent (League = 4 steps, Individual = 3). No hardcoded totals.
  const baseSteps = wizardStepsFor(data.storeType);
  const STEPS = templateMode ? baseSteps.filter((s) => s.key !== "products") : baseSteps;
  // Divisions & Teams structure (League Step 2) — same shape as the workspace org.
  const setStructure = (fn) => setData((d) => {
    const cur = d.leagueStructure || { groups: [], loose: [] };
    const next = typeof fn === "function" ? fn(cur) : fn;
    return { ...d, leagueStructure: { groups: next.groups || [], loose: next.loose || [] } };
  });
  // Keep indices valid when the step list shrinks (League → Individual).
  React.useEffect(() => {
    if (step > STEPS.length - 1) setStep(STEPS.length - 1);
    if (maxReached > STEPS.length - 1) setMaxReached(STEPS.length - 1);
  }, [STEPS.length]);
  const [step, setStep] = useStateCF(0);
  // Vault launches arrive with Step 2 already complete — reachable + review-optional.
  const [maxReached, setMaxReached] = useStateCF(presetData && presetData._fromVault ? 1 : 0);
  const [errors, setErrors] = useStateCF({});
  const set = (patch) => setData((d) => ({ ...d, ...patch }));

  const scrollTop = () => {
    const el = document.getElementById("wizard-scroll");
    if (el && el.scrollHeight > el.clientHeight + 4) el.scrollTo({ top: 0, behavior: "smooth" });
    window.scrollTo({ top: 0, behavior: "smooth" });
  };

  const validateStep = (s) => {
    const e = {};
    if ((STEPS[s] || {}).key === "basics") {
      if (!data.storeTitle || !data.storeTitle.trim()) e.storeTitle = "Store name is required";
      if (!data.storeSlug || !data.storeSlug.trim()) e.storeSlug = "Web address is required";
      // Team Manager is the default point-of-contact source — any channel left on
      // that mode makes the corresponding record field required (error lands HERE).
      const emMode = data.contactEmailMode === "service" ? "teammanager" : (data.contactEmailMode || "");
      const phMode = data.contactPhoneMode === "service" ? "teammanager" : (data.contactPhoneMode || "");
      if (!emMode) e.contactEmailMode = "Select an email point of contact";
      if (!phMode) e.contactPhoneMode = "Select a phone point of contact";
      if (!data.storeType) e.storeType = "Select a store type";
      if (!data.distributionFormat) e.distributionFormat = "Select a store schedule";
      if (!data.distributionModel) e.distributionModel = "Select a fulfillment method";
      if (emMode === "teammanager" && !isValidEmail(data.email)) e.email = "Required — the store's point of contact follows this record";
      if (phMode === "teammanager" && !isValidPhone(data.phone)) e.phone = "Enter a 10-digit phone — the store's point of contact follows this record";
      if (data.distributionFormat === "popup") {
        if (!data.storeOpenStart) e.storeOpenStart = "Launch date is required";
        if (!data.storeOpenEnd) e.storeOpenEnd = "Closing date is required";
      }
      if (data.collectTax) {
        if (!data.taxRate || isNaN(parseFloat(data.taxRate))) e.taxRate = "Enter a rate or select a state";
        if (!data.taxLiabilityAgreed) e.taxLiability = "You must accept the tax liability agreement to continue";
      }
    }
    setErrors(e);
    return Object.keys(e).length === 0;
  };

  const goTo = (s) => {
    if (s <= maxReached) {setStep(s);setErrors({});scrollTop();}
  };

  const next = () => {
    if (!validateStep(step)) return;
    const n = Math.min(step + 1, STEPS.length - 1);
    setStep(n);
    setMaxReached((m) => Math.max(m, n));
    scrollTop();
  };
  const back = () => {setStep((s) => Math.max(0, s - 1));setErrors({});scrollTop();};

  const deploy = () => {
    if (!validateStep(step)) return;
    const SP = [
    { value: "braeden-stokes", email: "braeden@stokesathletics.com", phone: "208-654-8619" },
    { value: "maria-lopez", email: "maria@prolook.com", phone: "801-555-0142" },
    { value: "devon-pierce", email: "devon@prolook.com", phone: "801-555-0188" }];

    const findSp = (v) => SP.find((s) => s.value === v) || {};
    // Team Manager mode inherits the manager record; salesperson/custom override.
    const derivedEmail = data.contactEmailMode === "salesperson" ? findSp(data.contactEmailSalesperson).email || data.email :
    data.contactEmailMode === "custom" ? data.contactEmailCustom || data.email :
    data.email;
    const derivedPhone = data.contactPhoneMode === "salesperson" ? findSp(data.contactPhoneSalesperson).phone || data.phone :
    data.contactPhoneMode === "custom" ? data.contactPhoneCustom || data.phone :
    data.phone;
    const mapped = {
      ...data,
      email: derivedEmail || data.email,
      phone: derivedPhone || data.phone,
      teamName: data.teamName || (TEAMS_TO_CONNECT.find((t) => t.value === data.connectedTeam) || {}).label || data.teamName,
      collectTax: !!data.collectTax,
      taxRate: data.collectTax ? data.taxRate || "0" : "0",
      taxState: data.collectTax ? data.taxState : "",
      taxMode: data.collectTax ? "flat" : "exempt",
      shipName: data.managerName || data.shipName,
      shipOption: data.distributionModel === "home" ? "Ship to each Player" : "Ship to Coach",
      storeOpenMode: data.distributionFormat === "locker" ? "stay-open" : "set-window",
      teamColors: data.teamColors && data.teamColors.length ? data.teamColors : [data.primaryColor, "#0e0e10", "#ffffff"]
    };
    onSubmit(mapped);
  };

  const isLast = step === STEPS.length - 1;
  const stepProps = { data, set, errors, accent, liveStores, stepNum: step + 1 };

  return (
    <div id="wizard-scroll" style={{ flex: 1, background: "#fbfbfc" }}>
      {/* Full-width header band — title, step badge, and the cross-viewport stepper */}
      <div style={{ background: "#fff", borderBottom: "1px solid #ececf0", padding: "28px 48px 4px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 20, marginBottom: 24 }}>
          <div>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".12em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>
              Team Store Setup
            </div>
            <h1 style={{ fontSize: 30, fontWeight: 800, margin: 0, letterSpacing: ".005em", color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>
              {templateMode ? "Finish Your Template Store" : "Create a Team Store"}
            </h1>
          </div>
          <span style={{
            flexShrink: 0, marginTop: 8, border: `1.5px solid ${accent}`, color: accent,
            borderRadius: 999, padding: "8px 16px", fontSize: 12, fontWeight: 800, letterSpacing: ".06em",
            textTransform: "uppercase", background: "#fff"
          }}>Step {step + 1} of {STEPS.length}</span>
        </div>

        <WizardStepper current={step} maxReached={maxReached} onJump={goTo} accent={accent} steps={STEPS} preDone={data._fromVault ? ["products"] : []} />
      </div>

      {/* Centered, shadowed workspace container */}
      <div style={{ maxWidth: 1180, margin: "0 auto", padding: "32px 40px 152px" }}>
        <div style={{
          background: "#fff", border: "1px solid #ececf0", borderRadius: 14,
          boxShadow: "0 16px 44px rgba(20,20,30,.09)", padding: "36px 36px",
        }}>
          {STEPS[step].key === "basics" && <Step1Basics {...stepProps} />}
          {STEPS[step].key === "structure" && <Step2Structure data={data} setStructure={setStructure} accent={accent} stepNum={step + 1} />}
          {STEPS[step].key === "products" && <Step3Products {...stepProps} />}
          {STEPS[step].key === "review" && <Step6Review data={data} accent={accent} athletes={athletes} stepNum={step + 1} />}
        </div>
      </div>

      {/* Footer actions */}
      <div style={{
        position: "fixed", bottom: 0, left: 0, right: 0,
        background: "#fff", borderTop: "1px solid #ececf0",
        padding: "16px 28px", display: "flex", alignItems: "center", justifyContent: "space-between",
        boxShadow: "0 -2px 8px rgba(20,20,30,.04)", zIndex: 60
      }}>
        <Button variant="ghost" onClick={onCancel}>CANCEL</Button>
        <div style={{ display: "flex", gap: 16, alignItems: "center" }}>
          {!isLast && Object.keys(errors).length > 0 && (
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, fontWeight: 700, color: "#b81d14" }}>
              <IconAlert size={15} style={{ strokeWidth: 2.2, flexShrink: 0 }} /> Please complete the required fields above
            </span>
          )}
          {step > 0 &&
          <Button variant="secondary" onClick={back}>
              <IconChevronLeft size={15} /> BACK
            </Button>
          }
          {isLast ?
          <button onClick={deploy} style={{
            display: "inline-flex", alignItems: "center", gap: 8, border: 0, borderRadius: 4,
            padding: "12px 28px", fontSize: 13, fontWeight: 800, letterSpacing: ".02em",
            color: "#fff", background: "#0e0e10", cursor: "pointer"
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#1f1f22"}
          onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
              <IconCheck size={16} style={{ strokeWidth: 3 }} /> SAVE AS DRAFT
            </button> :

          <Button variant="primary" onClick={next}>
              CONTINUE <IconChevronRight size={15} />
            </Button>
          }
        </div>
      </div>
    </div>);

}

Object.assign(window, { CreateForm, Section });

/* ═══════════════════ QUICK-TURN STORE TEMPLATES ═══════════════════ */
// Entry interstitial + full-screen curated-catalog workspace. Reuses the wizard
// design system (white cards, accent, Archivo/Inter) and the shared
// ConfirmSizesModal / LogoDropZone / ColorSwatches primitives.

// Pick a vivid brand color for the live-branding overlay (skip near-white/black/grey).
function tplPrimary(colors) {
  const list = colors || [];
  const lum = (hex) => {
    const h = (hex || "").replace("#", "");
    if (h.length < 6) return 0.5;
    const r = parseInt(h.slice(0, 2), 16), g = parseInt(h.slice(2, 4), 16), b = parseInt(h.slice(4, 6), 16);
    return (0.299 * r + 0.587 * g + 0.114 * b) / 255;
  };
  const sat = (hex) => {
    const h = (hex || "").replace("#", "");
    if (h.length < 6) return 0;
    const r = parseInt(h.slice(0, 2), 16) / 255, g = parseInt(h.slice(2, 4), 16) / 255, b = parseInt(h.slice(4, 6), 16) / 255;
    const mx = Math.max(r, g, b), mn = Math.min(r, g, b);
    return mx === 0 ? 0 : (mx - mn) / mx;
  };
  const vivid = list.find((c) => { const l = lum(c); return l > 0.14 && l < 0.86 && sat(c) > 0.28; });
  return vivid || list[1] || "#e1251b";
}

const TPL_LEVELS = [
  { key: "hs", label: "HIGH SCHOOL/COLLEGE", blurb: "Official-grade apparel meeting scholastic and collegiate standards." },
  { key: "club", label: "CLUB/TRAVEL", blurb: "Performance gear built for competitive club teams and travel tournaments." },
  { key: "youth", label: "YOUTH", blurb: "Built for the next generation. Small in size. Elite in performance." },
];

const TPL_PRODUCTS = {
  "home-uniform":  { cat: "uniforms", role: "HOME", title: "SFN Lite Uniform",     photo: "sfn-lite",          tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 189 },
  "away-uniform":  { cat: "uniforms", role: "AWAY", title: "Zone Uniform",         photo: "zone",              tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 171 },
  "alt-uniform":   { cat: "uniforms", title: "SFN Twill Uniform",                photo: "sfn-twill",         tag: "Twill",       fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 225 },
  "sfn-lite":      { cat: "uniforms", title: "Halo Uniform",                     photo: "halo",              tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 7 days",     msrp: 189 },
  "reversible":    { cat: "practice", title: "2-Ply Reversible Uniform",         photo: "reversible",        tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 7 days",     msrp: 133 },
  "practice-short":{ cat: "practice", title: "Practice Gear Set",               photo: "practice-gear",     tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 7 days",     msrp: 89 },
  "pro-jogger":    { cat: "practice", title: "Pro Stock Jogger",                 photo: "pro-jogger",        tag: "Stock",       fulfill: "3 Colors",    ships: "Ships in 4 days",     msrp: 92 },
  "sfn-warmup":    { cat: "shooting", title: "SFN Hooded Warmup",                photo: "sfn-warmup",        tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 112 },
  "tech-tee":      { cat: "shooting", title: "Hooded Tech Tee Shooting Shirt",   photo: "hooded-tech-tee",   tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 7 days",     msrp: 64 },
  "hooded-tech":   { cat: "shooting", title: "Performance Hoodie",               photo: "performance-hoodie",tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 112 },
  "pro-hoodie":    { cat: "travel",   title: "Performance Hoodie",               photo: "performance-hoodie",tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 112 },
  "travel-pant":   { cat: "travel",   title: "Pro Stock Travel Pant",            photo: "pro-jogger",        tag: "Stock",       fulfill: "3 Colors",    ships: "Ships in 4 days",     msrp: 92 },
  "squad-bag":     { cat: "bags",     title: "Squad Bag",                        photo: "squad-bag",         tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 99 },
  "duffel-bag":    { cat: "bags",     title: "Duffel Bag",                       photo: "duffel-bag",        tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 158 },
  "team-bag":      { cat: "bags",     title: "Team Bag",                         photo: "team-bag",          tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 3-4 weeks", msrp: 120 },
  "arm-sleeve":    { cat: "bags",     title: "Team Arm Sleeve",                  photo: "arm-sleeve",        tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 7 days",     msrp: 23 },
  "fan-tee":       { cat: "fan",      title: "Fan Spirit Tee",                   photo: "hooded-tech-tee",   tag: "Sublimation", fulfill: "Full Custom", ships: "Ships in 7 days",     msrp: 34 },
  "fan-hoodie":    { cat: "fan",      title: "Fan Spirit Hoodie",                photo: "performance-hoodie",tag: "Stock",       fulfill: "6 Colors",    ships: "Ships in 4 days",     msrp: 72 },
};

const TPL_SIDEBAR = [
  { key: "uniforms", label: "Uniforms",           thumb: "cat-uniforms" },
  { key: "practice", label: "Practice Gear",      thumb: "reversible" },
  { key: "shooting", label: "Shooting Shirts",    thumb: "cat-shooting" },
  { key: "travel",   label: "Travel Gear",        thumb: "cat-travel" },
  { key: "bags",     label: "Bags & Accessories", thumb: "cat-bags" },
];

const TPL_CAT_PRODUCTS = (() => {
  const m = {};
  Object.keys(TPL_PRODUCTS).forEach((id) => { const c = TPL_PRODUCTS[id].cat; (m[c] = m[c] || []).push(id); });
  return m;
})();

const TPL_EXTRAS = [
  { key: "alt",         label: "Alternative Uniform Options", desc: "Add a third jersey set for tournaments and rivalry games.", cat: "uniforms", ids: ["alt-uniform", "sfn-lite"] },
  { key: "accessories", label: "Accessories",                 desc: "Arm sleeves, squad bags and finishing pieces.",           cat: "bags",     ids: ["arm-sleeve", "squad-bag"] },
  { key: "practice",    label: "Practice Gear",               desc: "Reversibles and shorts for everyday training.",           cat: "practice", ids: ["reversible", "practice-short"] },
  { key: "travel",      label: "Travel Gear",                 desc: "Hoodies and joggers for the road.",                       cat: "travel",   ids: ["pro-hoodie", "travel-pant"] },
  { key: "fan",         label: "Fan Gear",                    desc: "Spirit wear so families can rep the team.",               cat: "fan",      ids: ["fan-tee", "fan-hoodie"] },
];

// Live-branding overlay drawn on top of a product photo.
function TplBrandOverlay({ name, logo, primary, role }) {
  const on = !!(name || logo);
  const mono = (name || "T").replace(/[^A-Za-z0-9 ]/g, "").split(/\s+/).map((w) => w[0]).join("").slice(0, 2).toUpperCase() || "T";
  return (
    <React.Fragment>
      {/* team-color trim bar */}
      <div style={{ position: "absolute", top: 0, left: 0, right: 0, height: 8, background: on ? primary : "transparent", transition: "background .3s" }} />
      {/* logo chip + role tag, top-left row */}
      <div style={{ position: "absolute", top: 12, left: 12, display: "flex", alignItems: "center", gap: 8 }}>
        <div style={{ opacity: on ? 1 : 0, transform: on ? "translateY(0)" : "translateY(-6px)", transition: "opacity .3s, transform .3s" }}>
          {logo && logo.url
            ? <img src={logo.url} alt="" style={{ width: 36, height: 36, borderRadius: 7, objectFit: "cover", boxShadow: "0 2px 6px rgba(0,0,0,.18)", background: "#fff", display: "block" }} />
            : <span style={{ width: 36, height: 36, borderRadius: 7, background: primary, color: "#fff", fontSize: 13, fontWeight: 800, fontFamily: "Archivo, sans-serif", display: "inline-flex", alignItems: "center", justifyContent: "center", boxShadow: "0 2px 6px rgba(0,0,0,.18)" }}>{mono}</span>}
        </div>
        {role && (
          <span style={{ background: "#0e0e10", color: "#fff", fontSize: 10, fontWeight: 800, letterSpacing: ".1em", padding: "4px 8px", borderRadius: 999 }}>{role}</span>
        )}
      </div>
      {/* name ribbon */}
      {name && (
        <div style={{ position: "absolute", left: 0, right: 0, bottom: 0, padding: "20px 12px 12px", background: "linear-gradient(to top, rgba(14,14,16,.72), transparent)", opacity: on ? 1 : 0, transition: "opacity .3s" }}>
          <div style={{ color: "#fff", fontSize: 14, fontWeight: 800, letterSpacing: ".04em", textTransform: "uppercase", fontFamily: "Archivo, sans-serif", textShadow: "0 1px 3px rgba(0,0,0,.4)" }}>{name}</div>
        </div>
      )}
    </React.Fragment>
  );
}

// One product card in the catalog grid — storefront-style: large image tile + info block.
function TplProductCard({ id, included, locked, onToggle, brand, accent, compact }) {
  const p = TPL_PRODUCTS[id];
  const [hover, setHover] = React.useState(false);
  const primary = brand.primary;
  const selectable = !locked;
  const active = included || locked;
  return (
    <div
      onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}
      style={{ display: "flex", flexDirection: "column" }}>
      {/* Image tile */}
      <div
        onClick={selectable ? onToggle : undefined}
        style={{
          position: "relative", aspectRatio: "1 / 1.18", background: "#f4f4f5", borderRadius: 10, overflow: "hidden",
          display: "flex", alignItems: "center", justifyContent: "center", padding: 12,
          cursor: selectable ? "pointer" : "default",
          outline: active ? `2.5px solid ${accent}` : "2.5px solid transparent", outlineOffset: -2.5,
          boxShadow: hover && selectable && !active ? "0 8px 22px rgba(20,20,30,.10)" : "none",
          transition: "outline-color .16s, box-shadow .16s",
        }}>
        <img src={`assets/products/${p.photo}.png`} alt={p.title} style={{ maxWidth: "100%", maxHeight: "100%", objectFit: "contain", display: "block" }} />
        {/* team-color trim + role tag (photos ship pre-branded) */}
        {p.role && (
          <span style={{ position: "absolute", top: 12, left: 12, background: "#0e0e10", color: "#fff", fontSize: 10, fontWeight: 800, letterSpacing: ".1em", padding: "4px 8px", borderRadius: 999 }}>{p.role}</span>
        )}
        {/* selection badge */}
        {active && (
          <span style={{ position: "absolute", top: 12, right: 12, width: 28, height: 28, borderRadius: "50%", background: accent, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", boxShadow: "0 2px 6px rgba(0,0,0,.2)" }}>
            <IconCheck size={15} style={{ strokeWidth: 3.4 }} />
          </span>
        )}
        {/* hover add affordance */}
        {selectable && !included && hover && (
          <span style={{ position: "absolute", left: 12, bottom: 12, display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", color: "#fff", fontSize: 11.5, fontWeight: 800, letterSpacing: ".04em", padding: "8px 12px", borderRadius: 999 }}>
            <IconPlus size={13} style={{ strokeWidth: 2.8 }} /> ADD TO STORE
          </span>
        )}
      </div>
      {/* Info block */}
      <div style={{ padding: "12px 2px 0" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 12, marginBottom: 8 }}>
          <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", lineHeight: 1.25, fontFamily: "Archivo, sans-serif" }}>{p.title}</div>
          <span style={{ flexShrink: 0, background: "#ececee", color: "#52525b", fontSize: 11, fontWeight: 600, padding: "4px 8px", borderRadius: 6 }}>{p.tag}</span>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 13.5, color: "#3f3f46", marginBottom: 4 }}>
          <span style={{ width: 16, height: 16, borderRadius: "50%", background: "conic-gradient(from 0deg,#e1251b,#f5b400,#1f9d55,#2563eb,#7c3aed,#e1251b)", flexShrink: 0 }} />
          {p.fulfill}
        </div>
        <div style={{ fontSize: 13.5, color: "#9a9aa1", marginBottom: 4 }}>{p.ships}</div>
        <div style={{ fontSize: 13.5, color: "#0e0e10" }}>
          <strong style={{ fontWeight: 800 }}>MSRP ${p.msrp}</strong> <span style={{ color: "#6b6b72" }}>- Team Pricing Available</span>
        </div>
        {/* include control */}
        {locked ? (
          <div style={{ marginTop: 12, display: "inline-flex", alignItems: "center", gap: 8, fontSize: 11.5, fontWeight: 800, letterSpacing: ".04em", color: accent }}>
            <IconCheck size={14} style={{ strokeWidth: 3.2 }} /> BASELINE · INCLUDED
          </div>
        ) : (
          <button type="button" onClick={onToggle} style={{
            marginTop: 12, height: 40, width: "100%", borderRadius: 8, cursor: "pointer", fontSize: 12, fontWeight: 800, letterSpacing: ".04em",
            border: included ? `1.5px solid ${accent}` : "1.5px solid #d4d4d8",
            background: included ? `${accent}0d` : "#fff", color: included ? accent : "#0e0e10",
            display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, transition: "all .14s",
          }}
            onMouseEnter={(e) => { if (!included) e.currentTarget.style.borderColor = "#0e0e10"; }}
            onMouseLeave={(e) => { if (!included) e.currentTarget.style.borderColor = "#d4d4d8"; }}>
            {included ? <React.Fragment><IconCheck size={14} style={{ strokeWidth: 3.2 }} /> IN STORE</React.Fragment> : <React.Fragment><IconPlus size={14} style={{ strokeWidth: 2.6 }} /> ADD TO STORE</React.Fragment>}
          </button>
        )}
      </div>
    </div>
  );
}

// Entry interstitial — Template vs. Scratch.
function StoreTemplateInterstitial({ open, onClose, onTemplate, onScratch, accent = "#e1251b" }) {
  return (
    <Modal open={open} onClose={onClose} width={720}>
      <div style={{ padding: "36px 36px 32px" }}>
        <div style={{ textAlign: "center", marginBottom: 28 }}>
          <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".14em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>New Team Store</div>
          <h2 style={{ fontSize: 26, fontWeight: 800, margin: 0, color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: ".005em" }}>How do you want to start?</h2>
          <p style={{ fontSize: 14, color: "#6b6b72", margin: "12px auto 0", maxWidth: 460, lineHeight: 1.55 }}>
            Launch fast from a curated gear collection, or build every product from scratch.
          </p>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
          {/* Template — recommended */}
          <button type="button" onClick={onTemplate} style={{
            textAlign: "left", cursor: "pointer", position: "relative",
            border: `1.5px solid ${accent}`, borderRadius: 14, background: `${accent}0a`, padding: "24px 24px 24px",
            display: "flex", flexDirection: "column", gap: 12, transition: "box-shadow .16s, transform .16s",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.boxShadow = `0 12px 30px ${accent}26`; e.currentTarget.style.transform = "translateY(-2px)"; }}
            onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.transform = "none"; }}>
            <span style={{ width: 48, height: 48, borderRadius: 11, background: accent, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconTemplateGrid size={24} /></span>
            <div style={{ fontSize: 17, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Start with a Template</div>
            <div style={{ fontSize: 13, color: "#52525b", lineHeight: 1.55 }}>
              Pick a pre-built gear collection, apply your team branding once, and go live in minutes.
            </div>
            <div style={{ marginTop: 4, display: "inline-flex", alignItems: "center", gap: 8, color: accent, fontSize: 13, fontWeight: 800 }}>
              Browse templates <IconChevronRight size={15} />
            </div>
          </button>
          {/* Scratch */}
          <button type="button" onClick={onScratch} style={{
            textAlign: "left", cursor: "pointer",
            border: "1.5px solid #d4d4d8", borderRadius: 14, background: "#fff", padding: "24px 24px 24px",
            display: "flex", flexDirection: "column", gap: 12, transition: "box-shadow .16s, transform .16s, border-color .16s",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 12px 30px rgba(20,20,30,.1)"; e.currentTarget.style.transform = "translateY(-2px)"; e.currentTarget.style.borderColor = "#0e0e10"; }}
            onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.transform = "none"; e.currentTarget.style.borderColor = "#d4d4d8"; }}>
            <span style={{ width: 48, height: 48, borderRadius: 11, background: "#f1f1f3", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconWrenchBuild size={23} /></span>
            <div style={{ fontSize: 17, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Start from Scratch</div>
            <div style={{ fontSize: 13, color: "#52525b", lineHeight: 1.55 }}>
              Configure every product and store setting step by step in the full creation wizard.
            </div>
            <div style={{ marginTop: 4, display: "inline-flex", alignItems: "center", gap: 8, color: "#0e0e10", fontSize: 13, fontWeight: 800 }}>
              Open the wizard <IconChevronRight size={15} />
            </div>
          </button>
        </div>
      </div>
    </Modal>
  );
}

// Small local glyphs for the interstitial.
function IconTemplateGrid({ size = 24 }) {
  return (<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.4" /><rect x="14" y="3" width="7" height="7" rx="1.4" /><rect x="3" y="14" width="7" height="7" rx="1.4" /><rect x="14" y="14" width="7" height="7" rx="1.4" /></svg>);
}
function IconWrenchBuild({ size = 23 }) {
  return (<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M14.7 6.3a4 4 0 0 0-5.4 5.3L4 16.9a1.5 1.5 0 0 0 2.1 2.1l5.3-5.3a4 4 0 0 0 5.3-5.4l-2.6 2.6-2.1-.6-.6-2.1z" /></svg>);
}

// ── The full-screen curated-catalog workspace (Template flow · Step 1) ──
function TemplateWorkspace({ presetData, layout = "sidebar", accent = "#e1251b", onCancel, onContinue }) {
  const p = presetData || {};
  const [level, setLevel] = React.useState("hs");
  const [category, setCategory] = React.useState("uniforms");
  const [teamName, setTeamName] = React.useState(p.teamName || "");
  const [teamLogo, setTeamLogo] = React.useState(p.teamLogo || null);
  const [teamColors, setTeamColors] = React.useState(p.teamColors && p.teamColors.length ? p.teamColors : ["#e1251b", "#0e0e10", "#ffffff"]);
  const [included, setIncluded] = React.useState({ "home-uniform": true, "away-uniform": true });
  const [extras, setExtras] = React.useState({});
  // per-item config modal state (shared ConfirmSizesModal)
  const [cfgItems, setCfgItems] = React.useState(null);
  const [cfgState, setCfgState] = React.useState([]);
  const [cfgActive, setCfgActive] = React.useState(0);

  const primary = tplPrimary(teamColors);
  const brand = { name: teamName, logo: teamLogo, primary };
  const levelObj = TPL_LEVELS.find((l) => l.key === level) || TPL_LEVELS[0];
  const includedIds = Object.keys(included).filter((k) => included[k]);
  const storeProxy = { id: "tpl-store", teamName: teamName || "Team Store", teamColors, teamLogo };

  const toggleProduct = (id) => setIncluded((s) => { const n = { ...s }; if (n[id]) delete n[id]; else n[id] = true; return n; });
  const countInCat = (cat) => (TPL_CAT_PRODUCTS[cat] || []).filter((id) => included[id]).length;

  const toggleExtra = (ex) => setExtras((s) => {
    const on = !s[ex.key];
    // Reveal/hide the extra's product grid without auto-selecting its items.
    // Turning the section off clears anything the user had selected in it.
    if (!on) setIncluded((inc) => { const n = { ...inc }; ex.ids.forEach((id) => { delete n[id]; }); return n; });
    return { ...s, [ex.key]: on };
  });

  // Commit → loop selected items through the shared item-validation modal.
  const beginConfirm = () => {
    const items = includedIds.map((id) => {
      const pr = TPL_PRODUCTS[id];
      return { id: `tpl-${id}`, srcId: id, title: pr.title, style: `${levelObj.label} · ${pr.tag}`, photo: pr.photo, adult: (pr.msrp * 0.62).toFixed(2), youth: (pr.msrp * 0.55).toFixed(2), teamProfit: 16.5 };
    });
    setCfgItems(items);
    setCfgState(items.map(() => ({ applicationSizes: defaultApplicationSizes(), teamSetting: "fan-gear", pricing: defaultPricing(), sizesConfirmed: false })));
    setCfgActive(0);
  };
  const updateActiveConfig = (next) => setCfgState((cs) => cs.map((c, i) => i === cfgActive ? next : c));
  const updateActiveRows = (rows) => setCfgState((cs) => cs.map((c, i) => i === cfgActive ? { ...c, applicationSizes: rows } : c));

  const confirmConfig = () => {
    if (!cfgItems) return;
    const stamped = cfgItems.map((it, i) => {
      const cfg = cfgState[i];
      return { ...it, teamSetting: cfg.teamSetting, applicationSizes: cfg.applicationSizes, pricing: cfg.pricing, adult: cfg.pricing.adult.retail, youth: cfg.pricing.youth.retail, sizesConfirmed: true };
    });
    setCfgItems(null);
    onContinue({
      ...p,
      teamName: teamName || p.teamName,
      storeTitle: teamName || p.storeTitle,
      teamLogo, teamColors,
      primaryColor: primary,
      curatedProducts: stamped,
    });
  };

  // ── shared sub-renders ──
  const levelControl = (
    <div style={{ display: "inline-flex", background: "#f1f1f3", borderRadius: 10, padding: 4, gap: 2 }}>
      {TPL_LEVELS.map((l) => {
        const on = level === l.key;
        return (
          <button key={l.key} type="button" onClick={() => setLevel(l.key)} style={{
            border: on ? "1px solid #d4d4d8" : "1px solid transparent", borderRadius: 7, cursor: "pointer",
            padding: "8px 20px", fontSize: 12.5, fontWeight: 800, letterSpacing: ".05em",
            background: on ? "#fff" : "transparent", color: on ? "#0e0e10" : "#6b6b72",
            boxShadow: on ? "0 1px 3px rgba(20,20,30,.1)" : "none", transition: "all .14s",
          }}>{l.label}</button>
        );
      })}
    </div>
  );

  const brandingPanel = (dense) => (
    <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 14, padding: dense ? "22px 22px" : "24px 24px", boxShadow: "0 1px 2px rgba(20,20,30,.04)" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}>
        <span style={{ width: 8, height: 8, borderRadius: "50%", background: accent }} />
        <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".1em", color: "#0e0e10", textTransform: "uppercase" }}>Global Team Branding</div>
      </div>
      <div style={{ fontSize: 12.5, color: "#9a9aa1", lineHeight: 1.5, marginBottom: 16 }}>Applied live to every product in the collection.</div>
      <div style={{ marginBottom: 16 }}>
        <div style={{ fontSize: 11, fontWeight: 700, color: "#6b6b72", marginBottom: 8 }}>Team Name</div>
        <input type="text" value={teamName} onChange={(e) => setTeamName(e.target.value)} placeholder="e.g. East Lake Lions"
          style={{ width: "100%", boxSizing: "border-box", height: 44, border: "1px solid #d4d4d8", borderRadius: 8, padding: "0 16px", fontSize: 14, color: "#0e0e10", outline: "none", fontFamily: "inherit" }}
          onFocus={(e) => e.target.style.borderColor = accent} onBlur={(e) => e.target.style.borderColor = "#d4d4d8"} />
      </div>
      <div style={{ marginBottom: 16 }}>
        <div style={{ fontSize: 11, fontWeight: 700, color: "#6b6b72", marginBottom: 8 }}>Team Logo</div>
        <LogoDropZone logo={teamLogo} onChange={setTeamLogo} />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 700, color: "#6b6b72", marginBottom: 8 }}>Team Colors</div>
        <ColorSwatches colors={teamColors} onChange={setTeamColors} />
      </div>
      <div style={{ marginTop: 16, paddingTop: 16, borderTop: "1px solid #f1f1f3", display: "flex", alignItems: "center", gap: 8, fontSize: 12.5, color: (teamName || teamLogo) ? "#1f9d55" : "#9a9aa1", fontWeight: 700 }}>
        <span style={{ width: 16, height: 16, borderRadius: "50%", background: (teamName || teamLogo) ? "#e7f5ed" : "#f1f1f3", color: (teamName || teamLogo) ? "#1f9d55" : "#c7c7cd", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconCheck size={11} style={{ strokeWidth: 3.4 }} /></span>
        {(teamName || teamLogo) ? `Applied to ${includedIds.length} item${includedIds.length === 1 ? "" : "s"}` : "Add a name or logo to brand your gear"}
      </div>
    </div>
  );

  const productGrid = (ids, compact) => (
    <div style={{ display: "grid", gridTemplateColumns: `repeat(auto-fill, minmax(${compact ? 240 : 260}px, 1fr))`, gap: 24 }}>
      {ids.map((id) => (
        <TplProductCard key={id} id={id} included={!!included[id]} locked={id === "home-uniform" || id === "away-uniform"}
          onToggle={() => toggleProduct(id)} brand={brand} accent={accent} compact={compact} />
      ))}
    </div>
  );

  const extrasCard = (
    <div style={{ background: "#fbfbfc", border: `1.5px dashed ${accent}66`, borderRadius: 14, padding: "24px 24px" }}>
      <div style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif", marginBottom: 4 }}>Expand your store catalog?</div>
      <div style={{ fontSize: 13, color: "#6b6b72", marginBottom: 16, lineHeight: 1.5 }}>Home &amp; Away are ready. Toggle any extras to drop pre-built gear grids onto your store.</div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 }}>
        {TPL_EXTRAS.map((ex) => {
          const on = !!extras[ex.key];
          return (
            <button key={ex.key} type="button" onClick={() => toggleExtra(ex)} style={{
              textAlign: "left", cursor: "pointer", display: "flex", alignItems: "flex-start", gap: 12,
              border: `1.5px solid ${on ? accent : "#e5e5e9"}`, background: on ? `${accent}0a` : "#fff",
              borderRadius: 10, padding: "12px 16px", transition: "all .14s",
            }}>
              <span style={{ flexShrink: 0, marginTop: 1, width: 24, height: 24, borderRadius: 6, border: on ? "none" : "1.5px solid #cfcfd5", background: on ? accent : "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
                {on && <IconCheck size={13} style={{ strokeWidth: 3.4, color: "#fff" }} />}
              </span>
              <span style={{ minWidth: 0 }}>
                <span style={{ display: "block", fontSize: 13.5, fontWeight: 800, color: "#0e0e10" }}>{ex.label}</span>
                <span style={{ display: "block", fontSize: 12, color: "#9a9aa1", lineHeight: 1.45, marginTop: 2 }}>{ex.desc}</span>
              </span>
            </button>
          );
        })}
      </div>
    </div>
  );

  // Canvas content depends on the active category.
  const canvas = (
    <div style={{ display: "flex", flexDirection: "column", gap: 28 }}>
      <div>
        <h1 style={{ fontSize: 30, fontWeight: 800, margin: "0 0 8px", color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: ".005em" }}>{levelObj.label}</h1>
        <div style={{ fontSize: 14, color: "#6b6b72" }}>{levelObj.blurb}</div>
      </div>

      {category === "uniforms" ? (
        <React.Fragment>
          <div>
            <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}>
              <div style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>Baseline Uniforms</div>
              <span style={{ fontSize: 11, fontWeight: 700, color: accent, background: `${accent}12`, padding: "4px 8px", borderRadius: 999 }}>REQUIRED</span>
            </div>
            {productGrid(["home-uniform", "away-uniform"], false)}
          </div>
          {extrasCard}
          {TPL_EXTRAS.filter((ex) => extras[ex.key]).map((ex) => (
            <div key={ex.key}>
              <div style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 16 }}>{ex.label}</div>
              {productGrid(ex.ids, true)}
            </div>
          ))}
        </React.Fragment>
      ) : (
        productGrid(TPL_CAT_PRODUCTS[category] || [], false)
      )}
    </div>
  );

  const catSidebar = (
    <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
      {TPL_SIDEBAR.map((c) => {
        const on = category === c.key;
        const n = countInCat(c.key);
        return (
          <button key={c.key} type="button" onClick={() => setCategory(c.key)} style={{
            position: "relative", display: "flex", alignItems: "stretch", textAlign: "left", cursor: "pointer", overflow: "hidden",
            border: `1.5px solid ${on ? accent : "transparent"}`, background: on ? "#eef0f2" : "#f7f7f8",
            borderRadius: 10, padding: 0, height: 88, transition: "all .14s",
          }}
            onMouseEnter={(e) => { if (!on) e.currentTarget.style.background = "#f1f1f3"; }}
            onMouseLeave={(e) => { if (!on) e.currentTarget.style.background = "#f7f7f8"; }}>
            <span style={{ flexShrink: 0, alignSelf: "center", padding: "0 12px", fontSize: 14, fontWeight: on ? 800 : 700, color: on ? "#0e0e10" : "#3f3f46", zIndex: 1 }}>{c.label}</span>
            <span style={{ flex: 1, minWidth: 0, position: "relative" }}>
              <img src={`assets/products/${c.thumb}.png`} alt="" style={{ position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "cover", objectPosition: "center top" }} />
            </span>
            {n > 0 && <span style={{ position: "absolute", top: 8, right: 8, minWidth: 20, height: 20, padding: "0 8px", borderRadius: 999, background: accent, color: "#fff", fontSize: 11, fontWeight: 800, display: "inline-flex", alignItems: "center", justifyContent: "center", zIndex: 1, boxShadow: "0 1px 3px rgba(0,0,0,.2)" }}>{n}</span>}
          </button>
        );
      })}
    </div>
  );

  return (
    <div style={{ flex: 1, background: "#fbfbfc", overflowY: "auto", display: "flex", flexDirection: "column" }}>
      {/* Header band */}
      <div style={{ background: "#fff", borderBottom: "1px solid #ececf0", padding: "24px 40px 24px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 20, marginBottom: 20 }}>
          <div>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".12em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>Store Template · Men's Basketball</div>
            <h1 style={{ fontSize: 26, fontWeight: 800, margin: 0, color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: ".005em" }}>Build from a Template</h1>
          </div>
          <span style={{ flexShrink: 0, marginTop: 4, border: `1.5px solid ${accent}`, color: accent, borderRadius: 999, padding: "8px 16px", fontSize: 12, fontWeight: 800, letterSpacing: ".06em", textTransform: "uppercase" }}>Step 1 · Choose Gear</span>
        </div>
        <div style={{ display: "flex", justifyContent: "center" }}>{levelControl}</div>
      </div>

      {/* Body */}
      {layout === "stacked" ? (
        <div style={{ maxWidth: 1180, margin: "0 auto", width: "100%", padding: "28px 40px 132px", display: "flex", flexDirection: "column", gap: 24 }}>
          {brandingPanel(true)}
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>
            {TPL_SIDEBAR.map((c) => {
              const on = category === c.key; const n = countInCat(c.key);
              return (
                <button key={c.key} type="button" onClick={() => setCategory(c.key)} style={{
                  display: "inline-flex", alignItems: "center", gap: 8, cursor: "pointer",
                  border: `1.5px solid ${on ? accent : "#e5e5e9"}`, background: on ? accent : "#fff", color: on ? "#fff" : "#52525b",
                  borderRadius: 999, padding: "8px 16px", fontSize: 13, fontWeight: 800, transition: "all .14s",
                }}>
                  {c.label}{n > 0 && <span style={{ minWidth: 16, height: 16, padding: "0 4px", borderRadius: 999, background: on ? "rgba(255,255,255,.25)" : accent, color: "#fff", fontSize: 10.5, fontWeight: 800, display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{n}</span>}
                </button>
              );
            })}
          </div>
          {canvas}
        </div>
      ) : (
        <div style={{ display: "grid", gridTemplateColumns: "232px 1fr 320px", gap: 0, flex: 1, alignItems: "start" }}>
          <div style={{ padding: "28px 20px", borderRight: "1px solid #ececf0", position: "sticky", top: 0 }}>
            <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".1em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>Categories</div>
            {catSidebar}
          </div>
          <div style={{ padding: "28px 32px 132px", minWidth: 0 }}>{canvas}</div>
          <div style={{ padding: "28px 24px", position: "sticky", top: 0 }}>{brandingPanel(false)}</div>
        </div>
      )}

      {/* Footer */}
      <div style={{ position: "fixed", bottom: 0, left: 0, right: 0, background: "#fff", borderTop: "1px solid #ececf0", padding: "16px 28px", display: "flex", alignItems: "center", justifyContent: "space-between", boxShadow: "0 -2px 8px rgba(20,20,30,.04)", zIndex: 60 }}>
        <Button variant="ghost" onClick={onCancel}>CANCEL</Button>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <div style={{ fontSize: 13, color: "#6b6b72" }}><strong style={{ color: "#0e0e10", fontWeight: 800 }}>{includedIds.length}</strong> item{includedIds.length === 1 ? "" : "s"} in store</div>
          <button onClick={beginConfirm} style={{
            display: "inline-flex", alignItems: "center", gap: 8, border: 0, borderRadius: 4,
            padding: "12px 28px", fontSize: 13, fontWeight: 800, letterSpacing: ".02em", color: "#fff", background: "#0e0e10", cursor: "pointer",
          }}
            onMouseEnter={(e) => e.currentTarget.style.background = "#000"}
            onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
            CONFIRM &amp; CONTINUE <IconChevronRight size={15} />
          </button>
        </div>
      </div>

      {cfgItems && (
        <ConfirmSizesModal
          open={!!cfgItems}
          product={cfgItems[cfgActive]}
          store={storeProxy}
          rows={cfgState[cfgActive].applicationSizes}
          onChange={updateActiveRows}
          productConfig={cfgState[cfgActive]}
          onConfigChange={updateActiveConfig}
          items={cfgItems}
          activeIndex={cfgActive}
          onActiveIndexChange={setCfgActive}
          confirmLabel={cfgItems.length > 1 ? `Confirm ${cfgItems.length} Products & Continue` : "Confirm & Continue"}
          onClose={() => setCfgItems(null)}
          onConfirm={confirmConfig} />
      )}
    </div>
  );
}

Object.assign(window, { TemplateWorkspace, StoreTemplateInterstitial });

/* ═══════════════ USER-GENERATED STORE TEMPLATES ═══════════════ */
// Turn a live store into a brand-agnostic, reusable template. On save the
// system strips all unique data (branding, roster, store name) and converts
// each customized product back to a foundational "agnostic product block".
// Only the structural flags — store type + distribution model — carry over.

const AGNOSTIC_BLOCKS = {
  uniform:  { label: "SFN Lite, Round Neck Jersey", cat: "Uniforms",   icon: IconShirt },
  jersey:   { label: "SFN Lite, V-Neck Jersey",     cat: "Uniforms",   icon: IconShirt },
  pant:     { label: "SFN Lite Short",              cat: "Uniforms",   icon: IconShirt },
  shooting: { label: "SFN Lite Shooting Shirt",     cat: "Warm-Ups",   icon: IconShirt },
  hoodie:   { label: "SFN Performance Hoodie",      cat: "Warm-Ups",   icon: IconShirt },
  tee:      { label: "SFN Lite Tee",                cat: "Spirit Wear", icon: IconShirt },
  bag:      { label: "SFN Squad Bag",               cat: "Bags",       icon: IconBag },
};
const AGNOSTIC_ORDER = Object.keys(AGNOSTIC_BLOCKS);

// Map one customized product back to its baseline silhouette.
function classifyAgnostic(p) {
  const s = ((p.title || "") + " " + (p.subtitle || "")).toLowerCase();
  if (/bag|duffel|backpack/.test(s)) return "bag";
  if (/pant|jogger|tight/.test(s)) return "pant";
  if (/shooting/.test(s)) return "shooting";
  if (/hoodie|warm.?up|jacket|pullover/.test(s)) return "hoodie";
  if (/uniform/.test(s)) return "uniform";
  if (/jersey/.test(s)) return "jersey";
  if (/tee|t-shirt|shirt|spirit/.test(s)) return "tee";
  return "jersey";
}

// Build serializable block descriptors from key→count pairs.
function mkBlocks(counts) {
  return AGNOSTIC_ORDER.filter((k) => counts[k]).map((k) => ({
    key: k, count: counts[k], label: AGNOSTIC_BLOCKS[k].label, cat: AGNOSTIC_BLOCKS[k].cat,
  }));
}

// Reduce a store's products into counted agnostic blocks. Falls back to a
// representative starter bundle when the source store has no products yet.
function agnostifyProducts(store) {
  const products = (store && store.products) || [];
  const counts = {};
  products.forEach((p) => { const k = classifyAgnostic(p); counts[k] = (counts[k] || 0) + 1; });
  if (!Object.keys(counts).length) { counts.uniform = 2; counts.bag = 1; }
  return mkBlocks(counts);
}

// Expand template blocks into wizard curatedProducts (pre-loaded in Path B).
function blocksToCuratedProducts(blocks) {
  const out = [];
  (blocks || []).forEach((b) => {
    for (let i = 0; i < b.count; i++) out.push({ id: `${b.key}-${i}`, srcId: `${b.key}-${i}`, title: b.label, subtitle: b.cat, status: "draft", teamSetting: "team-required", rosters: [] });
  });
  return out;
}

const tplTypeLabel = (k) => k === "single" ? "Individual Team Store" : "League Store";
const tplDistLabel = (k) => k === "home" ? "Direct-To-Home" : "Ship to Team Manager";

// A small stacked list of agnostic blocks (shared by modal + repository cards).
function AgnosticBlockList({ blocks, maxHeight, dense, editable, onSetCount, onRemove }) {
  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 8, maxHeight, overflowY: maxHeight ? "auto" : "visible", paddingRight: maxHeight ? 4 : 0 }}>
      {blocks.map((b) => {
        const IconC = AGNOSTIC_BLOCKS[b.key].icon;
        return (
          <div key={b.key} style={{
            display: "flex", alignItems: "center", gap: 12,
            border: "1px solid #ececf0", borderRadius: 9, padding: dense ? "9px 11px" : "11px 13px", background: "#fff",
          }}>
            <span style={{ width: dense ? 30 : 36, height: dense ? 30 : 36, borderRadius: 8, flexShrink: 0, background: "#f4f4f5", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
              <IconC size={dense ? 16 : 18} />
            </span>
            <div style={{ minWidth: 0, flex: 1 }}>
              <div style={{ fontSize: dense ? 13 : 13.5, fontWeight: 700, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{b.label}</div>
              <div style={{ fontSize: 11, color: "#9a9aa1", letterSpacing: ".02em" }}>{b.cat}</div>
            </div>
            {editable ? (
              <div style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8 }}>
                <div style={{ display: "inline-flex", alignItems: "center", border: "1px solid #d4d4d8", borderRadius: 8, overflow: "hidden" }}>
                  <button type="button" onClick={() => onSetCount && onSetCount(b.key, Math.max(1, b.count - 1))}
                    style={{ width: 32, height: 32, border: 0, background: "#fafafb", color: "#0e0e10", cursor: "pointer", fontSize: 16, fontWeight: 700, display: "inline-flex", alignItems: "center", justifyContent: "center" }}>−</button>
                  <input value={b.count} onChange={(e) => { const n = parseInt(e.target.value.replace(/[^0-9]/g, ""), 10); onSetCount && onSetCount(b.key, isNaN(n) || n < 1 ? 1 : n); }}
                    style={{ width: 40, height: 32, border: 0, borderLeft: "1px solid #ececf0", borderRight: "1px solid #ececf0", textAlign: "center", fontSize: 13, fontWeight: 800, color: "#0e0e10", outline: "none", fontFamily: "JetBrains Mono, monospace", boxSizing: "border-box" }} />
                  <button type="button" onClick={() => onSetCount && onSetCount(b.key, b.count + 1)}
                    style={{ width: 32, height: 32, border: 0, background: "#fafafb", color: "#0e0e10", cursor: "pointer", fontSize: 16, fontWeight: 700, display: "inline-flex", alignItems: "center", justifyContent: "center" }}>+</button>
                </div>
                <button type="button" onClick={() => onRemove && onRemove(b.key)} title="Remove product"
                  style={{ width: 32, height: 32, borderRadius: 8, border: "1px solid #ececf0", background: "#fff", color: "#9a9aa1", cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
                  <IconTrash size={15} />
                </button>
              </div>
            ) : (
              <span style={{ flexShrink: 0, fontSize: 12, fontWeight: 800, color: "#52525b", background: "#f1f1f3", borderRadius: 999, padding: "4px 12px", fontFamily: "JetBrains Mono, monospace" }}>×{b.count}</span>
            )}
          </div>
        );
      })}
    </div>
  );
}

// Flag pill used on template cards + inherited-config banner.
function TplFlagPill({ icon, label, value }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 8, border: "1px solid #ececf0", borderRadius: 8, padding: "8px 12px", background: "#fafafb", minWidth: 0 }}>
      <span style={{ width: 28, height: 28, borderRadius: 6, flexShrink: 0, background: "#0e0e10", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{icon}</span>
      <div style={{ minWidth: 0 }}>
        <div style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase" }}>{label}</div>
        <div style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{value}</div>
      </div>
    </div>
  );
}

// ── The "Create Store Template" configuration modal ──
function CreateTemplateModal({ open, store, onClose, onSave, accent = "#e1251b" }) {
  const src = store || {};
  const [storeType, setStoreType] = React.useState("league");
  const [distribution, setDistribution] = React.useState("coach");
  const [fundraising, setFundraising] = React.useState(false);
  React.useEffect(() => {
    if (open) {
      setStoreType(src.storeType || "league");
      setDistribution(src.distributionModel || "coach");
      setFundraising(src.fundraisingEnabled === true);
    }
  }, [open, src.storeType, src.distributionModel, src.fundraisingEnabled]);

  const [blocks, setBlocks] = React.useState([]);
  React.useEffect(() => { if (open) setBlocks(agnostifyProducts(src)); }, [open]);
  const setBlockCount = (key, count) => setBlocks((cur) => cur.map((b) => b.key === key ? { ...b, count } : b));
  const removeBlock = (key) => setBlocks((cur) => cur.filter((b) => b.key !== key));
  const total = blocks.reduce((n, b) => n + b.count, 0);

  const save = () => {
    onSave && onSave({
      id: "utpl-" + Date.now(),
      name: (src.teamName || src.storeTitle || "Untitled") + " Template",
      sourceName: src.teamName || src.storeTitle || "Store",
      sport: src.sport || "",
      storeType, distributionModel: distribution,
      fundraisingEnabled: fundraising,
      blocks, total,
      createdAt: Date.now(),
    });
  };

  return (
    <Modal open={open} onClose={onClose} width={620}>
      {/* Header */}
      <div style={{ padding: "28px 32px 20px", borderBottom: "1px solid #ececf0" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 8 }}>
          <span style={{ width: 40, height: 40, borderRadius: 10, flexShrink: 0, background: accent, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconTemplateGrid size={20} /></span>
          <h2 style={{ fontSize: 21, fontWeight: 800, margin: 0, color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: ".005em" }}>Create Store Template</h2>
        </div>
        <p style={{ fontSize: 13.5, color: "#6b6b72", margin: 0, lineHeight: 1.55 }}>
          Confirm the base products and inherited properties that will compose this reusable template configuration.
        </p>
      </div>

      {/* Body */}
      <div style={{ padding: "24px 32px 8px" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 12 }}>
          <span style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>Agnostic Product Blocks</span>
          <span style={{ fontSize: 11.5, fontWeight: 700, color: "#6b6b72" }}>{total} product{total === 1 ? "" : "s"}</span>
        </div>
        <AgnosticBlockList blocks={blocks} maxHeight={196} editable onSetCount={setBlockCount} onRemove={removeBlock} />
        <div style={{ display: "flex", alignItems: "flex-start", gap: 8, marginTop: 12, fontSize: 12, color: "#9a9aa1", lineHeight: 1.5 }}>
          <IconInfo size={14} style={{ flexShrink: 0, marginTop: 1 }} />
          <span>Team branding, custom colors, the store name, and all roster data are stripped. Customized products revert to these baseline blocks.</span>
        </div>

        {/* Settings verification matrix */}
        <div style={{ marginTop: 24, paddingTop: 20, borderTop: "1px solid #f1f1f3" }}>
          <span style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>Inherited Configuration</span>
          <div style={{ display: "flex", flexDirection: "column", gap: 16, marginTop: 16 }}>
            <SelectField label="Store Type" value={storeType} onChange={setStoreType}
              options={[{ value: "league", label: "League Store · Multi-Team Hub" }, { value: "single", label: "Individual Team Store · Single-Team Locker" }]} />
            <SelectField label="Distribution Model" value={distribution} onChange={setDistribution}
              options={[{ value: "coach", label: "Ship to Team Manager" }, { value: "home", label: "Direct-To-Home Residential Delivery" }]} />
            <SelectField label="Team Fundraising" value={fundraising ? "on" : "off"} onChange={(v) => setFundraising(v === "on")}
              options={[{ value: "on", label: "Enabled" }, { value: "off", label: "Disabled" }]} />
          </div>
        </div>
      </div>

      {/* Footer action ribbon */}
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "16px 32px", borderTop: "1px solid #ececf0", marginTop: 16 }}>
        <Button variant="secondary" onClick={onClose}>Cancel</Button>
        <Button variant="primary" onClick={save}><IconCheck size={16} style={{ strokeWidth: 3 }} /> Save Template</Button>
      </div>
    </Modal>
  );
}

// Curated system templates (Tab 1 of the repository).
const CURATED_TEMPLATES = [
  { id: "cur-hoops",    name: "Hardwood Locker",     sport: "Basketball", storeType: "single", distributionModel: "home",  blocks: mkBlocks({ uniform: 2, shooting: 1, hoodie: 1, bag: 1 }) },
  { id: "cur-gridiron", name: "Gridiron League Kit", sport: "Football",   storeType: "league", distributionModel: "coach", blocks: mkBlocks({ jersey: 2, pant: 2, hoodie: 1, bag: 1 }) },
  { id: "cur-diamond",  name: "Diamond Team Pack",   sport: "Baseball",   storeType: "single", distributionModel: "coach", blocks: mkBlocks({ jersey: 2, pant: 1, tee: 1, bag: 1 }) },
  { id: "cur-pitch",    name: "Pitch Club Bundle",   sport: "Soccer",     storeType: "league", distributionModel: "coach", blocks: mkBlocks({ jersey: 2, pant: 1, hoodie: 1, bag: 1 }) },
];

// One template option in the repository grid.
function TemplateSummaryCard({ tpl, kind, accent, onSelect }) {
  const [hover, setHover] = React.useState(false);
  const total = (tpl.blocks || []).reduce((n, b) => n + b.count, 0);
  return (
    <div
      onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}
      onClick={onSelect}
      style={{
        display: "flex", flexDirection: "column", cursor: "pointer",
        border: `1px solid ${hover ? accent : "#ececf0"}`, borderRadius: 14, background: "#fff", overflow: "hidden",
        boxShadow: hover ? "0 14px 34px rgba(20,20,30,.11)" : "0 1px 2px rgba(20,20,30,.03)",
        transform: hover ? "translateY(-2px)" : "none",
        transition: "border-color .16s, box-shadow .16s, transform .16s",
      }}>
      {/* Header strip */}
      <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "16px 20px", borderBottom: "1px solid #f1f1f3" }}>
        <span style={{ width: 44, height: 44, borderRadius: 11, flexShrink: 0, background: kind === "user" ? "#0e0e10" : accent, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
          {kind === "user" ? <IconLayers size={21} /> : <IconTemplateGrid size={20} />}
        </span>
        <div style={{ minWidth: 0, flex: 1 }}>
          <div style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{tpl.name}</div>
          <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 1 }}>
            {kind === "user" ? `From ${tpl.sourceName}` : tpl.sport} · {total} products
          </div>
        </div>
      </div>

      {/* Stacked agnostic-product summary */}
      <div style={{ padding: "16px 20px", flex: 1 }}>
        <div style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 12 }}>Pre-loaded blocks</div>
        <AgnosticBlockList blocks={tpl.blocks} dense maxHeight={168} />
      </div>

      {/* Structural flags */}
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, padding: "0 20px 16px" }}>
        <TplFlagPill icon={tpl.storeType === "single" ? <IconUsers size={14} /> : <IconLayers size={14} />} label="Structure" value={tplTypeLabel(tpl.storeType)} />
        <TplFlagPill icon={tpl.distributionModel === "home" ? <IconBag size={14} /> : <IconUsers size={14} />} label="Distribution" value={tplDistLabel(tpl.distributionModel)} />
      </div>

      {/* CTA */}
      <div style={{ padding: "0 20px 20px" }}>
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "center", gap: 8,
          background: hover ? "#0e0e10" : "#f4f4f5", color: hover ? "#fff" : "#0e0e10",
          borderRadius: 8, padding: "12px 0", fontSize: 13, fontWeight: 800, letterSpacing: ".02em",
          transition: "background .16s, color .16s",
        }}>
          Use this template <IconChevronRight size={15} />
        </div>
      </div>
    </div>
  );
}

// ── The tabbed "Start with Template" repository ──
function TemplateRepository({ userTemplates = [], accent = "#e1251b", onCancel, onPickCurated, onPickUser }) {
  const [tab, setTab] = React.useState("curated");
  const tabs = [
    { key: "curated", label: "Curated Templates", count: CURATED_TEMPLATES.length },
    { key: "user", label: "User-Generated", count: userTemplates.length },
  ];
  const list = tab === "curated" ? CURATED_TEMPLATES : userTemplates;

  return (
    <div style={{ flex: 1, background: "#fbfbfc", overflowY: "auto", minWidth: 0 }}>
      {/* Header band */}
      <div style={{ background: "#fff", borderBottom: "1px solid #ececf0", padding: "28px 48px 0" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 20, marginBottom: 20 }}>
          <div>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".12em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>Team Store Setup</div>
            <h1 style={{ fontSize: 30, fontWeight: 800, margin: 0, letterSpacing: ".005em", color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Choose a Template</h1>
            <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 0", maxWidth: 560, lineHeight: 1.55 }}>
              Start from a curated system package or one of your organization's saved store blueprints. Branding is applied after you pick.
            </p>
          </div>
          <Button variant="ghost" onClick={onCancel} style={{ flexShrink: 0, marginTop: 8 }}>CANCEL</Button>
        </div>

        {/* Tabs */}
        <div style={{ display: "flex", gap: 4 }}>
          {tabs.map((t) => {
            const on = tab === t.key;
            return (
              <button key={t.key} onClick={() => setTab(t.key)} style={{
                background: "transparent", border: 0, padding: "16px 4px", marginRight: 24,
                fontSize: 14, fontWeight: on ? 800 : 600, color: on ? "#0e0e10" : "#9a9aa1",
                cursor: "pointer", borderBottom: `2.5px solid ${on ? "#0e0e10" : "transparent"}`,
                display: "inline-flex", alignItems: "center", gap: 8, marginBottom: -1.5, transition: "color .15s",
              }}>
                {t.label}
                <span style={{ fontSize: 11, fontWeight: 800, color: on ? "#fff" : "#9a9aa1", background: on ? "#0e0e10" : "#ececf0", borderRadius: 999, padding: "2px 8px", minWidth: 20, textAlign: "center" }}>{t.count}</span>
              </button>
            );
          })}
        </div>
      </div>

      {/* Grid */}
      <div style={{ maxWidth: 1180, margin: "0 auto", padding: "32px 40px 120px" }}>
        {list.length === 0 ? (
          <div style={{ textAlign: "center", padding: "80px 20px", color: "#9a9aa1" }}>
            <span style={{ width: 56, height: 56, borderRadius: 14, background: "#f1f1f3", color: "#9a9aa1", display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}><IconLayers size={26} /></span>
            <div style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif", marginBottom: 8 }}>No saved templates yet</div>
            <div style={{ fontSize: 13.5, lineHeight: 1.55, maxWidth: 400, margin: "0 auto" }}>
              Open a live store and choose <strong style={{ color: "#52525b" }}>Create Template</strong> from its menu to save a reusable blueprint here.
            </div>
          </div>
        ) : (
          <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 20 }}>
            {list.map((tpl) => (
              <TemplateSummaryCard key={tpl.id} tpl={tpl} kind={tab === "user" ? "user" : "curated"} accent={accent}
                onSelect={() => tab === "user" ? onPickUser && onPickUser(tpl) : onPickCurated && onPickCurated(tpl)} />
            ))}
          </div>
        )}
      </div>
    </div>
  );
}

Object.assign(window, { CreateTemplateModal, TemplateRepository, agnostifyProducts, blocksToCuratedProducts });

/* ===== ASSET 15 : 2c5489d0-b4fe-47bd-8c38-ec337fb8589d ===== */
// 6-step "Create a Team Store" wizard — step bodies, stepper, and product
// curation. Renders in the existing PROLOOK light design system (white cards,
// red accent, Archivo/Inter). Field structure + logic adapted from the team-store
// setup spec; visual language is entirely our own.

const { useState: useStateW, useRef: useRefW, useEffect: useEffectW, useMemo: useMemoW } = React;

// ───────────────────────── shared primitives ─────────────────────────

// Numbered step heading: accent square + title + description.
function StepHeader({ num, title, desc, accent = "#e1251b", action }) {
  return (
    <div style={{ marginBottom: 28 }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 8 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <span style={{
            width: 32, height: 32, borderRadius: 7, background: accent, color: "#fff",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            fontSize: 15, fontWeight: 800, fontFamily: "Archivo, sans-serif", flexShrink: 0,
          }}>{num}</span>
          <h2 style={{
            fontSize: 22, fontWeight: 800, letterSpacing: ".01em", margin: 0, color: "#0e0e10",
            fontFamily: "Archivo, sans-serif",
          }}>{title}</h2>
        </div>
        {action}
      </div>
      {desc && <p style={{ fontSize: 13.5, color: "#6b6b72", margin: 0, lineHeight: 1.55, maxWidth: 760 }}>{desc}</p>}
    </div>
  );
}

// White card panel with a small uppercase eyebrow label.
function WizardPanel({ label, hint, children, style }) {
  return (
    <div style={{
      background: "#fff", border: "1px solid #ececf0", borderRadius: 10,
      padding: 28, marginBottom: 16, boxShadow: "0 1px 2px rgba(20,20,30,.03)", ...style,
    }}>
      {label && (
        <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 16, marginBottom: 16 }}>
          <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".10em", color: "#0e0e10", textTransform: "uppercase" }}>{label}</div>
          {hint && <div style={{ fontSize: 12, color: "#9a9aa1" }}>{hint}</div>}
        </div>
      )}
      {children}
    </div>
  );
}

// Selectable card used for radio-style choices. Accent ring + tint + check when active.
function ChoiceCard({ active, onSelect, icon, title, desc, badge, points, accent = "#e1251b", minHeight = 0 }) {
  const [hover, setHover] = useStateW(false);
  return (
    <button
      type="button"
      onClick={onSelect}
      onMouseEnter={() => setHover(true)}
      onMouseLeave={() => setHover(false)}
      style={{
        position: "relative", textAlign: "left", width: "100%", minHeight,
        background: active ? "#fdf2f1" : "#fff",
        border: `1.5px solid ${active ? accent : hover ? "#c7c7cd" : "#e5e5e9"}`,
        borderRadius: 10, padding: "20px 20px 16px", cursor: "pointer",
        transition: "border-color .14s, background .14s, box-shadow .14s",
        boxShadow: active ? `0 0 0 3px ${hexA(accent, 0.12)}` : "none",
        display: "flex", flexDirection: "column", gap: 0,
      }}>
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 16 }}>
        {icon && (
          <span style={{
            width: 44, height: 44, borderRadius: 9, flexShrink: 0,
            background: active ? accent : "#f1f1f3",
            color: active ? "#fff" : "#0e0e10",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            transition: "background .14s, color .14s",
          }}>{icon}</span>
        )}
        <span style={{
          width: 24, height: 24, borderRadius: "50%", flexShrink: 0, marginTop: 2,
          border: `1.5px solid ${active ? accent : "#cfcfd5"}`,
          background: active ? accent : "#fff",
          display: "inline-flex", alignItems: "center", justifyContent: "center",
        }}>
          {active && <IconCheck size={13} style={{ color: "#fff", strokeWidth: 3.4 }} />}
        </span>
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8 }}>
        <span style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>{title}</span>
        {badge && (
          <span style={{
            fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#1f8a55",
            background: "#e7f5ed", padding: "4px 8px", borderRadius: 999,
          }}>{badge}</span>
        )}
      </div>
      <span style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5 }}>{desc}</span>
      {points && points.length > 0 && (
        <span style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 14 }}>
          {points.map((pt) => (
            <span key={pt} style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 11.5, fontWeight: 600, color: "#4b4b52", background: active ? "#fff" : "#f4f4f6", borderRadius: 999, padding: "4px 12px" }}>
              <IconCheck size={12} style={{ strokeWidth: 3, color: accent }} /> {pt}
            </span>
          ))}
        </span>
      )}
    </button>
  );
}

// Checkbox card (multi-select).
function CheckOptionCard({ checked, onToggle, title, desc, accent = "#e1251b" }) {
  return (
    <button
      type="button"
      onClick={() => onToggle(!checked)}
      style={{
        textAlign: "left", width: "100%", display: "flex", gap: 16, alignItems: "flex-start",
        background: checked ? "#fdf2f1" : "#fff",
        border: `1.5px solid ${checked ? accent : "#e5e5e9"}`,
        borderRadius: 10, padding: "16px 20px", cursor: "pointer",
        transition: "border-color .14s, background .14s",
      }}>
      <span style={{
        width: 24, height: 24, borderRadius: 5, flexShrink: 0, marginTop: 1,
        border: `1.5px solid ${checked ? accent : "#cfcfd5"}`,
        background: checked ? accent : "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center",
      }}>
        {checked && <IconCheck size={14} style={{ color: "#fff", strokeWidth: 3.2 }} />}
      </span>
      <span>
        <span style={{ display: "block", fontSize: 14.5, fontWeight: 700, color: "#0e0e10", marginBottom: 4 }}>{title}</span>
        <span style={{ display: "block", fontSize: 13, color: "#6b6b72", lineHeight: 1.5 }}>{desc}</span>
      </span>
    </button>
  );
}

// iOS-style toggle. Accent when on.
function ToggleSwitch({ on, onChange, accent = "#e1251b" }) {
  return (
    <button
      type="button"
      onClick={() => onChange(!on)}
      style={{
        width: 52, height: 28, borderRadius: 999, border: 0, cursor: "pointer", flexShrink: 0,
        background: on ? "#0e0e10" : "#d4d4d8", position: "relative", transition: "background .18s", padding: 0,
      }}>
      <span style={{
        position: "absolute", top: 3, left: on ? 27 : 3, width: 22, height: 22, borderRadius: "50%",
        background: "#fff", boxShadow: "0 1px 3px rgba(20,20,30,.25)", transition: "left .18s",
      }} />
    </button>
  );
}

// Floating-label date input matching the app's TextField look.
function WizardDate({ label, value, onChange, error }) {
  const [focus, setFocus] = useStateW(false);
  return (
    <div style={{ position: "relative", flex: 1 }}>
      <div style={{
        position: "relative", border: `1px solid ${error ? "#b81d14" : focus ? "#0e0e10" : "#d4d4d8"}`,
        borderRadius: 4, background: "#fff", transition: "border-color .15s",
      }}>
        <label style={{
          position: "absolute", left: 12, top: -8, fontSize: 11, padding: "0 4px",
          color: error ? "#b81d14" : focus ? "#0e0e10" : "#6b6b72", background: "#fff",
          pointerEvents: "none", fontWeight: 500, zIndex: 1,
        }}>{label}</label>
        <input
          type="date" value={value || ""} onChange={(e) => onChange(e.target.value)}
          onFocus={() => setFocus(true)} onBlur={() => setFocus(false)}
          style={{
            width: "100%", border: 0, outline: 0, background: "transparent",
            padding: "16px 12px", fontSize: 14, color: value ? "#0e0e10" : "#9a9aa1", fontFamily: "inherit",
          }} />
      </div>
      {error && <div style={{ color: "#b81d14", fontSize: 11, marginTop: 4, marginLeft: 2 }}>{error}</div>}
    </div>
  );
}

// rgba helper from hex
function hexA(hex, a) {
  const h = (hex || "#e1251b").replace("#", "");
  const r = parseInt(h.slice(0, 2), 16), g = parseInt(h.slice(2, 4), 16), b = parseInt(h.slice(4, 6), 16);
  return `rgba(${r},${g},${b},${a})`;
}

// ───────────────────────── stepper ─────────────────────────

const WIZARD_STEPS = [
  { key: "basics", label: "Basics" },
  { key: "products", label: "Add Products" },
  { key: "review", label: "Review & Deploy" },
];
// Store-type-dependent step lists. League Stores get a Divisions & Teams step
// between Basics and Add Products. Before a Store Type is chosen the stepper
// shows the League (4-step) form — collapsing to 3 on picking Individual reads
// as a simplification; growing mid-task reads as the app changing the rules.
const WIZARD_STEPS_LEAGUE = [
  { key: "basics", label: "Basics" },
  { key: "structure", label: "Divisions & Teams" },
  { key: "products", label: "Add Products" },
  { key: "review", label: "Review & Deploy" },
];
function wizardStepsFor(storeType) {
  return storeType === "single" ? WIZARD_STEPS : WIZARD_STEPS_LEAGUE;
}

function WizardStepper({ current, maxReached, onJump, accent = "#e1251b", steps = WIZARD_STEPS, preDone = [] }) {
  const pct = (current / (steps.length - 1)) * 100;
  return (
    <div style={{ marginBottom: 32 }}>
      {/* progress track */}
      <div style={{ position: "relative", height: 4, background: "#ececf0", borderRadius: 999, marginBottom: 20 }}>
        <div style={{
          position: "absolute", left: 0, top: 0, height: "100%", width: `${pct}%`,
          background: accent, borderRadius: 999, transition: "width .3s ease",
        }} />
      </div>
      {/* step labels */}
      <div style={{ display: "flex", justifyContent: "space-between" }}>
        {steps.map((s, i) => {
          const pre = preDone.includes(s.key) && i !== current;
          const done = i < current || pre;
          const active = i === current;
          const reachable = i <= maxReached;
          return (
            <button
              key={s.key}
              type="button"
              disabled={!reachable}
              onClick={() => reachable && onJump(i)}
              style={{
                background: "transparent", border: 0, padding: 0,
                cursor: reachable ? "pointer" : "default",
                display: "flex", alignItems: "center", gap: 8, flex: "0 0 auto",
              }}>
              <span style={{
                width: 28, height: 28, borderRadius: "50%", flexShrink: 0,
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                fontSize: 12, fontWeight: 800, fontFamily: "Archivo, sans-serif",
                background: done ? accent : active ? "#fff" : "#f1f1f3",
                color: done ? "#fff" : active ? accent : "#9a9aa1",
                border: active ? `2px solid ${accent}` : done ? "none" : "1.5px solid #e5e5e9",
                transition: "all .18s",
              }}>
                {done ? <IconCheck size={13} style={{ strokeWidth: 3.4 }} /> : i + 1}
              </span>
              <span style={{
                fontSize: 12.5, fontWeight: active || done ? 800 : 600, letterSpacing: ".02em",
                color: active ? "#0e0e10" : done ? "#0e0e10" : "#9a9aa1",
                textTransform: "uppercase", whiteSpace: "nowrap",
              }}>{s.label}</span>
              {pre && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".06em", background: "#e7f5ed", color: "#1f9d55", borderRadius: 999, padding: "3px 8px", whiteSpace: "nowrap" }}>PRE-LOADED · REVIEW OPTIONAL</span>}
            </button>
          );
        })}
      </div>
    </div>
  );
}

// ───────────────────────── STEP 1 — Basics (+ schedule) ─────────────────────────

const STORE_SCHEDULES = [
  { key: "popup", title: "Temporary Pop-Up", icon: (p) => <IconClock {...p} />, desc: "Best for player uniforms and seasonal spiritwear. Closes on a set date for bulk production." },
  { key: "locker", title: "Always-On Locker", icon: (p) => <IconLock {...p} />, desc: "Perfect for continuous school branding, year-round academy shirts, or coach merchandise." },
];

const FULFILLMENT_METHODS = [
  { key: "coach", title: "Ship to Team Manager", badge: "FREE", icon: (p) => <IconUsers {...p} />, desc: "All orders are sorted by player roster name and dispatched in bulk directly to the Coach or Director." },
  { key: "home", title: "Direct-To-Home Residential Delivery", icon: (p) => <IconBag {...p} />, desc: "Individual carrier shipping rates applied — packages sent directly to each customer's door." },
];

// Teams pulled from the team database (mock).
const WIZARD_TEAMS = [
  { value: "carlsbad-padres", label: "Carlsbad Padres" },
  { value: "valley-hoyas", label: "Valley Hoyas" },
  { value: "north-rams", label: "North Rams" },
  { value: "honey-badgers", label: "Honey Badgers" },
  { value: "general-fc", label: "General FC" },
  { value: "stokes-athletics", label: "Stokes Athletics" },
  { value: "create-new", label: "+ Connect a new team…" },
];

const WIZARD_SPORTS = ["Baseball", "Basketball", "Football", "Hockey", "Lacrosse", "Soccer", "Softball", "Track & Field", "Volleyball", "Wrestling", "Cheer", "Golf"];

// Account reps available as a point of contact (mock salesperson directory).
const SALESPEOPLE = [
  { value: "braeden-stokes", label: "Braeden Stokes", email: "braeden@stokesathletics.com", phone: "208-654-8619" },
  { value: "maria-lopez", label: "Maria Lopez", email: "maria@prolook.com", phone: "801-555-0142" },
  { value: "devon-pierce", label: "Devon Pierce", email: "devon@prolook.com", phone: "801-555-0188" },
];

// PROLOOK service desk — the default footer contact.
const SERVICE_CONTACT = { email: "service@prolookstores.com", phone: "888-776-5665" };

// Small radio dot.
function RadioDot({ on, accent = "#e1251b" }) {
  return (
    <span style={{
      width: 20, height: 20, borderRadius: "50%", flexShrink: 0, marginTop: 1,
      border: `1.5px solid ${on ? accent : "#cfcfd5"}`,
      background: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center",
      transition: "border-color .14s",
    }}>
      {on && <span style={{ width: 12, height: 12, borderRadius: "50%", background: accent }} />}
    </span>
  );
}

// Grid multi-select (Sports) — uniform full-width pill buttons.
function MultiSelectChips({ options, value, onChange, accent = "#e1251b" }) {
  const sel = value || [];
  const toggle = (o) => onChange(sel.includes(o) ? sel.filter((x) => x !== o) : [...sel, o]);
  return (
    <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12 }}>
      {options.map((o) => {
        const on = sel.includes(o);
        return (
          <button key={o} type="button" onClick={() => toggle(o)} style={{
            display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8,
            padding: "12px 16px", borderRadius: 999, cursor: "pointer", fontSize: 13.5, fontWeight: 600,
            border: `1.5px solid ${on ? accent : "#e5e5e9"}`,
            background: on ? hexA(accent, 0.08) : "#fff",
            color: on ? accent : "#4b4b52", transition: "all .13s",
          }}>
            {on && <IconCheck size={13} style={{ strokeWidth: 3 }} />}
            {o}
          </button>
        );
      })}
    </div>
  );
}

// One contact channel (email or phone) with Team Manager / Salesperson / Custom
// radio rows. Team Manager (the default) inherits the Team Manager record live —
// it reads through to that record rather than copying the value.
function ContactChannel({ kind, mode, onMode, sp, onSp, custom, onCustom, accent, inherited, onEditManager }) {
  const isEmail = kind === "email";
  const Icon = isEmail ? IconMail : IconPhone;
  const m = mode === "service" ? "teammanager" : (mode || ""); // legacy "service" reads as Team Manager
  const labels = isEmail
    ? { tm: "Team Manager Email", sp: "Salesperson Email", custom: "Custom Email" }
    : { tm: "Team Manager Phone", sp: "Salesperson Phone", custom: "Custom Phone" };
  const placeholder = isEmail ? "example@email.com" : "000-000-0000";
  const spField = isEmail ? "email" : "phone";

  const Row = ({ val, label, children }) => (
    <button type="button" onClick={() => onMode(val)} style={{
      textAlign: "left", width: "100%", background: "transparent", border: 0, padding: 0,
      cursor: "pointer", display: "block",
    }}>
      <div style={{ display: "flex", gap: 12, alignItems: "flex-start" }}>
        <RadioDot on={m === val} accent={accent} />
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", lineHeight: 1.3 }}>{label}</div>
          {children}
        </div>
      </div>
    </button>
  );

  const inputStyle = {
    width: "100%", border: "1px solid #d4d4d8", borderRadius: 5, padding: "12px 12px",
    fontSize: 13.5, color: "#0e0e10", outline: "none", marginTop: 8, fontFamily: "inherit",
  };

  return (
    <div style={{ flex: 1, minWidth: 0 }}>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 16 }}>
        <Icon size={16} style={{ color: accent }} />
        <span style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>
          {isEmail ? "Email" : "Phone"}
        </span>
      </div>
      <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
        <Row val="teammanager" label={labels.tm}>
          {/* Inherits — read-only here; edited on the Team Manager record itself */}
          {(inherited || m === "teammanager") && <div style={{ marginTop: 2 }}>
            <div style={{ fontSize: 13, color: inherited ? "#0e0e10" : "#b06f1e", fontWeight: 600, fontFamily: (isEmail || !inherited) ? "inherit" : "JetBrains Mono, monospace" }}>
              {inherited || ("No " + (isEmail ? "email" : "phone number") + " on the Team Manager record yet")}
            </div>
            {m === "teammanager" && (
              <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 3 }}>
                Follows the Team Manager record.{" "}
                {onEditManager && <span role="button" onClick={(e) => { e.stopPropagation(); onEditManager(); }} style={{ color: "#0e0e10", fontWeight: 600, textDecoration: "underline", textUnderlineOffset: 2, cursor: "pointer" }}>Edit it there</span>}
              </div>
            )}
          </div>}
        </Row>
        <Row val="salesperson" label={labels.sp}>
          {m === "salesperson" && (
            <div onClick={(e) => e.stopPropagation()} style={{ marginTop: 8 }}>
              <SelectField label="Select a Salesperson" value={sp}
                onChange={onSp}
                options={SALESPEOPLE.map((s) => ({ value: s.value, label: s.label + " · " + s[spField] }))} />
            </div>
          )}
        </Row>
        <Row val="custom" label={labels.custom}>
          {m === "custom" && (
            <input onClick={(e) => e.stopPropagation()} value={custom || ""} placeholder={placeholder}
              type={isEmail ? "email" : "tel"}
              onChange={(e) => onCustom(isEmail ? e.target.value : formatPhone(e.target.value))} style={inputStyle} />
          )}
        </Row>
      </div>
    </div>
  );
}

function StorePointOfContact({ data, set, accent, errors = {} }) {
  // Scroll the wizard to the Team Manager record (no-op where the panel isn't on screen).
  const jumpToManagerPanel = () => {
    const el = document.getElementById("tm-panel");
    if (el) window.scrollTo({ top: window.scrollY + el.getBoundingClientRect().top - 100, behavior: "smooth" });
  };
  return (
    <div style={{ display: "grid", gridTemplateColumns: "200px 1fr", gap: 28 }}>
      <div style={{ fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>
        Displayed on the store's footer. Select the primary point of contact buyers will reach out to for this store.
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 40, alignItems: "start" }}>
        <div>
          <ContactChannel kind="email" accent={accent}
            inherited={data.email} onEditManager={jumpToManagerPanel}
            mode={data.contactEmailMode} onMode={(v) => set({ contactEmailMode: v })}
            sp={data.contactEmailSalesperson} onSp={(v) => set({ contactEmailSalesperson: v })}
            custom={data.contactEmailCustom} onCustom={(v) => set({ contactEmailCustom: v })} />
          {errors.contactEmailMode && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 12 }}>{errors.contactEmailMode}</div>}
        </div>
        <div>
          <ContactChannel kind="phone" accent={accent}
            inherited={data.phone} onEditManager={jumpToManagerPanel}
            mode={data.contactPhoneMode} onMode={(v) => set({ contactPhoneMode: v })}
            sp={data.contactPhoneSalesperson} onSp={(v) => set({ contactPhoneSalesperson: v })}
            custom={data.contactPhoneCustom} onCustom={(v) => set({ contactPhoneCustom: v })} />
          {errors.contactPhoneMode && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 12 }}>{errors.contactPhoneMode}</div>}
        </div>
      </div>
    </div>
  );
}

// ───────────────────────── Team Database (Connect to Team) ─────────────────────────
// Mock organizations directory. Selecting a row inherits the team's official
// logo, primary/secondary colors, and sports into the wizard.
const TEAM_DATABASE = [
  { id: "carlsbad-padres", name: "Carlsbad Padres", address: "3557 Lancer Way, Carlsbad, CA 92008", mono: "CP", logoColor: "#1c2a5e", primary: "#1c2a5e", secondary: "#f5a800", sports: ["Baseball"], mascot: 9, master: 4 },
  { id: "valley-hoyas", name: "Valley Hoyas", address: "812 Mountain Rd, Provo, UT 84604", mono: "VH", logoColor: "#0e0e10", primary: "#0e0e10", secondary: "#9b1b30", sports: ["Basketball", "Track & Field"], mascot: 12, master: 5 },
  { id: "north-rams", name: "North Rams", address: "245 Field Ave, Perrisville, CA 92571", mono: "NR", logoColor: "#1f7a44", primary: "#1f8a55", secondary: "#f5a800", sports: ["Football", "Wrestling"], mascot: 0, master: 0 },
  { id: "honey-badgers", name: "Honey Badgers", address: "98 Canyon Blvd, Tucson, AZ 85704", mono: "HB", logoColor: "#b5760a", primary: "#f5a800", secondary: "#0e0e10", sports: ["Soccer"], mascot: 4, master: 1 },
  { id: "abbeville-christian", name: "Abbeville Christian Academy", address: "9 M L Tillis Dr, Abbeville, AL 36310", mono: "AC", logoColor: "#4a148c", primary: "#4a148c", secondary: "#c5b358", sports: ["Football", "Basketball", "Baseball"], mascot: 6, master: 2 },
  { id: "aaron-academy", name: "Aaron Academy", address: "645 East Main Street, Hendersonville, TN 37075", mono: "AA", logoColor: "#9b1b30", primary: "#9b1b30", secondary: "#0e0e10", sports: ["Soccer", "Volleyball"], mascot: 2, master: 1 },
  { id: "21st-century", name: "21st Century Charter School", address: "724 Washington St, Gary, IN 46402", mono: "21", logoColor: "#1f53c4", primary: "#1f53c4", secondary: "#f5a800", sports: ["Basketball", "Track & Field"], mascot: 8, master: 1 },
  { id: "91st-psalm", name: "91st Psalm Christian School", address: "2020 E Baseline Rd, Phoenix, AZ 85042", mono: "91", logoColor: "#2563eb", primary: "#2563eb", secondary: "#0e0e10", sports: ["Cheer", "Soccer"], mascot: 0, master: 0 },
];

// A single labeled progress bar — used for the Mascot Traced / Master Reference
// metrics columns in the team database rows.
function TeamMetricBar({ label, value, max, color }) {
  const pct = Math.max(value > 0 ? 6 : 0, Math.min(100, (value / max) * 100));
  return (
    <div>
      <div style={{ fontSize: 12, color: "#6b6b72", marginBottom: 4 }}>
        {label}: <span style={{ color: "#0e0e10", fontWeight: 700 }}>{value}</span>
      </div>
      <div style={{ height: 8, borderRadius: 999, background: "#ececed", overflow: "hidden" }}>
        <div style={{ height: "100%", width: `${pct}%`, borderRadius: 999, background: color }} />
      </div>
    </div>
  );
}

function TeamDatabaseModal({ open, onClose, onSelect, accent, hasTeam, onClearTeam }) {
  const [name, setName] = useStateW("");
  const [zip, setZip] = useStateW("");
  const [q, setQ] = useStateW({ name: "", zip: "" });

  useEffectW(() => { if (open) { setName(""); setZip(""); setQ({ name: "", zip: "" }); } }, [open]);
  useEffectW(() => {
    if (!open) return;
    const h = (e) => { if (e.key === "Escape") onClose(); };
    window.addEventListener("keydown", h);
    document.body.style.overflow = "hidden";
    return () => { window.removeEventListener("keydown", h); document.body.style.overflow = ""; };
  }, [open, onClose]);
  if (!open) return null;

  const rows = TEAM_DATABASE.filter((t) =>
    (!q.name || t.name.toLowerCase().includes(q.name.toLowerCase())) &&
    (!q.zip || t.address.includes(q.zip))
  );

  const searchInput = {
    height: 40, borderRadius: 6, border: "1px solid #d4d4d8", background: "#fff",
    padding: "0 12px", fontSize: 13, color: "#0e0e10", outline: "none", fontFamily: "inherit",
  };
  const runSearch = () => setQ({ name, zip });
  const clearSearch = () => { setName(""); setZip(""); setQ({ name: "", zip: "" }); };

  return (
    <div onClick={onClose} style={{
      position: "fixed", inset: 0, zIndex: 140, background: "rgba(20,20,30,.55)",
      display: "flex", alignItems: "center", justifyContent: "center", padding: 24,
    }}>
      <div onClick={(e) => e.stopPropagation()} style={{
        width: "min(1080px, 100%)", height: "min(760px, 100%)", background: "#fff",
        borderRadius: 12, boxShadow: "0 32px 64px rgba(20,20,30,.34)", overflow: "hidden",
        display: "flex", flexDirection: "column",
      }}>
        {/* Search header */}
        <div style={{ padding: "16px 28px", borderBottom: "1px solid #ececf0", display: "flex", alignItems: "center", gap: 16, flexWrap: "wrap" }}>
          <div style={{ display: "flex", alignItems: "baseline", gap: 12, marginRight: "auto" }}>
            <span style={{ fontSize: 18, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Team Database</span>
            <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>{rows.length.toLocaleString()} organizations</span>
          </div>
          <input value={name} onChange={(e) => setName(e.target.value)} onKeyDown={(e) => e.key === "Enter" && runSearch()} placeholder="Organization name" style={{ ...searchInput, width: 220 }} />
          <input value={zip} onChange={(e) => setZip(e.target.value)} onKeyDown={(e) => e.key === "Enter" && runSearch()} placeholder="ZIP code" style={{ ...searchInput, width: 112 }} />
          <button onClick={runSearch} style={{
            height: 40, padding: "0 16px", border: 0, borderRadius: 6, background: accent, color: "#fff",
            fontSize: 13, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8,
          }}><IconSearch size={15} /> Search</button>
          <button onClick={() => { clearSearch(); if (onClearTeam && hasTeam) onClearTeam(); }} disabled={onClearTeam ? !hasTeam : false}
            title={onClearTeam ? (hasTeam ? "Clear the connected team \u2014 removes its logo, colors, and sports" : "No team selected to clear") : undefined} style={{
            height: 40, padding: "0 16px", border: "1px solid #d4d4d8", borderRadius: 6, background: "#fff",
            color: onClearTeam && !hasTeam ? "#b0b0b6" : "#0e0e10", fontSize: 13, fontWeight: 700, cursor: onClearTeam && !hasTeam ? "default" : "pointer",
          }}>Clear</button>
          <button onClick={onClose} title="Close" style={{
            width: 40, height: 40, borderRadius: "50%", border: "none", background: "transparent",
            color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.background = "#f1f1f3"; }}
            onMouseLeave={(e) => { e.currentTarget.style.background = "transparent"; }}>
            <IconX size={20} />
          </button>
        </div>

        {/* Status chips */}
        <div style={{ padding: "12px 28px", borderBottom: "1px solid #f1f1f3", display: "flex", gap: 12, alignItems: "center", fontSize: 12 }}>
          <span style={{ background: "#fdf3d3", color: "#8a6d12", fontWeight: 700, padding: "4px 12px", borderRadius: 5 }}>WIP Schools: 16,359</span>
          <span style={{ background: "#e7f5ed", color: "#1f7a44", fontWeight: 700, padding: "4px 12px", borderRadius: 5 }}>Completed Schools: 835</span>
          <span style={{ color: "#9a9aa1" }}>Total Vectored Logos: 6,016 / 19,843</span>
        </div>

        {/* Rows */}
        <div style={{ flex: 1, overflowY: "auto" }}>
          {rows.map((t) => (
            <div key={t.id} style={{
              display: "grid", gridTemplateColumns: "60px 1fr 280px 116px", gap: 24, alignItems: "center",
              padding: "16px 28px", borderBottom: "1px solid #f4f4f6",
            }}>
              <div style={{
                width: 56, height: 56, borderRadius: 10, background: t.logoColor, color: "#fff",
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                fontSize: 20, fontWeight: 800, fontFamily: "Archivo, sans-serif",
              }}>{t.mono}</div>
              <div style={{ minWidth: 0 }}>
                <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10", marginBottom: 4 }}>{t.name}</div>
                <div style={{ fontSize: 12.5, color: "#6b6b72", marginBottom: 8 }}>{t.address}</div>
                <div style={{ fontSize: 11.5, color: "#9a9aa1", fontWeight: 600, letterSpacing: ".02em", textTransform: "uppercase" }}>{t.sports.join(" · ")}</div>
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                <TeamMetricBar label="Mascot Traced" value={t.mascot} max={12} color={accent} />
                <TeamMetricBar label="Master Reference" value={t.master} max={5} color="#0e0e10" />
              </div>
              <button onClick={() => onSelect(t)} style={{
                height: 40, border: 0, borderRadius: 6, background: "#0e0e10", color: "#fff",
                fontSize: 13, fontWeight: 800, letterSpacing: ".02em", cursor: "pointer",
                display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8,
                transition: "background .14s",
              }}
                onMouseEnter={(e) => e.currentTarget.style.background = accent}
                onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
                Select <IconChevronRight size={14} style={{ strokeWidth: 2.6 }} />
              </button>
            </div>
          ))}
          {rows.length === 0 && (
            <div style={{ textAlign: "center", color: "#9a9aa1", fontSize: 14, fontWeight: 600, padding: "60px 0" }}>
              No organizations match your search
            </div>
          )}
        </div>

        {/* Pagination footer */}
        <div style={{ padding: "12px 28px", borderTop: "1px solid #ececf0", display: "flex", alignItems: "center", justifyContent: "space-between" }}>
          <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>Showing {rows.length} of 17,194</span>
          <div style={{ display: "flex", gap: 4, alignItems: "center" }}>
            {["1", "2", "3", "4", "5", "…", "860"].map((p, i) => (
              <span key={i} style={{
                minWidth: 32, height: 32, padding: "0 8px", borderRadius: 6, display: "inline-flex",
                alignItems: "center", justifyContent: "center", fontSize: 12.5, fontWeight: 700,
                background: p === "1" ? accent : "transparent", color: p === "1" ? "#fff" : "#6b6b72",
                cursor: p === "…" ? "default" : "pointer",
              }}>{p}</span>
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}

// ───────────────────────── Store Type (core branching) ─────────────────────────
// The first decision in the wizard. "The Multi-Team Hub" unlocks the multi-team
// architecture (division → team canvas, product-to-team assignment, manager
// mapping). "The Single-Team Locker" enforces a single-team, streamlined workflow.
const STORE_TYPES = [
  {
    key: "league",
    name: "League Store",
    tag: "League Store",
    icon: (p) => <IconLayers {...p} />,
    desc: "The Multi-Team Hub. Optimized for full leagues, multi-club organizations, and tournament circuits. Unlocks advanced structural hierarchies to manage distinct locations, tiered age divisions, endless uniform variations, and multi-roster sheets.",
    points: ["Divisions & teams on a visual canvas", "Product-to-team assignment", "Per-team branding & managers"],
  },
  {
    key: "single",
    name: "Individual Team Store",
    tag: "Individual Team Store",
    icon: (p) => <IconUsers {...p} />,
    desc: "The Single-Team Locker. Optimized for a standalone squad, independent club, or single school team. Outfitted with a streamlined workflow for one uniform profile, a single roster sheet, and immediate deployment.",
    points: ["One roster sheet", "One uniform profile", "Immediate deployment"],
  },
];

// Rich store-type dropdown — collapsed trigger shows the current selection;
// opening reveals both configurations with descriptive helper copy.
function StoreTypeSelect({ value, onChange, accent = "#e1251b" }) {
  const [open, setOpen] = useStateW(false);
  const ref = React.useRef(null);
  useEffectW(() => {
    if (!open) return;
    const onDoc = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);
  const cur = STORE_TYPES.find((t) => t.key === value) || STORE_TYPES[0];

  const OptionRow = ({ t }) => {
    const on = t.key === value;
    return (
      <button type="button" onClick={() => { onChange(t.key); setOpen(false); }} style={{
        display: "flex", gap: 16, width: "100%", textAlign: "left", cursor: "pointer",
        background: on ? hexA(accent, 0.05) : "#fff", border: 0, borderBottom: "1px solid #f1f1f3",
        padding: "16px 20px", transition: "background .12s",
      }}
        onMouseEnter={(e) => { if (!on) e.currentTarget.style.background = "#fafafb"; }}
        onMouseLeave={(e) => { if (!on) e.currentTarget.style.background = "#fff"; }}>
        <span style={{
          width: 44, height: 44, borderRadius: 10, flexShrink: 0, marginTop: 2,
          background: on ? accent : "#f1f1f3", color: on ? "#fff" : "#0e0e10",
          display: "inline-flex", alignItems: "center", justifyContent: "center", transition: "all .12s",
        }}>{t.icon({ size: 21 })}</span>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 4 }}>
            <span style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10", letterSpacing: "-.01em", fontFamily: "Archivo, sans-serif" }}>{t.name}</span>
          </div>
          <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>{t.desc}</div>
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 12 }}>
            {t.points.map((pt) => (
              <span key={pt} style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 11.5, fontWeight: 600, color: "#4b4b52", background: "#f4f4f6", borderRadius: 999, padding: "4px 12px" }}>
                <IconCheck size={12} style={{ strokeWidth: 3, color: accent }} /> {pt}
              </span>
            ))}
          </div>
        </div>
        <span style={{ flexShrink: 0, marginTop: 4 }}>
          <RadioDot on={on} accent={accent} />
        </span>
      </button>
    );
  };

  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button type="button" onClick={() => setOpen((o) => !o)} style={{
        display: "flex", alignItems: "center", gap: 16, width: "100%", textAlign: "left", cursor: "pointer",
        background: "#fff", border: `1.5px solid ${open ? accent : "#d4d4d8"}`, borderRadius: 10,
        padding: "16px 16px", boxShadow: open ? `0 0 0 3px ${hexA(accent, 0.12)}` : "none", transition: "border-color .14s, box-shadow .14s",
      }}>
        <span style={{
          width: 44, height: 44, borderRadius: 10, flexShrink: 0, background: accent, color: "#fff",
          display: "inline-flex", alignItems: "center", justifyContent: "center",
        }}>{cur.icon({ size: 22 })}</span>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <span style={{ fontSize: 17, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: "-.01em" }}>{cur.name}</span>
          </div>
          <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 4, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{cur.desc}</div>
        </div>
        <IconChevron size={20} style={{ flexShrink: 0, color: "#6b6b72", transform: open ? "rotate(180deg)" : "none", transition: "transform .16s" }} />
      </button>
      {open && (
        <div style={{
          position: "absolute", top: "calc(100% + 8px)", left: 0, right: 0, zIndex: 40,
          background: "#fff", border: "1px solid #e5e5e9", borderRadius: 12, overflow: "hidden",
          boxShadow: "0 20px 48px rgba(20,20,30,.18)",
        }}>
          {STORE_TYPES.map((t) => <OptionRow key={t.key} t={t} />)}
        </div>
      )}
    </div>
  );
}

// ─────────── Ship-To Address (Ship to Team Manager) ───────────
// One address, three surfaces: revealed in Step 1 under the Fulfillment Method
// choice, shown on the Settings → Distribution & Fulfillment card, and edited in
// that card's modal. Reads/writes the store's existing ship* fields (the manager
// record's freight address) — never a second copy of the same destination.
function ShipToAddressFields({ value, onChange }) {
  const v = value || {};
  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
      <div style={{ display: "flex", gap: 16 }}>
        <TextField label="Street Address" value={v.shipStreet} onChange={(x) => onChange({ shipStreet: x })} />
        <TextField label="Apt, Suite, etc" value={v.shipApt} onChange={(x) => onChange({ shipApt: x })} style={{ flex: ".6" }} />
      </div>
      <div style={{ display: "flex", gap: 16 }}>
        <TextField label="City" value={v.shipCity} onChange={(x) => onChange({ shipCity: x })} />
        <SelectField label="State" value={v.shipState} options={EG_STATES} onChange={(x) => onChange({ shipState: x })} />
        <TextField label="ZIP Code" value={v.shipZip} onChange={(x) => onChange({ shipZip: x })} style={{ flex: ".6" }} />
      </div>
    </div>
  );
}
function shipToSummary(s) {
  const v = s || {};
  const l1 = [v.shipStreet, v.shipApt].filter(Boolean).join(", ");
  const l2 = [v.shipCity, [v.shipState, v.shipZip].filter(Boolean).join(" ")].filter(Boolean).join(", ");
  return [l1, l2].filter(Boolean).join(", ");
}

function Step1Basics({ data, set, errors, accent }) {
  const isPopup = data.distributionFormat === "popup";
  const [teamModalOpen, setTeamModalOpen] = useStateW(false);
  const connectedTeam = TEAM_DATABASE.find((t) => t.id === data.connectedTeam);
  const connectedLabel = (connectedTeam || {}).name || data.teamName;

  // ── Template-break guard ──
  // While a store is still linked to a saved template (data._fromUserTemplate),
  // the three structural options the template pre-selected — Store Type,
  // Store Schedule, Fulfillment Method — are "template-defining". Changing
  // any of them diverges from the blueprint, so we intercept and confirm first.
  // Confirming applies ONLY that change and detaches the store from the template
  // (the "Started from …" banner disappears); every other pre-loaded value stays.
  // Once detached, edits are free — no more prompts.
  const [breakModal, setBreakModal] = useStateW(null); // { label, current, next, patch }
  const guardTemplateSet = (label, patch, current, next) => {
    if (data._fromUserTemplate && current !== next) {
      setBreakModal({ label, current, next, patch });
    } else {
      set(patch);
    }
  };
  const confirmBreak = () => {
    if (breakModal) set({ ...breakModal.patch, _fromUserTemplate: false, _templateName: "" });
    setBreakModal(null);
  };

  // League → Individual removes the Divisions & Teams step. If the rep already
  // built structure there, confirm before discarding — never a silent wipe.
  const [typeSwitch, setTypeSwitch] = useStateW(null);
  const structCounts = (() => {
    const s = data.leagueStructure || { groups: [], loose: [] };
    const teams = (s.groups || []).reduce((n, g) => n + (g.rosters || []).length, 0) + (s.loose || []).length;
    return { divisions: (s.groups || []).length, teams };
  })();
  const requestStoreType = (key) => {
    if (key === "single" && data.storeType === "league" && (structCounts.divisions > 0 || structCounts.teams > 0)) {
      setTypeSwitch(key);
      return;
    }
    guardTemplateSet("Store Type", { storeType: key }, data.storeType, key);
  };
  const confirmTypeSwitch = () => {
    guardTemplateSet("Store Type", { storeType: typeSwitch, leagueStructure: { groups: [], loose: [] } }, data.storeType, typeSwitch);
    setTypeSwitch(null);
  };

  // Inherit the selected team's official branding into the wizard.
  const applyTeam = (t) => {
    set({
      connectedTeam: t.id,
      teamName: t.name,
      storeTitle: data.storeTitle && data.storeTitle.trim() ? data.storeTitle : t.name,
      ...(data._slugTouched ? {} : { storeSlug: slugify(data.storeTitle || t.name) }),
      teamLogo: { mono: t.mono, color: t.logoColor },
      teamColors: [t.primary, t.secondary, "#ffffff"],
      primaryColor: t.primary,
      sports: t.sports,
    });
    setTeamModalOpen(false);
  };

  // Clear the connected team — removes the imported logo, colors, and sports.
  const [teamToast, setTeamToast] = useStateW("");
  const clearTeam = () => {
    set({ connectedTeam: "", teamLogo: null, teamColors: [], sports: [] });
    setTeamModalOpen(false);
    setTeamToast("Team cleared — logo, colors, and sports removed");
  };

  const managerInput = {
    width: "100%", border: "1px solid #d4d4d8", borderRadius: 5, padding: "12px 12px",
    fontSize: 14, color: "#0e0e10", outline: "none", fontFamily: "inherit",
  };
  const patchAddedManager = (id, patch) => set({ additionalManagers: (data.additionalManagers || []).map((x) => x.id === id ? { ...x, ...patch } : x) });

  return (
    <div>
      <StepHeader num="1" accent={accent} title="Store Basics"
        desc="Set up the store, choose how it distributes orders, connect it to a team, assign a point of contact, and configure sales tax." />

      {data._fromUserTemplate && (
        <div style={{ border: `1.5px solid ${accent}`, background: `${accent}0a`, borderRadius: 12, padding: "16px 20px", marginBottom: 28 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 12 }}>
            <span style={{ width: 40, height: 40, borderRadius: 9, flexShrink: 0, background: accent, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconTemplateGrid size={18} /></span>
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 14.5, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Started from {data._templateName || "a saved template"}</div>
              <div style={{ fontSize: 12.5, color: "#6b6b72", marginTop: 1 }}>Structure, distribution, and {(data.curatedProducts || []).length} product{(data.curatedProducts || []).length === 1 ? "" : "s"} are pre-loaded. Just add your team's branding below.</div>
            </div>
          </div>
          <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #ececf0", borderRadius: 999, padding: "8px 12px", fontSize: 12, fontWeight: 700, color: "#0e0e10" }}>
              {data.storeType === "single" ? <IconUsers size={13} /> : <IconLayers size={13} />} {tplTypeLabel(data.storeType)}
            </span>
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #ececf0", borderRadius: 999, padding: "8px 12px", fontSize: 12, fontWeight: 700, color: "#0e0e10" }}>
              {data.distributionModel === "home" ? <IconBag size={13} /> : <IconUsers size={13} />} {tplDistLabel(data.distributionModel)}
            </span>
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #ececf0", borderRadius: 999, padding: "8px 12px", fontSize: 12, fontWeight: 700, color: "#0e0e10" }}>
              <IconShirt size={13} /> {(data.curatedProducts || []).length} products pre-loaded
            </span>
          </div>
        </div>
      )}

      <WizardPanel label="Store Type" hint="Determines the structural workflow across the whole store">
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
          {STORE_TYPES.map((t) => (
            <ChoiceCard key={t.key} accent={accent} minHeight={172}
              active={data.storeType === t.key}
              onSelect={() => requestStoreType(t.key)}
              icon={t.icon({ size: 22 })} title={t.name} desc={t.desc} points={t.points} />
          ))}
        </div>
        {errors.storeType && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 12 }}>{errors.storeType}</div>}
        {data.storeType && (
          <div style={{ display: "flex", alignItems: "flex-start", gap: 8, marginTop: 16, fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>
            <IconInfo size={15} style={{ flexShrink: 0, marginTop: 1, color: accent }} />
            <span>
              {data.storeType === "single"
                ? "Single-Team Locker keeps one team — the multi-team canvas and product-to-team assignment stay off across this store."
                : "Multi-Team Hub unlocks the division → team canvas, per-team branding, product-to-team assignment, and manager mapping throughout the workspace."}
            </span>
          </div>
        )}
      </WizardPanel>

      <WizardPanel label="Store Details">
        <TextField label="Store Name" value={data.storeTitle}
          onChange={(v) => set({ storeTitle: v, ...(data._slugTouched ? {} : { storeSlug: slugify(v) }) })}
          error={errors.storeTitle} />

        <div style={{ marginTop: 24 }}>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Web Address</div>
          <div style={{
            display: "flex", border: `1px solid ${errors.storeSlug ? "#b81d14" : "#d4d4d8"}`,
            borderRadius: 4, background: "#fff", overflow: "hidden",
          }}>
            <span style={{
              display: "inline-flex", alignItems: "center", padding: "0 16px", background: "#f7f7f8",
              borderRight: "1px solid #e5e5e9", color: "#6b6b72", fontSize: 13.5,
              fontFamily: "JetBrains Mono, monospace", whiteSpace: "nowrap",
            }}>prolookstores.com/</span>
            <input
              value={data.storeSlug || ""}
              onChange={(e) => set({ storeSlug: slugify(e.target.value), _slugTouched: true })}
              placeholder="store-slug"
              style={{
                flex: 1, minWidth: 0, border: 0, outline: 0, background: "transparent", padding: "12px 12px",
                fontSize: 13.5, color: "#0e0e10", fontFamily: "JetBrains Mono, monospace",
              }} />
          </div>
          {errors.storeSlug && <div style={{ color: "#b81d14", fontSize: 11, marginTop: 4 }}>{errors.storeSlug}</div>}
        </div>

        {/* Team Fundraising */}
        <div style={{ marginTop: 24, paddingTop: 20, borderTop: "1px solid #f1f1f3" }}>
          <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>Team Fundraising</div>
          <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
            <span style={{
              width: 40, height: 40, borderRadius: 9, flexShrink: 0,
              background: (data.fundraisingEnabled ?? false) ? "#fdecea" : "#f1f1f3", color: (data.fundraisingEnabled ?? false) ? accent : "#6b6b72",
              display: "inline-flex", alignItems: "center", justifyContent: "center",
            }}><IconDollar size={20} /></span>
            <div style={{ flex: 1 }}>
              <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Enable Team Fundraising</div>
              <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
                {(data.fundraisingEnabled ?? false)
                  ? "Items can collect a per-item fundraising amount, and a Fundraising Report is available under Reports."
                  : "New items stop collecting fundraising. Items with orders keep collecting, and entered amounts are kept."}
              </div>
            </div>
            <ToggleSwitch on={data.fundraisingEnabled ?? false} onChange={(v) => set({ fundraisingEnabled: v })} accent={accent} />
          </div>
        </div>
      </WizardPanel>

      <WizardPanel label="Store Schedule">
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
          {STORE_SCHEDULES.map((f) => (
            <ChoiceCard key={f.key} accent={accent} minHeight={172}
              active={data.distributionFormat === f.key}
              onSelect={() => guardTemplateSet("Store Schedule", { distributionFormat: f.key, storeOpenMode: f.key === "locker" ? "stay-open" : "set-window" }, data.distributionFormat, f.key)}
              icon={f.icon({ size: 22 })} title={f.title} desc={f.desc} />
          ))}
        </div>
        {errors.distributionFormat && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 12 }}>{errors.distributionFormat}</div>}
      </WizardPanel>

      {/* Store Timeline — revealed only for the Temporary Pop-Up format. Always-On
          Locker stores have no batch deadline, so the schedule card is hidden. */}
      {isPopup && (
        <WizardPanel label="Store Timeline">
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
            <WizardDate label="Store Launch Date" value={data.storeOpenStart} onChange={(v) => set({ storeOpenStart: v })} error={errors.storeOpenStart} />
            <WizardDate label="Store Closing Deadline" value={data.storeOpenEnd} onChange={(v) => set({ storeOpenEnd: v })} error={errors.storeOpenEnd} />
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 16, marginTop: 24, paddingTop: 20, borderTop: "1px solid #f1f1f3" }}>
            <span style={{
              width: 40, height: 40, borderRadius: 9, background: "#fff4e0", color: "#b5760a",
              display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0,
            }}><IconClock size={20} /></span>
            <div style={{ flex: 1 }}>
              <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Enable Urgency Timers</div>
              <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>Show a real-time closing countdown on the storefront.</div>
            </div>
            <ToggleSwitch on={data.urgencyTimers} onChange={(v) => set({ urgencyTimers: v })} accent={accent} />
          </div>
        </WizardPanel>
      )}

      {/* Announcement Message — Epic H. Optional and OUTSIDE Next validation; no
          asterisk, no error, no incomplete affordance. Writes the same store-level
          field Settings → Store Customization & Design (Epic I) reads. Renders for
          BOTH schedules — an Always-On Locker has no timeline above but still has
          returns, sizing, and shipping to communicate. Deliberately NO theme color
          here: team colors are imported in Team Branding below. */}
      <WizardPanel label="Announcement Message" hint="Optional — buyers see it at the top of your storefront">
        <AnnouncementEditor value={data.announcement || ""} onChange={(v) => set({ announcement: v })} accent={accent} />
        <div style={{ display: "flex", flexDirection: "column", gap: 8, marginTop: 14 }}>
          <div style={{ display: "flex", alignItems: "flex-start", gap: 8, fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>
            <IconInfo size={14} style={{ flexShrink: 0, marginTop: 1 }} />
            <span>Leave it blank and the storefront shows <b>no banner at all</b> — skipping this is a clean outcome, not an unfinished step.</span>
          </div>
          {data.storeType === "league" && (
            <div style={{ display: "flex", alignItems: "flex-start", gap: 8, fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>
              <IconInfo size={14} style={{ flexShrink: 0, marginTop: 1 }} />
              <span>This is the <b>store-level</b> message. Divisions and standalone teams can carry their own later from Settings — they're built in the next step.</span>
            </div>
          )}
        </div>
      </WizardPanel>

      <WizardPanel label="Team Branding &amp; Sport">
        {/* Import Team Profile — opens the team database modal */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between", gap: 20,
          border: `1.5px solid ${data.connectedTeam ? accent : "#e5e5e9"}`, borderRadius: 10,
          background: data.connectedTeam ? "#fdf2f1" : "#fafafb", padding: "16px 20px", marginBottom: 24,
        }}>
          <div style={{ display: "flex", alignItems: "flex-start", gap: 16, minWidth: 0 }}>
            <span style={{
              width: 44, height: 44, borderRadius: 9, flexShrink: 0,
              background: data.connectedTeam ? accent : "#0e0e10", color: "#fff",
              display: "inline-flex", alignItems: "center", justifyContent: "center",
            }}><IconGroupAdd size={20} /></span>
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", marginBottom: 4 }}>
                {data.connectedTeam ? `Connected — ${connectedLabel}` : "Import Team Profile"}
              </div>
              <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5, maxWidth: 560 }}>
                {data.connectedTeam
                  ? "Official logo, colors, and sports were imported below. Reconnect to switch teams."
                  : "Connect directly to our team database to automatically import the official school colors and logos used on your physical player uniforms."}
              </div>
            </div>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 10, flexShrink: 0 }}>
            {data.connectedTeam && <button type="button" onClick={clearTeam}
              title={data.connectedTeam ? "Remove the connected team\u2019s logo, colors, and sports" : "No team selected to clear"} style={{
                height: 44, padding: "0 18px", borderRadius: 6, border: "1px solid #d4d4d8",
                background: "#fff", color: data.connectedTeam ? "#0e0e10" : "#b0b0b6",
                fontSize: 13, fontWeight: 700, letterSpacing: ".01em",
                cursor: data.connectedTeam ? "pointer" : "default", transition: "border-color .14s",
              }}
              onMouseEnter={(e) => { if (data.connectedTeam) e.currentTarget.style.borderColor = "#0e0e10"; }}
              onMouseLeave={(e) => { e.currentTarget.style.borderColor = "#d4d4d8"; }}>
              Clear
            </button>}
            <button type="button" onClick={() => setTeamModalOpen(true)} style={{
              flexShrink: 0, height: 44, padding: "0 20px", border: 0, borderRadius: 6,
              background: "#0e0e10", color: "#fff", fontSize: 13, fontWeight: 700, letterSpacing: ".01em",
              cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, transition: "background .14s",
            }}
              onMouseEnter={(e) => e.currentTarget.style.background = accent}
              onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
              {data.connectedTeam ? "Change Team" : "Connect to Team"}
            </button>
          </div>
        </div>
        {errors.connectedTeam && <div style={{ color: "#b81d14", fontSize: 12, marginTop: -12, marginBottom: 16 }}>{errors.connectedTeam}</div>}

        <div style={{ marginBottom: 24 }}>
          <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Team Name</div>
          <input type="text" value={data.teamName || ""} onChange={(e) => set({ teamName: e.target.value })}
            placeholder="e.g. Carlsbad Padres" style={managerInput} />
          <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 8, lineHeight: 1.5 }}>
            {data.connectedTeam
              ? "Pulled from the connected team profile — edit if needed."
              : "Auto-fills when you connect to a team in the database."}
          </div>
        </div>

        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 24 }}>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: 20 }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 16 }}>Team Logo</div>
            <LogoDropZone logo={data.teamLogo} onChange={(v) => set({ teamLogo: v })} />
            <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 12, lineHeight: 1.5 }}>Used to brand the storefront header and pre-set product customization.</div>
          </div>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: 20 }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 16 }}>Team Colors</div>
            <ColorSwatches colors={data.teamColors || []} onChange={(v) => set({ teamColors: v })} />
          </div>
        </div>

        <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 16 }}>Sports <span style={{ color: "#9a9aa1", fontWeight: 500 }}>· select all that apply</span></div>
        <MultiSelectChips options={WIZARD_SPORTS} value={data.sports} onChange={(v) => set({ sports: v })} accent={accent} />
      </WizardPanel>

      {/* Team & Roster — same controls as Settings → Team & Roster. Bulk defaults, not locks; neither blocks Next. */}
      <WizardPanel label="Team &amp; Roster" hint="Defaults for every roster in this store — override per team later in My Team(s)">
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <span style={{
            width: 40, height: 40, borderRadius: 9, flexShrink: 0,
            background: (data.allowDuplicateNumbers ?? false) ? "#fdecea" : "#f1f1f3", color: (data.allowDuplicateNumbers ?? false) ? accent : "#6b6b72",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
          }}><IconUsers size={20} /></span>
          <div style={{ flex: 1 }}>
            <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Allow Duplicate Numbers</div>
            <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
              Sets the default for every team's roster in this store. Any individual roster can override it later from the My Team(s) tab — a bulk default, not a lock.
            </div>
          </div>
          <ToggleSwitch on={data.allowDuplicateNumbers ?? false} onChange={(v) => set({ allowDuplicateNumbers: v })} accent={accent} />
        </div>
        <div style={{ marginTop: 24, paddingTop: 20, borderTop: "1px solid #f1f1f3" }}>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 4 }}>Jersey Number Selection</div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", marginBottom: 12 }}>Decide who chooses jersey numbers for numbered team gear.</div>
          <NumberSelectionOptions value={data.numberSelection || "manager"} onChange={(v) => set({ numberSelection: v })} />
          <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 4, lineHeight: 1.5 }}>{numberSelectionHelper(data.numberSelection || "manager")}</div>
        </div>
      </WizardPanel>

      <WizardPanel label={data.storeType === "league" ? "League Manager" : "Team Manager"} hint={data.storeType === "league" ? "League director" : "Team coach"}>
        <span id="tm-panel" style={{ display: "block", height: 0 }}></span>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 16 }}>
          <div>
            <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>First Name</div>
            <input type="text" value={data.firstName || ""} onChange={(e) => set({ firstName: e.target.value })} placeholder="Jordan" style={managerInput} />
          </div>
          <div>
            <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Last Name</div>
            <input type="text" value={data.lastName || ""} onChange={(e) => set({ lastName: e.target.value })} placeholder="Rivera" style={managerInput} />
          </div>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
          <div>
            <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Email</div>
            <input type="email" value={data.email || ""} onChange={(e) => set({ email: e.target.value })} placeholder="name@example.com" style={{ ...managerInput, ...(errors.email ? { borderColor: "#b81d14" } : {}) }} />
            {errors.email && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 6 }}>{errors.email}</div>}
          </div>
          <div>
            <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Phone Number</div>
            <input type="tel" value={data.phone || ""} onChange={(e) => set({ phone: formatPhone(e.target.value) })} placeholder="111-000-3243" style={{ ...managerInput, fontFamily: "JetBrains Mono, monospace", ...(errors.phone ? { borderColor: "#b81d14" } : {}) }} />
            {errors.phone && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 6 }}>{errors.phone}</div>}
          </div>
        </div>

        {/* Additional managers — optional contact records, not portal invitations. Only the primary feeds the point of contact. */}
        {(data.additionalManagers || []).map((m) => (
          <div key={m.id} style={{ marginTop: 24, paddingTop: 20, borderTop: "1px solid #f1f1f3" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 16 }}>
              <span style={{ fontSize: 10.5, fontWeight: 800, letterSpacing: ".06em", color: "#6b6b72", background: "#f1f1f3", borderRadius: 999, padding: "4px 10px", textTransform: "uppercase" }}>Additional Manager</span>
              <span style={{ fontSize: 12, color: "#9a9aa1" }}>Contact record only — doesn't feed the point of contact</span>
              <span style={{ flex: 1 }} />
              <button type="button" onClick={() => set({ additionalManagers: (data.additionalManagers || []).filter((x) => x.id !== m.id) })}
                style={{ display: "inline-flex", alignItems: "center", gap: 6, background: "none", border: 0, color: "#b81d14", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", padding: 4 }}>
                <IconTrash size={13} style={{ strokeWidth: 2 }} /> Remove
              </button>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 16 }}>
              <div>
                <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>First Name</div>
                <input type="text" value={m.firstName || ""} onChange={(e) => patchAddedManager(m.id, { firstName: e.target.value })} placeholder="Alex" style={managerInput} />
              </div>
              <div>
                <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Last Name</div>
                <input type="text" value={m.lastName || ""} onChange={(e) => patchAddedManager(m.id, { lastName: e.target.value })} placeholder="Nguyen" style={managerInput} />
              </div>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
              <div>
                <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Email</div>
                <input type="email" value={m.email || ""} onChange={(e) => patchAddedManager(m.id, { email: e.target.value })} placeholder="name@example.com" style={managerInput} />
              </div>
              <div>
                <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8 }}>Phone Number</div>
                <input type="tel" value={m.phone || ""} onChange={(e) => patchAddedManager(m.id, { phone: formatPhone(e.target.value) })} placeholder="111-000-3243" style={{ ...managerInput, fontFamily: "JetBrains Mono, monospace" }} />
              </div>
            </div>
          </div>
        ))}
        <div style={{ marginTop: 20, paddingTop: 16, borderTop: "1px solid #f1f1f3", display: "flex", alignItems: "center", gap: 12, flexWrap: "wrap" }}>
          <button type="button" onClick={() => set({ additionalManagers: [...(data.additionalManagers || []), { id: "mgr-" + Date.now(), firstName: "", lastName: "", email: "", phone: "" }] })}
            style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "10px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
            <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Add another manager
          </button>
          <span style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.5 }}>Optional. These are contact records, not portal invitations — Team Manager Portal invites live in Settings → People &amp; Access.</span>
        </div>
      </WizardPanel>

      <WizardPanel label="Store Point of Contact">
        <StorePointOfContact data={data} set={set} accent={accent} errors={errors} />
      </WizardPanel>

      <WizardPanel label="Fulfillment Method">
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
          {FULFILLMENT_METHODS.map((m) => (
            <ChoiceCard key={m.key} accent={accent} minHeight={172}
              active={data.distributionModel === m.key}
              onSelect={() => guardTemplateSet("Fulfillment Method", { distributionModel: m.key }, data.distributionModel, m.key)}
              icon={m.icon({ size: 22 })} title={m.title} desc={m.desc} badge={m.badge} />
          ))}
        </div>
        {errors.distributionModel && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 12 }}>{errors.distributionModel}</div>}
        {/* Ship to Team Manager reveals the bulk-shipment destination inline. Same store
            ship* fields the manager record / Settings edit — one address, not two. */}
        {data.distributionModel === "coach" && (
          <div style={{ marginTop: 20, paddingTop: 20, borderTop: "1px solid #f1f1f3" }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 4 }}>Ship-To Address</div>
            <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, marginBottom: 16 }}>
              Where the bulk shipment goes — usually the {data.storeType === "league" ? "league" : "team"} manager's address. Optional for now, but orders can't be submitted for fulfillment until a destination address exists. Editable later in Settings → Distribution &amp; Fulfillment.
            </div>
            <ShipToAddressFields value={data} onChange={(patch) => set(patch)} />
          </div>
        )}
      </WizardPanel>

      <WizardPanel label="Compliance &amp; Taxation" hint="Turn this toggle off to mark the store as tax exempt — no sales tax is collected from storefront buyers">
        {/* Epic G — primary binary toggle is the single entry point for tax config */}
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <span style={{
            width: 40, height: 40, borderRadius: 9, flexShrink: 0,
            background: data.collectTax ? "#fdecea" : "#f1f1f3", color: data.collectTax ? accent : "#6b6b72",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
          }}><IconDollar size={20} /></span>
          <div style={{ flex: 1 }}>
            <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Collect Sales Tax</div>
            <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
              {data.collectTax
                ? "Tax is applied to buyers in your designated state at checkout."
                : "Tax-exempt checkout — no tax is applied and no extra inputs are required."}
            </div>
          </div>
          <ToggleSwitch on={data.collectTax} onChange={(v) => set({ collectTax: v })} accent={accent} />
        </div>

        {/* Conditional reveal — only while the toggle is on */}
        {data.collectTax && (
          <div style={{ marginTop: 24, paddingTop: 24, borderTop: "1px solid #f1f1f3", display: "flex", flexDirection: "column", gap: 16 }}>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
              <StateSearchSelect label="State" value={data.taxState}
                onChange={(v) => set({ taxState: v, taxRate: STATE_TAX_RATES[v] != null ? String(STATE_TAX_RATES[v]) : data.taxRate })} />
              <TextField label="Tax Rate %" value={data.taxState ? data.taxRate : ""} readOnly
                placeholder="Select a state"
                suffix={<span style={{ color: "#9a9aa1", fontSize: 13 }}>%</span>}
                error={errors.taxRate} />
            </div>
            <div style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.5, marginTop: -4 }}>
              The tax rate is set automatically from the selected state. Tax is output exclusively to buyers in the selected state.
            </div>
            <div style={{
              background: errors.taxLiability ? "#fdecea" : "#fafafb",
              border: `1px solid ${errors.taxLiability ? "#f3c9c5" : "#ececf0"}`, borderRadius: 8, padding: "16px 16px",
            }}>
              <Checkbox checked={data.taxLiabilityAgreed} onChange={(v) => set({ taxLiabilityAgreed: v })}
                label="I acknowledge I am the taxing entity responsible for collecting and remitting sales tax, and that PROLOOK provides the collection tools only." />
              {errors.taxLiability && <div style={{ color: "#b81d14", fontSize: 12, marginTop: 8, marginLeft: 28 }}>{errors.taxLiability}</div>}
            </div>
          </div>
        )}
      </WizardPanel>

      <TeamDatabaseModal open={teamModalOpen} onClose={() => setTeamModalOpen(false)} onSelect={applyTeam} accent={accent} hasTeam={!!data.connectedTeam} onClearTeam={clearTeam} />
      <Toast message={teamToast} onDone={() => setTeamToast("")} />

      {/* League → Individual — confirm before discarding Divisions & Teams data */}
      <Modal open={!!typeSwitch} onClose={() => setTypeSwitch(null)} width={470}>
        <div style={{ padding: "28px 28px 24px" }}>
          <div style={{ width: 44, height: 44, borderRadius: "50%", background: "#fff3f2", color: "#b81d14", display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}>
            <IconAlert size={20} />
          </div>
          <h3 style={{ fontSize: 18, fontWeight: 800, margin: "0 0 8px", color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: ".005em" }}>
            Switching removes your divisions &amp; teams
          </h3>
          <p style={{ fontSize: 13.5, color: "#6b6b72", margin: "0 0 20px", lineHeight: 1.6 }}>
            An Individual Team Store has no Divisions &amp; Teams step. Switching discards the {structCounts.divisions} {structCounts.divisions === 1 ? "division" : "divisions"} and {structCounts.teams} {structCounts.teams === 1 ? "team" : "teams"} you set up — this can't be undone.
          </p>
          <div style={{ display: "flex", justifyContent: "flex-end", gap: 12 }}>
            <Button variant="ghost" onClick={() => setTypeSwitch(null)}>Keep League Store</Button>
            <Button variant="danger" onClick={confirmTypeSwitch}>Switch &amp; Discard</Button>
          </div>
        </div>
      </Modal>

      {/* Template-break confirmation — fires when a template-defining option is changed while still linked */}
      {breakModal && (() => {
        const readableVal = (label, val) =>
          label === "Store Type" ? tplTypeLabel(val)
          : label === "Store Schedule" ? (val === "locker" ? "Always-On Locker" : "Temporary Pop-Up")
          : label === "Fulfillment Method" ? (val === "home" ? "Direct-To-Home" : "Ship to Team Manager")
          : String(val);
        return (
          <Modal open={!!breakModal} onClose={() => setBreakModal(null)} width={460}>
            <div style={{ padding: "28px 28px 24px" }}>
              <div style={{ width: 44, height: 44, borderRadius: "50%", background: "#fff3f2", color: "#b81d14", display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}>
                <IconAlert size={20} />
              </div>
              <h3 style={{ fontSize: 18, fontWeight: 800, margin: "0 0 8px", color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: ".005em" }}>
                Change breaks the template
              </h3>
              <p style={{ fontSize: 13.5, color: "#6b6b72", margin: "0 0 16px", lineHeight: 1.6 }}>
                <strong style={{ color: "#0e0e10" }}>{breakModal.label}</strong> was pre-selected by <strong style={{ color: "#0e0e10" }}>{data._templateName || "the saved template"}</strong>. Changing it unlinks this store from the template. Everything else the template pre-loaded stays exactly as it is.
              </p>

              <div style={{ display: "flex", alignItems: "center", gap: 12, background: "#fafafb", border: "1px solid #ececf0", borderRadius: 9, padding: "12px 16px", marginBottom: 20 }}>
                <span style={{ fontSize: 12.5, fontWeight: 700, color: "#9a9aa1", textDecoration: "line-through" }}>{readableVal(breakModal.label, breakModal.current)}</span>
                <IconChevronRight size={15} style={{ color: "#c7c7cd", flexShrink: 0 }} />
                <span style={{ fontSize: 12.5, fontWeight: 800, color: "#0e0e10" }}>{readableVal(breakModal.label, breakModal.next)}</span>
              </div>

              <div style={{ display: "flex", justifyContent: "flex-end", gap: 12 }}>
                <Button variant="ghost" onClick={() => setBreakModal(null)}>Keep Template</Button>
                <Button variant="danger" onClick={confirmBreak}>Change &amp; Unlink</Button>
              </div>
            </div>
          </Modal>
        );
      })()}
    </div>
  );
}

// ───────────────────────── STEP 2 (League) — Divisions & Teams ─────────────────────────
// Reuses the workspace My Team(s) editor (TeamCanvasView) in wizard context:
// no branding, no team codes, no rosters — structure only, and always skippable.
function Step2Structure({ data, setStructure, accent, stepNum }) {
  const s = data.leagueStructure || { groups: [], loose: [] };
  const org = { groups: s.groups || [], loose: s.loose || [], managers: {}, modes: {}, bank: {} };
  const setOrg = (fn) => setStructure((cur) => {
    const prev = { groups: cur.groups || [], loose: cur.loose || [], managers: {}, modes: {}, bank: {} };
    const next = typeof fn === "function" ? fn(prev) : fn;
    return { groups: next.groups || [], loose: next.loose || [] };
  });
  const totalTeams = org.groups.reduce((n, g) => n + (g.rosters || []).length, 0) + org.loose.length;
  const [toast, setToast] = React.useState("");
  return (
    <div>
      <StepHeader num={String(stepNum || 2)} accent={accent} title="Divisions &amp; Teams"
        desc="Name your league's divisions and teams before adding products. This whole step is optional — skip it and build the structure anytime from the My Team(s) tab. Rosters aren't entered here; they fill in later or from orders." />
      <TeamCanvasView wizard
        groups={org.groups} loose={org.loose} org={org} setOrg={setOrg}
        store={{ storeType: "league", storeTitle: data.storeTitle }}
        managerGated={false}
        stats={{ totalGroups: org.groups.length, totalRosters: totalTeams, totalPlayers: 0 }}
        onOpenRoster={() => {}}
        setToast={setToast} />
      <Toast message={toast} onDone={() => setToast("")} />
    </div>
  );
}

// ───────────────────────── STEP 3 — Products (curation) ─────────────────────────

const WIZARD_CATALOG = [
  { id: "jersey-home", title: "Custom Jersey — Home", style: "Set-in Faux, Full-button Jersey", photo: "jersey-padres", adult: "65.41", youth: "58.13", teamProfit: 17.50 },
  { id: "jersey-away", title: "Custom Jersey — Away", style: "Set-in Faux, Full-button Jersey", photo: "jersey-warriors", adult: "65.41", youth: "58.13", teamProfit: 17.50 },
  { id: "hoodie", title: "Performance Hoodie", style: "Heavyweight Fleece Pullover", photo: "jersey-grizzlies", adult: "48.00", youth: "42.00", teamProfit: 12.00 },
  { id: "pants", title: "Team Pants", style: "Pro-cut Knit Bottom", photo: "pants-navy", adult: "39.50", youth: "34.00", teamProfit: 9.00 },
  { id: "shorts", title: "Team Shorts", style: "Performance Mesh Short", photo: "shorts-green", adult: "28.00", youth: "24.00", teamProfit: 7.50 },
  { id: "jacket", title: "Warm-Up Jacket", style: "Full-Zip Travel Jacket", photo: "jersey-lancers", adult: "72.00", youth: "64.00", teamProfit: 19.00 },
];

function CatalogPhoto({ photo, alt }) {
  return (
    <img src={`assets/products/${photo}.png`} alt={alt}
      style={{ width: "100%", height: "100%", objectFit: "contain", display: "block" }} />
  );
}

// Reps pull products from pre-existing saved work (Saved Designs + Saved Carts),
// mirroring the dashboard's "Add Saved Design" / "Add From Cart" injection paths.
const WIZARD_SAVED_DESIGNS = [
  { id: "sd1", title: "HOYAS WHITE", sport: "Basketball", category: "Uniform Jersey", block: "SFN Lite, V-Neck Jersey", photo: "jersey-grizzlies", adult: "65.41", youth: "58.13", teamProfit: 17.5 },
  { id: "sd2", title: "HOYAS DARK", sport: "Basketball", category: "Uniform Jersey", block: "2-Ply Reversible Jersey", photo: "jersey-warriors", adult: "65.41", youth: "58.13", teamProfit: 17.5 },
  { id: "sd3", title: "FALCONS HOME", sport: "Baseball", category: "Uniform Jersey", block: "Sublimated Traditional, Full-Button Jersey", photo: "jersey-padres", adult: "62.00", youth: "55.00", teamProfit: 16.0 },
  { id: "sd4", title: "ASTEROID KIT", sport: "Football", category: "Uniform Jersey", block: "Sublimated, Set-in Sleeve Jersey", photo: "jersey-asteroid", adult: "70.00", youth: "62.00", teamProfit: 18.0 },
  { id: "sd5", title: "LANCERS WARM-UP", sport: "Soccer", category: "Warm-Up Jacket", block: "Full-Zip, Tricot Warm-Up Jacket", photo: "jersey-lancers", adult: "72.00", youth: "64.00", teamProfit: 19.0 },
  { id: "sd6", title: "TEAM PANTS NAVY", sport: "Baseball", category: "Team Pants", block: "Sublimated Traditional, Knicker Length, Elastic Cuff Pant", photo: "pants-navy", adult: "39.50", youth: "34.00", teamProfit: 9.0 },
];

const WIZARD_SAVED_CARTS = [
  { id: "sc1", name: "Hoyas — Jersey + Hat Bundle", total: 624.0, items: [
    { id: "sc1-0", title: "Home Jersey", photo: "jersey-grizzlies", adult: "65.41", youth: "58.13", teamProfit: 17.5 },
    { id: "sc1-1", title: "Away Jersey", photo: "jersey-warriors", adult: "65.41", youth: "58.13", teamProfit: 17.5 },
    { id: "sc1-2", title: "Team Shorts", photo: "shorts-green", adult: "28.00", youth: "24.00", teamProfit: 7.5 },
  ] },
  { id: "sc2", name: "Spring Practice Tees", total: 312.5, items: [
    { id: "sc2-0", title: "Practice Tee", photo: "jersey-padres", adult: "24.00", youth: "20.00", teamProfit: 6.0 },
    { id: "sc2-1", title: "Mesh Short", photo: "shorts-navy", adult: "26.00", youth: "22.00", teamProfit: 6.5 },
  ] },
  { id: "sc3", name: "Full Team Travel Kit", total: 980.0, items: [
    { id: "sc3-0", title: "Game Jersey", photo: "jersey-asteroid", adult: "70.00", youth: "62.00", teamProfit: 18.0 },
    { id: "sc3-1", title: "Team Pants", photo: "pants-navy", adult: "39.50", youth: "34.00", teamProfit: 9.0 },
    { id: "sc3-2", title: "Warm-Up Jacket", photo: "jersey-lancers", adult: "72.00", youth: "64.00", teamProfit: 19.0 },
    { id: "sc3-3", title: "Team Shorts", photo: "shorts-green", adult: "28.00", youth: "24.00", teamProfit: 7.5 },
  ] },
];

// Overlapping thumbnail stack for a Saved Cart card (multiple products inside).
function StackedThumbs({ items }) {
  const shown = items.slice(0, 3);
  const extra = items.length - shown.length;
  return (
    <div style={{ position: "relative", width: "100%", height: "100%", display: "flex", alignItems: "center", justifyContent: "center" }}>
      {shown.map((it, i) => (
        <div key={it.id} style={{
          position: "absolute", width: 92, height: 116,
          left: `calc(50% - 48px + ${(i - (shown.length - 1) / 2) * 42}px)`,
          top: "50%", transform: `translateY(-50%) rotate(${(i - (shown.length - 1) / 2) * 5}deg)`,
          background: "#fff", border: "1px solid #e5e5e9", borderRadius: 7, padding: 8,
          boxShadow: "0 4px 12px rgba(20,20,30,.10)", zIndex: i,
        }}>
          <CatalogPhoto photo={it.photo} alt={it.title} />
        </div>
      ))}
      {extra > 0 && (
        <span style={{
          position: "absolute", right: 16, bottom: 12, zIndex: 9,
          background: "#0e0e10", color: "#fff", borderRadius: 999, padding: "4px 8px",
          fontSize: 11, fontWeight: 800, letterSpacing: ".02em",
        }}>+{extra}</span>
      )}
    </div>
  );
}

// ── Add Products source browser (4C) — one searchable grid across every source ──
// Source is a filter chip, not a tab: every card carries its source badge, so a rep
// who knows the product name never has to guess which section holds it.
function AddProductsBrowser({ groups, accent, right }) {
  const [q, setQ] = React.useState("");
  const [src, setSrc] = React.useState("all");
  const [sub, setSub] = React.useState(null);
  const list = (groups || []).filter(Boolean);
  const total = list.reduce((n, g) => n + g.items.length, 0);
  const needle = q.trim().toLowerCase();
  const match = (it) => !needle || ((it.title || "") + " " + (it.subtitle || "") + " " + (it.badge || "")).toLowerCase().indexOf(needle) >= 0;
  const rows = [];
  list.forEach((g) => { if (src === "all" || src === g.key) g.items.forEach((it) => { if (match(it) && (src === "all" || !sub || it.sub === sub)) rows.push(it); }); });
  const chip = (key, label, count) => {
    const on = src === key;
    return (
      <button key={key} type="button" onClick={() => { setSrc(key); setSub(null); }} style={{
        display: "inline-flex", alignItems: "center", gap: 7, height: 32, padding: "0 14px",
        borderRadius: 999, cursor: "pointer", fontSize: 12.5, fontWeight: 700, letterSpacing: ".01em",
        background: on ? "#0e0e10" : "#fff", color: on ? "#fff" : "#27272a",
        border: "1px solid " + (on ? "#0e0e10" : "#d4d4d8"), transition: "all .14s",
      }}>
        {label}<span style={{ fontSize: 11.5, fontWeight: 700, color: on ? "rgba(255,255,255,.7)" : "#9a9aa1" }}>{count}</span>
      </button>
    );
  };
  return (
    <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 12, padding: "20px 20px 24px", marginBottom: 24 }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}>
        <div style={{ position: "relative", flex: 1, minWidth: 0 }}>
          <span style={{ position: "absolute", left: 13, top: "50%", transform: "translateY(-50%)", color: "#9a9aa1", display: "inline-flex" }}><IconSearch size={15} /></span>
          <input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search all sources — designs, carts, vault blanks, templates, other stores" style={{
            width: "100%", height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 8,
            padding: "0 12px 0 36px", fontSize: 13, color: "#0e0e10", outline: "none", background: "#fff",
          }} />
        </div>
        {right}
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap", paddingBottom: 16, borderBottom: "1px solid #ececf0", marginBottom: 20 }}>
        {chip("all", "All sources", total)}
        {list.map((g) => chip(g.key, g.label, g.items.length))}
        <span style={{ marginLeft: "auto", fontSize: 12, color: "#9a9aa1" }}>{rows.length === total ? total + " item" + (total === 1 ? "" : "s") : rows.length + " of " + total + " shown"}</span>
      </div>
      {src !== "all" && (() => {
        const g = list.find((x) => x.key === src);
        if (!g || !g.subs || g.subs.length === 0) return null;
        return (
          <div style={{ display: "inline-flex", background: "#f1f1f3", borderRadius: 9, padding: 3, gap: 2, marginBottom: 18 }}>
            {[{ k: null, l: "All" }].concat(g.subs).map((t) => {
              const on = sub === t.k;
              return (
                <button key={String(t.k)} type="button" onClick={() => setSub(t.k)} style={{ border: 0, cursor: "pointer", fontFamily: "inherit", borderRadius: 6, padding: "8px 16px", fontSize: 12.5, fontWeight: 700, background: on ? "#fff" : "transparent", color: on ? "#09090b" : "#71717a", boxShadow: on ? "0 1px 3px rgba(9,9,11,.12)" : "none", transition: "background .14s, color .14s" }}>{t.l}</button>
              );
            })}
          </div>
        );
      })()}
      {rows.length === 0 ? (
        <div style={{ padding: "44px 0", textAlign: "center", color: "#9a9aa1", fontSize: 13 }}>
          {needle ? "Nothing matches \u201c" + q.trim() + "\u201d in this source." : (src !== "all" && (list.find((x) => x.key === src) || {}).emptyHint) || (total === 0 ? "No products available yet." : "No products in this source yet.")}
        </div>
      ) : (
        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(240px, 1fr))", gap: 16 }}>
          {rows.map((it) => <SavedAssetCard key={it._key} accent={accent} {...it} />)}
        </div>
      )}
    </div>
  );
}

// Unified saved-asset card — identical dimensions for designs and carts.
function SavedAssetCard({ kind, title, subtitle, photo, letter, badge, disabledReason, items, committed, onAdd, onRemove, onEdit, accent, assignEnabled, assignVariant, assigned, onAssign, committedSetting, menu, onRenameCommit }) {
  const [hover, setHover] = useStateW(false);
  const [menuOpen, setMenuOpen] = React.useState(false);
  const [renaming, setRenaming] = React.useState(null);
  return (
    <div
      onMouseEnter={() => setHover(true)}
      onMouseLeave={() => setHover(false)}
      style={{
        border: `1px solid ${committed ? "#1f9d55" : hover ? "#c7c7cd" : "#ececf0"}`, borderRadius: 10,
        overflow: "visible", background: "#fff", display: "flex", flexDirection: "column",
        transition: "border-color .14s, box-shadow .14s",
        boxShadow: hover ? "0 6px 18px rgba(20,20,30,.08)" : "none",
      }}>
      <div style={{ position: "relative", background: "#f1f2f4", height: 168, padding: 16, display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", borderTopLeftRadius: 10, borderTopRightRadius: 10 }}>
        {kind === "cart"
          ? <StackedThumbs items={items} />
          : photo ? <div style={{ width: 100, height: 132 }}><CatalogPhoto photo={photo} alt={title} /></div>
          : <div style={{ width: 76, height: 96, borderRadius: 8, background: "#e7e7ec", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 22, fontWeight: 800, color: "#b3b3bb" }}>{letter || (title || "P").slice(0, 1)}</div>}
        {kind === "cart" && (
          <span style={{
            position: "absolute", top: 12, left: 12, background: "#fff", border: "1px solid #e5e5e9",
            borderRadius: 999, padding: "4px 12px", fontSize: 11, fontWeight: 800, color: "#0e0e10",
            display: "inline-flex", alignItems: "center", gap: 8,
          }}><IconLayers size={12} /> {items.length} items</span>
        )}
        {committed && (
          <span style={{
            position: "absolute", top: 12, right: 12, background: "#1a5c3a", color: "#fff",
            borderRadius: 999, padding: "4px 12px", fontSize: 12, fontWeight: 700,
            display: "inline-flex", alignItems: "center", gap: 8,
          }}><IconCheck size={13} style={{ strokeWidth: 2.8 }} /> In Store</span>
        )}
      </div>
      <div style={{ padding: "16px 16px 16px", display: "flex", flexDirection: "column", flex: 1 }}>
        <div style={{ display: "flex", alignItems: "flex-start", gap: 8 }}>
          {renaming != null ? (
            <input autoFocus value={renaming} onChange={(e) => setRenaming(e.target.value)}
              onBlur={() => { const v = (renaming || "").trim(); setRenaming(null); if (v && v !== title && onRenameCommit) onRenameCommit(v); }}
              onKeyDown={(e) => { if (e.key === "Enter") e.currentTarget.blur(); if (e.key === "Escape") setRenaming(null); }}
              style={{ flex: 1, minWidth: 0, boxSizing: "border-box", fontSize: 14, fontWeight: 800, color: "#0e0e10", fontFamily: "inherit", border: "1px solid #0e0e10", borderRadius: 6, padding: "2px 6px", outline: "none", marginBottom: 4 }} />
          ) : (
            <div style={{ flex: 1, minWidth: 0, fontSize: 14, fontWeight: 800, color: "#0e0e10", marginBottom: 4, letterSpacing: ".01em" }}>{title}</div>
          )}
          {menu && menu.length > 0 && (
            <span style={{ position: "relative", flexShrink: 0 }}>
              <button type="button" onClick={() => setMenuOpen((v) => !v)} aria-label="Template actions" style={{ width: 26, height: 26, border: 0, borderRadius: 6, background: menuOpen ? "#f4f4f5" : "none", cursor: "pointer", color: "#71717a", fontSize: 15, fontFamily: "inherit", lineHeight: 1 }}>⋯</button>
              {menuOpen && <span onClick={() => setMenuOpen(false)} style={{ position: "fixed", inset: 0, zIndex: 40 }} />}
              {menuOpen && (
                <div style={{ position: "absolute", right: 0, top: "calc(100% + 4px)", zIndex: 50, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 8, boxShadow: "0 8px 24px rgba(20,20,30,.14)", padding: 6, minWidth: 180, textAlign: "left" }}>
                  {menu.map((mi, i) => (
                    <button key={i} type="button" onClick={() => { setMenuOpen(false); if (mi.rename) setRenaming(title || ""); else mi.act && mi.act(); }} style={{ display: "block", width: "100%", textAlign: "left", background: "none", border: 0, borderRadius: 6, padding: "8px 10px", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: mi.danger ? "#b81d14" : "#27272a", cursor: "pointer" }}>{mi.label}</button>
                  ))}
                </div>
              )}
            </span>
          )}
        </div>
        <div style={{ fontSize: 12, color: "#9a9aa1", marginBottom: 16 }}>{subtitle}</div>
        {assignEnabled && !committed && (
          <RosterAssignField variant={assignVariant} direction="down" value={assigned || []} onChange={onAssign} accent={accent} />
        )}
        <div style={{ marginTop: "auto" }}>
        {committed && assignEnabled && (() => {
          const ids = assigned || [];
          const allTeams = assignAllRosterIds();
          const isLeagueWide = ids.length === 0 || ids.length >= allTeams.length;
          const names = ids.map((id) => (assignRosterLabel(id) || {}).name).filter(Boolean);
          return (
            <div style={{ border: "1px solid #ececf0", borderRadius: 8, background: "#fafafb", padding: "12px 12px", marginBottom: 12 }}>
              <div style={{ display: "flex", alignItems: "flex-start", gap: 8 }}>
                  <IconUsers size={13} style={{ flexShrink: 0, marginTop: 2, color: "#9a9aa1" }} />
                  <div style={{ minWidth: 0 }}>
                    <div style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 4 }}>Assigned to</div>
                    {isLeagueWide ? (
                      <span style={{ fontSize: 12, fontWeight: 700, color: "#0e0e10" }}>Entire league · all teams</span>
                    ) : (
                      <div style={{ display: "flex", flexWrap: "wrap", gap: 4 }}>
                        {names.slice(0, 3).map((n, i) => (
                          <span key={i} style={{ fontSize: 11, fontWeight: 700, color: "#0e0e10", background: "#fff", border: "1px solid #e5e5e9", borderRadius: 999, padding: "2px 8px" }}>{n}</span>
                        ))}
                        {names.length > 3 && <span style={{ fontSize: 11, fontWeight: 800, color: "#6b6b72", alignSelf: "center" }}>+{names.length - 3}</span>}
                      </div>
                    )}
                  </div>
                </div>
            </div>
          );
        })()}
        {committed ? (
          <button type="button" onClick={onEdit} style={{
            width: "100%", padding: "12px", borderRadius: 6, cursor: "pointer", marginTop: assignEnabled ? 12 : 0,
            fontSize: 13, fontWeight: 700, letterSpacing: ".01em",
            background: "#fff", color: "#0e0e10", border: "1px solid #d4d4d8",
            display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8,
            transition: "border-color .14s, background .14s",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.borderColor = "#0e0e10"; e.currentTarget.style.background = "#f7f7f8"; }}
            onMouseLeave={(e) => { e.currentTarget.style.borderColor = "#d4d4d8"; e.currentTarget.style.background = "#fff"; }}>
            <IconEdit size={15} style={{ strokeWidth: 2.2 }} /> Edit Product
          </button>
        ) : disabledReason ? (
          <button type="button" disabled title={disabledReason} style={{ width: "100%", padding: "12px", borderRadius: 6, marginTop: assignEnabled ? 12 : 0, fontSize: 13, fontWeight: 700, border: "1px solid #ececf0", background: "#f7f7f8", color: "#a1a1aa", cursor: "not-allowed" }}>{disabledReason}</button>
        ) : (
          <button type="button" onClick={onAdd} style={{
            width: "100%", padding: "12px", borderRadius: 6, cursor: "pointer", marginTop: assignEnabled ? 12 : 0,
            fontSize: 13, fontWeight: 800, letterSpacing: ".02em", border: 0,
            background: "#0e0e10", color: "#fff",
            display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8,
            transition: "background .14s",
          }}
            onMouseEnter={(e) => e.currentTarget.style.background = accent}
            onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
            <IconPlus size={15} style={{ strokeWidth: 2.6 }} /> Add to Store
          </button>
        )}
        </div>
      </div>
    </div>
  );
}

function EditProductModal({ open, target, accent, isLeague, assigned, store, onClose, onSave, onRemove }) {
  const [teamRequired, setTeamRequired] = React.useState(false);
  const [teamSupplied, setTeamSupplied] = React.useState(false);
  const [rosters, setRosters] = React.useState([]);
  const [teamPrompt, setTeamPrompt] = React.useState(null); // { apply } | null — league TR/TS requires ≥1 team
  const [pricing, setPricing] = React.useState(() => defaultPricing());

  React.useEffect(() => {
    if (!open || !target) return;
    const c = target.curated || {};
    setTeamRequired(c.teamSetting === "team-required");
    setTeamSupplied(c.teamSetting === "team-supplied");
    setRosters(c.rosters || assigned || []);
    if (c.pricing) {
      const dp = defaultPricing();
      setPricing({ youth: { ...dp.youth, ...c.pricing.youth }, adult: { ...dp.adult, ...c.pricing.adult } });
    } else {
      const dp = defaultPricing();
      if (c.youth) { dp.youth.base = (parseFloat(c.youth) || 43.13).toFixed(2); dp.youth.retail = dp.youth.base; }
      if (c.adult) { dp.adult.base = (parseFloat(c.adult) || 50.41).toFixed(2); dp.adult.retail = dp.adult.base; }
      setPricing(dp);
    }
  }, [open, target ? target.id : null]);

  if (!open || !target) return null;

  const handleSave = () => {
    // League stores: Team Required / Team Supplied must be assigned to ≥1 team or division.
    if (isLeague && (teamRequired || teamSupplied) && rosters.length === 0) { setTeamPrompt({ apply: null }); return; }
    onSave({ teamSetting: teamRequired ? "team-required" : teamSupplied ? "team-supplied" : "fan-gear", pricing, rosters, youth: pricing.youth.retail, adult: pricing.adult.retail });
  };

  const APP_ROWS = [
    { type: "FRONT NUMBER", detail: "Font: 45", colors: ["#0e0e10", "#e1251b"] },
    { type: "BACK NUMBER",  detail: "Font: 45", colors: ["#0e0e10", "#e1251b"] },
    { type: "TEAM NAME",    detail: "Font: Arch", colors: ["#ffffff"] },
  ];

  const InfoDot = () => <span style={{ width: 16, height: 16, borderRadius: "50%", background: "#e5e5e9", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 9, color: "#6b6b72", fontWeight: 800, cursor: "default", flexShrink: 0 }}>i</span>;

  const Toggle = ({ on, onChange, label }) => (
    <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}>
      <div onClick={() => onChange(!on)} style={{ width: 44, height: 28, borderRadius: 13, cursor: "pointer", flexShrink: 0, background: on ? "#0e0e10" : "#d4d4d8", position: "relative", transition: "background .18s" }}>
        <div style={{ position: "absolute", top: 4, left: on ? 21 : 3, width: 20, height: 20, borderRadius: "50%", background: "#fff", transition: "left .18s", boxShadow: "0 1px 3px rgba(0,0,0,.25)" }} />
      </div>
      <span style={{ fontSize: 14, color: "#0e0e10" }}>{label}</span>
      <InfoDot />
    </div>
  );

  const UnitBtn = ({ val, active, onClick }) => <button type="button" onClick={onClick} style={{ padding: "0 12px", height: 36, border: 0, cursor: "pointer", fontSize: 12, fontWeight: 700, background: active ? "#0e0e10" : "#fff", color: active ? "#fff" : "#0e0e10", transition: "background .1s" }}>{val}</button>;
  const UnitToggle = ({ val, onChange }) => (
    <div style={{ display: "inline-flex", border: "1px solid #0e0e10", borderRadius: 4, overflow: "hidden", flexShrink: 0 }}>
      <UnitBtn val="$" active={val === "$"} onClick={() => onChange("$")} />
      <UnitBtn val="%" active={val === "%"} onClick={() => onChange("%")} />
    </div>
  );

  return (
    <Modal open={open} onClose={onClose} width={920}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "90vh" }}>
        {/* Header */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "20px 28px 16px", borderBottom: "1px solid #ececf0", flexShrink: 0 }}>
          <h3 style={{ fontSize: 18, fontWeight: 800, margin: 0, letterSpacing: ".01em", color: "#0e0e10" }}>Edit Product</h3>
          <button onClick={onClose} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72", display: "flex", alignItems: "center" }}><IconX size={20} /></button>
        </div>

        {/* Body */}
        <div style={{ display: "flex", flex: 1, minHeight: 0, overflow: "hidden" }}>
          {/* Left sidebar */}
          <div style={{ width: 260, flexShrink: 0, borderRight: "1px solid #ececf0", padding: 20, display: "flex", flexDirection: "column", gap: 12 }}>
            <div style={{ background: "#f1f2f4", borderRadius: 10, height: 192, display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 16 }}>
              {target.photo
                ? <img src={"assets/products/" + target.photo + ".png"} alt={target.title} style={{ width: "100%", height: "100%", objectFit: "contain" }} />
                : <div style={{ color: "#c7c7cd", fontSize: 12 }}>No preview</div>}
            </div>
            <div style={{ fontSize: 14, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>{target.title}</div>
            {target.sport && <div style={{ fontSize: 12, color: "#9a9aa1" }}>{target.sport} · {target.category}</div>}
            <button type="button" style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, color: "#0e0e10", marginTop: 2 }}>
              <IconEdit size={14} style={{ strokeWidth: 2.2 }} /> Customize
            </button>
          </div>

          {/* Right scrollable content */}
          <div style={{ flex: 1, overflow: "auto", padding: "20px 28px", display: "flex", flexDirection: "column", gap: 16 }}>
            {/* Product Settings */}
            <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px" }}>
              <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", marginBottom: 16 }}>Product Settings</div>
              <Toggle on={teamRequired} onChange={(v) => { if (v && isLeague && rosters.length === 0) { setTeamPrompt({ apply: () => setTeamRequired(true) }); return; } setTeamRequired(v); }} label="Team Required" />
              <Toggle on={teamSupplied} onChange={(v) => { if (v && isLeague && rosters.length === 0) { setTeamPrompt({ apply: () => setTeamSupplied(true) }); return; } setTeamSupplied(v); }} label="Team Supplied" />
            </div>

            {/* Assign to Teams — league stores only */}
            {isLeague && (
              <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px" }}>
                <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 8, color: "#0e0e10" }}>Assign to Teams</div>
                <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>
                  {(teamRequired || teamSupplied)
                    ? <span>Team Required and Team Supplied items must be assigned to at least one team or division.</span>
                    : <span>Choose which teams wear this product. Leave empty to make it a <strong style={{ color: "#0e0e10", fontWeight: 700 }}>league-wide item</strong> — available to every shopper on the storefront when published.</span>}
                </div>
                <RosterAssignField variant="dropdown" direction="down" value={rosters} onChange={setRosters} accent={accent} minOne={teamRequired || teamSupplied} store={store} />
              </div>
            )}

            {/* Price settings — shared Governing-rule-3 card ($ / % mode) */}
            <PriceSettingsCard pricing={pricing} onChange={setPricing} fundOn={store?.fundraisingEnabled === true} covered={teamSupplied} />

            {/* Confirm Application Sizes */}
            <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px" }}>
              <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", marginBottom: 16 }}>Confirm Application Sizes</div>
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr 1fr", gap: 8, paddingBottom: 8 }}>
                {["TYPE", "DETAILS", "COLORS", "SELECT SIZES", "NOTES"].map((h) => (
                  <div key={h} style={{ fontSize: 10, fontWeight: 700, color: "#9a9aa1", letterSpacing: ".06em", textTransform: "uppercase" }}>{h}</div>
                ))}
              </div>
              {APP_ROWS.map((row, ri) => (
                <div key={ri} style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr 1fr 1fr", gap: 8, borderTop: "1px solid #f1f1f3", padding: "12px 0", alignItems: "center" }}>
                  <div style={{ fontSize: 12, fontWeight: 700, color: "#0e0e10" }}>{row.type}</div>
                  <div style={{ fontSize: 12, color: "#6b6b72" }}>{row.detail}</div>
                  <div style={{ display: "flex", gap: 4 }}>
                    {row.colors.map((c, ci) => <span key={ci} style={{ width: 16, height: 16, borderRadius: "50%", background: c, border: c === "#ffffff" ? "1px solid #d4d4d8" : "none", display: "inline-block" }} />)}
                  </div>
                  <select style={{ fontSize: 12, padding: "4px 8px", border: "1px solid #d4d4d8", borderRadius: 4, background: "#fff", cursor: "pointer" }}>
                    <option>As Shown</option><option>XS – XL</option><option>S – 2XL</option>
                  </select>
                  <input placeholder="Optional" style={{ fontSize: 12, padding: "4px 8px", border: "1px solid #d4d4d8", borderRadius: 4, outline: "none", width: "100%" }} />
                </div>
              ))}
            </div>
          </div>
        </div>

        {/* Footer */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "16px 28px", borderTop: "1px solid #ececf0", flexShrink: 0, background: "#fff" }}>
          <button type="button" onClick={onRemove} style={{ background: "#fff", color: "#b81d14", border: "1px solid #e6b3af", borderRadius: 6, padding: "12px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, transition: "background .14s" }}
            onMouseEnter={(e) => e.currentTarget.style.background = "#fff3f2"}
            onMouseLeave={(e) => e.currentTarget.style.background = "#fff"}>
            <IconTrash size={15} style={{ strokeWidth: 2.2 }} /> Remove From Store
          </button>
          <div style={{ display: "flex", gap: 12 }}>
            <button type="button" onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, color: "#0e0e10", cursor: "pointer" }}>Cancel</button>
            <button type="button" onClick={handleSave} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 800, cursor: "pointer" }}>Save Changes</button>
          </div>
        </div>
      </div>
      <TeamAssignPrompt
        open={!!teamPrompt}
        initial={rosters}
        store={store}
        settingLabel={teamSupplied && !teamRequired ? "Team Supplied" : "Team Required"}
        onClose={() => setTeamPrompt(null)}
        onConfirm={(ids) => { setRosters(ids); const a = teamPrompt && teamPrompt.apply; setTeamPrompt(null); if (a) a(); }} />
    </Modal>
  );
}

// ─────────── Product-to-Roster Assignment (League stores) ───────────
// A League store is a single storefront, so products must be anchored to the
// specific teams that wear them. This field surfaces the nested roster
// architecture (groups → teams) as a multi-select: check a whole group to
// auto-select its teams, or check individual teams. Two UI treatments are
// available (see `variant`) so the flow can be compared in-context.

// Flatten selection helpers shared by both variants.
function assignAllRosterIds() {
  const ids = [];
  ROSTER_GROUP_SEED.forEach((g) => g.rosters.forEach((r) => ids.push(r.id)));
  STANDALONE_ROSTER_SEED.forEach((r) => ids.push(r.id));
  return ids;
}

// Teams offered for assignment are scoped to the store: a store with no team
// structure yet (unstructured profile, e.g. Bayshore) exposes ONLY teams created
// for it — never another store's seed tree. Returns null → use the global tree.
function storeScopedTeams(store) {
  if (!store || store.rosterProfile !== "unstructured") return null;
  window.__storeTeams = window.__storeTeams || {};
  return window.__storeTeams[store.id] || [];
}
function ScopedTeamRows({ teams, value = [], onChange, minOne = false }) {
  if (!teams.length) return <div style={{ padding: "22px 16px", fontSize: 12.5, color: "#9a9aa1", textAlign: "center" }}>No Teams in this store yet.</div>;
  return (
    <div>
      {teams.map((t) => {
        const on = value.includes(t.id);
        const lockLast = minOne && on && value.length === 1;
        return (
          <button key={t.id} type="button" disabled={lockLast} title={lockLast ? "Team Required / Team Supplied items must stay assigned to at least one Team" : undefined}
            onClick={() => onChange(on ? value.filter((x) => x !== t.id) : [...value, t.id])}
            style={{ display: "flex", alignItems: "center", gap: 12, width: "100%", textAlign: "left", background: on ? "#fafafb" : "#fff", border: 0, borderBottom: "1px solid #f3f3f4", padding: "12px 16px", cursor: lockLast ? "default" : "pointer", fontFamily: "inherit" }}>
            <span style={{ width: 18, height: 18, borderRadius: 5, flexShrink: 0, border: on ? 0 : "1.5px solid #c7c7cd", background: on ? "#0e0e10" : "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#fff", fontSize: 11, fontWeight: 800 }}>{on ? "✓" : ""}</span>
            <span style={{ flex: 1, fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{t.name}</span>
            <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{t.code}</span>
          </button>
        );
      })}
    </div>
  );
}
function assignRosterLabel(id) {
  for (const g of ROSTER_GROUP_SEED) {
    const r = g.rosters.find((x) => x.id === id);
    if (r) return { name: r.name, group: g.name, color: g.color };
  }
  const l = STANDALONE_ROSTER_SEED.find((x) => x.id === id);
  if (l) return { name: l.name, group: "Standalone", color: "#6b6b72" };
  return null;
}

// The shared nested checkbox tree used by every variant's popover.
function RosterTreePanel({ value, onChange, accent, minOne = false }) {
  const sel = new Set(value || []);
  const toggle = (id) => {
    if (minOne && sel.has(id) && sel.size === 1) return; // TR/TS items keep ≥1 Team
    const n = new Set(sel); n.has(id) ? n.delete(id) : n.add(id); onChange([...n]);
  };
  const Box = ({ on, mixed, color }) => (
    <span style={{
      width: 16, height: 16, borderRadius: 5, flexShrink: 0, border: `1.6px solid ${on || mixed ? (color || accent) : "#c7c7cd"}`,
      background: on ? (color || accent) : mixed ? hexA(color || accent, 0.16) : "#fff",
      display: "inline-flex", alignItems: "center", justifyContent: "center", transition: "all .12s",
    }}>
      {on && <IconCheck size={12} style={{ color: "#fff", strokeWidth: 3.2 }} />}
      {!on && mixed && <span style={{ width: 8, height: 2.5, borderRadius: 2, background: color || accent }} />}
    </span>
  );
  // Flattened parent → Team structure (no roster-group tier) — the multi-select
  // lists the League's child Teams only.
  const flatTeams = [];
  ROSTER_GROUP_SEED.forEach((g) => g.rosters.forEach((r) => flatTeams.push({ ...r, group: g.name, color: g.color })));
  STANDALONE_ROSTER_SEED.forEach((r) => flatTeams.push({ ...r, group: "Standalone", color: "#6b6b72" }));
  return (
    <div style={{ maxHeight: 320, overflowY: "auto", padding: "8px 0" }}>
      {flatTeams.map((r) => {
        const on = sel.has(r.id);
        const lockLast = minOne && on && sel.size === 1;
        return (
          <button key={r.id} type="button" disabled={lockLast} title={lockLast ? "Team Required / Team Supplied items must stay assigned to at least one Team" : undefined}
            onClick={() => toggle(r.id)} style={{
              display: "flex", alignItems: "center", gap: 12, width: "100%", textAlign: "left",
              background: "transparent", border: 0, padding: "9px 16px", cursor: lockLast ? "not-allowed" : "pointer", opacity: lockLast ? 0.55 : 1,
            }}
            onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
            onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
            <Box on={on} color={r.color} />
            <span style={{ width: 22, height: 22, borderRadius: 6, flexShrink: 0, background: r.color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 9, fontWeight: 800 }}>{rInitials(r.name)}</span>
            <span style={{ flex: 1, minWidth: 0, fontSize: 13, color: on ? "#0e0e10" : "#4b4b52", fontWeight: on ? 700 : 500, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{r.name}</span>
            <span style={{ fontSize: 11, color: "#c0c0c6", whiteSpace: "nowrap" }}>{r.group}</span>
          </button>
        );
      })}
      {flatTeams.length === 0 && <div style={{ padding: "22px 16px", fontSize: 12.5, color: "#9a9aa1", textAlign: "center" }}>No Teams yet.</div>}
    </div>
  );
}

// ─────────── Required team assignment prompt (League stores) ───────────
// Shown when Team Required / Team Supplied is set with no Team assigned — the
// setting can't save until ≥1 Team is chosen. Offers inline Team creation when
// the league has no Teams yet, so the rep isn't dead-ended.
function TeamAssignPrompt({ open, initial = [], settingLabel = "Team Required", store, onClose, onConfirm }) {
  const [ids, setIds] = React.useState(initial);
  const [creating, setCreating] = React.useState(false);
  const [newName, setNewName] = React.useState("");
  React.useEffect(() => { if (open) { setIds(initial || []); setCreating(false); setNewName(""); } }, [open]);
  if (!open) return null;
  const scoped = storeScopedTeams(store);
  const hasTeams = scoped ? scoped.length > 0 : assignAllRosterIds().length > 0;
  const teamCode = (name) => ((name || "TEAM").replace(/[^A-Za-z]/g, "").slice(0, 4).toUpperCase() || "TEAM") + "-" + String(Math.floor(1000 + Math.random() * 9000));
  const createTeam = () => {
    const name = newName.trim();
    if (!name) return;
    const id = "tm-new-" + Date.now();
    const rec = { id, name, players: 0, code: teamCode(name) };
    if (scoped) window.__storeTeams[store.id] = [...(window.__storeTeams[store.id] || []), rec];
    STANDALONE_ROSTER_SEED.push(rec);
    setIds((cur) => [...cur, id]);
    setNewName(""); setCreating(false);
  };
  return (
    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 420, padding: 24 }}>
      <div onClick={(e) => e.stopPropagation()} style={{ width: 520, maxWidth: "100%", background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.2)", overflow: "hidden" }}>
        <div style={{ padding: "22px 26px 0" }}>
          <div style={{ fontSize: 17, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif", marginBottom: 6 }}>Assign to Teams</div>
          <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>{settingLabel} items must be assigned to at least one Team before the setting can save.</div>
        </div>
        <div style={{ padding: "14px 26px 0" }}>
          {hasTeams
            ? <div style={{ border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden" }}>{scoped ? <ScopedTeamRows teams={scoped} value={ids} onChange={setIds} /> : <RosterTreePanel value={ids} onChange={setIds} accent="#e1251b" />}</div>
            : <div style={{ border: "1px dashed #d4d4d8", borderRadius: 10, padding: "22px 18px", textAlign: "center", fontSize: 13, color: "#6b6b72" }}>This store has no Teams yet — create one below to continue.</div>}
          {creating ? (
            <div style={{ display: "flex", gap: 10, alignItems: "center", marginTop: 12 }}>
              <input autoFocus value={newName} onChange={(e) => setNewName(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") createTeam(); }} placeholder="Team name"
                style={{ flex: 1, height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13, fontFamily: "inherit", outline: "none" }} />
              <span style={{ fontSize: 11.5, color: "#9a9aa1", whiteSpace: "nowrap" }}>Team code auto-generates</span>
              <button type="button" onClick={createTeam} disabled={!newName.trim()} style={{ background: newName.trim() ? "#0e0e10" : "#ececf0", color: newName.trim() ? "#fff" : "#9a9aa1", border: 0, borderRadius: 6, padding: "11px 16px", fontSize: 12.5, fontWeight: 700, cursor: newName.trim() ? "pointer" : "default", fontFamily: "inherit", whiteSpace: "nowrap" }}>Create Team</button>
            </div>
          ) : (
            <button type="button" onClick={() => setCreating(true)} style={{ background: "none", border: 0, padding: 0, marginTop: 12, fontSize: 12.5, fontWeight: 700, color: "#e1251b", cursor: "pointer", fontFamily: "inherit" }}>+ New Team</button>
          )}
          <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 8 }}>Manage Teams, codes and rosters in <span style={{ fontWeight: 700, color: "#52525b" }}>My Team(s)</span>.</div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 10, padding: "18px 26px 22px" }}>
          <button type="button" onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "11px 18px", fontSize: 13, fontWeight: 600, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
          <button type="button" onClick={() => ids.length && onConfirm(ids)} disabled={!ids.length} style={{ background: ids.length ? "#0e0e10" : "#ececf0", color: ids.length ? "#fff" : "#9a9aa1", border: 0, borderRadius: 6, padding: "11px 22px", fontSize: 13, fontWeight: 700, cursor: ids.length ? "pointer" : "default", fontFamily: "inherit" }}>Assign & Save</button>
        </div>
      </div>
    </div>
  );
}

function RosterAssignField({ variant = "dropdown", value = [], onChange, accent = "#e1251b", direction = "up", minOne = false, store }) {
  const [open, setOpen] = useStateW(false);
  const ref = React.useRef(null);
  useEffectW(() => {
    if (!open) return;
    const onDoc = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);

  const scoped = storeScopedTeams(store);
  const total = scoped ? scoped.length : assignAllRosterIds().length;
  const count = value.length;
  const allOn = count >= total && total > 0;
  const selectAll = () => onChange(allOn ? (minOne ? value.slice(0, 1) : []) : (scoped ? scoped.map((t) => t.id) : assignAllRosterIds()));

  const label = (
    <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 10.5, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase" }}>
      <IconUsers size={13} /> Assign to Teams
    </span>
  );

  const popover = open && (
    <div style={{
      position: "absolute", ...(direction === "down" ? { top: "calc(100% + 8px)" } : { bottom: "calc(100% + 8px)" }), left: 0, right: 0, zIndex: 50,
      background: "#fff", border: "1px solid #e5e5e9", borderRadius: 12, overflow: "hidden",
      boxShadow: "0 18px 44px rgba(20,20,30,.2)",
    }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "12px 16px", borderBottom: "1px solid #f1f1f3" }}>
        <span style={{ fontSize: 12, fontWeight: 800, color: "#0e0e10" }}>{count} team{count === 1 ? "" : "s"} selected</span>
        <button type="button" onClick={selectAll} style={{ background: "transparent", border: 0, cursor: "pointer", fontSize: 12, fontWeight: 700, color: accent }}>{allOn ? "Clear all" : "Select all"}</button>
      </div>
      {scoped ? <ScopedTeamRows teams={scoped} value={value} onChange={onChange} minOne={minOne} /> : <RosterTreePanel value={value} onChange={onChange} accent={accent} minOne={minOne} />}
      <div style={{ padding: "12px 16px", borderTop: "1px solid #f1f1f3", display: "flex", justifyContent: "flex-end" }}>
        <button type="button" onClick={() => setOpen(false)} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "8px 16px", fontSize: 12.5, fontWeight: 800, cursor: "pointer" }}>Done</button>
      </div>
    </div>
  );

  // Variant B — chips: selected teams shown as removable pills inline.
  if (variant === "chips") {
    const remove = (id) => onChange(value.filter((x) => x !== id));
    return (
      <div ref={ref} style={{ position: "relative", marginTop: 12 }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
          {label}
          <button type="button" onClick={() => setOpen((o) => !o)} style={{ display: "inline-flex", alignItems: "center", gap: 4, background: "transparent", border: 0, cursor: "pointer", fontSize: 12, fontWeight: 800, color: accent }}>
            <IconPlus size={13} style={{ strokeWidth: 2.6 }} /> {count ? "Edit" : "Assign teams"}
          </button>
        </div>
        <div onClick={() => setOpen(true)} style={{
          display: "flex", flexWrap: "wrap", gap: 8, minHeight: 40, alignItems: "center", cursor: "pointer",
          border: `1px dashed ${count ? "#d4d4d8" : "#cfcfd5"}`, borderRadius: 8, padding: "8px 12px", background: "#fafafb",
        }}>
          {count === 0 && <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>No teams assigned — tap to choose which teams wear this.</span>}
          {value.slice(0, 6).map((id) => {
            const l = assignRosterLabel(id); if (!l) return null;
            return (
              <span key={id} style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 11.5, fontWeight: 700, color: "#0e0e10", background: "#fff", border: "1px solid #e5e5e9", borderRadius: 999, padding: "4px 8px 4px 12px" }}>
                <span style={{ width: 8, height: 8, borderRadius: "50%", background: l.color }} />
                {l.name}
                <span onClick={(e) => { e.stopPropagation(); remove(id); }} style={{ display: "inline-flex", cursor: "pointer", color: "#9a9aa1" }}><IconX size={12} /></span>
              </span>
            );
          })}
          {count > 6 && <span style={{ fontSize: 11.5, fontWeight: 800, color: "#6b6b72" }}>+{count - 6} more</span>}
        </div>
        {popover}
      </div>
    );
  }

  // Variant A — dropdown: a select-style field summarizing the selection.
  let summary = "Select teams…";
  if (allOn) summary = "All teams";
  else if (count === 1) { const l = assignRosterLabel(value[0]); summary = l ? l.name : "1 team"; }
  else if (count > 1) summary = `${count} teams selected`;
  return (
    <div ref={ref} style={{ position: "relative", marginTop: 12, zIndex: open ? 60 : "auto" }}>
      <div style={{ marginBottom: 8 }}>{label}</div>
      <button type="button" onClick={() => setOpen((o) => !o)} style={{
        display: "flex", alignItems: "center", gap: 12, width: "100%", textAlign: "left", cursor: "pointer",
        background: "#fff", border: `1.5px solid ${open ? accent : count ? "#c7c7cd" : "#d4d4d8"}`, borderRadius: 8,
        padding: "12px 12px", boxShadow: open ? `0 0 0 3px ${hexA(accent, 0.12)}` : "none", transition: "border-color .14s, box-shadow .14s",
      }}>
        <span style={{ flex: 1, minWidth: 0, fontSize: 13, fontWeight: count ? 700 : 500, color: count ? "#0e0e10" : "#9a9aa1", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{summary}</span>
        {count > 0 && <span style={{ flexShrink: 0, fontSize: 10.5, fontWeight: 800, color: accent, background: hexA(accent, 0.1), borderRadius: 999, padding: "2px 8px" }}>{count}</span>}
        <IconChevron size={18} style={{ flexShrink: 0, color: "#6b6b72", transform: open ? "rotate(180deg)" : "none", transition: "transform .16s" }} />
      </button>
      {popover}
    </div>
  );
}

function Step3Products({ data, set, accent, liveStores, stepNum }) {
  const [customizerOpen, setCustomizerOpen] = useStateW(false);
  const [cfgItems, setCfgItems] = useStateW(null);   // product objects currently being configured
  const [cfgState, setCfgState] = useStateW([]);     // parallel per-item config (sizes/settings/pricing)
  const [cfgActive, setCfgActive] = useStateW(0);
  const [removeTarget, setRemoveTarget] = useStateW(null); // { id, title }
  const [cfgEdit, setCfgEdit] = useStateW(false); // Edit Product re-uses the Configure & Add modal
  const [toast, setToast] = useStateW(null);
  // Product-to-roster assignment (League stores only). Keyed by card id.
  const isLeague = data.storeType === "league";
  const assignments = data.rosterAssignments || {};
  const setAssign = (cardId, ids) => set({ rosterAssignments: { ...assignments, [cardId]: ids } });
  const [assignVariant, setAssignVariant] = useStateW("dropdown");
  // Customize New Product — XL 4-step wizard
  const [custWizStep, setCustWizStep] = useStateW(0);   // 0=sport 1=products 2=builder 3=review
  const [custSport, setCustSport] = useStateW(null);
  const [custProduct, setCustProduct] = useStateW(null);

  const curated = data.curatedProducts || [];
  const committedIds = new Set(curated.map((c) => c.srcId).filter(Boolean));
  const storeProxy = { id: "wizard-store", teamName: data.storeTitle || "Team Store", teamColors: data.teamColors, storeType: data.storeType, fundraisingEnabled: data.fundraisingEnabled ?? false };

  // Open the per-item config modal (application sizes + product settings + price
  // settings) before anything is committed to the store.
  const beginAdd = (items) => {
    setCfgItems(items);
    setCfgState(items.map((it) => ({
      applicationSizes: defaultApplicationSizes(),
      teamSetting: "fan-gear",
      pricing: pendingPricing(it),
      rosters: assignments[it.srcId] || assignments[it.id] || [],
      sizesConfirmed: false,
    })));
    setCfgEdit(false);
    setCfgActive(0);
  };

  // Edit Product on a committed card — re-opens the same Configure & Add modal seeded with the saved config.
  const beginEditCommitted = (srcId) => {
    const items = curated.filter((c) => c.srcId === srcId || c.id === srcId || c.pkgId === srcId);
    if (!items.length) return;
    setCfgItems(items.map((c) => ({ ...c })));
    setCfgState(items.map((c) => ({
      applicationSizes: c.applicationSizes || defaultApplicationSizes(),
      teamSetting: c.teamSetting || "fan-gear",
      pricing: c.pricing || pendingPricing(c),
      rosters: c.rosters || assignments[c.srcId] || assignments[c.id] || [],
      storeCategory: c.storeCategory || "", storeSubcategory: c.storeSubcategory || "",
      optRequired: !!c.optRequired, optSupplied: !!c.optSupplied,
      sizesConfirmed: !!c.sizesConfirmed,
    })));
    setCfgEdit(true);
    setCfgActive(0);
  };

  const updateActiveConfig = (next) => setCfgState((cs) => cs.map((c, i) => i === cfgActive ? next : c));
  const updateActiveRows = (rows) => setCfgState((cs) => cs.map((c, i) => i === cfgActive ? { ...c, applicationSizes: rows } : c));

  // Confirm config → commit every configured item into the store's product list.
  const confirmConfig = () => {
    if (!cfgItems) return;
    const stamped = cfgItems.map((it, i) => {
      const cfg = cfgState[i];
      return {
        ...it,
        teamSetting: cfg.teamSetting,
        storeCategory: (cfg.optRequired || cfg.optSupplied || cfg.teamSetting === "team-required" || cfg.teamSetting === "team-supplied") ? "Team Gear" : (cfg.storeCategory || ""),
        storeSubcategory: cfg.storeSubcategory || "",
        optRequired: !!cfg.optRequired || cfg.teamSetting === "team-required", optSupplied: !!cfg.optSupplied || cfg.teamSetting === "team-supplied",
        applicationSizes: cfg.applicationSizes,
        pricing: cfg.pricing,
        rosters: cfg.rosters || [],
        adult: cfg.pricing.adult.retail,
        youth: cfg.pricing.youth.retail,
        sizesConfirmed: true,
      };
    });
    if (cfgEdit) {
      const byId = new Map(stamped.map((s) => [s.id, s]));
      const nextCurated = curated.map((c) => byId.has(c.id) ? { ...c, ...byId.get(c.id) } : c);
      const editAssignments = { ...(data.rosterAssignments || {}) };
      stamped.forEach((s) => { editAssignments[s.id] = s.rosters || []; });
      set({ curatedProducts: nextCurated, rosterAssignments: editAssignments });
      setCfgItems(null);
      setCfgEdit(false);
      setToast("Product updated");
      return;
    }
    const existing = new Set(curated.map((c) => c.id));
    const merged = [...curated, ...stamped.filter((s) => !existing.has(s.id))];
    const nextAssignments = { ...(data.rosterAssignments || {}) };
    stamped.forEach((s) => { if (!existing.has(s.id)) nextAssignments[s.id] = s.rosters || []; });
    set({ curatedProducts: merged, rosterAssignments: nextAssignments });
    setCfgItems(null);
    setToast(`Added ${stamped.length} product${stamped.length === 1 ? "" : "s"} to the store`);
  };

  const addDesign = (d) => beginAdd(
    [{ id: `wd-${d.id}`, srcId: d.id, title: d.title, style: d.category, photo: d.photo, adult: d.adult, youth: d.youth, teamProfit: d.teamProfit }],
  );
  const addCart = (c) => beginAdd(
    c.items.map((it) => ({ id: `wc-${it.id}`, srcId: c.id, title: it.title, style: "From " + c.name, photo: it.photo, adult: it.adult, youth: it.youth, teamProfit: it.teamProfit })),
  );
  const pickCustom = (item) => {
    setCustomizerOpen(false);
    beginAdd(
      [{ id: `wx-${item.id}-${Date.now()}`, title: item.label + " — Custom", style: "Customized product", photo: "jersey-padres", adult: "60.00", youth: "52.00", teamProfit: 15.0 }],
    );
  };

  // Remove every committed product that originated from this saved asset.
  const confirmRemove = () => {
    if (!removeTarget) return;
    const next = curated.filter((c) => c.srcId !== removeTarget.id && c.id !== removeTarget.id);
    set({ curatedProducts: next });
    const removedCount = curated.length - next.length;
    setRemoveTarget(null);
    setToast(`Removed ${removedCount} product${removedCount === 1 ? "" : "s"} from the store`);
  };


  // ── Stock Vault + Other stores — Epic A/G (2026-08 revision).
  // Third-party seller catalog is paused for this pass — Vault is Prolook-only. ──
  const [vaultOffer, setVaultOffer] = React.useState(null);
  const addVaultItem = (item) => setVaultOffer(item);
  const confirmVaultOffer = (item, sizes, cws) => {
    const cw = (cws && cws[0]) || hcmDefaultCw(item);
    setVaultOffer(null);
    beginAdd([{ id: "wv-" + item.id, srcId: item.id, title: item.title, style: (item.source || "Stock Vault") + " · " + (item.vaultCategory || ""), srcKind: "vault", photo: cw && cw.front, sku: item.sku, priceAdult: item.priceAdult, priceYouth: item.priceYouth, adult: item.priceAdult, youth: item.priceYouth != null ? item.priceYouth : item.priceAdult, colorway: cw && cw.name, colorways: (cws && cws.length ? cws : item.colorways), offeredSizes: sizes }]);
  };
  const addTemplate = (t) => beginAdd(t.members.map((m) => ({ id: "wt-" + t.id + "-" + m.id, srcId: t.id + "-" + m.id, tplId: t.id, title: m.title, style: m.styleBlock, srcKind: "template", designLocked: !!m.locked, photo: m.photo, sku: m.sku })));
  const addFromOtherStore = (p, storeName) => beginAdd([{ id: "wo-" + p.id, srcId: p.id, title: p.title, style: (p.sku || p.subtitle || "Style block") + " · from " + storeName, srcKind: "other-store", photo: p.photo }]);

  const proStockItems = (window.__prostockItems || []).filter((i) => i.stockType !== "sideline");
  const sidelineItems = (window.__prostockItems || []).filter((i) => i.stockType === "sideline");
  const [tplBump, setTplBump] = React.useState(0);
  const renameTpl = (t, name) => { window.__catalogTemplates = (window.__catalogTemplates || []).map((x) => x.id === t.id ? { ...x, name } : x); setTplBump((n) => n + 1); setToast("Template renamed to \u201c" + name + "\u201d"); };
  const deleteTpl = (t) => { window.__catalogTemplates = (window.__catalogTemplates || []).filter((x) => x.id !== t.id); setTplBump((n) => n + 1); setToast("Template \u201c" + t.name + "\u201d deleted"); };
  const otherStores = liveStores || [];

  return (
    <div>
      <StepHeader num={String(stepNum || 2)} accent={accent} title="Add Products"
        desc="Pull products from the Customizer, the Stock Vault, a saved Catalog Template, or items already used in your other stores. Each item confirms its application sizes before it's added to the store."
        action={
          <button type="button" onClick={() => { setCustomizerOpen(true); setCustWizStep(0); setCustSport(null); setCustProduct(null); }} style={{
            display: "inline-flex", alignItems: "center", gap: 8, flexShrink: 0,
            background: "#fff", color: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6,
            padding: "12px 16px", fontSize: 13, fontWeight: 700, letterSpacing: ".01em", cursor: "pointer",
            transition: "background .14s, color .14s",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.background = "#0e0e10"; e.currentTarget.style.color = "#fff"; }}
            onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = "#0e0e10"; }}>
            <IconPlus size={15} style={{ strokeWidth: 2.6 }} /> Customize New Product
          </button>
        } />

      {data._fromVault && curated.length > 0 && (
        <div style={{ display: "flex", alignItems: "center", gap: 12, background: "#e7f5ed", border: "1px solid #bfe3cd", borderRadius: 10, padding: "14px 18px", marginBottom: 24 }}>
          <span style={{ width: 34, height: 34, borderRadius: "50%", background: "#1f9d55", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}><IconCheck size={16} style={{ strokeWidth: 3 }} /></span>
          <div>
            <div style={{ fontSize: 13.5, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>This step is already complete — {curated.length} Vault item{curated.length === 1 ? "" : "s"} staged as drafts</div>
            <div style={{ fontSize: 12.5, color: "#3f7a55", marginTop: 2 }}>Review is optional. Each item still confirms pricing, team setting, sizes and roster assignment before deploy, and entitlement is checked against this store's seller.</div>
          </div>
        </div>
      )}

      <AddProductsBrowser accent={accent} groups={[
        { key: "catalog", label: "Customizer", subs: [{ k: "designs", l: "Saved Designs" }, { k: "carts", l: "Saved Carts" }], items: [].concat(
          WIZARD_SAVED_DESIGNS.map((d) => ({ _key: "d-" + d.id, sub: "designs", kind: "design", badge: "Saved design", title: d.title, subtitle: d.block || `${d.sport} · ${d.category}`, photo: d.photo,
            committed: committedIds.has(d.id), onAdd: () => addDesign(d),
            committedSetting: (curated.find((c) => c.srcId === d.id) || {}).teamSetting,
            onEdit: () => beginEditCommitted(d.id),
            onRemove: () => setRemoveTarget({ id: d.id, title: d.title }) })),
          WIZARD_SAVED_CARTS.map((c) => ({ _key: "c-" + c.id, sub: "carts", kind: "cart", badge: "Saved cart", title: c.name, subtitle: `${c.items.length} items · $${c.total.toFixed(2)}`, items: c.items,
            committed: committedIds.has(c.id), onAdd: () => addCart(c),
            committedSetting: (curated.find((cc) => cc.srcId === c.id) || {}).teamSetting,
            onEdit: () => beginEditCommitted(c.id),
            onRemove: () => setRemoveTarget({ id: c.id, title: c.name }) })),
          curated.filter((c) => !c.srcId).map((p) => ({ _key: "x-" + p.id, sub: "designs", kind: "design", badge: "Custom", title: p.title, subtitle: p.style || "Custom product", photo: p.photo,
            committed: true, onAdd: () => {}, committedSetting: p.teamSetting,
            onEdit: () => beginEditCommitted(p.id),
            onRemove: () => setRemoveTarget({ id: p.id, title: p.title }) }))
        ) },
        { key: "vault", label: "Stock Vault", subs: [{ k: "pro", l: "Pro Stock" }, { k: "sideline", l: "Sideline Stock" }], items: [].concat(
          proStockItems.map((item) => ({ _key: "v-" + item.id, sub: "pro", kind: "design", badge: "Pro Stock", title: item.title, subtitle: `${vaultGenderLine(item)} · ${item.colorways.length} color ways · ${vaultPriceLine(item)}`, photo: hcmDefaultCw(item) && hcmDefaultCw(item).front,
            committed: committedIds.has(item.id), onAdd: () => addVaultItem(item), onEdit: () => beginEditCommitted(item.id), onRemove: () => {} })),
          sidelineItems.map((item) => ({ _key: "s-" + item.id, sub: "sideline", kind: "design", badge: "Sideline", title: item.title, subtitle: `${vaultGenderLine(item)} · ${item.colorways.length} color ways · ${vaultPriceLine(item)}`, photo: hcmDefaultCw(item) && hcmDefaultCw(item).front,
            committed: committedIds.has(item.id), onAdd: () => addVaultItem(item), onEdit: () => beginEditCommitted(item.id), onRemove: () => {} })),
        ) },
        { key: "templates", label: "Catalog Templates", subs: [], emptyHint: "No catalog templates yet \u2014 select items on a store's Products tab and choose Create Catalog Template. Templates carry their origin store's artwork; for the same gear without the branding, pull from Other stores instead.", items: (window.__catalogTemplates || []).map((t) => ({ _key: "t-" + t.id, sub: null, kind: "cart", badge: "Template", title: t.name, subtitle: t.members.length + " items \u00b7 $" + t.members.reduce((n, m) => n + (m.price || 0), 0).toFixed(2), items: t.members,
            committed: curated.some((c) => c.tplId === t.id), onAdd: () => addTemplate(t), onEdit: () => beginEditCommitted(t.id), onRemove: () => {},
            menu: [{ label: "Rename template", rename: true }, { label: "Delete template", danger: true, act: () => deleteTpl(t) }], onRenameCommit: (name) => renameTpl(t, name) })) },
        { key: "other", label: "Other stores", subs: otherStores.map((s) => ({ k: s.id, l: s.storeTitle || s.teamName || "Store" })), items: [].concat.apply([], otherStores.map((s) => ((s.products || []).filter((p) => !p.isBundle)).map((p) => ({
          _key: "o-" + s.id + "-" + p.id, sub: s.id, kind: "design", badge: s.storeTitle || s.teamName || "Store", title: p.title, subtitle: p.sku || p.subtitle || "Style block", photo: p.photo,
          committed: committedIds.has(p.id), onAdd: () => addFromOtherStore(p, s.storeTitle || s.teamName || "that store"), onEdit: () => beginEditCommitted(p.id), onRemove: () => {} })))) },
      ]} />

      {vaultOffer && <VaultOfferModal item={vaultOffer} onClose={() => setVaultOffer(null)} onConfirm={(sizes, cws) => confirmVaultOffer(vaultOffer, sizes, cws)} />}

      {/* Interaction A — per-item config modal (application sizes + product & price settings) */}
      {cfgItems && (
        <ConfirmSizesModal
          open={!!cfgItems}
          product={cfgItems[cfgActive]}
          store={storeProxy}
          rows={cfgState[cfgActive].applicationSizes}
          onChange={updateActiveRows}
          productConfig={cfgState[cfgActive]}
          onConfigChange={updateActiveConfig}
          items={cfgItems}
          activeIndex={cfgActive}
          onActiveIndexChange={setCfgActive}
          confirmLabel={cfgEdit ? "Save Changes" : cfgItems.length > 1 ? `Add ${cfgItems.length} Products to Store` : "Add to Store"}
          onClose={() => { setCfgItems(null); setCfgEdit(false); }}
          onConfirm={confirmConfig} />
      )}

      {/* Interaction B — Customize New Product — XL 4-step workflow modal */}
      <Modal open={customizerOpen} onClose={() => setCustomizerOpen(false)} width="min(1280px, 92vw)">
        <div style={{ display: "flex", flexDirection: "column", height: "88vh", borderRadius: 8, overflow: "hidden" }}>
          {/* Persistent header */}
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "16px 28px", borderBottom: "1px solid #ececf0", flexShrink: 0, background: "#fff" }}>
            <h3 style={{ fontSize: 18, fontWeight: 800, margin: 0, letterSpacing: ".01em", color: "#0e0e10" }}>Customize New Product</h3>
            <button onClick={() => setCustomizerOpen(false)} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
              <IconX size={20} />
            </button>
          </div>
          {/* Step content */}
          <div style={{ flex: 1, overflow: "hidden", minHeight: 0, background: "#fff" }}>
            {custWizStep === 0 && (
              <CustSportPickerStep
                storeSport={(data.sports && data.sports[0]) || "Basketball"}
                onPick={(item) => { setCustSport(item); setCustWizStep(1); }}
              />
            )}
            {custWizStep === 1 && (
              <CustProductPickerStep
                sport={custSport}
                onBack={() => setCustWizStep(0)}
                onPick={(product) => { setCustProduct(product); setCustWizStep(2); }}
              />
            )}
            {custWizStep === 2 && (
              <CustBuilderStep
                sport={custSport}
                product={custProduct}
                onBack={() => setCustWizStep(1)}
                onAddToStore={() => setCustWizStep(3)}
              />
            )}
            {custWizStep === 3 && (
              <CustReviewStep
                sport={custSport}
                product={custProduct}
                onBack={() => setCustWizStep(2)}
                onCancel={() => setCustomizerOpen(false)}
                onAddToStore={(finalProduct) => {
                  const existing = new Set(curated.map((c) => c.id));
                  if (!existing.has(finalProduct.id)) set({ curatedProducts: [...curated, finalProduct] });
                  setCustomizerOpen(false);
                  setToast("Added 1 product to the store");
                }}
              />
            )}
          </div>
        </div>
      </Modal>

      {/* Remove-from-store confirmation */}
      <Modal open={!!removeTarget} onClose={() => setRemoveTarget(null)} width={460}>
        <div style={{ padding: "28px 28px 24px" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 16, marginBottom: 16 }}>
            <span style={{
              width: 44, height: 44, borderRadius: 9, background: "#fff3f2", color: "#b81d14",
              display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0,
            }}><IconTrash size={20} /></span>
            <h3 style={{ fontSize: 18, fontWeight: 800, margin: 0, letterSpacing: ".01em", color: "#0e0e10" }}>Remove from store?</h3>
          </div>
          <p style={{ fontSize: 13.5, color: "#6b6b72", lineHeight: 1.55, margin: "0 0 24px" }}>
            Are you sure you want to remove <strong style={{ color: "#0e0e10", fontWeight: 700 }}>{removeTarget?.title}</strong> from this store? Its confirmed application sizes will be discarded — you can add it again later.
          </p>
          <div style={{ display: "flex", justifyContent: "flex-end", gap: 12 }}>
            <button onClick={() => setRemoveTarget(null)} style={{
              background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 24px", fontSize: 13, fontWeight: 700, color: "#0e0e10", cursor: "pointer",
            }}>Cancel</button>
            <button onClick={confirmRemove} style={{
              background: "#b81d14", color: "#fff", border: 0, borderRadius: 6,
              padding: "12px 24px", fontSize: 13, fontWeight: 800, cursor: "pointer",
              display: "inline-flex", alignItems: "center", gap: 8,
            }}>
              <IconTrash size={15} style={{ strokeWidth: 2.2 }} /> Remove from Store
            </button>
          </div>
        </div>
      </Modal>


      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
}

// ───────────────────────── STEP 4 — Compliance & Taxation ─────────────────────────

const TAX_MODES = [
  { key: "zip", title: "ZIP AutoTax", icon: (p) => <IconPin {...p} />, desc: "Dynamic real-time sales tax calculated automatically via the buyer's address ZIP." },
  { key: "flat", title: "Flat Rate", icon: (p) => <IconDollar {...p} />, desc: "Apply a single uniform percentage rate on all transaction orders." },
  { key: "exempt", title: "Tax Exempt", icon: (p) => <IconShield {...p} />, desc: "Configure 100% tax-free checkout for certified public programs." },
];

// Epic G — flat statewide base rates. Selecting a state populates the flat Tax Rate field.
const STATE_TAX_RATES = {
  Alabama: 4.00, Alaska: 0.00, Arizona: 5.60, Arkansas: 6.50, California: 7.25, Colorado: 2.90,
  Connecticut: 6.35, Delaware: 0.00, Florida: 6.00, Georgia: 4.00, Hawaii: 4.00, Idaho: 6.00,
  Illinois: 6.25, Indiana: 7.00, Iowa: 6.00, Kansas: 6.50, Kentucky: 6.00, Louisiana: 4.45,
  Maine: 5.50, Maryland: 6.00, Massachusetts: 6.25, Michigan: 6.00, Minnesota: 6.875, Mississippi: 7.00,
  Missouri: 4.225, Montana: 0.00, Nebraska: 5.50, Nevada: 6.85, "New Hampshire": 0.00, "New Jersey": 6.625,
  "New Mexico": 5.125, "New York": 4.00, "North Carolina": 4.75, "North Dakota": 5.00, Ohio: 5.75,
  Oklahoma: 4.50, Oregon: 0.00, Pennsylvania: 6.00, "Rhode Island": 7.00, "South Carolina": 6.00,
  "South Dakota": 4.20, Tennessee: 7.00, Texas: 6.25, Utah: 6.10, Vermont: 6.00, Virginia: 5.30,
  Washington: 6.50, "West Virginia": 6.00, Wisconsin: 5.00, Wyoming: 4.00,
};

function Step4Compliance({ data, set, errors, accent }) {
  return (
    <div>
      <StepHeader num="3" accent={accent} title="Compliance &amp; Taxation"
        desc="Configure how sales tax is calculated and applied to buyers at checkout." />

      <WizardPanel label="Sales Tax Compliance Protocol" hint="Defines how tax is logged and applied to storefront buyers">
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 16 }}>
          {TAX_MODES.map((m) => (
            <ChoiceCard key={m.key} accent={accent} minHeight={170}
              active={data.taxMode === m.key}
              onSelect={() => set({ taxMode: m.key })}
              icon={m.icon({ size: 22 })} title={m.title} desc={m.desc} />
          ))}
        </div>
        {data.taxMode === "flat" && (
          <div style={{ marginTop: 16, maxWidth: 260 }}>
            <TextField label="Flat Tax Rate %" value={data.flatTaxRate}
              onChange={(v) => set({ flatTaxRate: v.replace(/[^0-9.]/g, "") })}
              suffix={<span style={{ color: "#9a9aa1", fontSize: 13 }}>%</span>}
              error={errors.flatTaxRate} />
          </div>
        )}
        {data.taxMode === "exempt" && (
          <div style={{
            marginTop: 16, display: "flex", alignItems: "center", gap: 12,
            background: "#e7f5ed", color: "#1f7a44", borderRadius: 8, padding: "12px 16px", fontSize: 13,
          }}>
            <IconShield size={16} /> Tax-free checkout enabled — keep an exemption certificate on file for this program.
          </div>
        )}
      </WizardPanel>
    </div>
  );
}

// ───────────────────────── STEP 5 — Visual Design & Branding ─────────────────────────

const HERO_THEMES = [
  { key: "pitch", name: "Rangers Pitch", c1: "#1f4d2e", c2: "#0f2418" },
  { key: "gridiron", name: "Gridiron Lights", c1: "#13243f", c2: "#0a1322" },
  { key: "floor", name: "Championship Floor", c1: "#5a3210", c2: "#2a1707" },
  { key: "charcoal", name: "Minimal Charcoal", c1: "#3a3a3f", c2: "#19191c" },
];

const PRIMARY_SWATCHES = ["#e1251b", "#1d3a8a", "#1f8a55", "#9b1b30", "#bf5700", "#4a148c", "#0e0e10", "#2563eb"];

function HeroThemeTile({ theme, active, onSelect, accent }) {
  return (
    <button type="button" onClick={onSelect} style={{
      border: `1.5px solid ${active ? accent : "#e5e5e9"}`, borderRadius: 10, overflow: "hidden",
      background: "#fff", cursor: "pointer", padding: 0, textAlign: "left",
      boxShadow: active ? `0 0 0 3px ${hexA(accent, 0.12)}` : "none", transition: "border-color .14s, box-shadow .14s",
    }}>
      <div style={{
        height: 92, position: "relative",
        background: `linear-gradient(135deg, ${theme.c1}, ${theme.c2})`,
      }}>
        {/* subtle diagonal stripe placeholder texture */}
        <div style={{
          position: "absolute", inset: 0, opacity: 0.16,
          backgroundImage: "repeating-linear-gradient(135deg, #fff 0 2px, transparent 2px 12px)",
        }} />
        {active && (
          <span style={{
            position: "absolute", top: 8, right: 8, width: 24, height: 24, borderRadius: "50%",
            background: accent, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center",
          }}><IconCheck size={13} style={{ strokeWidth: 3.4 }} /></span>
        )}
      </div>
      <div style={{ padding: "12px 16px", fontSize: 12.5, fontWeight: 800, letterSpacing: ".04em", color: "#0e0e10", textTransform: "uppercase" }}>
        {theme.name}
      </div>
    </button>
  );
}

// Live storefront simulation — re-renders from the wizard data as the manager
// edits the hero theme, primary color, logo, and announcement.
function StorePreview({ data, accent }) {
  const theme = HERO_THEMES.find((t) => t.key === data.heroTheme) || HERO_THEMES[0];
  const primary = data.primaryColor || accent || "#e1251b";
  const products = ((data.curatedProducts && data.curatedProducts.length) ? data.curatedProducts : WIZARD_CATALOG).slice(0, 4);
  const title = (data.storeTitle && data.storeTitle.trim()) || "Team Store";
  const logo = data.teamLogo;
  const initials = title.split(/\s+/).map((w) => w[0]).join("").slice(0, 2).toUpperCase();

  return (
    <div style={{ border: "1px solid #e5e5e9", borderRadius: 12, overflow: "hidden", background: "#fff" }}>
      {/* Browser chrome */}
      <div style={{ height: 36, background: "#f1f1f3", borderBottom: "1px solid #e5e5e9", display: "flex", alignItems: "center", gap: 8, padding: "0 16px" }}>
        <span style={{ width: 12, height: 12, borderRadius: "50%", background: "#e1251b" }} />
        <span style={{ width: 12, height: 12, borderRadius: "50%", background: "#f5b400" }} />
        <span style={{ width: 12, height: 12, borderRadius: "50%", background: "#1f9d55" }} />
        <div style={{
          marginLeft: 12, flex: 1, maxWidth: 360, height: 24, borderRadius: 999, background: "#fff",
          border: "1px solid #e0e0e4", display: "inline-flex", alignItems: "center", padding: "0 12px",
          fontSize: 11.5, color: "#9a9aa1", fontFamily: "JetBrains Mono, monospace", whiteSpace: "nowrap", overflow: "hidden",
        }}>prolookstores.com/{data.storeSlug || "store"}</div>
      </div>

      {/* Hero */}
      <div style={{ position: "relative", background: `linear-gradient(135deg, ${theme.c1}, ${theme.c2})`, padding: "32px 36px 36px", color: "#fff", overflow: "hidden" }}>
        <div style={{ position: "absolute", inset: 0, opacity: 0.14, backgroundImage: "repeating-linear-gradient(135deg, #fff 0 2px, transparent 2px 16px)" }} />
        <div style={{ position: "relative" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
            <div style={{
              width: 60, height: 60, borderRadius: 12, flexShrink: 0, background: "#fff", overflow: "hidden",
              display: "inline-flex", alignItems: "center", justifyContent: "center", boxShadow: "0 4px 14px rgba(0,0,0,.22)",
            }}>
              {logo && logo.url
                ? <img src={logo.url} alt="" style={{ width: "100%", height: "100%", objectFit: "cover" }} />
                : <span style={{ fontSize: 22, fontWeight: 800, color: (logo && logo.color) || primary, fontFamily: "Archivo, sans-serif" }}>{(logo && logo.mono) || initials}</span>}
            </div>
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".14em", textTransform: "uppercase", opacity: 0.78 }}>Official Team Store</div>
              <div style={{ fontSize: 26, fontWeight: 900, fontFamily: "Archivo, sans-serif", letterSpacing: "-.01em", lineHeight: 1.1 }}>{title}</div>
            </div>
          </div>
          {data.announcement && (
            <p style={{ fontSize: 13.5, lineHeight: 1.55, opacity: 0.92, margin: "16px 0 0", maxWidth: 560 }}>{data.announcement}</p>
          )}
          <button type="button" style={{
            marginTop: 20, border: 0, borderRadius: 7, padding: "12px 24px", cursor: "pointer",
            background: primary, color: readableInk(primary), fontSize: 13, fontWeight: 800, letterSpacing: ".02em",
          }}>Shop the Collection</button>
        </div>
      </div>

      {/* Product grid */}
      <div style={{ padding: "24px 28px 28px" }}>
        <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", marginBottom: 16 }}>
          <span style={{ fontSize: 14, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>Featured Gear</span>
          <span style={{ fontSize: 12, color: "#9a9aa1" }}>{products.length} item{products.length === 1 ? "" : "s"}</span>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 16 }}>
          {products.map((p, i) => (
            <div key={p.id || i} style={{ border: "1px solid #ececf0", borderRadius: 9, overflow: "hidden", background: "#fff" }}>
              <div style={{ height: 112, background: "#f1f2f4", padding: 12, display: "flex", alignItems: "center", justifyContent: "center" }}>
                <CatalogPhoto photo={p.photo} alt={p.title} />
              </div>
              <div style={{ padding: "12px 12px 12px" }}>
                <div style={{ fontSize: 12, fontWeight: 700, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", marginBottom: 4 }}>{p.title}</div>
                <div style={{ fontSize: 13, fontWeight: 800, color: primary, fontFamily: "Archivo, sans-serif" }}>${p.adult}</div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}

function Step5Design({ data, set, accent }) {
  return (
    <div>
      <StepHeader num="4" accent={accent} title="Visual Design &amp; Branding"
        desc="Set the storefront's hero theme, primary brand color, team logo, and the welcome announcement buyers see." />

      <WizardPanel label="Store Hero Banner Theme">
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 16 }}>
          {HERO_THEMES.map((t) => (
            <HeroThemeTile key={t.key} theme={t} accent={accent}
              active={data.heroTheme === t.key} onSelect={() => set({ heroTheme: t.key })} />
          ))}
        </div>
      </WizardPanel>

      <WizardPanel label="Primary Brand Color">
        <div style={{ display: "flex", flexWrap: "wrap", gap: 12, alignItems: "center" }}>
          {PRIMARY_SWATCHES.map((c) => {
            const on = (data.primaryColor || "").toLowerCase() === c.toLowerCase();
            return (
              <button key={c} type="button" onClick={() => set({ primaryColor: c })} title={c} style={{
                width: 48, height: 48, borderRadius: 8, background: c, cursor: "pointer",
                border: c.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "1px solid rgba(0,0,0,.06)",
                outline: on ? "2.5px solid #0e0e10" : "none", outlineOffset: 2,
                display: "inline-flex", alignItems: "center", justifyContent: "center",
              }}>
                {on && <IconCheck size={18} style={{ color: readableInk(c), strokeWidth: 3 }} />}
              </button>
            );
          })}
        </div>
      </WizardPanel>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, alignItems: "start" }}>
        <WizardPanel label="Team Logo" style={{ marginBottom: 0 }}>
          <LogoDropZone logo={data.teamLogo} onChange={(v) => set({ teamLogo: v })} />
          <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 12 }}>Used to brand the storefront header and pre-set product customization.</div>
        </WizardPanel>

        <WizardPanel label="Team Colors" style={{ marginBottom: 0 }}>
          <ColorSwatches colors={data.teamColors || []} onChange={(v) => set({ teamColors: v })} />
        </WizardPanel>
      </div>

      <WizardPanel label="Announcement Message" style={{ marginTop: 16 }}>
        <textarea
          value={data.announcement || ""}
          onChange={(e) => set({ announcement: e.target.value })}
          placeholder="Welcome buyers to the storefront — custom jersey messaging, deadline reminders, etc."
          style={{
            width: "100%", minHeight: 112, resize: "vertical", border: "1px solid #d4d4d8",
            borderRadius: 6, padding: "12px 16px", fontSize: 14, fontFamily: "inherit", color: "#0e0e10",
            outline: "none", lineHeight: 1.55,
          }} />
      </WizardPanel>

      <WizardPanel label="Store Preview" hint="Live — updates as you edit" style={{ marginTop: 16 }}>
        <StorePreview data={data} accent={accent} />
      </WizardPanel>
    </div>
  );
}

// ───────────────────────── STEP 6 — Review & Deploy ─────────────────────────

function SummaryRowWiz({ label, value, mono, accent, last }) {
  return (
    <div style={{
      display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 24,
      padding: "16px 0", borderBottom: last ? "none" : "1px solid #ececf0",
    }}>
      <span style={{ fontSize: 13.5, color: "#6b6b72", flexShrink: 0 }}>{label}</span>
      <span style={{
        fontSize: 14, fontWeight: 700, textAlign: "right", wordBreak: "break-word",
        color: accent || "#0e0e10", fontFamily: mono ? "JetBrains Mono, monospace" : "inherit",
      }}>{value}</span>
    </div>
  );
}

const FORMAT_LABELS = { popup: "Temporary Pop-Up", locker: "Always-On Locker", redemption: "Redemption Store" };
const TAX_LABELS = { zip: "ZIP AutoTax — Automated", flat: "Flat Rate", exempt: "Tax Exempt" };
const SHIP_LABELS = { coach: "Ship to Team Manager", home: "Direct-To-Home Delivery" };

// Grid of the products the manager curated in Step 2, shown on the review step.
function ReviewProducts({ products, accent }) {
  if (!products || products.length === 0) {
    return (
      <WizardPanel label="Products Added" style={{ marginTop: 16, marginBottom: 0 }}>
        <div style={{ fontSize: 13, color: "#9a9aa1", textAlign: "center", padding: "12px 0" }}>
          No products were added to this store.
        </div>
      </WizardPanel>
    );
  }
  return (
    <WizardPanel
      label="Products Added"
      hint={`${products.length} product${products.length === 1 ? "" : "s"}`}
      style={{ marginTop: 16, marginBottom: 0 }}>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(150px, 1fr))", gap: 16 }}>
        {products.map((p, i) => {
          const price = p.adult != null ? p.adult : (p.retail != null ? p.retail : null);
          return (
            <div key={p.id || i} style={{
              border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden", background: "#fff",
            }}>
              <div style={{
                aspectRatio: "1 / 1", background: "#f6f6f8", display: "flex",
                alignItems: "center", justifyContent: "center", overflow: "hidden",
              }}>
                {p.photo
                  ? <div style={{ width: "78%", height: "78%" }}><CatalogPhoto photo={p.photo} alt={p.title || "Product"} /></div>
                  : <IconShirt size={34} style={{ color: "#c7c7cd" }} />}
              </div>
              <div style={{ padding: "12px 12px 12px" }}>
                <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10", lineHeight: 1.3, marginBottom: 4,
                  overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                  {p.title || "Untitled product"}
                </div>
                <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8 }}>
                  <span style={{ fontSize: 11.5, color: "#9a9aa1", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                    {[p.sport, p.category || p.style].filter(Boolean).join(" · ") || "Product"}
                  </span>
                  {price != null && (
                    <span style={{ fontSize: 12.5, fontWeight: 800, color: accent, flexShrink: 0 }}>
                      ${Number(price).toFixed(2)}
                    </span>
                  )}
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </WizardPanel>
  );
}

function estimateYield(data, athletes) {
  const perAthlete = (data.curatedProducts || [])
    .filter((p) => p.teamSetting === "team-required")
    .reduce((sum, p) => sum + (p.teamProfit || 0), 0);
  return perAthlete * athletes;
}

function Step6Review({ data, accent, athletes = 30, stepNum }) {
  const yieldAmt = estimateYield(data, athletes);
  const taxValue = data.collectTax
    ? `Collecting${data.taxState ? ` — ${data.taxState}` : ""} @ ${data.taxRate || "0"}%`
    : "Tax Exempt";
  return (
    <div>
      <StepHeader num={String(stepNum || 3)} accent={accent} title="Review &amp; Deploy"
        desc="Confirm every setting — campaign URL, taxes, logistics, and projected yield — before deploying the store." />

      <div style={{ maxWidth: 720, margin: "0 auto" }}>
        <WizardPanel label="Review Your Setup" style={{ marginBottom: 0 }}>
          <SummaryRowWiz label="Domain Web Slug" mono accent={accent}
            value={`prolookstores.com/${data.storeSlug || "—"}`} />
          <SummaryRowWiz label="Connected Team" value={(WIZARD_TEAMS.find((t) => t.value === data.connectedTeam) || {}).label || "—"} />
          <SummaryRowWiz label="Sports" value={(data.sports && data.sports.length) ? data.sports.join(", ") : "—"} />
          <SummaryRowWiz label="Campaign Type" value={FORMAT_LABELS[data.distributionFormat] || "—"} />
          <SummaryRowWiz label="Point of Contact" value={contactSummary(data)} />
          <SummaryRowWiz label="Compliance Sales Tax Code" value={taxValue} accent={!data.collectTax ? "#1f7a44" : undefined} />
          <SummaryRowWiz label="Duplicate Numbers Default" value={data.allowDuplicateNumbers ? "Allowed — rosters can override" : "Blocked — rosters can override"} />
          <SummaryRowWiz label="Jersey Number Selection" value={(data.numberSelection || "manager") === "player" ? "Players choose their own numbers" : "Team manager chooses for players"} />
          <SummaryRowWiz label="Curated SKUs" value={`${(data.curatedProducts || []).length} Product${(data.curatedProducts || []).length === 1 ? "" : "s"} Curated`} />
          <SummaryRowWiz label="Logistics Shipping Rule" value={SHIP_LABELS[data.distributionModel] || "—"} last={data.distributionModel !== "coach"} />
          {data.distributionModel === "coach" && (
            <SummaryRowWiz label="Ship-To Address"
              value={shipToSummary(data) || "Not set — orders can't be submitted for fulfillment until an address exists"}
              accent={shipToSummary(data) ? undefined : "#b06f1e"} last />
          )}
        </WizardPanel>

        <ReviewProducts products={data.curatedProducts || []} accent={accent} />

        <WizardPanel label={data.storeType === "league" ? "League Managers" : "Team Managers"}
          hint={((data.additionalManagers || []).length + 1) === 1 ? "1 manager" : ((data.additionalManagers || []).length + 1) + " managers"}
          style={{ marginTop: 16, marginBottom: 0 }}>
          {[{ id: "primary", primary: true, firstName: data.firstName, lastName: data.lastName, email: data.email, phone: data.phone }, ...(data.additionalManagers || [])].map((m, i, arr) => {
            const name = [m.firstName, m.lastName].filter(Boolean).join(" ");
            return (
              <div key={m.id || i} style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 0", borderBottom: i === arr.length - 1 ? "none" : "1px solid #ececf0" }}>
                <span style={{ width: 36, height: 36, borderRadius: 9, flexShrink: 0, background: m.primary ? "#0e0e10" : "#f1f1f3", color: m.primary ? "#fff" : "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 12 }}>
                  {(name || "—").split(/\s+/).map((w) => w[0]).join("").slice(0, 2).toUpperCase()}
                </span>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>{name || "Unnamed manager"}</div>
                  <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 1, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{[m.email, m.phone].filter(Boolean).join(" · ") || "No contact details"}</div>
                </div>
                {m.primary && <span title="The Store Point of Contact inherits from this record" style={{ flexShrink: 0, fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#1f7a44", background: "#e7f5ed", borderRadius: 999, padding: "4px 10px", textTransform: "uppercase" }}>Primary Contact</span>}
              </div>
            );
          })}
        </WizardPanel>

        {data.storeType === "league" && (() => {
          const s = data.leagueStructure || { groups: [], loose: [] };
          const nDiv = (s.groups || []).length;
          const nTeam = (s.groups || []).reduce((n, g) => n + (g.rosters || []).length, 0) + (s.loose || []).length;
          return (
            <WizardPanel label="Divisions &amp; Teams" hint={nDiv + (nDiv === 1 ? " division · " : " divisions · ") + nTeam + (nTeam === 1 ? " team" : " teams")} style={{ marginTop: 16, marginBottom: 0 }}>
              {nDiv === 0 && nTeam === 0
                ? <div style={{ fontSize: 13, color: "#9a9aa1", textAlign: "center", padding: "12px 0" }}>No divisions or teams yet — add them anytime from the My Team(s) tab.</div>
                : <div style={{ display: "flex", flexDirection: "column", gap: 14 }}>
                    {(s.groups || []).map((g) => (
                      <div key={g.id}>
                        <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8 }}>
                          <span style={{ width: 10, height: 10, borderRadius: 3, background: g.color || "#3f3f46", flexShrink: 0 }} />
                          <span style={{ fontSize: 13, fontWeight: 800, color: "#0e0e10" }}>{g.name}</span>
                          <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{(g.rosters || []).length} {(g.rosters || []).length === 1 ? "team" : "teams"}</span>
                        </div>
                        <div style={{ display: "flex", flexWrap: "wrap", gap: 6, paddingLeft: 18 }}>
                          {(g.rosters || []).length === 0
                            ? <span style={{ fontSize: 12, color: "#c0c0c6", fontStyle: "italic" }}>No teams yet</span>
                            : (g.rosters || []).map((t) => <span key={t.id} style={{ fontSize: 11.5, fontWeight: 700, color: "#4b4b52", background: "#f4f4f6", borderRadius: 999, padding: "4px 10px" }}>{t.name}</span>)}
                        </div>
                      </div>
                    ))}
                    {(s.loose || []).length > 0 && (
                      <div>
                        <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>Standalone Teams</div>
                        <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
                          {(s.loose || []).map((t) => <span key={t.id} style={{ fontSize: 11.5, fontWeight: 700, color: "#4b4b52", background: "#f4f4f6", borderRadius: 999, padding: "4px 10px" }}>{t.name}</span>)}
                        </div>
                      </div>
                    )}
                  </div>}
            </WizardPanel>
          );
        })()}
      </div>

      <p style={{ fontSize: 12.5, color: "#9a9aa1", textAlign: "center", margin: "24px auto 0", maxWidth: 620, lineHeight: 1.55, fontStyle: "italic" }}>
        On initialization, products index into centralized factory inventory and launch configurations propagate online immediately.
      </p>
    </div>
  );
}

// Compact "point of contact" summary for the review step.
function contactSummary(data) {
  const emailVal = data.contactEmailMode === "salesperson" ? ((SALESPEOPLE.find((s) => s.value === data.contactEmailSalesperson) || {}).email || "Salesperson")
    : data.contactEmailMode === "custom" ? (data.contactEmailCustom || "Custom email")
    : (data.email || "Team Manager email");
  return emailVal;
}

// darken/lighten a hex by ratio (-1..1)
function shade(hex, ratio) {
  const h = (hex || "#e1251b").replace("#", "");
  let r = parseInt(h.slice(0, 2), 16), g = parseInt(h.slice(2, 4), 16), b = parseInt(h.slice(4, 6), 16);
  const adj = (c) => Math.max(0, Math.min(255, Math.round(c + 255 * ratio)));
  r = adj(r); g = adj(g); b = adj(b);
  return `#${[r, g, b].map((c) => c.toString(16).padStart(2, "0")).join("")}`;
}

function slugify(s) {
  return (s || "").toLowerCase().trim()
    .replace(/&/g, "and").replace(/[^a-z0-9\s-]/g, "").replace(/\s+/g, "-").replace(/-+/g, "-").replace(/^-|-$/g, "");
}

Object.assign(window, {
  WizardStepper, WIZARD_STEPS, WIZARD_STEPS_LEAGUE, wizardStepsFor,
  Step1Basics, Step2Structure, Step3Products, Step4Compliance, Step5Design, Step6Review,
  ShipToAddressFields, shipToSummary, NumberSelectionOptions,
  estimateYield, slugify, shade, hexA, FORMAT_LABELS, TAX_LABELS, SHIP_LABELS, WIZARD_CATALOG,
});


/* ===== ASSET 16 : 8a802186-a9f0-4a95-bb9a-d60ff66980a2 ===== */
// Sales Tax Collection modal + Cancel confirmation modal + Loading overlay.

function SalesTaxModal({ open, onClose, onConfirm }) {
  const [agreed, setAgreed] = React.useState(false);
  React.useEffect(() => { if (!open) setAgreed(false); }, [open]);
  return (
    <Modal open={open} onClose={onClose} width={460}>
      <div style={{ padding: "24px 24px 16px" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
          <h3 style={{ fontSize: 14, fontWeight: 800, letterSpacing: ".06em", margin: 0 }}>SALES TAX COLLECTION</h3>
          <button onClick={onClose} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
            <IconX size={18} />
          </button>
        </div>
        <p style={{ fontSize: 13, color: "#3a3a40", margin: 0, marginBottom: 16, lineHeight: 1.55 }}>
          To help you manage your business, you can enable automated sales tax collection at checkout. Please note that by enabling this feature, you acknowledge that PROLOOK provides the tools for collection but does not provide tax advice.
        </p>
        <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Terms of Responsibility:</div>
        <ul style={{ fontSize: 13, color: "#3a3a40", margin: 0, marginBottom: 16, paddingLeft: 16, lineHeight: 1.6 }}>
          <li><b>Accuracy:</b> You are responsible for ensuring the tax rate entered is accurate for your jurisdiction.</li>
          <li><b>Remittance:</b> You are solely responsible for filing and remitting all collected taxes to the appropriate state and local authorities.</li>
          <li><b>Liability:</b> PROLOOK is not liable for any under-collection, over-collection, or failure to file taxes on your behalf.</li>
        </ul>
        <div style={{
          background: "#fafafb", border: "1px solid #ececf0", borderRadius: 6,
          padding: "12px 16px", marginBottom: 16,
        }}>
          <Checkbox
            checked={agreed}
            onChange={setAgreed}
            label="I have read the terms and acknowledge my responsibility as the taxing entity for collecting and remitting sales tax."
          />
        </div>
        <Button
          variant="primary"
          disabled={!agreed}
          onClick={onConfirm}
          style={{ width: "100%", padding: "16px", fontSize: 13 }}>
          I Agree & Create Store
        </Button>
      </div>
    </Modal>
  );
}

function CancelConfirmModal({ open, onClose, onConfirm }) {
  return (
    <Modal open={open} onClose={onClose} width={420}>
      <div style={{ padding: "28px 24px 20px" }}>
        <div style={{
          width: 44, height: 44, borderRadius: "50%",
          background: "#fff3f2", color: "#b81d14",
          display: "inline-flex", alignItems: "center", justifyContent: "center",
          marginBottom: 16,
        }}>
          <IconAlert size={20} />
        </div>
        <h3 style={{ fontSize: 16, fontWeight: 700, margin: 0, marginBottom: 8 }}>Discard this store?</h3>
        <p style={{ fontSize: 13, color: "#6b6b72", margin: 0, marginBottom: 24, lineHeight: 1.55 }}>
          You'll lose any details you've entered. This can't be undone.
        </p>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12 }}>
          <Button variant="ghost" onClick={onClose}>Keep Editing</Button>
          <Button variant="danger" onClick={onConfirm}>Discard</Button>
        </div>
      </div>
    </Modal>
  );
}

function LoadingOverlay({ visible }) {
  if (!visible) return null;
  return (
    <div style={{
      position: "fixed", inset: 0, zIndex: 200,
      background: "rgba(255,255,255,.94)",
      display: "flex", alignItems: "center", justifyContent: "center",
      animation: "fadeIn .2s ease",
    }}>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 16 }}>
        <div style={{ display: "flex", gap: 8 }}>
          {[0, 1, 2].map((i) => (
            <span key={i} style={{
              display: "inline-block", width: 8, height: 44, borderRadius: 1,
              background: "#0e0e10",
              animation: `bounce 1.1s ease-in-out ${i * 0.15}s infinite`,
            }} />
          ))}
        </div>
        <div style={{ fontSize: 13, color: "#6b6b72", letterSpacing: ".04em" }}>Creating your store…</div>
        <style>{`
          @keyframes bounce {
            0%, 100% { transform: scaleY(.4); opacity: .5 }
            50% { transform: scaleY(1); opacity: 1 }
          }
        `}</style>
      </div>
    </div>
  );
}

Object.assign(window, { SalesTaxModal, CancelConfirmModal, LoadingOverlay });


/* ===== ASSET 17 : b02891e8-2991-4772-a55b-1f7a4686257b ===== */
// Launch Team Store flow — confirmation modal, success modal, and the
// "live" header controls (View Store link, status pill, store switcher).

// ─────────── 1) Launch confirmation modal ───────────
function LaunchStoreModal({ open, onClose, onConfirm }) {
  return (
    <Modal open={open} onClose={onClose} width={640}>
      <div style={{ padding: "32px 36px 36px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 16 }}>
          <h2 style={{ fontSize: 26, fontWeight: 800, margin: 0, letterSpacing: ".005em", color: "#0e0e10" }}>
            Launch Team Store
          </h2>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#0e0e10",
            display: "inline-flex", marginTop: 2
          }}>
            <IconX size={22} style={{ strokeWidth: 2 }} />
          </button>
        </div>

        <p style={{ fontSize: 16, color: "#9a9aa1", margin: "0 0 28px", fontWeight: 500 }}>
          Ready to launch your new store?
        </p>

        <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10", marginBottom: 16 }}>
          By confirming to launch the team store:
        </div>
        <ul style={{ margin: 0, padding: 0, listStyle: "none", display: "flex", flexDirection: "column", gap: 12 }}>
          {[
          "Your storefront goes live at its public link — share the link and team codes so players, parents, and fans can shop. No roster is required; rosters can fill in from team-gear orders.",
          "You can put the store on hold at any time in the Team Stores menu, preventing users to access to the site."].
          map((t, i) =>
          <li key={i} style={{ display: "flex", gap: 12, fontSize: 15, color: "#3a3a40", lineHeight: 1.55 }}>
              <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#3a3a40", flexShrink: 0, marginTop: 8 }} />
              <span>{t}</span>
            </li>
          )}
        </ul>

        <div style={{ display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 28, marginTop: 36 }}>
          <button onClick={onClose} style={{
            background: "transparent", border: 0, cursor: "pointer",
            fontSize: 15, fontWeight: 700, color: "#0e0e10", fontFamily: "inherit",
            letterSpacing: ".04em", padding: "8px 4px"
          }}>CANCEL</button>
          <button onClick={onConfirm} style={{
            background: "#0e0e10", border: 0, borderRadius: 6, cursor: "pointer",
            fontSize: 15, fontWeight: 700, color: "#fff", fontFamily: "inherit",
            padding: "16px 28px"
          }}>Launch Team Store</button>
        </div>
      </div>
    </Modal>);

}

// ─────────── 2) Success modal ───────────
function LaunchSuccessModal({ open, store, onClose, onViewStore, onStayInEditor }) {
  return (
    <Modal open={open} onClose={onClose} width={560}>
      <div style={{ padding: "20px 36px 36px" }}>
        <div style={{ display: "flex", justifyContent: "flex-end" }}>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#0e0e10", display: "inline-flex"
          }}>
            <IconX size={22} style={{ strokeWidth: 2 }} />
          </button>
        </div>

        <div style={{ display: "flex", flexDirection: "column", alignItems: "center", textAlign: "center", padding: "4px 8px 0" }}>
          <div style={{
            width: 64, height: 64, borderRadius: "50%", background: "#f1f1f3",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            color: "#0e0e10", marginBottom: 20
          }}>
            <IconCircleCheck size={34} style={{ strokeWidth: 1.8 }} />
          </div>
          <h2 style={{ fontSize: 22, fontWeight: 800, margin: "0 0 12px", color: "#0e0e10" }}>
            Your store is now live!
          </h2>
          <p style={{ fontSize: 15, color: "#9a9aa1", margin: "0 0 24px", lineHeight: 1.55, maxWidth: 440, fontWeight: 500 }}>
            Congratulations! Your store is live at its public link. Share the link and team codes with your teams — no roster needed, team-gear orders can build it for you.
          </p>

          <ShareLinkRow slug={store?.storeSlug} />
          <p style={{ fontSize: 12.5, color: "#9a9aa1", margin: "0 0 28px", lineHeight: 1.55, maxWidth: 420 }}>
            Team codes live on each team's node in My Teams — buyers enter one at checkout for team gear, and player emails send as their orders arrive.
          </p>

          <div style={{ display: "flex", gap: 16, width: "100%", justifyContent: "center" }}>
            <button onClick={onViewStore} style={{
              flex: "0 1 220px", background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
              cursor: "pointer", fontSize: 15, fontWeight: 700, color: "#0e0e10", fontFamily: "inherit",
              padding: "16px 20px", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 12
            }}>
              <IconExternal size={18} style={{ strokeWidth: 1.9 }} /> View Store
            </button>
            <button onClick={onStayInEditor} style={{
              flex: "0 1 220px", background: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6,
              cursor: "pointer", fontSize: 15, fontWeight: 700, color: "#fff", fontFamily: "inherit",
              padding: "16px 20px", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 12
            }}>
              <IconClipboard size={18} style={{ strokeWidth: 1.9 }} /> Stay In Editor
            </button>
          </div>
        </div>
      </div>
    </Modal>);

}

// Rep-distributed share link — the launch "email" has no player audience under
// roster-optional stores, so launch hands the rep a link + team codes instead.
function ShareLinkRow({ slug }) {
  const [copied, setCopied] = React.useState(false);
  const url = `prolookstores.com/${slug || "your-store"}`;
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 12, width: "100%", boxSizing: "border-box", background: "#fafafb", border: "1px solid #ececf0", borderRadius: 8, padding: "10px 10px 10px 16px", marginBottom: 12 }}>
      <span style={{ flex: 1, minWidth: 0, textAlign: "left", fontFamily: "JetBrains Mono, monospace", fontSize: 13, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{url}</span>
      <button onClick={() => { try { navigator.clipboard?.writeText("https://" + url); } catch (e) {} setCopied(true); setTimeout(() => setCopied(false), 1600); }} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: copied ? "#e7f5ed" : "#fff", border: `1px solid ${copied ? "#1f9d55" : "#d4d4d8"}`, color: copied ? "#1f7a44" : "#0e0e10", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{copied ? "Copied" : "Copy Share Link"}</button>
    </div>);
}

// ─────────── Small logo chip reused by the controls ───────────
function StoreLogoChip({ store, size = 22 }) {
  if (store?.teamLogo?.url) {
    return <img src={store.teamLogo.url} alt="" style={{ width: size, height: size, borderRadius: 3, objectFit: "cover", flexShrink: 0 }} />;
  }
  return (
    <span style={{
      width: size, height: size, borderRadius: 3, flexShrink: 0,
      background: store?.teamLogo?.color || store?.teamColors?.[0] || "#e1e2dd",
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      color: "#0e0e10", fontWeight: 800, fontSize: size * 0.5
    }}>
      {store?.teamLogo?.mono || (store?.teamName || store?.storeTitle || "T")[0]?.toUpperCase()}
    </span>);

}

// ─────────── 3) Live header controls ───────────
// VIEW STORE link + ACTIVE/ON HOLD status pill (dropdown) + store switcher.
function LiveStoreControls({ store, liveStores = [], onViewStore, onSwitchStore, onSetStatus }) {
  const [switchOpen, setSwitchOpen] = React.useState(false);
  const switchRef = React.useRef();
  const onHold = store.status === "hold";

  React.useEffect(() => {
    const h = (e) => {
      if (switchRef.current && !switchRef.current.contains(e.target)) setSwitchOpen(false);
    };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);

  const pillBg = onHold ? "#fdf2d6" : "#d6f5e3";
  const pillInk = onHold ? "#9a6a05" : "#1f8a55";

  return (
    <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
      {/* VIEW STORE link */}
      <button onClick={onViewStore} style={{
        background: "transparent", border: 0, cursor: "pointer", color: "#0e0e10",
        display: "inline-flex", alignItems: "center", gap: 8,
        fontSize: 13, fontWeight: 700, letterSpacing: ".06em", fontFamily: "inherit"
      }}>
        <IconExternal size={17} style={{ strokeWidth: 1.9 }} /> View Store
      </button>

      {/* Status pill (read-only label) */}
      <span style={{
        background: pillBg, borderRadius: 999,
        color: pillInk, padding: "8px 16px",
        display: "inline-flex", alignItems: "center", gap: 8,
        fontSize: 12, fontWeight: 800, letterSpacing: ".08em", userSelect: "none",
      }}>
        <span style={{ width: 8, height: 8, borderRadius: "50%", background: onHold ? "#d4a017" : "#1f8a55", flexShrink: 0 }} />
        {onHold ? "ON HOLD" : "Active"}
      </span>

      {/* Store switcher */}
      <div ref={switchRef} style={{ position: "relative" }}>
        <button onClick={() => {setSwitchOpen((v) => !v);setStatusOpen(false);}} style={{
          background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, cursor: "pointer",
          padding: "8px 12px", display: "inline-flex", alignItems: "center", gap: 12,
          fontFamily: "inherit", minWidth: 172
        }}>
          <StoreLogoChip store={store} size={22} />
          <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", flex: 1, textAlign: "left", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
            {store.teamName || store.storeTitle || "[TEAM NAME]"}
          </span>
          <IconChevron size={16} style={{ transform: `rotate(${switchOpen ? 180 : 0}deg)`, transition: "transform .15s", color: "#6b6b72" }} />
        </button>
        {switchOpen &&
        <div style={{
          position: "absolute", top: "calc(100% + 8px)", right: 0, minWidth: 240,
          background: "#fff", border: "1px solid #e5e5e9", borderRadius: 8,
          boxShadow: "0 10px 28px rgba(20,20,30,.14)", zIndex: 60, overflow: "hidden", padding: 4
        }}>
            <div style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".10em", color: "#9a9aa1", padding: "8px 12px 8px" }}>
              LIVE STORES
            </div>
            {liveStores.map((s) => {
            const isCurrent = s.id === store.id;
            return (
              <button key={s.id} onClick={() => {onSwitchStore(s.id);setSwitchOpen(false);}} style={{
                width: "100%", background: isCurrent ? "#f7f7f8" : "transparent", border: 0, cursor: "pointer",
                padding: "8px 12px", borderRadius: 6, display: "flex", alignItems: "center", gap: 12, fontFamily: "inherit"
              }}
              onMouseEnter={(e) => {if (!isCurrent) e.currentTarget.style.background = "#fafafb";}}
              onMouseLeave={(e) => {if (!isCurrent) e.currentTarget.style.background = "transparent";}}>
                  <StoreLogoChip store={s} size={22} />
                  <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", flex: 1, textAlign: "left" }}>
                    {s.teamName || s.storeTitle}
                  </span>
                  <span style={{
                  fontSize: 9, fontWeight: 800, letterSpacing: ".06em",
                  color: s.status === "hold" ? "#9a6a05" : "#1f8a55",
                  background: s.status === "hold" ? "#fdf2d6" : "#d6f5e3",
                  borderRadius: 999, padding: "4px 8px"
                }}>{s.status === "hold" ? "ON HOLD" : "ACTIVE"}</span>
                  {isCurrent && <IconCheck size={15} style={{ color: "#0e0e10" }} />}
                </button>);

          })}
          </div>
        }
      </div>
    </div>);

}

function StatusOption({ label, desc, dotColor, active, onClick }) {
  return (
    <button onClick={onClick} style={{
      width: "100%", background: active ? "#f7f7f8" : "transparent", border: 0, cursor: "pointer",
      padding: "12px 12px", borderRadius: 6, display: "flex", alignItems: "flex-start", gap: 12, fontFamily: "inherit", textAlign: "left"
    }}
    onMouseEnter={(e) => {if (!active) e.currentTarget.style.background = "#fafafb";}}
    onMouseLeave={(e) => {if (!active) e.currentTarget.style.background = "transparent";}}>
      <span style={{ width: 8, height: 8, borderRadius: "50%", background: dotColor, flexShrink: 0, marginTop: 4 }} />
      <span style={{ flex: 1 }}>
        <span style={{ display: "block", fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{label}</span>
        <span style={{ display: "block", fontSize: 11, color: "#9a9aa1", marginTop: 1 }}>{desc}</span>
      </span>
      {active && <IconCheck size={15} style={{ color: "#0e0e10", marginTop: 2 }} />}
    </button>);

}

Object.assign(window, { LaunchStoreModal, LaunchSuccessModal, LiveStoreControls });

/* ===== ASSET 18 : 38d95154-b765-4cf7-969f-c900eb92cb12 ===== */
// Edit modals for the Settings page — Store Details + Shipping/Tax.
// Reuses the shared UI primitives (TextField, SelectField, PhoneField, Checkbox, Modal).

const EG_SPORTS = ["Baseball", "Basketball", "Football", "Hockey", "Lacrosse", "Soccer", "Softball", "Track & Field", "Volleyball", "Wrestling"];
const EG_CONTACT = ["Email Only", "Phone Only", "Email & Phone", "Text Message"];
const EG_COUNTRIES = ["United States", "Canada", "Mexico"];
// Two-letter postal abbreviations for type-ahead state search (Epic G).
const TAX_STATE_ABBR = { Alabama: "AL", Alaska: "AK", Arizona: "AZ", Arkansas: "AR", California: "CA", Colorado: "CO", Connecticut: "CT", Delaware: "DE", Florida: "FL", Georgia: "GA", Hawaii: "HI", Idaho: "ID", Illinois: "IL", Indiana: "IN", Iowa: "IA", Kansas: "KS", Kentucky: "KY", Louisiana: "LA", Maine: "ME", Maryland: "MD", Massachusetts: "MA", Michigan: "MI", Minnesota: "MN", Mississippi: "MS", Missouri: "MO", Montana: "MT", Nebraska: "NE", Nevada: "NV", "New Hampshire": "NH", "New Jersey": "NJ", "New Mexico": "NM", "New York": "NY", "North Carolina": "NC", "North Dakota": "ND", Ohio: "OH", Oklahoma: "OK", Oregon: "OR", Pennsylvania: "PA", "Rhode Island": "RI", "South Carolina": "SC", "South Dakota": "SD", Tennessee: "TN", Texas: "TX", Utah: "UT", Vermont: "VT", Virginia: "VA", Washington: "WA", "West Virginia": "WV", Wisconsin: "WI", Wyoming: "WY" };
// Type-ahead single-select over EG_STATES; filters by name or abbreviation and
// stays browsable when nothing is typed. Selection auto-fills the tax rate.
function StateSearchSelect({ label = "State", value, onChange }) {
  const [q, setQ] = React.useState("");
  const [open, setOpen] = React.useState(false);
  const [hi, setHi] = React.useState(0);
  const wrapRef = React.useRef(null);
  React.useEffect(() => {
    const h = (e) => { if (wrapRef.current && !wrapRef.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const ql = q.trim().toLowerCase();
  const list = EG_STATES.filter((s) => !ql || s.toLowerCase().includes(ql) || (TAX_STATE_ABBR[s] || "").toLowerCase().startsWith(ql));
  const pick = (s) => { onChange(s); setQ(""); setOpen(false); };
  return (
    <div ref={wrapRef} style={{ position: "relative" }}>
      <div style={{ fontSize: 12.5, fontWeight: 600, color: "#3f3f46", marginBottom: 7 }}>{label}</div>
      <input
        value={open ? q : (value || "")}
        placeholder={value || "Search by state or abbreviation"}
        onFocus={() => { setOpen(true); setQ(""); setHi(0); }}
        onChange={(e) => { setQ(e.target.value); setOpen(true); setHi(0); }}
        onKeyDown={(e) => {
          if (e.key === "ArrowDown") { e.preventDefault(); setHi((h) => Math.min(h + 1, list.length - 1)); }
          else if (e.key === "ArrowUp") { e.preventDefault(); setHi((h) => Math.max(h - 1, 0)); }
          else if (e.key === "Enter") { e.preventDefault(); if (list[hi]) pick(list[hi]); }
          else if (e.key === "Escape") setOpen(false);
        }}
        style={{ width: "100%", height: 44, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 5, padding: "0 12px", fontSize: 13.5, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }} />
      {open && (
        <div style={{ position: "absolute", left: 0, right: 0, top: "calc(100% + 4px)", zIndex: 60, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 8, boxShadow: "0 12px 28px rgba(20,20,30,.14)", maxHeight: 240, overflowY: "auto", padding: 4 }}>
          {list.length === 0 && <div style={{ padding: "12px 12px", fontSize: 12.5, color: "#9a9aa1" }}>No state matches "{q}"</div>}
          {list.map((s, i) => (
            <button key={s} type="button" onMouseEnter={() => setHi(i)} onClick={() => pick(s)} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 10, width: "100%", textAlign: "left", background: i === hi ? "#f4f4f5" : "#fff", border: 0, borderRadius: 6, padding: "9px 10px", fontSize: 13, fontWeight: value === s ? 700 : 500, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>
              <span>{s}</span>
              <span style={{ fontSize: 11, fontWeight: 700, color: "#9a9aa1" }}>{TAX_STATE_ABBR[s] || ""}</span>
            </button>
          ))}
        </div>
      )}
    </div>
  );
}
const EG_STATES = [
  "Alabama","Alaska","Arizona","Arkansas","California","Colorado","Connecticut","Delaware","Florida","Georgia",
  "Hawaii","Idaho","Illinois","Indiana","Iowa","Kansas","Kentucky","Louisiana","Maine","Maryland",
  "Massachusetts","Michigan","Minnesota","Mississippi","Missouri","Montana","Nebraska","Nevada","New Hampshire","New Jersey",
  "New Mexico","New York","North Carolina","North Dakota","Ohio","Oklahoma","Oregon","Pennsylvania","Rhode Island","South Carolina",
  "South Dakota","Tennessee","Texas","Utah","Vermont","Virginia","Washington","West Virginia","Wisconsin","Wyoming",
];

// ─────────── Shared modal chrome ───────────
function EditModalShell({ open, onClose, title, onSave, saveLabel = "Save Changes", width = 620, children }) {
  return (
    <Modal open={open} onClose={onClose} width={width}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "86vh" }}>
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          padding: "24px 28px", borderBottom: "1px solid #ececf0", flexShrink: 0,
        }}>
          <h2 style={{ fontSize: 16, fontWeight: 800, letterSpacing: ".04em", margin: 0 }}>{title}</h2>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 8, color: "#0e0e10", display: "inline-flex",
          }}>
            <IconX size={20} style={{ strokeWidth: 2 }} />
          </button>
        </div>
        <div style={{ padding: "28px 28px", overflowY: "auto", display: "flex", flexDirection: "column", gap: 32 }}>
          {children}
        </div>
        <div style={{
          padding: "16px 28px", borderTop: "1px solid #ececf0",
          display: "flex", justifyContent: "flex-end", gap: 12, flexShrink: 0, background: "#fff",
        }}>
          <Button variant="ghost" onClick={onClose}>Cancel</Button>
          <Button variant="primary" onClick={onSave}>{saveLabel}</Button>
        </div>
      </div>
    </Modal>
  );
}

function FieldLabelEG({ children }) {
  return <div style={{ fontSize: 11, fontWeight: 600, color: "#6b6b72", marginBottom: 8, paddingLeft: 4 }}>{children}</div>;
}

// ─────────── Team Fundraising — disable interstitial ───────────
// Consequence-tier review shown BEFORE the store-level switch is applied. Items
// with a non-cancelled order are exempt — fundraising can't be removed from
// money already committed — and keep collecting. Entered amounts are kept.
function FundraisingDisableModal({ open, store, onCancel, onConfirm }) {
  if (!open) return null;
  const collecting = fundCollectingItems(store);
  const exempt = collecting.filter((p) => pteLockState(p, store) === "full");
  const disabling = collecting.filter((p) => pteLockState(p, store) !== "full");
  const row = (p, lockNote) => (
    <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 12, padding: "10px 0", borderBottom: "1px solid #f1f1f3" }}>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 13.5, fontWeight: 600, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{p.title || "[product]"}{p.isBundle ? " · Bundle" : ""}</div>
        {lockNote && <div style={{ fontSize: 11.5, color: "#9a6b15", marginTop: 1 }}>Has orders — fundraising stays on for this item.</div>}
      </div>
      <span style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10", fontFamily: "JetBrains Mono, monospace", flexShrink: 0 }}>{itemFundLabel(p)}</span>
    </div>
  );
  return (
    <Modal open={open} onClose={onCancel} width={600}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "82vh" }}>
        <div style={{ padding: "28px 32px 0", flexShrink: 0 }}>
          <div style={{ display: "flex", alignItems: "flex-start", gap: 14, marginBottom: 8 }}>
            <span style={{ width: 40, height: 40, borderRadius: 9, background: "#fdecea", color: "#e1251b", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}><IconAlert size={20} /></span>
            <div>
              <h2 style={{ fontSize: 20, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Disable Team Fundraising?</h2>
              <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5, marginTop: 4 }}>
                Fundraising stops on the items below. Entered amounts are kept and come back if fundraising is re-enabled.
              </div>
            </div>
          </div>
        </div>
        <div style={{ padding: "4px 32px 12px", overflowY: "auto", flex: 1, minHeight: 0 }}>
          <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", margin: "12px 0 4px" }}>WILL BE DISABLED · {disabling.length}</div>
          {disabling.length === 0
            ? <div style={{ fontSize: 12.5, color: "#9a9aa1", padding: "10px 0" }}>No items to disable.</div>
            : disabling.map((p) => row(p, false))}
          {exempt.length > 0 && (
            <React.Fragment>
              <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", margin: "20px 0 4px" }}>CANNOT BE DISABLED · {exempt.length}</div>
              <div style={{ fontSize: 12, color: "#6b6b72", lineHeight: 1.5, marginBottom: 4 }}>These items already have orders, so their fundraising can't be removed — they keep collecting.</div>
              {exempt.map((p) => row(p, true))}
            </React.Fragment>
          )}
        </div>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 16, padding: "16px 32px", borderTop: "1px solid #ececf0", flexShrink: 0 }}>
          <button onClick={onCancel} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8, padding: "12px 24px", fontSize: 14, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
          <button onClick={onConfirm} style={{ background: "#e1251b", border: 0, borderRadius: 8, padding: "12px 24px", fontSize: 14, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>
            Disable Fundraising{disabling.length > 0 ? " (" + disabling.length + ")" : ""}
          </button>
        </div>
      </div>
    </Modal>
  );
}

// ─────────── Store Details ───────────
function EditStoreDetailsModal({ open, store, onClose, onSave, accent = "#e1251b" }) {
  const [f, setF] = React.useState({});
  const [teamModalOpen, setTeamModalOpen] = React.useState(false);
  const [fundOffReview, setFundOffReview] = React.useState(false); // disable interstitial
  React.useEffect(() => {
    if (!open) return;
    setF({
      storeTitle: store.storeTitle || "",
      teamName: store.teamName || "",
      sport: store.sport || "",
      contactPref: store.contactPref || "",
      email: store.email || "",
      phone: store.phone || "",
      storeOpenMode: store.storeOpenMode || "set-window",
      storeOpenStart: store.storeOpenStart || "",
      storeOpenEnd: store.storeOpenEnd || "",
      connectedTeam: store.connectedTeam || "",
      teamLogo: store.teamLogo || null,
      teamColors: store.teamColors || [],
      sports: store.sports && store.sports.length ? store.sports : (store.sport ? [store.sport] : []),
      primaryColor: store.primaryColor || "",
      distributionModel: store.distributionModel || "coach",
      allowDuplicateNumbers: store.allowDuplicateNumbers ?? false,
      fundraisingEnabled: store.fundraisingEnabled === true,
      collectTax: store.collectTax ?? true,
      taxState: store.taxState || "",
      taxRate: store.taxRate || "",
      contactEmailMode: store.contactEmailMode || "teammanager",
      contactEmailSalesperson: store.contactEmailSalesperson || "",
      contactEmailCustom: store.contactEmailCustom || "",
      contactPhoneMode: store.contactPhoneMode || "teammanager",
      contactPhoneSalesperson: store.contactPhoneSalesperson || "",
      contactPhoneCustom: store.contactPhoneCustom || "",
    });
    setTeamModalOpen(false);
    setFundOffReview(false);
  }, [open, store]);
  const set = (k, v) => setF((p) => ({ ...p, [k]: v }));
  const setMany = (patch) => setF((p) => ({ ...p, ...patch }));

  const connectedTeam = TEAM_DATABASE.find((t) => t.id === f.connectedTeam);
  const connectedLabel = (connectedTeam || {}).name || f.teamName;

  // Inherit the selected team's official branding — mirrors Step 1 of the wizard.
  const applyTeam = (t) => {
    setMany({
      connectedTeam: t.id,
      teamName: t.name,
      storeTitle: f.storeTitle && f.storeTitle.trim() ? f.storeTitle : t.name,
      teamLogo: { mono: t.mono, color: t.logoColor },
      teamColors: [t.primary, t.secondary, "#ffffff"],
      primaryColor: t.primary,
      sports: t.sports,
      sport: t.sports[0] || f.sport,
    });
    setTeamModalOpen(false);
  };

  return (
    <EditModalShell open={open} onClose={onClose} title="EDIT STORE DETAILS" width={806} onSave={() => onSave(f)}>
      <div style={{ display: "flex", gap: 16 }}>
        <TextField label="Store Name" value={f.storeTitle} onChange={(v) => set("storeTitle", v)} />
        <TextField label="Team Name" value={f.teamName} onChange={(v) => set("teamName", v)} />
      </div>

      {/* Team Branding & Sport — same Connect-to-Team component as wizard Step 1 */}
      <div>
        <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>Team Branding &amp; Sport</div>

        {/* Import Team Profile — opens the team database modal */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between", gap: 20,
          border: `1.5px solid ${f.connectedTeam ? accent : "#e5e5e9"}`, borderRadius: 10,
          background: f.connectedTeam ? "#fdf2f1" : "#fafafb", padding: "16px 20px", marginBottom: 24,
        }}>
          <div style={{ display: "flex", alignItems: "flex-start", gap: 16, minWidth: 0 }}>
            <span style={{
              width: 44, height: 44, borderRadius: 9, flexShrink: 0,
              background: f.connectedTeam ? accent : "#0e0e10", color: "#fff",
              display: "inline-flex", alignItems: "center", justifyContent: "center",
            }}><IconGroupAdd size={20} /></span>
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", marginBottom: 4 }}>
                {f.connectedTeam ? `Connected — ${connectedLabel}` : "Import Team Profile"}
              </div>
              <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5, maxWidth: 420 }}>
                {f.connectedTeam
                  ? "Official logo, colors, and sports were imported below. Reconnect to switch teams."
                  : "Connect directly to our team database to automatically import the official school colors and logos used on your physical player uniforms."}
              </div>
            </div>
          </div>
          <button type="button" onClick={() => setTeamModalOpen(true)} style={{
            flexShrink: 0, height: 44, padding: "0 20px", border: 0, borderRadius: 6,
            background: "#0e0e10", color: "#fff", fontSize: 13, fontWeight: 700, letterSpacing: ".01em",
            cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, transition: "background .14s",
          }}
            onMouseEnter={(e) => e.currentTarget.style.background = accent}
            onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
            {f.connectedTeam ? "Change Team" : "Connect to Team"}
          </button>
        </div>

        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 24 }}>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: 20 }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 16 }}>Team Logo</div>
            <LogoDropZone logo={f.teamLogo} onChange={(v) => set("teamLogo", v)} />
            <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 12, lineHeight: 1.5 }}>Used to brand the storefront header and pre-set product customization.</div>
          </div>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: 20 }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 16 }}>Team Colors</div>
            <ColorSwatches colors={f.teamColors || []} onChange={(v) => set("teamColors", v)} />
          </div>
        </div>

        <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 16 }}>Sports <span style={{ color: "#9a9aa1", fontWeight: 500 }}>· select all that apply</span></div>
        <MultiSelectChips options={WIZARD_SPORTS} value={f.sports} onChange={(v) => setMany({ sports: v, sport: v[0] || "" })} accent={accent} />
      </div>

      {/* Team Fundraising */}
      <div>
        <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>Team Fundraising</div>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <span style={{
            width: 40, height: 40, borderRadius: 9, flexShrink: 0,
            background: f.fundraisingEnabled ? "#fdecea" : "#f1f1f3", color: f.fundraisingEnabled ? accent : "#6b6b72",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
          }}><IconDollar size={20} /></span>
          <div style={{ flex: 1 }}>
            <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Enable Team Fundraising</div>
            <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
              {f.fundraisingEnabled
                ? "Items can collect a per-item fundraising amount, and a Fundraising Report is available under Reports."
                : "New items stop collecting fundraising. Items with orders keep collecting, and entered amounts are kept."}
            </div>
          </div>
          <ToggleSwitch on={f.fundraisingEnabled} onChange={(v) => {
            // Switching OFF with collecting items fires the review interstitial BEFORE applying.
            if (!v && fundCollectingItems(store).length > 0) setFundOffReview(true);
            else set("fundraisingEnabled", v);
          }} accent={accent} />
        </div>
      </div>

      {/* Compliance & Taxation */}
      <div>
        <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>Compliance &amp; Taxation</div>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <span style={{
            width: 40, height: 40, borderRadius: 9, flexShrink: 0,
            background: f.collectTax ? "#fdecea" : "#f1f1f3", color: f.collectTax ? accent : "#6b6b72",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
          }}><IconDollar size={20} /></span>
          <div style={{ flex: 1 }}>
            <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Collect Sales Tax</div>
            <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
              {f.collectTax
                ? "Tax is applied to buyers in your designated state at checkout."
                : "Tax-exempt checkout — no tax is applied and no extra inputs are required."}
            </div>
          </div>
          <ToggleSwitch on={f.collectTax} onChange={(v) => set("collectTax", v)} accent={accent} />
        </div>
        {f.collectTax && (
          <div style={{ marginTop: 16, paddingTop: 16, borderTop: "1px solid #f1f1f3", display: "flex", flexDirection: "column", gap: 16 }}>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
              <StateSearchSelect label="State" value={f.taxState}
                onChange={(v) => setMany({ taxState: v, taxRate: STATE_TAX_RATES[v] != null ? String(STATE_TAX_RATES[v]) : f.taxRate })} />
              <TextField label="Tax Rate %" value={f.taxState ? f.taxRate : ""} readOnly
                placeholder="Select a state"
                suffix={<span style={{ color: "#9a9aa1", fontSize: 13 }}>%</span>} />
            </div>
            <div style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.5, marginTop: -4 }}>
              Selecting a state fills its flat rate automatically — or enter a rate manually. Tax is output exclusively to buyers in the selected state.
            </div>
          </div>
        )}
      </div>

      {/* Store Point of Contact — moved here from the manager modal */}
      <div>
        <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>Store Point of Contact</div>
        <StorePointOfContact data={f} set={setMany} accent={accent} />
      </div>

      <TeamDatabaseModal open={teamModalOpen} onClose={() => setTeamModalOpen(false)} onSelect={applyTeam} accent={accent} />
      <FundraisingDisableModal
        open={fundOffReview}
        store={store}
        onCancel={() => setFundOffReview(false)}
        onConfirm={() => { setMany({ fundraisingEnabled: false, __applyFundDisable: true }); setFundOffReview(false); }} />
    </EditModalShell>
  );
}

// ─────────── Fulfillment Method — dedicated modal ───────────
function EditFulfillmentMethodModal({ open, store, onClose, onSave, accent = "#e1251b" }) {
  const [model, setModel] = React.useState("coach");
  const [addr, setAddr] = React.useState({});
  React.useEffect(() => { if (open) { setModel(store.distributionModel || "coach"); setAddr({ shipStreet: store.shipStreet || "", shipApt: store.shipApt || "", shipCity: store.shipCity || "", shipState: store.shipState || "", shipZip: store.shipZip || "" }); } }, [open, store]);
  return (
    <EditModalShell open={open} onClose={onClose} title="EDIT FULFILLMENT METHOD" width={620} onSave={() => onSave(model === "coach" ? { distributionModel: model, ...addr } : { distributionModel: model })}>
      <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
        {FULFILLMENT_METHODS.map((m) => (
          <ChoiceCard key={m.key} accent={accent} minHeight={104}
            active={model === m.key}
            onSelect={() => setModel(m.key)}
            icon={m.icon({ size: 22 })} title={m.title} desc={m.desc} badge={m.badge} />
        ))}
      </div>
      {model === "coach" && (
        <div style={{ borderTop: "1px solid #ececf0", paddingTop: 20 }}>
          <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 4 }}>Ship-To Address</div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, marginBottom: 16 }}>
            The destination for bulk shipments. Orders can't be submitted for fulfillment until a destination address exists. Clear the fields to remove it.
          </div>
          <ShipToAddressFields value={addr} onChange={(p) => setAddr((a) => ({ ...a, ...p }))} />
        </div>
      )}
      <div style={{ fontSize: 12.5, color: "#9a9aa1", lineHeight: 1.5 }}>
        Changing the fulfillment method updates how orders are grouped and shipped. Existing orders keep the model they were placed under.
      </div>
    </EditModalShell>
  );
}

// ─────────── Shipping & Tax ───────────
function EditShippingTaxModal({ open, store, onClose, onSave }) {
  const [f, setF] = React.useState({});
  React.useEffect(() => {
    if (!open) return;
    const full = (store.shipName || "").trim();
    const sp = full.indexOf(" ");
    setF({
      shipFirstName: store.shipFirstName || (sp > 0 ? full.slice(0, sp) : full),
      shipLastName: store.shipLastName || (sp > 0 ? full.slice(sp + 1) : ""),
      shipStreet: store.shipStreet || "",
      shipApt: store.shipApt || "",
      shipCountry: store.shipCountry || "United States",
      shipState: store.shipState || "",
      shipCity: store.shipCity || "",
      shipZip: store.shipZip || "",
      contactPref: store.contactPref || "Email & Phone",
      email: store.email || "",
      phone: store.phone || "",
    });
  }, [open, store]);
  const set = (k, v) => setF((p) => ({ ...p, [k]: v }));
  // Keep the combined shipName in sync for every surface that displays it.
  const save = () => onSave({ ...f, shipName: [f.shipFirstName, f.shipLastName].filter(Boolean).join(" ").trim() });

  return (
    <EditModalShell open={open} onClose={onClose} title={store.storeType === "league" ? "EDIT LEAGUE MANAGER INFORMATION" : "EDIT TEAM MANAGER INFORMATION"} width={806} onSave={save}>
      <div style={{ display: "flex", gap: 16 }}>
        <TextField label="First Name" value={f.shipFirstName} onChange={(v) => set("shipFirstName", v)} />
        <TextField label="Last Name" value={f.shipLastName} onChange={(v) => set("shipLastName", v)} />
      </div>
      <div style={{ display: "flex", gap: 16 }}>
        <TextField label="Street Address" value={f.shipStreet} onChange={(v) => set("shipStreet", v)} />
        <TextField label="Apt, Suite, etc" value={f.shipApt} onChange={(v) => set("shipApt", v)} style={{ flex: ".6" }} />
      </div>
      <div style={{ display: "flex", gap: 16 }}>
        <SelectField label="Country" value={f.shipCountry} options={EG_COUNTRIES} onChange={(v) => set("shipCountry", v)} />
        <SelectField label="State" value={f.shipState} options={EG_STATES} onChange={(v) => set("shipState", v)} />
      </div>
      <div style={{ display: "flex", gap: 16 }}>
        <TextField label="City" value={f.shipCity} onChange={(v) => set("shipCity", v)} />
        <TextField label="Zip Code" value={f.shipZip} onChange={(v) => set("shipZip", v)} />
      </div>
      <div style={{ borderTop: "1px solid #ececf0", paddingTop: 24, display: "flex", gap: 16 }}>
        <SelectField label="Contact Preference" value={f.contactPref} options={EG_CONTACT} onChange={(v) => set("contactPref", v)} />
        <div style={{ flex: 1 }} />
      </div>
      <div style={{ display: "flex", gap: 16, alignItems: "flex-start" }}>
        <TextField label="Email" value={f.email} onChange={(v) => set("email", v)} type="email" />
        <div style={{ flex: 1 }}>
          <FieldLabelEG>Phone Number</FieldLabelEG>
          <PhoneField value={f.phone} onChange={(v) => set("phone", v)} />
        </div>
      </div>
    </EditModalShell>
  );
}

// Native date input, styled to match the field set.
function DateInput({ value, onChange }) {
  const [focus, setFocus] = React.useState(false);
  return (
    <input
      type="date"
      value={value || ""}
      onChange={(e) => onChange(e.target.value)}
      onFocus={() => setFocus(true)}
      onBlur={() => setFocus(false)}
      style={{
        width: "100%", boxSizing: "border-box", padding: "12px 12px",
        border: `1px solid ${focus ? "#0e0e10" : "#d4d4d8"}`, borderRadius: 4,
        fontSize: 14, fontFamily: "inherit", color: "#0e0e10", outline: "none",
        background: "#fff",
      }} />
  );
}

Object.assign(window, { EditStoreDetailsModal, EditShippingTaxModal, EditFulfillmentMethodModal });


/* ===== ASSET 19 : 872b91cb-ba31-4a9e-9fba-2ac759b1a5ee ===== */
// New store dashboard — General Information tab (matches screenshot 6).

// ─────────── Tab badge persistence ───────────
// Red "needs attention" badges on the dashboard tabs. Dismissal is per-store and
// persisted to localStorage so cleared badges don't reappear after a refresh.
const BADGE_KEY = "prolook_tab_badges_v1";
function loadBadgeState(storeId) {
  try {
    const all = JSON.parse(localStorage.getItem(BADGE_KEY) || "{}");
    return all[storeId] || {};
  } catch (e) {return {};}
}
function saveBadgeState(storeId, state) {
  try {
    const all = JSON.parse(localStorage.getItem(BADGE_KEY) || "{}");
    all[storeId] = state;
    localStorage.setItem(BADGE_KEY, JSON.stringify(all));
  } catch (e) {}
}

// A red dot that fades + scales out when it clears (and in if it ever returns).
function TabDot({ show }) {
  const [mounted, setMounted] = React.useState(show);
  const [on, setOn] = React.useState(show);
  React.useEffect(() => {
    if (show) {
      setMounted(true);
      const id = requestAnimationFrame(() => setOn(true));
      return () => cancelAnimationFrame(id);
    }
    setOn(false);
    const t = setTimeout(() => setMounted(false), 380);
    return () => clearTimeout(t);
  }, [show]);
  if (!mounted) return null;
  return (
    <span style={{
      width: 8, height: 8, borderRadius: "50%", background: "#e1251b",
      display: "inline-block", marginLeft: 2, marginBottom: 8,
      opacity: on ? 1 : 0, transform: on ? "scale(1)" : "scale(0.3)",
      transition: "opacity .35s ease, transform .35s ease"
    }} />);

}


/* ===== Store Home tab (spec-adherent 11a/11b) ===== */
function homeLifecycle(store) {
  const archived = !!(store && (store.archived || store.status === "archived"));
  const closed = !archived && !!store && store.status === "closed";
  const open = !archived && !closed && !!(store && store.live);
  return { archived, closed, open, draft: !archived && !closed && !open };
}
function homeOrdersInfo(store) {
  const data = store && store.seeded ? makeOrdersDataset(store) : { myOrders: [], fanOrders: [], pending: [] };
  const pendingGroups = data.pending || [];
  const pending = pendingGroups.reduce(function (n, g) { return n + ((g.rows || []).length); }, 0);
  const submitted = (data.myOrders || []).length + (data.fanOrders || []).length;
  return { pending, submitted, total: pending + submitted, pendingGroups };
}
function homeBankInfo(org) {
  const bank = (org && org.bank) || {};
  let total = 0; const byTeam = {};
  Object.keys(bank).forEach(function (tid) {
    const n = (bank[tid] || []).filter(function (c) { return !c.committed; }).length;
    byTeam[tid] = n; total += n;
  });
  return { total, byTeam };
}
function homeStructureInfo(org) {
  const groups = (org && org.groups) || [];
  const loose = (org && org.loose) || [];
  const teams = groups.reduce(function (n, g) { return n + (g.rosters || []).length; }, 0) + loose.length;
  const players = groups.reduce(function (n, g) { return n + (g.rosters || []).reduce(function (m, r) { return m + (r.players || 0); }, 0); }, 0)
    + loose.reduce(function (n, r) { return n + (r.players || 0); }, 0);
  return { groups, loose, teams, players, divisions: groups.length };
}
function homeCloseInfo(store) {
  const raw = store && store.storeOpenEnd;
  if (!raw) return null;
  const end = new Date(raw + "T12:00:00");
  if (isNaN(end)) return null;
  const daysLeft = Math.max(0, Math.ceil((end - new Date()) / 86400000));
  return { label: end.toLocaleDateString("en-US", { month: "short", day: "numeric" }), daysLeft };
}
function homeFailedProducts(store) {
  return ((store && store.products) || []).filter(function (p) {
    return p && (p.renderFailed || p.imageFailed || p.renderStatus === "failed" || p.imageStatus === "failed");
  });
}
// The one attention model both the Home tab and the nav-dot rule read.
// Rail tones: red = blocking, blue = action, grey = informational. Never amber,
// never a severity label — rail color and ordering carry it.
function buildHomeAttention(store, org) {
  const lc = homeLifecycle(store);
  const oi = homeOrdersInfo(store);
  const bank = homeBankInfo(org);
  const close = homeCloseInfo(store);
  const cards = [];
  if (!lc.archived) {
    if (lc.draft) {
      if (((store.products || []).length) === 0) {
        cards.push({ tone: "red", navTab: "products", kind: "products", title: "Add a product before you launch", detail: "Customers need at least one thing to buy", action: "Go to Products" });
      } else {
        cards.push({ tone: "blue", kind: "launch", title: "Your store is ready to launch", detail: "Customers can start ordering as soon as you do", action: "Launch store" });
      }
      if (!String(store.shipStreet || "").trim()) {
        cards.push({ tone: "red", navTab: "general", kind: "settings-store", title: "Add a delivery address before orders can ship", detail: "Orders ship to the team manager, and there's no address on file yet", action: "Add address" });
      }
    } else {
      if (lc.open && (store.storeType || "league") === "league" && org) {
        const gatedNames = []; const gatedIds = {}; let target = null;
        (org.groups || []).forEach(function (g) {
          if (((org.modes || {})[g.id] || "league") !== "teams") return;
          const missing = (g.rosters || []).filter(function (r) { return !managerComplete((org.managers || {})[r.id]); });
          if (missing.length) { gatedNames.push(g.name); gatedIds[String(g.id).toLowerCase()] = true; if (!target) target = { groupId: g.id, rosterId: missing[0].id }; }
        });
        if (gatedNames.length) {
          const waiting = oi.pendingGroups.reduce(function (n, g) { return n + (gatedIds[String(g.shippingGroup || "").toLowerCase()] ? (g.rows || []).length : 0); }, 0);
          const names = gatedNames.length > 1 ? gatedNames.slice(0, -1).join(", ") + " and " + gatedNames[gatedNames.length - 1] : gatedNames[0];
          cards.push({ tone: "red", navTab: "general", kind: "assign", target: target,
            title: names + (gatedNames.length > 1 ? " need" : " needs") + " a manager before their orders can ship",
            detail: waiting ? waiting + " order" + (waiting === 1 ? " is" : "s are") + " waiting on this" : "Orders from these teams can't ship until a manager is assigned",
            action: "Assign managers" });
        }
      }
      const failed = homeFailedProducts(store);
      if (lc.open && failed.length) {
        cards.push({ tone: "red", navTab: "products", kind: "products",
          title: failed.length === 1 ? "Images failed for " + (failed[0].title || failed[0].name || "a product") : "Images failed for " + failed.length + " products",
          detail: (failed.length === 1 ? "It can't" : "They can't") + " go on the storefront until this is retried",
          action: "Retry" });
      }
      if (oi.pending > 0) {
        cards.push({ tone: "blue", kind: "pending", title: oi.pending + " pending order" + (oi.pending === 1 ? "" : "s") + " to submit", detail: "Oldest placed 3 days ago — orders reach the factory after you submit", action: "Go to Pending Orders" });
      }
      if (lc.open && bank.total > 0) {
        cards.push({ tone: "blue", kind: "bank", title: bank.total + " Roster Bank capture" + (bank.total === 1 ? "" : "s") + " awaiting review",
          detail: (store.storeType === "single") ? "Banked from storefront orders, not yet on this store's roster" : "Oldest captured 2 days ago — banked from storefront orders, not yet on a roster",
          action: "Open Roster Bank" });
      }
      if (lc.closed) {
        cards.push({ tone: "grey", kind: "report", link: true, title: "Your close report is ready", detail: "Totals, fundraising, and what shipped", action: "View report" });
        if (oi.submitted > 0) cards.push({ tone: "grey", kind: "orders", link: true, title: oi.submitted + " order" + (oi.submitted === 1 ? " is" : "s are") + " still being made", detail: "They'll ship as they're finished — nothing needed from you", action: "View orders" });
      }
      if (lc.open && close) {
        cards.push({ tone: "grey", kind: "schedule", link: true, title: "Ordering window closes " + close.label + " — " + close.daysLeft + " days left", detail: "Change the close date from the store schedule", action: "Edit Store Schedule" });
      }
    }
  }
  const rank = { red: 0, blue: 1, grey: 2 };
  cards.sort(function (a, b) { return rank[a.tone] - rank[b.tone]; });
  return { cards: cards, lc: lc, oi: oi, bank: bank, close: close };
}
// Red dot on a nav tab only when a red-rail card resolves to it.
function homeRedNavTabs(store, org) {
  const s = new Set();
  buildHomeAttention(store, org).cards.forEach(function (c) { if (c.tone === "red" && c.navTab) s.add(c.navTab); });
  return s;
}
function HomeLinkBtn({ label, onClick, muted }) {
  return (
    <button onClick={onClick} style={{ background: "transparent", border: 0, padding: 0, fontSize: 12, fontWeight: 700, color: muted ? "#6b6b72" : "#0e0e10", cursor: "pointer", fontFamily: "inherit", flexShrink: 0 }}>{label}</button>
  );
}
function HomeAttentionCard({ card, readOnly, onAction }) {
  const rail = card.tone === "red" ? "#e1251b" : card.tone === "blue" ? "#2563eb" : "#d4d4d8";
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 14, background: "#fff", border: "1px solid #ececf0", borderLeft: "3px solid " + rail, borderRadius: 10, padding: "14px 18px" }}>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>{card.title}</div>
        <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 2 }}>{card.detail}</div>
      </div>
      {!readOnly && (card.link ? (
        <button onClick={onAction} style={{ flexShrink: 0, background: "transparent", border: 0, fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", padding: "8px 0" }}>{card.action} ›</button>
      ) : (
        <button onClick={onAction} style={{
          flexShrink: 0, background: card.tone === "red" ? "#0e0e10" : "#fff", color: card.tone === "red" ? "#fff" : "#0e0e10",
          border: "1px solid " + (card.tone === "red" ? "#0e0e10" : "#d4d4d8"), fontSize: 12, fontWeight: 700, borderRadius: 7,
          padding: "8px 14px", cursor: "pointer", fontFamily: "inherit"
        }}>{card.action}</button>
      ))}
    </div>
  );
}
function HomeTab({ store, org, readOnly, onGoTab, onPendingOrders, onAllOrders, onRosterBank, onManagers, onLaunch, onGoSettings, onUpdateStore }) {
  const model = buildHomeAttention(store, org);
  const lc = model.lc, oi = model.oi, bank = model.bank, close = model.close, cards = model.cards;
  const st = homeStructureInfo(org);
  const isLeague = (store.storeType || "league") === "league";
  const [showAll, setShowAll] = React.useState(false);
  const [copied, setCopied] = React.useState(false);
  const products = store.products || [];
  const failedProducts = homeFailedProducts(store);
  const slug = store.storeSlug || tcStoreSlug(store);
  const url = "prolook.com/store/" + slug;
  const raised = lc.draft ? 0 : (store.revenue || (store.seeded ? 3810 : 0));
  const fmtMoney = function (n) { return "$" + Math.round(n).toLocaleString("en-US"); };
  const run = function (card) {
    if (readOnly) return;
    if (card.kind === "products") onGoTab("products");
    else if (card.kind === "launch") onLaunch();
    else if (card.kind === "settings-store") onGoSettings("store");
    else if (card.kind === "assign") onManagers(card.target);
    else if (card.kind === "pending") onPendingOrders();
    else if (card.kind === "bank") onRosterBank();
    else if (card.kind === "schedule") onGoSettings("schedule");
    else if (card.kind === "report") window.dispatchEvent(new CustomEvent("closereport:open", { detail: { layer: "summary" } }));
    else if (card.kind === "orders") onAllOrders();
  };
  const copyUrl = function () {
    try { navigator.clipboard && navigator.clipboard.writeText("https://" + url); } catch (e) {}
    setCopied(true); setTimeout(function () { setCopied(false); }, 1500);
  };
  const visible = showAll ? cards : cards.slice(0, 5);
  const hiddenCount = cards.length - visible.length;

  const pill = lc.archived ? { label: "Archived", bg: "#f1f1f3", color: "#6b6b72", dot: "#9a9aa1" }
    : lc.closed ? { label: "Closed", bg: "#fdf3d3", color: "#8a6408", dot: "#d9a514" }
    : lc.open ? { label: "Open", bg: "#d8f5e3", color: "#1f7a44", dot: "#1f9d55" }
    : { label: "Draft", bg: "#dbeafe", color: "#1d4ed8", dot: "#3b82f6" };

  // Manager panel rows — the record buyers see is tagged Point of Contact
  // (the exact name of the setting it feeds).
  const prefText = function (p) {
    const s = String(p || "").toLowerCase();
    if (s.indexOf("email") >= 0 && s.indexOf("phone") >= 0) return "Prefers email or phone";
    if (s.indexOf("phone") >= 0) return "Prefers phone";
    if (s.indexOf("email") >= 0) return "Prefers email";
    return null;
  };
  const primaryMgr = { name: store.managerName || store.shipName || "Team Manager", email: store.email || "", phone: store.phone || "", pref: prefText(store.contactPref), poc: true };
  const extras = [];
  const seen = {}; seen[(primaryMgr.email || "").toLowerCase()] = true;
  Object.keys((org && org.managers) || {}).forEach(function (k) {
    const m = org.managers[k]; if (!m) return;
    const nm = m.name || [m.firstName, m.lastName].filter(Boolean).join(" ");
    const em = String(m.email || "").toLowerCase();
    if (!nm || !em || seen[em]) return;
    seen[em] = true;
    extras.push({ name: nm, email: m.email, phone: m.phone, pref: null, poc: false });
  });
  const managerRows = [primaryMgr].concat(extras.slice(0, 3));

  // Recently worked on — activity trail. Absent when there is nothing to show
  // (the normal case on a Draft store). Rows read Open ›, never Resume.
  const trail = (!store.seeded || lc.draft) ? [] : isLeague ? [
    { name: "Division 7 → U12 Blue — roster", when: "2 hours ago", tabLabel: "My Teams", tab: "roster", icon: "users" },
    { name: "Fan Gear — pricing", when: "Yesterday", tabLabel: "Products", tab: "products", icon: "shirt" },
    { name: "Announcement message", when: "Monday", tabLabel: "Settings", tab: "general", icon: "settings" }
  ] : [
    { name: "Home Jersey — pricing", when: "2 hours ago", tabLabel: "My Team", tab: "roster", icon: "shirt" },
    { name: "Imported 4 players", when: "Yesterday", tabLabel: "My Team", tab: "roster", icon: "users" },
    { name: "Store banner image", when: "Monday", tabLabel: "Settings", tab: "general", icon: "settings" }
  ];
  const trailIcon = function (kind) {
    if (kind === "shirt") return <IconShirt size={15} />;
    if (kind === "settings") return <IconSettings size={15} />;
    return <IconUsers size={15} />;
  };

  const groupCaptures = function (g) { return (g.rosters || []).reduce(function (n, r) { return n + (bank.byTeam[r.id] || 0); }, 0); };

  const defaultTheme = (store.teamColors && store.teamColors[1]) || null;
  const themeColor = store.primaryColor || defaultTheme || "#e1251b";
  const themeCustom = !!(defaultTheme && store.primaryColor && String(store.primaryColor).toLowerCase() !== String(defaultTheme).toLowerCase());

  const storeRows = [
    { id: "roster", label: isLeague ? "My Teams" : "My Team", sub: st.players + " players", extra: bank.total ? { text: bank.total + " Roster Bank capture" + (bank.total === 1 ? "" : "s"), color: "#2563eb" } : null },
    { id: "products", label: "Products", sub: products.length + " products", extra: failedProducts.length ? { text: failedProducts.length + " render failed", color: "#b81d14" } : null },
    { id: "orders", label: "Orders", sub: (oi.total) + " orders", extra: oi.pending ? { text: oi.pending + " to submit", color: "#2563eb" } : null },
    { id: "payouts", label: "Reports", sub: "Store analytics" }
  ];

  const panelStyle = { background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden" };
  const sectionTitle = { fontSize: 15, fontWeight: 800, color: "#0e0e10" };

  return (
    <div data-screen-label="Home" style={{ display: "flex", flexDirection: "column", gap: 20 }}>
      {/* Status strip — the one place amber is allowed (canonical Closed pill). */}
      <div style={{ display: "flex", alignItems: "center", gap: 14, flexWrap: "wrap", background: "#fff", border: "1px solid #ececf0", borderRadius: 10, padding: "12px 18px" }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 7, background: pill.bg, color: pill.color, borderRadius: 999, padding: "5px 12px", fontSize: 12, fontWeight: 700 }}>
          <span style={{ width: 7, height: 7, borderRadius: "50%", background: pill.dot }} />{pill.label}
        </span>
        {lc.open && close && <span style={{ fontSize: 12.5, color: "#52525a" }}>Closes <strong style={{ color: "#0e0e10" }}>{close.label}</strong> · {close.daysLeft} days left</span>}
        {lc.closed && close && <span style={{ fontSize: 12.5, color: "#52525a" }}>Closed <strong style={{ color: "#0e0e10" }}>{close.label}</strong></span>}
        {!lc.draft && <span style={{ width: 1, height: 16, background: "#e6e6ea" }} />}
        {!lc.draft && <span style={{ fontSize: 12.5, color: "#52525a" }}><strong style={{ color: "#0e0e10" }}>{oi.total}</strong> orders · <strong style={{ color: "#0e0e10" }}>{fmtMoney(raised)}</strong> raised{lc.open ? " this window" : ""}</span>}
        <span style={{ marginLeft: "auto", display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, color: "#6b6b72", background: "#f6f6f8", borderRadius: 7, padding: "6px 10px" }}>
          {url}
          {!readOnly && <button onClick={copyUrl} style={{ background: "transparent", border: 0, padding: 0, fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>{copied ? "Copied" : "Copy"}</button>}
        </span>
      </div>

      <div style={{ display: "flex", gap: 20, alignItems: "flex-start" }}>
        {/* Main column */}
        <div style={{ flex: 1.65, display: "flex", flexDirection: "column", gap: 20, minWidth: 0 }}>
          {/* Needs your attention — always renders, even empty. */}
          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
              <span style={sectionTitle}>Needs your attention</span>
              {cards.length > 0 && <span style={{ display: "inline-flex", alignItems: "center", justifyContent: "center", minWidth: 20, height: 20, borderRadius: 999, background: "#e1251b", color: "#fff", fontSize: 11.5, fontWeight: 800, padding: "0 6px" }}>{cards.length}</span>}
            </div>
            {visible.map(function (card, i) { return <HomeAttentionCard key={i} card={card} readOnly={readOnly} onAction={function () { run(card); }} />; })}
            {hiddenCount > 0 && (
              <button onClick={function () { setShowAll(true); }} style={{ alignSelf: "flex-start", background: "transparent", border: 0, padding: "2px 4px", fontSize: 12, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit" }}>{hiddenCount} more</button>
            )}
            {cards.length === 0 && (
              <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, padding: "16px 18px", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
                {lc.archived ? "This store is archived and read-only — nothing is awaiting action." : lc.open ? "Nothing needs your attention right now. The store is open and taking orders." : "Nothing needs your attention right now."}
              </div>
            )}
          </div>

          {/* Recently worked on — absent entirely when there is nothing to show. */}
          {trail.length > 0 && (
            <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
              <span style={sectionTitle}>Recently worked on</span>
              <div style={panelStyle}>
                {trail.map(function (r, i) { return (
                  <div key={i} style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 18px", borderBottom: i < trail.length - 1 ? "1px solid #f2f2f5" : "none" }}>
                    <span style={{ width: 30, height: 30, borderRadius: 7, background: "#f4f4f5", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#52525a", flexShrink: 0 }}>{trailIcon(r.icon)}</span>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>{r.name}</div>
                      <div style={{ fontSize: 11.5, color: "#9a9aa1" }}>{r.when} · {r.tabLabel}</div>
                    </div>
                    <HomeLinkBtn label="Open ›" onClick={function () { onGoTab(r.tab); }} />
                  </div>
                ); })}
              </div>
            </div>
          )}

          {/* Manager panel */}
          <div style={panelStyle}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "14px 18px", borderBottom: "1px solid #f2f2f5" }}>
              <span style={{ fontSize: 13.5, fontWeight: 800, color: "#0e0e10" }}>{isLeague ? "League Manager Information" : "Team Manager Information"}</span>
              {!readOnly && (
                <button onClick={function () { onManagers(null); }} style={{ background: "#fff", border: "1px solid #d4d4d8", fontSize: 11.5, fontWeight: 700, borderRadius: 7, padding: "6px 12px", cursor: "pointer", fontFamily: "inherit", color: "#0e0e10" }}>Add manager</button>
              )}
            </div>
            {managerRows.map(function (m, i) { return (
              <div key={i} style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 18px", borderBottom: "1px solid #f2f2f5" }}>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                    <span style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{m.name}</span>
                    {m.poc && <span style={{ fontSize: 10, fontWeight: 700, color: "#6b6b72", background: "#f4f4f5", borderRadius: 4, padding: "2px 6px" }}>Point of Contact</span>}
                  </div>
                  <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 2 }}>{[m.email, m.phone, m.pref].filter(Boolean).join(" · ") || "No contact details yet"}</div>
                </div>
                {!readOnly && (
                  <span style={{ display: "inline-flex", gap: 14, flexShrink: 0 }}>
                    {!m.poc && <HomeLinkBtn label="Invite" onClick={function () { onGoSettings("store"); }} />}
                    <HomeLinkBtn label="Edit ›" onClick={function () { m.poc ? onGoSettings("store") : onManagers(null); }} />
                  </span>
                )}
              </div>
            ); })}
            <div style={{ padding: "12px 18px", background: "#fafafb", fontSize: 11.5, color: "#6b6b72", lineHeight: 1.55 }}>
              Adding a manager creates a contact record only — it does not grant portal access. Send invites from <strong style={{ color: "#0e0e10", cursor: readOnly ? "default" : "pointer" }} onClick={function () { if (!readOnly) onGoSettings("store"); }}>Settings → People &amp; Access</strong>.
            </div>
          </div>
        </div>

        {/* Right rail */}
        <div style={{ flex: 1, display: "flex", flexDirection: "column", gap: 14, minWidth: 0 }}>
          {/* In your store — one row per tab; first row matches the nav label exactly. */}
          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            <span style={sectionTitle}>In your store</span>
            <div style={panelStyle}>
              {storeRows.map(function (row, i) { return (
                <React.Fragment key={row.id}>
                  <div onClick={function () { if (!readOnly) onGoTab(row.id); }} style={{ display: "flex", alignItems: "center", gap: 12, padding: (row.id === "products" && products.length > 0) ? "14px 18px 10px" : "14px 18px", borderBottom: (i < storeRows.length - 1 && !(row.id === "products" && products.length > 0)) ? "1px solid #f2f2f5" : "none", cursor: readOnly ? "default" : "pointer" }}>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{row.label}</div>
                      <div style={{ fontSize: 11.5, color: "#9a9aa1" }}>{row.sub}{row.extra && <span> · <span style={{ color: row.extra.color, fontWeight: 700 }}>{row.extra.text}</span></span>}</div>
                    </div>
                    <span style={{ color: "#b9b9c2", fontWeight: 700 }}>›</span>
                  </div>
                  {row.id === "products" && products.length > 0 && (
                    <div style={{ padding: "0 18px 14px", display: "flex", gap: 8, borderBottom: i < storeRows.length - 1 ? "1px solid #f2f2f5" : "none" }}>
                      {products.slice(0, 4).map(function (p, j) { return <span key={j} style={{ width: 44, height: 50, borderRadius: 6, background: "#f0f0f3" }} />; })}
                      {products.length > 4 && <span style={{ width: 44, height: 50, borderRadius: 6, background: "#f6f6f8", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 11, fontWeight: 700, color: "#6b6b72" }}>+{products.length - 4}</span>}
                    </div>
                  )}
                </React.Fragment>
              ); })}
            </div>
          </div>

          {/* League structure — League stores only; bounded so it can't push the attention list off screen. */}
          {isLeague && (
            <div style={panelStyle}>
              <div style={{ padding: "14px 18px", borderBottom: "1px solid #f2f2f5" }}>
                <div style={{ fontSize: 13.5, fontWeight: 800, color: "#0e0e10" }}>League structure</div>
                <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 2 }}>{st.divisions} division{st.divisions === 1 ? "" : "s"} · {st.teams} team{st.teams === 1 ? "" : "s"} · {st.players} player{st.players === 1 ? "" : "s"}</div>
              </div>
              <div style={{ maxHeight: 300, overflowY: "auto" }}>
                {st.groups.length === 0 && st.loose.length === 0 && (
                  <div style={{ padding: "16px 18px", fontSize: 12, color: "#9a9aa1", lineHeight: 1.5 }}>No divisions yet — build the structure from {"My Teams"}.</div>
                )}
                {st.groups.map(function (g, i) { const caps = groupCaptures(g); return (
                  <div key={g.id} onClick={function () { if (!readOnly) onRosterBank(); }} style={{ display: "flex", alignItems: "center", gap: 12, padding: "14px 18px", borderBottom: "1px solid #f2f2f5", cursor: readOnly ? "default" : "pointer" }}>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{g.name}</div>
                      <div style={{ fontSize: 11.5, color: "#9a9aa1" }}>{(g.rosters || []).length} teams · {(g.rosters || []).reduce(function (n, r) { return n + (r.players || 0); }, 0)} players{caps > 0 && <span> · <span style={{ color: "#2563eb", fontWeight: 700 }}>{caps} capture{caps === 1 ? "" : "s"}</span></span>}</div>
                    </div>
                    <span style={{ color: "#b9b9c2", fontWeight: 700 }}>›</span>
                  </div>
                ); })}
                {st.loose.map(function (r, i) { const caps = bank.byTeam[r.id] || 0; return (
                  <div key={r.id} onClick={function () { if (!readOnly) onRosterBank(); }} style={{ display: "flex", alignItems: "center", gap: 12, padding: "14px 18px", borderBottom: i < st.loose.length - 1 ? "1px solid #f2f2f5" : "none", cursor: readOnly ? "default" : "pointer" }}>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{r.name} <span style={{ fontSize: 10, fontWeight: 700, color: "#6b6b72", background: "#f4f4f5", borderRadius: 4, padding: "2px 6px", marginLeft: 4 }}>STANDALONE</span></div>
                      <div style={{ fontSize: 11.5, color: "#9a9aa1" }}>{r.players || 0} players{caps > 0 && <span> · <span style={{ color: "#2563eb", fontWeight: 700 }}>{caps} capture{caps === 1 ? "" : "s"}</span></span>}</div>
                    </div>
                    <span style={{ color: "#b9b9c2", fontWeight: 700 }}>›</span>
                  </div>
                ); })}
              </div>
            </div>
          )}

          {/* Brand assets — editable after launch; only Archived locks it. */}
          <div style={panelStyle}>
            <div style={{ padding: "14px 18px", borderBottom: "1px solid #f2f2f5", fontSize: 13.5, fontWeight: 800, color: "#0e0e10" }}>Brand assets</div>
            <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 18px", borderBottom: "1px solid #f2f2f5" }}>
              <TeamEmblem store={store} size={34} />
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10" }}>Team Logo</div>
                <div style={{ fontSize: 11, color: "#9a9aa1" }}>From the Team Database</div>
              </div>
              {!readOnly && <HomeLinkBtn label="Edit ›" onClick={function () { onGoSettings("store"); }} />}
            </div>
            <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 18px", borderBottom: "1px solid #f2f2f5" }}>
              <span style={{ display: "inline-flex", gap: 6 }}>
                <span style={{ width: 22, height: 22, borderRadius: 5, background: (store.teamColors && store.teamColors[0]) || "#e1e2dd", border: "1px solid #ececf0" }} />
                <span style={{ width: 22, height: 22, borderRadius: 5, background: (store.teamColors && store.teamColors[1]) || "#f4f4f5", border: "1px solid #ececf0" }} />
              </span>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10" }}>Team Colors</div>
              </div>
              {!readOnly && <HomeLinkBtn label="Edit ›" onClick={function () { onGoSettings("store"); }} />}
            </div>
            <div style={{ display: "flex", alignItems: "flex-start", gap: 12, padding: "12px 18px", borderBottom: "1px solid #f2f2f5" }}>
              <span style={{ width: 22, height: 22, borderRadius: 5, background: themeColor, flexShrink: 0, marginTop: 2, border: "1px solid #ececf0" }} />
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                  <span style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10" }}>Store Theme Color</span>
                  <span style={{ fontSize: 10, fontWeight: 700, color: "#6b6b72", background: "#f4f4f5", borderRadius: 4, padding: "2px 6px" }}>{themeCustom ? "Custom" : "Team default"}</span>
                </div>
                <div style={{ fontSize: 11, color: "#9a9aa1", marginTop: 2 }}>Applies to storefront buttons and accents only.</div>
                {themeCustom && !readOnly && (
                  <div onClick={function () { onUpdateStore && onUpdateStore({ primaryColor: defaultTheme }); }} style={{ fontSize: 11.5, fontWeight: 700, marginTop: 4, cursor: "pointer", color: "#0e0e10" }}>Reset to team color</div>
                )}
              </div>
              {!readOnly && <HomeLinkBtn label="Edit ›" onClick={function () { onGoSettings("customization"); }} />}
            </div>
            <div onClick={function () { if (!readOnly) onGoSettings("store"); }} style={{ padding: "12px 18px", fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: readOnly ? "default" : "pointer" }}>
              Team artwork library <span style={{ fontWeight: 500, color: "#9a9aa1" }}>— team-level, spans seasons</span> ›
            </div>
          </div>

          <div style={{ background: "#f6f6f8", borderRadius: 10, padding: "14px 18px", fontSize: 12, color: "#6b6b72", lineHeight: 1.55 }}>
            Need orientation? <strong style={{ color: "#0e0e10", cursor: "pointer" }} onClick={function () { window.dispatchEvent(new CustomEvent("setupguide:open")); }}>Reopen the setup guide</strong>.
          </div>
        </div>
      </div>
    </div>
  );
}

function Dashboard({ store, onBack, onCustomize, initialTab, onProductsChange, simulateOrders, liveStores, onLaunch, onSwitchStore, onSetStatus, onViewStore, onUpdateStore, onCreateTemplate, embedded, readOnly }) {
  const [tab, setTab] = React.useState(initialTab || "home");
  React.useEffect(() => {if (initialTab) setTab(initialTab);}, [initialTab]);
  // Shared roster structure + manager mapping — lifted here so the Settings
  // tab's "Team Manager & Freight Mapping" and the Rosters tab mirror each
  // other live (both read/write the same org object).
  const [org, setOrg] = React.useState(() => makeRosterOrg(store, !!store.__fresh));
  const managerGated = store.storeType === "league" && (store.distributionModel || "coach") === "coach";
  const [managerFocus, setManagerFocus] = React.useState(null); // {groupId, rosterId, n}
  const jumpToManagers = (groupId, rosterId) => { setManagerFocus({ groupId, rosterId, n: Date.now() }); setSettingsSection("distribution"); setTab("general"); };
  // Settings page sub-nav (embedded workspace): Store Settings | Distribution & Fulfillment
  const [settingsSection, setSettingsSection] = React.useState("store");
  // Notify the onboarding tour which dashboard tab is active so it can advance.
  React.useEffect(() => {
    window.dispatchEvent(new CustomEvent("tour:dashboard-tab", { detail: tab }));
  }, [tab]);
  const [showLaunchModal, setShowLaunchModal] = React.useState(false);
  const [showSuccessModal, setShowSuccessModal] = React.useState(false);
  // Bumped whenever something requests a jump to a specific pending order, so
  // OrdersTabV2 can react and open that order's drawer.
  const [pendingJump, setPendingJump] = React.useState(0);
  // Bumped to request a jump to the Orders tab's "All Orders" sub-tab.
  const [allOrdersJump, setAllOrdersJump] = React.useState(0);
  // Bumped to request a jump to the Orders tab's "Pending Orders" sub-tab (no drawer).
  const [pendingTabJump, setPendingTabJump] = React.useState(0);
  React.useEffect(() => {
    window.__goToPendingOrder = () => {setTab("orders");setPendingJump((n) => n + 1);};
    return () => {delete window.__goToPendingOrder;};
  }, []);
  // 4 — the Settings card hands off to the Products tab's category control.
  React.useEffect(() => {
    const h = () => setTab("products");
    window.addEventListener("ts:openProductCats", h);
    return () => window.removeEventListener("ts:openProductCats", h);
  }, []);
  // Hooks for the onboarding checklist to drive navigation / launch.
  React.useEffect(() => {
    window.__goToDashTab = (t) => setTab(t);
    window.__openLaunchModal = () => {if (!store.live) setShowLaunchModal(true);};
    return () => {delete window.__goToDashTab;delete window.__openLaunchModal;};
  }, [store.live]);
  const canLaunch = (store.products || []).length > 0;
  const isLive = !!store.live;
  // Draft = never launched (Store Status & Lifecycle: Draft is the entry state only).
  const isDraftStore = !isLive && store.status !== "closed" && store.status !== "archived";

  // ─── Tab badges (Cody's matrix) ───
  const [viewed, setViewed] = React.useState(() => loadBadgeState(store.id));
  React.useEffect(() => {setViewed(loadBadgeState(store.id));}, [store.id]);
  const markViewed = React.useCallback((patch) => {
    setViewed((prev) => {
      const changed = Object.keys(patch).some((k) => prev[k] !== patch[k]);
      if (!changed) return prev;
      const next = { ...prev, ...patch };
      saveBadgeState(store.id, next);
      return next;
    });
  }, [store.id]);

  // Clear-on-view for General / Payouts / Roster; Orders marks its main tab
  // (sub-tabs report separately via onSubView).
  React.useEffect(() => {
    if (tab === "general") markViewed({ general: true });else
    if (tab === "payouts") markViewed({ payouts: true });else
    if (tab === "roster") markViewed({ roster: true });else
    if (tab === "orders") markViewed({ ordersTab: true });
  }, [tab, markViewed]);

  const ordersCleared = viewed.ordersTab && viewed.sub_pending && viewed.sub_all;
  const productsCleared = (store.products?.length || 0) >= 1;

  const tabBtnRefs = React.useRef({});
  const [tabIndicator, setTabIndicator] = React.useState({ left: 0, width: 0 });
  React.useLayoutEffect(() => {
    const btn = tabBtnRefs.current[tab];
    if (btn) setTabIndicator({ left: btn.offsetLeft, width: btn.offsetWidth });
  }, [tab]);

  const tabs = [
  { id: "home", label: "Home", dot: false },
  { id: "general", label: "Settings", dot: !viewed.general },
  { id: "orders", label: "Orders", dot: !ordersCleared },
  { id: "products", label: "Products", dot: !productsCleared },
  { id: "roster", label: (store && store.storeType === "league") ? "My Teams" : "My Team", dot: !viewed.roster },
  { id: "payouts", label: "Reports", dot: !viewed.payouts }];

  // Settings → "View in Products": switch tabs; the products list picks up the
  // still-collecting filter from the same event / pending flag.
  React.useEffect(() => {
    const h = () => { window.__pteFilterCollecting = true; setTab("products"); };
    window.addEventListener("pte:filter-collecting", h);
    return () => window.removeEventListener("pte:filter-collecting", h);
  }, []);

  const confirmLaunch = () => {
    setShowLaunchModal(false);
    onLaunch && onLaunch(store.id);
    setShowSuccessModal(true);
  };

  // Tab content shared between the legacy layout and the embedded workspace.
  const tabContent =
  <React.Fragment>
      {tab === "home" && <HomeTab store={store} org={org} readOnly={readOnly} onGoTab={setTab} onPendingOrders={() => {setTab("orders");setPendingTabJump((n) => n + 1);}} onAllOrders={() => {setTab("orders");setAllOrdersJump((n) => n + 1);}} onRosterBank={() => setTab("roster")} onManagers={(t) => jumpToManagers(t && t.groupId, t && t.rosterId)} onLaunch={() => {if ((store.products || []).length) setShowLaunchModal(true);}} onGoSettings={(s) => {setSettingsSection(s || "store");setTab("general");}} onUpdateStore={onUpdateStore} />}
      {tab === "general" && <React.Fragment><GeneralInfo store={store} onUpdateStore={onUpdateStore} org={org} setOrg={setOrg} managerFocus={managerFocus} onCreateTemplate={onCreateTemplate} onGoMyTeams={() => setTab("roster")} section={embedded ? settingsSection : "all"} />{(!embedded || settingsSection === "catalog") && <CatalogPolicySection store={store} onUpdate={onUpdateStore} />}</React.Fragment>}
      {tab === "orders" && <OrdersTabV2 store={store} org={org} setOrg={setOrg} pendingJump={pendingJump} allOrdersJump={allOrdersJump} pendingTabJump={pendingTabJump} onSubView={(s) => markViewed({ ["sub_" + s]: true })} />}
      {tab === "payouts" && <PayoutsTab store={store} onViewAllOrders={() => {setTab("orders");setAllOrdersJump((n) => n + 1);}} onViewPendingOrders={() => {setTab("orders");setPendingTabJump((n) => n + 1);}} />}
      {tab === "products" && <ProductsTab store={store} onCustomize={onCustomize} onProductsChange={onProductsChange} simulateOrders={simulateOrders} liveStores={liveStores} />}
      {tab === "roster" && <RosterTab startEmpty={!!store.__fresh} store={store} org={org} setOrg={setOrg} managerGated={managerGated} onManageManagers={jumpToManagers} onCustomize={onCustomize} onProductsChange={onProductsChange} />}
    </React.Fragment>;


  const launchModals =
  <React.Fragment>
      <LaunchStoreModal
      open={showLaunchModal}
      onClose={() => setShowLaunchModal(false)}
      onConfirm={confirmLaunch} />
      <LaunchSuccessModal
      open={showSuccessModal}
      store={store}
      onClose={() => setShowSuccessModal(false)}
      onViewStore={() => {setShowSuccessModal(false);onViewStore && onViewStore(store);}}
      onStayInEditor={() => setShowSuccessModal(false)} />
    </React.Fragment>;


  // ── Embedded workspace layout: icon side-rail + content, no header/tab strip ──
  if (embedded) {
    const homeRed = homeRedNavTabs(store, org);
    const badges = {
      general: !viewed.general || homeRed.has("general"),
      orders: !ordersCleared || homeRed.has("orders"),
      products: !productsCleared || homeRed.has("products"),
      roster: !viewed.roster || homeRed.has("roster"),
      payouts: !viewed.payouts
    };
    return (
      <div style={{ flex: 1, display: "flex", minWidth: 0, alignItems: "stretch", minHeight: 0, overflow: "hidden" }}>
        <StoreSideNav tab={tab} onChange={setTab} badges={badges} store={store} />
        <div ref={(el) => { if (el) { if (readOnly) el.setAttribute("inert", ""); else el.removeAttribute("inert"); } }} style={{ flex: 1, minWidth: 0, padding: "28px 36px 92px", background: "#fbfbfc", opacity: readOnly ? 0.72 : 1, transition: "opacity .15s", overflowY: "auto" }}>
          {/* Draft-state launch banner (Store Status & Lifecycle, Epic A) */}
          {isDraftStore &&
          <div style={{ display: "flex", alignItems: "center", gap: 20, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 12, padding: "18px 22px", marginBottom: 24, boxShadow: "0 1px 2px rgba(20,20,30,.04)" }}>
            <span style={{ width: 52, height: 52, borderRadius: 12, background: "#fdeeed", color: "#e1251b", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
              <IconStore size={24} />
            </span>
            <div style={{ flex: 1, minWidth: 0 }}>
              <div style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10", letterSpacing: "-.005em" }}>Ready to launch?</div>
              <div style={{ fontSize: 13.5, color: "#6b6b72", marginTop: 3, lineHeight: 1.5 }}>
                {canLaunch
                  ? "This store is a draft \u2014 buyers can't see it yet. Launch when you're ready to start taking orders."
                  : "This store is a draft \u2014 add at least one product, then launch to start taking orders."}
              </div>
            </div>
            <button disabled={!canLaunch} onClick={() => canLaunch && setShowLaunchModal(true)}
            title={canLaunch ? "Launch this team store" : "Add at least one product to launch your store"}
            style={{
              flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8,
              background: canLaunch ? "#0e0e10" : "#ececf0", color: canLaunch ? "#fff" : "#9a9aa1",
              border: 0, borderRadius: 8, padding: "14px 22px", fontSize: 14, fontWeight: 700,
              fontFamily: "inherit", cursor: canLaunch ? "pointer" : "not-allowed", transition: "background .12s"
            }}
            onMouseEnter={(e) => { if (canLaunch) e.currentTarget.style.background = "#1f1f22"; }}
            onMouseLeave={(e) => { if (canLaunch) e.currentTarget.style.background = "#0e0e10"; }}>
              Launch Store <IconArrowRight size={15} />
            </button>
          </div>}
          {/* Page header (24/800) + Settings sub-nav pills */}
          {tab !== "home" && <div style={{ display: "flex", flexDirection: "row", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: (tab === "products" || tab === "orders") ? 12 : 24 }}>
            <h1 data-screen-label={tab} style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, lineHeight: "32px", letterSpacing: "-.01em", color: "#09090b", margin: 0 }}>
              {tab === "general" ? "Settings"
                : tab === "payouts" ? "Reports"
                : tab === "roster" ? ((store && store.storeType === "league") ? "My Teams" : "My Team")
                : tab === "products" ? "Products"
                : "Orders"}
            </h1>
          </div>}
          <StoreCloseReportHost store={store} />
          <StoreSetupGuideHost store={store} />
          {tab === "general" ? (
            <div style={{ display: "flex", gap: 36, alignItems: "flex-start" }}>
              <StoreSettingsNav store={store} value={settingsSection} onChange={setSettingsSection} />
              <div style={{ flex: 1, minWidth: 0 }}>{tabContent}</div>
            </div>
          ) : tabContent}
        </div>
        {launchModals}
      </div>);

  }

  return (
    <div style={{ flex: 1, padding: "24px 36px 80px", background: "#fff" }}>
      {/* Header */}
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          {store.teamLogo?.url ?
          <img src={store.teamLogo.url} alt="" style={{ width: 36, height: 36, borderRadius: 4, objectFit: "cover" }} /> :

          <div style={{ width: 36, height: 36, borderRadius: 4, background: store.teamLogo?.color || "#e1e2dd", display: "inline-flex", alignItems: "center", justifyContent: "center", color: store.teamLogo?.color ? "#fff" : "#6b6b72", fontWeight: 800, fontSize: 13 }}>
              {store.teamLogo?.mono || store.teamName?.[0]?.toUpperCase() || "T"}
            </div>
          }
          <h1 style={{ fontSize: 18, fontWeight: 800, letterSpacing: ".02em", margin: 0 }}>
            {store.teamName || "[TEAM NAME]"}
          </h1>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
        {isLive ?
        <LiveStoreControls
          store={store}
          liveStores={liveStores || []}
          onViewStore={() => onViewStore && onViewStore(store)}
          onSwitchStore={(id) => onSwitchStore && onSwitchStore(id)}
          onSetStatus={(status) => onSetStatus && onSetStatus(store.id, status)} /> :

        <button data-tour="launch-btn" disabled={!canLaunch} onClick={() => canLaunch && setShowLaunchModal(true)} style={{
          background: canLaunch ? "#0e0e10" : "#ececf0",
          color: canLaunch ? "#fff" : "#9a9aa1",
          border: `1px solid ${canLaunch ? "#0e0e10" : "#ececf0"}`,
          padding: "12px 24px", borderRadius: 4, fontSize: 13, fontWeight: 800,
          letterSpacing: ".06em", cursor: canLaunch ? "pointer" : "not-allowed"
        }} title={canLaunch ? "Launch this team store" : "Add at least one product to launch your store"}>LAUNCH STORE</button>
        }
        </div>
      </div>

      {/* Tabs */}
      <div style={{ borderBottom: "1px solid #ececf0", marginBottom: 24, display: "flex", gap: 4, position: "relative" }}>
        <span aria-hidden="true" style={{
          position: "absolute", bottom: -1, height: 2, background: "#0e0e10",
          left: tabIndicator.left, width: tabIndicator.width,
          borderRadius: "1px 1px 0 0",
          transition: "left .2s cubic-bezier(.4,0,.2,1), width .2s cubic-bezier(.4,0,.2,1)",
        }} />
        {tabs.map((t) => {
          const active = tab === t.id;
          return (
            <button key={t.id} onClick={() => setTab(t.id)}
            ref={(el) => { tabBtnRefs.current[t.id] = el; }}
            data-tour={t.id === "products" ? "tab-products" : t.id === "roster" ? "tab-roster" : undefined}
            style={{
              background: "transparent", border: 0, padding: "16px 24px",
              fontSize: 13, fontWeight: active ? 700 : 500,
              color: active ? "#0e0e10" : "#6b6b72",
              cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8,
              marginBottom: -1, transition: "color .15s ease",
            }}>
              {t.label}
              <TabDot show={t.dot} />
            </button>);
        })}
      </div>

      {tabContent}

      <StoreCloseReportHost store={store} />
      <StoreSetupGuideHost store={store} />
      {launchModals}
    </div>);

}

// ─────────── Orders tab ───────────
function OrdersTab() {
  const [sub, setSub] = React.useState("pending");
  const subs = [
  { id: "my", label: "My Orders" },
  { id: "pending", label: "Pending Orders" },
  { id: "fan", label: "Fan Orders" }];

  return (
    <div>
      <div style={{ display: "flex", gap: 12, marginBottom: 40 }}>
        {subs.map((s) => {
          const active = sub === s.id;
          return (
            <button key={s.id} onClick={() => setSub(s.id)} style={{
              background: active ? "#0e0e10" : "#fff",
              color: active ? "#fff" : "#0e0e10",
              border: `1px solid ${active ? "#0e0e10" : "#d4d4d8"}`,
              padding: "8px 16px", borderRadius: 999,
              fontSize: 13, fontWeight: 600, cursor: "pointer",
              transition: "all .12s"
            }}>
              {s.label}
            </button>);

        })}
      </div>
      <EmptyTabState
        iconKind="chart"
        title={sub === "my" ? "No Orders Yet" : sub === "pending" ? "No Pending Orders" : "No Fan Orders"}
        body={
        sub === "my" ? "Your placed orders will appear here once you've made a purchase." :
        sub === "pending" ? "When customers place orders through your team store, they'll appear here for review." :
        "Fan and supporter orders will show up here as your store generates sales."
        } />
      
    </div>);

}

// ─────────── Reports tab ───────────
// Analytical dashboard mirroring the master Overview dashboard: a four-card KPI
// row, plus a Team Revenue bar chart and the Fundraising Report below it.
const REP_INK = "#0e0e10";
const REP_MUTED = "#6b6b72";
const REP_LINE = "#ececf0";
const REP_RED = "#e1251b";
const REP_GREEN = "#1f9d55";

function repFmt(n) {
  return "$" + Math.round(n).toLocaleString("en-US");
}
function repFmtK(n) {
  if (n >= 1000) return "$" + (n / 1000).toFixed(n >= 10000 ? 0 : 1) + "K";
  return "$" + Math.round(n);
}

// Deterministic monthly profit series that builds to a clean trend.
function buildProfit(total) {
  const weights = [0.6, 0.8, 1.1, 0.7, 0.9, 1.2, 1.35, 1.5, 1.0, 1.15, 1.3, 1.05];
  const wsum = weights.reduce((a, b) => a + b, 0);
  const now = new Date();
  return weights.map((w, i) => {
    const d = new Date(now.getFullYear(), now.getMonth() - (11 - i), 1);
    return { month: d.toLocaleDateString("en-US", { month: "short" }), profit: Math.round((total * w) / wsum), current: i === 11 };
  });
}

// KPI summary card — identical to the Overview dashboard's StatCard.
function RepStatCard({ label, value, icon, delta, deltaDir = "up", sub, onClick }) {
  const Trend = deltaDir === "up" ? IconTrendUp : IconTrendDown;
  const trendColor = deltaDir === "up" ? REP_GREEN : REP_RED;
  return (
    <div
      onClick={onClick}
      role={onClick ? "button" : undefined}
      title={onClick ? "View orders" : undefined}
      style={{
      background: "#fff", border: `1px solid ${REP_LINE}`, borderRadius: 12, padding: "20px 24px",
      display: "flex", flexDirection: "column", gap: 16, cursor: onClick ? "pointer" : "default",
      transition: "box-shadow .12s, border-color .12s"
    }}
      onMouseEnter={onClick ? (e) => {e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)";e.currentTarget.style.borderColor = "#c9c9cf";} : undefined}
      onMouseLeave={onClick ? (e) => {e.currentTarget.style.boxShadow = "none";e.currentTarget.style.borderColor = REP_LINE;} : undefined}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
        <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".07em", color: REP_MUTED, textTransform: "uppercase" }}>{label}</span>
        <span style={{ width: 36, height: 36, borderRadius: 9, background: "#f5f5f7", color: REP_INK, display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{icon}</span>
      </div>
      <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 32, fontWeight: 800, color: REP_INK, lineHeight: 1, letterSpacing: "-.01em" }}>{value}</div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12.5 }}>
        {delta != null &&
        <span style={{ display: "inline-flex", alignItems: "center", gap: 4, color: trendColor, fontWeight: 700 }}>
            <Trend size={15} /> {delta}
          </span>
        }
        <span style={{ color: REP_MUTED }}>{sub}</span>
      </div>
    </div>);

}

// Card shell — identical to the Overview dashboard's Panel.
function RepPanel({ title, children, style }) {
  return (
    <div style={{ background: "#fff", border: `1px solid ${REP_LINE}`, borderRadius: 12, padding: "20px 24px", ...style }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
        <h3 style={{ fontSize: 16, fontWeight: 800, margin: 0, color: REP_INK, letterSpacing: ".01em" }}>{title}</h3>
      </div>
      {children}
    </div>);

}

// Vertical column bar chart — same construction as the Overview Revenue chart.
function TeamProfitChart({ data }) {
  const max = Math.max(...data.map((d) => d.profit), 1);
  const [hover, setHover] = React.useState(null);
  const ticks = 4;
  return (
    <div style={{ position: "relative", height: 340, paddingLeft: 48, paddingBottom: 28 }}>
      {/* Y axis */}
      <div style={{ position: "absolute", left: 0, top: 0, bottom: 28, width: 44, display: "flex", flexDirection: "column", justifyContent: "space-between", alignItems: "flex-end", fontSize: 11, color: "#a4a4ab", fontFamily: "JetBrains Mono, monospace" }}>
        {Array.from({ length: ticks + 1 }).map((_, i) =>
        <span key={i}>{repFmtK((max * (ticks - i)) / ticks)}</span>
        )}
      </div>
      {/* Plot */}
      <div style={{ position: "relative", height: "100%", borderLeft: `1px solid ${REP_LINE}`, borderBottom: `1px solid ${REP_LINE}` }}>
        {Array.from({ length: ticks }).map((_, i) =>
        <div key={i} style={{ position: "absolute", left: 0, right: 0, top: `${i / ticks * 100}%`, borderTop: "1px dashed #f1f1f3" }} />
        )}
        <div style={{ position: "absolute", inset: "8px 8px 0", display: "flex", alignItems: "flex-end", justifyContent: "space-between", gap: 8 }}>
          {data.map((d, i) => {
            const totalH = d.profit / max * 100;
            return (
              <div key={d.month} onMouseEnter={() => setHover(i)} onMouseLeave={() => setHover(null)}
                style={{ position: "relative", flex: 1, display: "flex", flexDirection: "column", alignItems: "center", gap: 8, height: "100%", justifyContent: "flex-end", cursor: "default" }}>
                {hover === i && (
                  <div style={{ position: "absolute", bottom: `calc(${totalH}% + 10px)`, left: "50%", transform: "translateX(-50%)", background: REP_INK, color: "#fff", borderRadius: 8, padding: "8px 12px", whiteSpace: "nowrap", zIndex: 5, boxShadow: "0 6px 18px rgba(20,20,30,.22)", pointerEvents: "none" }}>
                    <div style={{ fontSize: 11, color: "#a4a4ab", marginBottom: 2 }}>{d.month}{d.current ? " · current" : ""}</div>
                    <div style={{ fontSize: 13.5, fontWeight: 700, fontFamily: "JetBrains Mono, monospace" }}>{repFmt(d.profit)}</div>
                  </div>
                )}
                <div style={{ width: "100%", maxWidth: 48, height: `${totalH}%`, borderRadius: 5, background: d.current ? REP_RED : REP_INK, opacity: hover === i ? 0.82 : 1, transition: "opacity .12s" }} />
                <span style={{ fontSize: 11, color: d.current ? REP_INK : REP_MUTED, fontWeight: d.current ? 700 : 400, position: "absolute", bottom: -24 }}>{d.month}</span>
              </div>);

          })}
        </div>
      </div>
    </div>);

}

// ─────────── Fundraising Report (Reports tab) ───────────
const FR_GREEN = "#6cbd45";
const FR_GREEN_HOVER = "#5faf3a";

function frMoney(n) {
  return "$" + (Math.round(n * 100) / 100).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
}
function frShortDate(d) {
  const dt = d instanceof Date ? d : new Date(d);
  const p = (n) => String(n).padStart(2, "0");
  return `${p(dt.getMonth() + 1)}/${p(dt.getDate())}/${String(dt.getFullYear()).slice(2)}`;
}
function frLongDate(d) {
  const dt = d instanceof Date ? d : new Date(d);
  const p = (n) => String(n).padStart(2, "0");
  return `${p(dt.getMonth() + 1)}/${p(dt.getDate())}/${dt.getFullYear()}`;
}

function FundraisingReport({ store }) {
  const teamName = store?.teamName || store?.storeTitle || "Team Store";
  const platformFeeLabel = store?.platformFeeLabel || "Platform Fees";
  const revenue = store?.revenue || 3810;
  const orders = Math.max(Math.round(revenue / 46), 1);

  // Fundraising is a subset of collected revenue (price additions flagged as
  // "Fundraising" on products). Modeled off store revenue for the prototype.
  const collected = Math.round(revenue * 0.18 * 100) / 100;
  const platformFees = Math.round(collected * 0.05 * 100) / 100;
  const cardFees = Math.round((collected * 0.029 + orders * 0.3) * 100) / 100;
  const net = Math.round((collected - platformFees - cardFees) * 100) / 100;

  const defStart = new Date(store?.storeOpenStart || "2026-02-03");
  const defEnd = new Date(store?.storeOpenEnd || "2026-07-06");
  const [range, setRange] = React.useState({ start: defStart, end: defEnd });
  const [pickerOpen, setPickerOpen] = React.useState(false);
  const [settingsOpen, setSettingsOpen] = React.useState(false);
  const pickerRef = React.useRef();
  React.useEffect(() => {
    const h = (e) => { if (pickerRef.current && !pickerRef.current.contains(e.target)) setPickerOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);

  const presets = [
    { label: "This season", start: defStart, end: defEnd },
    { label: "Last 7 days", start: new Date("2026-07-06"), end: new Date("2026-07-13") },
    { label: "Last 30 days", start: new Date("2026-06-13"), end: new Date("2026-07-13") },
    { label: "Year to date", start: new Date("2026-01-01"), end: new Date("2026-07-13") },
  ];

  const rows = [
    { label: "Fundraising Collected", val: collected },
    { label: platformFeeLabel, val: platformFees },
    { label: "Card Processing Fees", val: cardFees },
    { label: "Net Fundraising", val: net, bold: true },
  ];

  return (
    <div style={{ marginTop: 16 }}>
      {/* Report card */}
      <div style={{ background: "#fff", border: `1px solid ${REP_LINE}`, borderRadius: 12, padding: "24px 28px 28px", marginTop: 16 }}>
        {/* internal header block */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 20, marginBottom: 16, flexWrap: "wrap" }}>
          <div style={{ fontSize: 16, fontWeight: 800, color: REP_INK, letterSpacing: "-.01em" }}>Fundraising Report</div>
          <div style={{ display: "flex", alignItems: "center", gap: 12, flexWrap: "wrap" }}>
          <div ref={pickerRef} style={{ position: "relative" }}>
            <button onClick={() => setPickerOpen((v) => !v)} style={{
              display: "inline-flex", alignItems: "center", gap: 12, background: "#fff",
              border: `1.5px solid ${pickerOpen ? FR_GREEN : "#d4d4d8"}`, borderRadius: 8,
              padding: "12px 16px", cursor: "pointer", fontSize: 14, fontWeight: 600, color: REP_INK, transition: "border-color .12s",
            }}>
              <IconCalendar size={17} style={{ color: REP_MUTED }} />
              <span>{frShortDate(range.start)} - {frShortDate(range.end)}</span>
              <IconChevron size={16} style={{ color: REP_MUTED }} />
            </button>
            {pickerOpen && (
              <div style={{ position: "absolute", top: "calc(100% + 8px)", right: 0, zIndex: 20, minWidth: 220, background: "#fff", border: `1px solid ${REP_LINE}`, borderRadius: 10, boxShadow: "0 12px 30px rgba(20,20,30,.14)", padding: 8 }}>
                {presets.map((p) => {
                  const active = frShortDate(p.start) === frShortDate(range.start) && frShortDate(p.end) === frShortDate(range.end);
                  return (
                    <button key={p.label} onClick={() => { setRange({ start: p.start, end: p.end }); setPickerOpen(false); }} style={{
                      width: "100%", textAlign: "left", background: active ? "#f2faec" : "transparent", border: 0, borderRadius: 7,
                      padding: "12px 12px", cursor: "pointer", fontSize: 13.5, fontWeight: 600, color: REP_INK,
                      display: "flex", justifyContent: "space-between", alignItems: "center", gap: 12,
                    }}
                      onMouseEnter={(e) => { if (!active) e.currentTarget.style.background = "#fafafb"; }}
                      onMouseLeave={(e) => { if (!active) e.currentTarget.style.background = "transparent"; }}>
                      <span>{p.label}</span>
                      <span style={{ fontSize: 11.5, color: REP_MUTED, fontWeight: 500 }}>{frShortDate(p.start)}–{frShortDate(p.end)}</span>
                    </button>
                  );
                })}
              </div>
            )}
          </div>
          <button onClick={() => setSettingsOpen(true)} style={{
            display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", border: 0, borderRadius: 8,
            padding: "12px 20px", cursor: "pointer", fontSize: 14, fontWeight: 700, color: "#fff", transition: "background .12s",
          }}
            onMouseEnter={(e) => (e.currentTarget.style.background = "#2a2a2e")}
            onMouseLeave={(e) => (e.currentTarget.style.background = "#0e0e10")}>
            <IconDownload size={17} /> Download PDF
          </button>
          </div>
        </div>

        {/* Detailed breakdown matrix */}
        <div style={{ border: `1px solid ${REP_LINE}`, borderRadius: 10, overflow: "hidden" }}>
          <div style={{ padding: "16px 16px", background: "#f7f7f8", borderBottom: `1px solid ${REP_LINE}`, fontSize: 14, fontWeight: 700, color: REP_INK }}>Detailed breakdown</div>
          {rows.map((r, i) => (
            <div key={r.label} style={{
              display: "flex", alignItems: "center", justifyContent: "space-between",
              padding: "16px 16px", borderBottom: i < rows.length - 1 ? `1px solid ${REP_LINE}` : "none",
            }}>
              <span style={{ fontSize: 14.5, fontWeight: r.bold ? 800 : 500, color: REP_INK }}>{r.label}</span>
              <span style={{ fontSize: 14.5, fontWeight: r.bold ? 800 : 500, color: REP_INK, fontFamily: "JetBrains Mono, monospace" }}>{frMoney(r.val)}</span>
            </div>
          ))}
        </div>

        {/* Analytical subtext */}
        <p style={{ margin: "16px 2px 0", fontSize: 14, color: REP_MUTED, lineHeight: 1.6 }}>
          You&apos;ve currently collected a total fundraising amount of <strong style={{ color: REP_INK }}>{frMoney(collected)}</strong> from all <strong style={{ color: REP_INK }}>{orders}</strong> order(s) to-date. After fees of <strong style={{ color: REP_INK }}>{frMoney(platformFees + cardFees)}</strong>, your net fundraising is <strong style={{ color: REP_INK }}>{frMoney(net)}</strong>.
        </p>

        {/* Sticky note banner */}
        <div style={{ marginTop: 16, background: "#f5f5f7", borderRadius: 10, padding: "16px 16px", fontSize: 13.5, color: "#4a4a52", lineHeight: 1.55 }}>
          <strong style={{ color: REP_INK, fontWeight: 700 }}>NOTE:</strong> This report only includes price additions that are marked as &quot;Fundraising&quot; on the product edit page.
        </div>
      </div>

      <FundraisingReportSettingsModal open={settingsOpen} onClose={() => setSettingsOpen(false)} range={range} />
    </div>
  );
}

function FrRadio({ selected, onSelect, title, sub }) {
  return (
    <button onClick={onSelect} style={{
      display: "flex", alignItems: "flex-start", gap: 16, width: "100%", textAlign: "left",
      background: "transparent", border: 0, cursor: "pointer", padding: "4px 0",
    }}>
      <span style={{
        flexShrink: 0, marginTop: 2, width: 24, height: 24, borderRadius: "50%",
        border: `2px solid ${selected ? "#0e0e10" : "#c4c4c8"}`, display: "inline-flex", alignItems: "center", justifyContent: "center",
        transition: "border-color .12s",
      }}>
        {selected && <span style={{ width: 12, height: 12, borderRadius: "50%", background: "#0e0e10" }} />}
      </span>
      <span>
        <span style={{ display: "block", fontSize: 16, fontWeight: 800, color: REP_INK, marginBottom: 4 }}>{title}</span>
        <span style={{ display: "block", fontSize: 14, color: REP_MUTED, lineHeight: 1.5 }}>{sub}</span>
      </span>
    </button>
  );
}

function FrCheckbox({ checked, onToggle, title, sub }) {
  return (
    <button onClick={onToggle} style={{
      display: "flex", alignItems: "flex-start", gap: 16, width: "100%", textAlign: "left",
      background: "transparent", border: 0, cursor: "pointer", padding: "4px 0",
    }}>
      <span style={{
        flexShrink: 0, marginTop: 2, width: 24, height: 24, borderRadius: 6,
        border: `2px solid ${checked ? "#0e0e10" : "#c4c4c8"}`, background: checked ? "#0e0e10" : "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center", transition: "all .12s",
      }}>
        {checked && <IconCheck size={14} style={{ color: "#fff", strokeWidth: 3 }} />}
      </span>
      <span>
        <span style={{ display: "block", fontSize: 16, fontWeight: 800, color: REP_INK, marginBottom: 4 }}>{title}</span>
        <span style={{ display: "block", fontSize: 14, color: REP_MUTED, lineHeight: 1.5 }}>{sub}</span>
      </span>
    </button>
  );
}

function FundraisingReportSettingsModal({ open, onClose, range }) {
  const [summary, setSummary] = React.useState("net");
  const [opts, setOpts] = React.useState({ breakdown: false, dateRange: false, notes: false });
  React.useEffect(() => {
    if (open) { setSummary("net"); setOpts({ breakdown: false, dateRange: false, notes: false }); }
  }, [open]);
  const toggle = (k) => setOpts((p) => ({ ...p, [k]: !p[k] }));
  const rangeStr = range ? `${frLongDate(range.start)} - ${frLongDate(range.end)}` : "";

  return (
    <Modal open={open} onClose={onClose} width={720}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "86vh" }}>
        {/* header */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "28px 32px", borderBottom: `1px solid ${REP_LINE}` }}>
          <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 26, fontWeight: 800, margin: 0, color: REP_INK, letterSpacing: "-.01em" }}>Fundraising Report Settings</h2>
          <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#9a9aa1", display: "inline-flex" }}>
            <IconX size={24} />
          </button>
        </div>

        {/* body */}
        <div style={{ padding: "28px 32px", overflowY: "auto" }}>
          <div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
            <FrRadio selected={summary === "net"} onSelect={() => setSummary("net")}
              title="Net Fundraising Summary (Default)"
              sub="Displays the final fundraising amount after deducting all associated fees for the selected date range." />
            <FrRadio selected={summary === "gross"} onSelect={() => setSummary("gross")}
              title="Gross Total Summary"
              sub="Shows only the total fundraising collected before any fees are deducted." />
          </div>

          <div style={{ height: 1, background: REP_LINE, margin: "28px 0" }} />

          <div style={{ fontSize: 16, fontWeight: 800, color: REP_INK, marginBottom: 16 }}>Report Details:</div>
          <div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
            <FrCheckbox checked={opts.breakdown} onToggle={() => toggle("breakdown")}
              title="Detailed Breakdown (Full Report)"
              sub="Includes the full line-item breakdown of collected funds, fees, and the final net amount." />
            <FrCheckbox checked={opts.dateRange} onToggle={() => toggle("dateRange")}
              title="Selected Date Range"
              sub={`Adds the selected date range (${rangeStr}) to the top of the report PDF.`} />
            <FrCheckbox checked={opts.notes} onToggle={() => toggle("notes")}
              title="Include PDF Report Notes"
              sub={'Adds the notes from the "PDF Report Notes" section to the printed report output.'} />
          </div>
        </div>

        {/* footer */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 16, padding: "16px 32px", borderTop: `1px solid ${REP_LINE}` }}>
          <button onClick={onClose} style={{
            background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8, padding: "12px 24px",
            fontSize: 14, fontWeight: 700, color: REP_INK, cursor: "pointer", fontFamily: "inherit",
          }}>Cancel</button>
          <button onClick={onClose} style={{
            display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", border: 0, borderRadius: 8,
            padding: "12px 24px", fontSize: 14, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit", transition: "background .12s",
          }}
            onMouseEnter={(e) => (e.currentTarget.style.background = "#2a2a2e")}
            onMouseLeave={(e) => (e.currentTarget.style.background = "#0e0e10")}>
            <IconDownload size={17} /> Download PDF
          </button>
        </div>
      </div>
    </Modal>
  );
}

function PayoutsTab({ store, onViewAllOrders, onViewPendingOrders }) {
  // available so the page reads consistently across stores.
  const revenue = store?.revenue || 3810;
  const orders = Math.max(Math.round(revenue / 46), 1);
  const pending = Math.max(Math.round(orders * 0.17), 1);
  const aov = revenue / orders;

  const totalProfit = Math.round(revenue * 0.34);
  // Team Revenue chart — gross store revenue over the window (not a margin chart).
  const revenueData = buildProfit(revenue);
  const pendingClearance = Math.round(totalProfit * 0.24);
  const paidThisSeason = Math.round(totalProfit * 0.76);

  return (
    <div>
      <StoreCloseReportEntry store={store} variant="reports" />
      {/* KPI row — four uniform summary cards */}
      <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 16, marginBottom: 16 }}>
        <RepStatCard label="Total Revenue" value={repFmt(revenue)} icon={<IconDollar size={18} />} delta="12.4%" deltaDir="up" sub="vs last month" />
        <RepStatCard label="Orders" value={orders.toLocaleString()} icon={<IconReceipt size={18} />} delta="8.1%" deltaDir="up" sub="vs last month" onClick={onViewAllOrders} />
        <RepStatCard label="Pending Orders" value={pending.toLocaleString()} icon={<IconClock size={18} />} delta="4.6%" deltaDir="up" sub="vs last month" onClick={onViewPendingOrders} />
        <RepStatCard label="Avg. Order Value" value={repFmt(aov)} icon={<IconWallet size={18} />} delta="3.2%" deltaDir="down" sub="vs last month" />
      </div>

      {/* Team Revenue chart — full width. Gross revenue over the window, not margin. */}
      <RepPanel title="Team Revenue">
        <TeamProfitChart data={revenueData} />
      </RepPanel>

      {/* Fundraising Report — also renders when store-level fundraising is off but
          ordered items are still collecting, with a banner explaining that state. */}
      {(store?.fundraisingEnabled === true || fundCollectingItems(store).length > 0) && (
        <React.Fragment>
          {store?.fundraisingEnabled !== true && (() => {
            const n = fundCollectingItems(store).length;
            return (
              <div style={{ display: "flex", alignItems: "flex-start", gap: 12, background: "#fdf8ee", border: "1px solid #f2e2c9", borderRadius: 10, padding: "14px 18px", marginTop: 16 }}>
                <span style={{ color: "#9a6b15", display: "inline-flex", marginTop: 1 }}><IconInfo size={16} /></span>
                <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>
                  <strong style={{ color: "#0e0e10" }}>Team Fundraising is disabled for this store.</strong> {n} item{n === 1 ? "" : "s"} with orders {n === 1 ? "is" : "are"} still collecting, so this report stays active. Entered amounts were kept.
                </span>
              </div>
            );
          })()}
          <FundraisingReport store={store} />
        </React.Fragment>
      )}
    </div>);

}
// Internal helper — produces a "$min - $max" string for a given size, used when
// snapshotting bundle pricing.
function rangePart(items, size) {
  const vals = items.
  map((p) => parseFloat(p?.pricing?.[size]?.retail)).
  filter((v) => !isNaN(v));
  if (vals.length === 0) return "—";
  const min = Math.min(...vals);
  const max = Math.max(...vals);
  if (min === max) return min.toFixed(2);
  return `${min.toFixed(2)} - ${max.toFixed(2)}`;
}

// ─────────── Select Application Size prompt ───────────
// Shown when the user clicks "Add to Team Store" before confirming sizes on
// every pending product. "Specify Sizing" returns them to the configure cards;
// "Label As Shown" sets all applications to AS SHOWN and commits.
function SelectApplicationSizeModal({ open, onClose, onSpecify, onAsShown }) {
  return (
    <Modal open={open} onClose={onClose} width={760}>
      <div style={{ padding: "28px 32px 32px" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", paddingBottom: 16, borderBottom: "1px solid #ececf0" }}>
          <h2 style={{ fontSize: 24, fontWeight: 800, margin: 0, letterSpacing: ".01em", color: "#0e0e10" }}>SELECT APPLICATION SIZE</h2>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#0e0e10", display: "inline-flex"
          }}>
            <IconX size={22} style={{ strokeWidth: 2 }} />
          </button>
        </div>

        <div style={{ padding: "40px 0 48px", color: "#6b6b72", fontSize: 17, lineHeight: 1.55 }}>
          <p style={{ margin: "0 0 28px" }}>
            Application sizes have not been specified for all products. Would you like to set all applications to <strong style={{ color: "#0e0e10", fontWeight: 700 }}>&ldquo;As Shown&rdquo;</strong>?
          </p>
          <p style={{ margin: "0 0 28px" }}>
            We will match applications as close as possible to what is shown on the design.
          </p>
          <p style={{ margin: 0 }}>
            Choose &ldquo;<strong style={{ color: "#0e0e10", fontWeight: 700 }}>Specify Sizing</strong>&rdquo; if you have specific sizing requirements<br />
            (Example: Need to follow NFHS rules)
          </p>
        </div>

        <div style={{ display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 24 }}>
          <button onClick={onSpecify} style={{
            background: "transparent", border: 0, cursor: "pointer",
            fontSize: 16, fontWeight: 700, color: "#0e0e10", fontFamily: "inherit", padding: "8px 4px"
          }}>Specify Sizing</button>
          <button onClick={onAsShown} style={{
            background: "#0e0e10", border: 0, borderRadius: 8, cursor: "pointer",
            fontSize: 16, fontWeight: 700, color: "#fff", fontFamily: "inherit", padding: "16px 28px"
          }}>Label &ldquo;As Shown&rdquo;</button>
        </div>
      </div>
    </Modal>);

}

// ─────────── Add Products — centralized repository page ───────────
// Consolidates every intake channel (Saved Designs, Saved Carts, Custom Products)
// onto one dedicated page with a three-tab sub-navigation. Replaces the legacy
// fork-in-the-road modal. Mirrors Step 2 (Products) of the store-creation wizard.
function AddProductsPage({ store, onCancel, onCommit, customizer, onChangeCustomizer, liveStores }) {
  const accent = "#e1251b";
  const [staged, setStaged] = React.useState([]);            // items added this session
  const [cfgItems, setCfgItems] = React.useState(null);      // items being size-confirmed
  const [cfgState, setCfgState] = React.useState([]);        // parallel per-item config
  const [cfgActive, setCfgActive] = React.useState(0);
  const [cfgEdit, setCfgEdit] = React.useState(false); // Edit Product re-uses the Configure & Add modal
  const [toast, setToast] = React.useState(null);
  // Customize New Product — XL 4-step workflow (sport → product → builder → review)
  const [customizerOpen, setCustomizerOpen] = React.useState(false);
  const [custWizStep, setCustWizStep] = React.useState(0);
  const [custSport, setCustSport] = React.useState(null);
  const [custProduct, setCustProduct] = React.useState(null);

  const committedIds = new Set(staged.map((s) => s.srcId).filter(Boolean));
  const customStaged = staged.filter((s) => !s.srcId);
  const inStoreCount = (store.products || []).length + staged.length;

  const beginAdd = (items) => {
    setCfgItems(items);
    setCfgState(items.map((it) => ({
      applicationSizes: defaultApplicationSizes(),
      teamSetting: "fan-gear",
      pricing: pendingPricing(it),
      sizesConfirmed: false,
    })));
    setCfgEdit(false);
    setCfgActive(0);
  };
  // Edit Product on a staged card — re-opens the same Configure & Add modal seeded with the saved config.
  const beginEditCommitted = (srcId) => {
    const items = staged.filter((c) => c.srcId === srcId || c.id === srcId || c.pkgId === srcId);
    if (!items.length) return;
    setCfgItems(items.map((c) => ({ ...c })));
    setCfgState(items.map((c) => ({
      applicationSizes: c.applicationSizes || defaultApplicationSizes(),
      teamSetting: c.teamSetting || "fan-gear",
      pricing: c.pricing || pendingPricing(c),
      rosters: c.rosters || [],
      storeCategory: c.storeCategory || "", storeSubcategory: c.storeSubcategory || "",
      optRequired: !!c.optRequired, optSupplied: !!c.optSupplied,
      sizesConfirmed: !!c.sizesConfirmed,
    })));
    setCfgEdit(true);
    setCfgActive(0);
  };
  const updateActiveConfig = (next) => setCfgState((cs) => cs.map((c, i) => i === cfgActive ? next : c));
  const updateActiveRows = (rows) => setCfgState((cs) => cs.map((c, i) => i === cfgActive ? { ...c, applicationSizes: rows } : c));

  const stageItems = (arr) => setStaged((prev) => {
    const existing = new Set(prev.map((c) => c.id));
    return [...prev, ...arr.filter((s) => !existing.has(s.id))];
  });

  const confirmConfig = () => {
    if (!cfgItems) return;
    const stamped = cfgItems.map((it, i) => {
      const cfg = cfgState[i];
      return {
        ...it,
        teamSetting: cfg.teamSetting,
        storeCategory: (cfg.optRequired || cfg.optSupplied || cfg.teamSetting === "team-required" || cfg.teamSetting === "team-supplied") ? "Team Gear" : (cfg.storeCategory || ""),
        storeSubcategory: cfg.storeSubcategory || "",
        optRequired: !!cfg.optRequired || cfg.teamSetting === "team-required", optSupplied: !!cfg.optSupplied || cfg.teamSetting === "team-supplied",
        applicationSizes: cfg.applicationSizes,
        pricing: cfg.pricing,
        rosters: cfg.rosters || [],
        adult: cfg.pricing.adult.retail,
        youth: cfg.pricing.youth.retail,
        sizesConfirmed: true,
      };
    });
    if (cfgEdit) {
      const byId = new Map(stamped.map((s) => [s.id, s]));
      setStaged((prev) => prev.map((c) => byId.has(c.id) ? { ...c, ...byId.get(c.id) } : c));
      setCfgItems(null);
      setCfgEdit(false);
      setToast("Product updated");
      return;
    }
    stageItems(stamped);
    setCfgItems(null);
    setToast(stamped.length > 1 ? `Added ${stamped.length} products` : `Added "${stamped[0].title}"`);
  };

  const addDesign = (d) => beginAdd([{ id: `wd-${d.id}`, srcId: d.id, title: d.title, style: `${d.sport} · ${d.category}`, photo: d.photo, adult: d.adult, youth: d.youth, teamProfit: d.teamProfit }]);
  const addCart = (c) => beginAdd(c.items.map((it) => ({ id: `wc-${it.id}`, srcId: c.id, title: it.title, style: "From " + c.name, photo: it.photo, adult: it.adult, youth: it.youth, teamProfit: it.teamProfit })));

  // ── Stock Vault + Other stores — Epic A/G (2026-08 revision).
  // Third-party seller catalog is paused for this pass — Vault is Prolook-only. ──
  const [vaultOffer, setVaultOffer] = React.useState(null);
  const addVaultItem = (item) => setVaultOffer(item);
  const confirmVaultOffer = (item, sizes, cws) => {
    const cw = (cws && cws[0]) || hcmDefaultCw(item);
    setVaultOffer(null);
    beginAdd([{ id: "wv-" + item.id, srcId: item.id, title: item.title, style: (item.source || "Stock Vault") + " · " + (item.vaultCategory || ""), srcKind: "vault", photo: cw && cw.front, sku: item.sku, priceAdult: item.priceAdult, priceYouth: item.priceYouth, adult: item.priceAdult, youth: item.priceYouth != null ? item.priceYouth : item.priceAdult, colorway: cw && cw.name, colorways: (cws && cws.length ? cws : item.colorways), offeredSizes: sizes }]);
  };
  const addTemplate = (t) => beginAdd(t.members.map((m) => ({ id: "wt-" + t.id + "-" + m.id, srcId: t.id + "-" + m.id, tplId: t.id, title: m.title, style: m.styleBlock, srcKind: "template", designLocked: !!m.locked, photo: m.photo, sku: m.sku })));
  const addFromOtherStore = (p, storeName) => beginAdd([{ id: "wo-" + p.id, srcId: p.id, title: p.title, style: (p.sku || p.subtitle || "Style block") + " · from " + storeName, srcKind: "other-store", photo: p.photo }]);

  const proStockItems = (window.__prostockItems || []).filter((i) => i.stockType !== "sideline");
  const sidelineItems = (window.__prostockItems || []).filter((i) => i.stockType === "sideline");
  const [tplBump, setTplBump] = React.useState(0);
  const renameTpl = (t, name) => { window.__catalogTemplates = (window.__catalogTemplates || []).map((x) => x.id === t.id ? { ...x, name } : x); setTplBump((n) => n + 1); setToast("Template renamed to \u201c" + name + "\u201d"); };
  const deleteTpl = (t) => { window.__catalogTemplates = (window.__catalogTemplates || []).filter((x) => x.id !== t.id); setTplBump((n) => n + 1); setToast("Template \u201c" + t.name + "\u201d deleted"); };
  const otherStores = (liveStores || []).filter((s) => s.id !== store.id);

  const openCustomizer = () => { setCustomizerOpen(true); setCustWizStep(0); setCustSport(null); setCustProduct(null); };


  return (
    <div style={{ paddingBottom: 92 }}>
      {/* Header context */}
      <div style={{ marginBottom: 24 }}>
        <h2 style={{ fontSize: 18, fontWeight: 800, letterSpacing: ".01em", margin: "0 0 8px", color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Add Products</h2>
        <p style={{ fontSize: 14, color: "#6b6b72", margin: 0, lineHeight: 1.55, maxWidth: 780 }}>
          Pull products from the Customizer, the Stock Vault, a saved Catalog Template, or items already used in your other stores. Each item confirms its application sizes before it's added to the store.
        </p>
      </div>

      {customizer && (
        <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 16, width: "fit-content", background: "#f4f4f5", border: "1px solid #ececf0", borderRadius: 999, padding: "8px 16px 8px 12px" }}>
          <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".04em", textTransform: "uppercase", color: "#9a9aa1" }}>Source</span>
          {customizer.logoBlack || customizer.logoInk ? (
            <img src={customizer.logoBlack || customizer.logoInk} alt={customizer.name} style={{ height: 16, width: "auto" }} />
          ) : (
            <span style={{ fontSize: 13.5, fontWeight: 800, color: "#0e0e10" }}>{customizer.wordmark || customizer.name}</span>
          )}
          <button onClick={onChangeCustomizer || onCancel} title="Change customizer" style={{ border: 0, background: "transparent", cursor: "pointer", color: "#6b6b72", fontSize: 12.5, fontWeight: 600, padding: 0, textDecoration: "underline", textUnderlineOffset: 2 }}>Change</button>
        </div>
      )}

      <AddProductsBrowser accent={accent}
        right={
          <button type="button" onClick={openCustomizer} style={{
            display: "inline-flex", alignItems: "center", gap: 8, flexShrink: 0, height: 40,
            background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "0 16px",
            fontSize: 12.5, fontWeight: 700, letterSpacing: ".01em", cursor: "pointer", whiteSpace: "nowrap",
          }}><IconPlus size={15} style={{ strokeWidth: 2.6 }} /> Customize New Product</button>
        }
        groups={[
        { key: "catalog", label: "Customizer", subs: [{ k: "designs", l: "Saved Designs" }, { k: "carts", l: "Saved Carts" }], items: [].concat(
          WIZARD_SAVED_DESIGNS.map((d) => ({ _key: "d-" + d.id, sub: "designs", kind: "design", badge: "Saved design", title: d.title, subtitle: d.block || `${d.sport} · ${d.category}`, photo: d.photo,
            committed: committedIds.has(d.id), onAdd: () => addDesign(d), onEdit: () => beginEditCommitted(d.id), onRemove: () => {} })),
          WIZARD_SAVED_CARTS.map((c) => ({ _key: "c-" + c.id, sub: "carts", kind: "cart", badge: "Saved cart", title: c.name, subtitle: `${c.items.length} items · $${c.total.toFixed(2)}`, items: c.items,
            committed: committedIds.has(c.id), onAdd: () => addCart(c), onEdit: () => beginEditCommitted(c.id), onRemove: () => {} })),
          customStaged.map((p) => ({ _key: "x-" + p.id, sub: "designs", kind: "design", badge: "Custom", title: p.title, subtitle: p.style || "Custom product", photo: p.photo,
            committed: true, onAdd: () => {}, onEdit: () => beginEditCommitted(p.id), onRemove: () => {} }))
        ) },
        { key: "vault", label: "Stock Vault", subs: [{ k: "pro", l: "Pro Stock" }, { k: "sideline", l: "Sideline Stock" }], items: [].concat(
          proStockItems.map((item) => ({ _key: "v-" + item.id, sub: "pro", kind: "design", badge: "Pro Stock", title: item.title, subtitle: `${vaultGenderLine(item)} · ${item.colorways.length} color ways · ${vaultPriceLine(item)}`, photo: hcmDefaultCw(item) && hcmDefaultCw(item).front,
            committed: committedIds.has(item.id), onAdd: () => addVaultItem(item), onEdit: () => beginEditCommitted(item.id), onRemove: () => {} })),
          sidelineItems.map((item) => ({ _key: "s-" + item.id, sub: "sideline", kind: "design", badge: "Sideline", title: item.title, subtitle: `${vaultGenderLine(item)} · ${item.colorways.length} color ways · ${vaultPriceLine(item)}`, photo: hcmDefaultCw(item) && hcmDefaultCw(item).front,
            committed: committedIds.has(item.id), onAdd: () => addVaultItem(item), onEdit: () => beginEditCommitted(item.id), onRemove: () => {} })),
        ) },
        { key: "templates", label: "Catalog Templates", subs: [], emptyHint: "No catalog templates yet \u2014 select items on a store's Products tab and choose Create Catalog Template. Templates carry their origin store's artwork; for the same gear without the branding, pull from Other stores instead.", items: (window.__catalogTemplates || []).map((t) => ({ _key: "t-" + t.id, sub: null, kind: "cart", badge: "Template", title: t.name, subtitle: t.members.length + " items \u00b7 $" + t.members.reduce((n, m) => n + (m.price || 0), 0).toFixed(2), items: t.members,
            committed: staged.some((c) => c.tplId === t.id), onAdd: () => addTemplate(t), onEdit: () => beginEditCommitted(t.id), onRemove: () => {},
            menu: [{ label: "Rename template", rename: true }, { label: "Delete template", danger: true, act: () => deleteTpl(t) }], onRenameCommit: (name) => renameTpl(t, name) })) },
        { key: "other", label: "Other stores", subs: otherStores.map((s) => ({ k: s.id, l: s.storeTitle || s.teamName || "Store" })), items: [].concat.apply([], otherStores.map((s) => ((s.products || []).filter((p) => !p.isBundle)).map((p) => ({
          _key: "o-" + s.id + "-" + p.id, sub: s.id, kind: "design", badge: s.storeTitle || s.teamName || "Store", title: p.title, subtitle: p.sku || p.subtitle || "Style block", photo: p.photo,
          committed: committedIds.has(p.id), onAdd: () => addFromOtherStore(p, s.storeTitle || s.teamName || "that store"), onEdit: () => beginEditCommitted(p.id), onRemove: () => {} })))) },
      ]} />

      {vaultOffer && <VaultOfferModal item={vaultOffer} onClose={() => setVaultOffer(null)} onConfirm={(sizes, cws) => confirmVaultOffer(vaultOffer, sizes, cws)} />}

      {/* Per-item application-size confirmation */}
      {cfgItems && (
        <ConfirmSizesModal
          open={!!cfgItems}
          product={cfgItems[cfgActive]}
          store={store}
          rows={cfgState[cfgActive].applicationSizes}
          onChange={updateActiveRows}
          productConfig={cfgState[cfgActive]}
          onConfigChange={updateActiveConfig}
          items={cfgItems}
          activeIndex={cfgActive}
          onActiveIndexChange={setCfgActive}
          confirmLabel={cfgEdit ? "Save Changes" : cfgItems.length > 1 ? `Add ${cfgItems.length} Products to Store` : "Add to Store"}
          allowRosterAssign={true}
          onClose={() => { setCfgItems(null); setCfgEdit(false); }}
          onConfirm={confirmConfig} />
      )}

      {/* Customize New Product — XL 4-step workflow modal */}
      <Modal open={customizerOpen} onClose={() => setCustomizerOpen(false)} width="min(1280px, 92vw)">
        <div style={{ display: "flex", flexDirection: "column", height: "88vh", borderRadius: 8, overflow: "hidden" }}>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "16px 28px", borderBottom: "1px solid #ececf0", flexShrink: 0, background: "#fff" }}>
            <h3 style={{ fontSize: 18, fontWeight: 800, margin: 0, letterSpacing: ".01em", color: "#0e0e10" }}>Customize New Product</h3>
            <button onClick={() => setCustomizerOpen(false)} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
              <IconX size={20} />
            </button>
          </div>
          <div style={{ flex: 1, overflow: "hidden", minHeight: 0, background: "#fff" }}>
            {custWizStep === 0 && (
              <CustSportPickerStep
                storeSport={(store.sports && store.sports[0]) || "Basketball"}
                onPick={(item) => { setCustSport(item); setCustWizStep(1); }} />
            )}
            {custWizStep === 1 && (
              <CustProductPickerStep sport={custSport} onBack={() => setCustWizStep(0)}
                onPick={(product) => { setCustProduct(product); setCustWizStep(2); }} />
            )}
            {custWizStep === 2 && (
              <CustBuilderStep sport={custSport} product={custProduct}
                onBack={() => setCustWizStep(1)} onAddToStore={() => setCustWizStep(3)} />
            )}
            {custWizStep === 3 && (
              <CustReviewStep sport={custSport} product={custProduct}
                onBack={() => setCustWizStep(2)} onCancel={() => setCustomizerOpen(false)}
                onAddToStore={(finalProduct) => {
                  stageItems([finalProduct]);
                  setCustomizerOpen(false);
                  setToast("Added 1 custom product");
                }} />
            )}
          </div>
        </div>
      </Modal>

      {/* Sticky footer control ribbon */}
      <FloatingActionBar
        leftChildren={
          <button type="button" onClick={onCancel} style={{
            background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
            padding: "12px 28px", fontSize: 13, fontWeight: 700, color: "#0e0e10", cursor: "pointer",
            transition: "border-color .14s, background .14s",
          }}
            onMouseEnter={(e) => { e.currentTarget.style.borderColor = "#0e0e10"; e.currentTarget.style.background = "#f7f7f8"; }}
            onMouseLeave={(e) => { e.currentTarget.style.borderColor = "#d4d4d8"; e.currentTarget.style.background = "#fff"; }}>
            Cancel
          </button>
        }>
        <button type="button" disabled={staged.length === 0} onClick={() => onCommit(staged)} style={{
          background: staged.length === 0 ? "#e4e4e7" : "#0e0e10",
          color: staged.length === 0 ? "#a1a1aa" : "#fff",
          border: 0, borderRadius: 6, padding: "12px 32px",
          fontSize: 13, fontWeight: 700,
          cursor: staged.length === 0 ? "not-allowed" : "pointer",
          transition: "background .14s, color .14s",
        }}>
          Confirm Selection ({staged.length})
        </button>
      </FloatingActionBar>

      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
}

// Customizers the signed-in user is entitled to pull product from. Mirrors the
// "Choose a brand" step of the sign-in flow. Only entitled brands appear.
// MVP showcase: ProLook is the only connected Customizer. Add ids back here
// ("alli", "riddell", "game-one", "varsity", "squadlocker", "bsn-sports", "augusta")
// to demo the multi-brand experience — all multi-brand UI stays functional.
const ACCESSIBLE_CUSTOMIZERS = ["prolook"];

// Reused "Choose a brand" picker — shown before the Add Products flow so the
// rep selects which Customizer to pull product from.
function ChooseBrandModal({ open, onClose, onSelect, ids = ACCESSIBLE_CUSTOMIZERS }) {
  const items = ids.map((id) => ({ id, ...CUSTOMIZER_BRANDS[id] })).filter((b) => b.name);
  return (
    <Modal open={open} onClose={onClose} width={560}>
      <div style={{ padding: "40px 40px 44px", position: "relative" }}>
        <button onClick={onClose} title="Close" style={{
          position: "absolute", top: 20, right: 20, border: 0, background: "transparent", cursor: "pointer",
          color: "#9a9aa1", width: 32, height: 32, borderRadius: 8, display: "inline-flex", alignItems: "center", justifyContent: "center",
        }}>
          <IconX size={20} />
        </button>
        <h3 style={{ margin: "0 0 8px", fontSize: 25, fontWeight: 800, letterSpacing: "-.3px", color: "#09090b", textAlign: "center" }}>Choose a brand</h3>
        <p style={{ margin: "0 0 32px", fontSize: 15, color: "#71717a", textAlign: "center" }}>We use your Customizer sign-in.</p>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 16 }}>
          {items.map((c) => (
            <button key={c.id} onClick={() => onSelect(c)} title={c.name} style={{
              aspectRatio: "1 / 1", display: "flex", alignItems: "center", justifyContent: "center",
              padding: 12, background: "#f4f4f5", border: "1.5px solid #f4f4f5", borderRadius: 14, cursor: "pointer",
              transition: "border-color .12s, background .12s",
            }}
              onMouseEnter={(e) => { e.currentTarget.style.borderColor = "#dc2626"; e.currentTarget.style.background = "#fff"; }}
              onMouseLeave={(e) => { e.currentTarget.style.borderColor = "#f4f4f5"; e.currentTarget.style.background = "#f4f4f5"; }}>
              {c.logoBlack || c.logoInk ? (
                <img src={c.logoBlack || c.logoInk} alt={c.name} style={{ maxHeight: 36, maxWidth: "78%", width: "auto", objectFit: "contain" }} />
              ) : (
                <span style={{ fontSize: 17, fontWeight: 800, letterSpacing: "-.2px", color: "#18181b", lineHeight: 1.1, textAlign: "center" }}>{c.wordmark || c.name}</span>
              )}
            </button>
          ))}
        </div>
      </div>
    </Modal>
  );
}

function ProductsTab({ store, onCustomize, onProductsChange, simulateOrders, liveStores }) {
  const [showAdd, setShowAdd] = React.useState(false);
  const [addMode, setAddMode] = React.useState(false);
  const [chooseCustomizer, setChooseCustomizer] = React.useState(false);
  const [sourceCustomizer, setSourceCustomizer] = React.useState(null);
  const [toast, setToast] = React.useState(null);
  const [bundleMode, setBundleMode] = React.useState(false);
  const [showSizesPrompt, setShowSizesPrompt] = React.useState(false);
  const pending = store.pendingProducts || [];
  const products = store.products || [];
  // Tell the onboarding tour when products are being configured (drives the
  // m1-choose → m1-sizes step deterministically, no DOM polling).
  React.useEffect(() => {
    if (pending.length > 0) window.dispatchEvent(new CustomEvent("tour:configuring"));
  }, [pending.length]);
  // Catalog items available to bundle (un-bundled, non-bundle items only).
  const bundledIds = new Set(products.flatMap((p) => p.isBundle ? p.bundleItemIds || [] : []));
  const draftPool = products.filter((p) => !p.isBundle && !bundledIds.has(p.id));

  // ── Products Tab Evolution (spec 4231856129): view routing + canonical-record intake ──
  const multiSource = isMultiSourceStore(store);
  const [pteView, setPteView] = React.useState("list");
  const [detailId, setDetailId] = React.useState(null);      // product detail sub-page
  const [batchItems, setBatchItems] = React.useState(null);
  const [imagesId, setImagesId] = React.useState(null);
  const [editorId, setEditorId] = React.useState(null);
  // Register this store as the Stock Vault's deploy target while its
  // Products tab is open (re-assigned each render so closures stay fresh).
  React.useEffect(() => {
    window.__vaultDeployTarget = {
      label: store.storeTitle || store.teamName,
      entitled: store.entitledBrands || ["Marucci", "Victus", "Prolook"],
      deploy: (item) => {
        onProductsChange({ pendingProducts: pending, products: [...products, {
          id: "dep-" + item.id + "-" + Date.now(), title: item.title, brand: item.brand, source: item.source,
          category: item.category || "Fleece", stock: "in", deco: "approved", decoratable: true, photo: item.photo,
          status: "live", srcKind: "vault", teamSetting: "fan-gear", subtitle: "From Vault · confirm pricing, setting & sizes",
        }] });
        setPteView("list");
        setToast("Deployed from Vault — pricing, team setting & sizes need confirmation");
      },
    };
    return () => { delete window.__vaultDeployTarget; };
  });
  // Every intake channel passes items through the existing per-item confirm
  // (application sizes, team setting, pricing) before they land as drafts.
  const pteStagePending = (items) => {
    onProductsChange({ products, pendingProducts: [...pending, ...items.map((c) => ({
      __pendingId: "pte-" + c.id + "-" + Date.now() + Math.random().toString(36).slice(2, 5),
      title: c.title, photo: c.photo, thumbnail: c.photo ? <CatalogPhoto photo={c.photo} alt={c.title} /> : undefined,
      brand: c.brand, source: c.source, category: c.category, stock: c.stock, deco: c.deco, decoratable: c.decoratable,
      subtitle: c.subtitle, sku: c.sku,
    }))] });
    setPteView("list");
  };

  // Per-pending-item config state (sizesConfirmed, modal open, pricing, settings, sizes).
  const [configs, setConfigs] = React.useState({});

  // Initialize a config slot for any new pending items
  React.useEffect(() => {
    setConfigs((prev) => {
      const next = { ...prev };
      let changed = false;
      pending.forEach((p) => {
        const key = p.__pendingId;
        if (key && !next[key]) {
          next[key] = {
            teamSetting: p.teamSetting || "fan-gear",
            optRequired: p.teamSetting === "team-required",
            optSupplied: p.teamSetting === "team-supplied",
            pricing: p.pricing || defaultPricing(),
            applicationSizes: defaultApplicationSizes(),
            // Category pre-fills from source metadata when it matches a workspace category.
            storeCategory: (window.__prodCats || []).some((cc) => cc.name === p.category) ? p.category : "",
            storeSubcategory: "",
            rosters: p.rosters || [],
            // Pro Stock / Sideline Stock items have no applications — no sizing confirm needed.
            sizesConfirmed: !pteRestricted(p),
            showSizesModal: false
          };
          changed = true;
        }
      });
      // Garbage-collect configs for items no longer pending
      const pendingIds = new Set(pending.map((p) => p.__pendingId));
      Object.keys(next).forEach((k) => {
        if (!pendingIds.has(k)) {delete next[k];changed = true;}
      });
      return changed ? next : prev;
    });
  }, [pending]);

  const allConfirmed = pending.length > 0 && pending.every((p) => configs[p.__pendingId]?.sizesConfirmed);

  const handleChoice = (choice) => {
    setShowAdd(false);
    if (choice === "Customize New Product") {
      onCustomize && onCustomize();
      return;
    }
    if (choice === "Add From Cart") {
      window.__openMyCarts && window.__openMyCarts();
      return;
    }
    if (choice === "Add Saved Design") {
      window.__openSavedDesigns && window.__openSavedDesigns();
      return;
    }
    setToast(`Opening ${choice}…`);
  };

  const commitAllPending = (forceAsShown = false) => {
    // 2.4 / 3.1 — a category (or a team setting) is required per item before commit.
    const missing = pending.filter((p) => { const c = configs[p.__pendingId] || {}; return !(c.optRequired || c.optSupplied) && !c.storeCategory; });
    if (missing.length) {
      const ids = new Set(missing.map((p) => p.__pendingId));
      setConfigs((cs) => { const n = { ...cs }; ids.forEach((k) => { if (n[k]) n[k] = { ...n[k], catError: true }; }); return n; });
      setToast("Select a category for every item — items no longer default to Fan Gear");
      return;
    }
    // League stores: TR/TS items must be assigned to ≥1 team or division before commit.
    if (store?.storeType === "league") {
      const unassigned = pending.filter((p) => { const c = configs[p.__pendingId] || {}; return (c.optRequired || c.optSupplied) && !(c.rosters || []).length; });
      if (unassigned.length) {
        const ids = new Set(unassigned.map((p) => p.__pendingId));
        setConfigs((cs) => { const n = { ...cs }; ids.forEach((k) => { if (n[k]) n[k] = { ...n[k], teamError: true }; }); return n; });
        setToast("Team Required / Team Supplied items must be assigned to at least one team or division");
        return;
      }
    }
    const asShownRows = (rows) => (rows || []).map((r) => ({ ...r, adultSize: "AS SHOWN", youthSize: "AS SHOWN" }));
    const newDrafts = pending.map((p) => {
      const c = configs[p.__pendingId] || {};
      // Derive the legacy single-value teamSetting from the two toggles for the
      // product card badge. Required wins if both are on.
      const ts = c.optRequired ? "team-required" : c.optSupplied ? "team-supplied" : "fan-gear";
      return {
        ...p,
        teamSetting: ts,
        optRequired: !!c.optRequired,
        optSupplied: !!c.optSupplied,
        pricing: c.pricing,
        applicationSizes: forceAsShown ? asShownRows(c.applicationSizes) : c.applicationSizes,
        storeCategory: (c.optRequired || c.optSupplied) ? "Team Gear" : (c.storeCategory || ""),
        storeSubcategory: c.storeSubcategory || "",
        rosters: c.rosters || [],
        id: "prod-" + p.__pendingId,
        status: "live",
        ready: true
      };
    });
    onProductsChange({
      pendingProducts: [],
      products: [...products, ...newDrafts]
    });
    setConfigs({});
    // Sizes are confirmed at this point, so the product is Ready — let the tour advance.
    window.dispatchEvent(new CustomEvent("tour:product-ready"));
    setToast(newDrafts.length > 1 ? `Added ${newDrafts.length} items to the store` : `Added "${newDrafts[0].title}" to the store`);
  };

  // Open the Add Products flow, seeded with the chosen source customizer.
  const beginAddFromCustomizer = (c) => {
    setSourceCustomizer(c);
    setChooseCustomizer(false);
    setAddMode(true);
    setToast(`Pulling products from ${c.name}`);
  };

  // Commit items staged on the Add Products page into the store as ready drafts.
  const commitStaged = (staged) => {
    if (!staged || staged.length === 0) { setAddMode(false); return; }
    const newDrafts = staged.map((s) => {
      const ts = s.teamSetting || "fan-gear";
      return {
        ...s,
        id: s.id && String(s.id).startsWith("prod-") ? s.id : "prod-" + (s.id || Date.now()) + "-" + Math.random().toString(36).slice(2, 6),
        title: s.title,
        thumbnail: s.photo ? <CatalogPhoto photo={s.photo} alt={s.title} /> : undefined,
        status: "live",
        ready: true,
        teamSetting: ts,
        optRequired: ts === "team-required",
        optSupplied: ts === "team-supplied",
        pricing: s.pricing || defaultPricing(),
        applicationSizes: s.applicationSizes,
        isBundle: false,
      };
    });
    onProductsChange({ pendingProducts: pending, products: [...products, ...newDrafts] });
    setAddMode(false);
    window.dispatchEvent(new CustomEvent("tour:product-ready"));
    setToast(newDrafts.length > 1 ? `Added ${newDrafts.length} items to the store` : `Added "${newDrafts[0].title}" to the store`);
  };

  // ── Products Tab Evolution views ──
  if (pteView === "batch" && batchItems) {
    return (
      <React.Fragment>
        <BatchDecorateView
          store={store}
          items={batchItems}
          onCancel={() => { setBatchItems(null); setPteView("list"); }}
          onSave={(included) => {
            const fromStoreIds = new Set(included.filter((b) => b.fromStore).map((b) => b.id));
            const updated = products.map((p) => fromStoreIds.has(p.id)
              ? { ...p, deco: "render", source: "Decorated stock", subtitle: ((p.subtitle || p.sku || "").replace(/ · blank.*$/, "") + " · decorated — render queued") }
              : p);
            const added = included.filter((b) => !b.fromStore).map((c) => ({
              id: "bd-" + c.id + "-" + Date.now(), title: c.title + " — " + ((store.teamName || "Team").split(" ")[0]),
              brand: c.brand || "Marucci", source: "Decorated stock", category: c.category, stock: "in", deco: "render",
              decoratable: true, photo: c.photo, status: "live", teamSetting: "fan-gear",
              subtitle: "Batch decorated · render queued · art: Draft",
            }));
            onProductsChange({ pendingProducts: pending, products: [...updated, ...added] });
            setBatchItems(null);
            setPteView("list");
            setToast(included.length + " decorated items created — renders queued, art enters approval as Draft");
          }} />
        <Toast message={toast} onDone={() => setToast(null)} />
      </React.Fragment>
    );
  }
  // Artwork Editor overlay (replaces the old image editor) — placement, logo
  // color and visibility are tuned per color way in the artwork builder.
  const editorProduct = editorId ? products.find((x) => x.id === editorId) : null;
  const editorOverlay = editorProduct ? (
    <ArtworkBuilderPage
      item={editorProduct}
      store={store}
      name={editorProduct.title}
      styleNo={editorProduct.sku || ""}
      cwList={(editorProduct.colorways && editorProduct.colorways.length) ? editorProduct.colorways : [{ name: editorProduct.colorway || "Default", slug: "default", hex: "#1b1b1e", front: editorProduct.photo, back: editorProduct.photo }]}
      backLabel="Back to Products"
      onClose={() => setEditorId(null)}
      onSaveChanges={() => {
        const pid = editorProduct.id;
        setEditorId(null); setDetailId(null); setPteView("list");
        setToast('Changes saved to “' + editorProduct.title + '”');
        setTimeout(() => {
          const el = document.querySelector('[data-product-card="' + pid + '"]');
          if (!el) return;
          let sc = el.parentElement;
          while (sc && sc.scrollHeight <= sc.clientHeight + 4) sc = sc.parentElement;
          const r = el.getBoundingClientRect();
          if (sc && sc !== document.body && sc !== document.documentElement) {
            const cr = sc.getBoundingClientRect();
            sc.scrollTo({ top: sc.scrollTop + (r.top - cr.top) - 110, behavior: "smooth" });
          } else {
            window.scrollTo({ top: window.scrollY + r.top - 110, behavior: "smooth" });
          }
          el.style.outline = "2px solid #0e0e10";
          el.style.outlineOffset = "3px";
          el.style.borderRadius = "8px";
          setTimeout(() => { el.style.outline = ""; el.style.outlineOffset = ""; }, 2200);
        }, 160);
      }} />
  ) : null;

  // Products sub-page shell — Stock Vault no longer lives here (Epic G: it's
  // platform-level only, and is embedded as a source section in Add Products).
  const prodShell = (navValue, body) => (
    <div style={{ paddingBottom: 60 }}>
      {body}
      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
  const detailProduct = detailId ? products.find((x) => x.id === detailId) : null;
  if (pteView === "detail" && detailProduct) {
    return (
      <React.Fragment>
        {editorOverlay}
        {prodShell("list",
          <ProductDetailPage mode="edit" product={detailProduct} store={store}
            onBack={() => { setDetailId(null); setPteView("list"); }}
            onSave={(u) => onProductsChange({ pendingProducts: pending, products: products.map((x) => x.id === u.id ? u : x) })}
            onRemove={(id) => { onProductsChange({ pendingProducts: pending, products: products.filter((x) => x.id !== id) }); setDetailId(null); setPteView("list"); }}
            onOpenEditor={() => setEditorId(detailProduct.id)}
            onSendForApproval={() => setToast("Sent for approval")}
            onToast={setToast} />
        )}
      </React.Fragment>
    );
  }

  // 0) Add Products page — centralized repository (replaces the fork modal).
  if (addMode) {
    return (
      <React.Fragment>
        <AddProductsPage store={store} liveStores={liveStores} customizer={sourceCustomizer} onCancel={() => setAddMode(false)} onChangeCustomizer={() => { setAddMode(false); setChooseCustomizer(true); }} onCommit={commitStaged} />
        <Toast message={toast} onDone={() => setToast(null)} />
      </React.Fragment>
    );
  }

  // 1) If there are pending products to configure, show the configure cards
  if (pending.length > 0) {
    return (
      <div style={{ paddingBottom: 100 }}>
        <h2 style={{ fontSize: 22, fontWeight: 700, margin: "4px 0 16px", color: "#0e0e10" }}>
          {pending.length > 1 ? "Add Products" : "Add Product"}
        </h2>
        <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
          {pending.map((p, i) => {
            const cfg = configs[p.__pendingId];
            if (!cfg) return null;
            return (
              <ConfigureProduct
                key={p.__pendingId}
                product={p}
                store={store}
                index={i}
                total={pending.length}
                config={cfg}
                onConfigChange={(next) => setConfigs((cs) => ({ ...cs, [p.__pendingId]: next }))}
                onProductChange={(updated) => {
                  onProductsChange({
                    pendingProducts: pending.map((pp) => pp.__pendingId === p.__pendingId ? updated : pp),
                    products
                  });
                }}
                onDelete={() => {
                  setConfigs((cs) => {const n = { ...cs };delete n[p.__pendingId];return n;});
                  onProductsChange({
                    pendingProducts: pending.filter((pp) => pp.__pendingId !== p.__pendingId),
                    products
                  });
                }} />);


          })}
        </div>

        {/* Single shared footer */}
        <FloatingActionBar>
          <button
            data-tour={allConfirmed ? "add-to-store" : undefined}
            onClick={() => allConfirmed ? commitAllPending() : setShowSizesPrompt(true)}
            style={{
              background: "#0e0e10",
              color: "#fff",
              border: 0, borderRadius: 6, padding: "12px 28px",
              fontSize: 14, fontWeight: 600,
              cursor: "pointer",
              transition: "background .15s, color .15s"
            }}>Add to Team Store</button>
        </FloatingActionBar>
        <SelectApplicationSizeModal
          open={showSizesPrompt}
          onClose={() => setShowSizesPrompt(false)}
          onSpecify={() => setShowSizesPrompt(false)}
          onAsShown={() => {setShowSizesPrompt(false);commitAllPending(true);}} />
        <Toast message={toast} onDone={() => setToast(null)} />
      </div>);

  }

  // 2a) Bundle creation mode — inline panel that replaces the products list.
  if (bundleMode && products.length > 0) {
    return (
      <div>
        <CreateBundleSection
          store={store}
          draftProducts={draftPool}
          onCancel={() => setBundleMode(false)}
          onCreate={({ name, teamSetting, productIds, bundleType, category, bundleProfit, hidden, hideRetail, freeShipping, hideMembers }) => {
            const items = productIds.
            map((id) => products.find((p) => p.id === id)).
            filter(Boolean);
            const newBundle = {
              id: "bundle-" + Date.now(),
              title: name,
              isBundle: true,
              bundleType: bundleType || "all",
              storeCategory: teamSetting ? "Team Gear" : (category || ""),
              status: "live",
              teamSetting: teamSetting || "fan-gear",
              bundleItemIds: productIds,
              bundleProfit, hidden: !!hidden, hideRetail: !!hideRetail, freeShipping: !!freeShipping,
              // Snapshot the bundle-derived customer price — the card’s "from $X".
              pricing: bundleCardPricing(
                items, null, bundleProfit, bundleType || "all",
                teamSetting === "team-supplied", store?.fundraisingEnabled === true
              )
            };
            const memberSet = new Set(productIds);
            onProductsChange({
              pendingProducts: pending,
              // Epic J member-visibility prompt: hiding turns each member's Visibility off.
              products: [newBundle, ...products.map((p) => hideMembers && memberSet.has(p.id) ? { ...p, hidden: true } : p)]
            });
            setBundleMode(false);
            setToast(`Created bundle "${name}"`);
          }} />
        <Toast message={toast} onDone={() => setToast(null)} />
      </div>);

  }

  // 2) Drafts / Published / Vault list — rendered even with an empty catalog
  // (the Vault tab must be reachable before any products exist; empty Drafts /
  // Published tabs carry the Add Products call-to-action instead).
  {
    return (
      <div style={{ paddingBottom: 60 }}>
        {editorOverlay}
        <ProductsCatalogSections
          products={products}
          store={store}
          multiSource={multiSource}
          onOpenEditor={(id) => setEditorId(id)}
          onOpenDetail={(id) => { setDetailId(id); setPteView("detail"); }}
          onDecorateBatch={(items) => { setBatchItems(items); setPteView("batch"); }}
          onPatchStore={(patch) => onProductsChange({ pendingProducts: pending, products, ...patch })}
          onToast={setToast}
          onSendForApproval={(id) => {
            setToast("Sent for approval");
          }}
          onRemove={(id) => {
            onProductsChange({
              pendingProducts: pending,
              // Epic C/J — removal also leaves every bundle holding the item; a
              // bundle left with one member becomes Incomplete (held hidden).
              products: products.filter((p) => p.id !== id).map((b) => {
                if (!b.isBundle || !(b.bundleItemIds || []).includes(id)) return b;
                const ids2 = b.bundleItemIds.filter((x) => x !== id);
                return { ...b, bundleItemIds: ids2, hidden: ids2.length < 2 ? true : b.hidden };
              })
            });
          }}
          onAddProducts={() => { setSourceCustomizer({ id: "prolook", ...CUSTOMIZER_BRANDS.prolook }); setAddMode(true); }}
          onCreateBundle={() => setBundleMode(true)}
          onUpdateProduct={(updated) => {
            onProductsChange({
              pendingProducts: pending,
              products: products.map((p) => p.id === updated.id ? updated : p)
            });
            setToast("Product updated");
          }}
          onBulkPrice={(ids, profit) => {
            const idSet = new Set(ids);
            // The modal's mode overwrites each target's mode along with its values (Epic K).
            // Fields the modal wrote carry a bulk-pricing provenance note until re-typed.
            const applyTier = (tier, fundActive) => {
              const t = tier || {};
              const u = profit.mode === "%" ? "%" : "$";
              const next = { ...t, dealerUnit: u, fundUnit: u };
              if (profit.dealer !== "") { next.dealer = String(profit.dealer); next.dealerBulk = true; } else { next.dealer = "0"; delete next.dealerBulk; }
              if (profit.fund != null && profit.fund !== "") { next.fund = String(profit.fund); next.fundBulk = true; }
              next.retail = psRowRetail(next, fundActive);
              return next;
            };
            onProductsChange({
              pendingProducts: pending,
              products: products.map((p) => {
                if (!idSet.has(p.id)) return p;
                const pricing = p.pricing || defaultPricing();
                const fa = pricing.fundraisingOn ?? (store?.fundraisingEnabled === true);
                return { ...p, pricing: { ...pricing, youth: applyTier(pricing.youth, fa), adult: applyTier(pricing.adult, fa) } };
              })
            });
            setToast(ids.length > 1 ? `Pricing updated for ${ids.length} products` : "Pricing updated");
          }} />
        <Toast message={toast} onDone={() => setToast(null)} />
        <ChooseBrandModal open={chooseCustomizer} onClose={() => setChooseCustomizer(false)} onSelect={beginAddFromCustomizer} />
      </div>);

  }

  // 3) Empty state — single CTA into the centralized Add Products page (no fork).
  return (
    <div style={{ paddingBottom: 60 }}>
      <div style={{
        display: "flex", flexDirection: "column", alignItems: "center",
        padding: "152px 20px 24px", textAlign: "center"
      }}>
        <div style={{
          width: 56, height: 56, borderRadius: "50%", background: "#f1f1f3", color: "#0e0e10",
          display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16
        }}>
          <IconShirt size={26} />
        </div>
        <h2 style={{ fontSize: 22, fontWeight: 800, margin: 0, marginBottom: 8, color: "#0e0e10" }}>No products yet</h2>
        <div style={{ color: "#6b6b72", fontSize: 14, maxWidth: 420, lineHeight: 1.55, marginBottom: 24 }}>
          Pull products from your saved designs and carts, or customize a new one, to start building this store.
        </div>
        <button type="button" onClick={() => { setSourceCustomizer({ id: "prolook", ...CUSTOMIZER_BRANDS.prolook }); setAddMode(true); }} style={{
          background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
          padding: "12px 28px", fontSize: 14, fontWeight: 700, cursor: "pointer",
          display: "inline-flex", alignItems: "center", gap: 8,
        }}>
          <IconPlus size={16} style={{ strokeWidth: 2.6 }} /> Add Products
        </button>
      </div>

      <Toast message={toast} onDone={() => setToast(null)} />
      <ChooseBrandModal open={chooseCustomizer} onClose={() => setChooseCustomizer(false)} onSelect={beginAddFromCustomizer} />
    </div>);

}

// Three big choice cards — reusable inline and inside the modal.
function AddProductChoices({ onChoice }) {
  const choices = [
  {
    id: "customize",
    label: "Customize New Product",
    icon:
    <svg width="44" height="44" viewBox="0 0 40 40" fill="none">
          <circle cx="14" cy="13" r="6" fill="#3acc7a" />
          <circle cx="26" cy="13" r="6" fill="#e1251b" />
          <circle cx="20" cy="23" r="6" fill="#2563eb" />
          <rect x="22" y="22" width="14" height="3" rx="1.5" transform="rotate(35 22 22)" fill="#0e0e10" />
        </svg>

  },
  { id: "cart", label: "Add From Cart", icon: <IconCart size={40} /> },
  {
    id: "saved",
    label: "Add Saved Design",
    icon:
    <svg width="44" height="44" viewBox="0 0 40 40" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" style={{ color: "#0e0e10" }}>
          <path d="M12 10l4-3h8l4 3 3 3-3 4h-2v13a1 1 0 0 1-1 1H15a1 1 0 0 1-1-1V17h-2l-3-4 3-3z" />
        </svg>

  }];

  return (
    <div data-tour="add-product" style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 24 }}>
      {choices.map((c) =>
      <button key={c.id} onClick={() => onChoice(c.label)} style={{
        background: "#fff", border: "1px solid #ececf0", borderRadius: 8,
        padding: "56px 20px 48px", cursor: "pointer",
        display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 28,
        transition: "all .15s ease", minHeight: 240
      }}
      onMouseEnter={(e) => {
        e.currentTarget.style.borderColor = "#0e0e10";
        e.currentTarget.style.background = "#fafafb";
        e.currentTarget.style.transform = "translateY(-2px)";
        e.currentTarget.style.boxShadow = "0 6px 16px rgba(20,20,30,.08)";
      }}
      onMouseLeave={(e) => {
        e.currentTarget.style.borderColor = "#ececf0";
        e.currentTarget.style.background = "#fff";
        e.currentTarget.style.transform = "translateY(0)";
        e.currentTarget.style.boxShadow = "none";
      }}>
          <div style={{ height: 48, display: "inline-flex", alignItems: "center" }}>{c.icon}</div>
          <div style={{ fontSize: 16, fontWeight: 700, color: "#0e0e10" }}>{c.label}</div>
        </button>
      )}
    </div>);

}

// Add Product to Team Store modal — three big choice cards.
function AddProductModal({ open, onClose, onChoice }) {
  const choices = [
  {
    id: "customize",
    label: "Customize New Product",
    icon:
    <svg width="44" height="44" viewBox="0 0 40 40" fill="none">
          <circle cx="14" cy="13" r="6" fill="#3acc7a" />
          <circle cx="26" cy="13" r="6" fill="#e1251b" />
          <circle cx="20" cy="23" r="6" fill="#2563eb" />
          <rect x="22" y="22" width="14" height="3" rx="1.5" transform="rotate(35 22 22)" fill="#0e0e10" />
        </svg>

  },
  { id: "cart", label: "Add From Cart", icon: <IconCart size={40} /> },
  {
    id: "saved",
    label: "Add Saved Design",
    icon:
    <svg width="44" height="44" viewBox="0 0 40 40" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" style={{ color: "#0e0e10" }}>
          <path d="M12 10l4-3h8l4 3 3 3-3 4h-2v13a1 1 0 0 1-1 1H15a1 1 0 0 1-1-1V17h-2l-3-4 3-3z" />
        </svg>

  }];


  return (
    <Modal open={open} onClose={onClose} width={1180}>
      <div style={{ padding: "24px 32px 32px" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 4 }}>
          <h3 style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".10em", margin: 0, color: "#6b6b72" }}>ADD PRODUCT TO TEAM STORE</h3>
          <button onClick={onClose} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
            <IconX size={20} />
          </button>
        </div>
        <div style={{ borderTop: "1px solid #ececf0", marginTop: 16, paddingTop: 40 }}>
          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 24 }}>
            {choices.map((c) =>
            <button key={c.id} onClick={() => onChoice(c.label)} style={{
              background: "#fff", border: "1px solid #ececf0", borderRadius: 10,
              padding: "72px 20px 60px", cursor: "pointer",
              display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 28,
              transition: "all .15s ease", minHeight: 280
            }}
            onMouseEnter={(e) => {
              e.currentTarget.style.borderColor = "#0e0e10";
              e.currentTarget.style.background = "#fafafb";
              e.currentTarget.style.transform = "translateY(-2px)";
              e.currentTarget.style.boxShadow = "0 6px 16px rgba(20,20,30,.08)";
            }}
            onMouseLeave={(e) => {
              e.currentTarget.style.borderColor = "#ececf0";
              e.currentTarget.style.background = "#fff";
              e.currentTarget.style.transform = "translateY(0)";
              e.currentTarget.style.boxShadow = "none";
            }}>
                <div style={{ height: 48, display: "inline-flex", alignItems: "center" }}>{c.icon}</div>
                <div style={{ fontSize: 18, fontWeight: 700, color: "#0e0e10" }}>{c.label}</div>
              </button>
            )}
          </div>
        </div>
      </div>
    </Modal>);

}

// ─────────── My Team(s) tab (flattened division → team structure) ───────────
// Divisions (parents, formerly roster groups) carry branding; Teams (children,
// formerly rosters) each hold a roster + bank zone. Teams may sit unassigned.
// Rosters are optional — they front-load (CSV/manual/share link) or back-fill
// from checkout captures. Drilling into a team opens the player table.

const ROSTER_BRAND_COLORS = ["#7c1d2b", "#1e3a6e", "#1f5c3a", "#4a2367", "#b4531a", "#0f6e6e", "#8a3a12", "#2f3a8f"];

function rHexToRgb(h) { h = String(h || "").replace("#", ""); return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)]; }
function rTint(h, a) { const [r, g, b] = rHexToRgb(h); return `rgba(${r},${g},${b},${a})`; }
function rInitials(name) { const w = String(name || "").trim().split(/\s+/).filter((x) => /[a-z0-9]/i.test(x)); if (w.length === 0) return "?"; if (w.length === 1) return w[0].slice(0, 2).toUpperCase(); return (w[0][0] + w[1][0]).toUpperCase(); }

// Jazz Bantum Boys Basketball — a 20-team youth league across 5 cities
// (4 age-division teams per city). Each city carries its own high-school
// branding, mascot, and color palette, but all shop one master storefront.
const ROSTER_GROUP_SEED = [
  { id: "sf", name: "Lone Peak", sport: "Boys Basketball", color: "#7c1d2b", rosters: [
    { id: "sf-8", name: "8U Boys Basketball", division: "8U", players: 11 },
    { id: "sf-10", name: "10U Boys Basketball", division: "10U", players: 11 },
    { id: "sf-12", name: "12U Boys Basketball", division: "12U", players: 11 },
    { id: "sf-14", name: "14U Boys Basketball", division: "14U", players: 11 }] },
  { id: "af", name: "Corner Canyon", sport: "Boys Basketball", color: "#1e3a6e", rosters: [
    { id: "af-8", name: "8U Boys Basketball", division: "8U", players: 10 },
    { id: "af-10", name: "10U Boys Basketball", division: "10U", players: 9 },
    { id: "af-12", name: "12U Boys Basketball", division: "12U", players: 10 },
    { id: "af-14", name: "14U Boys Basketball", division: "14U", players: 10 }] },
  { id: "pg", name: "Farmington", sport: "Boys Basketball", color: "#1f5c3a", rosters: [
    { id: "pg-8", name: "8U Boys Basketball", division: "8U", players: 11 },
    { id: "pg-10", name: "10U Boys Basketball", division: "10U", players: 10 },
    { id: "pg-12", name: "12U Boys Basketball", division: "12U", players: 10 },
    { id: "pg-14", name: "14U Boys Basketball", division: "14U", players: 11 }] },
  { id: "or", name: "Lehi", sport: "Boys Basketball", color: "#4a2367", rosters: [
    { id: "or-8", name: "8U Boys Basketball", division: "8U", players: 11 },
    { id: "or-10", name: "10U Boys Basketball", division: "10U", players: 10 },
    { id: "or-12", name: "12U Boys Basketball", division: "12U", players: 11 },
    { id: "or-14", name: "14U Boys Basketball", division: "14U", players: 11 }] },
  { id: "pv", name: "Skyridge", sport: "Boys Basketball", color: "#b4531a", rosters: [
    { id: "pv-8", name: "8U Boys Basketball", division: "8U", players: 11 },
    { id: "pv-10", name: "10U Boys Basketball", division: "10U", players: 11 },
    { id: "pv-12", name: "12U Boys Basketball", division: "12U", players: 11 },
    { id: "pv-14", name: "14U Boys Basketball", division: "14U", players: 11 }] }];

const STANDALONE_ROSTER_SEED = [
  { id: "loose-coaches", name: "Coaches & Staff", players: 12 },
  { id: "loose-allstars", name: "Tournament All-Stars", players: 15 }];

// ─────────── Roster-optional real-world seeds ───────────
// Wasatch Elite Lacrosse — team mapping complete (3 regions × 6 age divisions),
// zero rosters: every player arrives through checkout captures in the bank.
const TEAMS_ONLY_GROUP_SEED = [
  { id: "wx-n", name: "North Region", sport: "Lacrosse", color: "#1e3a6e" },
  { id: "wx-c", name: "Central Region", sport: "Lacrosse", color: "#7c1d2b" },
  { id: "wx-s", name: "South Region", sport: "Lacrosse", color: "#1f5c3a" },
].map((g) => ({ ...g, rosters: ["8U", "10U", "12U", "14U", "16U", "18U"].map((d) => ({ id: g.id + "-" + d.toLowerCase(), name: d + " Boys Lacrosse", division: d, players: 0 })) }));

// Deterministic bulk capture generator — hundreds of checkout captures for
// stores that run roster-less. Same schema the storefront consent gate writes.
function bulkRng(seed) { let a = seed >>> 0; return () => { a |= 0; a = (a + 0x6D2B79F5) | 0; let t = Math.imul(a ^ (a >>> 15), 1 | a); t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t; return ((t ^ (t >>> 14)) >>> 0) / 4294967296; }; }
const BB_FIRST = ["Mason", "Liam", "Noah", "Ethan", "Owen", "Carter", "Wyatt", "Luke", "Jack", "Levi", "Miles", "Eli", "Ava", "Mia", "Zoe", "Ruby", "Nora", "Isla", "Cole", "Reid", "Beckett", "Finn", "Hudson", "Tate", "Brooks", "Nash", "Ivy", "June", "Sage", "Wren"];
const BB_LAST = ["Rivera", "Pruitt", "Chen", "Okafor", "Sorensen", "Whitaker", "Delgado", "Marsh", "Kowalski", "Bennett", "Vaughn", "Ferris", "Nakamura", "Ortiz", "Hale", "Barlow", "Quintana", "Mercer", "Solberg", "Ashton", "Ibarra", "Foss", "Redding", "Calloway", "Voss", "Tran", "Muir", "Ellison", "Padgett", "Rooks"];
function makeBulkBank(prefix, teamIds, clusterCount, items) {
  const rnd = bulkRng(prefix.length * 7349 + clusterCount);
  const pick = (a) => a[Math.floor(rnd() * a.length)];
  const bank = {};
  teamIds.forEach((t) => { bank[t] = []; });
  for (let i = 0; i < clusterCount; i++) {
    const tid = teamIds[i % teamIds.length];
    const first = pick(BB_FIRST), last = pick(BB_LAST);
    const player = first + " " + last;
    const email = (first + "." + last).toLowerCase() + (rnd() < 0.3 ? ".family" : "") + "@" + pick(["gmail.com", "yahoo.com", "outlook.com", "icloud.com"]);
    const phone = "(" + pick(["385", "801", "435", "619", "760"]) + ") 555-0" + String(100 + Math.floor(rnd() * 900)).slice(0, 3);
    const address = String(40 + Math.floor(rnd() * 9600)) + " " + pick(["Fox Hollow Ln", "Cedar St", "Aspen Way", "Harbor Dr", "Canyon Rd", "Willow Ct", "Seacrest Ave", "Alpine Loop"]) + ", " + pick(["Draper, UT 84020", "Lehi, UT 84043", "Sandy, UT 84094", "Provo, UT 84601", "Huntington Beach, CA 92646", "Carlsbad, CA 92009"]);
    const jersey = String(Math.floor(rnd() * 100));
    const date = "Jul " + (8 + Math.floor(rnd() * 18)) + ", 2026";
    const order = "435" + Array.from({ length: 10 }, () => pick(["A", "B", "C", "D", "E", "F", "G", "H", "J", "K", "M", "N", "P", "R", "S", "T", "U", "V", "W", "X", "Y", "Z", "2", "3", "4", "5", "6", "7", "8", "9"])).join("");
    const caps = [];
    const n = rnd() < 0.72 ? 1 : 2;
    for (let j = 0; j < n; j++) {
      const it = pick(items);
      caps.push({ id: prefix + "-c" + i + "-" + j, player, nameOnJersey: last, jersey, size: pick(["YS", "YM", "YL", "AS", "AM", "AL"]), item: it.item, tier: it.tier, email, phone, address, order, date });
    }
    bank[tid].push({ id: prefix + "-bz" + i, committed: false, captures: caps });
  }
  return bank;
}
const WASATCH_BANK_SEED = makeBulkBank("wx", TEAMS_ONLY_GROUP_SEED.flatMap((g) => g.rosters.map((r) => r.id)), 216,
  [{ item: "Lacrosse Game Jersey — Home", tier: "team-required" }, { item: "Lacrosse Game Shorts", tier: "team-required" }, { item: "Shooter Shirt — Practice", tier: "team-supplied" }]);
// Bayshore Summer Classic — no teams or divisions exist, so nothing can be set
// Team Required / Team Supplied and no team-gear captures occur. Bank starts empty.
const BAYSHORE_BANK_SEED = {};
function bankSeedCounts(seed) { let p = 0, c = 0; Object.values(seed).forEach((a) => a.forEach((cl) => { p++; c += cl.captures.length; })); return { proposed: p, captures: c }; }

function groupPlayerCount(g) { return g.rosters.reduce((n, r) => n + (r.players || 0), 0); }
// Deterministic, human-readable team code per group (e.g. "RT47A") — stable across renders.
function rTeamCode(seed) {
  let h = 5381; const s = String(seed || "");
  for (let i = 0; i < s.length; i++) h = ((h * 33) ^ s.charCodeAt(i)) >>> 0;
  const L = "ABCDEFGHJKMNPQRSTUVWXYZ";
  return L[h % 23] + L[(h >>> 5) % 23] + String(10 + ((h >>> 10) % 90)) + L[(h >>> 17) % 23];
}

// ─────────── Shared roster-org state (Rosters tab ⟷ Settings manager mapping) ───────────
function makeRosterOrg(store, startEmpty) {
  const singleMode = (store && store.storeType) === "single";
  const singleName = (store && (store.teamName || store.storeTitle)) || "Team Roster";
  const profile = store && store.rosterProfile;
  // A league store created through the wizard carries its Divisions & Teams
  // structure — seed the workspace org from it instead of demo data.
  const ws = !singleMode && store && store.leagueStructure;
  if (ws && ((ws.groups || []).length || (ws.loose || []).length)) {
    const groups = (ws.groups || []).map((g) => ({ ...g, rosters: (g.rosters || []).map((r) => ({ ...r })) }));
    const modes = {}; groups.forEach((g) => { modes[g.id] = "league"; });
    return { groups, loose: (ws.loose || []).map((r) => ({ ...r })), managers: {}, modes, bank: {} };
  }
  if (!singleMode && !startEmpty && profile === "teams-only") {
    // Team mapping complete, zero rosters — the bank back-fills every team.
    const groups = TEAMS_ONLY_GROUP_SEED.map((g) => ({ ...g, rosters: g.rosters.map((r) => ({ ...r })) }));
    const modes = {}; groups.forEach((g) => { modes[g.id] = "league"; });
    return { groups, loose: [], managers: {}, modes, bank: JSON.parse(JSON.stringify(WASATCH_BANK_SEED)) };
  }
  if (!singleMode && !startEmpty && profile === "unstructured") {
    // No teams, no rosters — captures wait store-wide under "__store".
    return { groups: [], loose: [], managers: {}, modes: {}, bank: JSON.parse(JSON.stringify(BAYSHORE_BANK_SEED)) };
  }
  const groups = singleMode || startEmpty ? [] : ROSTER_GROUP_SEED.map((g) => ({ ...g, rosters: g.rosters.map((r) => ({ ...r })) }));
  const loose = singleMode ? [{ id: "single-roster", name: singleName, players: startEmpty ? 0 : 18 }]
    : startEmpty ? [] : STANDALONE_ROSTER_SEED.map((r) => ({ ...r }));
  const managers = {}, modes = {};
  if (!singleMode && !startEmpty) {
    groups.forEach((g) => {
      managers[g.id] = { ...(MANAGER_SEED[g.id] || { name: "", email: "", phone: "" }) };
      g.rosters.forEach((r) => { if (TEAM_MANAGER_SEED[r.id]) managers[r.id] = { ...TEAM_MANAGER_SEED[r.id] }; });
      // Per-group fulfillment mode: individual teams when any roster is seeded,
      // group manager when one is assigned, otherwise league manager.
      // Two modes only (flattened structure): league-level or individual teams.
      modes[g.id] = g.rosters.some((r) => TEAM_MANAGER_SEED[r.id]) ? "teams" : "league";
    });
  }
  return { groups, loose, managers, modes, bank: makeBankSeed(singleMode, startEmpty) };
}
function leagueManagerName(store) { return (store && (store.shipName || store.teamName)) || "League Manager"; }
// A manager entry is submit-ready only when it has a full name, valid email,
// 10-digit phone, and a street address (required for order fulfillment).
function managerComplete(m) {
  if (!m) return false;
  const hasName = (m.firstName && m.lastName) || m.name;
  return !!(hasName && isValidEmail(m.email) && isValidPhone(m.phone) && String(m.address || "").trim());
}
// Effective manager for a roster: team manager (in teams mode) → group manager
// (in group mode) → league manager (league mode / standalone fallback).
// In teams mode a roster with no manager is UNASSIGNED — it no longer falls
// back to the league manager; pending-order submission is gated on it.
function effectiveManagerFor(org, store, groupId, rosterId) {
  const managers = (org && org.managers) || {}, modes = (org && org.modes) || {};
  const league = { name: leagueManagerName(store), email: (store && store.email) || "", phone: (store && store.phone) || "", src: "league" };
  if (groupId) {
    const mode = modes[groupId] || "league";
    if (mode === "teams") {
      const t = managers[rosterId];
      if (t && t.name) return { ...t, src: "team" };
      return { name: "", email: "", phone: "", src: "unassigned" };
    }
    return league;
  } else if (rosterId) {
    const t = managers[rosterId];
    if (t && t.name) return { ...t, src: "team" };
  }
  return league;
}
// Freight gate: given a roster-org and a pending shipping container key
// (uppercased group id / standalone roster id), report what blocks submission.
// Returns null when fulfillment routing is fully resolved.
function freightGateFor(org, store, shippingGroup) {
  if (!org || (store && store.storeType) === "single") return null;
  const key = String(shippingGroup || "").toLowerCase();
  const g = (org.groups || []).find((x) => x.id.toLowerCase() === key);
  if (!g) return null; // standalone rosters route to the league manager
  const mode = (org.modes || {})[g.id] || "league";
  const managers = org.managers || {};
  if (mode === "teams") {
    const missing = g.rosters.filter((r) => !managerComplete(managers[r.id]));
    if (missing.length) return { kind: "teams", group: g, missing };
  }
  return null;
}
const MGR_SRC_META = {
  team: { label: "Team manager", color: "#0e0e10", bg: "#eef1f6" },
  teams: { label: "Individual teams", color: "#0e0e10", bg: "#eef1f6" },
  group: { label: "Group manager", color: "#1f7a44", bg: "#e7f5ed" },
  league: { label: "League manager", color: "#6b6b72", bg: "#f1f1f3" },
  unassigned: { label: "Unassigned", color: "#b81d14", bg: "#fdecea" },
};
function MgrSrcBadge({ src }) {
  const s = MGR_SRC_META[src] || MGR_SRC_META.league;
  return <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".04em", textTransform: "uppercase", color: s.color, background: s.bg, borderRadius: 4, padding: "4px 8px", whiteSpace: "nowrap" }}>{s.label}</span>;
}
// ─────────── Shared manager entry fields (First/Last/Email/Phone/Address) ───────────
function managerFieldErrors(m) {
  const v = m || {}; const e = {};
  if (!String(v.firstName || "").trim()) e.firstName = "Required";
  if (!String(v.lastName || "").trim()) e.lastName = "Required";
  if (!isValidEmail(v.email)) e.email = "Enter a valid email";
  if (!isValidPhone(v.phone)) e.phone = "Enter a 10-digit phone number";
  if (!String(v.address || "").trim()) e.address = "Address is required for order fulfillment";
  return e;
}
function ManagerFieldsGrid({ value, onChange, errors = {} }) {
  const v = value || {};
  const set = (k, val) => {
    const next = { ...v, [k]: k === "phone" ? formatPhone(val) : val };
    if (k === "firstName" || k === "lastName") next.name = [next.firstName, next.lastName].filter(Boolean).join(" ").trim();
    onChange(next);
  };
  const input = (k, label, placeholder, opts = {}) => (
    <div style={{ gridColumn: opts.span ? "1 / -1" : "auto" }}>
      <div style={{ fontSize: 11, fontWeight: 700, color: errors[k] ? "#b81d14" : "#6b6b72", marginBottom: 6, letterSpacing: ".02em" }}>{label}</div>
      <input value={v[k] || ""} placeholder={placeholder}
        inputMode={opts.tel ? "tel" : undefined} maxLength={opts.tel ? 14 : undefined}
        onChange={(e) => set(k, e.target.value)}
        style={{ width: "100%", border: `1px solid ${errors[k] ? "#e1251b" : "#d4d4d8"}`, borderRadius: 6, padding: "12px 12px", fontSize: 13.5, color: "#0e0e10", outline: "none", boxSizing: "border-box", fontFamily: opts.tel ? "JetBrains Mono, monospace" : "inherit", background: "#fff" }} />
      {errors[k] && <div style={{ fontSize: 11.5, color: "#b81d14", marginTop: 4 }}>{errors[k]}</div>}
    </div>
  );
  return (
    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12 }}>
      {input("firstName", "First Name", "First name")}
      {input("lastName", "Last Name", "Last name")}
      {input("email", "Email", "coach@example.com")}
      {input("phone", "Phone", "(000) 000-0000", { tel: true })}
      {input("address", "Address", "Street, City, State ZIP", { span: true })}
    </div>
  );
}
// Blocking modal shown when pending orders are submitted for a group whose
// freight routing is unresolved. Collects the group manager, or a team manager
// for every unassigned roster; addresses are verified with the map service.
function AssignManagersModal({ gate, org, store, onClose, onComplete }) {
  const [drafts, setDrafts] = React.useState({});
  const [errors, setErrors] = React.useState({});
  const [validating, setValidating] = React.useState(false);
  React.useEffect(() => {
    if (!gate) return;
    const managers = (org && org.managers) || {}; const d = {};
    if (gate.kind === "group") d[gate.group.id] = { ...(managers[gate.group.id] || {}) };
    else gate.missing.forEach((r) => { d[r.id] = { ...(managers[r.id] || {}) }; });
    setDrafts(d); setErrors({});
  }, [gate]);
  if (!gate) return null;
  const targets = gate.kind === "group"
    ? [{ id: gate.group.id, label: "Group Manager", sub: `Receives every ${gate.group.name} order` }]
    : gate.missing.map((r) => ({ id: r.id, label: r.name, sub: "Team manager" }));
  const submit = async () => {
    const errs = {};
    targets.forEach((t) => { const e = managerFieldErrors(drafts[t.id]); if (Object.keys(e).length) errs[t.id] = e; });
    setErrors(errs);
    if (Object.keys(errs).length) return;
    setValidating(true);
    const next = { ...drafts };
    for (const t of targets) {
      const res = await validateAddressService(next[t.id].address);
      if (!res.ok) {
        setValidating(false);
        setErrors({ [t.id]: { address: "We couldn’t verify this address — check it and try again." } });
        return;
      }
      next[t.id] = { ...next[t.id], address: res.normalized };
    }
    setValidating(false);
    onComplete(next);
  };
  return (
    <Modal open onClose={validating ? undefined : onClose} width={640}>
      <div style={{ padding: "28px 28px 16px" }}>
        <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>
          {gate.kind === "group" ? "Assign a group manager" : "Assign team managers"}
        </h2>
        <p style={{ margin: "8px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
          {gate.kind === "group"
            ? <React.Fragment><strong style={{ color: "#0e0e10" }}>{gate.group.name}</strong> ships to a group manager, but none is assigned. Add one so we know where to send these orders.</React.Fragment>
            : <React.Fragment><strong style={{ color: "#0e0e10" }}>{gate.group.name}</strong> ships to individual team managers. {gate.missing.length === 1 ? "1 roster is" : `${gate.missing.length} rosters are`} missing a manager — add them so we know where to send these orders.</React.Fragment>}
        </p>
      </div>
      <div style={{ padding: "0 28px", maxHeight: "56vh", overflowY: "auto", display: "flex", flexDirection: "column", gap: 20 }}>
        {targets.map((t) => (
          <div key={t.id} style={{ border: "1px solid #ececf0", borderRadius: 10, padding: 16, background: "#fafafb" }}>
            <div style={{ display: "flex", alignItems: "baseline", gap: 8, marginBottom: 12 }}>
              <span style={{ fontSize: 13, fontWeight: 800, color: "#0e0e10" }}>{t.label}</span>
              <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{t.sub}</span>
            </div>
            <ManagerFieldsGrid value={drafts[t.id]} errors={errors[t.id] || {}} onChange={(v) => setDrafts((d) => ({ ...d, [t.id]: v }))} />
          </div>
        ))}
      </div>
      <div style={{ padding: "20px 28px 24px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
        <Button variant="secondary" onClick={onClose} disabled={validating}>Cancel</Button>
        <Button variant="primary" onClick={submit} disabled={validating}>
          {validating ? "Verifying addresses…" : "Save & Continue"}
        </Button>
      </div>
    </Modal>
  );
}
// Read-only manager strip for Rosters-tab detail views. Editing lives in the
// Settings tab ("Team Manager & Freight Mapping") — the link jumps there.
// Moving delivery van — order-fulfillment glyph (motion lines on the left).
function IconVan({ size = 20, style }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round" style={style}>
      <path d="M6 7h9v9.5H9.5" />
      <path d="M15 10h3.2l2.3 3v3.5h-1.6" />
      <circle cx="11.2" cy="16.9" r="1.7" />
      <circle cx="17.2" cy="16.9" r="1.7" />
      <path d="M1.5 9.5h3.2M2.7 12.5h2.6M1.5 15.5h3.2" />
    </svg>);
}
function RosterManagerStrip({ main, accent, accentColor, note, onManage, manageLabel }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 16, background: "#fff", border: "1px solid #ececf0", borderRadius: 10, padding: "16px 16px", marginBottom: 16, boxShadow: "0 1px 2px rgba(20,20,30,.03)" }}>
      <span style={{ width: 40, height: 40, borderRadius: 9, flexShrink: 0, background: "#f1f1f3", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconVan size={20} /></span>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#71717a", textTransform: "uppercase" }}>Order Fulfillment</div>
        <div style={{ marginTop: 3, fontSize: 14, fontWeight: 700, color: "#09090b", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
          {main || "Orders shipping to team managers"}
          {accent && <span style={{ marginLeft: 8, color: accentColor || "#c2410c" }}>{accent}</span>}
        </div>
        {note && <div style={{ marginTop: 3, fontSize: 12, color: "#9a9aa1" }}>{note}</div>}
      </div>
      {onManage &&
        <button type="button" onClick={onManage} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "8px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
          <IconEdit size={13} style={{ strokeWidth: 2 }} /> {manageLabel || "Manage in Settings"}
        </button>}
    </div>);
}

// Inline team-manager editing on the roster view — expands an editor under the
// strip instead of jumping to Settings; saves with a confirmation toast.
function RosterManagerStripEditable({ org, setOrg, rosterId, eff, onToast }) {
  const [open, setOpen] = React.useState(false);
  const [draft, setDraft] = React.useState({});
  const [errors, setErrors] = React.useState({});
  const start = () => { setDraft({ ...(((org && org.managers) || {})[rosterId] || {}) }); setErrors({}); setOpen(true); };
  const save = () => {
    const e = managerFieldErrors(draft);
    setErrors(e);
    if (Object.keys(e).length) return;
    setOrg((o) => ({ ...o, managers: { ...(o.managers || {}), [rosterId]: { ...draft } } }));
    setOpen(false);
    onToast && onToast(`Team manager ${draft.name ? draft.name + " " : ""}saved — freight for this roster routes to them`);
  };
  return (
    <div>
      <RosterManagerStrip main="Orders shipping to team manager"
        accent={eff.src !== "team" ? "Not assigned" : eff.name}
        accentColor={eff.src !== "team" ? "#c2410c" : "#1f7a44"}
        note={eff.src !== "team" ? "A team manager is required before pending orders for this roster can be submitted." : null}
        onManage={open ? null : start}
        manageLabel={eff.src !== "team" ? "Assign Manager" : "Edit Manager"} />
      {open &&
        <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, padding: 16, margin: "-8px 0 16px", boxShadow: "0 1px 2px rgba(20,20,30,.03)" }}>
          <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 12 }}>Team Manager <span style={{ fontWeight: 600, textTransform: "none", letterSpacing: 0, color: "#b0b0b6" }}>· receives this roster's bulk freight</span></div>
          <ManagerFieldsGrid value={draft} errors={errors} onChange={setDraft} />
          <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 16 }}>
            <Button variant="secondary" onClick={() => setOpen(false)}>Cancel</Button>
            <Button variant="primary" onClick={save}>Save Manager</Button>
          </div>
        </div>}
    </div>);
}

// ─────────── Roster tab controller ───────────
function RosterTab({ startEmpty, store, org, setOrg, managerGated, onManageManagers, onCustomize, onProductsChange }) {
  // Single-Team Locker enforces one roster: no groups, no grouping UI.
  const singleMode = (store && store.storeType) === "single";
  // 5 — team-code links need the store slug from row menus deep in the tree.
  if (typeof window !== "undefined") window.__tcStore = store;
  // Roster structure lives in the shared org (lifted to Dashboard) so the
  // Settings tab's manager mapping mirrors it live.
  const groups = org.groups, loose = org.loose;
  const setGroups = (fn) => setOrg((o) => ({ ...o, groups: typeof fn === "function" ? fn(o.groups) : fn }));
  const setLoose = (fn) => setOrg((o) => ({ ...o, loose: typeof fn === "function" ? fn(o.loose) : fn }));
  const [nav, setNav] = React.useState({ view: "home" });
  const [toast, setToast] = React.useState("");
  // Bank-zone plumbing: committed captures flow into the player table (per
  // team), and the table reports claimed numbers back for conflict flagging.
  const [injected, setInjected] = React.useState({});
  const [rosterNums, setRosterNums] = React.useState([]);
  const handleNumbers = React.useCallback((nums) => setRosterNums(nums), []);
  function commitFromBank(teamId, player) {
    setInjected((cur) => ({ ...cur, [teamId]: [...(cur[teamId] || []), player] }));
    setOrg((o) => ({ ...o,
      groups: o.groups.map((g) => ({ ...g, rosters: g.rosters.map((r) => r.id === teamId ? { ...r, players: (r.players || 0) + 1 } : r) })),
      loose: o.loose.map((r) => r.id === teamId ? { ...r, players: (r.players || 0) + 1 } : r) }));
    setToast(`${player.first} ${player.last} committed to the roster`);
  }

  // 8 — a move lands the intact player record on the destination roster and
  // adjusts both player counts. Numbers were already re-validated in the modal.
  function movePlayersTo(fromId, dest, assignments) {
    const arrivals = assignments.map((a, i) => ({ ...a.player, id: "mv-" + Date.now() + "-" + i, number: a.number }));
    setInjected((cur) => ({ ...cur, [dest.id]: [...(cur[dest.id] || []), ...arrivals] }));
    const n = arrivals.length;
    setOrg((o) => ({ ...o,
      groups: o.groups.map((g) => ({ ...g, rosters: g.rosters.map((r) => r.id === dest.id ? { ...r, players: (r.players || 0) + n } : r.id === fromId ? { ...r, players: Math.max(0, (r.players || 0) - n) } : r) })),
      loose: o.loose.map((r) => r.id === dest.id ? { ...r, players: (r.players || 0) + n } : r.id === fromId ? { ...r, players: Math.max(0, (r.players || 0) - n) } : r) }));
  }
  // Destination candidates stay inside the player's own roster group.
  function moveTargetsFor(rosterId) {
    const all = orgRosterList(org);
    const me = all.find((r) => r.id === rosterId);
    return all.filter((r) => (me ? r.groupId === me.groupId : r.groupId === null));
  }

  // Modal state
  const [groupModal, setGroupModal] = React.useState(null); // {mode:'create'|'edit', group?}
  const [confirm, setConfirm] = React.useState(null); // {kind, id, groupId, name, rosterCount}
  const [looseModal, setLooseModal] = React.useState(null); // {mode:'create'|'edit', roster?}
  const [addRosterFor, setAddRosterFor] = React.useState(null); // group object

  const totalGroups = groups.length;
  const totalRosters = groups.reduce((n, g) => n + g.rosters.length, 0) + loose.length;
  const totalPlayers = groups.reduce((n, g) => n + groupPlayerCount(g), 0) + loose.reduce((n, r) => n + (r.players || 0), 0);

  function saveGroup({ mode, id, name, color, manager }) {
    if (mode === "edit") {
      setOrg((o) => {
        const gs = o.groups.map((g) => g.id === id ? { ...g, name, color: color || g.color } : g);
        let managers = o.managers, modes = o.modes;
        if (manager) {
          modes = { ...o.modes, [id]: manager.mode };
          managers = { ...o.managers };
          if (manager.mode === "group" && manager.entry) managers[id] = { ...(managers[id] || {}), ...manager.entry };
          else if (manager.mode === "league") managers[id] = { name: "", email: "", phone: "" };
        }
        return { ...o, groups: gs, managers, modes };
      });
      setToast("Roster group updated");
    } else {
      const g = { id: "grp-" + Date.now(), name, sport: "Boys Basketball", color, rosters: [] };
      setGroups((cur) => [...cur, g]);
      setToast(`Roster group "${name}" created`);
    }
    setGroupModal(null);
  }
  function saveLoose({ mode, id, name, groupId }) {
    if (mode === "edit") {
      setLoose((cur) => cur.map((r) => r.id === id ? { ...r, name } : r));
      setToast("Roster renamed");
    } else if (groupId) {
      const roster = { id: "ros-" + Date.now(), name, players: 0 };
      const grp = groups.find((g) => g.id === groupId);
      setGroups((cur) => cur.map((g) => g.id === groupId ? { ...g, rosters: [...g.rosters, roster] } : g));
      setToast(`Roster "${name}" added to ${grp ? grp.name : "group"}`);
    } else {
      setLoose((cur) => [...cur, { id: "ros-" + Date.now(), name, players: 0 }]);
      setToast(`Roster "${name}" created`);
    }
    setLooseModal(null);
  }
  function addRoster(groupId, { name, division }) {
    const roster = { id: "ros-" + Date.now(), name, division, players: 0 };
    setGroups((cur) => cur.map((g) => g.id === groupId ? { ...g, rosters: [...g.rosters, roster] } : g));
    setAddRosterFor(null);
    setToast(`Roster "${name}" added`);
  }
  function deleteLoose(id) {
    setLoose((cur) => cur.filter((r) => r.id !== id));
    setToast("Roster removed");
  }
  // Deleting a group keeps its rosters — they become standalone rosters,
  // dropping the group's branding/division tag.
  function deleteGroup(groupId) {
    const g = groups.find((x) => x.id === groupId);
    if (!g) return;
    const freed = g.rosters.map((r) => ({ id: r.id, name: r.name, players: r.players || 0 }));
    setLoose((cur) => [...cur, ...freed]);
    setGroups((cur) => cur.filter((x) => x.id !== groupId));
    if (nav.view === "group" && nav.groupId === groupId) setNav({ view: "home" });
    setToast(freed.length
      ? `Group deleted — ${freed.length} ${freed.length === 1 ? "roster" : "rosters"} kept as standalone`
      : "Roster group deleted");
  }
  function deleteGroupRoster(groupId, rosterId) {
    setGroups((cur) => cur.map((g) => g.id === groupId ? { ...g, rosters: g.rosters.filter((r) => r.id !== rosterId) } : g));
    setToast("Roster removed");
  }

  function openGroup(g) { setNav({ view: "group", groupId: g.id }); }
  function openRoster(group, roster) {
    setNav({
      view: "roster", groupId: group ? group.id : null, rosterId: roster.id,
      title: roster.name,
      subtitle: group ? `${group.sport} · Uses ${group.name} branding` : `Team Code ${rTeamCode(roster.id)}`,
      color: group ? group.color : "#3f3f46",
      avatar: roster.division || rInitials(roster.name),
      fresh: (roster.players || 0) === 0,
      groupName: group ? group.name : null });
  }

  let body;
  if (singleMode) {
    // Single-Team Locker: no groups, no naming, no breadcrumbs/banner —
    // clicking the Roster tab drops straight into the one roster's player table.
    const single = loose[0];
    const sid = single ? single.id : "single-roster";
    body = (
      <React.Fragment>
        <RosterPlayerTable store={store} startEmpty={(single?.players || 0) === 0}
          rosterName={single ? single.name : "your team"} rosterId={sid}
          moveTargets={moveTargetsFor(sid)}
          onMovePlayers={(dest, assignments) => movePlayersTo(sid, dest, assignments)}
          injected={injected[sid]} onNumbers={handleNumbers} />
        <div style={{ marginTop: 20 }}>
          <BankZone teamId={sid} teamName={single ? single.name : "your team"} org={org} setOrg={setOrg} rosterNumbers={rosterNums} onCommit={(p) => commitFromBank(sid, p)} />
        </div>
      </React.Fragment>);
  } else if (nav.view === "roster") {
    const crumbs = nav.groupId ?
      [{ label: "My Teams", onClick: () => setNav({ view: "home" }) },
       { label: nav.groupName, onClick: () => setNav({ view: "home" }) },
       { label: nav.title }] :
      [{ label: "My Teams", onClick: () => setNav({ view: "home" }) }, { label: nav.title }];
    // Per-roster fulfillment only applies when the group is set to individual
    // team managers in Settings; in group/league-manager modes freight is handled
    // at the group level, so no strip or manager label shows here.
    const teamsMode = managerGated && nav.groupId && (((org && org.modes) || {})[nav.groupId] || "league") === "teams";
    const eff = teamsMode ? effectiveManagerFor(org, store, nav.groupId, nav.rosterId) : null;
    const subtitle = eff && nav.groupId
      ? (eff.src !== "team"
        ? <a href="#" onClick={(e) => { e.preventDefault(); onManageManagers && onManageManagers(nav.groupId, nav.rosterId); }}
            style={{ color: "#2563eb", fontWeight: 700, textDecoration: "none" }}>Add team manager</a>
        : <React.Fragment>Team Manager <span style={{ color: "#0e0e10", fontWeight: 600 }}>{eff.name}</span></React.Fragment>)
      : nav.subtitle;
    const header = (
      <div>
        <RosterCrumbs items={crumbs} />
        {eff &&
          <RosterManagerStripEditable org={org} setOrg={setOrg} rosterId={nav.rosterId} eff={eff} onToast={setToast} />}
        <RosterBanner color={nav.color} avatar={nav.avatar} title={nav.title} subtitle={subtitle} />
      </div>);
    body = <RosterPlayerTable store={store} startEmpty={nav.fresh} header={header} injected={injected[nav.rosterId]} onNumbers={handleNumbers}
      rosterName={nav.title} rosterId={nav.rosterId}
      moveTargets={moveTargetsFor(nav.rosterId)}
      onMovePlayers={(dest, assignments) => movePlayersTo(nav.rosterId, dest, assignments)}
      onDeleteRoster={() => {
        if (nav.groupId) deleteGroupRoster(nav.groupId, nav.rosterId); else deleteLoose(nav.rosterId);
        setNav({ view: "home" });
        setToast("Roster \"" + nav.title + "\" deleted");
      }} />;
    body = (
      <React.Fragment>
        {body}
        <div style={{ marginTop: 20 }}>
          <BankZone teamId={nav.rosterId} teamName={nav.title} org={org} setOrg={setOrg} rosterNumbers={rosterNums} onCommit={(p) => commitFromBank(nav.rosterId, p)} />
        </div>
      </React.Fragment>);
  } else {
    // Flattened structure: the division → team canvas replaces the old
    // roster-group card grid entirely (My Teams).
    body = (
      <TeamCanvasView
        groups={groups} loose={loose} org={org} setOrg={setOrg} store={store}
        managerGated={managerGated} onManageManagers={onManageManagers}
        stats={{ totalGroups, totalRosters, totalPlayers }}
        onOpenRoster={openRoster}
        onCustomize={onCustomize} onProductsChange={onProductsChange}
        setToast={setToast} />);
  }

  return (
    <React.Fragment>
      {body}
      <RosterGroupModal
        open={!!groupModal}
        mode={groupModal?.mode}
        initial={groupModal?.group}
        org={org} store={store} managerGated={managerGated}
        onClose={() => setGroupModal(null)}
        onSave={saveGroup}
        onDelete={() => {
          const g = groupModal?.group; setGroupModal(null);
          if (g) setConfirm({ kind: "group", id: g.id, name: g.name, rosterCount: g.rosters.length });
        }} />
      <NewRosterModal
        open={!!looseModal}
        mode={looseModal?.mode}
        initial={looseModal?.roster}
        groups={groups}
        store={store}
        org={org}
        onClose={() => setLooseModal(null)}
        onSave={saveLoose} />
      <AddRosterModal
        open={!!addRosterFor}
        group={addRosterFor}
        store={store}
        org={org}
        onClose={() => setAddRosterFor(null)}
        onAdd={(payload) => addRoster(addRosterFor.id, payload)} />
      <RosterConfirmDeleteModal
        confirm={confirm}
        onClose={() => setConfirm(null)}
        onConfirm={() => {
          if (!confirm) return;
          if (confirm.kind === "group") deleteGroup(confirm.id);
          else if (confirm.kind === "groupRoster") deleteGroupRoster(confirm.groupId, confirm.id);
          setConfirm(null);
        }} />
      <Toast message={toast} onDone={() => setToast("")} />
    </React.Fragment>);
}

// ─────────── Bank zone placeholder (Phase 2 builds the real queue) ───────────
function IconBankInbox({ size = 18, style }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round" style={style}>
      <path d="M22 12h-6l-2 3h-4l-2-3H2" />
      <path d="M5.4 5.1 2 12v6a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2v-6l-3.4-6.9A2 2 0 0 0 16.8 4H7.2a2 2 0 0 0-1.8 1.1Z" />
    </svg>);
}
function BankZonePlaceholder({ teamName }) {
  return (
    <div style={{ border: "1px dashed #d8d8de", borderRadius: 10, padding: "14px 20px", marginBottom: 20, display: "flex", alignItems: "center", gap: 16, background: "#fafafb" }}>
      <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: "#f1f1f3", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconBankInbox size={18} /></span>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
          <span style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".06em", color: "#3f3f46", textTransform: "uppercase" }}>Roster Bank</span>
          <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#ececf0", borderRadius: 4, padding: "3px 8px", textTransform: "uppercase" }}>Coming soon</span>
        </div>
        <div style={{ marginTop: 3, fontSize: 12.5, color: "#9a9aa1", lineHeight: 1.5 }}>Team-gear checkout captures will land here for review — commit them to {teamName || "the roster"} with one click.</div>
      </div>
      <span style={{ flexShrink: 0, fontSize: 11.5, fontWeight: 600, color: "#9a9aa1", fontFamily: "JetBrains Mono, monospace" }}>0 issued · 0 committed · 0 outstanding</span>
    </div>);
}

// ─────────── Bank Zone (Epic C): capture → pre-group → commit ───────────
// Team-gear checkout captures stage here as *proposed players* until the rep
// commits them. The system pre-groups captures sharing signals (email, phone,
// address, similar name) — a suggestion, not an authority: the rep splits a
// wrong merge (two siblings on one family email) or merges clusters the
// system missed ("Jonathan" vs "Jon"). Uncommitted captures still count:
// their jersey numbers read as claimed and they show in completion tracking.
// Fan Gear orders never create captures.

function makeBankSeed(singleMode, startEmpty) {
  if (startEmpty) return {};
  const seed = (p) => [
    // Pre-grouped by shared contact email — includes a WRONG merge (Ellie is
    // Mason's sibling on the family email) for the split demo.
    { id: p + "-bz1", committed: false, captures: [
      { id: p + "-c1", player: "Mason Rivera", nameOnJersey: "Rivera", jersey: "23", size: "YM", item: "Home Game Jersey", tier: "team-required", email: "rivera.family@gmail.com", phone: "(385) 555-0114", address: "1180 Fox Hollow Ln, Draper, UT 84020", order: "435AFHSPY4D91", date: "Jul 21, 2026" },
      { id: p + "-c2", player: "Mason Rivera", nameOnJersey: "Rivera", jersey: "23", size: "YM", item: "Home Game Shorts", tier: "team-required", email: "rivera.family@gmail.com", phone: "(385) 555-0114", address: "1180 Fox Hollow Ln, Draper, UT 84020", order: "435AFHSPY4D91", date: "Jul 21, 2026" },
      { id: p + "-c3", player: "Ellie Rivera", nameOnJersey: "E. Rivera", jersey: "15", size: "YS", item: "Home Game Jersey", tier: "team-required", email: "rivera.family@gmail.com", phone: "(385) 555-0114", address: "1180 Fox Hollow Ln, Draper, UT 84020", order: "435AFHSPY4D92", date: "Jul 22, 2026" }] },
    // "Jonathan" vs "Jon" under different emails — the merge demo.
    { id: p + "-bz2", committed: false, captures: [
      { id: p + "-c4", player: "Jonathan Pruitt", nameOnJersey: "Pruitt", jersey: "4", size: "YL", item: "Away Game Jersey", tier: "team-required", email: "j.pruitt.home@gmail.com", phone: "(801) 555-0177", address: "44 S Cedar St, Lehi, UT 84043", order: "435AFHSPY4D93", date: "Jul 23, 2026" }] },
    { id: p + "-bz3", committed: false, captures: [
      { id: p + "-c5", player: "Jon Pruitt", nameOnJersey: "Pruitt", jersey: "4", size: "YL", item: "Team Warm-Up Hoodie", tier: "team-supplied", email: "jpruitt@velocityhvac.com", phone: "(801) 555-0142", address: "PO Box 210, Lehi, UT 84043", order: "435AFHSPY4D94", date: "Jul 24, 2026" }] },
    // Claims #15 — conflicts with Ellie's uncommitted capture above.
    { id: p + "-bz4", committed: false, captures: [
      { id: p + "-c6", player: "Ava Chen", nameOnJersey: "Chen", jersey: "15", size: "YM", item: "Home Game Jersey", tier: "team-required", email: "chenfam@yahoo.com", phone: "(385) 555-0163", address: "2210 E Aspen Way, Sandy, UT 84094", order: "435AFHSPY4D95", date: "Jul 25, 2026" }] }];
  // Live captures written by the storefront prototype's consent-gated checkout
  // (same-origin localStorage bridge) land alongside the seeded clusters.
  const liveFor = (prefix) => {
    let raw = [];
    try { raw = JSON.parse(localStorage.getItem("prolook_bank_captures_v1") || "[]"); } catch (e) {}
    if (!Array.isArray(raw)) raw = [];
    return raw.map((c, i) => ({
      id: prefix + "-live-" + (c.id || i), committed: false,
      captures: (c.items || []).map((it, j) => ({
        id: prefix + "-live-" + (c.id || i) + "-" + j, player: c.player || "Unknown player",
        nameOnJersey: it.nameOnJersey || "", jersey: String(it.jersey || ""), size: it.size || "",
        item: it.item || "Team item", tier: it.tier || "team-required",
        email: c.email || "", phone: c.phone || "", address: c.address || "",
        order: c.order || "", date: c.date || "" })),
    })).filter((cl) => cl.captures.length);
  };
  return singleMode
    ? { "single-roster": [...seed("single"), ...liveFor("single")] }
    : { "sf-8": [...seed("sf8"), ...liveFor("sf8")] };
}

function bzTierBadge(tier) {
  return tier === "team-supplied"
    ? { label: "Team Supplied", color: "#2944a8", bg: "#eef1fb" }
    : { label: "Team Required", color: "#b81d14", bg: "#fdecea" };
}
function bzClusterJersey(cl) { const c = cl.captures.find((x) => x.jersey); return c ? String(c.jersey) : ""; }
function bzClusterName(cl) { return (cl.captures[0] && cl.captures[0].player) || "Unknown player"; }
function bzSignals(cl) {
  if (cl.captures.length < 2) return null;
  const same = (k) => cl.captures.every((c) => c[k] && c[k] === cl.captures[0][k]);
  if (same("email")) return "same contact email";
  if (same("phone")) return "same phone";
  if (same("address")) return "same shipping address";
  return "similar player name";
}
function bzToPlayer(cl, jersey) {
  const c0 = cl.captures[0];
  const parts = String(c0.player || "").trim().split(/\s+/);
  const first = parts[0] || "", last = parts.slice(1).join(" ");
  return { id: "bank-" + cl.id + "-" + Date.now(), first, last, number: jersey, nameOnJersey: c0.nameOnJersey || last || first, email: c0.email || "", phone: c0.phone || "", associatedEmails: [], shippingAddress: c0.address || "", addressFromCheckout: true };
}

function BankConflictModal({ cluster, conflictLabel, isTaken, onClose, onCommit }) {
  const orig = bzClusterJersey(cluster);
  const [num, setNum] = React.useState(orig);
  const clean = String(num).replace(/[^0-9]/g, "").slice(0, 2);
  const stillTaken = clean !== "" && isTaken(clean, cluster.id);
  const canCommit = clean !== "" && !stillTaken;
  return (
    <Modal open onClose={onClose} width={460}>
      <div style={{ padding: "28px 28px 24px" }}>
        <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Resolve jersey number</h2>
        <p style={{ margin: "8px 0 20px", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
          <strong style={{ color: "#0e0e10" }}>#{orig}</strong> is already claimed {conflictLabel}. Pick a different number to commit <strong style={{ color: "#0e0e10" }}>{bzClusterName(cluster)}</strong> to the roster.
        </p>
        <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>Jersey Number</label>
        <input value={num} autoFocus inputMode="numeric" maxLength={2}
          onChange={(e) => setNum(e.target.value.replace(/[^0-9]/g, "").slice(0, 2))}
          style={{ width: 120, boxSizing: "border-box", padding: "12px 14px", fontSize: 16, fontWeight: 700, fontFamily: "JetBrains Mono, monospace", border: `1px solid ${stillTaken ? "#e1251b" : "#d4d4d8"}`, borderRadius: 8, outline: "none", color: "#0e0e10" }} />
        {stillTaken && <div style={{ marginTop: 8, fontSize: 12, color: "#b81d14", fontWeight: 600 }}>#{clean} is also taken — uncommitted captures count too.</div>}
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
          <button onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
          <button disabled={!canCommit} onClick={() => canCommit && onCommit(clean)} style={{ background: canCommit ? "#0e0e10" : "#d4d4d8", border: "1px solid transparent", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", color: "#fff", cursor: canCommit ? "pointer" : "not-allowed", fontFamily: "inherit" }}>Add to Roster{clean ? ` · #${clean}` : ""}</button>
        </div>
      </div>
    </Modal>);
}

function BankZone({ teamId, teamName, org, setOrg, rosterNumbers, onCommit, commitDisabledReason }) {
  const clusters = ((org && org.bank) || {})[teamId] || [];
  const outstanding = clusters.filter((c) => !c.committed);
  const issued = clusters.reduce((n, c) => n + c.captures.length, 0);
  const committedCaptures = clusters.filter((c) => c.committed).reduce((n, c) => n + c.captures.length, 0);
  const [selected, setSelected] = React.useState(() => new Set());
  const [filter, setFilter] = React.useState("all");
  const [committing, setCommitting] = React.useState({});
  const [resolve, setResolve] = React.useState(null);
  const [open, setOpen] = React.useState(false);
  const [limit, setLimit] = React.useState(25);
  const setBank = (fn) => setOrg((o) => ({ ...o, bank: { ...(o.bank || {}), [teamId]: fn(((o.bank || {})[teamId]) || []) } }));

  // Jersey claims — other uncommitted clusters count, plus the committed roster.
  const isTaken = (n, exceptId) => !!outstanding.find((c) => c.id !== exceptId && bzClusterJersey(c) === n) || (rosterNumbers || []).includes(n);
  const conflictFor = (cl) => {
    const n = bzClusterJersey(cl);
    if (!n) return null;
    const other = outstanding.find((c) => c.id !== cl.id && bzClusterJersey(c) === n);
    if (other) return { n, label: `by ${bzClusterName(other)} — an uncommitted capture in the Roster Bank` };
    if ((rosterNumbers || []).includes(n)) return { n, label: "by a player already on the roster" };
    return null;
  };
  const conflictCount = outstanding.filter((c) => conflictFor(c)).length;
  const shown = filter === "conflicts" ? outstanding.filter((c) => conflictFor(c)) : outstanding;

  const clusterOf = (cid) => outstanding.find((c) => c.captures.some((x) => x.id === cid));
  const selClusterIds = [...new Set([...selected].map((cid) => { const c = clusterOf(cid); return c && c.id; }).filter(Boolean))];
  const splitSrc = selClusterIds.length === 1 ? outstanding.find((c) => c.id === selClusterIds[0]) : null;
  const canSplit = !!(splitSrc && selected.size > 0 && selected.size < splitSrc.captures.length);
  const canMerge = selClusterIds.length >= 2;
  function toggleCapture(cid) { setSelected((s) => { const n = new Set(s); if (n.has(cid)) n.delete(cid); else n.add(cid); return n; }); }
  function splitSel() {
    if (!canSplit) return;
    const srcId = splitSrc.id;
    setBank((cur) => {
      const src = cur.find((c) => c.id === srcId);
      if (!src) return cur;
      const moved = src.captures.filter((x) => selected.has(x.id));
      const kept = src.captures.filter((x) => !selected.has(x.id));
      return [...cur.map((c) => c.id === srcId ? { ...c, captures: kept } : c), { id: "bz-" + Date.now(), committed: false, captures: moved }];
    });
    setSelected(new Set());
  }
  function mergeSel() {
    if (!canMerge) return;
    const ids = selClusterIds;
    setBank((cur) => {
      const merging = cur.filter((c) => !c.committed && ids.includes(c.id));
      const rest = cur.filter((c) => c.committed || !ids.includes(c.id));
      return [...rest, { id: "bz-" + Date.now(), committed: false, captures: merging.flatMap((c) => c.captures) }];
    });
    setSelected(new Set());
  }
  function commitCluster(cl, jerseyOverride) {
    const jersey = jerseyOverride || bzClusterJersey(cl);
    setCommitting((s) => ({ ...s, [cl.id]: true }));
    setTimeout(() => {
      setBank((cur) => cur.map((c) => c.id === cl.id ? { ...c, committed: true } : c));
      setCommitting((s) => { const n = { ...s }; delete n[cl.id]; return n; });
      onCommit && onCommit(bzToPlayer(cl, jersey));
    }, 340);
  }
  function requestCommit(cl) {
    if (conflictFor(cl)) setResolve({ cluster: cl });
    else commitCluster(cl);
  }

  const chip = (label, on, onClick) => (
    <button type="button" onClick={onClick} style={{ padding: "6px 12px", border: 0, borderRadius: 999, cursor: "pointer", fontSize: 11.5, fontWeight: 700, fontFamily: "inherit", background: on ? "#0e0e10" : "#f1f1f3", color: on ? "#fff" : "#6b6b72" }}>{label}</button>);

  return (
    <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 12, marginBottom: 20, boxShadow: "0 1px 2px rgba(20,20,30,.04)", overflow: "hidden" }}>
      <button type="button" onClick={() => setOpen((v) => !v)} style={{ display: "flex", alignItems: "center", gap: 14, width: "100%", textAlign: "left", background: "#fff", border: 0, padding: "14px 20px", cursor: "pointer", fontFamily: "inherit" }}>
        <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: outstanding.length ? "#fdf6e7" : "#f1f1f3", color: outstanding.length ? "#8a5f10" : "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconBankInbox size={18} /></span>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
            <span style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".06em", color: "#3f3f46", textTransform: "uppercase" }}>Roster Bank</span>
            {outstanding.length > 0 &&
              <span style={{ fontSize: 10.5, fontWeight: 800, color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "3px 9px" }}>{outstanding.length} proposed {outstanding.length === 1 ? "player" : "players"}</span>}
            {conflictCount > 0 &&
              <span style={{ fontSize: 10.5, fontWeight: 800, color: "#b81d14", background: "#fdecea", borderRadius: 999, padding: "3px 9px" }}>{conflictCount} number {conflictCount === 1 ? "conflict" : "conflicts"}</span>}
          </div>
          <div style={{ marginTop: 3, fontSize: 12.5, color: "#9a9aa1" }}>Team-gear checkout captures awaiting review for {teamName || "this roster"}</div>
        </div>
        <span style={{ flexShrink: 0, fontSize: 11.5, fontWeight: 600, color: "#6b6b72", fontFamily: "JetBrains Mono, monospace" }}>{issued} issued · {committedCaptures} committed · {issued - committedCaptures} outstanding</span>
        <IconChevron size={18} style={{ flexShrink: 0, color: "#9a9aa1", transform: open ? "rotate(180deg)" : "none", transition: "transform .16s" }} />
      </button>
      {open &&
        <div style={{ borderTop: "1px solid #f3f3f4" }}>
          {outstanding.length > 0 &&
            <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "10px 20px", background: "#fafafb", borderBottom: "1px solid #f3f3f4" }}>
              <span style={{ flex: 1, fontSize: 12, color: "#9a9aa1", lineHeight: 1.5 }}>Captures sharing an email, phone, address, or similar name are pre-grouped into proposed players — a suggestion, not a decision. Select captures to split or merge; you make the final call.</span>
              {chip("All", filter === "all", () => setFilter("all"))}
              {chip(`Conflicts · ${conflictCount}`, filter === "conflicts", () => setFilter("conflicts"))}
            </div>}
          {shown.length === 0 &&
            <div style={{ padding: "36px 20px", textAlign: "center", fontSize: 13, color: "#9a9aa1" }}>
              {outstanding.length === 0 ? "Roster Bank clear — new team-gear captures will land here as orders come in." : "No conflicts — switch back to All to keep reviewing."}
            </div>}
          {shown.slice(0, limit).map((cl) => {
            const conf = conflictFor(cl);
            const hiding = !!committing[cl.id];
            const sig = bzSignals(cl);
            const jersey = bzClusterJersey(cl);
            return (
              <div key={cl.id} style={{ overflow: "hidden", maxHeight: hiding ? 0 : 600, opacity: hiding ? 0 : 1, transform: hiding ? "translateX(32px)" : "none", transition: "max-height .34s ease, opacity .26s ease, transform .26s ease", borderTop: "1px solid #f3f3f4" }}>
                <div style={{ padding: "14px 20px 12px" }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
                    <span style={{ width: 36, height: 36, borderRadius: 9, flexShrink: 0, background: "#f1f1f3", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 12.5, fontFamily: "JetBrains Mono, monospace" }}>#{jersey || "—"}</span>
                    <div style={{ flex: 1, minWidth: 0 }}>
                      <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
                        <span style={{ fontSize: 14, fontWeight: 800, color: "#0e0e10" }}>{bzClusterName(cl)}</span>
                        <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 4, padding: "2px 6px", textTransform: "uppercase" }}>Proposed</span>
                        {sig && <span style={{ fontSize: 10.5, fontWeight: 700, color: "#6b6b72", background: "#f1f1f3", borderRadius: 4, padding: "3px 7px" }}>Grouped — {sig}</span>}
                        {conf && <span title={`#${conf.n} is claimed ${conf.label}`} style={{ display: "inline-flex", alignItems: "center", gap: 5, fontSize: 10.5, fontWeight: 800, color: "#b81d14", background: "#fdecea", borderRadius: 4, padding: "3px 7px" }}>⚠ #{conf.n} taken</span>}
                      </div>
                      <div style={{ marginTop: 2, fontSize: 12, color: "#9a9aa1" }}>{cl.captures.length} {cl.captures.length === 1 ? "capture" : "captures"} · {cl.captures[0].email}</div>
                    </div>
                    <button type="button" title={commitDisabledReason || undefined} onClick={() => !commitDisabledReason && requestCommit(cl)} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: commitDisabledReason ? "#d4d4d8" : "#0e0e10", border: 0, color: "#fff", borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 800, cursor: commitDisabledReason ? "not-allowed" : "pointer", fontFamily: "inherit" }}>
                      <IconPlus size={13} style={{ strokeWidth: 2.6 }} /> Add to Roster
                    </button>
                  </div>
                  <div style={{ marginTop: 10, marginLeft: 48 }}>
                    {cl.captures.map((c) => {
                      const b = bzTierBadge(c.tier);
                      const on = selected.has(c.id);
                      return (
                        <label key={c.id} style={{ display: "grid", gridTemplateColumns: "16px 1.3fr auto 40px 1.3fr 1fr", alignItems: "center", gap: 10, padding: "7px 8px", borderRadius: 6, cursor: "pointer", background: on ? "#f5f7ff" : "transparent" }}>
                          <input type="checkbox" checked={on} onChange={() => toggleCapture(c.id)} style={{ accentColor: "#0e0e10", width: 14, height: 14, cursor: "pointer" }} />
                          <span style={{ fontSize: 12.5, fontWeight: 600, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{c.item}</span>
                          <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".04em", color: b.color, background: b.bg, borderRadius: 4, padding: "3px 7px", textTransform: "uppercase", whiteSpace: "nowrap" }}>{b.label}</span>
                          <span style={{ fontSize: 12, color: "#6b6b72", fontWeight: 600 }}>{c.size}</span>
                          <span style={{ fontSize: 12, color: "#9a9aa1", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{c.player} · {c.email}</span>
                          <span style={{ fontSize: 11, color: "#9a9aa1", fontFamily: "JetBrains Mono, monospace", textAlign: "right", whiteSpace: "nowrap" }}>{c.order} · {c.date}</span>
                        </label>);
                    })}
                  </div>
                </div>
              </div>);
          })}
          {shown.length > limit &&
            <button type="button" onClick={() => setLimit((l) => l + 50)} style={{ display: "block", width: "100%", textAlign: "center", background: "#fafafb", border: 0, borderTop: "1px solid #f3f3f4", padding: "13px 20px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Show 50 more · {shown.length - limit} more proposed players in the backlog</button>}
          {selected.size > 0 &&
            <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "12px 20px", borderTop: "1px solid #f3f3f4", background: "#fafafb" }}>
              <span style={{ flex: 1, fontSize: 12.5, fontWeight: 700, color: "#0e0e10" }}>{selected.size} {selected.size === 1 ? "capture" : "captures"} selected</span>
              <button type="button" disabled={!canSplit} onClick={splitSel} title={canSplit ? "Move the selected captures into their own proposed player" : "Select part of one multi-capture group to split it"}
                style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: canSplit ? "#0e0e10" : "#b0b0b6", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: canSplit ? "pointer" : "not-allowed", fontFamily: "inherit" }}>Split into new player</button>
              <button type="button" disabled={!canMerge} onClick={mergeSel} title={canMerge ? "Combine the selected proposed players into one" : "Select captures from two or more groups to merge them"}
                style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: canMerge ? "#0e0e10" : "#b0b0b6", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: canMerge ? "pointer" : "not-allowed", fontFamily: "inherit" }}>Merge into one player</button>
              <button type="button" onClick={() => setSelected(new Set())} style={{ background: "transparent", border: 0, color: "#6b6b72", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", padding: "8px 6px" }}>Clear</button>
            </div>}
        </div>}
      {resolve &&
        <BankConflictModal cluster={resolve.cluster} conflictLabel={(conflictFor(resolve.cluster) || {}).label || ""} isTaken={isTaken}
          onClose={() => setResolve(null)}
          onCommit={(n) => { const cl = resolve.cluster; setResolve(null); commitCluster(cl, n); }} />}
    </div>);
}

// ─────────── My Teams canvas (flattened division → team builder) ───────────
// Parents (Divisions/Regions — formerly roster groups) head each lane; child
// Teams (formerly rosters) hang beneath, connected by tree edges. Teams may
// sit unassigned and be drag-connected later; dragging an assigned team to a
// different division re-parents it (with confirmation). Auto-layout — nodes
// snap into the grid, no free positioning.

// ─────────── Product ↔ Team association on the My Teams canvas (league stores) ───────────
// Products carry `rosters` (team and/or division ids). The canvas surfaces those
// assignments on the nodes (thumbnail strips + division count pills), in the detail
// panel's Products tab, and through an Add-from-Catalog drawer with drag-to-assign
// (dropping on a division header covers all of its teams).
const CP_SETTINGS = {
  "team-required": { label: "Team Required", color: "#d9483b" },
  "team-supplied": { label: "Team Supplied", color: "#2944a8" },
  "fan-gear": { label: "Fan Gear", color: "#1f7a44" },
};
function cpSetting(p) { return CP_SETTINGS[p.teamSetting] || CP_SETTINGS["fan-gear"]; }
function cpPrice(p, tier) {
  const dp = defaultPricing();
  const v = p.pricing?.[tier]?.retail ?? p[tier] ?? dp[tier]?.retail;
  return v != null && v !== "" ? "$" + v : "—";
}
function CpThumb({ product, store, size = 24 }) {
  return (
    <span style={{ width: size, height: size, borderRadius: Math.max(4, Math.round(size / 5)), flexShrink: 0, background: "#f1f2f4", display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden", boxSizing: "border-box", padding: Math.max(2, Math.round(size / 10)) }}>
      <span style={{ width: "62%", height: "88%", display: "block" }}>{product.thumbnail || <DefaultProductThumb store={store} product={product} />}</span>
    </span>);
}
// Floating product card on thumbnail hover — mirrors the Products-tab card zones
// (thumb, name, design id, setting, teams, youth/adult price) in a compact popover.
function CpHoverCard({ product, store, anchor }) {
  const w = 300;
  const left = Math.max(8, Math.min(anchor.left + anchor.width / 2 - w / 2, (typeof window !== "undefined" ? window.innerWidth : 1400) - w - 8));
  const top = anchor.bottom + 8;
  const s = cpSetting(product);
  const names = (product.rosters || []).map((id) => (assignRosterLabel(id) || {}).name).filter(Boolean);
  const supplied = product.teamSetting === "team-supplied";
  return (
    <div style={{ position: "fixed", left, top, width: w, zIndex: 4200, background: "#fff", border: "1px solid #ececf0", borderRadius: 12, boxShadow: "0 16px 40px rgba(9,9,11,.18)", padding: 14, pointerEvents: "none", boxSizing: "border-box" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <div style={{ width: 56, height: 56, flexShrink: 0, background: "#f1f2f4", borderRadius: 8, display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 8, boxSizing: "border-box" }}>
          <div style={{ width: 32, height: 44 }}>{product.thumbnail || <DefaultProductThumb store={store} product={product} />}</div>
        </div>
        <div style={{ minWidth: 0, flex: 1 }}>
          <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{product.title}</div>
          {product.subtitle && <div style={{ marginTop: 2, fontSize: 11.5, color: "#8a8a90", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{product.subtitle}</div>}
          <div style={{ marginTop: 4, fontSize: 10.5, fontWeight: 800, letterSpacing: ".03em", color: s.color }}>{s.label}</div>
        </div>
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 16, marginTop: 12, paddingTop: 10, borderTop: "1px solid #f3f3f4" }}>
        <span style={{ flex: 1, minWidth: 0, fontSize: 11.5, color: "#6b6b72", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{names.length ? names.join(", ") : "League-wide"}</span>
        <span style={{ flexShrink: 0, fontSize: 12.5, fontWeight: 700, color: supplied ? "#b0b0b6" : "#0e0e10", fontVariantNumeric: "tabular-nums" }}>{supplied ? "Covered" : "Y " + cpPrice(product, "youth") + " · A " + cpPrice(product, "adult")}</span>
      </div>
      <div style={{ marginTop: 8, fontSize: 10.5, color: "#9a9aa1" }}>Click to open product details</div>
    </div>);
}
function CpChip({ product, store, onClick, size = 24, title }) {
  const [rect, setRect] = React.useState(null);
  const ref = React.useRef(null);
  return (
    <span ref={ref} title={title}
      onMouseEnter={() => { if (ref.current) setRect(ref.current.getBoundingClientRect()); }}
      onMouseLeave={() => setRect(null)}
      onClick={onClick ? (e) => { e.stopPropagation(); setRect(null); onClick(); } : undefined}
      style={{ display: "inline-flex", cursor: onClick ? "pointer" : "default", flexShrink: 0 }}>
      <CpThumb product={product} store={store} size={size} />
      {rect && <CpHoverCard product={product} store={store} anchor={rect} />}
    </span>);
}
// Split "Add Products" entry: Add New Product (customize flow) / Add from Catalog
// (assign an existing store product), scoped to a team, a division, or store-wide.
function CpAddMenu({ label = "Add Products", scopeName, fullWidth, onNew, onCatalog }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, [open]);
  const item = (icon, title, sub, onClick) => (
    <div onClick={() => { setOpen(false); onClick && onClick(); }}
      onMouseEnter={(e) => { e.currentTarget.style.background = "#f7f7f9"; }}
      onMouseLeave={(e) => { e.currentTarget.style.background = "transparent"; }}
      style={{ display: "flex", alignItems: "flex-start", gap: 10, padding: "9px 10px", borderRadius: 6, cursor: "pointer" }}>
      <span style={{ width: 28, height: 28, borderRadius: 6, background: "#f1f1f3", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>{icon}</span>
      <span style={{ minWidth: 0 }}>
        <span style={{ display: "block", fontSize: 12, fontWeight: 700, color: "#0e0e10" }}>{title}</span>
        <span style={{ display: "block", fontSize: 10.5, color: "#9a9aa1", marginTop: 1, lineHeight: 1.45 }}>{sub}</span>
      </span>
    </div>);
  return (
    <div ref={ref} style={{ position: "relative", width: fullWidth ? "100%" : "auto", display: fullWidth ? "block" : "inline-block", flexShrink: 0 }}>
      <button type="button" onClick={() => setOpen((o) => !o)} style={{ width: fullWidth ? "100%" : "auto", background: "#0e0e10", color: "#fff", border: "1px solid #0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, borderRadius: 6, padding: "10px 16px", fontSize: 12.5, fontWeight: 700, letterSpacing: ".02em", cursor: "pointer", fontFamily: "inherit", boxSizing: "border-box" }}>
        <IconPlus size={13} style={{ strokeWidth: 2.4 }} /> {label}
        <svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="3" strokeLinecap="round" strokeLinejoin="round" style={{ marginLeft: 2 }}><path d="M6 9l6 6 6-6" /></svg>
      </button>
      {open &&
        <div style={{ position: "absolute", top: "calc(100% + 6px)", right: 0, minWidth: 264, width: fullWidth ? "100%" : undefined, boxSizing: "border-box", background: "#fff", border: "1px solid #ececf0", borderRadius: 9, boxShadow: "0 14px 34px rgba(20,20,30,.18)", padding: 6, zIndex: 80 }}>
          {item(<IconPlus size={14} style={{ strokeWidth: 2.4 }} />, "Add New Product", "Customize a new product in the builder — assign teams while configuring", onNew)}
          {item(<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><rect x="3" y="3" width="7" height="7" rx="1.5" /><rect x="14" y="3" width="7" height="7" rx="1.5" /><rect x="3" y="14" width="7" height="7" rx="1.5" /><rect x="14" y="14" width="7" height="7" rx="1.5" /></svg>, "Add from Catalog", scopeName ? "Assign existing store products to " + scopeName : "Assign existing store products to teams", onCatalog)}
        </div>}
    </div>);
}
function CpPanelTabs({ tab, onChange, count }) {
  const seg = (id, label) => (
    <span key={id} onClick={() => onChange(id)} style={{ flex: 1, height: 30, display: "inline-flex", alignItems: "center", justifyContent: "center", borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer", color: tab === id ? "#0e0e10" : "#6b6b72", background: tab === id ? "#fff" : "transparent", boxShadow: tab === id ? "0 1px 2px rgba(20,20,30,.1)" : "none", whiteSpace: "nowrap" }}>{label}</span>);
  return (
    <div style={{ display: "flex", background: "#ececf0", borderRadius: 8, padding: 3, gap: 2, marginBottom: 18 }}>
      {seg("setup", "Setup")}
      {seg("products", count != null ? "Products · " + count : "Products")}
    </div>);
}
function CpPanelRow({ p, store, via, onOpen }) {
  const s = cpSetting(p);
  const others = (p.rosters || []).length - (via ? 0 : 1);
  const meta = (p.teamSetting === "team-supplied" ? "Covered" : cpPrice(p, "adult"))
    + (via ? " · via " + via : others > 0 ? " · " + others + " other " + (others === 1 ? "assignment" : "assignments") : " · this team only");
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "8px 0", borderBottom: "1px solid #f3f3f4" }}>
      <span onClick={onOpen} title="Open product details" style={{ cursor: "pointer", display: "inline-flex", flexShrink: 0 }}><CpThumb product={p} store={store} size={32} /></span>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div onClick={onOpen} title="Open product details"
          onMouseEnter={(e) => { e.currentTarget.style.textDecoration = "underline"; }}
          onMouseLeave={(e) => { e.currentTarget.style.textDecoration = "none"; }}
          style={{ fontSize: 12, fontWeight: 700, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap", cursor: "pointer" }}>{p.title}</div>
        <div style={{ marginTop: 1, fontSize: 10.5, color: "#9a9aa1", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{meta}</div>
      </div>
      <span style={{ flexShrink: 0, fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: s.color, background: rTint(s.color, 0.08), borderRadius: 4, padding: "3px 6px", textTransform: "uppercase", whiteSpace: "nowrap" }}>{s.label}</span>
    </div>);
}
function CpTeamPanel({ t, parent, products, store, onOpenDetail, onAddCatalog, onCustomize, onOpenRoster }) {
  const color = t.colorOverride || (parent && parent.color) || "#3f3f46";
  const direct = products.filter((p) => (p.rosters || []).includes(t.id));
  const via = parent ? products.filter((p) => { const r = p.rosters || []; return r.includes(parent.id) && !r.includes(t.id); }) : [];
  const n = direct.length + via.length;
  return (
    <React.Fragment>
      <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 20 }}>
        <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: rTint(color, 0.14), color, display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11 }}>{t.division || rInitials(t.name)}</span>
        <div style={{ minWidth: 0 }}>
          <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase" }}>Team</div>
          <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{t.name}</div>
        </div>
      </div>
      <CanvasPanelField label={"Products · " + n}>
        {n === 0 &&
          <div style={{ fontSize: 12, color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 8, padding: "10px 12px", lineHeight: 1.5 }}>
            No products assigned yet — buyers can't shop team gear for {t.name}. Add products below.
          </div>}
        {direct.map((p) => <CpPanelRow key={p.id} p={p} store={store} onOpen={() => onOpenDetail(p.id)} />)}
        {via.map((p) => <CpPanelRow key={p.id} p={p} store={store} via={parent.name} onOpen={() => onOpenDetail(p.id)} />)}
      </CanvasPanelField>
      <div style={{ display: "flex", flexDirection: "column", gap: 8, marginTop: 4 }}>
        <CpAddMenu fullWidth label="Add Products" scopeName={t.name} onNew={onCustomize} onCatalog={onAddCatalog} />
        <button type="button" onClick={onOpenRoster} style={{ width: "100%", boxSizing: "border-box", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "10px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
          <IconUsers size={14} /> Roster · {t.players || 0} {(t.players || 0) === 1 ? "player" : "players"}
        </button>
      </div>
    </React.Fragment>);
}
function CpDivisionPanel({ g, products, store, onOpenDetail, onAddCatalog, onCustomize, onAddTeam }) {
  const wide = products.filter((p) => (p.rosters || []).includes(g.id));
  const direct = (t) => products.filter((p) => (p.rosters || []).includes(t.id));
  return (
    <React.Fragment>
      <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 20 }}>
        <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: g.color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13 }}>{rInitials(g.name)}</span>
        <div style={{ minWidth: 0 }}>
          <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase" }}>Division</div>
          <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{g.name}</div>
        </div>
      </div>
      <CanvasPanelField label={"Assigned division-wide · " + wide.length}>
        {wide.length === 0
          ? <div style={{ fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>None yet. Division-wide products cover every team in {g.name} — including teams added later.</div>
          : <React.Fragment>
              {wide.map((p) => <CpPanelRow key={p.id} p={p} store={store} onOpen={() => onOpenDetail(p.id)} />)}
              <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Division-wide products auto-cover new teams.</div>
            </React.Fragment>}
      </CanvasPanelField>
      <CanvasPanelField label="Product coverage by team">
        {g.rosters.length === 0
          ? <div style={{ fontSize: 11.5, color: "#9a9aa1" }}>No teams yet — add a team below.</div>
          : <div style={{ border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden" }}>
              {g.rosters.map((t, i) => {
                const d = direct(t);
                const bare = d.length === 0 && wide.length === 0;
                return (
                  <div key={t.id} style={{ display: "flex", alignItems: "center", gap: 8, padding: "9px 12px", borderTop: i === 0 ? "none" : "1px solid #f3f3f4", background: bare ? "#fdf6e7" : "#fff" }}>
                    <span style={{ flex: 1, minWidth: 0, fontSize: 12, fontWeight: 700, color: bare ? "#8a5f10" : "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{t.name}</span>
                    {bare
                      ? <button type="button" onClick={() => onAddCatalog({ kind: "team", id: t.id, name: t.name })} style={{ flexShrink: 0, height: 26, padding: "0 10px", borderRadius: 6, border: "1px solid #efdcae", background: "#fff", fontSize: 11, fontWeight: 700, color: "#8a5f10", cursor: "pointer", fontFamily: "inherit" }}>+ Assign products</button>
                      : <span style={{ display: "inline-flex", alignItems: "center", gap: 4, flexShrink: 0 }}>
                          {d.slice(0, 3).map((p) => <CpChip key={p.id} product={p} store={store} size={20} title={p.title} onClick={() => onOpenDetail(p.id)} />)}
                          {d.length > 3 && <span style={{ fontSize: 10.5, fontWeight: 800, color: "#6b6b72" }}>+{d.length - 3}</span>}
                          {d.length === 0 && <span style={{ fontSize: 10.5, color: "#9a9aa1", fontStyle: "italic" }}>division-wide only</span>}
                        </span>}
                  </div>);
              })}
            </div>}
      </CanvasPanelField>
      <div style={{ display: "flex", flexDirection: "column", gap: 8, marginTop: 4 }}>
        <CpAddMenu fullWidth label="Add Products" scopeName={g.name} onNew={onCustomize} onCatalog={() => onAddCatalog({ kind: "parent", id: g.id, name: g.name })} />
        <button type="button" onClick={onAddTeam} style={{ width: "100%", boxSizing: "border-box", display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "10px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
          <IconPlus size={13} style={{ strokeWidth: 2.4 }} /> Add Team
        </button>
      </div>
    </React.Fragment>);
}
// Add from Catalog — docks the store catalog beside the canvas. Rows drag onto
// team/division nodes (grip), or click Assign to land on the launching scope.
function CpCatalogDrawer({ products, store, target, draggingId, onClose, onAssign, onDragStart, onCustomize }) {
  const [q, setQ] = React.useState("");
  const [filter, setFilter] = React.useState("all");
  const list0 = products.filter((p) => !p.isBundle);
  const unassignedN = list0.filter((p) => !(p.rosters || []).length).length;
  const isOn = (p) => !!target && (p.rosters || []).includes(target.id);
  let list = list0;
  const qq = q.trim().toLowerCase();
  if (qq) list = list.filter((p) => (p.title || "").toLowerCase().includes(qq));
  if (filter === "un") list = list.filter((p) => !(p.rosters || []).length);
  if (filter === "notOn") list = list.filter((p) => !isOn(p));
  const chip = (id, label) => (
    <span key={id} onClick={() => setFilter(id)} style={{ fontSize: 10.5, fontWeight: 700, background: filter === id ? "#0e0e10" : "#f1f1f3", color: filter === id ? "#fff" : "#6b6b72", borderRadius: 999, padding: "4px 10px", cursor: "pointer", whiteSpace: "nowrap" }}>{label}</span>);
  return (
    <div style={{ width: 320, flexShrink: 0, alignSelf: "flex-start", position: "sticky", top: 16, background: "#fff", border: "1px solid #ececf0", borderRadius: 12, padding: "16px 16px", boxShadow: "-8px 8px 28px rgba(20,20,30,.10)", boxSizing: "border-box" }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 8 }}>
        <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase" }}>Store catalog · {list0.length}</span>
        <button type="button" title="Close" onClick={onClose} style={{ width: 26, height: 26, borderRadius: 6, border: 0, background: "transparent", color: "#9a9aa1", cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center", padding: 0 }}>
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.4" strokeLinecap="round"><path d="M6 6l12 12M18 6L6 18" /></svg>
        </button>
      </div>
      <div style={{ marginTop: 6, fontSize: 11.5, color: "#6b6b72", lineHeight: 1.5 }}>
        Drag a product onto a team — or onto a division header to cover all its teams.{target ? <React.Fragment> Clicking <strong style={{ color: "#0e0e10" }}>Assign</strong> lands it on {target.name}.</React.Fragment> : null}
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, border: "1px solid #ececf0", borderRadius: 7, padding: "7px 10px", margin: "12px 0 8px" }}>
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="#9a9aa1" strokeWidth="2.2" strokeLinecap="round" style={{ flexShrink: 0 }}><circle cx="11" cy="11" r="7" /><path d="M21 21l-4.3-4.3" /></svg>
        <input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search products…" style={{ flex: 1, minWidth: 0, border: 0, outline: "none", fontSize: 12, fontFamily: "inherit", color: "#0e0e10", background: "transparent" }} />
      </div>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 12 }}>
        {chip("all", "All")}
        {chip("un", "Unassigned · " + unassignedN)}
        {target && chip("notOn", "Not on " + target.name)}
      </div>
      <div style={{ display: "flex", flexDirection: "column", gap: 8, maxHeight: 440, overflowY: "auto" }}>
        {list.length === 0 && <div style={{ padding: "18px 4px", fontSize: 12, color: "#9a9aa1", textAlign: "center" }}>No products match.</div>}
        {list.map((p) => {
          const on = isOn(p);
          const dragging = draggingId === p.id;
          const nAssign = (p.rosters || []).length;
          return (
            <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 9, border: "1px solid #ececf0", borderRadius: 8, padding: "8px 10px", background: "#fff", opacity: on && !dragging ? 0.55 : 1, boxShadow: dragging ? "0 6px 16px rgba(20,20,30,.14)" : "none" }}>
              <span title="Drag onto a team or division" onPointerDown={(e) => { if (e.button === 0) onDragStart(p, e); }} style={{ cursor: "grab", touchAction: "none", color: "#c0c0c6", display: "inline-flex", flexShrink: 0 }}>
                <svg width="10" height="14" viewBox="0 0 10 14" fill="currentColor"><circle cx="2.5" cy="2" r="1.4" /><circle cx="7.5" cy="2" r="1.4" /><circle cx="2.5" cy="7" r="1.4" /><circle cx="7.5" cy="7" r="1.4" /><circle cx="2.5" cy="12" r="1.4" /><circle cx="7.5" cy="12" r="1.4" /></svg>
              </span>
              <CpChip product={p} store={store} size={30} />
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 11.5, fontWeight: 700, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{p.title}</div>
                <div style={{ fontSize: 10, color: "#9a9aa1", marginTop: 1 }}>{p.teamSetting === "team-supplied" ? "Covered" : cpPrice(p, "adult")} · {nAssign ? nAssign + " " + (nAssign === 1 ? "assignment" : "assignments") : "unassigned"}{dragging ? " · dragging…" : ""}</div>
              </div>
              {target && (on
                ? <span style={{ flexShrink: 0, fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#f1f1f3", borderRadius: 4, padding: "3px 6px" }}>ASSIGNED</span>
                : <button type="button" onClick={() => onAssign(p)} style={{ flexShrink: 0, height: 24, padding: "0 9px", borderRadius: 5, border: "1px solid #d4d4d8", background: "#fff", fontSize: 10.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", color: "#0e0e10" }}>Assign</button>)}
            </div>);
        })}
        <button type="button" onClick={onCustomize} style={{ height: 34, borderRadius: 7, border: "1px dashed #c9c9cf", background: "transparent", fontSize: 12, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit", marginTop: 2 }}>+ Add New Product instead</button>
      </div>
    </div>);
}

function CanvasHandle({ side, active, title, onPointerDown }) {
  return (
    <span title={title} onPointerDown={onPointerDown} style={{
      position: "absolute",
      ...(side === "bottom" ? { bottom: -7, left: "50%", transform: "translateX(-50%)" } : { left: -7, top: "50%", transform: "translateY(-50%)" }),
      width: 14, height: 14, borderRadius: "50%", boxSizing: "border-box",
      background: active ? "#0e0e10" : "#fff", border: "2px solid #0e0e10",
      cursor: "crosshair", zIndex: 5, touchAction: "none",
    }} />);
}

function CanvasParentNode({ g, sel, drag, onSelect, onAddTeam, onHandleDown, nodeRefs, prodCount }) {
  const isSel = sel && sel.kind === "parent" && sel.id === g.id;
  const isTarget = drag && drag.over === "p:" + g.id;
  const canTarget = drag && (drag.from.kind === "team" || drag.from.kind === "prod");
  return (
    <div ref={(el) => { nodeRefs.current["p:" + g.id] = el; }} onClick={(e) => { e.stopPropagation(); onSelect(); }}
      style={{
        position: "relative", background: g.color, borderRadius: 10, padding: "14px 14px", cursor: "pointer",
        display: "flex", alignItems: "center", gap: 12, color: "#fff",
        boxShadow: isSel ? "0 0 0 2px #fff, 0 0 0 4px #0e0e10" : isTarget ? "0 0 0 2px #fff, 0 0 0 4px #1f9d55" : "0 1px 2px rgba(20,20,30,.08)",
        outline: canTarget && !isTarget ? "2px dashed rgba(14,14,16,.3)" : "none", outlineOffset: 4,
        transition: "box-shadow .12s",
      }}>
      <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: "rgba(255,255,255,.16)", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13 }}>{rInitials(g.name)}</span>
      <div style={{ minWidth: 0, flex: 1 }}>
        <div style={{ fontSize: 14, fontWeight: 800, lineHeight: 1.2, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{g.name}</div>
        <div style={{ marginTop: 3, fontSize: 10.5, fontWeight: 700, letterSpacing: ".06em", color: "rgba(255,255,255,.75)", textTransform: "uppercase", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{drag && drag.from.kind === "prod" ? "Drop = all " + g.rosters.length + " " + (g.rosters.length === 1 ? "team" : "teams") : prodCount != null ? g.rosters.length + " " + (g.rosters.length === 1 ? "team" : "teams") + " · " + prodCount + " prod" : "Division · " + g.rosters.length + " " + (g.rosters.length === 1 ? "team" : "teams")}</div>
      </div>
      <button title="Add team" onClick={(e) => { e.stopPropagation(); onAddTeam(); }} style={{ width: 28, height: 28, borderRadius: 7, border: 0, flexShrink: 0, background: "rgba(255,255,255,.18)", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", cursor: "pointer" }}><IconPlus size={14} style={{ strokeWidth: 2.6 }} /></button>
      <CanvasHandle side="bottom" active={drag && drag.from.kind === "parent" && drag.from.id === g.id} title="Drag to a team to connect it" onPointerDown={(e) => onHandleDown(e)} />
    </div>);
}

function CanvasTeamNode({ t, parent, rail, sel, drag, onSelect, onHandleDown, nodeRefs, bankCount, prods, store, onProdClick, onProdAdd }) {
  const isSel = sel && sel.kind === "team" && sel.id === t.id;
  const isTarget = drag && drag.over === "t:" + t.id;
  const color = t.colorOverride || (parent && parent.color) || "#3f3f46";
  return (
    <div style={{ display: "flex", alignItems: "stretch" }}>
      {rail &&
        <div style={{ width: 18, position: "relative", flexShrink: 0 }}>
          <span style={{ position: "absolute", left: 0, top: 0, bottom: 0, width: 2, background: "#d8d8de" }} />
          <span style={{ position: "absolute", left: 0, top: "50%", width: 18, height: 2, background: "#d8d8de", transform: "translateY(-1px)" }} />
        </div>}
      <div ref={(el) => { nodeRefs.current["t:" + t.id] = el; }} onClick={(e) => { e.stopPropagation(); onSelect(); }}
        style={{
          position: "relative", flex: 1, minWidth: 0, background: "#fff", border: "1px solid #ececf0", borderRadius: 10,
          padding: "10px 10px", margin: "5px 0", cursor: "pointer", display: "flex", flexDirection: "column",
          boxShadow: isSel ? "0 0 0 2px #0e0e10" : isTarget ? "0 0 0 2px #1f9d55" : "0 1px 2px rgba(20,20,30,.05)",
          transition: "box-shadow .12s",
        }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, minWidth: 0 }}>
        <span style={{ width: 32, height: 32, borderRadius: 8, flexShrink: 0, background: rTint(color, 0.12), color, display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 10.5 }}>{t.division || rInitials(t.name)}</span>
        <div style={{ minWidth: 0, flex: 1 }}>
          <div style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{t.name}</div>
          <div style={{ marginTop: 1, fontSize: 10.5, fontWeight: 600, color: "#9a9aa1", letterSpacing: ".04em" }}>{rTeamCode(t.id)} · {t.players || 0} players</div>
        </div>
        {bankCount > 0 &&
          <span title={bankCount + " proposed players waiting in the Roster Bank"} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 4, fontSize: 10, fontWeight: 800, color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "2px 7px" }}><IconBankInbox size={11} /> {bankCount}</span>}
        </div>
        {prods &&
          <div style={{ display: "flex", alignItems: "center", gap: 5, paddingTop: 7, marginTop: 8, borderTop: "1px solid #f3f3f4" }}>
            {prods.length === 0
              ? <span style={{ fontSize: 10, fontWeight: 700, color: "#6b6b72", background: "#f1f1f3", border: "1px solid #e4e4e7", borderRadius: 5, padding: "2px 7px", whiteSpace: "nowrap" }}>No products yet</span>
              : <React.Fragment>
                  {prods.slice(0, 4).map((p) => <CpChip key={p.id} product={p} store={store} title={p.title} onClick={() => onProdClick(p)} />)}
                  {prods.length > 4 &&
                    <span title={prods.slice(4).map((p) => p.title).join(", ")} style={{ height: 24, padding: "0 7px", borderRadius: 5, background: "#f7f7f9", border: "1px solid #ececf0", display: "inline-flex", alignItems: "center", fontSize: 10, fontWeight: 800, color: "#6b6b72", boxSizing: "border-box" }}>+{prods.length - 4}</span>}
                </React.Fragment>}
            <span style={{ flex: 1 }}></span>
            <button type="button" title={"Add product to " + t.name} onClick={(e) => { e.stopPropagation(); onProdAdd(); }} style={{ width: 24, height: 24, borderRadius: 5, border: "1px dashed #c9c9cf", background: "transparent", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center", cursor: "pointer", padding: 0 }}><IconPlus size={12} style={{ strokeWidth: 2.4 }} /></button>
          </div>}
        <CanvasHandle side="left" active={drag && drag.from.kind === "team" && drag.from.id === t.id} title={parent ? "Drag to another division to move this team" : "Drag to a division to connect"} onPointerDown={(e) => onHandleDown(e)} />
      </div>
    </div>);
}

function CanvasAddTeamTile({ onAdd, rail }) {
  return (
    <div style={{ display: "flex", alignItems: "stretch" }}>
      {rail &&
        <div style={{ width: 18, position: "relative", flexShrink: 0 }}>
          <span style={{ position: "absolute", left: 0, top: 0, bottom: "50%", width: 2, background: "#d8d8de" }} />
          <span style={{ position: "absolute", left: 0, top: "50%", width: 18, height: 2, background: "#d8d8de", transform: "translateY(-1px)" }} />
        </div>}
      <button onClick={onAdd} style={{
        flex: 1, margin: "5px 0", background: "transparent", border: "1px dashed #c9c9cf", borderRadius: 10,
        padding: "10px 12px", fontSize: 12, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit",
        display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8,
      }}><IconPlus size={13} style={{ strokeWidth: 2.4 }} /> Add Team</button>
    </div>);
}

function CanvasPanelField({ label, children }) {
  return (
    <div style={{ marginBottom: 18 }}>
      <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>{label}</div>
      {children}
    </div>);
}

function CanvasSwatches({ value, onPick, allowInherit, onInherit, inheritOn }) {
  return (
    <div style={{ display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center" }}>
      {allowInherit &&
        <button type="button" onClick={onInherit} title="Inherit division branding" style={{
          height: 28, padding: "0 10px", borderRadius: 6, cursor: "pointer", fontFamily: "inherit",
          fontSize: 11, fontWeight: 700, color: inheritOn ? "#fff" : "#0e0e10",
          background: inheritOn ? "#0e0e10" : "#fff", border: "1px solid " + (inheritOn ? "#0e0e10" : "#d4d4d8"),
        }}>Inherit</button>}
      {ROSTER_BRAND_COLORS.map((c) =>
        <button key={c} type="button" onClick={() => onPick(c)} title={c} style={{
          width: 28, height: 28, borderRadius: 6, background: c, cursor: "pointer",
          border: value === c ? "2px solid #0e0e10" : "2px solid transparent",
          outline: value === c ? "2px solid #fff" : "none", outlineOffset: -4,
          boxShadow: value === c ? "0 0 0 1px #0e0e10" : "0 0 0 1px rgba(0,0,0,.06)",
        }} />)}
    </div>);
}

// 5 — a team code is distributed two ways: read the code aloud, or send a link with
// the code already embedded. Both are readable text, each copies with its own toast.
function tcStoreSlug(store) {
  const st = store || (typeof window !== "undefined" ? window.__tcStore : null);
  const s = String((st && (st.storeTitle || st.teamName)) || "team-store").toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
  return s.slice(0, 28) || "team-store";
}
function teamCodeUrl(store, code) { return "prolook.com/s/" + tcStoreSlug(store) + "?team=" + code; }
// Copy confirmations are toasts named after what landed on the clipboard — a rep must
// never be unsure whether they copied the code or the link. Body-mounted so it works
// from row menus that unmount the moment they're clicked.
function tcCopy(text, msg) {
  try { navigator.clipboard?.writeText(text); } catch (e) {}
  try {
    const el = document.createElement("div");
    el.textContent = msg;
    el.setAttribute("style", "position:fixed;left:50%;bottom:32px;transform:translateX(-50%);z-index:4000;background:#0e0e10;color:#fff;border-radius:8px;padding:12px 18px;font:600 13px 'Inter',system-ui,sans-serif;box-shadow:0 12px 32px rgba(9,9,11,.28);pointer-events:none;");
    document.body.appendChild(el);
    setTimeout(() => { el.remove(); }, 2200);
  } catch (e) {}
}
function TeamCodeShare({ code, store, compact }) {
  const url = teamCodeUrl(store, code);
  const btn = (primary) => ({
    flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 6,
    background: primary ? "#0e0e10" : "#fff", color: primary ? "#fff" : "#0e0e10",
    border: primary ? "1px solid #0e0e10" : "1px solid #d4d4d8", borderRadius: 6,
    padding: compact ? "6px 9px" : "7px 11px", fontSize: 11.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap",
  });
  return (
    <div style={{ background: "#fafafb", border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 10, padding: compact ? "8px 8px 8px 11px" : "10px 10px 10px 12px" }}>
        <span style={{ flex: 1, minWidth: 0 }}>
          <span style={{ display: "block", fontSize: 9.5, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", marginBottom: 2 }}>CODE</span>
          <span style={{ display: "block", fontFamily: "JetBrains Mono, monospace", fontSize: 14, fontWeight: 700, letterSpacing: ".1em", color: "#0e0e10", userSelect: "all" }}>{code}</span>
        </span>
        <button type="button" onClick={() => tcCopy(code, "Team code " + code + " copied")} style={btn(false)}>
          <IconClipboard size={12} style={{ strokeWidth: 2 }} /> Copy code
        </button>
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 10, padding: compact ? "8px 8px 8px 11px" : "10px 10px 10px 12px", borderTop: "1px solid #ececf0", background: "#fff" }}>
        <span style={{ flex: 1, minWidth: 0 }}>
          <span style={{ display: "block", fontSize: 9.5, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", marginBottom: 2 }}>STORE LINK · CODE EMBEDDED</span>
          <span style={{ display: "block", fontFamily: "JetBrains Mono, monospace", fontSize: 11.5, color: "#3f3f46", userSelect: "all", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{url}</span>
        </span>
        <button type="button" onClick={() => tcCopy("https://" + url, "Store link for " + code + " copied")} style={btn(true)}>
          <IconLink size={12} style={{ strokeWidth: 2 }} /> Copy link
        </button>
      </div>
    </div>
  );
}

function CanvasCopyCode({ code, store }) {
  return <TeamCodeShare code={code} store={store} />;
}

function CanvasConfirmModal({ confirm, onClose, onConfirm }) {
  if (!confirm) return null;
  const danger = confirm.type === "delete-team" || confirm.type === "delete-parent" || confirm.type === "delete-parent-rehome";
  const blocked = confirm.type === "blocked-parent";
  const title = confirm.type === "reparent" ? `Move ${confirm.teamName}?`
    : confirm.type === "delete-team" ? "Delete team?"
    : confirm.type === "delete-parent" || confirm.type === "delete-parent-rehome" ? "Delete division?"
    : "Remove its teams first";
  const body = confirm.type === "reparent"
    ? `${confirm.teamName} will leave ${confirm.fromName} and connect to ${confirm.toName}, inheriting its branding. Players, orders, and freight routing move with it.`
    : confirm.type === "delete-team"
    ? `Are you sure you want to delete "${confirm.name}"? Its roster and edge are removed. This can't be undone.`
    : confirm.type === "delete-parent"
    ? `Delete the empty division "${confirm.name}"? This can't be undone.`
    : confirm.type === "delete-parent-rehome"
    ? `Deleting "${confirm.name}" won't delete its ${confirm.count} ${confirm.count === 1 ? "team" : "teams"} — ${confirm.count === 1 ? "it" : "they"} will be kept as standalone. This can't be undone.`
    : `"${confirm.name}" still has ${confirm.count} ${confirm.count === 1 ? "team" : "teams"} connected. Delete or move its teams to another division first.`;
  return (
    <Modal open onClose={onClose} width={460}>
      <div style={{ padding: "28px 28px 24px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
          <div style={{
            width: 44, height: 44, borderRadius: "50%", flexShrink: 0,
            background: danger ? "#fdecec" : "#f1f1f3", color: danger ? "#b81d14" : "#0e0e10",
            display: "inline-flex", alignItems: "center", justifyContent: "center"
          }}>
            {danger ? <IconTrash size={20} style={{ strokeWidth: 2 }} /> : <IconUsers size={20} />}
          </div>
          <div>
            <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>{title}</h2>
            <p style={{ margin: "8px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>{body}</p>
          </div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
          {!blocked &&
            <button onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>}
          <button onClick={blocked ? onClose : onConfirm} style={{
            background: danger ? "#b81d14" : "#0e0e10", border: "1px solid transparent", borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
            color: "#fff", cursor: "pointer", fontFamily: "inherit"
          }}>{blocked ? "Got it" : confirm.type === "reparent" ? "Move Team" : "Delete"}</button>
        </div>
      </div>
    </Modal>);
}

function TeamCanvasView({ groups, loose, org, setOrg, store, managerGated, onManageManagers, stats, onOpenRoster, setToast, wizard, onCustomize, onProductsChange }) {
  const [sel, setSel] = React.useState(null); // {kind:'parent'|'team', id}
  const [drag, setDrag] = React.useState(null); // {from:{kind,id}, x1,y1,x2,y2, over}
  const [confirm, setConfirm] = React.useState(null);
  // Product ↔ team association (league stores outside the wizard only).
  const showProds = !wizard && store && store.storeType === "league" && !!onProductsChange;
  const products = showProds ? (store.products || []) : [];
  const [panelTab, setPanelTab] = React.useState("setup");
  const [catalog, setCatalog] = React.useState(null); // {target:{kind:'team'|'parent',id,name}|null}
  const [detailPid, setDetailPid] = React.useState(null);
  const wrapRef = React.useRef(null);
  const nodeRefs = React.useRef({});
  const dragRef = React.useRef(null);
  React.useEffect(() => { dragRef.current = drag; }, [drag]);

  const setGroups = (fn) => setOrg((o) => ({ ...o, groups: typeof fn === "function" ? fn(o.groups) : fn }));
  const setLoose = (fn) => setOrg((o) => ({ ...o, loose: typeof fn === "function" ? fn(o.loose) : fn }));
  const findTeam = (tid) => {
    for (const g of groups) { const r = g.rosters.find((x) => x.id === tid); if (r) return { team: r, parent: g }; }
    const r = loose.find((x) => x.id === tid); return r ? { team: r, parent: null } : null;
  };
  const selParent = sel && sel.kind === "parent" ? groups.find((g) => g.id === sel.id) : null;
  const selTeam = sel && sel.kind === "team" ? findTeam(sel.id) : null;
  const selectNode = (s) => { setSel(s); setPanelTab("setup"); };
  const prodsFor = (tid, gid) => products.filter((p) => { const r = p.rosters || []; return r.includes(tid) || (gid && r.includes(gid)); });
  const divProdCount = (g) => products.filter((p) => { const r = p.rosters || []; return r.includes(g.id) || g.rosters.some((t) => r.includes(t.id)); }).length;
  const updateProduct = (pid, patch) => onProductsChange && onProductsChange({ products: (store.products || []).map((x) => x.id === pid ? { ...x, ...patch } : x) });
  function assignProduct(pid, key) {
    const p = products.find((x) => x.id === pid);
    if (!p) return;
    const id = key.slice(2);
    const cur = p.rosters || [];
    let label;
    if (key.startsWith("t:")) { const ft = findTeam(id); label = ft ? ft.team.name : "the team"; }
    else { const g = groups.find((x) => x.id === id); label = g ? g.name + " — covers all " + g.rosters.length + " " + (g.rosters.length === 1 ? "team" : "teams") : "the division"; }
    if (cur.includes(id)) { setToast(p.title + " is already assigned there"); return; }
    updateProduct(pid, { rosters: [...cur, id] });
    setToast(p.title + " assigned to " + label);
  }
  const openCatalog = (target) => {
    setCatalog({ target: target || null });
    if (target && target.kind === "team") setSel({ kind: "team", id: target.id });
    else if (target && target.kind === "parent") setSel({ kind: "parent", id: target.id });
  };
  // Store-wide bank-zone rollup: badge team nodes and list teams needing review.
  const bank = (org && org.bank) || {};
  const bankCount = (tid) => (bank[tid] || []).filter((c) => !c.committed).length;
  const bankRows = [];
  groups.forEach((g) => g.rosters.forEach((r) => { const n = bankCount(r.id); if (n) bankRows.push({ team: r, parent: g, n }); }));
  loose.forEach((r) => { const n = bankCount(r.id); if (n) bankRows.push({ team: r, parent: null, n }); });
  // Contextual scope: a selected team/division narrows the Roster Bank below the canvas.
  const bankScopeLabel = selTeam ? selTeam.team.name : selParent ? selParent.name : null;
  const scopedBankRows = selTeam ? bankRows.filter((b) => b.team.id === selTeam.team.id)
    : selParent ? bankRows.filter((b) => b.parent && b.parent.id === selParent.id)
    : bankRows;

  function addParent() {
    const used = groups.map((g) => g.color);
    const color = ROSTER_BRAND_COLORS.find((c) => !used.includes(c)) || ROSTER_BRAND_COLORS[groups.length % ROSTER_BRAND_COLORS.length];
    const g = { id: "grp-" + Date.now(), name: "New Division", sport: "Boys Basketball", color, rosters: [] };
    setGroups((cur) => [...cur, g]);
    setSel({ kind: "parent", id: g.id });
  }
  function addTeam(gid) {
    const t = { id: "ros-" + Date.now(), name: "New Team", players: 0 };
    if (gid) setGroups((cur) => cur.map((g) => g.id === gid ? { ...g, rosters: [...g.rosters, t] } : g));
    else setLoose((cur) => [...cur, t]);
    setSel({ kind: "team", id: t.id });
  }
  function patchParent(gid, patch) { setGroups((cur) => cur.map((g) => g.id === gid ? { ...g, ...patch } : g)); }
  function patchTeam(tid, patch) {
    setGroups((cur) => cur.map((g) => ({ ...g, rosters: g.rosters.map((r) => r.id === tid ? { ...r, ...patch } : r) })));
    setLoose((cur) => cur.map((r) => r.id === tid ? { ...r, ...patch } : r));
  }
  function moveTeam(tid, toGid) {
    setOrg((o) => {
      let team = o.loose.find((r) => r.id === tid) || null;
      const gs = o.groups.map((g) => {
        const hit = g.rosters.find((r) => r.id === tid);
        if (hit) team = hit;
        return { ...g, rosters: g.rosters.filter((r) => r.id !== tid) };
      });
      const ls = o.loose.filter((r) => r.id !== tid);
      if (!team) return o;
      const clean = { ...team };
      if (toGid) return { ...o, groups: gs.map((g) => g.id === toGid ? { ...g, rosters: [...g.rosters, clean] } : g), loose: ls };
      return { ...o, groups: gs, loose: [...ls, clean] };
    });
  }
  function deleteTeam(tid) {
    setOrg((o) => ({ ...o,
      groups: o.groups.map((g) => ({ ...g, rosters: g.rosters.filter((r) => r.id !== tid) })),
      loose: o.loose.filter((r) => r.id !== tid) }));
    setSel((s) => s && s.kind === "team" && s.id === tid ? null : s);
    setToast("Team deleted");
  }
  function deleteParent(gid) {
    setGroups((cur) => cur.filter((g) => g.id !== gid));
    setSel((s) => s && s.kind === "parent" && s.id === gid ? null : s);
    setToast("Division deleted");
  }
  function doConfirm() {
    const c = confirm; setConfirm(null);
    if (!c) return;
    if (c.type === "reparent") { moveTeam(c.tid, c.toGid); setToast(`Moved to ${c.toName}`); }
    else if (c.type === "delete-team") deleteTeam(c.tid);
    else if (c.type === "delete-parent") deleteParent(c.gid);
    else if (c.type === "delete-parent-rehome") {
      // Wizard context: deleting a division re-homes its teams as standalone.
      setOrg((o) => {
        const g = o.groups.find((x) => x.id === c.gid);
        const freed = g ? g.rosters.map((r) => ({ ...r })) : [];
        return { ...o, groups: o.groups.filter((x) => x.id !== c.gid), loose: [...o.loose, ...freed] };
      });
      setSel((s) => s && s.kind === "parent" && s.id === c.gid ? null : s);
      setToast(c.count ? "Division deleted — " + c.count + (c.count === 1 ? " team" : " teams") + " kept as standalone" : "Division deleted");
    }
  }

  // ── drag-to-connect ──
  const relPoint = (e) => {
    const el = wrapRef.current; if (!el) return { x: 0, y: 0 };
    const r = el.getBoundingClientRect();
    return { x: e.clientX - r.left + el.scrollLeft, y: e.clientY - r.top + el.scrollTop };
  };
  function startDrag(from, e) {
    e.preventDefault(); e.stopPropagation();
    const p = relPoint(e);
    setDrag({ from, x1: p.x, y1: p.y, x2: p.x, y2: p.y, over: null });
  }
  React.useEffect(() => {
    if (!drag) return;
    const move = (e) => {
      const d = dragRef.current; if (!d) return;
      const p = relPoint(e);
      let over = null;
      const wrap = wrapRef.current;
      Object.entries(nodeRefs.current).forEach(([key, el]) => {
        if (!el || !wrap) return;
        const from = d.from;
        let valid;
        if (from.kind === "prod") {
          valid = key.startsWith("t:") || key.startsWith("p:");
        } else if (from.kind === "parent") {
          const g = groups.find((x) => x.id === from.id);
          valid = key.startsWith("t:") && !(g && g.rosters.some((r) => "t:" + r.id === key));
        } else {
          const cur = findTeam(from.id);
          valid = key.startsWith("p:") && key !== "p:" + ((cur && cur.parent && cur.parent.id) || "");
        }
        if (!valid) return;
        const a = el.getBoundingClientRect(), b = wrap.getBoundingClientRect();
        const x = a.left - b.left + wrap.scrollLeft, y = a.top - b.top + wrap.scrollTop;
        if (p.x >= x && p.x <= x + a.width && p.y >= y && p.y <= y + a.height) over = key;
      });
      setDrag((dd) => dd ? { ...dd, x2: p.x, y2: p.y, over } : dd);
    };
    const up = () => {
      const d = dragRef.current;
      if (d && d.over) {
        if (d.from.kind === "prod") { assignProduct(d.from.id, d.over); setDrag(null); return; }
        let tid, toGid;
        if (d.from.kind === "parent") { tid = d.over.slice(2); toGid = d.from.id; }
        else { tid = d.from.id; toGid = d.over.slice(2); }
        const cur = findTeam(tid);
        const target = groups.find((g) => g.id === toGid) || {};
        if (cur && cur.parent && cur.parent.id !== toGid) {
          setConfirm({ type: "reparent", tid, teamName: cur.team.name, fromName: cur.parent.name, toGid, toName: target.name });
        } else if (cur && !cur.parent) {
          moveTeam(tid, toGid);
          setToast(`Connected to ${target.name}`);
        }
      }
      setDrag(null);
    };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
    return () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); };
  }, [!!drag]);

  const inputStyle = { width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "10px 12px", fontSize: 13.5, color: "#0e0e10", outline: "none", fontFamily: "inherit", background: "#fff" };
  const dangerBtn = { display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #f0c9c5", color: "#b81d14", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" };

  // ── side panel content ──
  let panel;
  if (selParent) {
    const g = selParent;
    const mode = ((org && org.modes) || {})[g.id] || "league";
    const mgrsSet = g.rosters.filter((r) => managerComplete(((org && org.managers) || {})[r.id])).length;
    panel = (
      <React.Fragment>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 20 }}>
          <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: g.color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13 }}>{rInitials(g.name)}</span>
          <div style={{ minWidth: 0 }}>
            <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase" }}>Division</div>
            <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{g.name}</div>
          </div>
        </div>
        <CanvasPanelField label="Division / Region Name">
          <input value={g.name} onChange={(e) => patchParent(g.id, { name: e.target.value })} style={inputStyle} />
        </CanvasPanelField>
        <CanvasPanelField label="Sport">
          <SelectField value={g.sport || ""} placeholder="Select a sport"
            onChange={(v) => patchParent(g.id, { sport: v })}
            options={WIZARD_SPORTS.map((s) => ({ value: s, label: s }))} />
        </CanvasPanelField>
        {/* No branding or codes during store creation — backend keys and brand
            styling have no place in setup; both are managed from My Team(s). */}
        {!wizard && <React.Fragment>
        <CanvasPanelField label="Brand Color">
          <CanvasSwatches value={g.color} onPick={(c) => patchParent(g.id, { color: c })} />
          <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Teams inherit this branding — override per team if needed.</div>
        </CanvasPanelField>
        <CanvasPanelField label="Division Code · optional">
          <CanvasCopyCode code={rTeamCode(g.id)} store={store} />
        </CanvasPanelField>
        </React.Fragment>}
        {managerGated &&
          <CanvasPanelField label="Order Fulfillment">
            <div style={{ fontSize: 12.5, color: "#4b4b52", lineHeight: 1.55, marginBottom: 10 }}>
              {mode === "teams"
                ? <React.Fragment><strong style={{ color: "#0e0e10" }}>Individual teams</strong> — {mgrsSet}/{g.rosters.length} team managers set.</React.Fragment>
                : <React.Fragment><strong style={{ color: "#0e0e10" }}>League-level</strong> — orders route to {leagueManagerName(store)}.</React.Fragment>}
            </div>
            {onManageManagers &&
              <button type="button" onClick={() => onManageManagers(g.id, null)} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
                <IconEdit size={13} style={{ strokeWidth: 2 }} /> Manage in Settings
              </button>}
          </CanvasPanelField>}
        <CanvasPanelField label={`Teams · ${g.rosters.length}`}>
          <Button variant="secondary" onClick={() => addTeam(g.id)}><IconPlus size={13} style={{ strokeWidth: 2.4 }} /> Add Team</Button>
        </CanvasPanelField>
        <div style={{ borderTop: "1px solid #ececf0", paddingTop: 16, marginTop: 4 }}>
          <button type="button" style={dangerBtn}
            onClick={() => g.rosters.length > 0
              ? (wizard
                ? setConfirm({ type: "delete-parent-rehome", gid: g.id, name: g.name, count: g.rosters.length })
                : setConfirm({ type: "blocked-parent", name: g.name, count: g.rosters.length }))
              : setConfirm({ type: "delete-parent", gid: g.id, name: g.name })}>
            <IconTrash size={13} style={{ strokeWidth: 2 }} /> Delete Division
          </button>
          {g.rosters.length > 0 && !wizard &&
            <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Blocked while teams are connected — delete or move them first.</div>}
          {g.rosters.length > 0 && wizard &&
            <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Its teams aren't deleted — they're kept as standalone teams.</div>}
        </div>
      </React.Fragment>);
  } else if (selTeam) {
    const t = selTeam.team, p = selTeam.parent;
    const color = t.colorOverride || (p && p.color) || "#3f3f46";
    const mode = p ? (((org && org.modes) || {})[p.id] || "league") : "league";
    const eff = managerGated ? effectiveManagerFor(org, store, p && p.id, t.id) : null;
    panel = (
      <React.Fragment>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 20 }}>
          <span style={{ width: 38, height: 38, borderRadius: 9, flexShrink: 0, background: rTint(color, 0.14), color, display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11 }}>{t.division || rInitials(t.name)}</span>
          <div style={{ minWidth: 0 }}>
            <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase" }}>Team</div>
            <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{t.name}</div>
          </div>
        </div>
        <CanvasPanelField label="Team Name">
          <input value={t.name} onChange={(e) => patchTeam(t.id, { name: e.target.value })} style={inputStyle} />
        </CanvasPanelField>
        <CanvasPanelField label="Division">
          <div style={{ fontSize: 13, fontWeight: 700, color: p ? "#0e0e10" : "#c2410c" }}>{p ? p.name : "Unassigned"}</div>
          <div style={{ marginTop: 4, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>{p ? "Drag this team's handle to another division to move it." : "Drag this team's handle to a division node to connect it."}</div>
        </CanvasPanelField>
        {!wizard && <React.Fragment>
        <CanvasPanelField label="Branding">
          <CanvasSwatches value={t.colorOverride || null} onPick={(c) => patchTeam(t.id, { colorOverride: c })}
            allowInherit={!!p} inheritOn={!t.colorOverride} onInherit={() => patchTeam(t.id, { colorOverride: null })} />
          {p && <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>{t.colorOverride ? "Overriding " + p.name + "'s branding." : "Inheriting " + p.name + "'s branding."}</div>}
        </CanvasPanelField>
        <CanvasPanelField label="Team Code">
          <CanvasCopyCode code={rTeamCode(t.id)} store={store} />
          <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Buyers enter this code on the storefront — or open the link, which scopes the store to this team with no typing.</div>
        </CanvasPanelField>
        </React.Fragment>}
        {managerGated && mode === "teams" &&
          <CanvasPanelField label="Team Manager">
            <div style={{ fontSize: 12.5, color: eff && eff.src === "team" ? "#0e0e10" : "#c2410c", fontWeight: 700 }}>
              {eff && eff.src === "team" ? eff.name : "Not assigned"}
            </div>
            {onManageManagers &&
              <button type="button" onClick={() => onManageManagers(p && p.id, t.id)} style={{ marginTop: 8, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
                <IconEdit size={13} style={{ strokeWidth: 2 }} /> Manage in Settings
              </button>}
          </CanvasPanelField>}
        {wizard
          ? <CanvasPanelField label="Roster">
              <div style={{ fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Rosters aren't entered during setup — they're optional and fill in later from the My Team(s) tab or from orders.</div>
            </CanvasPanelField>
          : <CanvasPanelField label={`Roster · ${t.players || 0} players`}>
              <Button variant="primary" onClick={() => onOpenRoster(p, t)}>Open Roster</Button>
              <div style={{ marginTop: 8, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Empty is normal — the roster can back-fill from team-gear orders.</div>
            </CanvasPanelField>}
        <div style={{ borderTop: "1px solid #ececf0", paddingTop: 16, marginTop: 4 }}>
          <button type="button" style={dangerBtn} onClick={() => setConfirm({ type: "delete-team", tid: t.id, name: t.name })}>
            <IconTrash size={13} style={{ strokeWidth: 2 }} /> Delete Team
          </button>
        </div>
      </React.Fragment>);
  } else {
    panel = (
      <React.Fragment>
        <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 16 }}>League Structure</div>
        {[["Divisions", stats.totalGroups], ["Teams", stats.totalRosters], ["Players", stats.totalPlayers]].map(([l, v]) =>
          <div key={l} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "10px 0", borderBottom: "1px solid #f3f3f4" }}>
            <span style={{ fontSize: 13, color: "#6b6b72", fontWeight: 600 }}>{l}</span>
            <span style={{ fontSize: 14, color: "#0e0e10", fontWeight: 800 }}>{v}</span>
          </div>)}
        <div style={{ margin: "16px 0 20px", fontSize: 12.5, color: "#6b6b72", lineHeight: 1.6 }}>
          Select a node to edit its settings. Drag a connection handle — the dot on a node's edge — to connect a team to a division.
        </div>
        <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
          <Button variant="primary" onClick={addParent}><IconPlus size={13} style={{ strokeWidth: 2.4 }} /> New Division</Button>
          <Button variant="secondary" onClick={() => addTeam(null)}><IconPlus size={13} style={{ strokeWidth: 2.4 }} /> New Team</Button>
        </div>
        <div style={{ marginTop: 20, border: "1px dashed #d8d8de", borderRadius: 8, padding: "12px 14px", fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.55, background: "#fafafb" }}>
          {wizard
            ? "This step is optional — Next is never blocked. Rosters aren't entered here; they fill in later from the My Team(s) tab or from orders."
            : "Rosters are optional. Launch with empty teams and let them fill in from team-gear orders — the Roster Bank collects checkout captures for review."}
        </div>
      </React.Fragment>);
  }

  // Products tab content for the side panel (league stores only).
  let panelProducts = null, prodTabCount = null;
  if (showProds && selParent) {
    prodTabCount = divProdCount(selParent);
    panelProducts = <CpDivisionPanel g={selParent} products={products} store={store}
      onOpenDetail={setDetailPid}
      onAddCatalog={(target) => openCatalog(target)}
      onCustomize={() => onCustomize && onCustomize()}
      onAddTeam={() => addTeam(selParent.id)} />;
  } else if (showProds && selTeam) {
    prodTabCount = prodsFor(selTeam.team.id, selTeam.parent && selTeam.parent.id).length;
    panelProducts = <CpTeamPanel t={selTeam.team} parent={selTeam.parent} products={products} store={store}
      onOpenDetail={setDetailPid}
      onAddCatalog={() => openCatalog({ kind: "team", id: selTeam.team.id, name: selTeam.team.name })}
      onCustomize={() => onCustomize && onCustomize()}
      onOpenRoster={() => onOpenRoster(selTeam.parent, selTeam.team)} />;
  }

  const empty = groups.length === 0 && loose.length === 0;
  const svgW = wrapRef.current ? wrapRef.current.scrollWidth : 0;
  const svgH = wrapRef.current ? wrapRef.current.scrollHeight : 0;

  return (
    <div style={{ paddingBottom: 100 }}>
      <div style={{ display: "flex", alignItems: "flex-start", gap: 16, marginBottom: 20 }}>
        <div style={{ flex: 1 }}>
          <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase" }}>{wizard ? "League Structure" : "My Teams"}</div>
          <div style={{ marginTop: 6, fontSize: 13.5, color: "#6b6b72" }}>
            {stats.totalGroups} {stats.totalGroups === 1 ? "division" : "divisions"} · {stats.totalRosters} {stats.totalRosters === 1 ? "team" : "teams"}{wizard ? null : <React.Fragment> · {stats.totalPlayers} players</React.Fragment>}
          </div>
        </div>
        <Button variant="secondary" onClick={() => addTeam(null)}><IconPlus size={14} style={{ strokeWidth: 2.4 }} /> New Team</Button>
        <Button variant="primary" onClick={addParent}><IconPlus size={14} style={{ strokeWidth: 2.4 }} /> New Division</Button>
      </div>
      <div style={{ display: "flex", gap: 20, alignItems: "stretch" }}>
        <div style={{ flex: 1, minWidth: 0 }}>
        <div ref={wrapRef} onClick={() => setSel(null)} style={{
          position: "relative", border: "1px solid #ececf0", borderRadius: 12,
          background: "#fbfbfc", backgroundImage: "radial-gradient(#e6e6ea 1px, transparent 1px)", backgroundSize: "20px 20px",
          padding: 24, overflowX: "auto", minHeight: 480, boxSizing: "border-box",
        }}>
          {empty ?
            <div style={{ padding: "110px 20px", textAlign: "center" }}>
              <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10" }}>Lay out your league</div>
              <div style={{ margin: "8px auto 20px", fontSize: 13, color: "#6b6b72", maxWidth: 400, lineHeight: 1.6 }}>
                {wizard
                  ? "Divisions group teams into age brackets, regions, or clubs; teams are what rosters and orders attach to. Setting them up now is optional — add divisions and teams anytime from the My Team(s) tab."
                  : "Add a division, hang teams beneath it, and connect them by dragging. Rosters are optional — teams can fill in from orders after launch. Team Required and Team Supplied products stay locked until at least one team or division exists."}
              </div>
              <Button variant="primary" onClick={addParent}><IconPlus size={14} style={{ strokeWidth: 2.4 }} /> New Division</Button>
            </div> :
            <div style={{ display: "flex", gap: 24, alignItems: "flex-start" }}>
              {groups.map((g) =>
                <div key={g.id} style={{ width: 230, flexShrink: 0 }}>
                  <CanvasParentNode g={g} sel={sel} drag={drag} nodeRefs={nodeRefs} prodCount={showProds ? divProdCount(g) : null}
                    onSelect={() => selectNode({ kind: "parent", id: g.id })}
                    onAddTeam={() => addTeam(g.id)}
                    onHandleDown={(e) => startDrag({ kind: "parent", id: g.id }, e)} />
                  <div style={{ paddingLeft: 14 }}>
                    {g.rosters.map((t) =>
                      <CanvasTeamNode key={t.id} t={t} parent={g} rail sel={sel} drag={drag} nodeRefs={nodeRefs} bankCount={bankCount(t.id)}
                        prods={showProds ? prodsFor(t.id, g.id) : null} store={store}
                        onProdClick={(p) => setDetailPid(p.id)}
                        onProdAdd={() => openCatalog({ kind: "team", id: t.id, name: t.name })}
                        onSelect={() => selectNode({ kind: "team", id: t.id })}
                        onHandleDown={(e) => startDrag({ kind: "team", id: t.id }, e)} />)}
                    <CanvasAddTeamTile rail={true} onAdd={() => addTeam(g.id)} />
                  </div>
                </div>)}
              {loose.length > 0 &&
                <div style={{ width: 230, flexShrink: 0 }}>
                  <div style={{ border: "1px dashed #c9c9cf", borderRadius: 12, padding: "12px 12px 8px", background: "rgba(255,255,255,.6)" }}>
                    <div style={{ fontSize: 10.5, fontWeight: 800, letterSpacing: ".07em", color: "#9a9aa1", textTransform: "uppercase", padding: "0 4px 4px" }}>Unassigned Teams</div>
                    {loose.map((t) =>
                      <CanvasTeamNode key={t.id} t={t} parent={null} rail={false} sel={sel} drag={drag} nodeRefs={nodeRefs} bankCount={bankCount(t.id)}
                        prods={showProds ? prodsFor(t.id, null) : null} store={store}
                        onProdClick={(p) => setDetailPid(p.id)}
                        onProdAdd={() => openCatalog({ kind: "team", id: t.id, name: t.name })}
                        onSelect={() => selectNode({ kind: "team", id: t.id })}
                        onHandleDown={(e) => startDrag({ kind: "team", id: t.id }, e)} />)}
                    <div style={{ fontSize: 11, color: "#b0b0b6", padding: "4px 4px 6px", lineHeight: 1.45 }}>Drag a team's handle onto a division to connect it.</div>
                  </div>
                </div>}
            </div>}
          {drag &&
            <svg style={{ position: "absolute", left: 0, top: 0, width: svgW, height: svgH, pointerEvents: "none", zIndex: 30 }}>
              <line x1={drag.x1} y1={drag.y1} x2={drag.x2} y2={drag.y2} stroke="#0e0e10" strokeWidth="2" strokeDasharray="7 5" strokeLinecap="round" />
              <circle cx={drag.x2} cy={drag.y2} r="5" fill={drag.over ? "#1f9d55" : "#0e0e10"} />
            </svg>}
        </div>
        {!wizard && (scopedBankRows.length > 0 || bankScopeLabel) &&
          <div style={{ marginTop: 16, background: "#fff", border: "1px solid #ececf0", borderRadius: 12, overflow: "hidden", boxShadow: "0 1px 2px rgba(20,20,30,.04)" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "14px 20px", borderBottom: "1px solid #f3f3f4" }}>
              <span style={{ width: 30, height: 30, borderRadius: 7, flexShrink: 0, background: "#fdf6e7", color: "#8a5f10", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconBankInbox size={15} /></span>
              <span style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".06em", textTransform: "uppercase", color: "#3f3f46" }}>Roster Bank · {bankScopeLabel || "Store-wide"}</span>
              <span style={{ fontSize: 12, color: "#9a9aa1" }}>{bankScopeLabel ? "Team-gear checkout captures for " + bankScopeLabel : "Team-gear checkout captures awaiting roster review"}</span>
            </div>
            {scopedBankRows.map(({ team, parent, n }, i) => (
              <div key={team.id} style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 20px", borderTop: i === 0 ? "none" : "1px solid #f3f3f4" }}>
                <span style={{ width: 32, height: 32, borderRadius: 8, flexShrink: 0, background: rTint(team.colorOverride || (parent && parent.color) || "#3f3f46", 0.12), color: team.colorOverride || (parent && parent.color) || "#3f3f46", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 10.5 }}>{team.division || rInitials(team.name)}</span>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{team.name}</div>
                  <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 1 }}>{parent ? parent.name : "Unassigned"} · {rTeamCode(team.id)}</div>
                </div>
                <span style={{ fontSize: 11.5, fontWeight: 800, color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "4px 10px" }}>{n} proposed {n === 1 ? "player" : "players"}</span>
                <button type="button" onClick={() => onOpenRoster(parent, team)} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Review</button>
              </div>))}
            {scopedBankRows.length === 0 &&
              <div style={{ padding: "24px 20px", textAlign: "center", fontSize: 12.5, color: "#9a9aa1" }}>The Roster Bank for {bankScopeLabel} is empty — team-gear checkout captures for this {selTeam ? "team" : "division"} will land here as orders come in.</div>}
          </div>}
        {!wizard && !bankScopeLabel && (bank["__store"] || []).some((c) => !c.committed) &&
          <div style={{ marginTop: 16 }}>
            <div style={{ display: "flex", alignItems: "flex-start", gap: 12, padding: "14px 18px", marginBottom: 12, background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 10 }}>
              <span style={{ color: "#8a5f10", flexShrink: 0, marginTop: 1 }}><IconBankInbox size={16} /></span>
              <div style={{ fontSize: 12.5, color: "#6b5514", lineHeight: 1.55 }}><strong style={{ color: "#54430e" }}>These captures have no team on file.</strong> Buyers checked out without a team code, so their orders wait store-wide. Fulfillment keeps working — add teams above whenever you want to start assigning them.</div>
            </div>
            <BankZone teamId="__store" teamName="this store — store-wide, no team on file" org={org} setOrg={setOrg} rosterNumbers={[]}
              commitDisabledReason="Add a team on the canvas first — captures commit onto a team roster" />
          </div>}
        </div>
        {catalog
          ? <CpCatalogDrawer products={products} store={store} target={catalog.target}
              draggingId={drag && drag.from.kind === "prod" ? drag.from.id : null}
              onClose={() => setCatalog(null)}
              onAssign={(p) => catalog.target && assignProduct(p.id, (catalog.target.kind === "team" ? "t:" : "p:") + catalog.target.id)}
              onDragStart={(p, e) => startDrag({ kind: "prod", id: p.id }, e)}
              onCustomize={() => { setCatalog(null); onCustomize && onCustomize(); }} />
          : <div style={{ width: 320, flexShrink: 0, alignSelf: "flex-start", position: "sticky", top: 16, background: "#fff", border: "1px solid #ececf0", borderRadius: 12, padding: "20px 20px", boxShadow: "0 1px 2px rgba(20,20,30,.04)", boxSizing: "border-box" }}>
              {panelProducts && <CpPanelTabs tab={panelTab} onChange={setPanelTab} count={prodTabCount} />}
              {panelTab === "products" && panelProducts ? panelProducts : panel}
            </div>}
      </div>
      {detailPid && (() => {
        // Product detail modal — the Products-tab detail page, hosted in a modal.
        const dp = products.find((x) => x.id === detailPid);
        if (!dp) return null;
        return (
          <Modal open onClose={() => setDetailPid(null)} width={Math.min(1140, (typeof window !== "undefined" ? window.innerWidth : 1200) - 64)}>
            <div style={{ maxHeight: "82vh", overflowY: "auto", padding: "12px 28px 28px", boxSizing: "border-box" }}>
              <ProductDetailPage mode="edit" product={dp} store={store}
                onBack={() => setDetailPid(null)}
                onSave={(u) => { onProductsChange({ products: (store.products || []).map((x) => x.id === u.id ? u : x) }); setDetailPid(null); setToast("Product updated"); }}
                onRemove={(id) => { onProductsChange({ products: (store.products || []).filter((x) => x.id !== id) }); setDetailPid(null); setToast("Product removed from the store"); }}
                onOpenEditor={() => setToast("Open the design editor from the Products tab")}
                onSendForApproval={() => setToast("Sent for approval")}
                onToast={setToast} />
            </div>
          </Modal>);
      })()}
      <CanvasConfirmModal confirm={confirm} onClose={() => setConfirm(null)} onConfirm={doConfirm} />
    </div>);
}

// ─────────── Breadcrumb + colored banner (shared by detail views) ───────────
function RosterCrumbs({ items }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 16, fontSize: 13.5, flexWrap: "wrap" }}>
      {items.map((it, i) => {
        const last = i === items.length - 1;
        return (
          <React.Fragment key={i}>
            {it.onClick && !last ?
              <button onClick={it.onClick} style={{ background: "transparent", border: 0, padding: 0, cursor: "pointer", color: "#6b6b72", fontWeight: 600, fontFamily: "inherit", fontSize: 13.5 }}>{it.label}</button> :
              <span style={{ color: "#0e0e10", fontWeight: 700 }}>{it.label}</span>}
            {!last && <IconChevronRight size={14} style={{ color: "#c0c0c6", strokeWidth: 2 }} />}
          </React.Fragment>);
      })}
    </div>);
}

function RosterBanner({ color, avatar, title, subtitle, right, onEdit, onDelete }) {
  return (
    <div style={{
      display: "flex", alignItems: "center", gap: 16,
      background: rTint(color, 0.08), borderLeft: `4px solid ${color}`,
      borderRadius: 10, padding: "24px 28px", marginBottom: 28
    }}>
      <div style={{
        width: 52, height: 52, borderRadius: 12, flexShrink: 0, background: color, color: "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center",
        fontWeight: 800, fontSize: avatar.length > 2 ? 15 : 17, letterSpacing: ".02em"
      }}>{avatar}</div>
      <div style={{ minWidth: 0, flex: 1 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
          <h1 style={{ margin: 0, fontSize: 20, fontWeight: 800, color: "#0e0e10" }}>{title}</h1>
        </div>
        <div style={{ marginTop: 4, fontSize: 13.5, color: "#6b6b72" }}>{subtitle}</div>
      </div>
      {right && <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10", flexShrink: 0 }}>{right}</div>}
      {onEdit &&
        <button onClick={onEdit} title="Edit group" style={{
          width: 32, height: 32, borderRadius: 4, flexShrink: 0, background: "transparent", border: 0,
          padding: 0, cursor: "pointer", color: "#6b6b72",
          display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}><IconEdit size={16} style={{ strokeWidth: 2 }} /></button>}
    </div>);
}

// ─────────── Home: groups grid + standalone rosters ───────────
function RosterHome({ groups, loose, stats, singleMode, org, store, managerGated, onManageManagers, onOpenGroup, onOpenRoster, onNewGroup, onNewRoster, onEditRoster, onDeleteRoster, onEditGroup, onDeleteGroup }) {
  const empty = groups.length === 0 && loose.length === 0;
  return (
    <div style={{ paddingBottom: 100 }}>
      {/* Page header */}
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 16, marginBottom: 24 }}>
        <div>
          <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase" }}>{singleMode ? "My Team" : "My Teams"}</div>
          <div style={{ marginTop: 8, fontSize: 13.5, color: "#6b6b72" }}>
            {singleMode
              ? `Single-team store · ${stats.totalPlayers} players`
              : `${stats.totalGroups} ${stats.totalGroups === 1 ? "group" : "groups"} · ${stats.totalRosters} ${stats.totalRosters === 1 ? "roster" : "rosters"} · ${stats.totalPlayers} players`}
          </div>
        </div>
        {singleMode ? (
          <span style={{ display: "inline-flex", alignItems: "center", gap: 8, flexShrink: 0, fontSize: 12, fontWeight: 700, color: "#6b6b72", background: "#f1f1f3", borderRadius: 999, padding: "8px 16px" }}>
            <IconUsers size={14} /> Single-Team Locker — one roster
          </span>
        ) : null}
      </div>

      {empty &&
        <div style={{ border: "1px dashed #dcdce1", borderRadius: 12, padding: "80px 20px", textAlign: "center" }}>
          <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>No rosters yet</div>
          <div style={{ marginTop: 8, fontSize: 13.5, color: "#6b6b72", maxWidth: 420, marginInline: "auto", lineHeight: 1.6 }}>
            Add divisions and teams on the canvas — or launch without a roster and let teams fill in from orders.
          </div>
        </div>}

      {/* Groups grid */}
      {groups.length > 0 &&
        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(300px, 1fr))", gap: 16 }}>
          {groups.map((g) => <RosterGroupCard key={g.id} group={g} onOpen={() => onOpenGroup(g)} onEdit={() => onEditGroup && onEditGroup(g)} onDelete={() => onDeleteGroup && onDeleteGroup(g)} />)}
        </div>}

      {/* Standalone rosters */}
      {loose.length > 0 &&
        <div style={{ marginTop: groups.length > 0 ? 40 : 0 }}>
          {!singleMode &&
          <div style={{ display: "flex", alignItems: "baseline", gap: 12, marginBottom: 16 }}>
            <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase" }}>Rosters</div>
            <div style={{ fontSize: 12.5, color: "#9a9aa1" }}>Not in a group</div>
          </div>}
          <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden" }}>
            {loose.map((r, i) =>
              <LooseRosterRow key={r.id} roster={r} first={i === 0}
                mgr={managerGated ? effectiveManagerFor(org, store, null, r.id) : null}
                onAssign={onManageManagers ? () => onManageManagers(null, r.id) : null}
                onOpen={() => onOpenRoster(r)}
                onEdit={() => onEditRoster(r)}
                onDelete={() => onDeleteRoster(r.id)} />)}
          </div>
        </div>}
      {!singleMode && (
        <FloatingActionBar>
          <Button variant="secondary" onClick={onNewRoster}>
            <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> New Roster
          </Button>
          <Button variant="primary" onClick={onNewGroup}>
            <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> New Roster Group
          </Button>
        </FloatingActionBar>
      )}
    </div>);
}

function RosterGroupCard({ group, onOpen, onEdit, onDelete }) {
  const [hover, setHover] = React.useState(false);
  const [menu, setMenu] = React.useState(false);
  const players = groupPlayerCount(group);
  return (
    <div onClick={onOpen}
      onMouseEnter={() => setHover(true)} onMouseLeave={() => { setHover(false); setMenu(false); }}
      style={{
        textAlign: "left", padding: 0, cursor: "pointer", fontFamily: "inherit",
        background: "#fff", border: "1px solid #ececf0", borderRadius: 12, overflow: "hidden",
        boxShadow: hover ? "0 10px 26px rgba(20,20,30,.12)" : "0 1px 2px rgba(20,20,30,.04)",
        transform: hover ? "translateY(-2px)" : "translateY(0)",
        transition: "box-shadow .16s ease, transform .16s ease", display: "flex", flexDirection: "column", position: "relative"
      }}>
      {/* Colored header */}
      <div style={{ background: group.color, padding: "20px 20px", display: "flex", alignItems: "center", gap: 16 }}>
        <div style={{
          width: 44, height: 44, borderRadius: 10, flexShrink: 0, background: "rgba(255,255,255,.16)",
          color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center",
          fontWeight: 800, fontSize: 15, letterSpacing: ".02em"
        }}>{rInitials(group.name)}</div>
        <div style={{ minWidth: 0, flex: 1 }}>
          <div style={{ fontSize: 17, fontWeight: 800, color: "#fff", lineHeight: 1.2 }}>{group.name}</div>
          <div style={{ marginTop: 4, fontSize: 11.5, fontWeight: 700, letterSpacing: ".08em", color: "rgba(255,255,255,.75)" }}>TEAM CODE {rTeamCode(group.id)}</div>
        </div>
        <div style={{ position: "relative", flexShrink: 0 }}>
          <div role="button" title="Group actions" onClick={(e) => { e.stopPropagation(); setMenu((m) => !m); }}
            style={{
              width: 32, height: 32, borderRadius: 6, cursor: "pointer", color: "#fff",
              background: menu ? "rgba(255,255,255,.2)" : "transparent",
              display: "inline-flex", alignItems: "center", justifyContent: "center"
            }}>
            <svg width={18} height={18} viewBox="0 0 24 24" fill="currentColor"><circle cx="5" cy="12" r="1.7" /><circle cx="12" cy="12" r="1.7" /><circle cx="19" cy="12" r="1.7" /></svg>
          </div>
          {menu &&
            <RosterGroupRowMenu
              code={rTeamCode(group.id)}
              onEdit={() => { setMenu(false); onEdit && onEdit(); }}
              onDelete={() => { setMenu(false); onDelete && onDelete(); }}
              onClose={() => setMenu(false)} />}
        </div>
      </div>
      {/* Division tags */}
      <div style={{ padding: "16px 20px 8px", display: "flex", flexWrap: "wrap", gap: 8, flex: 1 }}>
        {group.rosters.length === 0 ?
          <span style={{ fontSize: 12.5, color: "#c0c0c6", fontStyle: "italic" }}>No rosters yet</span> :
          group.rosters.map((r) =>
            <span key={r.id} style={{
              fontSize: 11.5, fontWeight: 700, letterSpacing: ".02em",
              color: group.color, background: rTint(group.color, 0.1),
              borderRadius: 6, padding: "4px 8px"
            }}>{r.name}</span>)}
      </div>
      {/* Footer */}
      <div style={{ padding: "12px 20px 16px", borderTop: "1px solid #f3f3f4", display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: 8 }}>
        <span style={{ fontSize: 12.5, color: "#6b6b72", fontWeight: 600 }}>
          {group.rosters.length} {group.rosters.length === 1 ? "roster" : "rosters"} · {players} players
        </span>
        <IconChevronRight size={16} style={{ color: "#c0c0c6", strokeWidth: 2 }} />
      </div>
    </div>);
}

function LooseRosterRow({ roster, first, mgr, onAssign, onOpen, onEdit, onDelete }) {
  const [hover, setHover] = React.useState(false);
  const [menu, setMenu] = React.useState(false);
  return (
    <div onMouseEnter={() => setHover(true)} onMouseLeave={() => { setHover(false); setMenu(false); }}
      style={{
        display: "flex", alignItems: "center", gap: 16, padding: "16px 20px",
        borderTop: first ? "none" : "1px solid #f3f3f4",
        background: hover ? "#fafafb" : "#fff", cursor: "pointer", position: "relative"
      }} onClick={onOpen}>
      <div style={{
        width: 40, height: 40, borderRadius: 10, flexShrink: 0, background: "#f1f1f3", color: "#6b6b72",
        display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13
      }}>{rInitials(roster.name)}</div>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>{roster.name}</div>
        <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 1, fontWeight: 600, letterSpacing: ".04em" }}>
          Team Code {rTeamCode(roster.id)}
          {mgr && <span style={{ letterSpacing: "normal", fontWeight: 400 }}>{" · "}
            {mgr.src === "league"
              ? "No team manager assigned"
              : <React.Fragment>Team manager <span style={{ color: "#0e0e10", fontWeight: 600 }}>{mgr.name}</span></React.Fragment>}
          </span>}
        </div>
      </div>
      <span style={{ fontSize: 13, color: "#6b6b72", fontWeight: 600 }}>{roster.players} players</span>
      <div style={{ position: "relative" }}>
        <button onClick={(e) => { e.stopPropagation(); setMenu((m) => !m); }} title="Roster actions" style={{
          width: 32, height: 32, borderRadius: 4, background: "transparent", border: 0, padding: 0,
          cursor: "pointer", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}>
          <svg width={18} height={18} viewBox="0 0 24 24" fill="currentColor"><circle cx="5" cy="12" r="1.7" /><circle cx="12" cy="12" r="1.7" /><circle cx="19" cy="12" r="1.7" /></svg>
        </button>
        {menu &&
          <RosterRowMenu
            code={rTeamCode(roster.id)}
            onEdit={() => { setMenu(false); onEdit(); }}
            onDelete={() => { setMenu(false); onDelete(); }}
            onClose={() => setMenu(false)} />}
      </div>
      <IconChevronRight size={16} style={{ color: "#c0c0c6", strokeWidth: 2 }} />
    </div>);
}

// ─────────── Group detail: banner + roster list + add roster ───────────
function RosterGroupDetail({ group, onHome, onOpenRoster, onAddRoster, onEdit, onDelete, onDeleteRoster, org, store, managerGated, onManageManagers }) {
  const players = groupPlayerCount(group);
  // Freight-mapping mode from Settings → Team Manager & Freight Mapping:
  // "group" shows the group-manager label/Assign; "teams" shows per-roster
  // team-manager labels instead.
  const mgrMode = managerGated ? (((org && org.modes) || {})[group.id] || "league") : null;
  return (
    <div style={{ paddingBottom: 100 }}>
      <RosterCrumbs items={[{ label: "Roster Groups", onClick: onHome }, { label: group.name }]} />

      {managerGated && (() => {
        const mode = mgrMode;
        if (mode === "teams") {
          const set = group.rosters.filter((r) => managerComplete(((org && org.managers) || {})[r.id])).length;
          return <RosterManagerStrip
            accent={`${set} of ${group.rosters.length} assigned`}
            accentColor={set >= group.rosters.length && group.rosters.length > 0 ? "#1f7a44" : "#c2410c"}
            note="Every roster needs its own team manager before pending orders for this group can be submitted."
            onManage={onManageManagers ? () => onManageManagers(group.id, null) : null} />;
        }
        if (mode === "league") {
          return <RosterManagerStrip main="Orders shipping to league manager"
            accent={leagueManagerName(store)}
            accentColor="#1f7a44"
            note="Set in Settings → Team Manager & Freight Mapping — bulk freight for this group routes to the league manager." />;
        }
        const eff = effectiveManagerFor(org, store, group.id, null);
        if (eff.src !== "group") {
          return <RosterManagerStrip main="Orders shipping to group manager"
            accent="Not assigned"
            accentColor="#c2410c"
            note="A group manager is required before pending orders for this group can be submitted."
            onManage={onManageManagers ? () => onManageManagers(group.id, null) : null} />;
        }
        return <RosterManagerStrip main="Orders shipping to group manager"
          accent={eff.name}
          accentColor="#1f7a44"
          note={`Applies to all ${group.rosters.length} teams in this group.`}
          onManage={onManageManagers ? () => onManageManagers(group.id, null) : null} />;
      })()}
      <RosterBanner
        color={group.color} avatar={rInitials(group.name)} title={group.name}
        subtitle={managerGated && mgrMode === "group" && effectiveManagerFor(org, store, group.id, null).src === "group"
          ? <React.Fragment>Team Code {rTeamCode(group.id)}{" · "}Group manager{" "}
              {(() => { const eff = effectiveManagerFor(org, store, group.id, null);
                return <span style={{ color: "#0e0e10", fontWeight: 600 }}>{eff.name}</span>; })()}
            </React.Fragment>
          : `Team Code ${rTeamCode(group.id)}`}
        right={`${players} players`} onEdit={onEdit} />

      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
        <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>
          {group.rosters.length} {group.rosters.length === 1 ? "Roster" : "Rosters"}
        </div>
        <Button variant="secondary" onClick={onAddRoster}>
          <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Add Roster
        </Button>
      </div>

      {group.rosters.length === 0 ?
        <div style={{ border: "1px dashed #dcdce1", borderRadius: 12, padding: "72px 20px", textAlign: "center" }}>
          <div style={{ fontSize: 14.5, fontWeight: 700, color: "#0e0e10" }}>No rosters in this group yet</div>
          <div style={{ marginTop: 8, fontSize: 13, color: "#6b6b72" }}>Add a roster and it will inherit {group.name}'s branding.</div>
        </div> :
        <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden" }}>
          {group.rosters.map((r, i) =>
            <GroupRosterRow key={r.id} roster={r} group={group} first={i === 0} onOpen={() => onOpenRoster(r)} onDelete={() => onDeleteRoster && onDeleteRoster(r)}
              mgr={managerGated && mgrMode === "teams" ? effectiveManagerFor(org, store, group.id, r.id) : null}
              onAssign={onManageManagers ? () => onManageManagers(group.id, r.id) : null} />)}
        </div>}
    </div>);
}

// Secondary label: team-manager assignment (Assign jumps to Settings → Team
// Manager & Freight Mapping with this roster's group already open).
function TeamManagerLabel({ mgr, onAssign }) {
  return (
    <React.Fragment>
      Team manager{" "}
      {mgr.src !== "team"
        ? <a href="#" onClick={(e) => { e.preventDefault(); e.stopPropagation(); onAssign && onAssign(); }}
            style={{ color: "#2563eb", fontWeight: 700, textDecoration: "none" }}>Assign</a>
        : <span style={{ color: "#0e0e10", fontWeight: 600 }}>{mgr.name}</span>}
    </React.Fragment>);
}

function GroupRosterRow({ roster, group, first, onOpen, onDelete, mgr, onAssign }) {
  const [hover, setHover] = React.useState(false);
  const [menu, setMenu] = React.useState(false);
  return (
    <div onClick={onOpen} onMouseEnter={() => setHover(true)} onMouseLeave={() => { setHover(false); setMenu(false); }}
      style={{
        display: "flex", alignItems: "center", gap: 16, padding: "16px 20px",
        borderTop: first ? "none" : "1px solid #f3f3f4",
        background: hover ? "#fafafb" : "#fff", cursor: "pointer", position: "relative"
      }}>
      <div style={{
        width: 40, height: 40, borderRadius: 10, flexShrink: 0, background: group.color, color: "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center",
        fontWeight: 800, fontSize: (roster.division || "").length > 2 ? 12 : 13, letterSpacing: ".02em"
      }}>{roster.division || rInitials(roster.name)}</div>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>{roster.name}</div>
        <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 1 }}>
          {mgr ? <TeamManagerLabel mgr={mgr} onAssign={onAssign} /> : <React.Fragment>{group.name} · {group.sport}</React.Fragment>}
        </div>
      </div>
      <span style={{ fontSize: 13, color: "#6b6b72", fontWeight: 600 }}>{roster.players} players</span>
      <div style={{ position: "relative" }}>
        <button onClick={(e) => { e.stopPropagation(); setMenu((m) => !m); }} title="Roster actions" style={{
          width: 32, height: 32, borderRadius: 4, background: "transparent", border: 0, padding: 0,
          cursor: "pointer", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}>
          <svg width={18} height={18} viewBox="0 0 24 24" fill="currentColor"><circle cx="5" cy="12" r="1.7" /><circle cx="12" cy="12" r="1.7" /><circle cx="19" cy="12" r="1.7" /></svg>
        </button>
        {menu &&
          <RosterGroupRowMenu
            code={rTeamCode(roster.id)}
            deleteLabel="Delete Roster"
            onDelete={() => { setMenu(false); onDelete && onDelete(); }}
            onClose={() => setMenu(false)} />}
      </div>
      <IconChevronRight size={16} style={{ color: "#c0c0c6", strokeWidth: 2 }} />
    </div>);
}

// ─────────── Roster modals ───────────
function RosterRowMenu({ onEdit, onDelete, onClose, code, store, onMove }) {
  React.useEffect(() => {
    const handler = () => onClose();
    const t = setTimeout(() => document.addEventListener("click", handler), 0);
    return () => { clearTimeout(t); document.removeEventListener("click", handler); };
  }, [onClose]);
  return (
    <div onClick={(e) => e.stopPropagation()} style={{
      position: "absolute", right: 0, top: 36,
      background: "#fff", border: "1px solid #ececf0",
      borderRadius: 6, boxShadow: "0 10px 28px rgba(0,0,0,.10)",
      minWidth: 186, zIndex: 20, overflow: "hidden"
    }}>
      <MenuItem onClick={onEdit} icon={<IconEdit size={14} />}>Rename</MenuItem>
      {code && <MenuItem onClick={() => { onClose(); tcCopy(code, "Team code " + code + " copied"); }} icon={<IconClipboard size={14} />}>Copy team code</MenuItem>}
      {code && <MenuItem onClick={() => { onClose(); tcCopy("https://" + teamCodeUrl(store, code), "Store link for " + code + " copied"); }} icon={<IconLink size={14} />}>Copy store link</MenuItem>}
      <MenuItem onClick={onDelete} icon={<IconTrash size={14} />} danger>Delete Roster</MenuItem>
    </div>);
}

// Menu for roster-group cards (Rename + Delete) and in-group roster rows (Delete only).
function RosterGroupRowMenu({ onEdit, onDelete, onClose, deleteLabel, code, store }) {
  React.useEffect(() => {
    const handler = () => onClose();
    const t = setTimeout(() => document.addEventListener("click", handler), 0);
    return () => { clearTimeout(t); document.removeEventListener("click", handler); };
  }, [onClose]);
  return (
    <div onClick={(e) => e.stopPropagation()} style={{
      position: "absolute", right: 0, top: 36,
      background: "#fff", border: "1px solid #ececf0",
      borderRadius: 6, boxShadow: "0 10px 28px rgba(0,0,0,.10)",
      minWidth: 172, zIndex: 40, overflow: "hidden"
    }}>
      {onEdit && <MenuItem onClick={onEdit} icon={<IconEdit size={14} />}>Edit Group</MenuItem>}
      {code && <MenuItem onClick={() => { onClose(); tcCopy(code, "Team code " + code + " copied"); }} icon={<IconClipboard size={14} />}>Copy team code</MenuItem>}
      {code && <MenuItem onClick={() => { onClose(); tcCopy("https://" + teamCodeUrl(store, code), "Store link for " + code + " copied"); }} icon={<IconLink size={14} />}>Copy store link</MenuItem>}
      <MenuItem onClick={onDelete} icon={<IconTrash size={14} />} danger>{deleteLabel || "Delete Group"}</MenuItem>
    </div>);
}

// Confirm-delete dialog for roster groups and in-group rosters.
function RosterConfirmDeleteModal({ confirm, onClose, onConfirm }) {
  const isGroup = confirm?.kind === "group";
  const n = confirm?.rosterCount || 0;
  const title = isGroup ? "Delete roster group?" : "Delete roster?";
  const body = isGroup
    ? (n > 0
        ? `Deleting "${confirm?.name}" will not delete its ${n} ${n === 1 ? "roster" : "rosters"} — ${n === 1 ? "it" : "they"} will be kept as standalone ${n === 1 ? "roster" : "rosters"}. This can't be undone.`
        : `Delete the empty group "${confirm?.name}"? This can't be undone.`)
    : `Are you sure you want to delete "${confirm?.name}"? This action can't be undone.`;
  return (
    <Modal open={!!confirm} onClose={onClose} width={460}>
      <div style={{ padding: "28px 28px 24px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
          <div style={{
            width: 44, height: 44, borderRadius: "50%", flexShrink: 0,
            background: "#fdecec", color: "#b81d14",
            display: "inline-flex", alignItems: "center", justifyContent: "center"
          }}>
            <IconTrash size={20} style={{ strokeWidth: 2 }} />
          </div>
          <div>
            <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>{title}</h2>
            <p style={{ margin: "8px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>{body}</p>
          </div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
          <button onClick={onClose} style={{
            background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
            color: "#0e0e10", cursor: "pointer", fontFamily: "inherit"
          }}>Cancel</button>
          <button onClick={onConfirm} style={{
            background: "#b81d14", border: "1px solid #b81d14", borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
            color: "#fff", cursor: "pointer", fontFamily: "inherit"
          }}>Delete</button>
        </div>
      </div>
    </Modal>);
}

function RModalInput({ value, onChange, placeholder, autoFocus, onEnter }) {
  const [focus, setFocus] = React.useState(false);
  return (
    <input value={value} autoFocus={autoFocus}
      onChange={(e) => onChange(e.target.value)}
      onFocus={() => setFocus(true)} onBlur={() => setFocus(false)}
      onKeyDown={(e) => { if (e.key === "Enter" && onEnter) onEnter(); }}
      placeholder={placeholder}
      style={{
        width: "100%", boxSizing: "border-box", padding: "12px 16px", fontSize: 14,
        border: `1px solid ${focus ? "#0e0e10" : "#d4d4d8"}`, borderRadius: 8,
        outline: "none", fontFamily: "inherit", color: "#0e0e10", background: "#fff"
      }} />);
}

function RModalSelect({ value, onChange, placeholder, options }) {
  const [focus, setFocus] = React.useState(false);
  return (
    <div style={{ position: "relative" }}>
      <select value={value} onChange={(e) => onChange(e.target.value)}
        onFocus={() => setFocus(true)} onBlur={() => setFocus(false)}
        style={{
          width: "100%", boxSizing: "border-box", padding: "12px 40px 12px 16px", fontSize: 14,
          border: `1px solid ${focus ? "#0e0e10" : "#d4d4d8"}`, borderRadius: 8,
          outline: "none", fontFamily: "inherit", color: value ? "#0e0e10" : "#9a9aa1",
          background: "#fff", appearance: "none", WebkitAppearance: "none", cursor: "pointer"
        }}>
        <option value="">{placeholder}</option>
        {options.map((o) => <option key={o.value} value={o.value} style={{ color: "#0e0e10" }}>{o.label}</option>)}
      </select>
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#6b6b72" strokeWidth="2.4"
        strokeLinecap="round" strokeLinejoin="round"
        style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)", pointerEvents: "none" }}>
        <polyline points="6 9 12 15 18 9" />
      </svg>
    </div>);
}

function RModalFooter({ onCancel, onConfirm, confirmLabel, disabled }) {
  return (
    <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
      <button onClick={onCancel} style={{
        background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
        padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
        color: "#0e0e10", cursor: "pointer", fontFamily: "inherit"
      }}>Cancel</button>
      <button onClick={disabled ? undefined : onConfirm} disabled={disabled} style={{
        background: disabled ? "#d4d4d8" : "#0e0e10", border: "1px solid transparent", borderRadius: 6,
        padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
        color: "#fff", cursor: disabled ? "not-allowed" : "pointer", fontFamily: "inherit"
      }}>{confirmLabel}</button>
    </div>);
}

function RosterGroupModal({ open, mode, initial, org, store, managerGated, onClose, onSave, onDelete }) {
  const editing = mode === "edit";
  const [name, setName] = React.useState("");
  const [color, setColor] = React.useState(ROSTER_BRAND_COLORS[0]);
  const [mgrMode, setMgrMode] = React.useState("league");
  const [mgrDraft, setMgrDraft] = React.useState({});
  const [copied, setCopied] = React.useState(false);
  React.useEffect(() => { if (!open) setCopied(false); }, [open]);
  React.useEffect(() => {
    if (!open) return;
    setName(initial?.name || ""); setColor(initial?.color || ROSTER_BRAND_COLORS[0]);
    const gm = ((org && org.managers) || {})[initial?.id] || {};
    const m = ((org && org.modes) || {})[initial?.id] || "league";
    setMgrMode(m);
    setMgrDraft({ ...gm });
  }, [open]);
  const showManager = editing && managerGated && org;
  const mgrErrs = showManager && mgrMode === "group" ? managerFieldErrors(mgrDraft) : {};
  // A group-manager entry may be left blank (submission is gated later), but a
  // partial entry must be completed — all five fields, valid email/phone.
  const mgrTouched = ["firstName", "lastName", "email", "phone", "address"].some((k) => String(mgrDraft[k] || "").trim());
  const canSave = name.trim().length > 0 && !(showManager && mgrMode === "group" && mgrTouched && Object.keys(mgrErrs).length > 0);
  function confirm() {
    if (!canSave) return;
    onSave({
      mode: editing ? "edit" : "create", id: initial?.id, name: name.trim(), color,
      manager: showManager ? { mode: mgrMode, entry: mgrMode === "group" && mgrTouched ? mgrDraft : null } : null
    });
  }
  const teamCount = (initial?.rosters || []).length;
  function MgrRadio({ value, label, note }) {
    const on = mgrMode === value;
    return (
      <button type="button" onClick={() => setMgrMode(value)} style={{
        display: "flex", alignItems: "flex-start", gap: 12, width: "100%", textAlign: "left",
        background: on ? "#fafafb" : "#fff", border: `1px solid ${on ? "#0e0e10" : "#e4e4e7"}`,
        borderRadius: 8, padding: "12px 12px", cursor: "pointer", fontFamily: "inherit"
      }}>
        <span style={{
          width: 20, height: 20, borderRadius: 999, flexShrink: 0, marginTop: 1, boxSizing: "border-box",
          border: `2px solid ${on ? "#0e0e10" : "#c0c0c6"}`, display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}>{on && <span style={{ width: 8, height: 8, borderRadius: 999, background: "#0e0e10" }} />}</span>
        <span style={{ minWidth: 0 }}>
          <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>{label}</span>
          <span style={{ display: "block", marginTop: 2, fontSize: 12, color: "#6b6b72", lineHeight: 1.5 }}>{note}</span>
        </span>
      </button>);
  }
  return (
    <Modal open={open} onClose={onClose} width={editing ? 520 : 480}>
      <div style={{ padding: "28px 28px 24px", maxHeight: "84vh", overflowY: "auto", boxSizing: "border-box" }}>
        <h2 style={{ margin: 0, fontSize: 20, fontWeight: 800 }}>{editing ? "Edit Roster Group" : "New Roster Group"}</h2>
        <p style={{ margin: "8px 0 24px", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
          {editing ? "Rename the group, change its brand color, or update who manages it." : "Groups collect related rosters — like a city or club — under one set of branding."}
        </p>
        {editing &&
          <div style={{ marginBottom: 20 }}>
            <div style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>Team Code</div>
            <TeamCodeShare code={rTeamCode(initial?.id)} store={store} />
          </div>}
        <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>Group Name</label>
        <RModalInput value={name} onChange={setName} placeholder="e.g. Spanish Fork" autoFocus={!editing} onEnter={confirm} />
        <div style={{ marginTop: 24 }}>
          <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 12 }}>Brand Color</label>
          <div style={{ display: "flex", flexWrap: "wrap", gap: 12, alignItems: "center" }}>
            {ROSTER_BRAND_COLORS.map((c) =>
              <button key={c} onClick={() => setColor(c)} title={c} style={{
                width: 36, height: 36, borderRadius: 8, background: c, cursor: "pointer",
                border: color === c ? "2px solid #0e0e10" : "2px solid transparent",
                outline: color === c ? "2px solid #fff" : "none", outlineOffset: -4,
                boxShadow: color === c ? "0 0 0 1px #0e0e10" : "0 0 0 1px rgba(0,0,0,.06)"
              }} />)}
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginLeft: "auto" }}>
              <span style={{ fontSize: 12, color: "#9a9aa1", fontWeight: 600 }}>Preview</span>
              <div style={{
                width: 36, height: 36, borderRadius: 8, background: color, color: "#fff",
                display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 12
              }}>{rInitials(name) === "?" ? "AB" : rInitials(name)}</div>
            </div>
          </div>
        </div>
        {showManager &&
          <div style={{ marginTop: 24 }}>
            <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 12 }}>Group Manager</label>
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              <MgrRadio value="league" label={`League manager — ${leagueManagerName(store)}`} note="Bulk freight and roster questions route to the league manager." />
              <MgrRadio value="group" label="Group manager" note={`One person manages all ${teamCount || ""} teams in this group. Required before pending orders can be submitted.`} />
              <MgrRadio value="teams" label="Individual teams" note="Each roster has its own manager — every one must be assigned before pending orders can be submitted." />
            </div>
            {mgrMode === "group" &&
              <div style={{ marginTop: 12 }}>
                <ManagerFieldsGrid value={mgrDraft} errors={mgrTouched ? mgrErrs : {}} onChange={setMgrDraft} />
              </div>}
          </div>}
        {editing && onDelete &&
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, marginTop: 24, paddingTop: 20, borderTop: "1px solid #ececf0" }}>
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Delete this group</div>
              <div style={{ marginTop: 2, fontSize: 12, color: "#6b6b72" }}>Its rosters are kept as standalone rosters.</div>
            </div>
            <button type="button" onClick={onDelete} style={{
              flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff",
              border: "1px solid #f0c9c5", color: "#b81d14", borderRadius: 6,
              padding: "8px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit"
            }}><IconTrash size={13} style={{ strokeWidth: 2 }} /> Delete Group</button>
          </div>}
        <RModalFooter onCancel={onClose} onConfirm={confirm} confirmLabel={editing ? "Save Changes" : "Create Group"} disabled={!canSave} />
      </div>
    </Modal>);
}

// ── 6 — Roster naming assist. Reps name teams by coach, not division. Three
// approaches sit side by side in the demo so the trade-off is visible: a passive
// suggestion, an auto-composed editable preview, and a store-wide pattern. ──
function rosterNaming() {
  window.__rosterNaming = window.__rosterNaming || { mode: "chip", pattern: "coachPrefix", auto: true };
  return window.__rosterNaming;
}
const NAME_PATTERNS = [
  { key: "coachPrefix", label: "Coach {coach} {division}", build: (p) => [p.coach ? "Coach " + p.coach : "", p.division].filter(Boolean).join(" ") },
  { key: "coachPlain", label: "{coach} {division}", build: (p) => [p.coach, p.division].filter(Boolean).join(" ") },
  { key: "divisionFirst", label: "{division} \u2014 Coach {coach}", build: (p) => [p.division, p.coach ? "Coach " + p.coach : ""].filter(Boolean).join(" \u2014 ") },
  { key: "groupDivision", label: "{group} {division}", build: (p) => [p.group, p.division].filter(Boolean).join(" ") },
];
const NAME_MODES = [
  { key: "chip", label: "Suggestion chip", note: "Least intrusive \u2014 the rep types whatever they want and can accept the suggestion or ignore it. Nothing is imposed; consistency isn\u2019t guaranteed." },
  { key: "compose", label: "Auto-compose", note: "The name is built from parts and stays editable. Fastest for one-off teams; each rep can still compose differently." },
  { key: "pattern", label: "Store pattern", note: "Set once per store and every new roster inherits it \u2014 consistent across a league, but a one-off team has to be overridden by hand." },
];
function RosterNameAssist({ store, org, groupId, groupName, rosterId, division, onDivision, value, onChange }) {
  const cfg = rosterNaming();
  const [mode, setMode] = React.useState(cfg.mode);
  const [pattern, setPattern] = React.useState(cfg.pattern);
  const [auto, setAuto] = React.useState(cfg.auto);
  const [parts, setParts] = React.useState({ coach: true, division: true, sport: false, group: false });
  const [localDiv, setLocalDiv] = React.useState("");
  const [coach, setCoach] = React.useState("");
  const [dismissed, setDismissed] = React.useState(false);
  const mapped = React.useMemo(() => {
    try {
      const eff = typeof effectiveManagerFor === "function" ? effectiveManagerFor(org, store, groupId || null, rosterId || null) : null;
      return eff && eff.src !== "league" && eff.name ? eff.name : "";
    } catch (e) { return ""; }
  }, [org, store, groupId, rosterId]);
  const coachName = (coach || mapped || "").trim();
  const div = (division != null ? division : localDiv) || "";
  const sport = (store && store.sport) || (org && org.groups || []).find((g) => g.id === groupId)?.sport || "";
  const p = { coach: coachName, division: div, sport, group: groupName || "" };
  const composed = [parts.coach && coachName ? "Coach " + coachName : "", parts.division ? div : "", parts.sport ? sport : "", parts.group ? p.group : ""].filter(Boolean).join(" ").trim();
  const patterned = (NAME_PATTERNS.find((x) => x.key === pattern) || NAME_PATTERNS[0]).build(p);
  const suggestion = mode === "compose" ? composed : mode === "pattern" ? patterned : (coachName ? "Coach " + coachName + (div ? " " + div : "") : div);
  const activeNote = (NAME_MODES.find((m) => m.key === mode) || NAME_MODES[0]).note;
  const apply = () => { onChange(suggestion); setDismissed(false); };
  const setModePersist = (k) => { setMode(k); cfg.mode = k; };
  const field = (label, val, set, ph) => (
    <label style={{ display: "block", flex: "1 1 130px", minWidth: 0 }}>
      <span style={{ display: "block", fontSize: 9.5, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", marginBottom: 4 }}>{label}</span>
      <input value={val} onChange={(e) => set(e.target.value)} placeholder={ph}
        style={{ width: "100%", boxSizing: "border-box", height: 34, border: "1px solid #e4e4e7", borderRadius: 6, padding: "0 9px", fontSize: 12.5, fontFamily: "inherit", color: "#0e0e10", outline: "none" }} />
    </label>
  );
  return (
    <div style={{ marginTop: 12, background: "#fafafb", border: "1px solid #ececf0", borderRadius: 9, padding: "12px 13px" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 9, flexWrap: "wrap" }}>
        <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".07em", color: "#0e0e10" }}>NAMING ASSIST</span>
        <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "2px 7px" }}>3 APPROACHES · FOR REVIEW</span>
        <div style={{ marginLeft: "auto", display: "inline-flex", background: "#f1f1f3", borderRadius: 7, padding: 3 }}>
          {NAME_MODES.map((m) => {
            const on = mode === m.key;
            return <button key={m.key} type="button" onClick={() => setModePersist(m.key)} style={{ padding: "5px 9px", border: 0, borderRadius: 5, cursor: "pointer", fontSize: 10.5, fontWeight: 700, fontFamily: "inherit", background: on ? "#fff" : "transparent", color: on ? "#0e0e10" : "#6b6b72", boxShadow: on ? "0 1px 2px rgba(20,20,30,.12)" : "none" }}>{m.label}</button>;
          })}
        </div>
      </div>
      <div style={{ display: "flex", gap: 9, flexWrap: "wrap", alignItems: "flex-end", marginBottom: 10 }}>
        {field("COACH / TEAM MANAGER", coach || mapped, setCoach, "e.g. Johnson")}
        {division == null && field("DIVISION / AGE", localDiv, setLocalDiv, "e.g. 12U")}
      </div>
      <div style={{ fontSize: 11, color: mapped ? "#1f7a44" : "#9a9aa1", marginTop: -4, marginBottom: 10, lineHeight: 1.45 }}>
        {mapped ? "Pulled from manager mapping \u2014 edit here without changing the mapping." : "No team manager assigned yet \u2014 type the coach\u2019s name, or name the team without it."}
      </div>

      {mode === "compose" &&
        <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 10 }}>
          {[{ k: "coach", l: "Coach name" }, { k: "division", l: "Division" }, { k: "sport", l: "Sport" }, { k: "group", l: "Group" }].map((o) => {
            const on = !!parts[o.k];
            const empty = !p[o.k];
            return <button key={o.k} type="button" disabled={empty} onClick={() => setParts((s) => ({ ...s, [o.k]: !s[o.k] }))}
              style={{ background: on && !empty ? "#0e0e10" : "#fff", color: on && !empty ? "#fff" : empty ? "#c4c4cb" : "#3f3f46", border: "1px solid " + (on && !empty ? "#0e0e10" : "#e4e4e7"), borderRadius: 999, padding: "5px 11px", fontSize: 11, fontWeight: 700, cursor: empty ? "not-allowed" : "pointer", fontFamily: "inherit" }}>{o.l}</button>;
          })}
        </div>}

      {mode === "pattern" &&
        <div style={{ marginBottom: 10 }}>
          <div style={{ display: "flex", flexWrap: "wrap", gap: 6 }}>
            {NAME_PATTERNS.map((o) => {
              const on = pattern === o.key;
              return <button key={o.key} type="button" onClick={() => { setPattern(o.key); cfg.pattern = o.key; }}
                style={{ background: on ? "#0e0e10" : "#fff", color: on ? "#fff" : "#3f3f46", border: "1px solid " + (on ? "#0e0e10" : "#e4e4e7"), borderRadius: 6, padding: "5px 10px", fontSize: 11, fontWeight: 700, cursor: "pointer", fontFamily: "JetBrains Mono, monospace" }}>{o.label}</button>;
            })}
          </div>
          <label style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 9, fontSize: 11.5, color: "#3f3f46", cursor: "pointer" }}>
            <input type="checkbox" checked={auto} onChange={(e) => { setAuto(e.target.checked); cfg.auto = e.target.checked; }} />
            Apply automatically to every new roster in this store
          </label>
          <div style={{ fontSize: 11, color: "#9a9aa1", marginTop: 5, lineHeight: 1.45 }}>New rosters only — names already saved are never rewritten.</div>
        </div>}

      <div style={{ display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap", paddingTop: 9, borderTop: "1px solid #ececf0" }}>
        {suggestion && !dismissed
          ? <React.Fragment>
              <span style={{ fontSize: 11, fontWeight: 700, color: "#9a9aa1" }}>SUGGESTED</span>
              <button type="button" onClick={apply} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: value === suggestion ? "#e7f5ed" : "#fff", border: "1px solid " + (value === suggestion ? "#bfe3cd" : "#d4d4d8"), borderRadius: 999, padding: "6px 13px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>
                {value === suggestion ? <IconCheck size={12} style={{ strokeWidth: 2.6, color: "#1f7a44" }} /> : <IconPlus size={12} style={{ strokeWidth: 2.4 }} />}
                {suggestion}
              </button>
              {value !== suggestion && <button type="button" onClick={() => setDismissed(true)} style={{ background: "none", border: 0, fontSize: 11.5, fontWeight: 700, color: "#9a9aa1", cursor: "pointer", fontFamily: "inherit" }}>Ignore</button>}
            </React.Fragment>
          : <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{dismissed ? "Suggestion dismissed \u2014 the name above is yours to type." : "Add a coach name or division to get a suggestion."}</span>}
      </div>
      <div style={{ marginTop: 8, fontSize: 11, color: "#6b6b72", lineHeight: 1.5 }}>{activeNote}</div>
    </div>
  );
}

function NewRosterModal({ open, mode, initial, groups, onClose, onSave, store, org }) {
  const editing = mode === "edit";
  const [name, setName] = React.useState("");
  const [groupId, setGroupId] = React.useState("");
  React.useEffect(() => { if (open) { setName(initial?.name || ""); setGroupId(""); } }, [open]);
  const canSave = name.trim().length > 0;
  const hasGroups = !editing && Array.isArray(groups) && groups.length > 0;
  function confirm() { if (canSave) onSave({ mode: editing ? "edit" : "create", id: initial?.id, name: name.trim(), groupId: groupId || null }); }
  return (
    <Modal open={open} onClose={onClose} width={460}>
      <div style={{ padding: "28px 28px 24px" }}>
        <h2 style={{ margin: 0, fontSize: 20, fontWeight: 800 }}>{editing ? "Rename Roster" : "New Roster"}</h2>
        <p style={{ margin: "8px 0 24px", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
          {editing ? "Give this roster a new name." : "Give it a name to get started. Optionally add it to a roster group to inherit that group's branding."}
        </p>
        <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>Roster Name</label>
        <RModalInput value={name} onChange={setName} placeholder="e.g. Varsity, Coaches & Staff, 12U Boys" autoFocus onEnter={confirm} />
        <RosterNameAssist store={store} org={org} groupId={groupId || null} groupName={(groups || []).find((g) => g.id === groupId)?.name || ""} rosterId={initial?.id} value={name} onChange={setName} />
        {hasGroups &&
          <div style={{ marginTop: 16 }}>
            <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>
              Roster Group <span style={{ fontWeight: 600, color: "#9a9aa1" }}>· optional</span>
            </label>
            <RModalSelect
              value={groupId}
              onChange={setGroupId}
              placeholder="No group — standalone roster"
              options={groups.map((g) => ({ value: g.id, label: g.name }))} />
            <p style={{ margin: "8px 2px 0", fontSize: 12, color: "#9a9aa1", lineHeight: 1.5 }}>
              {groupId ? "This roster joins the selected group and inherits its branding." : "Leave blank to create a standalone roster that isn't tied to any group."}
            </p>
          </div>}
        <RModalFooter onCancel={onClose} onConfirm={confirm} confirmLabel={editing ? "Save" : "Create Roster"} disabled={!canSave} />
      </div>
    </Modal>);
}

function AddRosterModal({ open, group, onClose, onAdd, store, org }) {
  const [name, setName] = React.useState("");
  const [division, setDivision] = React.useState("");
  React.useEffect(() => { if (open) { setName(""); setDivision(""); } }, [open]);
  const canSave = name.trim().length > 0;
  function pick(d) { setDivision(d); setName(`${d} Boys Basketball`); }
  function confirm() { if (canSave) onAdd({ name: name.trim(), division: division || null }); }
  return (
    <Modal open={open} onClose={onClose} width={480}>
      <div style={{ padding: "28px 28px 24px" }}>
        <h2 style={{ margin: 0, fontSize: 20, fontWeight: 800 }}>Add Roster</h2>
        <p style={{ margin: "8px 0 24px", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
          This roster joins <strong style={{ color: "#0e0e10" }}>{group?.name}</strong> and inherits its branding.
        </p>
        <label style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>Roster Name</label>
        <RModalInput value={name} onChange={setName} placeholder="e.g. 8U Boys Basketball" autoFocus onEnter={confirm} />
        <RosterNameAssist store={store} org={org} groupId={group?.id || null} groupName={group?.name || ""} division={division} onDivision={setDivision} value={name} onChange={setName} />
        <div style={{ display: "flex", gap: 8, marginTop: 12, flexWrap: "wrap" }}>
          {["8U", "10U", "12U", "14U"].map((d) =>
            <button key={d} onClick={() => pick(d)} style={{
              padding: "8px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
              borderRadius: 6, border: `1px solid ${division === d ? "#0e0e10" : "#d4d4d8"}`,
              background: division === d ? "#0e0e10" : "#fff", color: division === d ? "#fff" : "#0e0e10"
            }}>{d}</button>)}
        </div>
        <RModalFooter onCancel={onClose} onConfirm={confirm} confirmLabel="Add Roster" disabled={!canSave} />
      </div>
    </Modal>);
}

// ─────────── Roster player table ───────────
const ROSTER_COLS = "44px 1.5fr .8fr 1.2fr 1.4fr 1.1fr 1.2fr 56px";
// Direct-to-Home Delivery stores expose an extra Player Address column (Epic 3),
// so the grid gains one track between Phone and the row-actions cell.
const ROSTER_COLS_HOME = "44px 1.3fr .66fr 1fr 1.25fr 1fr 1.5fr 1.1fr 56px";

const ROSTER_SORT_OPTIONS = ["Name (A-Z)", "Name (Z-A)", "Number (low to high)", "Number (high to low)"];

// Player shipping addresses are verified against a map service on the backend
// before a record is accepted (Epic 3). This mock stands in for that round-trip:
// it normalizes whitespace and rejects entries that don't look like a real
// street address (needs a house number and at least three tokens).
function validateAddressService(raw) {
  return new Promise((resolve) => {
    setTimeout(() => {
      const s = String(raw || "").replace(/\s+/g, " ").trim();
      const ok = /\d/.test(s) && s.split(" ").length >= 3;
      resolve({ ok, normalized: s });
    }, 750);
  });
}

// ─────────── Team Gear (roster column + drawer) ───────────
// Team Required / Team Supplied items assigned to the roster. Pulled from the
// store's products when configured; falls back to a demo catalog.
const DEFAULT_TEAM_GEAR = [
  { id: "tg-1", title: "HOME GAME JERSEY", setting: "team-required", price: 65 },
  { id: "tg-2", title: "HOME GAME SHORTS", setting: "team-required", price: 65 },
  { id: "tg-3", title: "AWAY GAME JERSEY", setting: "team-required", price: 65 },
  { id: "tg-4", title: "AWAY GAME SHORTS", setting: "team-required", price: 58 },
  { id: "tg-5", title: "TEAM WARM-UP HOODIE", setting: "team-supplied", price: 0 },
];
function teamGearCatalog(store) {
  const fromStore = (store?.products || [])
    .filter((p) => p.teamSetting === "team-required" || p.teamSetting === "team-supplied")
    .map((p, i) => ({
      id: p.id || `p-${i}`,
      title: String(p.title || "Team Item").toUpperCase(),
      setting: p.teamSetting,
      price: typeof p.adult === "number" ? p.adult : 65,
    }));
  return fromStore.length ? fromStore : DEFAULT_TEAM_GEAR;
}
// Deterministic demo mix per player — some complete, some partial, some none.
function playerTeamGear(player, store) {
  const catalog = teamGearCatalog(store);
  const seed = (parseInt(player.number, 10) || 0) +
    String(player.first || "").length * 3 + String(player.last || "").length;
  // Two of every five players have ordered nothing yet — the common real state,
  // and the one where a rep can still move them between teams.
  const orderedCount = seed % 5 <= 1 ? 0 : seed % (catalog.length + 1);
  const sizes = ["S", "M", "L", "XL", "XXL"];
  const statuses = ["delivered", "shipped", "artwork-production", "order-confirmed"];
  const dates = ["Jan 10, 2026", "Jan 14, 2026", "Feb 2, 2026", "Feb 9, 2026"];
  const items = catalog.map((g, i) => i < orderedCount ? {
    ...g, ordered: true,
    size: sizes[(seed + i) % sizes.length], qty: 1,
    orderNumber: "435AFHSPY4D" + (90 + ((seed + i) % 10)),
    orderDate: dates[(seed + i) % dates.length],
    status: statuses[(seed + i) % statuses.length],
  } : { ...g, ordered: false });
  return { items, orderedCount, total: catalog.length };
}

// Right-side Team Gear drawer (figma "Team Gear Modal" reference).
function TeamGearDrawer({ player, store, onClose, onSendReminder }) {
  if (!player) return null;
  const { items, orderedCount, total } = playerTeamGear(player, store);
  const incomplete = orderedCount < total;
  const settingBadge = (s) => s === "team-supplied"
    ? { label: "Team Supplied", color: "#2944a8", bg: "#eef1fb" }
    : { label: "Team Required", color: "#b81d14", bg: "#fdecea" };
  const lbl = { fontWeight: 500, fontSize: 12, lineHeight: "16px", letterSpacing: ".03em", color: "#09090b" };
  return (
    <React.Fragment>
      <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(14,14,16,.4)", zIndex: 90 }} />
      <div style={{
        position: "fixed", top: 0, right: 0, bottom: 0, width: 587, maxWidth: "94vw", zIndex: 91,
        background: "#fff", borderRadius: "8px 0 0 8px",
        boxShadow: "-12px 0 40px rgba(14,14,16,.18), inset 0 0 0 1px #d4d4d8",
        display: "flex", flexDirection: "column", boxSizing: "border-box",
      }}>
        {/* header — title + player info */}
        <div style={{ padding: "16px 16px 0", flexShrink: 0 }}>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "flex-start" }}>
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              <span style={{ fontSize: 14, fontWeight: 700, lineHeight: "20px", letterSpacing: ".01em", color: "#09090b" }}>Team Gear</span>
              <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                <span style={{ fontSize: 12, lineHeight: "16px", letterSpacing: ".01em", color: "#09090b" }}>{player.first} {player.last}</span>
                <IconShirt size={16} style={{ color: "#71717a" }} />
                <span style={{ fontSize: 12, lineHeight: "16px", letterSpacing: ".01em", color: "#09090b" }}>#{player.number || "\u2014"}</span>
              </div>
            </div>
            <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: 0, padding: 0, cursor: "pointer", color: "#09090b", display: "inline-flex" }}>
              <IconX size={20} style={{ strokeWidth: 2 }} />
            </button>
          </div>
          <div style={{ height: 1, background: "#d4d4d8", margin: "16px 0 0" }} />
        </div>
        {/* product list */}
        <div style={{ flex: 1, overflowY: "auto", padding: "16px 16px 16px", minHeight: 0 }}>
          <div style={{ fontSize: 12, fontWeight: 700, lineHeight: "16px", letterSpacing: ".01em", color: "#71717a", marginBottom: 8 }}>
            TEAM GEAR · {orderedCount}/{total} ORDERED
          </div>
          <div style={{ display: "flex", flexDirection: "column" }}>
            {items.map((it, i) => {
              const b = settingBadge(it.setting);
              return (
                <div key={it.id} style={{
                  border: "1px solid #d4d4d8", marginTop: i === 0 ? 0 : -1,
                  display: "flex", flexDirection: "row", justifyContent: "space-between", alignItems: "center",
                  padding: "16px 16px 16px 12px", gap: 16, boxSizing: "border-box",
                  background: it.ordered ? "#fff" : "#fbfbfc",
                }}>
                  <div style={{ display: "flex", gap: 8, alignItems: it.ordered ? "flex-start" : "center", minWidth: 0 }}>
                    <div style={{ width: 56, display: "flex", flexDirection: "column", gap: 4, alignItems: "center", flexShrink: 0 }}>
                      <div style={{ width: 40, height: 40, borderRadius: 4, border: "1px solid #ececf0", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#9a9aa1" }}>
                        <IconShirt size={20} style={{ strokeWidth: 1.6 }} />
                      </div>
                      {it.ordered
                        ? <span style={{ height: 20, borderRadius: 4, background: "#f4f4f5", color: "#71717a", fontSize: 12, fontWeight: 500, lineHeight: "16px", padding: "2px 8px", boxSizing: "border-box" }}>{it.size}</span>
                        : <IconX size={16} style={{ color: "#b91c1c", strokeWidth: 2.4 }} />}
                    </div>
                    <div style={{ display: "flex", flexDirection: "column", gap: 8, justifyContent: "center", minWidth: 0 }}>
                      <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
                        <span style={{ fontSize: 12, fontWeight: 700, lineHeight: "16px", letterSpacing: ".01em", color: it.ordered ? "#09090b" : "#71717a" }}>{it.title}</span>
                        <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: ".04em", color: b.color, background: b.bg, borderRadius: 4, padding: "2px 8px", whiteSpace: "nowrap" }}>{b.label}</span>
                        {it.ordered &&
                        <span style={{ display: "inline-flex", gap: 8 }}>
                          <span style={{ fontSize: 12, fontWeight: 500, lineHeight: "16px", color: "#71717a" }}>Size: {it.size}</span>
                          <span style={{ fontSize: 12, fontWeight: 500, lineHeight: "16px", color: "#71717a" }}>Qty: {it.qty}</span>
                        </span>}
                      </div>
                      {it.ordered ?
                      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                        <div style={{ display: "flex", alignItems: "center", gap: 4 }}>
                          <span style={lbl}>Order Number</span>
                          <span style={{ fontSize: 12, lineHeight: "16px", letterSpacing: ".01em", color: "#2563eb" }}>{it.orderNumber}</span>
                        </div>
                        <div style={{ display: "flex", alignItems: "center", gap: 4 }}>
                          <span style={lbl}>Order Date</span>
                          <span style={{ fontSize: 12, lineHeight: "16px", letterSpacing: ".01em", color: "#71717a" }}>{it.orderDate}</span>
                        </div>
                        <div style={{ display: "flex", alignItems: "center", gap: 4 }}>
                          <span style={lbl}>Order Status</span>
                          {(() => { const s = STATUS_STYLES[it.status] || STATUS_STYLES["delivered"]; return (
                            <span style={{ fontSize: 11, fontWeight: 700, borderRadius: 999, padding: "2px 10px", background: s.bg, color: s.fg, border: `1px solid ${s.border}`, whiteSpace: "nowrap" }}>{s.label}</span>
                          ); })()}
                        </div>
                      </div> :
                      <span style={{ fontSize: 12, lineHeight: "16px", color: "#b91c1c", fontWeight: 500 }}>Not ordered</span>}
                    </div>
                  </div>
                  {it.ordered &&
                  <span style={{ fontSize: 12, lineHeight: "16px", letterSpacing: ".01em", color: "#09090b", flexShrink: 0 }}>
                    {it.setting === "team-supplied" ? "$0.00" : `$${it.price.toFixed(2)}`}
                  </span>}
                </div>
              );
            })}
          </div>
        </div>
        {/* footer — only when the player still has un-ordered gear */}
        {incomplete &&
        <div style={{ borderTop: "1px solid #d4d4d8", padding: "24px 16px", flexShrink: 0 }}>
          <button onClick={() => onSendReminder && onSendReminder(player)} style={{
            width: "100%", height: 40, borderRadius: 4, background: "#0e0e10", color: "#fff",
            border: 0, fontSize: 13.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#000"}
          onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
            Send Reminder
          </button>
        </div>}
      </div>
    </React.Fragment>
  );
}

function RosterRulesDefaultCard({ store, org, onUpdateStore, onToast }) {
  const [tick, setTick] = React.useState(0);
  const [prompt, setPrompt] = React.useState(null);
  const [showList, setShowList] = React.useState(false);
  const def = !!store.allowDuplicateNumbers;
  const div = rosterDupeDivergence(store, org);
  const apply = (v, mode) => {
    try { window.localStorage.setItem("ts_allow_dupes", v ? "1" : "0"); } catch (e) {}
    if (mode === "all") div.list.forEach((r) => setRosterDupe(r.id, null));
    onUpdateStore && onUpdateStore({ allowDuplicateNumbers: v });
    setTick((n) => n + 1);
    setPrompt(null);
    onToast && onToast(mode === "all"
      ? (v ? "Duplicates allowed on all " : "Duplicates blocked on all ") + div.total + " teams \u2014 overrides cleared"
      : "Default updated \u2014 " + div.overridden + (div.overridden === 1 ? " team keeps its override" : " teams keep their overrides"));
  };
  const change = (v) => { if (div.overridden > 0) setPrompt({ value: v }); else apply(v, "all"); };
  return (
    <div style={{ marginTop: 0, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, padding: "18px 20px" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 12 }}>
        <span style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".04em", color: "#0e0e10" }}>Roster Rules</span>
        <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#f4f4f5", borderRadius: 999, padding: "3px 8px" }}>STORE DEFAULT · SET PER TEAM</span>
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
        <span style={{
          width: 40, height: 40, borderRadius: 9, flexShrink: 0,
          background: def ? "#fdecea" : "#f1f1f3", color: def ? "#e1251b" : "#6b6b72",
          display: "inline-flex", alignItems: "center", justifyContent: "center",
        }}><IconUsers size={20} /></span>
        <div style={{ flex: 1 }}>
          <div style={{ fontSize: 14, fontWeight: 500, color: "#0e0e10", marginBottom: 2 }}>Allow Duplicate Numbers</div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
            The value in force lives on each team. Setting this pushes the default out; a team can still diverge in My Team{store.storeType === "league" ? "s" : ""}.
          </div>
        </div>
        <span style={{ display: "flex", alignItems: "center", gap: 12, flexShrink: 0 }}>
          <span style={{ fontSize: 12, fontWeight: 700, color: def ? "#1f9d55" : "#6b6b72" }}>{def ? "Allowed" : "Blocked"}</span>
          <SettingsToggle value={def} onChange={change} />
        </span>
      </div>
      {/* What's actually in force — a control that hides divergence is lying. */}
      <div style={{ marginTop: 14, paddingTop: 14, borderTop: "1px solid #f1f1f3", display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap" }}>
        {div.overridden === 0
          ? <span style={{ fontSize: 12.5, color: "#6b6b72" }}>All {div.total} teams follow this default.</span>
          : <React.Fragment>
              <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".04em", color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "4px 10px" }}>{div.overridden} OF {div.total} TEAMS OVERRIDDEN</span>
              <button type="button" onClick={() => setShowList(!showList)} style={{ background: "none", border: 0, fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", textDecoration: "underline", textUnderlineOffset: 2 }}>{showList ? "Hide" : "Which teams?"}</button>
              <button type="button" onClick={() => apply(def, "all")} style={{ background: "none", border: 0, fontSize: 12, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit" }}>Reset all to default</button>
            </React.Fragment>}
      </div>
      {showList && div.overridden > 0 &&
        <div style={{ marginTop: 10, display: "flex", flexWrap: "wrap", gap: 6 }}>
          {div.names.map((n) => <span key={n} style={{ fontSize: 11.5, fontWeight: 600, color: "#3f3f46", background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 999, padding: "4px 10px" }}>{n} · {def ? "unique numbers" : "duplicates allowed"}</span>)}
        </div>}
      <Modal open={!!prompt} onClose={() => setPrompt(null)} width={480}>
        <div style={{ padding: "28px 28px 8px" }}>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800, fontFamily: "Archivo, sans-serif" }}>{div.overridden} {div.overridden === 1 ? "team has" : "teams have"} their own setting</h2>
          <p style={{ margin: "12px 0 0", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>
            Push <strong style={{ color: "#0e0e10" }}>{prompt && prompt.value ? "duplicates allowed" : "unique numbers"}</strong> to every team, or only to the {div.total - div.overridden} still following the default?
          </p>
        </div>
        <div style={{ display: "flex", flexDirection: "column", gap: 10, padding: "20px 28px 24px" }}>
          <Button variant="secondary" onClick={() => apply(prompt.value, "keep")}>Only the {div.total - div.overridden} following the default</Button>
          <Button variant="primary" onClick={() => apply(prompt.value, "all")}>Apply to all {div.total} teams — clear overrides</Button>
        </div>
      </Modal>
    </div>
  );
}

// ── 7a — allow-duplicates is a TEAM (roster) setting. Settings holds the bulk
// default; each roster can diverge, and the map below is the authority. ──
function rosterDupeMap() { window.__rosterDupes = window.__rosterDupes || {}; return window.__rosterDupes; }
function rosterDupeAllowed(store, rosterId) {
  const m = rosterDupeMap();
  if (rosterId != null && Object.prototype.hasOwnProperty.call(m, rosterId)) return !!m[rosterId];
  return !!(store && store.allowDuplicateNumbers);
}
function rosterDupeOverridden(rosterId) { return rosterId != null && Object.prototype.hasOwnProperty.call(rosterDupeMap(), rosterId); }
function setRosterDupe(rosterId, v) {
  const m = rosterDupeMap();
  if (v === null) delete m[rosterId]; else m[rosterId] = !!v;
  // The buyer-facing number entry reads the same rule (see the storefront file).
  try { if (v !== null) window.localStorage.setItem("ts_allow_dupes", v ? "1" : "0"); } catch (e) {}
}
function orgRosterList(org) {
  const out = [];
  ((org && org.groups) || []).forEach((g) => (g.rosters || []).forEach((r) => out.push({ id: r.id, name: r.name, division: r.division || null, groupId: g.id, groupName: g.name, color: g.color, players: r.players || 0 })));
  ((org && org.loose) || []).forEach((r) => out.push({ id: r.id, name: r.name, groupId: null, groupName: null, color: "#3f3f46", players: r.players || 0 }));
  return out;
}
function rosterDupeDivergence(store, org) {
  const list = orgRosterList(org);
  const def = !!(store && store.allowDuplicateNumbers);
  const overridden = list.filter((r) => rosterDupeOverridden(r.id) && rosterDupeAllowed(store, r.id) !== def);
  return { total: list.length, overridden: overridden.length, list, names: overridden.map((r) => r.name) };
}

// ── 7b/7c/7d — one number-entry component, two input models. A taken number is a
// CONFLICT when duplicates are off (locked, unselectable, red) and merely IN USE
// when they're on (neutral, informational, still selectable). ──
function jerseyHolders(n, existing, selfId) {
  const s = String(n);
  return (existing || []).filter((p) => p.id !== selfId && String(p.number || "").trim() === s);
}
function jerseyHolderNames(hs) { return hs.map((p) => (p.first || "") + " " + (p.last || "")).join(", ").trim(); }
function JerseyGrid({ value, onChange, existing, selfId, allowDupes, cell = 30 }) {
  const takenCount = {};
  (existing || []).forEach((p) => { if (p.id === selfId) return; const n = String(p.number || "").trim(); if (n) takenCount[n] = (takenCount[n] || 0) + 1; });
  return (
    <div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(10, " + cell + "px)", gap: 4 }}>
        {Array.from({ length: 100 }, (_, n) => {
          const s = String(n);
          const used = takenCount[s] || 0;
          const sel = String(value || "") === s;
          const locked = used > 0 && !allowDupes;
          const dupe = used > 0 && allowDupes;
          const hs = used ? jerseyHolders(s, existing, selfId) : [];
          const title = used ? (allowDupes ? s + " · duplicate — also worn by " + jerseyHolderNames(hs) : s + " taken by " + jerseyHolderNames(hs)) : "Assign " + s;
          return (
            <button key={n} type="button" title={title} disabled={locked}
              onClick={() => { if (!locked) onChange(sel ? "" : s); }}
              style={{
                height: cell, width: cell, padding: 0, borderRadius: 5, fontFamily: "inherit",
                fontSize: 11.5, fontWeight: sel ? 800 : 600, position: "relative",
                background: sel ? "#0e0e10" : locked ? "#f4f4f5" : "#fff",
                color: sel ? "#fff" : locked ? "#c4c4cb" : dupe ? "#52525b" : "#3f3f46",
                border: sel ? "1px solid #0e0e10" : dupe ? "1px dashed #b6b6bd" : "1px solid #e4e4e7",
                textDecoration: locked ? "line-through" : "none",
                cursor: locked ? "not-allowed" : "pointer",
              }}>
              {s}
              {dupe && !sel && <span aria-hidden="true" style={{ position: "absolute", top: 2, right: 3, width: 4, height: 4, borderRadius: 999, background: "#a1a1aa" }} />}
            </button>
          );
        })}
      </div>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 14, marginTop: 10, fontSize: 10.5, color: "#6b6b72" }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 5 }}><span style={{ width: 11, height: 11, borderRadius: 3, background: "#fff", border: "1px solid #e4e4e7" }} />Available</span>
        {allowDupes
          ? <span style={{ display: "inline-flex", alignItems: "center", gap: 5 }}><span style={{ width: 11, height: 11, borderRadius: 3, background: "#fff", border: "1px dashed #b6b6bd" }} />In use · duplicate allowed</span>
          : <span style={{ display: "inline-flex", alignItems: "center", gap: 5 }}><span style={{ width: 11, height: 11, borderRadius: 3, background: "#f4f4f5", border: "1px solid #e4e4e7" }} />Taken · locked</span>}
        <span style={{ display: "inline-flex", alignItems: "center", gap: 5 }}><span style={{ width: 11, height: 11, borderRadius: 3, background: "#0e0e10" }} />Selected</span>
      </div>
    </div>
  );
}
function JerseyNumberField({ value, onChange, existing, selfId, allowDupes, mode, onModeChange, autoFocus }) {
  const [localMode, setLocalMode] = React.useState("grid");
  const m = mode || localMode;
  const setMode = onModeChange || setLocalMode;
  const clean = String(value || "").trim();
  const hs = clean ? jerseyHolders(clean, existing, selfId) : [];
  const conflict = hs.length > 0 && !allowDupes;
  return (
    <div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 10 }}>
        <div style={{ display: "inline-flex", background: "#f1f1f3", borderRadius: 7, padding: 3 }}>
          {[{ k: "grid", l: "Number grid" }, { k: "type", l: "Type it" }].map((o) => {
            const on = m === o.k;
            return <button key={o.k} type="button" onClick={() => setMode(o.k)} style={{ padding: "5px 11px", border: 0, borderRadius: 5, cursor: "pointer", fontSize: 11.5, fontWeight: 700, fontFamily: "inherit", background: on ? "#fff" : "transparent", color: on ? "#0e0e10" : "#6b6b72", boxShadow: on ? "0 1px 2px rgba(20,20,30,.12)" : "none" }}>{o.l}</button>;
          })}
        </div>
        <span style={{ marginLeft: "auto", fontSize: 11, fontWeight: 700, color: allowDupes ? "#6b6b72" : "#0e0e10" }}>{allowDupes ? "Duplicates allowed on this team" : "Unique numbers required"}</span>
      </div>
      {m === "grid"
        ? <JerseyGrid value={value} onChange={onChange} existing={existing} selfId={selfId} allowDupes={allowDupes} />
        : <input value={clean} autoFocus={autoFocus} inputMode="numeric" maxLength={2} placeholder="#"
            onChange={(e) => onChange(e.target.value.replace(/\D/g, "").slice(0, 2))}
            style={{ width: 92, boxSizing: "border-box", height: 40, border: "1px solid " + (conflict ? "#e1251b" : "#d4d4d8"), borderRadius: 6, padding: "0 12px", fontSize: 16, fontWeight: 700, fontFamily: "JetBrains Mono, monospace", color: "#0e0e10", outline: "none" }} />}
      {clean && hs.length > 0 &&
        <div style={{ marginTop: 10, display: "flex", alignItems: "flex-start", gap: 7, fontSize: 12, fontWeight: 600, lineHeight: 1.45, color: conflict ? "#b81d14" : "#6b6b72" }}>
          {conflict ? <IconAlert size={13} style={{ strokeWidth: 2.2, flexShrink: 0, marginTop: 1 }} /> : <IconInfo size={13} style={{ strokeWidth: 2, flexShrink: 0, marginTop: 1 }} />}
          <span>{conflict
            ? clean + " is taken by " + jerseyHolderNames(hs) + " \u2014 pick a number that isn\u2019t locked."
            : clean + " \u00b7 duplicate \u2014 also worn by " + jerseyHolderNames(hs) + ". Allowed on this team."}</span>
        </div>}
    </div>
  );
}
// Inline roster row: the grid can't live in a 90px cell, so the cell opens it.
function JerseyCellPicker({ value, onChange, existing, selfId, allowDupes, invalid }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    const t = setTimeout(() => document.addEventListener("mousedown", h), 0);
    return () => { clearTimeout(t); document.removeEventListener("mousedown", h); };
  }, [open]);
  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button type="button" onClick={() => setOpen(!open)} style={{
        display: "flex", alignItems: "center", justifyContent: "space-between", gap: 6, width: "100%",
        height: 40, boxSizing: "border-box", padding: "0 10px", cursor: "pointer", fontFamily: "inherit",
        background: "#fff", border: "1px solid " + (invalid ? "#e1251b" : open ? "#0e0e10" : "#d4d4d8"), borderRadius: 4,
        fontSize: 14, fontWeight: value ? 700 : 400, color: value ? "#0e0e10" : "#9a9aa1",
      }}>
        {value || "#"} <IconGrid size={13} style={{ color: "#9a9aa1", flexShrink: 0 }} />
      </button>
      {open &&
        <div style={{ position: "absolute", top: 46, left: 0, zIndex: 60, width: 372, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, boxShadow: "0 16px 40px rgba(20,20,30,.16)", padding: 14 }}>
          <JerseyNumberField value={value} onChange={onChange} existing={existing} selfId={selfId} allowDupes={allowDupes} />
          <div style={{ display: "flex", justifyContent: "flex-end", gap: 10, marginTop: 12, paddingTop: 12, borderTop: "1px solid #f1f1f3" }}>
            <button type="button" onClick={() => { onChange(""); }} style={{ background: "none", border: 0, fontSize: 12, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit" }}>Clear</button>
            <button type="button" onClick={() => setOpen(false)} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "8px 14px", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Done</button>
          </div>
        </div>}
    </div>
  );
}

// ── 8 — Move to different team. The crux is the jersey number: it is released on
// the origin roster and re-validated against the destination's own rules before
// the move commits. Players with team-gear orders can't move (same reasoning as
// the publish gate — fulfillment is already in flight). ──
function rosterNumberRegistry() { window.__rosterNumbers = window.__rosterNumbers || {}; return window.__rosterNumbers; }
function rosterTakenNumbers(rosterId, playerCount) {
  const reg = rosterNumberRegistry();
  if (reg[rosterId]) return reg[rosterId];
  // Unopened rosters: deterministic stand-in so collisions are demonstrable.
  let h = 5381; const s = String(rosterId || "");
  for (let i = 0; i < s.length; i++) h = ((h * 33) ^ s.charCodeAt(i)) >>> 0;
  const out = [];
  const n = Math.max(0, Math.min(playerCount || 0, 24));
  for (let i = 0; i < n; i++) { h = (Math.imul(h, 1664525) + 1013904223) >>> 0; const v = h % 100; if (!out.includes(String(v))) out.push(String(v)); }
  return out;
}
function firstFreeNumber(taken) {
  for (let i = 0; i < 100; i++) if (!taken.includes(String(i))) return String(i);
  return "";
}
function MovePlayersModal({ open, players, current, targets, store, onClose, onConfirm }) {
  const [destId, setDestId] = React.useState("");
  const [query, setQuery] = React.useState("");
  const [choices, setChoices] = React.useState({});
  React.useEffect(() => { if (open) { setDestId(""); setQuery(""); setChoices({}); } }, [open, players]);
  if (!open) return null;
  const gear = (p) => playerTeamGear(p, store);
  const blocked = (players || []).filter((p) => gear(p).orderedCount > 0);
  const movable = (players || []).filter((p) => gear(p).orderedCount === 0);
  const q = query.trim().toLowerCase();
  const list = (targets || []).filter((t) => !q || t.name.toLowerCase().includes(q) || (t.groupName || "").toLowerCase().includes(q));
  const dest = (targets || []).find((t) => t.id === destId) || null;
  const destTaken = dest ? rosterTakenNumbers(dest.id, dest.players) : [];
  const destDupes = dest ? rosterDupeAllowed(store, dest.id) : false;
  const rows = movable.map((p) => {
    const num = String(p.number || "").trim();
    const collides = !!num && destTaken.includes(num) && !destDupes;
    const choice = choices[p.id] || (collides ? "new" : "keep");
    const suggested = firstFreeNumber(destTaken.concat(movable.map((x) => String(choices[x.id + ":n"] || "")).filter(Boolean)));
    const assigned = choice === "keep" ? num : choice === "none" ? "" : String(choices[p.id + ":n"] || suggested);
    return { p, num, collides, choice, assigned };
  });
  const commit = () => {
    if (!dest || !rows.length) return;
    onConfirm(dest, rows.map((r) => ({ player: r.p, number: r.assigned })));
  };
  const cardBtn = (on) => ({
    display: "flex", alignItems: "center", gap: 10, width: "100%", textAlign: "left", cursor: "pointer",
    background: on ? "#fafafb" : "#fff", border: "1px solid " + (on ? "#0e0e10" : "#ececf0"), borderRadius: 8,
    padding: "10px 12px", fontFamily: "inherit", marginBottom: 6,
  });
  return (
    <Modal open onClose={onClose} width={600}>
      <div style={{ padding: "28px 28px 0" }}>
        <h2 style={{ margin: 0, fontSize: 19, fontWeight: 800, fontFamily: "Archivo, sans-serif" }}>Move {players.length === 1 ? (players[0].first + " " + players[0].last) : players.length + " players"} to a different team</h2>
        <p style={{ margin: "10px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
          Contact details, associated emails, name on jersey and address move with the player. The jersey number is released on {current?.name || "this roster"} and re-validated against the destination.
        </p>
      </div>
      <div style={{ padding: "20px 28px 0", maxHeight: "56vh", overflowY: "auto" }}>
        {blocked.length > 0 &&
          <div style={{ display: "flex", gap: 10, alignItems: "flex-start", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 8, padding: "12px 14px", marginBottom: 16 }}>
            <IconLock size={14} style={{ flexShrink: 0, marginTop: 2, color: "#8a5f10" }} />
            <div style={{ fontSize: 12.5, color: "#6b5314", lineHeight: 1.5 }}>
              <strong style={{ color: "#8a5f10" }}>{blocked.length} {blocked.length === 1 ? "player can\u2019t move" : "players can’t move"} — team-gear orders already placed.</strong>
              <div style={{ marginTop: 3 }}>{blocked.map((p) => p.first + " " + p.last + " (" + gear(p).orderedCount + " ordered)").join(", ")}. Orders can’t be reassigned once they’re in fulfillment — same rule as an ordered product that can no longer be unpublished.</div>
            </div>
          </div>}
        {movable.length === 0
          ? <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.55, padding: "4px 0 18px" }}>
              <strong style={{ color: "#0e0e10" }}>Nothing left to move.</strong> A player follows their orders: cancel the team-gear order in the Orders tab to free them up, or wait until the batch is fulfilled and move them next season. Editing the player’s record still works from the row menu.
            </div>
          : <React.Fragment>
            <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", marginBottom: 8 }}>DESTINATION TEAM</div>
            {(targets || []).length > 6 &&
              <div style={{ position: "relative", marginBottom: 10 }}>
                <IconSearch size={14} style={{ position: "absolute", left: 10, top: "50%", transform: "translateY(-50%)", color: "#9a9aa1" }} />
                <input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search teams"
                  style={{ width: "100%", boxSizing: "border-box", fontFamily: "inherit", fontSize: 13, padding: "9px 10px 9px 32px", border: "1px solid #e4e4e7", borderRadius: 6, outline: "none" }} />
              </div>}
            {list.length === 0 && <div style={{ fontSize: 12.5, color: "#9a9aa1", padding: "10px 0" }}>No other teams in {current?.groupName ? current.groupName : "this store"} match.</div>}
            {list.map((t) => {
              const isCur = t.id === current?.id;
              const on = t.id === destId;
              return (
                <button key={t.id} type="button" disabled={isCur} onClick={() => setDestId(t.id)} style={{ ...cardBtn(on), cursor: isCur ? "default" : "pointer", opacity: isCur ? .55 : 1 }}>
                  <span style={{ width: 28, height: 28, borderRadius: 7, background: t.color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: t.division ? 10 : 10.5, fontWeight: 800, flexShrink: 0 }}>{t.division || rInitials(t.name)}</span>
                  <span style={{ minWidth: 0, flex: 1 }}>
                    <span style={{ display: "block", fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{t.name}</span>
                    <span style={{ display: "block", fontSize: 11.5, color: "#9a9aa1", marginTop: 1 }}>{t.groupName ? t.groupName + " \u00b7 " : "Standalone roster \u00b7 "}{t.players} players · {rosterDupeAllowed(store, t.id) ? "duplicates allowed" : "unique numbers"}</span>
                  </span>
                  {isCur && <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#f1f1f3", borderRadius: 999, padding: "3px 8px" }}>CURRENT TEAM</span>}
                </button>
              );
            })}
            <div style={{ marginTop: 4, fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>
              {current?.groupName
                ? "Only teams inside " + current.groupName + " are listed \u2014 roster groups carry their own branding and freight routing, so cross-group moves aren\u2019t allowed."
                : "Only standalone rosters are listed \u2014 moving into a roster group would change branding and freight routing."}
            </div>

            {dest &&
              <div style={{ marginTop: 20, paddingTop: 16, borderTop: "1px solid #ececf0" }}>
                <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", marginBottom: 8 }}>JERSEY NUMBERS AT {String(dest.name).toUpperCase()}</div>
                {rows.map((r) => (
                  <div key={r.p.id} style={{ border: "1px solid " + (r.collides ? "#f0d4d1" : "#ececf0"), borderRadius: 8, padding: "11px 13px", marginBottom: 8, background: "#fff" }}>
                    <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
                      <span style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10", flex: 1, minWidth: 0 }}>{r.p.first} {r.p.last}</span>
                      <span style={{ fontFamily: "JetBrains Mono, monospace", fontSize: 12, fontWeight: 700, color: "#6b6b72" }}>#{r.num || "\u2014"}</span>
                    </div>
                    <div style={{ marginTop: 4, fontSize: 12, color: r.collides ? "#b81d14" : "#1f7a44", fontWeight: 600 }}>
                      {!r.num ? "No number to carry \u2014 moves without one."
                        : r.collides ? "#" + r.num + " is already taken at " + dest.name + " \u2014 resolve before moving."
                        : "#" + r.num + " is free at " + dest.name + " \u2014 the number moves with the player."}
                    </div>
                    {r.collides &&
                      <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginTop: 9 }}>
                        {[{ k: "new", l: "Assign #" + r.assigned }, { k: "none", l: "Move without a number" }].map((o) => {
                          const on = r.choice === o.k;
                          return <button key={o.k} type="button" onClick={() => setChoices((c) => ({ ...c, [r.p.id]: o.k }))} style={{ background: on ? "#0e0e10" : "#fff", color: on ? "#fff" : "#0e0e10", border: "1px solid " + (on ? "#0e0e10" : "#d4d4d8"), borderRadius: 6, padding: "7px 11px", fontSize: 11.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{o.l}</button>;
                        })}
                      </div>}
                  </div>
                ))}
                <div style={{ fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>Team-gear completion resets at {dest.name} — required items differ per team. Nothing is ordered for these players, so nothing is left behind.</div>
              </div>}
          </React.Fragment>}
      </div>
      <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, padding: "20px 28px 24px" }}>
        <Button variant="secondary" onClick={onClose}>Cancel</Button>
        <Button variant="primary" disabled={!dest || rows.length === 0} onClick={commit}>Move {rows.length || ""} {rows.length === 1 ? "player" : "players"}</Button>
      </div>
    </Modal>
  );
}

function RosterPlayerTable({ startEmpty, store, header, injected, onNumbers, rosterName, onDeleteRoster, rosterId, moveTargets, onMovePlayers }) {
  const [players, setPlayers] = React.useState(startEmpty ? [] : [
  { id: 1, first: "Jalen", last: "Hurts", number: "1", nameOnJersey: "Hurtski", email: "name@example.com", phone: "202-111-5555", associatedEmails: ["susanQuinones@gmail.com"], shippingAddress: "2001 Lincoln Ave, Philadelphia, PA 19145" },
  { id: 2, first: "Omar", last: "Calzoni", number: "17", nameOnJersey: "Calzoni", email: "fwright@outlook.com", phone: "679-604-5561", associatedEmails: [] },
  { id: 3, first: "Abram", last: "Passaquindici Arcand", number: "11", nameOnJersey: "A. Passaquindici-Arcand", email: "igeorge@gmail.com", phone: "439-431-7751", associatedEmails: [], shippingAddress: "88 Maple Court, Austin, TX 78701", addressFromCheckout: true },
  { id: 4, first: "Cooper", last: "Curtis", number: "8", nameOnJersey: "Curtis", email: "khall@aol.com", phone: "297-362-8278", associatedEmails: [] },
  { id: 5, first: "Davis", last: "Aminoff", number: "2", nameOnJersey: "Aminoff", email: "abrown@hotmail.com", phone: "559-506-5875", associatedEmails: [] },
  { id: 6, first: "Lincoln", last: "Herwitz", number: "22", nameOnJersey: "Herwitz", email: "egarcia@yahoo.com", phone: "787-648-4307", associatedEmails: [] },
  { id: 7, first: "Ahmad", last: "Passaquindici Arcand", number: "23", nameOnJersey: "Passaquindici-Arcand", email: "emitchell@outlook.com", phone: "822-984-1775", associatedEmails: [] },
  { id: 8, first: "Corey", last: "Stanton", number: "0", nameOnJersey: "Stanton", email: "sjones@icloud.com", phone: "516-761-0087", associatedEmails: [] },
  { id: 9, first: "Jon", last: "Jones", number: "7", nameOnJersey: "Jones", email: "jonjones@icloud.com", phone: "111-761-0087", associatedEmails: [] }]
  );
  const [adding, setAdding] = React.useState(false);
  const [editingPlayer, setEditingPlayer] = React.useState(null);
  const [openMenuId, setOpenMenuId] = React.useState(null);
  const [selected, setSelected] = React.useState(new Set());
  const [sort, setSort] = React.useState("Name (A-Z)");
  const [uploadOpen, setUploadOpen] = React.useState(false);
  const [shareOpen, setShareOpen] = React.useState(false);
  const [templateOpen, setTemplateOpen] = React.useState(false);
  const [confirmDeleteOpen, setConfirmDeleteOpen] = React.useState(false);
  const [confirmClearOpen, setConfirmClearOpen] = React.useState(false);
  const [confirmRosterDeleteOpen, setConfirmRosterDeleteOpen] = React.useState(false);
  const [gearPlayer, setGearPlayer] = React.useState(null);
  const [toast, setToast] = React.useState("");
  // 7a — this roster's own allow-duplicates value (Settings only seeds the default).
  const [dupeTick, setDupeTick] = React.useState(0);
  const allowDupes = rosterDupeAllowed(store, rosterId);
  const dupeOverridden = rosterDupeOverridden(rosterId);
  const [movePlayers, setMovePlayers] = React.useState(null);

  // Bank-zone commits flow into the table as committed roster records.
  React.useEffect(() => {
    if (!injected || !injected.length) return;
    setPlayers((cur) => {
      const add = injected.filter((p) => !cur.some((c) => c.id === p.id));
      return add.length ? [...cur, ...add] : cur;
    });
  }, [injected]);
  // Report claimed jersey numbers so the bank zone can flag conflicts
  // (uncommitted captures count against these, and vice versa).
  React.useEffect(() => {
    onNumbers && onNumbers(players.map((p) => String(p.number)));
    // 8 — publish this roster's claimed numbers so a move can validate against it.
    if (rosterId) rosterNumberRegistry()[rosterId] = players.map((p) => String(p.number || "").trim()).filter(Boolean);
  }, [players, rosterId]);

  const allChecked = players.length > 0 && selected.size === players.length;
  const someChecked = selected.size > 0 && selected.size < players.length;

  // Epic 3 — Direct-to-Home Delivery stores collect a per-player shipping address.
  const homeDelivery = (store?.distributionModel || "coach") === "home";
  const cols = homeDelivery ? ROSTER_COLS_HOME : ROSTER_COLS;

  function toggleAll() {
    if (allChecked) setSelected(new Set());else
    setSelected(new Set(players.map((p) => p.id)));
  }
  function toggleOne(id) {
    const next = new Set(selected);
    if (next.has(id)) next.delete(id);else next.add(id);
    setSelected(next);
  }

  function addPlayer(p) {
    setPlayers((cur) => [...cur, { ...p, id: Date.now() }]);
    setAdding(false);
    window.dispatchEvent(new CustomEvent("tour:roster-added"));
  }
  function updatePlayer(p) {
    setPlayers((cur) => cur.map((x) => x.id === p.id ? p : x));
    setEditingPlayer(null);
  }
  function deletePlayer(id) {
    setPlayers((cur) => cur.filter((x) => x.id !== id));
    setSelected((s) => {const n = new Set(s);n.delete(id);return n;});
  }
  function removeSelected() {
    setPlayers((cur) => cur.filter((x) => !selected.has(x.id)));
    setSelected(new Set());
  }
  function bulkAddPlayers(rows) {
    setPlayers((cur) => [
    ...cur,
    ...rows.map((r, i) => ({ ...r, id: Date.now() + i }))]
    );
    setUploadOpen(false);
    if (rows && rows.length) window.dispatchEvent(new CustomEvent("tour:roster-added"));
  }
  function shareStoreLink(p) {
    const link = `https://prolook.com/store/hoyas?invite=${p.first.toLowerCase()}-${p.id}`;
    try {navigator.clipboard?.writeText(link);} catch (e) {}
    setToast(`Store link for ${p.first} ${p.last} copied to clipboard`);
  }

  // Players currently selected, and the de-duped pool of emails to pre-fill the share modal.
  const selectedPlayers = players.filter((p) => selected.has(p.id));
  // 8 — players with team-gear orders can't move, so say so before the modal opens.
  const movableSelected = selectedPlayers.filter((p) => playerTeamGear(p, store).orderedCount === 0).length;
  const sharePool = React.useMemo(() => {
    const set = new Set();
    selectedPlayers.forEach((p) => {
      if (p.email) set.add(p.email);
      (p.associatedEmails || []).forEach((e) => e && set.add(e));
    });
    return [...set];
  }, [selected, players]);

  function clearSelection() {setSelected(new Set());}

  // Sorted view of the roster (sorting never mutates the underlying list).
  const sortedPlayers = React.useMemo(() => {
    const arr = [...players];
    const name = (p) => `${p.first || ""} ${p.last || ""}`.trim().toLowerCase();
    const num = (p) => {const n = parseInt(p.number, 10);return isNaN(n) ? Infinity : n;};
    if (sort === "Name (A-Z)") arr.sort((a, b) => name(a).localeCompare(name(b)));else
    if (sort === "Name (Z-A)") arr.sort((a, b) => name(b).localeCompare(name(a)));else
    if (sort === "Number (low to high)") arr.sort((a, b) => num(a) - num(b));else
    if (sort === "Number (high to low)") arr.sort((a, b) => num(b) - num(a));
    return arr;
  }, [players, sort]);

  const doMove = (dest, assignments) => {
    const ids = new Set(assignments.map((a) => a.player.id));
    setPlayers((cur) => cur.filter((p) => !ids.has(p.id)));
    setSelected(new Set());
    setMovePlayers(null);
    onMovePlayers && onMovePlayers(dest, assignments);
    setToast(assignments.length + (assignments.length === 1 ? " player moved from " : " players moved from ") + (rosterName || "this roster") + " to " + dest.name);
  };

  return (
    <div style={{ minHeight: "800px" }}>
      {header}
      {/* 7a — roster rules in force for THIS team, editable where the rep is working */}
      <div style={{ display: "flex", alignItems: "center", gap: 12, flexWrap: "wrap", background: "#fff", border: "1px solid #ececf0", borderRadius: 8, padding: "12px 16px", marginBottom: 16 }}>
        <span style={{ width: 32, height: 32, borderRadius: 8, flexShrink: 0, background: allowDupes ? "#f4f4f5" : "#f1f1f3", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconUsers size={16} /></span>
        <span style={{ minWidth: 200, flex: 1 }}>
          <span style={{ display: "block", fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Duplicate numbers on this team</span>
          <span style={{ display: "block", fontSize: 12, color: "#6b6b72", marginTop: 1 }}>
            {allowDupes ? "Two players can share a number. Taken numbers stay selectable." : "Numbers are unique — taken numbers are locked in the picker."}
          </span>
        </span>
        {dupeOverridden
          ? <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "3px 9px" }}>OVERRIDES STORE DEFAULT</span>
          : <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#f4f4f5", borderRadius: 999, padding: "3px 9px" }}>FOLLOWING STORE DEFAULT</span>}
        {dupeOverridden &&
          <button type="button" onClick={() => { setRosterDupe(rosterId, null); setDupeTick((n) => n + 1); setToast("Reset to the store default"); }}
            style={{ background: "none", border: 0, fontSize: 12, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit" }}>Reset to default</button>}
        <span style={{ display: "flex", alignItems: "center", gap: 10, flexShrink: 0 }}>
          <span style={{ fontSize: 12, fontWeight: 700, color: allowDupes ? "#1f9d55" : "#6b6b72" }}>{allowDupes ? "Allowed" : "Blocked"}</span>
          <SettingsToggle value={allowDupes} onChange={(v) => { setRosterDupe(rosterId, v); setDupeTick((n) => n + 1); setToast(v ? "Duplicates allowed on " + (rosterName || "this team") : "Duplicates blocked on " + (rosterName || "this team")); }} />
        </span>
      </div>
      {/* Sort toolbar — shares the Stores/Leagues list toolbar component */}
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16, gap: 16 }}>
        <div style={{ fontSize: 13.5, fontWeight: 600, color: "#6b6b72" }}>
          {players.length} {players.length === 1 ? "Player" : "Players"}
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 10 }}>
          <button onClick={() => setUploadOpen(true)} style={{ display: "inline-flex", alignItems: "center", gap: 8, borderRadius: 6, padding: "11px 18px", fontSize: 13, fontWeight: 700, fontFamily: "inherit", background: "#0e0e10", border: "1px solid #0e0e10", color: "#fff", cursor: "pointer" }}>
            <IconUpload size={14} style={{ strokeWidth: 2.4 }} /> Upload Roster
          </button>
          <button disabled={players.length === 0} onClick={() => setConfirmClearOpen(true)} style={{ display: "inline-flex", alignItems: "center", gap: 8, borderRadius: 6, padding: "11px 18px", fontSize: 13, fontWeight: 700, fontFamily: "inherit", background: "#fff", border: "1px solid #d4d4d8", color: players.length === 0 ? "#a1a1aa" : "#0e0e10", cursor: players.length === 0 ? "default" : "pointer" }}>
            Clear Roster
          </button>
          <button data-tour="roster-add" onClick={() => {setAdding(true);}} disabled={adding} style={{ display: "inline-flex", alignItems: "center", gap: 8, borderRadius: 6, padding: "11px 18px", fontSize: 13, fontWeight: 700, fontFamily: "inherit", background: "#fff", border: "1px solid #d4d4d8", color: adding ? "#a1a1aa" : "#0e0e10", cursor: adding ? "default" : "pointer" }}>
            <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Add Player
          </button>
          {onDeleteRoster && <button onClick={() => setConfirmRosterDeleteOpen(true)} style={{ display: "inline-flex", alignItems: "center", gap: 8, borderRadius: 6, padding: "11px 18px", fontSize: 13, fontWeight: 700, fontFamily: "inherit", background: "#fff", border: "1px solid #f0c9c5", color: "#b81d14", cursor: "pointer" }}>
            <IconTrash size={14} style={{ strokeWidth: 2.2 }} /> Delete Roster
          </button>}
          <ListToolbar
            showSearch={false}
            sort={{ value: sort, options: ROSTER_SORT_OPTIONS, onChange: setSort, width: 248 }} />
        </div>
      </div>
      {/* Bulk selection bar — appears once one or more players are checked */}
      {selected.size > 0 &&
      <div style={{
        display: "flex", alignItems: "center", justifyContent: "space-between",
        background: "#f4f4f6", borderRadius: 40,
        padding: "16px 16px 16px 28px", marginBottom: 16, gap: 16
      }}>
          <span style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>
            {selected.size} Selected
          </span>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <button onClick={() => movableSelected > 0 && setMovePlayers(selectedPlayers)} disabled={movableSelected === 0}
            title={movableSelected === 0 ? "Every selected player has team-gear orders — those can't be moved" : ""} style={{
            display: "inline-flex", alignItems: "center", gap: 8,
            background: "#fff", border: "1px solid #e2e2e6", borderRadius: 6,
            padding: "12px 16px", fontSize: 14, fontWeight: 600, color: movableSelected === 0 ? "#b6b6bd" : "#0e0e10",
            cursor: movableSelected === 0 ? "not-allowed" : "pointer", fontFamily: "inherit"
          }}>
              <IconArrowRight size={15} style={{ strokeWidth: 2 }} /> Move to team
              {selected.size > 1 && <span style={{ fontWeight: 700, color: movableSelected === 0 ? "#b6b6bd" : "#6b6b72" }}>{movableSelected} of {selected.size}</span>}
            </button>
            <button onClick={() => setConfirmDeleteOpen(true)} style={{
            display: "inline-flex", alignItems: "center", gap: 8,
            background: "#fff", border: "1px solid #e2e2e6", borderRadius: 6,
            padding: "12px 16px", fontSize: 14, fontWeight: 600, color: "#0e0e10",
            cursor: "pointer", fontFamily: "inherit"
          }}>
              <IconTrash size={15} style={{ strokeWidth: 2 }} /> Delete
            </button>
            <button onClick={() => setShareOpen(true)} style={{
            display: "inline-flex", alignItems: "center", gap: 8,
            background: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6,
            padding: "12px 16px", fontSize: 14, fontWeight: 600, color: "#fff",
            cursor: "pointer", fontFamily: "inherit"
          }}>
              Share Team Store Link
            </button>
            <button onClick={clearSelection} aria-label="Clear selection" style={{
            width: 40, height: 40, borderRadius: 8, background: "transparent",
            border: 0, cursor: "pointer", color: "#0e0e10",
            display: "inline-flex", alignItems: "center", justifyContent: "center"
          }}>
              <IconX size={20} style={{ strokeWidth: 2 }} />
            </button>
          </div>
        </div>
      }

      <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 8, overflow: "visible" }}>
        {/* Header row */}
        <div style={{
          display: "grid", gridTemplateColumns: cols, gap: 16,
          padding: "16px 24px", borderBottom: "1px solid #ececf0",
          fontSize: 12, color: "#6b6b72", fontWeight: 600,
          letterSpacing: ".02em", alignItems: "center"
        }}>
          <RosterCheckbox checked={allChecked} indeterminate={someChecked} onChange={toggleAll} />
          <div>Player Name</div>
          <div>Jersey Number</div>
          <div>Name on Jersey</div>
          <div>Email</div>
          <div>Phone Number</div>
          {homeDelivery && <div>Player Address</div>}
          <div>Team Gear</div>
          <div></div>
        </div>

        {/* Body */}
        <div style={{ minHeight: 360, height: "700px" }}>
          {players.length === 0 && !adding &&
          <div style={{ padding: "120px 20px", textAlign: "center", color: "#9a9aa1", fontSize: 13 }}>
              No players yet — that's a normal state. Team-gear orders will fill this roster in, or add players with the Add Player button above / upload a CSV.
            </div>
          }
          {sortedPlayers.map((p) =>
          <div key={p.id} style={{
            display: "grid", gridTemplateColumns: cols, gap: 16,
            padding: "16px 24px", borderBottom: "1px solid #f3f3f4",
            fontSize: 14, color: "#0e0e10", alignItems: "center"
          }}>
                <RosterCheckbox checked={selected.has(p.id)} onChange={() => toggleOne(p.id)} />
                <div style={{ fontWeight: 600 }}>{p.first} {p.last}</div>
                <div>{p.number || "—"}</div>
                <div>{p.nameOnJersey || "—"}</div>
                <div style={{ color: "#9a9aa1" }}>{p.email || "—"}</div>
                <div>{p.phone || "—"}</div>
                {homeDelivery &&
            <div style={{ minWidth: 0 }}>
                  {p.shippingAddress ?
                <React.Fragment>
                      <div style={{ color: "#0e0e10", lineHeight: 1.35 }}>{p.shippingAddress}</div>
                      {p.addressFromCheckout &&
                  <span style={{
                    display: "inline-flex", alignItems: "center", gap: 4, marginTop: 4,
                    fontSize: 10.5, fontWeight: 700, letterSpacing: ".04em", textTransform: "uppercase",
                    color: "#2563eb", background: "#eef3ff", borderRadius: 999, padding: "2px 8px"
                  }}>
                        <IconCart size={11} style={{ strokeWidth: 2.2 }} /> From checkout
                      </span>
                  }
                    </React.Fragment> :
                <span style={{ color: "#c0c0c6", fontStyle: "italic" }}>Awaiting checkout</span>
                }
                </div>
            }
                <div>
                  {(() => { const g = playerTeamGear(p, store); return (
                    <button onClick={() => setGearPlayer(p)} style={{
                      background: "none", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit",
                      fontSize: 13, fontWeight: 600, color: "#2563eb", textAlign: "left", whiteSpace: "nowrap",
                    }}
                    onMouseEnter={(e) => e.currentTarget.style.textDecoration = "underline"}
                    onMouseLeave={(e) => e.currentTarget.style.textDecoration = "none"}>
                      {g.orderedCount}/{g.total} team gear items
                    </button>
                  ); })()}
                </div>
                <div style={{ position: "relative", justifySelf: "end" }}>
                  <button onClick={(e) => {e.stopPropagation();setOpenMenuId(openMenuId === p.id ? null : p.id);}}
              style={{
                width: 32, height: 32, borderRadius: 4, background: "transparent",
                border: 0, padding: 0, cursor: "pointer", color: "#6b6b72",
                display: "inline-flex", alignItems: "center", justifyContent: "center"
              }}
              title="Row actions">
                    <svg width={18} height={18} viewBox="0 0 24 24" fill="currentColor"><circle cx="5" cy="12" r="1.7" /><circle cx="12" cy="12" r="1.7" /><circle cx="19" cy="12" r="1.7" /></svg>
                  </button>
                  {openMenuId === p.id &&
              <RowMenu
                onEdit={() => {setEditingPlayer(p);setOpenMenuId(null);}}
                onShare={() => {shareStoreLink(p);setOpenMenuId(null);}}
                moveBlocked={playerTeamGear(p, store).orderedCount > 0}
                onMove={() => {setMovePlayers([p]);setOpenMenuId(null);}}
                onDelete={() => {deletePlayer(p.id);setOpenMenuId(null);}}
                onClose={() => setOpenMenuId(null)} />
              }
                </div>
              </div>
          )}

          {adding && <PlayerEditRow existing={players} cols={cols} homeDelivery={homeDelivery} allowDupes={allowDupes} onCancel={() => setAdding(false)} onSave={addPlayer} />}
        </div>

      </div>

      <RosterDangerConfirmModal
        open={confirmClearOpen}
        title="Clear this roster?"
        body={"All " + players.length + (players.length === 1 ? " player" : " players") + " will be removed from " + (rosterName || "this roster") + ". The roster itself is kept. This action can't be undone."}
        confirmLabel="Clear Roster"
        onClose={() => setConfirmClearOpen(false)}
        onConfirm={() => { const n = players.length; setPlayers([]); setSelected(new Set()); setConfirmClearOpen(false); setToast("Roster cleared — " + n + (n === 1 ? " player" : " players") + " removed"); }} />
      <RosterDangerConfirmModal
        open={confirmRosterDeleteOpen}
        title="Delete this roster?"
        body={(rosterName || "This roster") + " and its " + players.length + (players.length === 1 ? " player" : " players") + " will be permanently deleted. This action can't be undone."}
        confirmLabel="Delete Roster"
        onClose={() => setConfirmRosterDeleteOpen(false)}
        onConfirm={() => { setConfirmRosterDeleteOpen(false); onDeleteRoster && onDeleteRoster(); }} />
      <UploadRosterModal open={uploadOpen} onClose={() => setUploadOpen(false)} onImport={bulkAddPlayers} store={store} />

      <ShareRosterTemplateModal
        open={templateOpen}
        store={store}
        onClose={() => setTemplateOpen(false)}
        onSend={() => {setTemplateOpen(false);setToast("Roster template sent to team managers");}} />

      <ShareTeamStoreModal
        open={shareOpen}
        count={selected.size}
        emails={sharePool}
        onClose={() => setShareOpen(false)}
        onSend={() => {setShareOpen(false);setToast("Team store link shared");}} />

      <ConfirmDeleteModal
        open={confirmDeleteOpen}
        count={selected.size}
        onClose={() => setConfirmDeleteOpen(false)}
        onConfirm={() => {
          const n = selected.size;
          removeSelected();
          setConfirmDeleteOpen(false);
          setToast(`${n} player${n === 1 ? "" : "s"} removed`);
        }} />


      <MovePlayersModal
        open={!!movePlayers}
        players={movePlayers || []}
        current={{ id: rosterId, name: rosterName, groupName: (moveTargets || []).find((t) => t.id === rosterId)?.groupName || null }}
        targets={moveTargets || []}
        store={store}
        onClose={() => setMovePlayers(null)}
        onConfirm={doMove} />

      <PlayerDrawer
        player={editingPlayer}
        existing={players}
        allowDupes={allowDupes}
        homeDelivery={homeDelivery}
        onClose={() => setEditingPlayer(null)}
        onSave={updatePlayer}
        onShare={(p) => shareStoreLink(p)} />

      {gearPlayer &&
      <TeamGearDrawer
        player={gearPlayer}
        store={store}
        onClose={() => setGearPlayer(null)}
        onSendReminder={(p) => { setGearPlayer(null); setToast(`Reminder sent to ${p.first} ${p.last}`); }} />}

      <Toast message={toast} onDone={() => setToast("")} />
    </div>);
}

// Roster field validation: phone must be a 10-digit US number; email a basic
// valid address. Both are optional, but if entered they must be well-formed.
function formatPhone(raw) {
  const d = String(raw || "").replace(/\D/g, "").slice(0, 10);
  if (d.length <= 3) return d;
  if (d.length <= 6) return `(${d.slice(0, 3)}) ${d.slice(3)}`;
  return `(${d.slice(0, 3)}) ${d.slice(3, 6)}-${d.slice(6)}`;
}
function phoneDigits(v) {return String(v || "").replace(/\D/g, "");}
function isValidPhone(v) {return phoneDigits(v).length === 10;}
function isValidEmail(v) {return /^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(String(v || "").trim());}
// Jersey number is optional (Roster spec #12) but, when present, must be 0–99 and
// unique on the roster. Returns the colliding player, or null.
function rosterNumberConflict(number, existing, selfId) {
  const n = String(number || "").trim();
  if (!n) return null;
  return (existing || []).find((p) => p.id !== selfId && String(p.number || "").trim() === n) || null;
}

function PlayerEditRow({ initial, existing, cols = ROSTER_COLS, homeDelivery, onCancel, onSave, allowDupes }) {
  const [first, setFirst] = React.useState(initial?.first || "");
  const [last, setLast] = React.useState(initial?.last || "");
  const [number, setNumber] = React.useState(initial?.number || "");
  const [nameOnJersey, setNameOnJersey] = React.useState(initial?.nameOnJersey || "");
  const [email, setEmail] = React.useState(initial?.email || "");
  const [phone, setPhone] = React.useState(initial?.phone || "");
  const [address, setAddress] = React.useState(initial?.shippingAddress || "");
  const [validating, setValidating] = React.useState(false);
  const [addrErr, setAddrErr] = React.useState("");

  const numConflict = rosterNumberConflict(number, existing, initial?.id);
  // 7c — a taken number only blocks when duplicates are off for this team.
  const numBlocked = !!numConflict && !allowDupes;
  // Shipping address is OPTIONAL (mirrors Name on Jersey) — never blocks save.
  const canSave =
  first.trim() && last.trim() &&
  isValidEmail(email) && (
  !phone || isValidPhone(phone)) &&
  !numBlocked;

  async function commit() {
    if (!canSave || validating) return;
    let finalAddr = address.trim();
    // Direct-to-Home stores validate any entered address against a map service.
    if (homeDelivery && finalAddr) {
      setValidating(true);
      setAddrErr("");
      const res = await validateAddressService(finalAddr);
      setValidating(false);
      if (!res.ok) {
        setAddrErr("We couldn’t verify this address. Check it and try again — or leave it blank.");
        return;
      }
      finalAddr = res.normalized;
    }
    onSave({
      ...(initial || {}),
      first: first.trim(), last: last.trim(),
      number: number.trim(), nameOnJersey: nameOnJersey.trim(),
      email: email.trim(), phone: phone.trim(),
      shippingAddress: finalAddr
    });
  }

  function onKey(e) {
    if (e.key === "Enter") commit();
    if (e.key === "Escape") onCancel();
  }

  const showError = numBlocked || addrErr;

  return (
    <React.Fragment>
    <div onKeyDown={onKey} style={{
      display: "grid", gridTemplateColumns: cols, gap: 16,
      padding: "12px 24px", borderBottom: showError || validating ? "none" : "1px solid #f3f3f4",
      alignItems: "center", background: "#fafafb"
    }} data-tour="roster-player-fields">
      <div></div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 8 }}>
        <RosterInput value={first} onChange={setFirst} placeholder="First Name" autoFocus />
        <RosterInput value={last} onChange={setLast} placeholder="Last Name" />
      </div>
      <JerseyCellPicker value={number} onChange={setNumber} existing={existing} selfId={initial?.id} allowDupes={allowDupes} invalid={numBlocked} />
      <RosterInput value={nameOnJersey} onChange={setNameOnJersey} placeholder="Name on Jersey" />
      <RosterInput value={email} onChange={setEmail} placeholder="Email" type="email" inputMode="email" invalid={!!email && !isValidEmail(email)} />
      <RosterInput value={phone} onChange={(v) => setPhone(formatPhone(v))} placeholder="Phone Number" type="tel" inputMode="tel" maxLength={14} invalid={!!phone && !isValidPhone(phone)} />
      {homeDelivery &&
      <RosterInput value={address} onChange={(v) => { setAddress(v); if (addrErr) setAddrErr(""); }} placeholder="Player Shipping Address (optional)" invalid={!!addrErr} />
      }
      <div></div>
      <div style={{ display: "flex", gap: 8, justifySelf: "end" }}>
        <button onClick={commit} disabled={!canSave || validating} title="Save"
        style={{
          width: 36, height: 36, borderRadius: 4,
          background: canSave && !validating ? "#1f9d55" : "#cfead9",
          color: "#fff", border: 0, display: "inline-flex",
          alignItems: "center", justifyContent: "center",
          cursor: canSave && !validating ? "pointer" : "not-allowed"
        }}>
          <IconCheck size={16} style={{ strokeWidth: 2.6 }} />
        </button>
        <button onClick={onCancel} title="Cancel"
        style={{
          width: 36, height: 36, borderRadius: 4,
          background: "#fff", color: "#0e0e10",
          border: "1px solid #d4d4d8", display: "inline-flex",
          alignItems: "center", justifyContent: "center", cursor: "pointer"
        }}>
          <IconX size={14} style={{ strokeWidth: 2.4 }} />
        </button>
      </div>
    </div>
    {numConflict &&
      <div style={{
        display: "flex", alignItems: "center", gap: 8,
        padding: "8px 24px 12px", background: "#fafafb",
        borderBottom: "1px solid #f3f3f4",
        fontSize: 12.5, fontWeight: 600, color: numBlocked ? "#b81d14" : "#6b6b72"
      }}>
        {numBlocked
          ? <React.Fragment><IconAlert size={14} style={{ strokeWidth: 2.2, flexShrink: 0 }} />Number {number} is taken by {numConflict.first} {numConflict.last} — locked while duplicates are off</React.Fragment>
          : <React.Fragment><IconInfo size={14} style={{ strokeWidth: 2, flexShrink: 0 }} />Number {number} · duplicate — also worn by {numConflict.first} {numConflict.last}</React.Fragment>}
      </div>
    }
    {!numConflict && validating &&
      <div style={{
        display: "flex", alignItems: "center", gap: 8,
        padding: "8px 24px 12px", background: "#fafafb",
        borderBottom: "1px solid #f3f3f4",
        fontSize: 12.5, fontWeight: 600, color: "#6b6b72"
      }}>
        <IconPin size={14} style={{ strokeWidth: 2.2, flexShrink: 0 }} />
        Verifying address with map service…
      </div>
    }
    {!numConflict && !validating && addrErr &&
      <div style={{
        display: "flex", alignItems: "center", gap: 8,
        padding: "8px 24px 12px", background: "#fafafb",
        borderBottom: "1px solid #f3f3f4",
        fontSize: 12.5, fontWeight: 600, color: "#b81d14"
      }}>
        <IconAlert size={14} style={{ strokeWidth: 2.2, flexShrink: 0 }} />
        {addrErr}
      </div>
    }
    </React.Fragment>);
}

function RosterInput({ value, onChange, placeholder, type = "text", autoFocus, invalid, inputMode, maxLength }) {
  const [focus, setFocus] = React.useState(false);
  const borderColor = invalid ? "#e1251b" : focus ? "#0e0e10" : "#d4d4d8";
  return (
    <input
      type={type}
      value={value}
      placeholder={placeholder}
      autoFocus={autoFocus}
      inputMode={inputMode}
      maxLength={maxLength}
      aria-invalid={invalid ? "true" : undefined}
      onChange={(e) => onChange(e.target.value)}
      onFocus={() => setFocus(true)}
      onBlur={() => setFocus(false)}
      style={{
        width: "100%", boxSizing: "border-box",
        padding: "8px 12px", fontSize: 13, fontFamily: "inherit",
        background: invalid ? "#fdf2f1" : "#fff",
        border: `1px solid ${borderColor}`,
        borderRadius: 4, color: "#0e0e10", outline: "none"
      }} />);
}

function RosterCheckbox({ checked, indeterminate, onChange }) {
  return (
    <button onClick={onChange} aria-pressed={!!checked} style={{
      width: 20, height: 20, borderRadius: 3,
      background: checked || indeterminate ? "#0e0e10" : "#fff",
      border: `1.5px solid ${checked || indeterminate ? "#0e0e10" : "#c4c4c8"}`,
      cursor: "pointer", padding: 0,
      display: "inline-flex", alignItems: "center", justifyContent: "center"
    }}>
      {checked && !indeterminate &&
      <svg width={12} height={12} viewBox="0 0 24 24" fill="none" stroke="#fff" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round"><path d="M4 12l5 5L20 6" /></svg>
      }
      {indeterminate &&
      <div style={{ width: 12, height: 2, background: "#fff", borderRadius: 1 }} />
      }
    </button>);
}

function RowMenu({ onEdit, onShare, onDelete, onClose, onMove, moveBlocked }) {
  React.useEffect(() => {
    const handler = () => onClose();
    const t = setTimeout(() => document.addEventListener("click", handler), 0);
    return () => {clearTimeout(t);document.removeEventListener("click", handler);};
  }, [onClose]);
  return (
    <div onClick={(e) => e.stopPropagation()} style={{
      position: "absolute", right: 0, top: 36,
      background: "#fff", border: "1px solid #ececf0",
      borderRadius: 6, boxShadow: "0 10px 28px rgba(0,0,0,.10)",
      minWidth: 196, zIndex: 20, overflow: "hidden"
    }}>
      <MenuItem onClick={onEdit} icon={<IconEdit size={14} />}>Edit Player</MenuItem>
      {onMove && <MenuItem onClick={onMove} icon={<IconArrowRight size={14} />}
        disabled={!!moveBlocked} title={moveBlocked ? "Team-gear orders already placed — cancel or fulfil them first" : "Move this player to another team in this group"}>
        {moveBlocked ? "Move to different team — has orders" : "Move to different team"}
      </MenuItem>}
      <MenuItem onClick={onShare} icon={<ShareLinkIcon size={14} />}>Share Store Link</MenuItem>
      <MenuItem onClick={onDelete} icon={<IconTrash size={14} />} danger>Delete Player</MenuItem>
    </div>);
}

const ShareLinkIcon = ({ size = 14 }) =>
<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M10 13a5 5 0 0 0 7 0l3-3a5 5 0 1 0-7-7l-1 1" />
    <path d="M14 11a5 5 0 0 0-7 0l-3 3a5 5 0 1 0 7 7l1-1" />
  </svg>;

// ─────────── Edit Player drawer ───────────
function PlayerDrawer({ player, existing, homeDelivery, onClose, onSave, onShare, allowDupes }) {
  const [first, setFirst] = React.useState("");
  const [last, setLast] = React.useState("");
  const [nameOnJersey, setNameOnJersey] = React.useState("");
  const [number, setNumber] = React.useState("");
  const [phone, setPhone] = React.useState("");
  const [email, setEmail] = React.useState("");
  const [associatedEmails, setAssociatedEmails] = React.useState([]);
  const [address, setAddress] = React.useState("");
  const [validating, setValidating] = React.useState(false);
  const [addrErr, setAddrErr] = React.useState("");

  React.useEffect(() => {
    if (!player) return;
    setFirst(player.first || "");
    setLast(player.last || "");
    setNameOnJersey(player.nameOnJersey || "");
    setNumber(player.number || "");
    setPhone(player.phone || "");
    setEmail(player.email || "");
    setAssociatedEmails(player.associatedEmails || []);
    setAddress(player.shippingAddress || "");
    setAddrErr("");
    setValidating(false);
  }, [player]);

  React.useEffect(() => {
    if (!player) return;
    const h = (e) => {if (e.key === "Escape") onClose();};
    window.addEventListener("keydown", h);
    return () => window.removeEventListener("keydown", h);
  }, [player, onClose]);

  async function commit() {
    if ((!allowDupes && rosterNumberConflict(number, existing, player?.id)) || validating) return;
    let finalAddr = address.trim();
    // Direct-to-Home stores validate any entered address against a map service.
    if (homeDelivery && finalAddr && finalAddr !== (player?.shippingAddress || "").trim()) {
      setValidating(true);
      setAddrErr("");
      const res = await validateAddressService(finalAddr);
      setValidating(false);
      if (!res.ok) {
        setAddrErr("We couldn’t verify this address. Check it and try again — or leave it blank.");
        return;
      }
      finalAddr = res.normalized;
    }
    onSave({
      ...player,
      first: first.trim(), last: last.trim(),
      nameOnJersey: nameOnJersey.trim(), number: number.trim(),
      phone: phone.trim(), email: email.trim(),
      shippingAddress: finalAddr,
      // A rep-verified edit clears the “from checkout” origin flag.
      addressFromCheckout: finalAddr && finalAddr !== (player?.shippingAddress || "").trim() ? false : player?.addressFromCheckout,
      associatedEmails
    });
  }

  function removeAssociated(em) {
    setAssociatedEmails((cur) => cur.filter((x) => x !== em));
  }

  const open = !!player;

  return (
    <>
      {/* Scrim */}
      <div onClick={onClose} style={{
        position: "fixed", inset: 0, zIndex: 90,
        background: open ? "rgba(20,20,30,.40)" : "transparent",
        opacity: open ? 1 : 0, pointerEvents: open ? "auto" : "none",
        transition: "opacity .2s ease"
      }} />

      {/* Panel */}
      <aside style={{
        position: "fixed", top: 0, right: 0, bottom: 0, width: 480, maxWidth: "96vw",
        background: "#fff", zIndex: 100,
        boxShadow: "-12px 0 40px rgba(20,20,30,.18)",
        transform: open ? "translateX(0)" : "translateX(100%)",
        transition: "transform .26s cubic-bezier(.2,.85,.3,1)",
        display: "flex", flexDirection: "column"
      }}>
        {/* Header */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          padding: "24px 28px", borderBottom: "1px solid #ececf0"
        }}>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Edit Player</h2>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, padding: 8, cursor: "pointer",
            color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center"
          }}>
            <IconX size={20} style={{ strokeWidth: 2 }} />
          </button>
        </div>

        {/* Body */}
        <div style={{ flex: 1, overflowY: "auto", padding: "24px 28px", display: "flex", flexDirection: "column", gap: 32 }}>
          <TextField label="First Name" value={first} onChange={setFirst} />
          <TextField label="Last Name" value={last} onChange={setLast} />
          <TextField label="Name on Jersey" value={nameOnJersey} onChange={setNameOnJersey} />
          <div>
            <div style={{ fontSize: 11, fontWeight: 500, color: "#6b6b72", marginBottom: 8, paddingLeft: 4 }}>Jersey Number</div>
            <JerseyNumberField value={number} onChange={(v) => setNumber(String(v).replace(/\D/g, "").slice(0, 2))}
              existing={existing} selfId={player?.id} allowDupes={allowDupes} />
          </div>

          <div>
            <div style={{ fontSize: 11, fontWeight: 500, color: "#6b6b72", marginBottom: 8, paddingLeft: 4 }}>Phone Number</div>
            <PhoneField value={phone} onChange={setPhone} />
          </div>

          <TextField label="Email" value={email} onChange={setEmail} type="email" />

          {homeDelivery &&
          <div>
            <div style={{ fontSize: 11, fontWeight: 500, color: "#6b6b72", marginBottom: 8, paddingLeft: 4 }}>
              Player Shipping Address <span style={{ color: "#9a9aa1", fontWeight: 400 }}>(optional)</span>
            </div>
            <TextField value={address} onChange={(v) => { setAddress(v); if (addrErr) setAddrErr(""); }} placeholder="Street, City, State ZIP" />
            {validating &&
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 8, paddingLeft: 4, fontSize: 12.5, fontWeight: 600, color: "#6b6b72" }}>
              <IconPin size={13} style={{ strokeWidth: 2.2, flexShrink: 0 }} /> Verifying address with map service…
            </div>
            }
            {!validating && addrErr &&
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 8, paddingLeft: 4, fontSize: 12.5, fontWeight: 600, color: "#b81d14" }}>
              <IconAlert size={13} style={{ strokeWidth: 2.2, flexShrink: 0 }} /> {addrErr}
            </div>
            }
            {!validating && !addrErr && player?.addressFromCheckout && address === (player?.shippingAddress || "") &&
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 8, paddingLeft: 4, fontSize: 12, fontWeight: 600, color: "#2563eb" }}>
              <IconCart size={13} style={{ strokeWidth: 2.2, flexShrink: 0 }} /> Provided by the player at checkout
            </div>
            }
          </div>
          }

          <div>
            <div style={{ fontSize: 11, fontWeight: 500, color: "#6b6b72", marginBottom: 8, paddingLeft: 4 }}>Associated Emails</div>
            <div style={{
              border: "1px solid #d4d4d8", borderRadius: 4, background: "#fff",
              minHeight: 48, padding: 8, display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center"
            }}>
              {associatedEmails.length === 0 &&
              <span style={{ color: "#9a9aa1", fontSize: 13, padding: "0 8px" }}>No associated emails</span>
              }
              {associatedEmails.map((em) =>
              <span key={em} style={{
                display: "inline-flex", alignItems: "center", gap: 8,
                background: "#f1f1f3", borderRadius: 999,
                padding: "8px 8px 8px 12px", fontSize: 13, color: "#0e0e10"
              }}>
                  {em}
                  <button onClick={() => removeAssociated(em)} aria-label={`Remove ${em}`} style={{
                  width: 16, height: 16, borderRadius: "50%",
                  background: "#9a9aa1", color: "#fff", border: 0,
                  display: "inline-flex", alignItems: "center", justifyContent: "center",
                  cursor: "pointer", padding: 0
                }}>
                    <IconX size={10} style={{ strokeWidth: 3 }} />
                  </button>
                </span>
              )}
            </div>
          </div>
        </div>

        {/* Footer */}
        <div style={{
          padding: "16px 28px", borderTop: "1px solid #ececf0",
          display: "flex", justifyContent: "space-between", gap: 12, background: "#fff"
        }}>
          <Button variant="secondary" onClick={() => onShare(player)}>Share Link</Button>
          <Button variant="primary" onClick={commit} disabled={(!allowDupes && !!rosterNumberConflict(number, existing, player?.id)) || validating}>{validating ? "Verifying…" : "Save Changes"}</Button>
        </div>
      </aside>
    </>);
}

function MenuItem({ icon, children, onClick, danger, disabled, title }) {
  return (
    <button onClick={disabled ? undefined : onClick} disabled={disabled} title={title} style={{
      display: "flex", alignItems: "center", gap: 12,
      padding: "12px 16px", fontSize: 13,
      color: disabled ? "#b6b6bd" : danger ? "#b81d14" : "#0e0e10",
      background: "transparent", border: 0,
      width: "100%", textAlign: "left", cursor: disabled ? "not-allowed" : "pointer",
      fontFamily: "inherit"
    }}>
      {icon}{children}
    </button>);
}

// ─────────── Confirm delete players modal ───────────
function ConfirmDeleteModal({ open, count, onClose, onConfirm }) {
  const noun = count === 1 ? "this player" : `these ${count} players`;
  return (
    <Modal open={open} onClose={onClose} width={440}>
      <div style={{ padding: "28px 28px 24px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
          <div style={{
            width: 44, height: 44, borderRadius: "50%", flexShrink: 0,
            background: "#fdecec", color: "#b81d14",
            display: "inline-flex", alignItems: "center", justifyContent: "center"
          }}>
            <IconTrash size={20} style={{ strokeWidth: 2 }} />
          </div>
          <div>
            <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>
              Delete {count === 1 ? "player" : `${count} players`}?
            </h2>
            <p style={{ margin: "8px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>
              Are you sure you want to delete {noun} from the roster? This action can't be undone.
            </p>
          </div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
          <button onClick={onClose} style={{
            background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
            color: "#0e0e10", cursor: "pointer", fontFamily: "inherit"
          }}>
            Cancel
          </button>
          <button onClick={onConfirm} style={{
            background: "#b81d14", border: "1px solid #b81d14", borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
            color: "#fff", cursor: "pointer", fontFamily: "inherit"
          }}>
            Delete
          </button>
        </div>
      </div>
    </Modal>);
}

// ─────────── Confirm clear / delete roster modal ───────────
function RosterDangerConfirmModal({ open, title, body, confirmLabel, onClose, onConfirm }) {
  return (
    <Modal open={open} onClose={onClose} width={440}>
      <div style={{ padding: "28px 28px 24px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
          <div style={{ width: 44, height: 44, borderRadius: "50%", flexShrink: 0, background: "#fdecec", color: "#b81d14", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
            <IconTrash size={20} style={{ strokeWidth: 2 }} />
          </div>
          <div>
            <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>{title}</h2>
            <p style={{ margin: "8px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>{body}</p>
          </div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, marginTop: 28 }}>
          <button onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
          <button onClick={onConfirm} style={{ background: "#b81d14", border: "1px solid #b81d14", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em", color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>{confirmLabel}</button>
        </div>
      </div>
    </Modal>);
}

// ─────────── Share Team Store Link modal ───────────
const SHARE_DEFAULT_MESSAGE =
"Hi Team,\n\nIt's that time of year! Our official team store is now open and ready for orders. We've made it easy to get everything you need for the upcoming season in one place. Whether you're picking up required uniforms or grabbing some new fan gear to wear in the stands, just follow the link below to get started.";

function ShareTeamStoreModal({ open, count, emails, onClose, onSend }) {
  const [chips, setChips] = React.useState([]);
  const [draft, setDraft] = React.useState("");
  const [subject, setSubject] = React.useState("");
  const [message, setMessage] = React.useState(SHARE_DEFAULT_MESSAGE);

  React.useEffect(() => {
    if (open) {
      setChips(emails && emails.length ? emails : []);
      setDraft("");
      setSubject("");
      setMessage(SHARE_DEFAULT_MESSAGE);
    }
  }, [open]);

  function addDraft() {
    const v = draft.trim().replace(/,$/, "");
    if (v && !chips.includes(v)) setChips((c) => [...c, v]);
    setDraft("");
  }
  function onDraftKey(e) {
    if (e.key === "Enter" || e.key === ",") {e.preventDefault();addDraft();}
    if (e.key === "Backspace" && !draft && chips.length) setChips((c) => c.slice(0, -1));
  }
  function removeChip(em) {setChips((c) => c.filter((x) => x !== em));}

  const canSend = chips.length > 0;

  return (
    <Modal open={open} onClose={onClose} width={620}>
      <div style={{ padding: "28px 28px 0", display: "flex", alignItems: "center", justifyContent: "space-between" }}>
        <h2 style={{ margin: 0, fontSize: 20, fontWeight: 800 }}>Share Team Store Link</h2>
        <button onClick={onClose} aria-label="Close" style={{
          background: "transparent", border: 0, padding: 8, cursor: "pointer", color: "#0e0e10",
          display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}>
          <IconX size={22} style={{ strokeWidth: 2 }} />
        </button>
      </div>

      <div style={{ padding: "24px 28px 24px", display: "flex", flexDirection: "column", gap: 20 }}>
        {/* Associated Emails */}
        <div>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>
            Associated Emails <span style={{ color: "#b81d14" }}>*</span>
          </div>
          <div style={{
            border: "1px solid #d4d4d8", borderRadius: 8, background: "#fff",
            minHeight: 52, padding: 8, display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center"
          }}>
            {chips.map((em) =>
            <span key={em} style={{
              display: "inline-flex", alignItems: "center", gap: 8,
              background: "#f1f1f3", borderRadius: 999,
              padding: "8px 8px 8px 16px", fontSize: 13, color: "#0e0e10"
            }}>
                {em}
                <button onClick={() => removeChip(em)} aria-label={`Remove ${em}`} style={{
                width: 16, height: 16, borderRadius: "50%",
                background: "#9a9aa1", color: "#fff", border: 0,
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                cursor: "pointer", padding: 0
              }}>
                  <IconX size={10} style={{ strokeWidth: 3 }} />
                </button>
              </span>
            )}
            <input
              value={draft}
              onChange={(e) => setDraft(e.target.value)}
              onKeyDown={onDraftKey}
              onBlur={addDraft}
              placeholder={chips.length ? "" : "Add email and press Enter"}
              style={{
                flex: 1, minWidth: 140, border: 0, outline: 0, background: "transparent",
                padding: "8px 4px", fontSize: 13, color: "#0e0e10", fontFamily: "inherit"
              }} />
          </div>
        </div>

        {/* Subject */}
        <div>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Subject</div>
          <input
            value={subject}
            onChange={(e) => setSubject(e.target.value)}
            placeholder="Ready to gear up for the [Team Name] season? 🏆"
            style={{
              width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 8,
              padding: "12px 16px", fontSize: 14, color: "#0e0e10", outline: "none",
              fontFamily: "inherit", background: "#fff"
            }} />
        </div>

        {/* Message */}
        <div>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Message</div>
          <textarea
            value={message}
            onChange={(e) => setMessage(e.target.value)}
            rows={6}
            style={{
              width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 8,
              padding: "12px 16px", fontSize: 14, color: "#0e0e10", outline: "none",
              fontFamily: "inherit", background: "#fff", resize: "vertical", lineHeight: 1.55
            }} />
        </div>
      </div>

      <div style={{ padding: "0 28px 28px", display: "flex", justifyContent: "flex-end" }}>
        <button onClick={onSend} disabled={!canSend} style={{
          background: canSend ? "#0e0e10" : "#cfcfd4", color: "#fff", border: 0,
          borderRadius: 999, padding: "12px 40px", fontSize: 15, fontWeight: 700,
          cursor: canSend ? "pointer" : "not-allowed", fontFamily: "inherit"
        }}>
          Send
        </button>
      </div>
    </Modal>);
}

// ─────────── Share Roster Template modal ───────────
// Standalone workflow: emails a blank roster template to team managers so they
// can fill in player details and return it for import. The [Rep Name] signature
// is pulled from the active logged-in platform user shown in the header.
const REP_NAME = "George";
const TEMPLATE_FILE_NAME = "prolook_roster_template.csv";
const TEMPLATE_DEFAULT_SUBJECT = "Action Required: Roster Template for Team Store Setup";
const TEMPLATE_DEFAULT_MESSAGE =
`Hi Team Manager,

Attached is the roster template file for your upcoming team store.

Please open the attached template and fill in your players' details (Names, Jersey Numbers, and Contact Info). Once completed, please reply directly to this email or send the finalized file back to me so I can import your roster and launch your store.

Let me know if you have any questions!

Best regards,
${REP_NAME}`;

function ShareRosterTemplateModal({ open, store, onClose, onSend }) {
  const [chips, setChips] = React.useState([]);
  const [overflow, setOverflow] = React.useState(0);
  const [draft, setDraft] = React.useState("");
  const [subject, setSubject] = React.useState(TEMPLATE_DEFAULT_SUBJECT);
  const [message, setMessage] = React.useState(TEMPLATE_DEFAULT_MESSAGE);

  React.useEffect(() => {
    if (open) {
      // Seed with dummy recipient chips + a "3+" overflow counter for multi-recipient styling.
      setChips(["susanQuinones@gmail.com", "marcusReed@outlook.com"]);
      setOverflow(3);
      setDraft("");
      setSubject(TEMPLATE_DEFAULT_SUBJECT);
      setMessage(TEMPLATE_DEFAULT_MESSAGE);
    }
  }, [open]);

  function addDraft() {
    const v = draft.trim().replace(/,$/, "");
    if (v && !chips.includes(v)) setChips((c) => [...c, v]);
    setDraft("");
  }
  function onDraftKey(e) {
    if (e.key === "Enter" || e.key === ",") {e.preventDefault();addDraft();}
    if (e.key === "Backspace" && !draft && chips.length) setChips((c) => c.slice(0, -1));
  }
  function removeChip(em) {setChips((c) => c.filter((x) => x !== em));}

  const canSend = chips.length > 0 || overflow > 0;

  return (
    <Modal open={open} onClose={onClose} width={620}>
      {/* Header & context */}
      <div style={{ padding: "28px 28px 16px", borderBottom: "1px solid #ececf0" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 16 }}>
          <h2 style={{ margin: 0, fontSize: 20, fontWeight: 800 }}>Share Roster Template</h2>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, padding: 4, cursor: "pointer", color: "#0e0e10",
            display: "inline-flex", alignItems: "center", justifyContent: "center", marginTop: 2
          }}>
            <IconX size={22} style={{ strokeWidth: 2 }} />
          </button>
        </div>
        <p style={{ margin: "8px 0 0", fontSize: 13, color: "#9a9aa1", lineHeight: 1.55, maxWidth: 520, textWrap: "pretty" }}>
          Send a blank roster template to your team managers. They can fill out their players' information and return it to you for rapid roster import.
        </p>
      </div>

      <div style={{ padding: "24px 28px 24px", display: "flex", flexDirection: "column", gap: 20 }}>
        {/* Associated Emails */}
        <div>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>
            Associated Emails <span style={{ color: "#b81d14" }}>*</span>
          </div>
          <div style={{
            border: "1px solid #d4d4d8", borderRadius: 8, background: "#fff",
            minHeight: 52, padding: 8, display: "flex", flexWrap: "wrap", gap: 8, alignItems: "center"
          }}>
            {chips.map((em) =>
            <span key={em} style={{
              display: "inline-flex", alignItems: "center", gap: 8,
              background: "#f1f1f3", borderRadius: 999,
              padding: "8px 8px 8px 16px", fontSize: 13, color: "#0e0e10"
            }}>
                {em}
                <button onClick={() => removeChip(em)} aria-label={`Remove ${em}`} style={{
                width: 16, height: 16, borderRadius: "50%",
                background: "#9a9aa1", color: "#fff", border: 0,
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                cursor: "pointer", padding: 0
              }}>
                  <IconX size={10} style={{ strokeWidth: 3 }} />
                </button>
              </span>
            )}
            {overflow > 0 &&
            <span style={{
              display: "inline-flex", alignItems: "center",
              background: "#ececf0", borderRadius: 999,
              padding: "8px 16px", fontSize: 13, fontWeight: 600, color: "#6b6b72"
            }}>
                {overflow}+
              </span>
            }
            <input
              value={draft}
              onChange={(e) => setDraft(e.target.value)}
              onKeyDown={onDraftKey}
              onBlur={addDraft}
              placeholder={chips.length ? "" : "Add email and press Enter"}
              style={{
                flex: 1, minWidth: 140, border: 0, outline: 0, background: "transparent",
                padding: "8px 4px", fontSize: 13, color: "#0e0e10", fontFamily: "inherit"
              }} />
          </div>
        </div>

        {/* Subject */}
        <div>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Subject</div>
          <input
            value={subject}
            onChange={(e) => setSubject(e.target.value)}
            style={{
              width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 8,
              padding: "12px 16px", fontSize: 14, color: "#0e0e10", outline: "none",
              fontFamily: "inherit", background: "#fff"
            }} />
        </div>

        {/* Message */}
        <div>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Message</div>
          <textarea
            value={message}
            onChange={(e) => setMessage(e.target.value)}
            rows={9}
            style={{
              width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 8,
              padding: "12px 16px", fontSize: 14, color: "#0e0e10", outline: "none",
              fontFamily: "inherit", background: "#fff", resize: "vertical", lineHeight: 1.55
            }} />
        </div>
      </div>

      {/* Footer action bar: attachment (left) + Send (right) */}
      <div style={{
        padding: "16px 28px 24px", borderTop: "1px solid #ececf0",
        display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16
      }}>
        <div style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, color: "#0e0e10", minWidth: 0 }}>
          <IconPaperclip size={16} style={{ strokeWidth: 2, color: "#6b6b72", flexShrink: 0 }} />
          <span style={{ fontWeight: 600, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
            {TEMPLATE_FILE_NAME}
          </span>
        </div>
        <button onClick={onSend} disabled={!canSend} style={{
          background: canSend ? "#0e0e10" : "#cfcfd4", color: "#fff", border: 0,
          borderRadius: 6, padding: "12px 36px", fontSize: 15, fontWeight: 700,
          cursor: canSend ? "pointer" : "not-allowed", fontFamily: "inherit", flexShrink: 0
        }}>
          Send
        </button>
      </div>
    </Modal>);
}

// ─────────── Upload Roster modal ───────────
function UploadRosterModal({ open, onClose, onImport, store }) {
  const [phase, setPhase] = React.useState("idle"); // "idle" | "uploading" | "done"
  const [fileName, setFileName] = React.useState("");
  const [progress, setProgress] = React.useState(0);
  const [parsed, setParsed] = React.useState([]);
  const [dragOver, setDragOver] = React.useState(false);
  const inputRef = React.useRef(null);
  const timerRef = React.useRef(null);

  React.useEffect(() => {
    if (!open) {
      clearInterval(timerRef.current);
      setPhase("idle"); setFileName(""); setProgress(0); setParsed([]); setDragOver(false);
    }
  }, [open]);

  const report = React.useMemo(() => validateRosterRows(parsed), [parsed]);

  const TEMPLATE_CSV =
    "First Name,Last Name,Jersey Number,Name on Jersey,Email,Phone Number\n" +
    "Jalen,Hurts,1,Hurtski,jalen@example.com,202-111-5555\n";
  const templateHref = "data:text/csv;charset=utf-8," + encodeURIComponent(TEMPLATE_CSV);

  function handleFile(file) {
    if (!file) return;
    clearInterval(timerRef.current);
    setFileName(file.name);
    setPhase("uploading");
    setProgress(0);
    let prog = 0;
    timerRef.current = setInterval(() => {
      prog += 10 + Math.random() * 20;
      if (prog >= 100) {
        prog = 100;
        clearInterval(timerRef.current);
        const reader = new FileReader();
        reader.onload = () => {
          const rows = parseRosterCSV(String(reader.result || ""));
          setParsed(rows);
          setTimeout(() => setPhase("done"), 220);
        };
        reader.readAsText(file);
      }
      setProgress(Math.min(prog, 100));
    }, 110);
  }

  function clearFile() {
    clearInterval(timerRef.current);
    setPhase("idle"); setFileName(""); setProgress(0); setParsed([]);
  }

  const PREVIEW_COLS = [
    { key: "first", label: "First" },
    { key: "last", label: "Last" },
    { key: "number", label: "#" },
    { key: "nameOnJersey", label: "Name on Jersey" },
    { key: "email", label: "Email" },
  ];

  // Shared file chip used in uploading + done phases
  const FileChip = () => (
    <div style={{ display: "flex", alignItems: "center", gap: 12, background: "#f3f3f4", borderRadius: 8, padding: "12px 16px" }}>
      <div style={{
        width: 36, height: 36, borderRadius: 7, background: "#e8e8eb",
        display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0
      }}>
        <svg width="15" height="15" viewBox="0 0 15 15" fill="none">
          <path d="M2.5 1.5h7l3 3v9h-10v-12z" stroke="#0e0e10" strokeWidth="1.3" fill="none" strokeLinejoin="round"/>
          <path d="M9.5 1.5v3h3" stroke="#0e0e10" strokeWidth="1.3" strokeLinejoin="round"/>
        </svg>
      </div>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{fileName}</div>
        {phase === "done" && (
          <div style={{ fontSize: 11.5, color: "#6b6b72", marginTop: 1 }}>
            {parsed.length} row{parsed.length === 1 ? "" : "s"} · {report.valid.length} valid
            {report.errors.length > 0 && ` · ${report.errors.length} skipped`}
          </div>
        )}
      </div>
      <button onClick={clearFile} style={{ background: "transparent", border: 0, padding: 4, cursor: "pointer", color: "#9a9aa1", display: "inline-flex", borderRadius: 4, flexShrink: 0 }}>
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none"><path d="M10.5 3.5L3.5 10.5M3.5 3.5l7 7" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round"/></svg>
      </button>
    </div>
  );

  return (
    <Modal open={open} onClose={onClose} width={560}>
      {/* ── Header ── */}
      <div style={{ padding: "24px 28px 16px", borderBottom: "1px solid #ececf0" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Upload Roster</h2>
          <button onClick={onClose} style={{ background: "transparent", border: 0, padding: 4, cursor: "pointer", color: "#6b6b72", borderRadius: 4, display: "inline-flex" }}>
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none"><path d="M12 4L4 12M4 4l8 8" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"/></svg>
          </button>
        </div>
      </div>

      {/* ── Body ── */}
      <div style={{ padding: "24px 28px" }}>

        {/* ── idle: drop zone ── */}
        {phase === "idle" && (
          <>
            <div
              onClick={() => inputRef.current?.click()}
              onDragOver={(e) => { e.preventDefault(); setDragOver(true); }}
              onDragLeave={() => setDragOver(false)}
              onDrop={(e) => { e.preventDefault(); setDragOver(false); handleFile(e.dataTransfer.files?.[0]); }}
              style={{
                border: `1.5px dashed ${dragOver ? "#0e0e10" : "#d4d4d8"}`,
                borderRadius: 10, padding: "36px 20px",
                display: "flex", flexDirection: "column", alignItems: "center", gap: 12,
                cursor: "pointer", background: dragOver ? "#f7f7f8" : "#fafafb",
                textAlign: "center", transition: "all .12s"
              }}>
              <div style={{
                width: 48, height: 48, borderRadius: "50%",
                background: "#fff", border: "1px solid #ececf0",
                display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#0e0e10"
              }}>
                <IconUpload size={20} style={{ strokeWidth: 2 }} />
              </div>
              <div>
                <div style={{ fontSize: 14, fontWeight: 600, color: "#0e0e10" }}>Drop CSV here or click to browse</div>
                <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 4 }}>CSV files only</div>
              </div>
              <input ref={inputRef} type="file" accept=".csv,text/csv" style={{ display: "none" }}
                onChange={(e) => handleFile(e.target.files?.[0])} />
            </div>
            <div style={{ marginTop: 16 }}>
              <a href={templateHref} download="roster-template.csv"
                style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12.5, color: "#0e0e10", textDecoration: "none", fontWeight: 600 }}
                onMouseEnter={(e) => e.currentTarget.style.color = "#e1251b"}
                onMouseLeave={(e) => e.currentTarget.style.color = "#0e0e10"}>
                <IconDownload size={14} style={{ strokeWidth: 2 }} />
                <span style={{ textDecoration: "underline", textUnderlineOffset: 2 }}>Download CSV template</span>
              </a>
            </div>
          </>
        )}

        {/* ── uploading: progress ── */}
        {phase === "uploading" && (
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            <FileChip />
            <div>
              <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: 8 }}>
                <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>Uploading…</span>
                <span style={{ fontSize: 12, color: "#6b6b72" }}>{Math.round(progress)}%</span>
              </div>
              <div style={{ height: 8, background: "#ececf0", borderRadius: 99, overflow: "hidden" }}>
                <div style={{ height: "100%", width: `${progress}%`, background: "#0e0e10", borderRadius: 99, transition: "width .1s linear" }}></div>
              </div>
            </div>
          </div>
        )}

        {/* ── done: preview table + errors ── */}
        {phase === "done" && (
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            <FileChip />
            {report.valid.length > 0 && (
              <div style={{ border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden" }}>
                <div style={{ overflowX: "auto", maxHeight: 232 }}>
                  <table style={{ width: "100%", borderCollapse: "collapse", fontSize: 12 }}>
                    <thead>
                      <tr style={{ background: "#f7f7f8", borderBottom: "1px solid #ececf0" }}>
                        {PREVIEW_COLS.map(c => (
                          <th key={c.key} style={{ padding: "8px 12px", textAlign: "left", fontWeight: 600, color: "#6b6b72", whiteSpace: "nowrap", fontSize: 11 }}>{c.label}</th>
                        ))}
                      </tr>
                    </thead>
                    <tbody>
                      {report.valid.slice(0, 8).map((row, i) => (
                        <tr key={i} style={{ borderBottom: i < Math.min(report.valid.length, 8) - 1 ? "1px solid #f3f3f4" : "none" }}>
                          {PREVIEW_COLS.map(c => (
                            <td key={c.key} style={{ padding: "8px 12px", color: "#0e0e10", maxWidth: 120, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                              {row[c.key] || <span style={{ color: "#c4c4ca" }}>—</span>}
                            </td>
                          ))}
                        </tr>
                      ))}
                      {report.valid.length > 8 && (
                        <tr><td colSpan={PREVIEW_COLS.length} style={{ padding: "8px 12px", color: "#9a9aa1", fontSize: 11, fontStyle: "italic" }}>+{report.valid.length - 8} more players</td></tr>
                      )}
                    </tbody>
                  </table>
                </div>
              </div>
            )}
            {report.errors.length > 0 && (
              <div style={{ border: "1px solid #f3c9c5", borderRadius: 8, background: "#fdf2f1", overflow: "hidden" }}>
                <div style={{ display: "flex", alignItems: "center", gap: 8, padding: "12px 16px", borderBottom: "1px solid #f3c9c5", fontSize: 12.5, fontWeight: 700, color: "#b81d14" }}>
                  <IconAlert size={14} style={{ strokeWidth: 2.2 }} />
                  {report.errors.length} row{report.errors.length === 1 ? "" : "s"} skipped — fix and re-upload
                </div>
                <div style={{ maxHeight: 100, overflowY: "auto", padding: "8px 16px" }}>
                  {report.errors.map((e, i) => (
                    <div key={i} style={{ fontSize: 12, color: "#7a1610", padding: "4px 0", fontFamily: "'JetBrains Mono', monospace" }}>{e.msg}</div>
                  ))}
                </div>
              </div>
            )}
          </div>
        )}
      </div>

      {/* ── Footer ── */}
      <div style={{ padding: "16px 28px", borderTop: "1px solid #ececf0", display: "flex", justifyContent: "flex-end", gap: 12 }}>
        <Button variant="secondary" size="sm" onClick={onClose}>Cancel</Button>
        {phase === "uploading" && (
          <Button variant="primary" size="sm" disabled>Uploading…</Button>
        )}
        {phase === "done" && (
          <Button variant="primary" size="sm" disabled={report.valid.length === 0}
            onClick={() => onImport(report.valid)}>
            Import {report.valid.length > 0 ? `${report.valid.length} player${report.valid.length === 1 ? "" : "s"}` : ""}
          </Button>
        )}
      </div>
    </Modal>
  );
}

function parseRosterCSV(text) {
  const lines = text.split(/\r?\n/);
  const rows = [];
  // Detect a header on the first non-empty line.
  let started = false, headerSkipped = false;
  for (let i = 0; i < lines.length; i++) {
    const line = lines[i].trim();
    if (!line) continue;
    if (!started) {
      started = true;
      if (/first|name/i.test(line)) { headerSkipped = true; continue; }
    }
    const cells = line.split(",").map((c) => c.trim());
    if (!cells[0] && !cells[1]) continue;
    rows.push({
      _row: i + 1, // 1-based line number in the uploaded file
      first: cells[0] || "",
      last: cells[1] || "",
      number: cells[2] || "",
      nameOnJersey: cells[3] || "",
      email: cells[4] || "",
      phone: cells[5] || ""
    });
  }
  return rows;
}

// Validate parsed CSV rows. Returns { valid, errors:[{row,msg}] }.
// Required: First + Last name. Optional but, if present, well-formed: email,
// phone (10-digit), jersey number (0–99, unique across the import).
function validateRosterRows(rows) {
  const errors = [];
  const valid = [];
  const seenNum = {};
  rows.forEach((r) => {
    const where = `Row ${r._row}`;
    let ok = true;
    if (!r.first || !r.last) { errors.push({ row: r._row, msg: `${where}: Missing player name` }); ok = false; }
    if (r.email && !isValidEmail(r.email)) { errors.push({ row: r._row, msg: `${where}: Invalid email format` }); ok = false; }
    if (r.phone && !isValidPhone(r.phone)) { errors.push({ row: r._row, msg: `${where}: Invalid phone number` }); ok = false; }
    const num = String(r.number || "").trim();
    if (num) {
      if (!/^\d{1,2}$/.test(num)) { errors.push({ row: r._row, msg: `${where}: Jersey number must be 0–99` }); ok = false; }
      else if (seenNum[num] != null) { errors.push({ row: r._row, msg: `${where}: Duplicate jersey number ${num} (also Row ${seenNum[num]})` }); ok = false; }
      else seenNum[num] = r._row;
    }
    if (ok) valid.push(r);
  });
  return { valid, errors };
}

function FloatingActionBar({ children, leftChildren }) {
  return (
    <div style={{
      position: "fixed", bottom: 0, left: 116, right: 0,
      background: "#fff", borderTop: "1px solid #ececf0",
      padding: "16px 36px", display: "flex", alignItems: "center",
      justifyContent: "space-between", gap: 12,
      zIndex: 30
    }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12 }}>{leftChildren}</div>
      <div style={{ display: "flex", alignItems: "center", gap: 12 }}>{children}</div>
    </div>);

}

function EmptyTabState({ iconKind = "chart", title, body }) {
  return (
    <div style={{
      display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
      padding: "140px 20px", textAlign: "center"
    }}>
      <div style={{
        width: 56, height: 56, borderRadius: "50%",
        background: iconKind === "chart" ? "#dff5ea" : "#e7f0fc",
        color: iconKind === "chart" ? "#1f9d55" : "#2563eb",
        display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16
      }}>
        {iconKind === "chart" ? <ChartIcon size={22} /> : <DocIcon size={22} />}
      </div>
      <div style={{ fontSize: 15, fontWeight: 700, marginBottom: 8 }}>{title}</div>
      <div style={{ fontSize: 13, color: "#6b6b72", maxWidth: 440, lineHeight: 1.55 }}>{body}</div>
    </div>);

}

const ChartIcon = ({ size = 20 }) =>
<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <rect x="3" y="3" width="18" height="18" rx="2" />
    <line x1="8" y1="16" x2="8" y2="12" />
    <line x1="12" y1="16" x2="12" y2="8" />
    <line x1="16" y1="16" x2="16" y2="10" />
  </svg>;

const DocIcon = ({ size = 18 }) =>
<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" />
    <path d="M14 2v6h6" />
    <line x1="8" y1="13" x2="16" y2="13" />
    <line x1="8" y1="17" x2="14" y2="17" />
  </svg>;


function formatStoreDate(iso) {
  if (!iso) return "—";
  // Accept "YYYY-MM-DD" → "MM/DD/YYYY"
  const m = /^(\d{4})-(\d{2})-(\d{2})/.exec(iso);
  if (m) return `${m[2]}/${m[3]}/${m[1]}`;
  return iso;
}

// ─────────── Team Manager & Freight Mapping (feature-gated) ───────────
// Delegates logistics to per-team managers/coaches. Strictly gated: only visible
// when the store chose "Ship to Team Manager" during Step 1. Two modes per
// division: League-level or Individual-Team — bulk freight routes to whoever
// is effective per team. Hidden entirely for direct-to-home shipping.
const MANAGER_SEED = {
  "sf": { firstName: "Marcus", lastName: "Whitfield", name: "Marcus Whitfield", email: "marcus.w@jazzbantum.org", phone: "(480) 555-0132", address: "1420 E Highland Dr, Draper, UT 84020" },
  "af": { firstName: "Denise", lastName: "Alvarez", name: "Denise Alvarez", email: "denise.a@jazzbantum.org", phone: "(480) 555-0148", address: "883 Canyon View Rd, Sandy, UT 84094" },
  "pg": { name: "", email: "", phone: "" },
  "or": { firstName: "Terrence", lastName: "Cole", name: "Terrence Cole", email: "terrence.c@jazzbantum.org", phone: "(480) 555-0175", address: "2210 N Center St, Lehi, UT 84043" },
  "pv": { name: "", email: "", phone: "" },
};
// Individual team (roster) overrides — assigning at the team level does NOT
// require the rest of the group to be assigned. Seeded to demonstrate: one team
// overriding its group manager (sf-14), and one team in a group with no group
// manager (pg-8).
const TEAM_MANAGER_SEED = {
  "sf-14": { firstName: "Rico", lastName: "DiBiase", name: "Rico DiBiase", email: "rico.d@jazzbantum.org", phone: "(480) 555-0166", address: "77 W Vine St, Murray, UT 84107" },
  "pg-8": { firstName: "Renee", lastName: "Park", name: "Renee Park", email: "renee.p@jazzbantum.org", phone: "(480) 555-0210", address: "512 S 900 E, Farmington, UT 84025" },
};

function TeamManagerMapping({ store, org, setOrg, focus }) {
  // Live structure + manager state come from the shared org (lifted to
  // Dashboard) so this section and the Rosters tab mirror each other.
  const groups = org.groups;
  const loose = org.loose.filter((r) => r.id !== "single-roster");
  const leagueManager = leagueManagerName(store);
  const managers = org.managers || {};
  const modes = org.modes || {};
  const setManagers = (fn) => setOrg((o) => ({ ...o, managers: typeof fn === "function" ? fn(o.managers || {}) : fn }));
  const setModes = (fn) => setOrg((o) => ({ ...o, modes: typeof fn === "function" ? fn(o.modes || {}) : fn }));
  const [openId, setOpenId] = React.useState(groups[0] ? groups[0].id : null);
  const [toast, setToast] = React.useState(null);
  const [autoSwitched, setAutoSwitched] = React.useState(null); // { gid } — set by jump-in auto-switch
  // Jump target from the Rosters tab: open the right panel. If a specific team
  // was targeted while its group used a single group manager, auto-switch the
  // group to individual-team mode — warned inline, undoable.
  React.useEffect(() => {
    if (!focus) return;
    if (focus.groupId) {
      setOpenId(focus.groupId);
      if (focus.rosterId && (modes[focus.groupId] || "league") !== "teams") {
        setModes((c) => ({ ...c, [focus.groupId]: "teams" }));
        setAutoSwitched({ gid: focus.groupId });
      }
    } else if (focus.rosterId) {
      setOpenId("standalone");
    }
  }, [focus && focus.n]);
  const setMode = (gid, v) => { setModes((c) => ({ ...c, [gid]: v })); if (autoSwitched && autoSwitched.gid === gid) setAutoSwitched(null); };
  const setField = (id, f, val) => setManagers((cur) => {
    const rec = { ...(cur[id] || {}), [f]: f === "phone" ? formatPhone(val) : val };
    if (f === "firstName" || f === "lastName") rec.name = [rec.firstName, rec.lastName].filter(Boolean).join(" ").trim();
    return { ...cur, [id]: rec };
  });
  const addTeamOverride = (rid) => setManagers((cur) => ({ ...cur, [rid]: { firstName: "", lastName: "", name: "", email: "", phone: "", address: "" } }));
  const removeTeamOverride = (rid) => setManagers((cur) => { const n = { ...cur }; delete n[rid]; return n; });

  // Effective manager for a team, respecting the group's mode.
  const effFor = (g, r) => effectiveManagerFor(org, store, g && g.id, r.id);
  const overrideTotal = groups.reduce((n, g) => n + g.rosters.filter((r) => (managers[r.id] || {}).name).length, 0)
    + loose.filter((r) => (managers[r.id] || {}).name).length;
  const groupAssigned = groups.filter((g) => managerComplete(managers[g.id])).length;

  const field = (id, f, label, placeholder, mono) => (
    <div>
      <div style={{ fontSize: 11, fontWeight: 700, color: "#6b6b72", marginBottom: 8, letterSpacing: ".02em" }}>{label}</div>
      <input value={(managers[id] || {})[f] || ""} onChange={(e) => setField(id, f, e.target.value)} placeholder={placeholder}
        inputMode={mono ? "tel" : undefined} maxLength={mono ? 14 : undefined}
        style={{ width: "100%", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 12px", fontSize: 13.5, color: "#0e0e10", outline: "none", fontFamily: mono ? "JetBrains Mono, monospace" : "inherit", boxSizing: "border-box" }} />
    </div>
  );
  // Full manager field set — First/Last/Email/Phone/Address (address required for fulfillment).
  const managerFields = (id) => (
    <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1.4fr", gap: 12 }}>
        {field(id, "firstName", "First Name", "First name")}
        {field(id, "lastName", "Last Name", "Last name")}
        {field(id, "email", "Email", "coach@example.com")}
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 2.4fr", gap: 12 }}>
        {field(id, "phone", "Phone", "(000) 000-0000", true)}
        {field(id, "address", "Address", "Street, City, State ZIP")}
      </div>
    </div>
  );

  const srcBadge = (src) => <MgrSrcBadge src={src} />;

  return (
    <div style={{ marginTop: 24, background: "#fff", border: "1px solid #ececf0", borderRadius: 8, boxShadow: "0 1px 2px rgba(20,20,30,.03)", overflow: "hidden" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 16, padding: "20px 24px", borderBottom: "1px solid #ececf0" }}>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <h2 style={{ fontSize: 13, fontWeight: 700, margin: 0, color: "#0e0e10" }}>Team manager &amp; order fulfillment mapping</h2>
            <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#1f9d55", background: "#e7f5ed", borderRadius: 4, padding: "4px 8px" }}>SHIP TO TEAM MANAGER</span>
          </div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", marginTop: 4, lineHeight: 1.5 }}>
            Route bulk freight at the league level, or assign an individual manager per team. Orders are sorted by team and freighted to whoever is effective for each one.
          </div>
        </div>
        <span style={{ flexShrink: 0, fontSize: 12, fontWeight: 700, color: "#6b6b72", background: "#f1f1f3", borderRadius: 999, padding: "8px 12px" }}>{overrideTotal} team {overrideTotal === 1 ? "manager" : "managers"} assigned</span>
      </div>
      <div>
        {groups.length === 0 && loose.length === 0 &&
          <div style={{ padding: "28px 24px", fontSize: 13, color: "#9a9aa1" }}>No teams yet — add divisions and teams on the My Teams tab and they will appear here for manager assignment.</div>}
        {groups.map((g, i) => {
          const gm = managers[g.id] || {};
          const open = openId === g.id;
          const mode = modes[g.id] || "league";
          const groupAssignedHere = managerComplete(gm);
          const teamsSet = g.rosters.filter((r) => managerComplete(managers[r.id])).length;
          return (
            <div key={g.id} style={{ borderTop: i === 0 ? "none" : "1px solid #f3f3f4" }}>
              <button type="button" onClick={() => setOpenId(open ? null : g.id)} style={{
                display: "flex", alignItems: "center", gap: 16, width: "100%", textAlign: "left",
                background: open ? "#fafafb" : "#fff", border: 0, padding: "16px 24px", cursor: "pointer",
              }}>
                <span style={{ width: 36, height: 36, borderRadius: 9, flexShrink: 0, background: g.color, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13 }}>{rInitials(g.name)}</span>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ fontSize: 14.5, fontWeight: 800, color: "#0e0e10" }}>{g.name}</div>
                  <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 1 }}>{g.rosters.length} teams · {g.sport}</div>
                </div>
                <span style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8 }}>
                  {mode === "teams"
                    ? <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, fontWeight: 700, color: teamsSet >= g.rosters.length ? "#0e0e10" : "#b81d14" }}><span style={{ width: 8, height: 8, borderRadius: "50%", background: teamsSet >= g.rosters.length ? "#1f9d55" : "#e1251b" }} /> {teamsSet}/{g.rosters.length} teams set</span>
                    : <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, fontWeight: 700, color: "#6b6b72" }}><span style={{ width: 8, height: 8, borderRadius: "50%", background: "#c0c0c6" }} /> League Manager</span>}
                </span>
                <IconChevron size={18} style={{ flexShrink: 0, color: "#9a9aa1", transform: open ? "rotate(180deg)" : "none", transition: "transform .16s" }} />
              </button>
              {open && (
                <div style={{ padding: "8px 24px 24px 76px", background: "#fafafb" }}>
                  {/* Fulfillment routing: league manager, one group manager, or individual team managers */}
                  <div style={{ display: "inline-flex", background: "#eef0f2", borderRadius: 8, padding: 4, marginBottom: 16 }}>
                    {[{ k: "league", l: "League-level" }, { k: "teams", l: "Individual teams" }].map((o) => {
                      const on = mode === o.k;
                      return (
                        <button key={o.k} type="button" onClick={() => setMode(g.id, o.k)} style={{
                          padding: "8px 16px", border: 0, borderRadius: 6, cursor: "pointer", fontSize: 12.5, fontWeight: 700,
                          background: on ? "#fff" : "transparent", color: on ? "#0e0e10" : "#6b6b72",
                          boxShadow: on ? "0 1px 3px rgba(20,20,30,.12)" : "none", transition: "all .13s",
                        }}>{o.l}</button>
                      );
                    })}
                  </div>

                  {autoSwitched && autoSwitched.gid === g.id && mode === "teams" &&
                    <div style={{ display: "flex", alignItems: "center", gap: 12, background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 8, padding: "12px 16px", marginBottom: 16 }}>
                      <svg width={16} height={16} viewBox="0 0 24 24" fill="none" stroke="#b07a10" strokeWidth={2} strokeLinecap="round" strokeLinejoin="round" style={{ flexShrink: 0 }}><path d="M10.3 3.9 1.8 18a2 2 0 0 0 1.7 3h17a2 2 0 0 0 1.7-3L13.7 3.9a2 2 0 0 0-3.4 0Z" /><line x1="12" y1="9" x2="12" y2="13" /><line x1="12" y1="17" x2="12.01" y2="17" /></svg>
                      <span style={{ flex: 1, fontSize: 12.5, color: "#8a5f10", lineHeight: 1.45 }}>{g.name} was switched to <strong>individual team managers</strong> so you can assign this team directly. The group manager no longer applies.</span>
                      <button type="button" onClick={() => setMode(g.id, "group")} style={{ flexShrink: 0, background: "#fff", border: "1px solid #e3ca8f", color: "#8a5f10", borderRadius: 6, padding: "8px 12px", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Undo</button>
                    </div>}

                  {mode === "league" && (
                  <div style={{ display: "flex", alignItems: "center", gap: 12, background: "#fff", border: "1px solid #ececf0", borderRadius: 8, padding: "14px 16px" }}>
                    <span style={{ width: 32, height: 32, borderRadius: 7, flexShrink: 0, background: "#f1f1f3", color: "#3f3f46", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconVan size={17} /></span>
                    <span style={{ fontSize: 13, color: "#4b4b52", lineHeight: 1.5 }}>All {g.name} orders ship to the league manager — <strong style={{ color: "#0e0e10", fontWeight: 700 }}>{leagueManager}</strong>.</span>
                  </div>
                  )}


                  {mode === "teams" && (
                  <React.Fragment>
                  <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>Individual Teams <span style={{ fontWeight: 600, textTransform: "none", letterSpacing: 0, color: "#b0b0b6" }}>· assign a coach per team</span></div>
                  <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden" }}>
                    {g.rosters.map((r, ri) => {
                      const hasOverride = managers[r.id] !== undefined;
                      const eff = effFor(g, r);
                      return (
                        <div key={r.id} style={{ borderTop: ri === 0 ? "none" : "1px solid #f3f3f4", padding: "12px 16px" }}>
                          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
                            <span style={{ width: 32, height: 32, borderRadius: 7, flexShrink: 0, background: rTint(g.color, 0.14), color: g.color, display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11 }}>{r.division || rInitials(r.name)}</span>
                            <div style={{ flex: 1, minWidth: 0 }}>
                              <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{r.name}</div>
                              <div style={{ display: "inline-flex", alignItems: "center", gap: 8, marginTop: 4 }}>
                                <span style={{ fontSize: 12, color: "#6b6b72" }}>{eff.name}</span>
                                {srcBadge(eff.src)}
                              </div>
                            </div>
                            {hasOverride
                              ? <button type="button" onClick={() => removeTeamOverride(r.id)} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #e6b3af", color: "#b81d14", borderRadius: 6, padding: "8px 12px", fontSize: 12, fontWeight: 700, cursor: "pointer" }}><IconX size={13} /> Remove</button>
                              : <button type="button" onClick={() => addTeamOverride(r.id)} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "8px 12px", fontSize: 12, fontWeight: 700, cursor: "pointer" }}><IconPlus size={13} style={{ strokeWidth: 2.4 }} /> Assign coach</button>}
                          </div>
                          {hasOverride && (
                            <div style={{ marginTop: 12 }}>{managerFields(r.id)}</div>
                          )}
                        </div>
                      );
                    })}
                  </div>
                  </React.Fragment>
                  )}

                  <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: 16 }}>
                    <span style={{ fontSize: 12, color: "#9a9aa1" }}>{mode === "teams" ? "Every roster needs a complete team manager (incl. address) before pending orders can be submitted." : <React.Fragment>Orders route to <strong style={{ color: "#6b6b72", fontWeight: 700 }}>{leagueManager}</strong> — nothing else to assign.</React.Fragment>}</span>
                    <button type="button" onClick={() => { setOpenId(null); setToast(`Mapping saved for ${g.name}`); }} style={{
                      background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "8px 16px", fontSize: 12.5, fontWeight: 800, cursor: "pointer",
                    }}>Save Mapping</button>
                  </div>
                </div>
              )}
            </div>
          );
        })}
      </div>
      {loose.length > 0 && (() => {
        const open = openId === "standalone";
        const setCnt = loose.filter((r) => (managers[r.id] || {}).name).length;
        return (
          <div style={{ borderTop: "1px solid #f3f3f4" }}>
            <button type="button" onClick={() => setOpenId(open ? null : "standalone")} style={{ display: "flex", alignItems: "center", gap: 16, width: "100%", textAlign: "left", background: open ? "#fafafb" : "#fff", border: 0, padding: "16px 24px", cursor: "pointer" }}>
              <span style={{ width: 36, height: 36, borderRadius: 9, flexShrink: 0, background: "#3f3f46", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconUsers size={17} /></span>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ fontSize: 14.5, fontWeight: 800, color: "#0e0e10" }}>Unassigned Teams</div>
                <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 1 }}>{loose.length} {loose.length === 1 ? "team" : "teams"} · not connected to a division</div>
              </div>
              <span style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, fontWeight: 700, color: "#0e0e10" }}><span style={{ width: 8, height: 8, borderRadius: "50%", background: setCnt ? "#1f9d55" : "#c0c0c6" }} /> {setCnt}/{loose.length} set</span>
              <IconChevron size={18} style={{ flexShrink: 0, color: "#9a9aa1", transform: open ? "rotate(180deg)" : "none", transition: "transform .16s" }} />
            </button>
            {open && (
              <div style={{ padding: "8px 24px 24px 76px", background: "#fafafb" }}>
                <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 8 }}>Unassigned Teams <span style={{ fontWeight: 600, textTransform: "none", letterSpacing: 0, color: "#b0b0b6" }}>· assign a manager per team</span></div>
                <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden" }}>
                  {loose.map((r, ri) => {
                    const hasOverride = managers[r.id] !== undefined;
                    const eff = effectiveManagerFor(org, store, null, r.id);
                    return (
                      <div key={r.id} style={{ borderTop: ri === 0 ? "none" : "1px solid #f3f3f4", padding: "12px 16px" }}>
                        <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
                          <span style={{ width: 32, height: 32, borderRadius: 7, flexShrink: 0, background: "#f1f1f3", color: "#3f3f46", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11 }}>{rInitials(r.name)}</span>
                          <div style={{ flex: 1, minWidth: 0 }}>
                            <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{r.name}</div>
                            <div style={{ display: "inline-flex", alignItems: "center", gap: 8, marginTop: 4 }}>
                              <span style={{ fontSize: 12, color: "#6b6b72" }}>{eff.name}</span>
                              {srcBadge(eff.src)}
                            </div>
                          </div>
                          {hasOverride
                            ? <button type="button" onClick={() => removeTeamOverride(r.id)} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #e6b3af", color: "#b81d14", borderRadius: 6, padding: "8px 12px", fontSize: 12, fontWeight: 700, cursor: "pointer" }}><IconX size={13} /> Remove</button>
                            : <button type="button" onClick={() => addTeamOverride(r.id)} style={{ flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", color: "#0e0e10", borderRadius: 6, padding: "8px 12px", fontSize: 12, fontWeight: 700, cursor: "pointer" }}><IconPlus size={13} style={{ strokeWidth: 2.4 }} /> Assign manager</button>}
                        </div>
                        {hasOverride && (
                          <div style={{ marginTop: 12 }}>{managerFields(r.id)}</div>
                        )}
                      </div>
                    );
                  })}
                </div>
                <div style={{ marginTop: 16, fontSize: 12, color: "#9a9aa1" }}>Rosters with no manager fall back to <strong style={{ color: "#6b6b72", fontWeight: 700 }}>{leagueManager}</strong>.</div>
              </div>
            )}
          </div>
        );
      })()}
      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
}

// ─────────── Active Order Window · per-team-code scheduling (Epic E+) ───────────
// One store entity, N independent ordering windows — mapped to team codes.
const OW_TZS = [
  { value: "America/Los_Angeles", label: "Pacific · PT" },
  { value: "America/Denver", label: "Mountain · MT" },
  { value: "America/Chicago", label: "Central · CT" },
  { value: "America/New_York", label: "Eastern · ET" },
];
const OW_TZ_SHORT = { "America/Los_Angeles": "PT", "America/Denver": "MT", "America/Chicago": "CT", "America/New_York": "ET" };
const OW_MONTHS = ["Jan","Feb","Mar","Apr","May","Jun","Jul","Aug","Sep","Oct","Nov","Dec"];
const OW_MONTHS_FULL = ["January","February","March","April","May","June","July","August","September","October","November","December"];

function owPad(n) { return String(n).padStart(2, "0"); }
function owToLocal(d) { return d.getFullYear() + "-" + owPad(d.getMonth() + 1) + "-" + owPad(d.getDate()) + "T" + owPad(d.getHours()) + ":" + owPad(d.getMinutes()); }
function owParse(s) { return s ? new Date(s) : null; }
function owFmtDate(s) { const d = owParse(s); if (!d || isNaN(d)) return "—"; return OW_MONTHS[d.getMonth()] + " " + d.getDate() + ", " + d.getFullYear(); }
function owFmtTime(s) {
  const d = owParse(s); if (!d || isNaN(d)) return "";
  let h = d.getHours(); const m = d.getMinutes(); const ap = h >= 12 ? "PM" : "AM";
  h = h % 12 || 12; return h + ":" + owPad(m) + " " + ap;
}
function owDayDiff(a, b) { return Math.round((b - a) / 86400000); }

// Every team code inside the store — flattened from the shared roster org so the
// list always mirrors My Teams. Read-only here; teams are added on that tab.
function owTeams(org, store) {
  const out = [];
  ((org && org.groups) || []).forEach((g) => (g.rosters || []).forEach((r) =>
    out.push({ id: r.id, name: r.name, division: g.name, color: g.color || "#3f3f46", players: r.players || 0, code: rTeamCode(r.id) })));
  ((org && org.loose) || []).forEach((r) =>
    out.push({ id: r.id, name: r.name, division: null, color: "#3f3f46", players: r.players || 0, code: rTeamCode(r.id) }));
  if (!out.length) out.push({ id: "store-team", name: (store && (store.teamName || store.storeTitle)) || "Team Roster", division: null, color: "#3f3f46", players: 0, code: rTeamCode((store && store.id) || "store") });
  return out;
}

// Seeded demo schedule: a deliberate mix of closed / active / pending / unset so
// the sequential sort, status pills and validation flags are all exercised.
const OW_SEED_OFFSETS = [[-34, -6], [-19, 5], [-11, 12], [2, 23], null, [9, 30]];
function owSeed(teams) {
  const w = {};
  teams.forEach((t, i) => {
    const tz = OW_TZS[i % 4].value;
    const off = OW_SEED_OFFSETS[i % OW_SEED_OFFSETS.length];
    if (!off) { w[t.id] = { start: "", end: "", tz }; return; }
    const s = new Date(2026, 5, 4 + off[0], 9, 0);
    const e = new Date(2026, 5, 4 + off[1], 21, 0);
    w[t.id] = { start: owToLocal(s), end: owToLocal(e), tz };
  });
  return w;
}

function owStatus(win, today, storeClosed) {
  if (!win || !win.start || !win.end)
    return { key: "unset", label: "Not set", bg: "#fff", color: "#b81d14", ring: "#f3c9c5", dashed: true, sort: 0 };
  const s = owParse(win.start), e = owParse(win.end);
  if (isNaN(s) || isNaN(e) || e <= s)
    return { key: "error", label: "Invalid range", bg: "#fbe3e1", color: "#b81d14", ring: "#f3c9c5", sort: 0 };
  if (storeClosed) return { key: "closed", label: "Closed · store", bg: "#f1f1f3", color: "#6b6b72", ring: "#e4e4e7", sort: 3 };
  if (today < s) return { key: "pending", label: "Pending", bg: "#fdf6e7", color: "#8a5f10", ring: "#efdcae", sort: 2 };
  if (today > e) return { key: "closed", label: "Closed", bg: "#f1f1f3", color: "#6b6b72", ring: "#e4e4e7", sort: 3 };
  return { key: "active", label: "Active", bg: "#e7f5ed", color: "#1f7a44", ring: "#bfe3ce", sort: 1 };
}

// Segmented control — operational mode + row-editor style.
function OWSegmented({ value, options, onChange, dense }) {
  return (
    <div style={{ display: "grid", gridTemplateColumns: "repeat(" + options.length + ", 1fr)", gap: 4, background: "#f1f1f3", border: "1px solid #ececf0", borderRadius: 8, padding: 4 }}>
      {options.map((o) => {
        const on = o.value === value;
        return (
          <button key={o.value} onClick={() => onChange(o.value)} style={{
            textAlign: dense ? "center" : "left", cursor: "pointer", fontFamily: "inherit",
            background: on ? "#fff" : "transparent", border: on ? "1px solid #d4d4d8" : "1px solid transparent",
            borderRadius: 6, padding: dense ? "7px 10px" : "12px 14px",
            boxShadow: on ? "0 1px 2px rgba(20,20,30,.06)" : "none", transition: "background .12s, box-shadow .12s",
          }}>
            <span style={{ display: "block", fontSize: dense ? 11.5 : 13, fontWeight: on ? 800 : 700, color: on ? "#0e0e10" : "#6b6b72" }}>{o.label}</span>
            {o.desc && <span style={{ display: "block", marginTop: 4, fontSize: 11.5, lineHeight: 1.45, color: "#9a9aa1", fontWeight: 400 }}>{o.desc}</span>}
          </button>
        );
      })}
    </div>
  );
}

// Bare datetime input, sized for a dense row.
function OWDateTimeInput({ value, onChange, invalid }) {
  const [focus, setFocus] = React.useState(false);
  return (
    <input type="datetime-local" value={value || ""} onChange={(e) => onChange(e.target.value)}
      onFocus={() => setFocus(true)} onBlur={() => setFocus(false)}
      style={{
        width: "100%", boxSizing: "border-box", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600,
        color: value ? "#0e0e10" : "#9a9aa1", background: "#fff", padding: "8px 8px",
        border: "1px solid " + (invalid ? "#e1251b" : focus ? "#0e0e10" : "#e4e4e7"),
        borderRadius: 6, outline: "none", transition: "border-color .12s",
      }} />
  );
}

function OWReadCell({ value, placeholder }) {
  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 1 }}>
      <span style={{ fontSize: 12.5, fontWeight: 700, color: value ? "#0e0e10" : "#b81d14" }}>{value ? owFmtDate(value) : placeholder}</span>
      {value && <span style={{ fontSize: 10.5, color: "#9a9aa1" }}>{owFmtTime(value)}</span>}
    </div>
  );
}

function OWTzSelect({ value, onChange, compact }) {
  return (
    <select value={value} onChange={(e) => onChange(e.target.value)} style={{
      fontFamily: "inherit", fontSize: compact ? 10.5 : 12.5, fontWeight: 700, color: "#6b6b72",
      background: "#fff", border: "1px solid #e4e4e7", borderRadius: 6,
      padding: compact ? "3px 4px" : "8px 8px", outline: "none", cursor: "pointer", width: "100%",
    }}>
      {OW_TZS.map((t) => <option key={t.value} value={t.value}>{compact ? OW_TZ_SHORT[t.value] : t.label}</option>)}
    </select>
  );
}

function OWPill({ st }) {
  return (
    <span style={{
      display: "inline-flex", alignItems: "center", gap: 6, whiteSpace: "nowrap",
      background: st.bg, color: st.color, border: (st.dashed ? "1px dashed " : "1px solid ") + st.ring,
      borderRadius: 999, padding: "3px 10px", fontSize: 11.5, fontWeight: 800,
    }}>
      <span style={{ width: 6, height: 6, borderRadius: "50%", background: "currentColor" }} />{st.label}
    </span>
  );
}

// Bulk scheduling utility — one range across every selected team code.
function OWBulkModal({ open, count, onClose, onApply, storeStart, storeEnd }) {
  const [start, setStart] = React.useState("");
  const [end, setEnd] = React.useState("");
  const [tz, setTz] = React.useState("America/Los_Angeles");
  const [onlyUnset, setOnlyUnset] = React.useState(false);
  React.useEffect(() => { if (open) { setStart(""); setEnd(""); setOnlyUnset(false); } }, [open]);
  const bad = start && end && new Date(end) <= new Date(start);
  return (
    <Modal open={open} onClose={onClose} width={520}>
      <div style={{ padding: "24px 28px 20px" }}>
        <div style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase" }}>Bulk schedule</div>
        <h3 style={{ margin: "8px 0 4px", fontSize: 19, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Apply dates to {count} team {count === 1 ? "code" : "codes"}</h3>
        <p style={{ margin: "0 0 20px", fontSize: 13, color: "#6b6b72", lineHeight: 1.55 }}>Sets one identical window across the selection. Adjust outliers row by row afterwards.</p>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 14 }}>
          <label>
            <span style={{ display: "block", fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Opens</span>
            <OWDateTimeInput value={start} onChange={setStart} />
          </label>
          <label>
            <span style={{ display: "block", fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Closes</span>
            <OWDateTimeInput value={end} onChange={setEnd} invalid={!!bad} />
          </label>
        </div>
        <div style={{ marginTop: 14 }}>
          <span style={{ display: "block", fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Timezone</span>
          <OWTzSelect value={tz} onChange={setTz} />
        </div>
        {(storeStart || storeEnd) &&
        <button onClick={() => { if (storeStart) setStart(storeStart.length > 10 ? storeStart : storeStart + "T09:00"); if (storeEnd) setEnd(storeEnd.length > 10 ? storeEnd : storeEnd + "T21:00"); }}
          style={{ marginTop: 12, background: "transparent", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit", fontSize: 12.5, fontWeight: 700, color: "#e1251b", textDecoration: "underline", textUnderlineOffset: 2 }}>
          Match the store-wide window
        </button>}
        <div style={{ marginTop: 18, paddingTop: 16, borderTop: "1px solid #ececf0" }}>
          <Checkbox checked={onlyUnset} onChange={setOnlyUnset} label={<span style={{ fontSize: 13, color: "#0e0e10" }}>Only fill team codes that have no dates yet</span>} />
        </div>
        {bad && <div style={{ marginTop: 14, fontSize: 12.5, fontWeight: 700, color: "#b81d14" }}>The closing date must come after the opening date.</div>}
      </div>
      <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, padding: "16px 28px", borderTop: "1px solid #ececf0", background: "#fafafb" }}>
        <Button variant="secondary" onClick={onClose}>Cancel</Button>
        <Button variant="primary" disabled={!start || !end || !!bad} onClick={() => onApply({ start, end, tz, onlyUnset })}>Apply Bulk Dates</Button>
      </div>
    </Modal>
  );
}

function OWGroupHeader({ label, count, tone }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 10, padding: "14px 14px 8px", background: "#fafafb", borderTop: "1px solid #ececf0" }}>
      <span style={{ width: 7, height: 7, borderRadius: "50%", background: tone, flexShrink: 0 }} />
      <span style={{ fontSize: 10.5, fontWeight: 800, letterSpacing: ".1em", color: "#0e0e10", textTransform: "uppercase" }}>{label}</span>
      <span style={{ fontSize: 10.5, fontWeight: 800, color: "#9a9aa1" }}>{count}</span>
    </div>
  );
}

function OrderWindowScheduler({ store, org, today, storeClosed, hasWindow, onToast }) {
  const teams = React.useMemo(() => owTeams(org, store), [org, store]);
  const [mode, setMode] = React.useState(store.orderWindowMode === "team" ? "team" : "store");
  const [wins, setWins] = React.useState(() => owSeed(owTeams(org, store)));
  const [sel, setSel] = React.useState({});
  const [bulkOpen, setBulkOpen] = React.useState(false);
  const [openRow, setOpenRow] = React.useState(null);
  const [query, setQuery] = React.useState("");
  const [gStart, setGStart] = React.useState((store.storeOpenStart || "2026-05-01") + "T09:00");
  const [gEnd, setGEnd] = React.useState((store.storeOpenEnd || "2026-06-30") + "T21:00");
  const [gTz, setGTz] = React.useState("America/Los_Angeles");

  // Teams can be added on My Teams while this panel is mounted — back-fill.
  React.useEffect(() => {
    setWins((w) => {
      let next = w, dirty = false;
      teams.forEach((t) => { if (!next[t.id]) { if (!dirty) { next = { ...w }; dirty = true; } next[t.id] = { start: "", end: "", tz: "America/Los_Angeles" }; } });
      return dirty ? next : w;
    });
  }, [teams]);

  const setWin = (id, patch) => setWins((w) => ({ ...w, [id]: { ...(w[id] || { tz: "America/Los_Angeles" }), ...patch } }));
  // Per-team-code windows only apply to league stores; individual team stores are always store-wide.
  const isLeague = store.storeType === "league";
  const perTeam = isLeague && mode === "team";

  const rows = React.useMemo(() => {
    const q = query.trim().toLowerCase();
    return teams
      .filter((t) => !q || t.name.toLowerCase().includes(q) || t.code.toLowerCase().includes(q) || (t.division || "").toLowerCase().includes(q))
      .map((t) => ({ t, w: wins[t.id] || { start: "", end: "", tz: "America/Los_Angeles" }, st: owStatus(wins[t.id], today, storeClosed) }))
      .sort((a, b) => {
        if (a.st.sort !== b.st.sort) return a.st.sort - b.st.sort;
        const as = owParse(a.w.start), bs = owParse(b.w.start);
        if (as && bs) return as - bs;
        if (as) return -1;
        if (bs) return 1;
        return a.t.name.localeCompare(b.t.name);
      });
  }, [teams, wins, today, storeClosed, query]);

  const counts = rows.reduce((acc, r) => { acc[r.st.key] = (acc[r.st.key] || 0) + 1; return acc; }, {});
  const unsetCount = (counts.unset || 0) + (counts.error || 0);
  const selIds = Object.keys(sel).filter((k) => sel[k]);
  const allSel = rows.length > 0 && rows.every((r) => sel[r.t.id]);

  // Coverage gap detection across the sequential windows.
  const gap = React.useMemo(() => {
    const set = rows.filter((r) => r.w.start && r.w.end && owParse(r.w.end) > owParse(r.w.start))
      .map((r) => ({ s: owParse(r.w.start), e: owParse(r.w.end) })).sort((a, b) => a.s - b.s);
    let reach = null;
    for (const w of set) {
      if (reach && w.s > reach) { const d = owDayDiff(reach, w.s); if (d >= 1) return { from: reach, to: w.s, days: d }; }
      if (!reach || w.e > reach) reach = w.e;
    }
    return null;
  }, [rows]);

  const applyBulk = ({ start, end, tz, onlyUnset }) => {
    setWins((w) => {
      const next = { ...w };
      selIds.forEach((id) => {
        const cur = next[id] || {};
        if (onlyUnset && cur.start && cur.end) return;
        next[id] = { start, end, tz };
      });
      return next;
    });
    setBulkOpen(false);
    setSel({});
    onToast && onToast("Window applied to " + selIds.length + " team " + (selIds.length === 1 ? "code" : "codes"));
  };

  const cols = "34px 108px minmax(140px,1fr) 150px 150px 62px 128px 32px";

  if (!hasWindow) return null;

  return (
    <div style={{ marginTop: 20, paddingTop: 20, borderTop: "1px solid #ececf0" }}>
      {/* Scheduling scope — the store-wide range or independent per-team-code windows */}
      <div style={{ marginBottom: 4, fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>Order Window Scope</div>
      <p style={{ margin: "0 0 14px", fontSize: 12.5, color: "#6b6b72", lineHeight: 1.55, maxWidth: 620 }}>
        {mode === "team"
          ? "Each team code follows its own schedule — a team's storefront opens and closes on its own dates."
          : "One start / end range governs every product and team code in this store."}
      </p>
      {isLeague &&
      <OWSegmented value={mode} onChange={(v) => { setMode(v); setSel({}); }} options={[
        { value: "store", label: "Store-wide Window", desc: "One start / end range for every product and team." },
        { value: "team", label: "Per Team Code Windows", desc: "Independent schedules mapped to each team code." },
      ]} />}

      {/* Store-wide (default) — a single global range */}
      {!perTeam &&
      <div style={{ marginTop: 16, border: "1px solid #ececf0", borderRadius: 10, padding: 16, background: "#fafafb" }}>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 132px", gap: 12, alignItems: "end" }}>
          <label>
            <span style={{ display: "block", fontSize: 10.5, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Store opens</span>
            <OWDateTimeInput value={gStart} onChange={setGStart} />
          </label>
          <label>
            <span style={{ display: "block", fontSize: 10.5, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Store closes</span>
            <OWDateTimeInput value={gEnd} onChange={setGEnd} invalid={!!(gStart && gEnd && new Date(gEnd) <= new Date(gStart))} />
          </label>
          <label>
            <span style={{ display: "block", fontSize: 10.5, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Timezone</span>
            <OWTzSelect value={gTz} onChange={setGTz} />
          </label>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 12, fontSize: 12, color: "#6b6b72" }}>
          <IconUsers size={14} style={{ flexShrink: 0, color: "#9a9aa1" }} />
          <span>Applies to all {teams.length} team {teams.length === 1 ? "code" : "codes"} in this store. Any per-team dates you have set are kept and restored if you switch back.</span>
        </div>
      </div>}

      {/* Per Team Code — sequential scheduling workspace */}
      {perTeam &&
      <div style={{ marginTop: 16 }}>
        {storeClosed &&
        <div style={{ display: "flex", alignItems: "flex-start", gap: 10, marginBottom: 12, padding: "12px 14px", background: "#f1f1f3", border: "1px solid #e4e4e7", borderRadius: 8 }}>
          <IconLock size={14} style={{ flexShrink: 0, marginTop: 1, color: "#6b6b72" }} />
          <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}><strong style={{ color: "#0e0e10" }}>The store itself is closed.</strong> Every team code reads as Closed regardless of its window — re-open the store to let these schedules take effect.</span>
        </div>}

        {/* Toolbar: bulk utility + editor style + search */}
        <div style={{ display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap", marginBottom: 12 }}>
          <button onClick={() => { if (!selIds.length) { setSel(Object.fromEntries(rows.map((r) => [r.t.id, true]))); } setBulkOpen(true); }}
            style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6, padding: "9px 14px", fontSize: 12.5, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>
            <IconCalendar size={14} /> Apply Bulk Dates
          </button>
          <div style={{ flex: 1, minWidth: 160 }}>
            {teams.length > 6 &&
            <div style={{ position: "relative" }}>
              <IconSearch size={14} style={{ position: "absolute", left: 10, top: "50%", transform: "translateY(-50%)", color: "#9a9aa1" }} />
              <input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search team or code"
                style={{ width: "100%", boxSizing: "border-box", fontFamily: "inherit", fontSize: 12.5, padding: "9px 10px 9px 32px", border: "1px solid #e4e4e7", borderRadius: 6, outline: "none", color: "#0e0e10" }} />
            </div>}
          </div>
        </div>

        {/* Validation + coverage summary */}
        {(unsetCount > 0 || gap) &&
        <div style={{ display: "flex", flexDirection: "column", gap: 8, marginBottom: 12 }}>
          {unsetCount > 0 &&
          <div style={{ display: "flex", alignItems: "flex-start", gap: 10, padding: "11px 14px", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 8 }}>
            <IconAlert size={14} style={{ flexShrink: 0, marginTop: 1, color: "#8a5f10" }} />
            <span style={{ fontSize: 12.5, color: "#6b5314", lineHeight: 1.5 }}><strong style={{ color: "#8a5f10" }}>{unsetCount} team {unsetCount === 1 ? "code has" : "codes have"} no valid order window.</strong> Their storefront stays hidden until dates are set — the rest of the store is unaffected.</span>
          </div>}
          {gap &&
          <div style={{ display: "flex", alignItems: "flex-start", gap: 10, padding: "11px 14px", background: "#fafafb", border: "1px solid #e4e4e7", borderRadius: 8 }}>
            <IconClock size={14} style={{ flexShrink: 0, marginTop: 1, color: "#9a9aa1" }} />
            <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}><strong style={{ color: "#0e0e10" }}>{gap.days}-day coverage gap</strong> between {owFmtDate(owToLocal(gap.from))} and {owFmtDate(owToLocal(gap.to))} — no team code is accepting orders in that span.</span>
          </div>}
        </div>}

        {/* Matrix */}
        <div style={{ border: "1px solid #e4e4e7", borderRadius: 10, overflow: "hidden", background: "#fff" }}>
          <div style={{ display: "grid", gridTemplateColumns: cols, gap: 10, alignItems: "center", padding: "10px 14px", background: "#fff", borderBottom: "1px solid #ececf0" }}>
            <Checkbox checked={allSel} onChange={(v) => setSel(v ? Object.fromEntries(rows.map((r) => [r.t.id, true])) : {})} size={15} />
            {["Team code", "Team / roster", "Start date & time", "End date & time", "TZ", "Window status", ""].map((h, i) =>
              <span key={i} style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase" }}>{h}</span>)}
          </div>

          {rows.length === 0 &&
          <div style={{ padding: "28px 16px", textAlign: "center", fontSize: 13, color: "#9a9aa1" }}>No team codes match “{query}”.</div>}

          {rows.map((r, i) => {
            const prev = rows[i - 1];
            const showHead = !prev || prev.st.sort !== r.st.sort;
            const headLabel = r.st.sort === 0 ? "Needs dates" : r.st.sort === 1 ? "Active windows" : r.st.sort === 2 ? "Upcoming windows" : "Closed windows";
            const headTone = r.st.sort === 0 ? "#e1251b" : r.st.sort === 1 ? "#1f9d55" : r.st.sort === 2 ? "#d19f24" : "#a1a1aa";
            const groupCount = rows.filter((x) => x.st.sort === r.st.sort).length;
            const checked = !!sel[r.t.id];
            const isOpen = openRow === r.t.id;
            const invalidEnd = !!(r.w.start && r.w.end && owParse(r.w.end) <= owParse(r.w.start));
            return (
              <React.Fragment key={r.t.id}>
                {showHead && <OWGroupHeader label={headLabel} count={groupCount} tone={headTone} />}
                <div style={{
                  display: "grid", gridTemplateColumns: cols, gap: 10, alignItems: "center",
                  padding: "10px 14px", borderTop: showHead ? "1px solid #ececf0" : "1px solid #f4f4f6",
                  background: checked ? "#fafafb" : "#fff", transition: "background .12s",
                }}
                onMouseEnter={(e) => { if (!checked) e.currentTarget.style.background = "#fcfcfd"; }}
                onMouseLeave={(e) => { if (!checked) e.currentTarget.style.background = "#fff"; }}>
                  <Checkbox checked={checked} onChange={(v) => setSel((s) => ({ ...s, [r.t.id]: v }))} size={15} />
                  <span style={{ fontFamily: "JetBrains Mono, ui-monospace, monospace", fontSize: 11.5, fontWeight: 700, color: "#0e0e10", background: "#f4f4f6", border: "1px solid #ececf0", borderRadius: 5, padding: "3px 7px", justifySelf: "start", letterSpacing: ".02em" }}>{r.t.code}</span>
                  <div style={{ minWidth: 0 }}>
                    <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{r.t.name}</div>
                    <div style={{ fontSize: 10.5, color: "#9a9aa1", marginTop: 1 }}>{r.t.division ? r.t.division + " · " : ""}{r.t.players} players</div>
                  </div>
                  <OWReadCell value={r.w.start} placeholder="Not set" />
                  <OWReadCell value={r.w.end} placeholder="Not set" />
                  <span style={{ fontSize: 11, fontWeight: 800, color: "#6b6b72" }}>{OW_TZ_SHORT[r.w.tz] || "PT"}</span>
                  <span style={{ justifySelf: "start" }}><OWPill st={r.st} /></span>
                  <button onClick={() => setOpenRow(isOpen ? null : r.t.id)} title={isOpen ? "Close editor" : "Edit window"} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: isOpen ? "#0e0e10" : "#9a9aa1", display: "inline-flex", borderRadius: 4, transform: isOpen ? "rotate(180deg)" : "none", transition: "transform .15s, color .12s" }}>
                    <IconChevron size={15} />
                  </button>
                </div>

                {/* Validation flag row */}
                {(r.st.key === "unset" || r.st.key === "error") &&
                <div style={{ display: "flex", alignItems: "center", gap: 8, padding: "0 14px 10px 156px", background: checked ? "#fafafb" : "#fff" }}>
                  <IconAlert size={13} style={{ flexShrink: 0, color: "#b81d14" }} />
                  <span style={{ fontSize: 11.5, fontWeight: 700, color: "#b81d14" }}>
                    {r.st.key === "error" ? "End date is on or before the start date — window ignored." : "No order window set — storefront hidden for this team code."}
                  </span>
                </div>}

                {/* Row editor drawer */}
                {isOpen &&
                <div style={{ padding: "16px 14px 18px 156px", background: "#fafafb", borderTop: "1px solid #f4f4f6" }}>
                  <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 150px", gap: 12 }}>
                    <label>
                      <span style={{ display: "block", fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Opens</span>
                      <OWDateTimeInput value={r.w.start} onChange={(v) => setWin(r.t.id, { start: v })} />
                    </label>
                    <label>
                      <span style={{ display: "block", fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Closes</span>
                      <OWDateTimeInput value={r.w.end} onChange={(v) => setWin(r.t.id, { end: v })} invalid={invalidEnd} />
                    </label>
                    <label>
                      <span style={{ display: "block", fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Timezone</span>
                      <OWTzSelect value={r.w.tz || "America/Los_Angeles"} onChange={(v) => setWin(r.t.id, { tz: v })} />
                    </label>
                  </div>
                  <div style={{ marginTop: 14, maxWidth: 440 }}>
                    <div style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 6 }}>Distribute this team code</div>
                    <TeamCodeShare code={r.t.code} store={store} compact />
                  </div>
                  <div style={{ display: "flex", alignItems: "center", gap: 16, marginTop: 12 }}>
                    <button onClick={() => setWin(r.t.id, { start: gStart, end: gEnd })} style={{ background: "transparent", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit", fontSize: 12, fontWeight: 700, color: "#0e0e10", textDecoration: "underline", textUnderlineOffset: 2 }}>Copy store-wide window</button>
                    <button onClick={() => setWin(r.t.id, { start: "", end: "" })} style={{ background: "transparent", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit", fontSize: 12, fontWeight: 700, color: "#b81d14", textDecoration: "underline", textUnderlineOffset: 2 }}>Clear dates</button>
                  </div>
                </div>}
              </React.Fragment>
            );
          })}

          {/* Sticky bulk action bar */}
          {selIds.length > 0 &&
          <div style={{ position: "sticky", bottom: 0, display: "flex", alignItems: "center", gap: 12, padding: "12px 14px", background: "#0e0e10", borderTop: "1px solid #0e0e10" }}>
            <span style={{ fontSize: 12.5, fontWeight: 800, color: "#fff" }}>{selIds.length} team {selIds.length === 1 ? "code" : "codes"} selected</span>
            <span style={{ flex: 1 }} />
            <button onClick={() => { setWins((w) => { const n = { ...w }; selIds.forEach((id) => { n[id] = { ...(n[id] || {}), start: "", end: "" }; }); return n; }); onToast && onToast("Dates cleared"); }}
              style={{ background: "transparent", border: "1px solid rgba(255,255,255,.28)", borderRadius: 6, padding: "8px 12px", fontSize: 12, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>Clear dates</button>
            <button onClick={() => setSel({})} style={{ background: "transparent", border: 0, padding: "8px 4px", fontSize: 12, fontWeight: 700, color: "rgba(255,255,255,.7)", cursor: "pointer", fontFamily: "inherit" }}>Deselect</button>
            <button onClick={() => setBulkOpen(true)} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#e1251b", border: "1px solid #e1251b", borderRadius: 6, padding: "8px 14px", fontSize: 12.5, fontWeight: 800, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>
              <IconCalendar size={14} /> Apply Bulk Dates
            </button>
          </div>}
        </div>

        {/* Rollup */}
        <div style={{ display: "flex", alignItems: "center", gap: 16, flexWrap: "wrap", marginTop: 12, fontSize: 12, color: "#6b6b72" }}>
          <span><strong style={{ color: "#0e0e10" }}>{counts.active || 0}</strong> active</span>
          <span><strong style={{ color: "#0e0e10" }}>{counts.pending || 0}</strong> upcoming</span>
          <span><strong style={{ color: "#0e0e10" }}>{counts.closed || 0}</strong> closed</span>
          <span><strong style={{ color: unsetCount ? "#b81d14" : "#0e0e10" }}>{unsetCount}</strong> needs dates</span>
          <span style={{ marginLeft: "auto", color: "#9a9aa1" }}>Sorted by upcoming start date · times shown in each team's timezone</span>
        </div>
      </div>}

      <OWBulkModal open={bulkOpen} count={selIds.length} onClose={() => setBulkOpen(false)} onApply={applyBulk}
        storeStart={store.storeOpenStart} storeEnd={store.storeOpenEnd} />
    </div>
  );
}

function GeneralInfo({ store, onUpdateStore, org, setOrg, managerFocus, section = "all", onCreateTemplate, onGoMyTeams }) {
  const [editing, setEditing] = React.useState(null);
  const [toast, setToast] = React.useState(null);
  const [confirmClose, setConfirmClose] = React.useState(false);
  const [confirmArchive, setConfirmArchive] = React.useState(false);
  const save = (patch) => {
    const next = { ...patch };
    // Team Fundraising master switch — per-item effects. Items with a non-cancelled
    // order are exempt (pinned on); everything else turns off, amounts preserved.
    const wasOn = store.fundraisingEnabled === true;
    if (next.__applyFundDisable) {
      delete next.__applyFundDisable;
      next.products = (store.products || []).map((p) => {
        if (!(itemFundOn(p, store) && itemFundAmount(p) > 0)) return p;
        if (pteLockState(p, store) === "full") {
          return p.isBundle
            ? { ...p, bundleProfit: { ...(p.bundleProfit || {}), fundraisingOn: true } }
            : { ...p, pricing: { ...p.pricing, fundraisingOn: true } };
        }
        if (p.isBundle) return { ...p, bundleProfit: { ...(p.bundleProfit || {}), fundraisingOn: false, fundOffByStore: true } };
        const conv = (r) => r ? { ...r, retail: psRowRetail(r, false) } : r;
        return { ...p, pricing: { ...p.pricing, fundraisingOn: false, fundOffByStore: true, youth: conv(p.pricing?.youth), adult: conv(p.pricing?.adult) } };
      });
    } else if (next.fundraisingEnabled === true && !wasOn) {
      // Re-enable restores each item's prior state — no interstitial.
      next.products = (store.products || []).map((p) => {
        if (p.isBundle) {
          const bp = p.bundleProfit || {};
          if (!bp.fundOffByStore) return p;
          const { fundraisingOn, fundOffByStore, ...rest } = bp;
          return { ...p, bundleProfit: rest };
        }
        const pr = p.pricing || {};
        if (!pr.fundOffByStore) return p;
        const { fundraisingOn, fundOffByStore, ...rest } = pr;
        const conv = (r) => r ? { ...r, retail: psRowRetail(r, true) } : r;
        return { ...p, pricing: { ...rest, youth: conv(rest.youth), adult: conv(rest.adult) } };
      });
    }
    onUpdateStore && onUpdateStore(next);
    setEditing(null);
    setToast("Changes saved");
  };

  // Schedule mode toggle — lives next to the Schedule & Status heading
  // (replaces the old Edit Store Schedule modal).
  const setScheduleMode = (mode) => {
    if ((store.storeOpenMode || "set-window") === mode) return;
    const patch = { storeOpenMode: mode };
    if (mode === "set-window") {
      if (!store.storeOpenStart) patch.storeOpenStart = "2026-06-04";
      if (!store.storeOpenEnd) patch.storeOpenEnd = "2026-07-04";
    }
    onUpdateStore && onUpdateStore(patch);
    setToast(mode === "stay-open" ? "Store set to always open" : "Store set to a scheduled window");
  };
  // #10 — store status + ordering-window math (Detail tab, Epics C–E).
  const closed = store.status === "closed";
  const archived = store.status === "archived";
  // Draft = never launched. Lifecycle statuses: Draft / Open / Closed / Archived.
  const draft = !store.live && !closed && !archived;
  const canLaunchNow = (store.products || []).length > 0;
  const isSetWindow = store.storeOpenMode !== "stay-open" && (store.storeOpenStart || store.storeOpenEnd);
  const today = new Date("2026-06-04");
  const wStart = new Date(store.storeOpenStart || "2026-01-01");
  const wEnd = new Date(store.storeOpenEnd || "2026-12-31");
  const wTotal = Math.max((wEnd - wStart) / 86400000, 1);
  const wElapsed = Math.min(Math.max((today - wStart) / 86400000, 0), wTotal);
  const wPct = Math.round((wElapsed / wTotal) * 100);
  const daysLeft = Math.max(Math.round((wEnd - today) / 86400000), 0);
  const raised = store.revenue || 0;
  // Mirror the Overview "Active Ordering Windows" row treatment (Epic E).
  const winUrgent = !closed && daysLeft <= 7;
  const winMono = (store.teamName || store.storeTitle || "T").slice(0, 2).toUpperCase();
  const winColor = store.teamColors?.[0] || "#1d3a8a";
  return (
    <React.Fragment>
    {(section === "store" || section === "all") && <React.Fragment>
    <h3 style={{ margin: "0 0 4px", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Store Details</h3>
    <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 18px" }}>Identity, branding, fundraising, the store's point of contact, and manager information.</p>
    <DetailCard title="Store details" onEdit={() => setEditing("details")}>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "0 40px" }}>
          <DetailRow label="Store Name" value={store.storeTitle} />
          <DetailRow label="Store Type" value={store.storeType === "single" ? "Individual Team Store" : "League Store"} />
          <DetailRow label="Team Name" value={store.teamName} />
          <DetailRow label="Sport" value={store.sport} />
        </div>
        {/* Team Logo + Team Colors — boxed for visual separation (figma Settings-tab reference) */}
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginTop: 20 }}>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "16px 16px" }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 12 }}>Team Logo</div>
            {store.teamLogo?.url ?
              <img src={store.teamLogo.url} alt="" style={{ width: 44, height: 44, borderRadius: 4, objectFit: "cover", border: "1px solid #ececf0" }} /> :
              store.teamLogo?.mono ?
              <div style={{ width: 44, height: 44, borderRadius: 4, background: store.teamLogo.color || "#0e0e10", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 15, border: "1px solid #ececf0" }}>{store.teamLogo.mono}</div> :
              <div style={{ width: 44, height: 44, borderRadius: 4, border: "1.5px dashed #d4d4d8" }} />}
          </div>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "16px 16px" }}>
            <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 12 }}>Team Colors</div>
            <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>
              {store.teamColors?.length > 0 ? store.teamColors.map((c, i) =>
                <div key={i} style={{
                  width: 36, height: 36, borderRadius: 4, background: c,
                  border: c?.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none"
                }} />
                ) : <span style={{ color: "#9a9aa1", fontSize: 13 }}>—</span>}
            </div>
          </div>
        </div>

        {/* Team Fundraising — view only (edit in modal) */}
        <div style={{ marginTop: 16, border: "1px solid #ececf0", borderRadius: 10, padding: "16px 16px" }}>
          <div style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".04em", color: "#0e0e10", marginBottom: 12 }}>Team Fundraising</div>
          {(() => {
            const on = store.fundraisingEnabled === true;
            const collecting = fundCollectingItems(store);
            const still = !on ? collecting.length : 0;
            return (
              <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
                <span style={{
                  width: 40, height: 40, borderRadius: 9, flexShrink: 0,
                  background: on ? "#fdecea" : "#f1f1f3", color: on ? "#e1251b" : "#6b6b72",
                  display: "inline-flex", alignItems: "center", justifyContent: "center",
                }}><IconDollar size={20} /></span>
                <div style={{ flex: 1 }}>
                  <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>Team Fundraising</div>
                  <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>
                    {on
                      ? "Items can add a fundraising amount on top of retail. Each item carries its own fundraising toggle."
                      : still > 0
                        ? <span>Fundraising is off for new items, but items with orders keep collecting. <a href="#" onClick={(e) => { e.preventDefault(); window.dispatchEvent(new CustomEvent("pte:filter-collecting")); }} style={{ color: "#0e0e10", fontWeight: 700 }}>View in Products →</a></span>
                        : "Fundraising is turned off for this store."}
                  </div>
                </div>
                {on
                  ? <span style={{ fontSize: 12, fontWeight: 700, borderRadius: 999, padding: "4px 12px", background: "#e7f5ed", color: "#1f9d55" }}>Enabled{collecting.length > 0 ? " · " + collecting.length + " item" + (collecting.length === 1 ? "" : "s") + " collecting" : ""}</span>
                  : still > 0
                    ? <span style={{ fontSize: 12, fontWeight: 700, borderRadius: 999, padding: "4px 12px", background: "#fdf3e2", color: "#9a6b15" }}>Disabled · {still} item{still === 1 ? "" : "s"} still collecting</span>
                    : <span style={{ fontSize: 12, fontWeight: 700, borderRadius: 999, padding: "4px 12px", background: "#f1f1f3", color: "#6b6b72" }}>Disabled</span>}
              </div>
            );
          })()}
        </div>
        {/* Store Point of Contact — moved into Store Details (edited via the Store Details modal) */}
        <div style={{ marginTop: 16, border: "1px solid #ececf0", borderRadius: 10, padding: "16px 16px" }}>
          <div style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".04em", color: "#0e0e10", marginBottom: 16 }}>Store Point of Contact</div>
          {(() => {
            const emLabel = store.contactEmailMode === "salesperson" ? "Salesperson Email" : store.contactEmailMode === "custom" ? "Custom Email" : "Team Manager Email";
            const emVal = store.contactEmailMode === "salesperson"
              ? ((SALESPEOPLE.find((s) => s.value === store.contactEmailSalesperson) || {}).email || "—")
              : store.contactEmailMode === "custom" ? (store.contactEmailCustom || "—") : (store.email || "—");
            const phLabel = store.contactPhoneMode === "salesperson" ? "Salesperson Phone" : store.contactPhoneMode === "custom" ? "Custom Phone" : "Team Manager Phone";
            const phVal = store.contactPhoneMode === "salesperson"
              ? ((SALESPEOPLE.find((s) => s.value === store.contactPhoneSalesperson) || {}).phone || "—")
              : store.contactPhoneMode === "custom" ? (store.contactPhoneCustom || "—") : (store.phone || "—");
            return (
              <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 24 }}>
                <div>
                  <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8 }}>
                    <IconMail size={15} style={{ color: "#e1251b" }} />
                    <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>Email</span>
                  </div>
                  <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{emLabel}</div>
                  <div style={{ fontSize: 13, color: "#6b6b72", marginTop: 2 }}>{emVal}</div>
                </div>
                <div>
                  <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 8 }}>
                    <IconPhone size={15} style={{ color: "#e1251b" }} />
                    <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase" }}>Phone</span>
                  </div>
                  <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{phLabel}</div>
                  <div style={{ fontSize: 13, color: "#6b6b72", marginTop: 2, fontFamily: "JetBrains Mono, monospace" }}>{phVal}</div>
                </div>
              </div>
            );
          })()}
        </div>
    </DetailCard>

    {/* League / Team manager information — lives in General (details, branding & contact) */}
    <div style={{ marginTop: 24 }}>
      <DetailCard title={store.storeType === "league" ? "League manager information" : "Team manager information"} onEdit={() => setEditing("shipping")}>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1.3fr 1.1fr 1.3fr auto", gap: 28, alignItems: "start", marginTop: 2 }}>
          <DetailField label="Name" value={store.shipName} />
          <div>
            <div style={{ fontSize: 12, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Street Address</div>
            <div style={{ fontSize: 14, color: "#3a3a40", lineHeight: 1.5 }}>
              {store.shipStreet ? store.shipStreet + (store.shipApt ? `, ${store.shipApt}` : "") : "—"}
              {(store.shipCity || store.shipState || store.shipZip) &&
                <div>{[store.shipCity, [store.shipState, store.shipZip].filter(Boolean).join(" ")].filter(Boolean).join(", ")}</div>}
            </div>
          </div>
          <DetailField label="Phone Number" value={store.phone ? `🇺🇸 +1 ${store.phone}` : "—"} />
          <DetailField label="Email" value={store.email} />
          <div>
            <div style={{ fontSize: 12, fontWeight: 600, color: "#0e0e10", marginBottom: 8 }}>Contact Preference</div>
            <div style={{ display: "flex", gap: 8 }}>
              {["Email", "Phone"].map((ch) => {
                const on = (store.contactPref || "").toLowerCase().includes(ch.toLowerCase());
                return <span key={ch} style={{ fontSize: 12, fontWeight: 700, borderRadius: 999, padding: "4px 12px", background: on ? "#0e0e10" : "#f1f1f3", color: on ? "#fff" : "#6b6b72" }}>{ch}</span>;
              })}
            </div>
          </div>
        </div>
      </DetailCard>
    </div>

    {/* 4 — categories are curated on the Products tab; this card is the pointer. */}
    <div style={{ marginTop: 24 }}>
      <DetailCard title="Storefront categories &amp; sorting" desc="Category sections, subcategories, and product order are managed inline on the Products tab.">
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginTop: 2 }}>
          <span style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5, maxWidth: 520 }}>
            Drag sections and products on the Products tab to set the storefront order. Categories added there become the storefront header tabs.
          </span>
          <button onClick={() => { window.dispatchEvent(new CustomEvent("ts:openProductCats")); }} style={{
            flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8,
            background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
            padding: "12px 16px", fontSize: 13.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit",
          }}>
            <IconGrid size={14} /> Open Products
          </button>
        </div>
      </DetailCard>
    </div>

    {/* #10 — Store schedule / order window + status controls + danger zone */}
    </React.Fragment>}

    {(section === "schedule" || section === "all") && <React.Fragment>
    <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, flexWrap: "wrap", margin: section === "all" ? "32px 0 4px" : "0 0 4px" }}>
      <h3 style={{ margin: 0, fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Schedule & Status</h3>
      <div style={{ display: "inline-flex", gap: 4, background: "#f1f1f3", borderRadius: 8, padding: 4 }}>
        {[{ id: "set-window", label: "Set window" }, { id: "stay-open", label: "Always open" }].map((o) => {
          const on = (store.storeOpenMode || "set-window") === o.id;
          return (
            <button key={o.id} type="button" onClick={() => setScheduleMode(o.id)} style={{
              border: 0, borderRadius: 6, padding: "8px 16px", fontSize: 12.5, fontWeight: 700, fontFamily: "inherit",
              background: on ? "#0e0e10" : "transparent", color: on ? "#fff" : "#6b6b72",
              cursor: on ? "default" : "pointer", transition: "background .12s, color .12s",
            }}>{o.label}</button>
          );
        })}
      </div>
    </div>
    <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 18px" }}>The store's active ordering window and its live status, including the archive action.</p>
    <div style={{ display: "grid", gridTemplateColumns: "1fr", gap: 16 }}>
      <div style={{ background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, padding: 22, position: "relative", transition: "box-shadow .12s, border-color .12s" }} onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)"; e.currentTarget.style.borderColor = "#c9c9cf"; }} onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#e4e4e7"; }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 16 }}>
          <h2 style={{ fontSize: 13, fontWeight: 700, margin: 0, color: "#0e0e10" }}>Active Order Window</h2>
        </div>
        {/* Overview "Active Ordering Windows" row treatment, in card form (Epic E). */}
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 16 }}>
          <span style={{ width: 40, height: 40, borderRadius: "50%", background: winColor, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13.5, flexShrink: 0 }}>{winMono}</span>
          <div style={{ flex: 1, minWidth: 0 }}>
            <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", lineHeight: 1.2, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{store.teamName || store.storeTitle}</div>
            <div style={{ fontSize: 13, color: "#6b6b72", marginTop: 4 }}>
              <span style={{ fontWeight: 700, color: "#0e0e10" }}>${raised.toLocaleString(undefined, { minimumFractionDigits: 2, maximumFractionDigits: 2 })}</span> raised{isSetWindow ? " this window" : " so far"}
            </div>
          </div>
          {isSetWindow
            ? <span style={{ flexShrink: 0, whiteSpace: "nowrap", fontSize: 12, fontWeight: 700, borderRadius: 999, padding: "4px 12px", background: closed ? "#f1f1f3" : winUrgent ? "#fdeaea" : "#eef3fb", color: closed ? "#6b6b72" : winUrgent ? "#e1251b" : "#2563eb" }}>{closed ? "Closed" : `${daysLeft}d left`}</span>
            : <span style={{ flexShrink: 0, whiteSpace: "nowrap", fontSize: 12, fontWeight: 700, borderRadius: 999, padding: "4px 12px", background: closed ? "#f1f1f3" : "#e7f5ed", color: closed ? "#6b6b72" : "#1f9d55" }}>{closed ? "Closed" : "Always open"}</span>}
        </div>
        {isSetWindow ? (
          <React.Fragment>
            <div style={{ height: 8, borderRadius: 999, background: "#f1f1f3", overflow: "hidden" }}>
              <div style={{ width: `${closed ? 100 : wPct}%`, height: "100%", borderRadius: 999, background: closed ? "#b81d14" : winUrgent ? "#e1251b" : "#0e0e10" }} />
            </div>
            <div style={{ display: "flex", justifyContent: "space-between", marginTop: 8, fontSize: 12, color: "#9a9aa1" }}>
              <span>{formatStoreDate(store.storeOpenStart)}</span>
              <span>{formatStoreDate(store.storeOpenEnd)}</span>
            </div>
          </React.Fragment>
        ) : (
          <div style={{ display: "flex", alignItems: "center", gap: 8, padding: "12px 16px", background: "#f7f7f9", borderRadius: 8, fontSize: 13, color: "#6b6b72" }}>
            <IconClock size={15} style={{ flexShrink: 0 }} />
            <span>No closing date — this store stays open until you close it manually.</span>
          </div>
        )}
        <OrderWindowScheduler store={store} org={org} today={today} storeClosed={closed || archived} hasWindow={isSetWindow} onToast={setToast} />
        {/* 3.2 — repeating close dates (league stores: sub-option of the single window) */}
        <RepeatingCloseBlock store={store} onUpdateStore={onUpdateStore} onToast={setToast} />
      </div>

      <DetailCard title="Store status" desc="Open or close the storefront to buyers; closing is reversible.">
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 8 }}>
          <span style={{
            display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, fontWeight: 700,
            color: archived ? "#6b6b72" : closed ? "#b81d14" : draft ? "#2563eb" : "#1f9d55",
            background: archived ? "#f1f1f3" : closed ? "#fbe3e1" : draft ? "#eef3fb" : "#e7f5ed",
            borderRadius: 999, padding: "4px 12px",
          }}>
            <span style={{ width: 8, height: 8, borderRadius: "50%", background: "currentColor" }} />
            {archived ? "Archived" : closed ? "Closed" : draft ? "Draft" : "Open"}
          </span>
        </div>
        <p style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.55, margin: "8px 0 16px" }}>
          {archived
            ? "This store is archived and hidden from buyers. Its data is retained for reporting."
            : closed
              ? "Buyers can't place new orders while the store is closed. You can re-open it at any time."
              : draft
                ? "This store is a draft \u2014 it hasn't launched yet, so buyers can't see it. Launch when you're ready to start taking orders."
                : "Your store is live and accepting orders. You can temporarily close it without losing any data."}
        </p>
        {!archived &&
        <div style={{ display: "flex", flexDirection: draft ? "column" : "row", gap: draft ? 8 : 12, flexWrap: "wrap", alignItems: draft ? "flex-start" : "center" }}>
          {draft
            ? <React.Fragment>
                <button disabled={!canLaunchNow} onClick={() => { window.__openLaunchModal && window.__openLaunchModal(); }}
                title={canLaunchNow ? "Launch this team store" : "Add at least one product to launch your store"}
                style={{
                  display: "inline-flex", alignItems: "center", gap: 8,
                  background: canLaunchNow ? "#0e0e10" : "#ececf0", color: canLaunchNow ? "#fff" : "#9a9aa1",
                  border: `1px solid ${canLaunchNow ? "#0e0e10" : "#ececf0"}`, borderRadius: 6,
                  padding: "12px 20px", fontSize: 13.5, fontWeight: 700, fontFamily: "inherit",
                  cursor: canLaunchNow ? "pointer" : "not-allowed"
                }}>
                  <IconStore size={14} style={{ strokeWidth: 2.2 }} /> Launch Store
                </button>
                {!canLaunchNow && <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>Add at least one product to launch your store.</span>}
              </React.Fragment>
            : closed
              ? <Button variant="primary" onClick={() => { onUpdateStore && onUpdateStore({ status: "active", live: true }); setToast("Store re-opened"); }}>
                  <IconUnlock size={14} style={{ strokeWidth: 2.2 }} /> Re-open Store
                </Button>
              : <Button variant="secondary" onClick={() => setConfirmClose(true)}>
                  <IconLock size={14} style={{ strokeWidth: 2.2 }} /> Close Store
                </Button>}
        </div>
        }

        <StoreCloseReportEntry store={store} variant="settings" />

        {/* Danger zone — owner only (Epic C) */}
        {store.role !== "manager" && !archived && !draft &&
        <div style={{ marginTop: 24, paddingTop: 16, borderTop: "1px solid #f3c9c5" }}>
          <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".06em", color: "#b81d14", marginBottom: 8 }}>DANGER ZONE</div>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16 }}>
            <span style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5 }}>Archive this store. It will be hidden from buyers and removed from your active list.</span>
            <button onClick={() => setConfirmArchive(true)} style={{
              flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8,
              background: "#fff", border: "1px solid #e1251b", borderRadius: 6,
              padding: "12px 16px", fontSize: 13.5, fontWeight: 700, color: "#b81d14", cursor: "pointer", fontFamily: "inherit",
            }}>
              <IconTrash size={14} style={{ strokeWidth: 2.2 }} /> Archive Store
            </button>
          </div>
        </div>
        }
      </DetailCard>
    </div>
    </React.Fragment>}

    {(section === "team" || section === "all") && <React.Fragment>
    <h3 style={{ margin: section === "all" ? "32px 0 4px" : "0 0 4px", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Team & Roster</h3>
    <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 18px" }}>Roster rules and how jersey numbers are chosen for numbered team gear.</p>
        {/* 7a — Roster Rules is a BULK DEFAULT. The value in force lives on each team. */}
        <RosterRulesDefaultCard store={store} org={org} onUpdateStore={onUpdateStore} onToast={setToast} />
    <NumberSelectionCard store={store} onUpdateStore={onUpdateStore} onToast={setToast} />
    </React.Fragment>}

    {/* Store Customization & Design (Epic I) — storefront color & announcement.
        Both cards are EXEMPT from post-launch locking (Epic B): no lock icons,
        no disabled states, on any store state. */}
    {(section === "customization" || section === "all") && <React.Fragment>
    <div style={{ marginTop: section === "all" ? 32 : 0 }}>
      <StoreCustomizationSection store={store} org={org} onUpdateStore={onUpdateStore} onToast={setToast} onGoMyTeams={onGoMyTeams} />
    </div>
    </React.Fragment>}

    {(section === "people" || section === "all") && <React.Fragment>
    <div style={{ marginTop: section === "all" ? 32 : 0 }}>
      <TMAccessSection store={store} />
    </div>
    </React.Fragment>}

    {(section === "distribution" || section === "all") && <React.Fragment>
    {/* Fulfillment Method — view only (edit in modal) */}
    <div style={{ marginTop: section === "distribution" ? 0 : 24 }}>
      <DetailCard title="Fulfillment method" onEdit={() => setEditing("distribution")}>
        {(() => {
          const m = FULFILLMENT_METHODS.find((x) => x.key === (store.distributionModel || "coach")) || FULFILLMENT_METHODS[0];
          return (
            <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
              <span style={{ width: 40, height: 40, borderRadius: 9, flexShrink: 0, background: "#f1f1f3", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{m.icon({ size: 20 })}</span>
              <div style={{ minWidth: 0 }}>
                <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}>
                  <span style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10" }}>{m.title}</span>
                  {m.badge && <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".06em", color: "#1f9d55", background: "#e7f5ed", borderRadius: 4, padding: "2px 8px" }}>{m.badge}</span>}
                </div>
                <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45 }}>{m.desc}</div>
                {m.key === "coach" && (
                  <div style={{ marginTop: 12, paddingTop: 12, borderTop: "1px solid #f1f1f3" }}>
                    <div style={{ fontSize: 10.5, fontWeight: 800, letterSpacing: ".06em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 4 }}>Ship-To Address</div>
                    {shipToSummary(store)
                      ? <div style={{ fontSize: 13, color: "#0e0e10", fontWeight: 600 }}>{shipToSummary(store)}</div>
                      : <div style={{ fontSize: 12.5, color: "#b06f1e", fontWeight: 600 }}>No destination address yet — orders can't be submitted for fulfillment until one is set.</div>}
                  </div>
                )}
              </div>
            </div>
          );
        })()}
      </DetailCard>
    </div>

    {/* Team Manager & Freight Mapping — gated: League store + Group Coach Freight */}
    {(store.storeType === "league") && ((store.distributionModel || "coach") === "coach") && org && (
      <TeamManagerMapping store={store} org={org} setOrg={setOrg} focus={managerFocus} />
    )}
    </React.Fragment>}

    {/* Close store confirm (reversible) */}
    <Modal open={confirmClose} onClose={() => setConfirmClose(false)} width={460}>
      <div style={{ padding: "28px 28px 8px" }}>
        <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Close this store?</h2>
        <p style={{ margin: "12px 0 0", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>
          Buyers won't be able to place new orders while the store is closed. Existing orders are unaffected, and you can re-open the store at any time.
        </p>
      </div>
      <div style={{ padding: "20px 28px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
        <Button variant="secondary" onClick={() => setConfirmClose(false)}>Cancel</Button>
        <Button variant="primary" onClick={() => { onUpdateStore && onUpdateStore({ status: "closed", live: false }); setConfirmClose(false); setToast("Store closed"); }}>Close Store</Button>
      </div>
    </Modal>

    {/* Archive confirm (destructive, owner-only) */}
    <Modal open={confirmArchive} onClose={() => setConfirmArchive(false)} width={480}>
      <div style={{ padding: "28px 28px 8px" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 12 }}>
          <span style={{ width: 40, height: 40, borderRadius: "50%", background: "#fbe3e1", color: "#b81d14", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
            <IconAlert size={19} style={{ strokeWidth: 2.2 }} />
          </span>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800 }}>Archive {store.teamName || store.storeTitle}?</h2>
        </div>
        <p style={{ margin: "0 0 12px", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>Archiving a store will:</p>
        <ul style={{ margin: "0 0 4px", paddingLeft: 20, fontSize: 13.5, color: "#0e0e10", lineHeight: 1.85 }}>
          <li>Hide the storefront from all buyers immediately</li>
          <li>Stop all new orders and close any active ordering window</li>
          <li>Remove it from your active stores list</li>
        </ul>
        <p style={{ margin: "12px 0 0", fontSize: 13, color: "#6b6b72", lineHeight: 1.6 }}>Order history and reports are retained. This can be undone by support.</p>
      </div>
      <div style={{ padding: "20px 28px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
        <Button variant="secondary" onClick={() => setConfirmArchive(false)}>Cancel</Button>
        <button onClick={() => { onUpdateStore && onUpdateStore({ status: "archived", live: false, archived: true }); setConfirmArchive(false); setToast("Store archived"); }} style={{
          display: "inline-flex", alignItems: "center", gap: 8, background: "#e1251b", border: 0,
          borderRadius: 6, padding: "12px 20px", fontSize: 14, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit",
        }}>
          <IconTrash size={14} style={{ strokeWidth: 2.2 }} /> Archive Store
        </button>
      </div>
    </Modal>

    <EditStoreDetailsModal open={editing === "details"} store={store} onClose={() => setEditing(null)} onSave={save} accent={store.primaryColor || "#e1251b"} />
    <EditFulfillmentMethodModal open={editing === "distribution"} store={store} onClose={() => setEditing(null)} onSave={save} accent={store.primaryColor || "#e1251b"} />
    <EditShippingTaxModal open={editing === "shipping"} store={store} onClose={() => setEditing(null)} onSave={save} />
    <Toast message={toast} onDone={() => setToast(null)} />
    </React.Fragment>);

}

function DetailCard({ title, desc, children, onEdit }) {
  return (
    <div style={{
      background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10,
      padding: 22, position: "relative", transition: "box-shadow .12s, border-color .12s"
    }} onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)"; e.currentTarget.style.borderColor = "#c9c9cf"; }} onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#e4e4e7"; }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: desc ? 4 : 16 }}>
        <h2 style={{ fontSize: 13, fontWeight: 700, margin: 0, color: "#0e0e10" }}>{title}</h2>
        {onEdit && <button onClick={onEdit} style={{
          background: "transparent", border: 0, cursor: "pointer", padding: 4,
          color: "#6b6b72", display: "inline-flex", borderRadius: 4, transition: "color .12s, background .12s"
        }} title="Edit"
        onMouseEnter={(e) => {e.currentTarget.style.color = "#0e0e10";e.currentTarget.style.background = "#f1f1f3";}}
        onMouseLeave={(e) => {e.currentTarget.style.color = "#6b6b72";e.currentTarget.style.background = "transparent";}}>
          <IconEdit size={16} />
        </button>}
      </div>
      {desc && <p style={{ fontSize: 12, color: "#71717a", margin: "0 0 14px", lineHeight: 1.5 }}>{desc}</p>}
      {children}
    </div>);

}

function DetailRow({ label, value }) {
  return (
    <div style={{ marginTop: 16 }}>
      <div style={{ fontSize: 12, fontWeight: 600, color: "#0e0e10", marginBottom: 4 }}>{label}</div>
      <div style={{ fontSize: 13, color: value ? "#6b6b72" : "#c4c4cc" }}>{value || "—"}</div>
    </div>);

}

function DetailField({ label, value }) {
  return (
    <div>
      <div style={{ fontSize: 12, fontWeight: 600, color: "#0e0e10", marginBottom: 4 }}>{label}</div>
      <div style={{ fontSize: 13, color: value ? "#6b6b72" : "#c4c4cc" }}>{value || "—"}</div>
    </div>);

}

function _Unused_TabPlaceholder({ name }) {
  return (
    <div style={{
      border: "1px dashed #d4d4d8", borderRadius: 8, padding: "80px 20px",
      textAlign: "center", color: "#6b6b72"
    }}>
      <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>No {name.toLowerCase()} yet.</div>
      <div style={{ fontSize: 13 }}>This is a preview — set up {name.toLowerCase()} to launch your store.</div>
    </div>);

}

Object.assign(window, { Dashboard });

/* ===== ASSET 20 : bb8dea27-db64-4569-b0c1-4c9a0ff0db38 ===== */
// Products flow — configure a pending product, then list drafts/published.

// ─────────── Draft / Ready status badge ───────────
// A product is a "Draft" until its logo application sizes are confirmed; once
// they are, it becomes "Ready" to publish to parents. Used on the configure
// card and on each product card in the Drafts list.
function ProductStatusBadge({ ready, size = "md" }) {
  const sm = size === "sm";
  const c = ready ?
  { bg: "#e7f5ed", ink: "#1f8a55", label: "READY" } :
  { bg: "#fff4e0", ink: "#b5760a", label: "DRAFT" };
  return (
    <span style={{
      display: "inline-flex", alignItems: "center", gap: sm ? 5 : 6,
      background: c.bg, color: c.ink, borderRadius: 999,
      padding: sm ? "3px 9px" : "5px 11px",
      fontSize: sm ? 10 : 11, fontWeight: 800, letterSpacing: ".06em"
    }}>
      <span style={{ width: sm ? 5 : 6, height: sm ? 5 : 6, borderRadius: "50%", background: c.ink, display: "inline-block" }} />
      {c.label}
    </span>);

}

// ─────────── Configure a pending product (the "pre-add" page) ───────────
// Fully controlled — parent owns config so multiple items can be configured in parallel.
function ConfigureProduct({ product, store, config, onConfigChange, onProductChange, onDelete, index, total }) {
  const teamSetting = config.teamSetting;
  const pricing = config.pricing;
  const sizesConfirmed = config.sizesConfirmed;
  const showSizesModal = config.showSizesModal;
  const applicationSizes = config.applicationSizes;
  const [editingName, setEditingName] = React.useState(false);
  const nameInputRef = React.useRef(null);
  React.useEffect(() => {
    if (editingName && nameInputRef.current) {
      nameInputRef.current.focus();
      nameInputRef.current.select();
    }
  }, [editingName]);

  const patch = (p) => onConfigChange({ ...config, ...p });
  const setTeamSetting = (v) => patch({ teamSetting: v });
  const setShowSizesModal = (v) => patch({ showSizesModal: v });
  const setApplicationSizes = (v) => patch({ applicationSizes: v });

  const updatePricing = (size, field, value) => {
    const next = { ...pricing, [size]: { ...pricing[size], [field]: value } };
    const baseNum = parseFloat(next[size].base) || 0;
    const dealerNum = parseFloat(next[size].dealer) || 0;
    const fundNum = parseFloat(next[size].fund) || 0;
    const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
    const fundAmt = next[size].fundUnit === "$" ? fundNum : baseNum * fundNum / 100;
    next[size].retail = (baseNum + dealerAmt + ((next.fundraisingOn ?? (store?.fundraisingEnabled === true)) ? fundAmt : 0)).toFixed(2);
    patch({ pricing: next });
  };
  const fundOn = store?.fundraisingEnabled === true;
  const priceGridCols = fundOn ? "60px 80px 1fr 1fr 1fr 80px" : "60px 80px 1fr 1fr 80px";

  return (
    <div>
      <div style={{
        background: "#fff", border: "1px solid #ececf0", borderRadius: 12,
        boxShadow: "0 1px 2px rgba(20,20,30,.03)", padding: 28
      }}>
        {/* Top row: product name + actions */}
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 24, gap: 16 }}>
          <div style={{ minWidth: 0, flex: 1 }}>
            {editingName ?
            <input
              ref={nameInputRef}
              value={product.title || ""}
              placeholder="[product name]"
              onChange={(e) => onProductChange && onProductChange({ ...product, title: e.target.value })}
              onBlur={() => setEditingName(false)}
              onKeyDown={(e) => {if (e.key === "Enter" || e.key === "Escape") setEditingName(false);}}
              style={{
                fontSize: 20, fontWeight: 700, color: "#0e0e10",
                border: "1px solid #d4d4d8", borderRadius: 6, padding: "4px 12px",
                outline: 0, background: "#fff", width: "100%", maxWidth: 460,
                fontFamily: "inherit"
              }} /> :


            <button
              onClick={() => setEditingName(true)}
              title="Edit product name"
              style={{
                background: "transparent", border: 0, padding: 0, cursor: "text",
                display: "inline-flex", alignItems: "center", gap: 12,
                color: "#0e0e10"
              }}>
                <h2 style={{ fontSize: 20, fontWeight: 700, margin: 0, color: "#0e0e10" }}>
                  {product.title || "[product name]"}
                </h2>
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="#6b6b72" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" style={{ flexShrink: 0 }}>
                  <path d="M12 20h9" />
                  <path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z" />
                </svg>
              </button>
            }
            <div style={{ fontSize: 13, color: "#6b6b72", marginTop: 4 }}>
              {product.styleName || "Set-in Faux, Full button Jersey"}
            </div>
            <div style={{ marginTop: 12 }}>
              <ProductStatusBadge ready={sizesConfirmed} />
            </div>
            {total > 1 &&
            <div style={{ fontSize: 11, color: "#9a9aa1", fontWeight: 700, letterSpacing: ".10em", marginTop: 8 }}>
                ITEM {index + 1} OF {total}
              </div>
            }
          </div>

          <div style={{ display: "inline-flex", alignItems: "center", gap: 12, flexShrink: 0 }}>
            <button
              onClick={onDelete}
              style={{
                background: "#fff", color: "#0e0e10",
                border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 16px",
                fontSize: 13, fontWeight: 600, cursor: "pointer",
                display: "inline-flex", alignItems: "center", gap: 8
              }}>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <polyline points="3 6 5 6 21 6" />
                <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" />
                <path d="M10 11v6" />
                <path d="M14 11v6" />
                <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" />
              </svg>
              Delete Product
            </button>
            {pteRestricted(product) && <button data-tour={sizesConfirmed ? undefined : "confirm-sizes"} onClick={() => setShowSizesModal(true)} style={{
              background: sizesConfirmed ? "#1f9d55" : "#0e0e10", color: "#fff",
              border: 0, borderRadius: 6, padding: "12px 24px",
              fontSize: 13, fontWeight: 700, cursor: "pointer",
              display: "inline-flex", alignItems: "center", gap: 8
            }}>
              {sizesConfirmed && <IconCheck size={14} style={{ strokeWidth: 3 }} />}
              {sizesConfirmed ? "Application Sizes Confirmed" : "Confirm Application Sizes"}
            </button>}
          </div>
        </div>

        {/* Made-to-order notice — restricted sources only */}
        {pteRestricted(product) && (
        <div style={{ display: "flex", gap: 10, alignItems: "flex-start", background: "#fff8ef", border: "1px solid #f2e2c9", borderRadius: 8, padding: "10px 14px", marginBottom: 16 }}>
          <span style={{ color: "#b06f1e", display: "inline-flex", marginTop: 1 }}><IconAlert size={15} /></span>
          <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}><strong style={{ color: "#0e0e10" }}>This item is made to order.</strong> Its design cannot be changed after it is added to the store, and no changes can be made after its first order.</span>
        </div>
        )}

        {/* Main three-column row */}
        <div style={{ display: "grid", gridTemplateColumns: "260px 320px 1fr", gap: 16, alignItems: "stretch" }}>
          {/* Image */}
          <div style={{
            background: "#f1f2f4", borderRadius: 8,
            display: "flex", alignItems: "center", justifyContent: "center",
            padding: 24
          }}>
            <div style={{ width: 160, aspectRatio: "2 / 3", maxHeight: "100%" }}>
              {product.thumbnail || <DefaultProductThumb store={store} product={product} />}
            </div>
          </div>

          {/* Product Options */}
          <div style={{ border: "1px solid #ececf0", borderRadius: 8, padding: 24, display: "flex", flexDirection: "column" }}>
            <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 16 }}>
              Product Options
            </div>
            <div style={{ flex: 1 }}>
              {[
              { key: "optRequired", label: "Team Required", tipTitle: "Team Required", tipBody: "Mandatory gear that rostered players must purchase to participate (e.g., home/away uniforms, warm-ups). The player is responsible for covering the full cost of these items at checkout." },
              { key: "optSupplied", label: "Team Supplied", tipTitle: "Team Supplied", tipBody: "Mandatory participation gear that has already been paid for collectively (e.g., through team sign-up fees or fundraisers). Players must still select their sizes, but they will pay $0 for these items at checkout." }].
              map((opt) =>
              <div key={opt.key} style={{ display: "flex", alignItems: "center", gap: 12, padding: "12px 0" }}>
                  <BundleToggle on={!!config[opt.key]} onChange={(v) => patch(
                  // A product is Team Required OR Team Supplied — never both. Toggling
                  // on locks Category to Team Gear; both off clears it (selection required).
                  v ?
                  { optRequired: opt.key === "optRequired", optSupplied: opt.key === "optSupplied", storeCategory: "Team Gear", storeSubcategory: "", catError: false } :
                  { [opt.key]: false, storeCategory: "", storeSubcategory: "" }
                )} />
                  <span style={{ fontSize: 14, color: "#0e0e10" }}>{opt.label}</span>
                  <InfoTooltip title={opt.tipTitle} body={opt.tipBody} />
                </div>
              )}
              {(() => {
                const tOn = !!(config.optRequired || config.optSupplied);
                const subs = (((window.__prodCats || []).find((c) => c.name === (tOn ? "Team Gear" : config.storeCategory))) || {}).subs || [];
                return (
                  <div style={{ marginTop: 10, display: "grid", gap: 8 }}>
                    {tOn ?
                    <div style={{ display: "flex", alignItems: "center", gap: 8, height: 36, boxSizing: "border-box", border: "1px solid #ececf0", borderRadius: 6, padding: "0 10px", fontSize: 12.5, background: "#fafafb", color: "#0e0e10", fontWeight: 600 }}>Team Gear<span style={{ marginLeft: "auto", display: "inline-flex", color: "#9a9aa1" }}><IconLock size={12} /></span></div> :
                    <select value={config.storeCategory || ""} onChange={(e) => patch({ storeCategory: e.target.value, storeSubcategory: "", catError: false })} style={{ width: "100%", height: 36, boxSizing: "border-box", border: "1px solid " + (config.catError ? "#e1251b" : "#d4d4d8"), borderRadius: 6, padding: "0 10px", fontSize: 12.5, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }}>
                      <option value="">Select a category *</option>
                      {(window.__prodCats || []).map((c) => <option key={c.id} value={c.name}>{c.name}</option>)}
                    </select>}
                    <select value={config.storeSubcategory || ""} onChange={(e) => patch({ storeSubcategory: e.target.value })} disabled={subs.length === 0} style={{ width: "100%", height: 36, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 10px", fontSize: 12.5, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }}>
                      <option value="">{subs.length === 0 ? "No subcategories" : "No subcategory"}</option>
                      {subs.map((s) => <option key={s} value={s}>{s}</option>)}
                    </select>
                    {config.catError && <div style={{ fontSize: 11.5, color: "#e1251b", fontWeight: 600 }}>Category is required — items no longer default to Fan Gear.</div>}
                    {store?.storeType === "league" && (
                      <RosterAssignField variant="dropdown" direction="down" value={config.rosters || []} onChange={(ids) => patch({ rosters: ids })} accent="#e1251b" minOne={tOn} store={store} />
                    )}
                  </div>
                );
              })()}
            </div>
            <div style={{ marginTop: 20 }}>
              <button style={{
                width: "100%",
                background: "#fff", border: "1px solid #ececf0", borderRadius: 6,
                padding: "12px", fontSize: 13, color: "#0e0e10", cursor: "pointer",
                display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, fontWeight: 500
              }}>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none">
                  <path d="M4 14L14 4l3 3-10 10H4z" fill="#3acc7a" />
                  <path d="M14 4l3 3 3-3-3-3z" fill="#e1251b" />
                  <path d="M17 7l3-3" stroke="#2563eb" strokeWidth="1.5" />
                </svg>
                Customize
              </button>
            </div>
          </div>

          {/* Price settings — shared Governing-rule-3 card ($ / % mode) */}
          <PriceSettingsCard
            pricing={pricing}
            onChange={(next) => patch({ pricing: next })}
            fundOn={fundOn}
            covered={teamSetting === "team-supplied"} />
        </div>
      </div>

            {/* Sizes confirmation modal */}
      <ConfirmSizesModal
        open={showSizesModal}
        product={product}
        store={store}
        rows={applicationSizes}
        onChange={setApplicationSizes}
        onClose={() => setShowSizesModal(false)}
        onConfirm={() => patch({ sizesConfirmed: true, showSizesModal: false })} />
      
    </div>);

}

function defaultApplicationSizes() {
  return [
  {
    id: "front-number", type: "FRONT NUMBER", placement: "FRONT",
    iconKind: "hash",
    details: { TEXT: "15", ACCENT: "TWO COLOR", FONT: "HAWKS", PATTERN: "NONE", LAYOUT: "NONE" },
    colors: ["#0e0e10", "#0e0e10"],
    adultSize: "AS SHOWN", youthSize: "AS SHOWN",
    notes: ""
  },
  {
    id: "team-name", type: "TEAM NAME", placement: "FRONT",
    iconKind: "text",
    details: { TEXT: "HOYAS", ACCENT: "TWO COLOR", FONT: "HAWKS", PATTERN: "NONE", LAYOUT: "STRAIGHT" },
    colors: ["#0e0e10", "#0e0e10"],
    adultSize: "AS SHOWN", youthSize: "AS SHOWN",
    notes: ""
  },
  {
    id: "back-number", type: "BACK NUMBER", placement: "BACK",
    iconKind: "hash",
    details: { TEXT: "15", ACCENT: "TWO COLOR", FONT: "HAWKS", PATTERN: "NONE", LAYOUT: "NONE" },
    colors: ["#0e0e10", "#0e0e10"],
    adultSize: "AS SHOWN", youthSize: "AS SHOWN",
    notes: ""
  }];

}

// Product Settings + Price Settings sections rendered inside the sizes modal
// (used by the store-creation wizard so a product is fully configured before
// it's committed). Mirrors the ConfigureProduct page's settings + pricing UI.
function ModalConfigSections({ config, onChange, store, allowRosterAssign, product }) {
  const pricing = config.pricing;
  const fundOn = store?.fundraisingEnabled === true;
  // Stock items without a youth price (e.g. most Sideline Stock) hide youth pricing.
  const noYouth = !!product && product.srcKind === "vault" && product.priceYouth == null;
  const [teamPrompt, setTeamPrompt] = React.useState(null); // { apply } | null
  const tOn = config.teamSetting === "team-required" || config.teamSetting === "team-supplied";
  // Confirm was blocked because a TR/TS item has no team assigned — surface the prompt.
  React.useEffect(() => {
    if (config.teamError) setTeamPrompt({ apply: (extra) => onChange({ ...config, teamError: false, ...(extra || {}) }) });
  }, [config.teamError]);
  const mSubs = (((window.__prodCats || []).find((c) => c.name === (tOn ? "Team Gear" : config.storeCategory))) || {}).subs || [];
  // Inline "add new" for Category / Subcategory dropdowns.
  const [addingCat, setAddingCat] = React.useState(false);
  const [addingSub, setAddingSub] = React.useState(false);
  const [newCatName, setNewCatName] = React.useState("");
  const [newSubName, setNewSubName] = React.useState("");
  const commitNewCat = () => {
    const name = newCatName.trim();
    setAddingCat(false); setNewCatName("");
    if (!name) return;
    const list = window.__prodCats || (window.__prodCats = []);
    let c = list.find((x) => x.name.toLowerCase() === name.toLowerCase());
    if (!c) { c = { id: "cat-" + name.toLowerCase().replace(/[^a-z0-9]+/g, "-"), name, subs: [] }; window.__prodCats = [...list, c]; }
    onChange({ ...config, storeCategory: c.name, storeSubcategory: "", catError: false });
  };
  const commitNewSub = () => {
    const name = newSubName.trim();
    setAddingSub(false); setNewSubName("");
    if (!name) return;
    const c = (window.__prodCats || []).find((x) => x.name === (tOn ? "Team Gear" : config.storeCategory));
    if (!c) return;
    if (!(c.subs || []).some((s) => s.toLowerCase() === name.toLowerCase())) c.subs = [...(c.subs || []), name];
    onChange({ ...config, storeSubcategory: name });
  };
  const addInputStyle = { width: "100%", height: 40, boxSizing: "border-box", border: "1px solid #0e0e10", borderRadius: 6, padding: "0 10px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" };
  const mGridCols = fundOn ? "70px 90px 1fr 1fr 1fr 90px" : "70px 90px 1fr 1fr 90px";
  const updatePricing = (size, field, value) => {
    const next = { ...pricing, [size]: { ...pricing[size], [field]: value } };
    const baseNum = parseFloat(next[size].base) || 0;
    const dealerNum = parseFloat(next[size].dealer) || 0;
    const fundNum = parseFloat(next[size].fund) || 0;
    const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
    const fundAmt = next[size].fundUnit === "$" ? fundNum : baseNum * fundNum / 100;
    next[size].retail = (baseNum + dealerAmt + ((next.fundraisingOn ?? (store?.fundraisingEnabled === true)) ? fundAmt : 0)).toFixed(2);
    onChange({ ...config, pricing: next });
  };
  const setTeam = (which) => onChange({ ...config, teamSetting: which });
  const TEAM_OPTS = [
    { key: "team-required", label: "Team Required", tip: "Mandatory gear that rostered players must purchase to participate (e.g., home/away uniforms). The player covers the full cost at checkout." },
    { key: "team-supplied", label: "Team Supplied", tip: "Mandatory gear already paid for collectively (sign-up fees or fundraisers). Players still pick sizes but pay $0 at checkout." },
  ];
  return (
    <div style={{ marginTop: 24, display: "grid", gap: 16 }}>
      {/* Made-to-order notice — restricted sources only (Vault / catalog items show none) */}
      {(!product || pteRestricted(product)) && (
      <div style={{ display: "flex", gap: 10, alignItems: "flex-start", background: "#fff8ef", border: "1px solid #f2e2c9", borderRadius: 10, padding: "12px 16px" }}>
        <span style={{ color: "#b06f1e", display: "inline-flex", marginTop: 1 }}><IconAlert size={15} /></span>
        <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}><strong style={{ color: "#0e0e10" }}>This item is made to order.</strong> Its design cannot be changed after it is added to the store, and no changes can be made after its first order.</span>
      </div>
      )}
      {/* Category + team behavior — TR/TS auto-locks Category to Team Gear */}
      <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px" }}>
        <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 12, color: "#0e0e10" }}>Category</div>
        {TEAM_OPTS.map((opt) => (
          <div key={opt.key} style={{ display: "flex", alignItems: "center", gap: 12, padding: "8px 0" }}>
            <BundleToggle on={config.teamSetting === opt.key} onChange={(v) => {
              const next = v ? opt.key : "fan-gear";
              const nextOn = next !== "fan-gear";
              const apply = (extra) => onChange({ ...config, teamSetting: next, storeCategory: nextOn ? "Team Gear" : "", storeSubcategory: "", catError: false, ...(extra || {}) });
              // League stores: TR/TS can't save without ≥1 assigned Team.
              if (nextOn && allowRosterAssign && store?.storeType === "league" && !(config.rosters || []).length) { setTeamPrompt({ apply }); return; }
              apply();
            }} />
            <span style={{ fontSize: 14, color: "#0e0e10" }}>{opt.label}</span>
            <InfoTooltip title={opt.label} body={opt.tip} />
          </div>
        ))}
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginTop: 10 }}>
          <div>
            <label style={{ display: "block", fontSize: 11, fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 6 }}>Category{!tOn && <span style={{ color: "#e1251b" }}> *</span>}</label>
            {tOn ?
            <div style={{ display: "flex", alignItems: "center", gap: 8, height: 40, boxSizing: "border-box", border: "1px solid #ececf0", borderRadius: 6, padding: "0 10px", fontSize: 13, background: "#fafafb", color: "#0e0e10", fontWeight: 600 }}>Team Gear<span style={{ marginLeft: "auto", display: "inline-flex", color: "#9a9aa1" }}><IconLock size={13} /></span></div> :
            addingCat ?
            <input autoFocus value={newCatName} onChange={(e) => setNewCatName(e.target.value)} placeholder="New category name" onBlur={commitNewCat}
              onKeyDown={(e) => { if (e.key === "Enter") commitNewCat(); if (e.key === "Escape") { setAddingCat(false); setNewCatName(""); } }} style={addInputStyle} /> :
            <select value={config.storeCategory || ""} onChange={(e) => { if (e.target.value === "__add_new__") { setAddingCat(true); return; } onChange({ ...config, storeCategory: e.target.value, storeSubcategory: "", catError: false }); }} style={{ width: "100%", height: 40, boxSizing: "border-box", border: "1px solid " + (config.catError ? "#e1251b" : "#d4d4d8"), borderRadius: 6, padding: "0 10px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }}>
              <option value="">Select a category</option>
              {(window.__prodCats || []).map((c) => <option key={c.id} value={c.name}>{c.name}</option>)}
              <option value="__add_new__">+ Add new category…</option>
            </select>}
          </div>
          <div>
            <label style={{ display: "block", fontSize: 11, fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 6 }}>Subcategory</label>
            {addingSub ?
            <input autoFocus value={newSubName} onChange={(e) => setNewSubName(e.target.value)} placeholder="New subcategory name" onBlur={commitNewSub}
              onKeyDown={(e) => { if (e.key === "Enter") commitNewSub(); if (e.key === "Escape") { setAddingSub(false); setNewSubName(""); } }} style={addInputStyle} /> :
            <select value={config.storeSubcategory || ""} onChange={(e) => { if (e.target.value === "__add_new__") { setAddingSub(true); return; } onChange({ ...config, storeSubcategory: e.target.value }); }} disabled={!tOn && !config.storeCategory} style={{ width: "100%", height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 10px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }}>
              <option value="">{mSubs.length === 0 ? (!tOn && !config.storeCategory ? "No subcategories" : "No subcategories yet") : "No subcategory"}</option>
              {mSubs.map((s) => <option key={s} value={s}>{s}</option>)}
              {(tOn || config.storeCategory) && <option value="__add_new__">+ Add new subcategory…</option>}
            </select>}
          </div>
        </div>
        {config.catError && <div style={{ fontSize: 12, color: "#e1251b", marginTop: 8, fontWeight: 600 }}>Select a category before adding to the store — items no longer default to Fan Gear.</div>}
        {tOn && <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 8 }}>Category is locked to Team Gear while Team Required or Team Supplied is on. Subcategory stays selectable.</div>}
      </div>
      <TeamAssignPrompt
        open={!!teamPrompt}
        initial={config.rosters || []}
        store={store}
        settingLabel="Team Required / Team Supplied"
        onClose={() => { setTeamPrompt(null); if (config.teamError) onChange({ ...config, teamError: false }); }}
        onConfirm={(ids) => { const a = teamPrompt && teamPrompt.apply; setTeamPrompt(null); if (a) a({ rosters: ids }); }} />

      {/* Assign to Teams — existing league stores only (new stores have no Teams yet) */}
      {allowRosterAssign && store?.storeType === "league" && (
        <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px" }}>
          <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 8, color: "#0e0e10" }}>Assign to Teams</div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>
            {tOn
              ? <span>Team Required and Team Supplied items must stay assigned to at least one Team.</span>
              : <span>Choose which Teams wear this product. Leave empty to make it <strong style={{ color: "#0e0e10", fontWeight: 700 }}>available league-wide</strong>.</span>}
          </div>
          <RosterAssignField variant="dropdown" direction="down" value={config.rosters || []} onChange={(ids) => onChange({ ...config, rosters: ids })} accent="#e1251b" minOne={tOn} store={store} />
        </div>
      )}
      {/* Price settings — shared Governing-rule-3 card ($ / % mode) */}
      <PriceSettingsCard
        pricing={pricing}
        onChange={(next) => onChange({ ...config, pricing: next })}
        fundOn={fundOn}
        covered={config.teamSetting === "team-supplied"}
        noYouth={noYouth} />
    </div>
  );
}

function ConfirmSizesModal({ open, product, store, rows, onChange, onClose, onConfirm, productConfig, onConfigChange, items, activeIndex, onActiveIndexChange, confirmLabel, allowRosterAssign }) {
  const sizeOptions = ["AS SHOWN", "XS", "S", "M", "L", "XL", "XXL"];
  const updateRow = (id, patch) => onChange(rows.map((r) => r.id === id ? { ...r, ...patch } : r));
  const multi = items && items.length > 1;
  const rich = !!productConfig;
  // Stock items (Pro Stock / Sideline Stock) have no decoration applications — skip sizing.
  const showSizes = !product || pteRestricted(product);

  return (
    <Modal open={open} onClose={onClose} width={1080}>
      <div style={{ padding: "24px 28px 24px" }}>
        {/* Header */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 8 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
            <div style={{ width: 40, height: 48, background: "#f1f2f4", borderRadius: 4, display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 2 }}>
              {product.photo
                ? <CatalogPhoto photo={product.photo} alt={product.title} />
                : <div style={{ width: 28, height: 36 }}><DefaultProductThumb store={store} product={product} /></div>}
            </div>
            <div style={{ fontSize: 16, fontWeight: 700 }}>{product.title || "[Product name]"}</div>
          </div>
          <button onClick={onClose} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
            <IconX size={20} />
          </button>
        </div>

        <h3 style={{ fontSize: 18, fontWeight: 700, margin: "16px 0 16px" }}>Configure &amp; Add to Store</h3>

        {multi && (
          <div style={{
            display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12,
            background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 8, padding: "12px 16px", marginBottom: 16,
          }}>
            <div style={{ display: "flex", alignItems: "center", gap: 12, minWidth: 0 }}>
              <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".10em", color: "#9a9aa1", flexShrink: 0 }}>ITEM {activeIndex + 1} OF {items.length}</span>
              <span style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{items[activeIndex]?.title}</span>
            </div>
            <div style={{ display: "flex", alignItems: "center", gap: 8, flexShrink: 0 }}>
              <button onClick={() => onActiveIndexChange(Math.max(0, activeIndex - 1))} disabled={activeIndex === 0} style={{
                width: 32, height: 32, borderRadius: 6, border: "1px solid #d4d4d8", background: "#fff",
                cursor: activeIndex === 0 ? "default" : "pointer", color: activeIndex === 0 ? "#cfcfd5" : "#0e0e10",
                display: "inline-flex", alignItems: "center", justifyContent: "center",
              }}><IconChevronLeft size={16} /></button>
              <button onClick={() => onActiveIndexChange(Math.min(items.length - 1, activeIndex + 1))} disabled={activeIndex === items.length - 1} style={{
                width: 32, height: 32, borderRadius: 6, border: "1px solid #d4d4d8", background: "#fff",
                cursor: activeIndex === items.length - 1 ? "default" : "pointer", color: activeIndex === items.length - 1 ? "#cfcfd5" : "#0e0e10",
                display: "inline-flex", alignItems: "center", justifyContent: "center",
              }}><IconChevronRight size={16} /></button>
            </div>
          </div>
        )}

        <div style={{ maxHeight: rich ? "64vh" : "none", overflowY: rich ? "auto" : "visible", margin: rich ? "0 -2px" : 0, paddingRight: rich ? 4 : 0 }}>
        {rich && <ModalConfigSections config={productConfig} onChange={onConfigChange} store={store} allowRosterAssign={allowRosterAssign} product={product} />}
        {showSizes && <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px", marginTop: rich ? 16 : 0 }}>
        <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", marginBottom: 4 }}>Confirm Application Sizes</div>
        {/* Column headers */}
        <div style={{
          display: "grid",
          gridTemplateColumns: "160px 1fr 120px 1.2fr 1fr",
          gap: 16, fontSize: 11, fontWeight: 700, letterSpacing: ".10em",
          color: "#9a9aa1", padding: "12px 0", borderBottom: "1px solid #ececf0"
        }}>
          <div>TYPE</div>
          <div>DETAILS</div>
          <div>COLORS</div>
          <div>SELECT SIZES</div>
          <div>NOTES</div>
        </div>

        {/* Rows */}
        <div style={{ maxHeight: rich ? "none" : "55vh", overflowY: rich ? "visible" : "auto" }}>
          {rows.map((row) =>
          <div key={row.id} style={{
            display: "grid",
            gridTemplateColumns: "160px 1fr 120px 1.2fr 1fr",
            gap: 16, padding: "20px 0", borderBottom: "1px solid #ececf0", alignItems: "flex-start"
          }}>
              {/* Type */}
              <div>
                <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10", letterSpacing: ".06em" }}>{row.type}</div>
                <div style={{ fontSize: 11, color: "#6b6b72", marginTop: 4 }}>( {row.placement} )</div>
              </div>

              {/* Details */}
              <div style={{ display: "flex", gap: 12, alignItems: "flex-start" }}>
                <div style={{
                width: 56, height: 56, border: "1px solid #ececf0", borderRadius: 4,
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                fontSize: 26, fontWeight: 700, color: "#0e0e10", flexShrink: 0,
                fontFamily: "Inter, sans-serif"
              }}>
                  {row.iconKind === "hash" ? "#" : "T"}
                </div>
                <div style={{ fontSize: 11, color: "#0e0e10", lineHeight: 1.6, fontWeight: 500, letterSpacing: ".04em" }}>
                  {Object.entries(row.details).map(([k, v]) =>
                <div key={k}>
                      <span style={{ color: "#9a9aa1", fontWeight: 600 }}>{k}</span>
                      <span style={{ color: "#9a9aa1" }}> : </span>
                      <span>{v}</span>
                    </div>
                )}
                </div>
              </div>

              {/* Colors */}
              <div style={{ display: "flex", gap: 8 }}>
                {row.colors.map((c, i) =>
              <div key={i} style={{
                width: 40, height: 40, borderRadius: 4, background: c,
                border: c?.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none",
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                color: "#fff", fontSize: 11, fontWeight: 700
              }}>B</div>
              )}
              </div>

              {/* Select sizes */}
              <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                <SizeRow label="ADULT" value={row.adultSize} options={sizeOptions} onChange={(v) => updateRow(row.id, { adultSize: v })} />
                <SizeRow label="YOUTH" value={row.youthSize} options={sizeOptions} onChange={(v) => updateRow(row.id, { youthSize: v })} />
              </div>

              {/* Notes */}
              <div>
                <textarea
                placeholder="Notes Here"
                value={row.notes}
                onChange={(e) => updateRow(row.id, { notes: e.target.value })}
                style={{
                  width: "100%", minHeight: 80, resize: "vertical",
                  border: "1px solid #d4d4d8", borderRadius: 4,
                  padding: "8px 12px", fontSize: 12, fontFamily: "inherit",
                  color: "#0e0e10", outline: 0
                }} />
              
              </div>
            </div>
          )}
        </div>
        </div>}
        </div>

        {/* Footer actions */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: multi ? "space-between" : "flex-end", gap: 12, marginTop: 20 }}>
          {multi && (
            <span style={{ fontSize: 12.5, fontWeight: 700, color: "#6b6b72" }}>
              Product {activeIndex + 1} of {items.length}
            </span>
          )}
          <div style={{ display: "flex", gap: 12 }}>
            {multi && activeIndex > 0 && (
              <button onClick={() => onActiveIndexChange(activeIndex - 1)} style={{
                background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
                padding: "12px 24px", fontSize: 13, fontWeight: 600, color: "#0e0e10", cursor: "pointer"
              }}>Back</button>
            )}
            <button onClick={onClose} style={{
              background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 24px", fontSize: 13, fontWeight: 600, color: "#0e0e10", cursor: "pointer"
            }}>Cancel</button>
            {multi && activeIndex < items.length - 1 ? (
              <button onClick={() => onActiveIndexChange(activeIndex + 1)} style={{
                display: "inline-flex", alignItems: "center", gap: 8,
                background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
                padding: "12px 28px", fontSize: 13, fontWeight: 600, cursor: "pointer"
              }}>Confirm &amp; Next Product <IconChevronRight size={16} /></button>
            ) : (
              <button data-tour="sizes-modal-confirm" onClick={() => {
                // 2.4 / 3.1 — category (or TR/TS) required before the item commits.
                if (rich && productConfig) {
                  const tOn2 = productConfig.teamSetting === "team-required" || productConfig.teamSetting === "team-supplied";
                  if (!tOn2 && !productConfig.storeCategory) { onConfigChange && onConfigChange({ ...productConfig, catError: true }); return; }
                  // League stores: TR/TS items must be assigned to ≥1 team or division.
                  if (tOn2 && allowRosterAssign && store?.storeType === "league" && !(productConfig.rosters || []).length) { onConfigChange && onConfigChange({ ...productConfig, teamError: true }); return; }
                }
                onConfirm();
              }} style={{
                background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
                padding: "12px 28px", fontSize: 13, fontWeight: 600, cursor: "pointer"
              }}>{confirmLabel || "Confirm"}</button>
            )}
          </div>
        </div>
      </div>
    </Modal>);

}

function SizeRow({ label, value, options, onChange }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef();
  React.useEffect(() => {
    const h = (e) => {if (ref.current && !ref.current.contains(e.target)) setOpen(false);};
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
      <span style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", minWidth: 52 }}>{label} :</span>
      <div ref={ref} style={{ position: "relative" }}>
        <button onClick={() => setOpen((v) => !v)} style={{
          background: "transparent", border: 0, cursor: "pointer",
          display: "inline-flex", alignItems: "center", gap: 4,
          fontSize: 12, fontWeight: 700, color: "#0e0e10", padding: "4px 0"
        }}>
          {value}
          <IconChevron size={12} />
        </button>
        {open &&
        <div style={{
          position: "absolute", top: "100%", left: 0, zIndex: 30, marginTop: 4,
          background: "#fff", border: "1px solid #ececf0", borderRadius: 6,
          boxShadow: "0 8px 24px rgba(20,20,30,.10)", padding: 4, minWidth: 112
        }}>
            {options.map((o) =>
          <div key={o} onClick={() => {onChange(o);setOpen(false);}} style={{
            padding: "8px 12px", fontSize: 12, cursor: "pointer", borderRadius: 4,
            background: value === o ? "#fafafb" : "transparent"
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
          onMouseLeave={(e) => e.currentTarget.style.background = value === o ? "#fafafb" : "transparent"}>{o}</div>
          )}
          </div>
        }
      </div>
    </div>);

}

function defaultPricing() {
  return {
    youth: { base: "43.13", dealerUnit: "$", dealer: "0.00", fundUnit: "$", fund: "0.00", retail: "43.13" },
    adult: { base: "50.41", dealerUnit: "$", dealer: "0.00", fundUnit: "$", fund: "0.00", retail: "50.41" }
  };
}

// Seed the config modal from a pending item's real prices (Vault Pro Stock /
// Sideline Stock carry priceAdult/priceYouth); custom items keep the defaults.
function pendingPricing(it) {
  if (!it || it.priceAdult == null) return defaultPricing();
  const tier = (b) => ({ base: b.toFixed(2), dealerUnit: "$", dealer: "0.00", fundUnit: "$", fund: "0.00", retail: b.toFixed(2) });
  const a = parseFloat(it.priceAdult) || 0;
  const y = it.priceYouth != null ? (parseFloat(it.priceYouth) || 0) : a;
  return { youth: tier(y), adult: tier(a) };
}

// ─────────── Per-item fundraising helpers ───────────
// Effective state: the item's own toggle when set, else the store's Team Fundraising setting.
function itemFundOn(p, store) {
  const own = p.isBundle ? (p.bundleProfit || {}).fundraisingOn : (p.pricing || {}).fundraisingOn;
  return own != null ? own : store?.fundraisingEnabled === true;
}
function itemFundAmount(p) {
  if (p.isBundle) { const n = parseFloat((p.bundleProfit || {}).fund); return isNaN(n) ? 0 : n; }
  const pr = p.pricing || {};
  return Math.max(...["youth", "adult"].map((s) => { const n = parseFloat(pr[s]?.fund); return isNaN(n) ? 0 : n; }), 0);
}
function itemFundLabel(p) {
  const unit = p.isBundle ? (p.bundleProfit || {}).fundUnit : ((p.pricing || {}).adult || {}).fundUnit;
  const amt = itemFundAmount(p);
  return unit === "%" ? amt + "%" : "$" + amt.toFixed(2);
}
function fundCollectingItems(store) {
  return ((store && store.products) || []).filter((p) => itemFundOn(p, store) && itemFundAmount(p) > 0);
}
Object.assign(window, { itemFundOn, itemFundAmount, itemFundLabel, fundCollectingItems });

function ProfitInput({ unit, value, onUnit, onValue }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
      <div style={{ display: "flex", alignItems: "center", border: "1px solid #d4d4d8", borderRadius: 4, overflow: "hidden", background: "#fff" }}>
        <span style={{ padding: "8px 12px", fontSize: 13, fontWeight: 700, color: "#6b6b72", background: "#f5f5f7", borderRight: "1px solid #d4d4d8" }}>{unit === "%" ? "%" : "$"}</span>
        <input
          type="text"
          value={value}
          onChange={(e) => onValue(e.target.value)}
          style={{
            width: 72, border: 0, borderRadius: 0,
            padding: "8px 12px", fontSize: 13, color: "#0e0e10", outline: 0
          }} />
      </div>
    </div>);

}

// ─────────── Governing rule 3 — shared price settings ($ / % mode) ───────────
// One mode governs a whole section; % resolves against each row's own base
// cost, rounded to the cent before summing; switching modes clears the values.
function psAmt(unit, val, base) {
  const n = parseFloat(val) || 0;
  return unit === "%" ? Math.round(base * n) / 100 : n;
}
function psRowRetail(row, fundOn) {
  const base = parseFloat(row.base) || 0;
  const total = base + psAmt(row.dealerUnit, row.dealer, base) + (fundOn ? psAmt(row.fundUnit || "$", row.fund, base) : 0);
  return total.toFixed(2);
}
function pricingMode(pricing) {
  const rows = [pricing && pricing.youth, pricing && pricing.adult].filter(Boolean);
  return rows.some((r) => r.dealerUnit === "%" || r.fundUnit === "%") ? "%" : "$";
}
function switchPricingMode(pricing, mode, fundOn) {
  const conv = (r) => {
    if (!r) return r;
    const next = { ...r, dealerUnit: mode, fundUnit: mode, dealer: "", fund: "" };
    delete next.dealerBulk; delete next.fundBulk;
    next.retail = psRowRetail(next, fundOn);
    return next;
  };
  return { ...pricing, youth: conv(pricing.youth), adult: conv(pricing.adult) };
}
function PSModeControl({ mode, onChange, locked }) {
  return (
    <span title="One mode for the whole section — switching clears the entered values" style={{ display: "inline-flex", border: "1px solid #d4d4d8", borderRadius: 6, overflow: "hidden", flexShrink: 0, background: "#fff", ...(locked ? { pointerEvents: "none", opacity: 0.55 } : {}) }}>
      {["$", "%"].map((m2, i) => (
        <button key={m2} type="button" onClick={() => onChange(m2)} style={{
          width: 36, height: 28, border: 0, cursor: "pointer", fontSize: 12.5, fontWeight: 700, fontFamily: "inherit",
          borderLeft: i > 0 ? "1px solid #d4d4d8" : 0,
          background: mode === m2 ? "#0e0e10" : "#fff", color: mode === m2 ? "#fff" : "#6b6b72",
          transition: "background .12s, color .12s"
        }}>{m2}</button>
      ))}
    </span>
  );
}
// The shared PRICE SETTINGS card: header-mounted $ / % control, per-row column
// headers, read-only base cost, prefix affordances, derived retail, and the
// "Retail price visible to customers" footer. bare = body only (no card chrome).
function PriceSettingsCard({ pricing, onChange, fundOn, locked, noYouth, bare, covered }) {
  const sizes = noYouth ? ["adult"] : ["youth", "adult"];
  const mode = pricingMode(pricing);
  // Per-item fundraising — the item's own toggle, defaulting to the store's Team Fundraising setting.
  const fundActive = pricing.fundraisingOn ?? fundOn;
  const cols = fundActive ? "64px 84px 1fr 1fr 84px" : "72px 96px 1fr 96px";
  const update = (size, field, value) => {
    const row = { ...pricing[size], [field]: value };
    if (field === "dealer") delete row.dealerBulk; // typing overrides bulk-pricing provenance
    if (field === "fund") delete row.fundBulk;
    const next = { ...pricing, [size]: row };
    next[size].retail = psRowRetail(next[size], fundActive);
    onChange(next);
  };
  const setMode = (m2) => { if (m2 !== mode) onChange(switchPricingMode(pricing, m2, fundActive)); };
  const setFund = (on) => {
    const conv = (r) => r ? { ...r, retail: psRowRetail(r, on) } : r;
    onChange({ ...pricing, fundraisingOn: on, youth: conv(pricing.youth), adult: conv(pricing.adult) });
  };
  // Entered amounts survive the toggle — hint at what's saved while off.
  const savedFund = sizes
    .map((s) => ({ s, amt: psAmt(pricing[s].fundUnit || "$", pricing[s].fund, parseFloat(pricing[s].base) || 0) }))
    .filter((x) => x.amt > 0);
  const fundToggleRow = (
    <div style={{ display: "flex", alignItems: "center", gap: 14, marginTop: 16, paddingTop: 14, borderTop: "1px solid #ececf0", ...(locked ? { opacity: 0.55, pointerEvents: "none" } : {}) }}>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Fundraising on this item</div>
        <div style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.45, marginTop: 2 }}>
          {fundActive
            ? "Added on top of retail for this item. Defaults to the store's Team Fundraising setting."
            : savedFund.length > 0
              ? "Off — saved amount" + (savedFund.length > 1 ? "s" : "") + " (" + savedFund.map((x) => x.s.toUpperCase() + " " + (mode === "%" ? (parseFloat(pricing[x.s].fund) || 0) + "%" : "$" + x.amt.toFixed(2))).join(" · ") + ") kept and restored if re-enabled."
              : "Off — this item doesn't collect fundraising."}
        </div>
      </div>
      <button type="button" onClick={() => setFund(!fundActive)} style={{ background: "none", border: 0, padding: 0, cursor: "pointer", flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, fontFamily: "inherit" }}>
        <span style={{ width: 34, height: 19, borderRadius: 999, position: "relative", background: fundActive ? "#1f8a55" : "#d4d4d8", transition: "background .14s", display: "inline-block" }}>
          <span style={{ position: "absolute", top: 2, left: fundActive ? 17 : 2, width: 15, height: 15, borderRadius: 999, background: "#fff", transition: "left .14s" }} />
        </span>
        <span style={{ fontSize: 11.5, fontWeight: 700, width: 24, textAlign: "left", color: fundActive ? "#1f8a55" : "#71717a" }}>{fundActive ? "On" : "Off"}</span>
      </button>
    </div>
  );
  // Team Supplied — the whole component is replaced by Covered (Governing rule 3).
  if (covered) {
    const note = (
      <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
        <span style={{ fontSize: 13, fontWeight: 800, color: "#1f7a44", background: "#e7f5ed", borderRadius: 6, padding: "6px 12px", flexShrink: 0 }}>Covered</span>
        <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>Team Supplied — the team has already paid for this gear, so shoppers pay $0 at checkout. No pricing applies.</span>
      </div>
    );
    if (bare) return note;
    return (
      <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px" }}>
        <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".10em", color: "#0e0e10", marginBottom: 14 }}>PRICE SETTINGS</div>
        {note}
      </div>
    );
  }
  const body = (
    <React.Fragment>
      <div style={locked ? { pointerEvents: "none" } : undefined}>
        {sizes.map((size, i) => (
          <div key={size} style={{ paddingTop: i > 0 ? 16 : 0, borderTop: i > 0 ? "1px solid #ececf0" : 0, marginTop: i > 0 ? 16 : 0 }}>
            <div style={{ display: "grid", gridTemplateColumns: cols, gap: 16, fontSize: 11, fontWeight: 700, letterSpacing: ".08em", color: "#9a9aa1", marginBottom: 10 }}>
              <div>SIZE</div><div>BASE COST</div><div>DEALER PROFIT</div>{fundActive && <div>FUNDRAISING</div>}<div>RETAIL</div>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: cols, gap: 16, alignItems: "center" }}>
              <div style={{ fontSize: 14, fontWeight: 800, color: "#0e0e10" }}>{size.toUpperCase()}</div>
              <div style={{ fontSize: 13, color: "#6b6b72" }}>{"$" + pricing[size].base}</div>
              <div>
                <ProfitInput unit={mode} value={pricing[size].dealer} onValue={(v) => update(size, "dealer", v)} />
                {pricing[size].dealerBulk && <div style={{ fontSize: 10.5, color: "#9a9aa1", marginTop: 4, display: "flex", alignItems: "center", gap: 4 }}><span style={{ width: 5, height: 5, borderRadius: "50%", background: "#c4c4cb", flexShrink: 0 }} />Set by bulk pricing</div>}
              </div>
              {fundActive && <div>
                <ProfitInput unit={mode} value={pricing[size].fund ?? ""} onValue={(v) => update(size, "fund", v)} />
                {pricing[size].fundBulk && <div style={{ fontSize: 10.5, color: "#9a9aa1", marginTop: 4, display: "flex", alignItems: "center", gap: 4 }}><span style={{ width: 5, height: 5, borderRadius: "50%", background: "#c4c4cb", flexShrink: 0 }} />Set by bulk pricing</div>}
              </div>}
              <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10" }}>{"$" + pricing[size].retail}</div>
            </div>
          </div>
        ))}
      </div>
      {fundToggleRow}
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 16, marginTop: 16, paddingTop: 14, borderTop: "1px solid #ececf0" }}>
        <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>Retail price visible to customers</span>
        <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>
          {sizes.map((size, i) => (
            <React.Fragment key={size}>
              <span style={{ fontWeight: 700, letterSpacing: ".06em", marginLeft: i > 0 ? 16 : 0 }}>{size.toUpperCase() + " "}</span>
              <strong style={{ color: "#0e0e10" }}>{"$" + pricing[size].retail}</strong>
            </React.Fragment>
          ))}
        </span>
      </div>
      {!locked && (
        <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 8 }}>
          {mode === "%" ? "Percentages apply to each size's own base cost. " : ""}Switching $ / % clears the entered values.
        </div>
      )}
    </React.Fragment>
  );
  if (bare) return <div style={locked ? { opacity: 0.55 } : undefined}>{body}</div>;
  return (
    <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "20px 24px", ...(locked ? { opacity: 0.55 } : {}) }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, marginBottom: 16 }}>
        <span style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".10em", color: "#0e0e10" }}>PRICE SETTINGS</span>
        <PSModeControl mode={mode} onChange={setMode} locked={locked} />
      </div>
      {body}
    </div>
  );
}

function Radio({ checked, onChange }) {
  return (
    <span onClick={onChange} style={{
      width: 16, height: 16, borderRadius: "50%",
      border: `1.5px solid ${checked ? "#e1251b" : "#9a9aa1"}`,
      background: "#fff",
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      cursor: "pointer", flexShrink: 0
    }}>
      {checked && <span style={{ width: 12, height: 12, borderRadius: "50%", background: "#e1251b" }} />}
    </span>);

}

// Hover-triggered info tooltip for product settings.
function InfoTooltip({ title, body }) {
  const [open, setOpen] = React.useState(false);
  return (
    <span
      onMouseEnter={() => setOpen(true)}
      onMouseLeave={() => setOpen(false)}
      onFocus={() => setOpen(true)}
      onBlur={() => setOpen(false)}
      tabIndex={0}
      style={{ position: "relative", display: "inline-flex", outline: "none" }}>
      <span style={{
        width: 16, height: 16, borderRadius: "50%",
        border: "1.5px solid #9a9aa1",
        color: open ? "#0e0e10" : "#9a9aa1",
        borderColor: open ? "#0e0e10" : "#9a9aa1",
        background: "#fff",
        display: "inline-flex", alignItems: "center", justifyContent: "center",
        fontSize: 10, fontWeight: 700, cursor: "help",
        fontFamily: "Georgia, serif", fontStyle: "italic",
        transition: "color .12s, border-color .12s"
      }}>i</span>
      {open &&
      <span
        role="tooltip"
        style={{
          position: "absolute",
          left: "50%",
          bottom: "calc(100% + 12px)",
          transform: "translateX(-50%)",
          background: "#1A1A1A",
          color: "#fff",
          padding: "12px 16px",
          borderRadius: 8,
          width: 260,
          boxShadow: "0 8px 24px rgba(20,20,30,.18)",
          zIndex: 50,
          pointerEvents: "none",
          animation: "tipIn .12s ease-out"
        }}>
          <span style={{ display: "block", fontSize: 13, fontWeight: 700, marginBottom: 4, letterSpacing: ".01em" }}>{title}</span>
          <span style={{ display: "block", fontSize: 12, lineHeight: 1.5, color: "#d6d6d8" }}>{body}</span>
          <span style={{
          position: "absolute", left: "50%", top: "100%", transform: "translateX(-50%)",
          width: 0, height: 0,
          borderLeft: "6px solid transparent",
          borderRight: "6px solid transparent",
          borderTop: "6px solid #1A1A1A"
        }} />
        </span>
      }
      <style>{`@keyframes tipIn { from { opacity: 0; transform: translate(-50%, 4px) } to { opacity: 1; transform: translate(-50%, 0) } }`}</style>
    </span>);

}

// ─────────── Products list — DRAFTS / PUBLISHED ───────────
function ProductsList({ products, store, onPublish, onPublishAll, onRemove, onUnpublish, onSendForApproval, onAddProducts, onCreateBundle, onUpdateProduct, onBulkPrice }) {
  const [tab, setTab] = React.useState("drafts");
  const [checked, setChecked] = React.useState({});
  const [editingId, setEditingId] = React.useState(null);
  const [bulkOpen, setBulkOpen] = React.useState(false);
  const [sort, setSort] = React.useState("Alphabetical");
  const [filter, setFilter] = React.useState("All");

  // Derive a product category from `kind` when present, else infer from the title
  // (created products carry only title + teamSetting, no `kind`).
  const categoryOf = (p) => {
    if (p.kind === "jersey") return "Jerseys";
    if (p.kind === "shorts" || p.kind === "pants") return "Pants & Shorts";
    if (p.kind) return "Accessories";
    const t = (p.title || "").toLowerCase();
    if (/(pant|short|legging|tight)/.test(t)) return "Pants & Shorts";
    if (/(jersey|jacket|hoodie|tee|shirt|crew|top|uniform|pullover|fleece)/.test(t)) return "Jerseys";
    if (/(bag|backpack|sock|hat|cap|beanie|glove|headband|accessor)/.test(t)) return "Accessories";
    return "Jerseys";
  };

  // Only offer Category sub-options that actually exist in this product list, so
  // picking a category never empties the list unexpectedly.
  const CATEGORY_ORDER = ["Jerseys", "Pants & Shorts", "Accessories"];
  const presentCategories = CATEGORY_ORDER.filter((c) => products.some((p) => categoryOf(p) === c));

  // Filter options — Sport/Category expand inline into sub-choices.
  const filterOptions = [
    "All", "Team Required", "Team Supplied",
    { label: "Sport", sub: [store?.sport || "Team Sport"] },
    ...(presentCategories.length ? [{ label: "Category", sub: presentCategories }] : []),
  ];
  const sortOptions = ["Price", "Alphabetical", "Published", "Not Published"];

  const priceOf = (p) => {
    const pr = p?.pricing || {};
    for (const k of Object.keys(pr)) {
      const v = parseFloat(String(pr[k]?.retail).replace(/[^0-9.]/g, ""));
      if (!isNaN(v)) return v;
    }
    return 0;
  };
  const matchesFilter = (p) => {
    switch (filter) {
      case "All": return true;
      case "Team Required": return p.teamSetting === "team-required";
      case "Team Supplied": return p.teamSetting === "team-supplied";
      case "Jerseys":
      case "Pants & Shorts":
      case "Accessories": return categoryOf(p) === filter;
      default: return true; // Sport leaf — products inherit the store's sport
    }
  };
  const applySort = (arr) => {
    const a = [...arr];
    if (sort === "Price") a.sort((x, y) => priceOf(x) - priceOf(y));
    else if (sort === "Alphabetical") a.sort((x, y) => (x.title || "").localeCompare(y.title || ""));
    else if (sort === "Published") a.sort((x, y) => Number(y.status === "published") - Number(x.status === "published"));
    else if (sort === "Not Published") a.sort((x, y) => Number(x.status === "published") - Number(y.status === "published"));
    return a;
  };

  const filteredProducts = products.filter(matchesFilter);
  const drafts = filteredProducts.filter((p) => p.status === "draft");
  const published = filteredProducts.filter((p) => p.status === "published");
  const visible = applySort(tab === "drafts" ? drafts : published);
  const editingProduct = products.find((p) => p.id === editingId) || null;

  // Only count selected DRAFTS for the batch publish button (shown when ≥2 selected).
  const selectedDraftIds = drafts.filter((p) => checked[p.id]).map((p) => p.id);
  const canPublishSelected = tab === "drafts" && selectedDraftIds.length >= 1;
  const publishSelected = () => {
    onPublish(selectedDraftIds);
    setChecked({});
  };

  // Select-all for the drafts tab.
  const allDraftsChecked = drafts.length > 0 && drafts.every((p) => checked[p.id]);
  const someDraftsChecked = drafts.some((p) => checked[p.id]) && !allDraftsChecked;
  const toggleSelectAll = () => {
    setChecked((c) => {
      const next = { ...c };
      if (allDraftsChecked) {
        drafts.forEach((p) => {delete next[p.id];});
      } else {
        drafts.forEach((p) => {next[p.id] = true;});
      }
      return next;
    });
  };

  return (
    <div style={{ paddingBottom: 100 }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 24, flexWrap: "wrap" }}>
        <SubNavChips
          tabs={[
          { id: "drafts", label: "Drafts", Icon: DraftDocIcon, count: drafts.length },
          { id: "published", label: "Published", Icon: GlobeIcon, count: published.length }]}
          value={tab} onChange={setTab} />
        <ListToolbar
          showSearch={false}
          filter={{ value: filter, options: filterOptions, onChange: setFilter, width: 232 }}
          sort={{ value: sort, options: sortOptions, onChange: setSort, width: 220 }} />
      </div>

      {tab === "drafts" && drafts.length > 0 &&
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 16 }}>
        <label style={{
          display: "inline-flex", alignItems: "center", gap: 12,
          cursor: "pointer", userSelect: "none"
        }}>
          <input
          type="checkbox"
          checked={allDraftsChecked}
          ref={(el) => {if (el) el.indeterminate = someDraftsChecked;}}
          onChange={toggleSelectAll}
          style={{ width: 16, height: 16, accentColor: "#0e0e10", cursor: "pointer", margin: 0 }} />
          <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>
            {allDraftsChecked ? "Deselect all" : "Select all"}
            <span style={{ color: "#9a9aa1", fontWeight: 500 }}> ({selectedDraftIds.length}/{drafts.length})</span>
          </span>
        </label>
        {canPublishSelected ?
        <button
          onClick={publishSelected}
          style={{
            background: "#0e0e10", color: "#fff",
            border: 0, borderRadius: 6, padding: "12px 24px",
            fontSize: 13, fontWeight: 700, cursor: "pointer",
            transition: "background .15s, color .15s"
          }}>
            Publish Selected ({selectedDraftIds.length})
          </button> :
        <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12.5, fontWeight: 500, color: "#9a9aa1" }}>
          <IconCheck size={14} style={{ strokeWidth: 2.4 }} />
          Select products to publish
        </span>}
      </div>
      }

      {visible.length === 0 ?
      <div style={{ padding: "120px 20px", textAlign: "center", color: "#6b6b72", fontSize: 14 }}>
          {tab === "drafts" ? "No drafts yet — added products will appear here." : "No published products yet."}
        </div> :

      <div>
          {/* Column header row — aligned to the product rows below */}
          <div style={{
            display: "grid", gridTemplateColumns: PRODUCTS_GRID, alignItems: "end",
            gap: 32, padding: "0 24px 16px",
            fontSize: 11, fontWeight: 700, letterSpacing: ".08em", textTransform: "uppercase", color: "#9a9aa1"
          }}>
            <span />
            <span>Product</span>
            <span>design id</span>
            <span>Product Setting</span>
            <span>teams</span>
            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", columnGap: 20, borderLeft: "1px solid #ececf0", paddingLeft: 24 }}>
              <span style={{ textAlign: "right" }}>youth</span>
              <span style={{ textAlign: "right" }}>adult</span>
            </div>
            <span />
          </div>
          <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
          {visible.map((p) =>
        <ProductCard
          key={p.id}
          product={p}
          checked={!!checked[p.id]}
          onCheck={(v) => setChecked((c) => ({ ...c, [p.id]: v }))}
          onEdit={() => setEditingId(p.id)}
          onUnpublish={() => onUnpublish && onUnpublish(p.id)}
          onSendForApproval={() => onSendForApproval && onSendForApproval(p.id)}
          onRemove={() => onRemove(p.id)}
          onPublish={tab === "drafts" ? () => onPublish([p.id]) : undefined}
          store={store} />

        )}
          </div>
        </div>
      }

      <FloatingActionBar
        leftChildren={
        tab === "drafts" && drafts.length > 0 &&
        <Button variant="secondary" onClick={() => setBulkOpen(true)} style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
            <span style={{
            width: 20, height: 20, borderRadius: "50%", background: "#fafafb",
            border: "1px solid #ececf0", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#0e0e10"
          }}>$</span>
            BULK PRICING
          </Button>
        }>
        {tab === "drafts" &&
        <Button variant="secondary" onClick={onCreateBundle}>
          <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Create Bundle
        </Button>
        }
        <Button variant="primary" onClick={onAddProducts}>
          <IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Add Products
        </Button>
      </FloatingActionBar>

      <EditProductDrawer
        open={!!editingProduct}
        product={editingProduct}
        store={store}
        onClose={() => setEditingId(null)}
        onSave={(updated) => {onUpdateProduct && onUpdateProduct(updated);setEditingId(null);}}
        onUnpublish={() => {if (editingProduct) onUnpublish && onUnpublish(editingProduct.id);}}
        onDelete={() => {if (editingProduct) {onRemove(editingProduct.id);setEditingId(null);}}} />

      <BulkPricingModal
        open={bulkOpen}
        store={store}
        draftProducts={drafts}
        onClose={() => setBulkOpen(false)}
        onApply={(ids, profit) => {
          onBulkPrice && onBulkPrice(ids, profit);
          setBulkOpen(false);
        }} />
    </div>);

}

// Shared grid template for the products list header + each product row.
// Rigid "balanced-zone" columns: checkbox · product · design ID · status · teams · price-zone · kebab
const PRODUCTS_GRID = "24px minmax(230px, 1.7fr) minmax(170px, 1.3fr) minmax(150px, 1fr) minmax(160px, 1.3fr) 208px 30px";

// Teams cell — shows the first assigned team as a chip, then a "+N" affordance
// that reveals an elegant tooltip listing the remaining teams on hover.
function TeamsCell({ names, leagueWide }) {
  const [hover, setHover] = React.useState(false);
  if (leagueWide) {
    return (
      <span style={{ display: "inline-flex", alignItems: "center", fontSize: 11.5, fontWeight: 700, color: "#0e0e10", background: "#f1f1f3", borderRadius: 999, padding: "4px 12px", whiteSpace: "nowrap" }}>League-wide</span>);
  }
  if (!names.length) {
    return <span style={{ fontSize: 13, color: "#c4c4ca" }}>—</span>;
  }
  const first = names[0];
  const rest = names.slice(1);
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 8, minWidth: 0 }}>
      <span style={{
        fontSize: 11.5, fontWeight: 700, color: "#0e0e10", background: "#f1f1f3",
        borderRadius: 999, padding: "4px 12px", maxWidth: 148,
        whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis"
      }}>{first}</span>
      {rest.length > 0 &&
      <span
        onMouseEnter={() => setHover(true)}
        onMouseLeave={() => setHover(false)}
        style={{
          position: "relative", flexShrink: 0, cursor: "default",
          fontSize: 11.5, fontWeight: 800, color: "#6b6b72", background: "#ececf0",
          borderRadius: 999, padding: "4px 8px"
        }}>
          +{rest.length}
          <div style={{
            position: "absolute", bottom: "calc(100% + 12px)", left: "50%",
            transform: `translateX(-50%) translateY(${hover ? 0 : 6}px) scale(${hover ? 1 : 0.96})`,
            transformOrigin: "bottom center",
            opacity: hover ? 1 : 0, pointerEvents: "none",
            transition: "opacity .16s cubic-bezier(0.2,0,0,1), transform .18s cubic-bezier(0.2,0,0,1)",
            background: "#18181b", color: "#fff", borderRadius: 10,
            padding: "12px 12px", minWidth: 168, maxWidth: 260, zIndex: 60,
            boxShadow: "0 12px 30px rgba(15,15,25,.28), 0 2px 8px rgba(15,15,25,.18)"
          }}>
            <div style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".08em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 8 }}>Also on {rest.length} more</div>
            <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
              {rest.map((n, i) =>
              <div key={i} style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12.5, fontWeight: 600, color: "#f4f4f5", whiteSpace: "nowrap" }}>
                  <span style={{ width: 4, height: 4, borderRadius: "50%", background: "#6b6b72", flexShrink: 0 }} />
                  {n}
                </div>)}
            </div>
            <span style={{
              position: "absolute", top: "100%", left: "50%", transform: "translateX(-50%)",
              width: 0, height: 0, borderLeft: "6px solid transparent", borderRight: "6px solid transparent",
              borderTop: "6px solid #18181b"
            }} />
          </div>
        </span>
      }
    </div>);

}

function ProductCard({ product, checked, onCheck, onEdit, onUnpublish, onSendForApproval, onRemove, onPublish, store }) {
  const settingColor = {
    "team-required": "#d9483b",
    "team-supplied": "#2944a8",
    "fan-gear": "#1f7a44"
  }[product.teamSetting] || "#1f7a44";
  const settingLabel = {
    "team-required": "Team Required",
    "team-supplied": "Team Supplied",
    "fan-gear": "Fan Gear"
  }[product.teamSetting] || "Fan Gear";

  const isBundle = !!product.isBundle;
  const isTeamSupplied = product.teamSetting === "team-supplied";
  // Resolve bundle constituent items by id, falling back gracefully.
  const allProducts = store?.products || [];
  const bundleItems = isBundle ?
  (product.bundleItemIds || []).
  map((id) => allProducts.find((p) => p.id === id)).
  filter(Boolean) :
  [];

  // Teams / rosters — assigned team names for the teams column.
  const rosterIds = product.rosters || [];
  const allRosters = assignAllRosterIds();
  const isLeague = store?.storeType === "league";
  const leagueWide = isLeague && (rosterIds.length === 0 || rosterIds.length >= allRosters.length);
  const teamNames = rosterIds.map((id) => (assignRosterLabel(id) || {}).name).filter(Boolean);

  const priceText = (tier) => {
    const dp = defaultPricing();
    const v = product.pricing?.[tier]?.retail ?? product[tier] ?? dp[tier]?.retail;
    return v != null && v !== "" ? `$${v}` : "—";
  };

  return (
    <div style={{
      display: "grid", gridTemplateColumns: PRODUCTS_GRID, alignItems: "stretch", gap: 32,
      background: "#fff", border: "1px solid #ececf0", borderRadius: 14, padding: "16px 24px",
      transition: "box-shadow .14s, border-color .14s"
    }}
      onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 16px rgba(20,20,30,.06)"; e.currentTarget.style.borderColor = "#dedee3"; }}
      onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#ececf0"; }}>
      {/* Checkbox */}
      <div style={{ display: "flex", alignItems: "center" }}>
        <span
          onClick={(e) => {e.stopPropagation();onCheck(!checked);}}
          style={{
            width: 20, height: 20, borderRadius: 4,
            border: `1.5px solid ${checked ? "#0e0e10" : "#c4c4ca"}`,
            background: checked ? "#0e0e10" : "#fff",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            cursor: "pointer", flexShrink: 0
          }}>
          {checked && <IconCheck size={13} style={{ color: "#fff", strokeWidth: 3 }} />}
        </span>
      </div>

      {/* ZONE 1 — Product: 56px thumbnail + name + setting pill */}
      <div style={{ display: "flex", alignItems: "center", gap: 16, minWidth: 0 }}>
        <div style={{
          width: 56, height: 56, flexShrink: 0, background: "#f1f2f4", borderRadius: 8,
          display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden",
          padding: 8, position: "relative"
        }}>
          {isBundle ?
          <BundleThumbCollage items={bundleItems} store={store} /> :
          <div style={{ width: 32, height: 44 }}>
              {product.thumbnail || <DefaultProductThumb store={store} product={product} />}
            </div>
          }
        </div>
        <div style={{ minWidth: 0 }}>
          <div style={{ fontSize: 14, fontWeight: 600, color: "#0e0e10", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
            {product.title || (isBundle ? "[bundle-name]" : "[product-name]")}
          </div>
        </div>
      </div>

      {/* ZONE 2 — design id (plain text) */}
      <div style={{ display: "flex", alignItems: "center", minWidth: 0 }}>
        <span style={{
          fontSize: 14, color: "#8a8a90", lineHeight: 1.4, minWidth: 0,
          display: "-webkit-box", WebkitLineClamp: 2, WebkitBoxOrient: "vertical", overflow: "hidden"
        }}>
          {isBundle ? `${bundleItems.length} Items` : (product.subtitle || "—")}
        </span>
      </div>

      {/* ZONE 3 — Product Setting (plain text) */}
      <div style={{ display: "flex", alignItems: "center", minWidth: 0 }}>
        <span style={{ fontSize: 14, color: "#6b6b72", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>
          {settingLabel}
        </span>
      </div>

      {/* ZONE 4 — Teams */}
      <div style={{ display: "flex", alignItems: "center", minWidth: 0 }}>
        <TeamsCell names={teamNames} leagueWide={leagueWide} />
      </div>

      {/* ZONE 5 — Price (independent right-hand zone, set off by a full-height hairline) */}
      <div style={{
        display: "grid", gridTemplateColumns: "1fr 1fr", columnGap: 20, alignItems: "center",
        borderLeft: "1px solid #ececf0", paddingLeft: 24
      }}>
        <span style={{ fontSize: 15, fontWeight: 600, color: isTeamSupplied ? "#b0b0b6" : "#0e0e10", textAlign: "right", fontVariantNumeric: "tabular-nums" }}>
          {isTeamSupplied ? "Covered" : priceText("youth")}
        </span>
        <span style={{ fontSize: 15, fontWeight: 600, color: isTeamSupplied ? "#b0b0b6" : "#0e0e10", textAlign: "right", fontVariantNumeric: "tabular-nums" }}>
          {isTeamSupplied ? "Covered" : priceText("adult")}
        </span>
      </div>

      {/* Kebab */}
      <div style={{ display: "flex", alignItems: "center", justifyContent: "center" }}>
        <KebabMenu
          status={product.status}
          onEdit={onEdit}
          onUnpublish={onUnpublish}
          onSendForApproval={onSendForApproval} />
      </div>
    </div>);

}

// Kebab (3-dot) menu shown on each product card.
// - For drafts: clicking opens the slide-out drawer directly.
// - For published: clicking reveals a small dropdown (Update Product / Send for Approval / Unpublish).
function KebabMenu({ status, onEdit, onUnpublish, onSendForApproval }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef();
  React.useEffect(() => {
    const h = (e) => {if (ref.current && !ref.current.contains(e.target)) setOpen(false);};
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const isPublished = status === "published";

  const handleClick = (e) => {
    e.stopPropagation();
    if (isPublished) {
      setOpen((v) => !v);
    } else {
      onEdit && onEdit();
    }
  };

  const menuItem = (icon, label, handler, danger) =>
  <button
    onClick={() => {setOpen(false);handler && handler();}}
    style={{
      display: "flex", alignItems: "center", gap: 12,
      width: "100%", textAlign: "left",
      background: "transparent", border: 0, cursor: "pointer",
      padding: "12px 16px", fontSize: 14, fontWeight: 500,
      color: danger ? "#e1251b" : "#0e0e10"
    }}
    onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
    onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
      <span style={{ color: danger ? "#e1251b" : "#6b6b72", display: "inline-flex" }}>{icon}</span>
      {label}
    </button>;


  return (
    <div ref={ref} style={{ position: "relative", flexShrink: 0 }}>
      <button
        onClick={handleClick}
        title={isPublished ? "Product options" : "Edit product"}
        style={{
          background: "transparent", border: 0, cursor: "pointer",
          padding: 4, color: "#6b6b72", display: "inline-flex",
          borderRadius: 4
        }}
        onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
        onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
        <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
          <circle cx="5" cy="12" r="1.6" />
          <circle cx="12" cy="12" r="1.6" />
          <circle cx="19" cy="12" r="1.6" />
        </svg>
      </button>
      {isPublished && open &&
      <div style={{
        position: "absolute", top: "calc(100% + 8px)", right: 0,
        minWidth: 220,
        background: "#fff", border: "1px solid #ececf0", borderRadius: 8,
        boxShadow: "0 12px 28px rgba(20,20,30,.12)",
        padding: "8px 0", zIndex: 40,
        animation: "kebabIn .12s ease-out"
      }}>
          {menuItem(
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M12 20h9" /><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4 12.5-12.5z" /></svg>,
          "Update Product",
          onEdit
        )}
          {menuItem(
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M14 3h7v7" /><path d="M10 14L21 3" /><path d="M21 14v5a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5" /></svg>,
          "Send for Approval",
          onSendForApproval
        )}
          <div style={{ height: 1, background: "#ececf0", margin: "4px 0" }} />
          {menuItem(
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><polyline points="3 6 5 6 21 6" /><path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" /><path d="M10 11v6" /><path d="M14 11v6" /><path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" /></svg>,
          "Unpublish",
          onUnpublish,
          true
        )}
          <style>{`@keyframes kebabIn{from{opacity:0;transform:translateY(-4px)}to{opacity:1;transform:translateY(0)}}`}</style>
        </div>
      }
    </div>);

}

// Right-side slide-out drawer for editing a product's team setting + pricing.
function EditProductDrawer({ open, product, store, onClose, onSave, onDelete, onUnpublish }) {
  const [teamSetting, setTeamSetting] = React.useState("team-required");
  const [title, setTitle] = React.useState("");
  const [titleEditing, setTitleEditing] = React.useState(false);
  const [optRequired, setOptRequired] = React.useState(true);
  const [optSupplied, setOptSupplied] = React.useState(false);
  const [pricing, setPricing] = React.useState(defaultPricing());
  const [rosters, setRosters] = React.useState([]);
  // 3.3 — store-item category assignment (Vault items are not categorized)
  const [prodCat, setProdCat] = React.useState("");
  const [prodSubcat, setProdSubcat] = React.useState("");

  // Bundle-specific state
  const [bundleRequired, setBundleRequired] = React.useState(false);
  const [bundleSupplied, setBundleSupplied] = React.useState(false);
  const [itemPricings, setItemPricings] = React.useState({});
  const [expandedItemId, setExpandedItemId] = React.useState(null);
  const [bundleProfit, setBundleProfit] = React.useState(defaultBundleProfit());
  const [bundleHidden, setBundleHidden] = React.useState(false);
  const [bundleHideRetail, setBundleHideRetail] = React.useState(false);
  const [bundleFreeShip, setBundleFreeShip] = React.useState(false);
  const updateBundleProfit = (field, value) =>
  setBundleProfit((prev) => ({ ...prev, [field]: value }));

  const isBundle = !!product?.isBundle;
  // Source + order-state gating (July 2026): publish state is gone. Restricted
  // sources (made to order) lock design at add; any non-cancelled order locks
  // everything except placement (Team assignment, Category, Subcategory).
  // Vault / seller-catalog items carry no restrictions at all.
  const lock = product ? pteLockState(product, store) : "design";
  const fullLock = lock === "full";
  const designLock = lock !== "none";
  const [catError, setCatError] = React.useState(false);
  const [teamPrompt, setTeamPrompt] = React.useState(null); // { apply } | null
  const allProducts = store?.products || [];
  const bundleItems = isBundle ?
  (product.bundleItemIds || []).
  map((id) => allProducts.find((p) => p.id === id)).
  filter(Boolean) :
  [];

  // Sync local state when a new product opens
  React.useEffect(() => {
    if (product) {
      const ts = product.teamSetting || "fan-gear";
      setTeamSetting(ts);
      setTitle(product.title || "");
      setTitleEditing(false);
      setOptRequired(product.optRequired ?? ts === "team-required");
      setOptSupplied(product.optSupplied ?? ts === "team-supplied");
      setPricing(product.pricing || defaultPricing());
      setRosters(product.rosters || []);
      const tOn = (product.optRequired ?? ts === "team-required") || (product.optSupplied ?? ts === "team-supplied");
      setProdCat(tOn ? "Team Gear" : (product.storeCategory || ""));
      setProdSubcat(product.storeSubcategory || "");
      setCatError(false);
      if (product.isBundle) {
        // Default toggles based on the bundle's own teamSetting; a bundle can be
        // both Required AND Supplied (see screenshot — independent switches).
        const ts = product.teamSetting || "fan-gear";
        setBundleRequired(product.bundleRequired ?? ts === "team-required");
        setBundleSupplied(product.bundleSupplied ?? ts === "team-supplied");
        // Initialize per-item pricing from each item's saved pricing.
        const init = {};
        (product.bundleItemIds || []).forEach((id) => {
          const it = allProducts.find((p) => p.id === id);
          if (it) init[id] = it.pricing || defaultPricing();
        });
        // Allow per-bundle overrides if previously saved.
        Object.assign(init, product.itemPricings || {});
        setItemPricings(init);
        setBundleProfit(product.bundleProfit || defaultBundleProfit());
        setBundleHidden(!!product.hidden);
        setBundleHideRetail(!!product.hideRetail);
        setBundleFreeShip(!!product.freeShipping);
        const firstId = (product.bundleItemIds || [])[0];
        setExpandedItemId(firstId || null);
      }
    }
  }, [product?.id]);

  const settingBadge = {
    "team-required": { label: "TEAM REQUIRED", color: "#e8590c" },
    "team-supplied": { label: "TEAM SUPPLIED", color: "#2944a8" },
    "fan-gear": { label: "FAN GEAR", color: "#1f7a44" }
  }[teamSetting] || { label: "FAN GEAR", color: "#1f7a44" };

  const updatePricing = (size, field, value) => {
    const next = { ...pricing, [size]: { ...pricing[size], [field]: value } };
    const baseNum = parseFloat(next[size].base) || 0;
    const dealerNum = parseFloat(next[size].dealer) || 0;
    const fundNum = parseFloat(next[size].fund) || 0;
    const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
    const fundAmt = next[size].fundUnit === "$" ? fundNum : baseNum * fundNum / 100;
    next[size].retail = (baseNum + dealerAmt + ((next.fundraisingOn ?? (store?.fundraisingEnabled === true)) ? fundAmt : 0)).toFixed(2);
    setPricing(next);
  };
  const drawerFundOn = store?.fundraisingEnabled === true;
  const teamOn = optRequired || optSupplied;
  const catSubs = (((window.__prodCats || []).find((c) => c.name === prodCat)) || {}).subs || [];
  const drawerPriceGridCols = drawerFundOn ? "60px 80px 1fr 1fr 1fr 80px" : "60px 80px 1fr 1fr 80px";

  const handleSave = () => {
    if (!product) return;
    if (isBundle) {
      // Persist bundle toggles + per-item pricing snapshots.
      // Also recompute the bundle card's price range.
      onSave({
        ...product,
        title: title.trim() || product.title,
        bundleRequired, bundleSupplied,
        teamSetting: bundleRequired ? "team-required" : bundleSupplied ? "team-supplied" : "fan-gear",
        itemPricings,
        bundleProfit,
        hidden: bundleHidden,
        hideRetail: bundleHideRetail,
        freeShipping: bundleFreeShip,
        rosters,
        // Card range comes off the bundle-derived customer price, so a Choice
        // bundle’s "from $X" is the cheapest option a shopper can actually buy.
        pricing: bundleCardPricing(
          bundleItems, itemPricings, bundleProfit, product.bundleType || "all",
          bundleSupplied, drawerFundOn
        )
      });
      return;
    }
    // 2.4 — no invisible "blank = Fan Gear" fallback: category is required when
    // neither team setting is on.
    if (!teamOn && !prodCat) { setCatError(true); return; }
    // 4.2 — a TR/TS item in a League store can never be unassigned.
    if (teamOn && store?.storeType === "league" && rosters.length === 0) { setTeamPrompt({ apply: null }); return; }
    onSave({
      ...product,
      title: title.trim() || product.title,
      teamSetting: optRequired ? "team-required" : optSupplied ? "team-supplied" : "fan-gear",
      optRequired, optSupplied,
      storeCategory: teamOn ? "Team Gear" : prodCat, storeSubcategory: prodSubcat,
      rosters,
      pricing
    });
  };

  // Per-bundle-item pricing updater (mirrors the non-bundle updatePricing logic).
  const updateItemPricing = (itemId, size, field, value) => {
    setItemPricings((prev) => {
      const cur = prev[itemId] || defaultPricing();
      const next = { ...cur, [size]: { ...cur[size], [field]: value } };
      const baseNum = parseFloat(next[size].base) || 0;
      const dealerNum = parseFloat(next[size].dealer) || 0;
      const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
      next[size].retail = (baseNum + dealerAmt).toFixed(2);
      return { ...prev, [itemId]: next };
    });
  };

  return (
    <React.Fragment>
      {/* Backdrop */}
      <div
        onClick={onClose}
        style={{
          position: "fixed", inset: 0, background: "rgba(20,20,30,.32)",
          opacity: open ? 1 : 0, pointerEvents: open ? "auto" : "none",
          transition: "opacity .2s ease", zIndex: 80
        }} />
      {/* Drawer */}
      <aside
        style={{
          position: "fixed", top: 0, right: 0, bottom: 0,
          width: 720, maxWidth: "95vw",
          background: "#fff", boxShadow: "-10px 0 32px rgba(20,20,30,.12)",
          transform: open ? "translateX(0)" : "translateX(100%)",
          transition: "transform .25s ease",
          zIndex: 90, display: "flex", flexDirection: "column"
        }}>
        {product &&
        <React.Fragment>
            {/* Header */}
            <div style={{ padding: "24px 28px 16px", display: "flex", alignItems: "flex-start", gap: 16 }}>
              <div style={{
              width: 44, height: 56, background: "#f1f2f4", borderRadius: 4,
              display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0
            }}>
                <div style={{ width: 32, height: 40 }}>
                  {product.thumbnail || <DefaultProductThumb store={store} product={product} />}
                </div>
              </div>
              <div style={{ flex: 1, minWidth: 0 }}>
                <div style={{ display: "inline-flex", alignItems: "center", gap: 12, marginBottom: 8 }}>
                  {titleEditing && !fullLock ?
                <input
                  value={title}
                  autoFocus
                  onChange={(e) => setTitle(e.target.value)}
                  onBlur={() => setTitleEditing(false)}
                  onKeyDown={(e) => { if (e.key === "Enter") { e.preventDefault(); setTitleEditing(false); } if (e.key === "Escape") { setTitle(product.title || ""); setTitleEditing(false); } }}
                  placeholder={isBundle ? "Bundle name" : "Product name"}
                  style={{
                    fontSize: 18, fontWeight: 700, color: "#0e0e10", fontFamily: "inherit",
                    border: "1px solid #0e0e10", borderRadius: 6, padding: "4px 8px",
                    outline: "none", minWidth: 240, background: "#fff"
                  }} /> :
                <h3
                  onClick={fullLock ? undefined : () => setTitleEditing(true)}
                  title={fullLock ? undefined : "Click to rename"}
                  style={{ fontSize: 18, fontWeight: 700, margin: 0, color: "#0e0e10", display: "inline-flex", alignItems: "center", gap: 8, cursor: fullLock ? "default" : "text" }}>
                    {title || product.title || (isBundle ? "[bundle]" : "[product name]")}
                    {!fullLock &&
                  <span style={{ color: "#9a9aa1", display: "inline-flex" }}>
                        <IconEdit size={14} style={{ strokeWidth: 2 }} />
                      </span>
                  }
                  </h3>
                }
                  {isBundle ?
                <span style={{
                  display: "inline-flex", alignItems: "center", gap: 8,
                  background: "#e7f0fc", color: "#2563eb",
                  padding: "4px 12px", borderRadius: 999,
                  fontSize: 11, fontWeight: 700, letterSpacing: ".02em"
                }}>
                      <BundleStackIcon size={12} color="#2563eb" />
                      Bundle
                    </span> :

                <span style={{
                  background: "#fff4e6", color: settingBadge.color,
                  padding: "4px 12px", borderRadius: 4,
                  fontSize: 10, fontWeight: 800, letterSpacing: ".06em"
                }}>{settingBadge.label}</span>
                }
                </div>
                <div style={{ fontSize: 13, color: "#6b6b72" }}>
                  {product.subtitle || "Adjust settings, placement and profit margins."}
                </div>
              </div>
              <button onClick={onClose} style={{
              background: "transparent", border: 0, cursor: "pointer",
              padding: 4, color: "#6b6b72", display: "inline-flex"
            }}>
                <IconX size={22} />
              </button>
            </div>

            {/* Body (scrollable) */}
            <div style={{ flex: 1, overflowY: "auto", padding: "4px 28px 24px" }}>
              {fullLock &&
              <div style={{
                display: "flex", alignItems: "flex-start", gap: 12,
                background: "#fff8ef", border: "1px solid #f2e2c9",
                borderRadius: 8, padding: "16px 16px", marginBottom: 16
              }}>
                <span style={{
                  width: 32, height: 32, borderRadius: "50%", flexShrink: 0,
                  background: "#fef0d9", color: "#b06f1e",
                  display: "inline-flex", alignItems: "center", justifyContent: "center"
                }}>
                  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                    <rect x="4" y="11" width="16" height="10" rx="2" />
                    <path d="M8 11V8a4 4 0 0 1 8 0v3" />
                  </svg>
                </span>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 4 }}>This item has orders — changes are locked because it is made to order.</div>
                  <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5 }}>
                    Team assignment, Category and Subcategory stay editable. If every order is cancelled, the remaining fields re-enable automatically.
                  </div>
                </div>
              </div>
              }
              {!isBundle && designLock &&
              <div style={{ border: "1px solid #ececf0", borderRadius: 8, padding: "16px 20px", marginBottom: 16, display: "flex", alignItems: "center", gap: 14, background: "#fafafb", opacity: fullLock ? 0.5 : 1 }}>
                <span style={{ width: 34, height: 34, borderRadius: 8, background: "#ececf0", color: "#52525b", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}><IconLock size={15} /></span>
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>Design &amp; customizations</div>
                  <div style={{ fontSize: 12.5, color: "#6b6b72", fontStyle: "italic", marginTop: 2 }}>Design is locked once an item is added to the store.</div>
                </div>
                <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".06em", background: "#ececf0", color: "#52525b", borderRadius: 4, padding: "3px 8px", flexShrink: 0 }}>LOCKED</span>
              </div>
              }
              <div>
              {isBundle ?
            <BundleEditBody
              items={bundleItems}
              store={store}
              locked={fullLock}
              fundOn={drawerFundOn}
              bundleType={product.bundleType || "all"}
              bundleRequired={bundleRequired}
              bundleSupplied={bundleSupplied}
              onBundleRequired={setBundleRequired}
              onBundleSupplied={setBundleSupplied}
              bundleProfit={bundleProfit}
              onBundleProfit={updateBundleProfit}
              bundleHidden={bundleHidden}
              onBundleHidden={setBundleHidden}
              hideRetail={bundleHideRetail}
              onHideRetail={setBundleHideRetail}
              freeShipping={bundleFreeShip}
              onFreeShipping={setBundleFreeShip}
              itemPricings={itemPricings}
              expandedId={expandedItemId}
              onToggleExpanded={(id) => setExpandedItemId((cur) => cur === id ? null : id)}
              onUpdateItemPricing={updateItemPricing} /> :


            <React.Fragment>
              {/* Category & team behavior — TR/TS are behavioral controls beside the Category fields */}
              <div style={{ border: "1px solid " + (fullLock ? "#0e0e10" : "#ececf0"), borderRadius: 8, padding: 24, marginBottom: 16, position: "relative" }}>
                {fullLock && <span style={{ position: "absolute", top: -9, left: 16, background: "#0e0e10", color: "#fff", fontSize: 10, fontWeight: 800, letterSpacing: ".06em", borderRadius: 4, padding: "2px 8px" }}>STILL EDITABLE</span>}
                <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 4 }}>Category</div>
                <div style={{ fontSize: 12.5, color: "#6b6b72", marginBottom: 12 }}>How this product is grouped on the storefront. Manage the list in Workspace Settings → Product Categories.</div>
                <div style={fullLock ? { pointerEvents: "none", opacity: 0.5 } : undefined}>
                {[
                { key: "req", label: "Team Required", on: optRequired, set: setOptRequired,
                  tip: "Mandatory gear that rostered players must purchase to participate (e.g., home/away uniforms, warm-ups). The player is responsible for covering the full cost of these items at checkout." },
                { key: "sup", label: "Team Supplied", on: optSupplied, set: setOptSupplied,
                  tip: "Mandatory participation gear that has already been paid for collectively (e.g., through team sign-up fees or fundraisers). Players must still select their sizes, but they will pay $0 for these items at checkout." }].
                map((row) =>
                <div key={row.key} style={{
                  display: "flex", alignItems: "center", gap: 12, padding: "6px 0"
                }}>
                    <BundleToggle on={row.on} onChange={(v) => {
                    const apply = () => {
                      // A product is Team Required OR Team Supplied — never both.
                      if (row.key === "req") {setOptRequired(v);if (v) setOptSupplied(false);} else
                      {setOptSupplied(v);if (v) setOptRequired(false);}
                      // Toggling on locks Category to Team Gear (prior value discarded);
                      // toggling both off unlocks it and requires a selection before save.
                      if (v) { setProdCat("Team Gear"); setProdSubcat(""); setCatError(false); }
                      else if (!(row.key === "req" ? optSupplied : optRequired)) { setProdCat(""); setProdSubcat(""); }
                    };
                    // League stores: TR/TS can't save without ≥1 assigned Team.
                    if (v && store?.storeType === "league" && rosters.length === 0) { setTeamPrompt({ apply }); return; }
                    apply();
                  }} />
                    <span style={{ fontSize: 14, color: "#0e0e10" }}>{row.label}</span>
                    <InfoTooltip title={row.label} body={row.tip} />
                  </div>
                )}
                </div>
                <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12, marginTop: 12 }}>
                  <div>
                    <label style={{ display: "block", fontSize: 11, fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 6 }}>Category{!teamOn && <span style={{ color: "#e1251b" }}> *</span>}</label>
                    {teamOn ?
                    <div style={{ display: "flex", alignItems: "center", gap: 8, height: 40, boxSizing: "border-box", border: "1px solid #ececf0", borderRadius: 6, padding: "0 10px", fontSize: 13, background: "#fafafb", color: "#0e0e10", fontWeight: 600 }}>
                      Team Gear
                      <span style={{ marginLeft: "auto", display: "inline-flex", color: "#9a9aa1" }}><IconLock size={13} /></span>
                    </div> :
                    <select value={prodCat} onChange={(e) => { setProdCat(e.target.value); setProdSubcat(""); setCatError(false); }} style={{ width: "100%", height: 40, boxSizing: "border-box", border: "1px solid " + (catError ? "#e1251b" : "#d4d4d8"), borderRadius: 6, padding: "0 10px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }}>
                      <option value="">Select a category</option>
                      {(window.__prodCats || []).map((c) => <option key={c.id} value={c.name}>{c.name}</option>)}
                    </select>
                    }
                  </div>
                  <div>
                    <label style={{ display: "block", fontSize: 11, fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 6 }}>Subcategory</label>
                    <select value={prodSubcat} onChange={(e) => setProdSubcat(e.target.value)} disabled={!prodCat || catSubs.length === 0} style={{ width: "100%", height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 10px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" }}>
                      <option value="">{!prodCat ? "Pick a category first" : catSubs.length === 0 ? "No subcategories" : "No subcategory"}</option>
                      {catSubs.map((s) => <option key={s} value={s}>{s}</option>)}
                    </select>
                  </div>
                </div>
                {catError && <div style={{ fontSize: 12, color: "#e1251b", marginTop: 8, fontWeight: 600 }}>Select a category before saving — items no longer default to Fan Gear.</div>}
                {teamOn && !fullLock && <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 8 }}>Category is locked to Team Gear while Team Required or Team Supplied is on. Subcategory stays selectable.</div>}
              </div>
              {/* Price settings — shared Governing-rule-3 card ($ / % mode) */}
              <PriceSettingsCard pricing={pricing} onChange={setPricing} fundOn={drawerFundOn} locked={fullLock} covered={optSupplied} />
              </React.Fragment>
            }
              {store?.storeType === "league" && (
                <div style={{ border: "1px solid " + (fullLock ? "#0e0e10" : "#ececf0"), borderRadius: 8, padding: 24, marginTop: 16, position: "relative" }}>
                  {fullLock && <span style={{ position: "absolute", top: -9, left: 16, background: "#0e0e10", color: "#fff", fontSize: 10, fontWeight: 800, letterSpacing: ".06em", borderRadius: 4, padding: "2px 8px" }}>STILL EDITABLE</span>}
                  <div style={{ marginBottom: 8 }}>
                    <span style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10" }}>Assign to Teams</span>
                  </div>
                  <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, marginBottom: 4 }}>
                    {teamOn
                      ? <span>Team Required and Team Supplied items must stay assigned to at least one Team.</span>
                      : <span>Choose which Teams wear this product. Leave empty to make it <strong style={{ color: "#0e0e10", fontWeight: 700 }}>available league-wide</strong>.</span>}
                  </div>
                  <RosterAssignField variant="dropdown" direction="down" value={rosters} onChange={setRosters} accent="#e1251b" minOne={teamOn} store={store} />
                </div>
              )}
            </div>
            </div>

            {/* Footer */}
            <div style={{
            borderTop: "1px solid #ececf0", padding: "16px 28px",
            display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12
          }}>
              {isBundle ? (
                <button onClick={fullLock ? undefined : onDelete} title={fullLock ? "This bundle has orders — its structure is locked" : undefined} style={{
                background: "#fff", color: fullLock ? "#c4a29f" : "#e1251b",
                border: "1px solid " + (fullLock ? "#f0dedd" : "#f1c4c2"), borderRadius: 6,
                padding: "12px 16px", fontSize: 13, fontWeight: 600, cursor: fullLock ? "not-allowed" : "pointer",
                display: "inline-flex", alignItems: "center", gap: 8
              }}>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <polyline points="3 6 5 6 21 6" />
                  <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" />
                  <path d="M10 11v6" />
                  <path d="M14 11v6" />
                  <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" />
                </svg>
                Delete Bundle
              </button>
              ) : (
              <button onClick={onDelete} style={{
              background: "#fff", color: "#e1251b",
              border: "1px solid #f1c4c2", borderRadius: 6,
              padding: "12px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer",
              display: "inline-flex", alignItems: "center", gap: 8
            }}>
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                  <polyline points="3 6 5 6 21 6" />
                  <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" />
                  <path d="M10 11v6" />
                  <path d="M14 11v6" />
                  <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" />
                </svg>
                Delete Product
              </button>
              )}
              <button onClick={handleSave} style={{
              background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
              padding: "12px 28px", fontSize: 13, fontWeight: 700, cursor: "pointer"
            }}>Save Changes</button>
            </div>
          </React.Fragment>
        }
      </aside>
      <TeamAssignPrompt
        open={!!teamPrompt}
        initial={rosters}
        store={store}
        settingLabel={optSupplied ? "Team Supplied" : "Team Required"}
        onClose={() => setTeamPrompt(null)}
        onConfirm={(ids) => {
          setRosters(ids);
          const a = teamPrompt && teamPrompt.apply;
          setTeamPrompt(null);
          if (a) a();
        }} />
    </React.Fragment>);

}

// Curated catalog of real product photos. Products are mapped to one of these
// deterministically so a given product always shows the same photo.
const PRODUCT_PHOTOS = [
"assets/products/jersey-grizzlies.png",
"assets/products/jersey-lancers.png",
"assets/products/jersey-padres.png",
"assets/products/jersey-warriors.png",
"assets/products/jersey-asteroid.png",
"assets/products/pants-navy.png",
"assets/products/pants-orange.png",
"assets/products/shorts-green.png",
"assets/products/shorts-navy.png"];


function photoForKey(key) {
  const s = String(key || "");
  let h = 0;
  for (let i = 0; i < s.length; i++) h = h * 31 + s.charCodeAt(i) >>> 0;
  return PRODUCT_PHOTOS[h % PRODUCT_PHOTOS.length];
}

function DefaultProductThumb({ store, product }) {
  const key = product?.id || product?.__pendingId || product?.title || store?.id || store?.teamName || "team";
  return (
    <img
      src={photoForKey(key)}
      alt={product?.title || "Product"}
      style={{ width: "100%", height: "100%", objectFit: "contain", display: "block" }} />);

}

const SmileIcon = ({ size = 14 }) =>
<svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
    <circle cx="12" cy="12" r="10" />
    <path d="M8 14s1.5 2 4 2 4-2 4-2" />
    <line x1="9" y1="9" x2="9.01" y2="9" />
    <line x1="15" y1="9" x2="15.01" y2="9" />
  </svg>;

// Filled document glyph — internal lines are knocked out in the pill's bg color.
const DraftDocIcon = ({ size = 22, color = "currentColor", bg = "#fff" }) =>
<svg width={size} height={size} viewBox="0 0 24 24" fill="none">
    <path d="M6 2.5h7.2L19 8.3V20.5a1.4 1.4 0 0 1-1.4 1.4H6a1.4 1.4 0 0 1-1.4-1.4V3.9A1.4 1.4 0 0 1 6 2.5Z" fill={color} />
    <path d="M13 2.6V8.4h5.8" fill="none" stroke={bg} strokeWidth="1.6" strokeLinejoin="round" />
    <line x1="8.4" y1="12.4" x2="15.4" y2="12.4" stroke={bg} strokeWidth="1.7" strokeLinecap="round" />
    <line x1="8.4" y1="15.6" x2="15.4" y2="15.6" stroke={bg} strokeWidth="1.7" strokeLinecap="round" />
    <line x1="8.4" y1="18.8" x2="12.4" y2="18.8" stroke={bg} strokeWidth="1.7" strokeLinecap="round" />
  </svg>;

// Filled globe glyph — meridian/parallels knocked out in the pill's bg color.
const GlobeIcon = ({ size = 22, color = "currentColor", bg = "#fff" }) =>
<svg width={size} height={size} viewBox="0 0 24 24" fill="none">
    <circle cx="12" cy="12" r="9.4" fill={color} />
    <g stroke={bg} strokeWidth="1.4" fill="none" strokeLinecap="round">
      <ellipse cx="12" cy="12" rx="3.9" ry="9.4" />
      <line x1="2.7" y1="12" x2="21.3" y2="12" />
      <path d="M4.2 7.6h15.6M4.2 16.4h15.6" />
    </g>
  </svg>;

// Trophy-style success banner that slides in from the bottom right when a product is published.
function SuccessBanner({ message, body, onDone, duration = 3600 }) {
  React.useEffect(() => {
    if (!message) return;
    const t = setTimeout(() => onDone && onDone(), duration);
    return () => clearTimeout(t);
  }, [message, duration, onDone]);
  if (!message) return null;
  return (
    <div style={{
      position: "fixed", bottom: 88, right: 28,
      background: "#fff", borderRadius: 10,
      boxShadow: "0 16px 40px rgba(20,20,30,.18), 0 2px 6px rgba(20,20,30,.06)",
      border: "1px solid #ececf0",
      padding: "16px 16px",
      display: "flex", alignItems: "flex-start", gap: 16,
      minWidth: 340, maxWidth: 420, zIndex: 200,
      animation: "cheersIn .3s cubic-bezier(.2,.7,.2,1)"
    }}>
      <div style={{
        width: 40, height: 40, borderRadius: 8,
        background: "#eef3fb", color: "#2563eb",
        display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0
      }}>
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
          <path d="M8 21h8" />
          <path d="M12 17v4" />
          <path d="M7 4h10v5a5 5 0 0 1-10 0z" />
          <path d="M17 5h3v3a3 3 0 0 1-3 3" />
          <path d="M7 5H4v3a3 3 0 0 0 3 3" />
        </svg>
      </div>
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10", marginBottom: 2 }}>{message}</div>
        {body && <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.45 }}>{body}</div>}
      </div>
      <button onClick={() => onDone && onDone()} style={{
        background: "transparent", border: 0, cursor: "pointer",
        padding: 2, color: "#9a9aa1", display: "inline-flex", flexShrink: 0
      }}>
        <IconX size={18} />
      </button>
      <style>{`@keyframes cheersIn{from{opacity:0;transform:translate(20px, 8px)}to{opacity:1;transform:translate(0,0)}}`}</style>
    </div>);

}


Object.assign(window, { ConfigureProduct, ProductsList, DefaultProductThumb, defaultApplicationSizes, defaultPricing, SuccessBanner, UnpublishWarningModal, ConfirmSizesModal });

// ─────────── Unpublish warning modals ───────────
// Two variants:
//  - "locked": at least one rostered player has already ordered. Offers
//    "Keep Published for Now" + "Go to Pending Orders" (routes to the order).
//  - "confirm": no one has ordered yet — confirmation w/ Cancel + Unpublish.
function UnpublishWarningModal({ open, variant = "confirm", onClose, onConfirm, onGoToPending }) {
  const isLocked = variant === "locked";
  return (
    <Modal open={open} onClose={onClose} width={480}>
      <div style={{ padding: "20px 28px 28px", position: "relative" }}>
        <button onClick={onClose} aria-label="Close" style={{
          position: "absolute", top: 16, right: 16,
          background: "transparent", border: 0, cursor: "pointer",
          padding: 4, color: "#6b6b72", display: "inline-flex"
        }}>
          <IconX size={20} />
        </button>

        <div style={{
          display: "flex", flexDirection: "column", alignItems: "center",
          padding: "28px 8px 12px"
        }}>
          <div style={{
            width: 72, height: 72, borderRadius: "50%",
            background: isLocked ? "#fdecec" : "#f4f4f6",
            color: isLocked ? "#e1251b" : "#6b6b72",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            marginBottom: 24
          }}>
            {isLocked ?
            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <rect x="4" y="11" width="16" height="10" rx="2" />
                <path d="M8 11V8a4 4 0 0 1 8 0v3" />
              </svg> :

            <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
                <polyline points="3 6 5 6 21 6" />
                <path d="M19 6l-1 14a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6" />
                <path d="M10 11v6" />
                <path d="M14 11v6" />
                <path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2" />
              </svg>
            }
          </div>

          <h3 style={{
            fontSize: 20, fontWeight: 700, margin: 0, marginBottom: 16,
            color: "#0e0e10", textAlign: "center"
          }}>
            {isLocked ? "Active Orders Exist for This Item" : "Unpublish Required Product?"}
          </h3>
          <p style={{
            fontSize: 14, color: "#6b6b72", lineHeight: 1.55,
            textAlign: "center", margin: 0, maxWidth: 420
          }}>
            {isLocked ?
            "This item has already been ordered by at least one rostered player. To prevent uniform mismatch issues with the manufacturer, you must cancel the existing pending orders before unpublishing or changing this product." :
            "This is a Team Required item, but since no rostered players have ordered it yet, it can be safely hidden. Your configurations will be saved, but you must republish it to make it available again."}
          </p>
        </div>

        <div style={{
          display: "flex", gap: 12, marginTop: 28,
          justifyContent: "stretch"
        }}>
          {isLocked ?
          <React.Fragment>
            <button onClick={onClose} style={{
              flex: 1, background: "#fff", color: "#0e0e10",
              border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 24px", fontSize: 14, fontWeight: 600, cursor: "pointer"
            }}>Keep Published for Now</button>
            <button onClick={onGoToPending} style={{
              flex: 1, background: "#0e0e10", color: "#fff",
              border: 0, borderRadius: 6, padding: "16px 24px",
              fontSize: 14, fontWeight: 700, cursor: "pointer"
            }}>Go to Pending Orders</button>
          </React.Fragment> :

          <React.Fragment>
              <button onClick={onClose} style={{
              flex: 1, background: "#fff", color: "#0e0e10",
              border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 24px", fontSize: 14, fontWeight: 600, cursor: "pointer"
            }}>Cancel</button>
              <button onClick={onConfirm} style={{
              flex: 1, background: "#0e0e10", color: "#fff",
              border: 0, borderRadius: 6, padding: "16px 24px",
              fontSize: 14, fontWeight: 700, cursor: "pointer"
            }}>Unpublish</button>
            </React.Fragment>
          }
        </div>
      </div>
    </Modal>);

}

/* ===== ASSET 21 : c222cd55-a463-4611-b136-34be8e5e16e2 ===== */
// Bundle flow — create a bundle of draft products in the Products tab.
//
// Two surfaces:
//   • CreateBundleSection — inline panel in the Products tab
//   • SelectProductsModal — full product picker modal ("See All Products")

// ─────────── Stack-of-layers icon (used in the header) ───────────
function BundleStackIcon({ size = 26, color = "#0e0e10" }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round">
      <path d="M12 3 2.5 8 12 13l9.5-5L12 3z" />
      <path d="M2.5 12 12 17l9.5-5" />
      <path d="M2.5 16 12 21l9.5-5" />
    </svg>);

}

// Small product thumbnail used inside picker cards and selected chips.
function ProductMiniThumb({ product, store, size = 44 }) {
  return (
    <div style={{
      width: size, height: size, background: "#f1f2f4", borderRadius: 4,
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      flexShrink: 0, overflow: "hidden"
    }}>
      <div style={{ width: size - 12, height: size - 8 }}>
        {product.thumbnail || <DefaultProductThumb store={store} product={product} />}
      </div>
    </div>);

}

// ─────────── Setting type pill (used on cards) ───────────
function SettingPill({ teamSetting, small }) {
  const map = {
    "team-required": { label: "Team Required", color: "#e8590c" },
    "team-supplied": { label: "Team Supplied", color: "#2944a8" },
    "fan-gear": { label: "Fan Gear", color: "#1f7a44" }
  };
  const m = map[teamSetting];
  if (!m) return null;
  return (
    <span style={{
      color: m.color, fontSize: small ? 10 : 11, fontWeight: 600,
      letterSpacing: ".02em"
    }}>{m.label}</span>);

}

// ─────────── Picker tile — used in the inline grid AND modal list rows ───────────
function PickerCard({ product, store, selected, disabled, onAdd, lockNote }) {
  return (
    <div title={lockNote || undefined} style={{
      display: "flex", alignItems: "center", gap: 12,
      padding: "12px 12px",
      border: "1px solid #ececf0", borderRadius: 8,
      background: disabled ? "#fafafb" : "#fff",
      opacity: disabled ? 0.55 : 1,
      cursor: disabled ? "default" : "pointer",
      transition: "border-color .12s, background .12s"
    }}
      onMouseEnter={(e) => {if (!disabled) e.currentTarget.style.borderColor = "#0e0e10";}}
      onMouseLeave={(e) => {if (!disabled) e.currentTarget.style.borderColor = "#ececf0";}}
      onClick={() => {if (!disabled) onAdd && onAdd();}}>
      <ProductMiniThumb product={product} store={store} />
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{
          fontSize: 12, fontWeight: 700, color: "#0e0e10",
          letterSpacing: ".04em", textTransform: "uppercase",
          whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis"
        }}>{product.title || "Untitled Product"}</div>
        <div style={{ marginTop: 2 }}>
          <SettingPill teamSetting={product.teamSetting} small />
        </div>
      </div>
      {lockNote ?
      <span style={{ color: "#9a9aa1", display: "inline-flex", padding: 4 }}><IconLock size={16} /></span> :
      <button
        onClick={(e) => {e.stopPropagation();if (!disabled) onAdd && onAdd();}}
        disabled={disabled}
        title={selected ? "Already in bundle" : "Add"}
        style={{
          background: "transparent", border: 0, padding: 4,
          color: selected ? "#1f9d55" : "#0e0e10",
          cursor: disabled ? "default" : "pointer", display: "inline-flex"
        }}>
        {selected ?
        <IconCheck size={18} style={{ strokeWidth: 2.5 }} /> :
        <IconPlus size={18} style={{ strokeWidth: 2.2 }} />
        }
      </button>
      }
    </div>);

}

// ─────────── Selected chip — the row of products already in the bundle ───────────
function SelectedChip({ product, store, onRemove }) {
  const price = product.pricing?.adult?.retail || "00.00";
  return (
    <div style={{
      display: "flex", alignItems: "center", gap: 12,
      padding: "12px 12px",
      border: "1px solid #ececf0", borderRadius: 8, background: "#fff"
    }}>
      <ProductMiniThumb product={product} store={store} size={40} />
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{
          fontSize: 12, fontWeight: 700, color: "#0e0e10",
          letterSpacing: ".04em", textTransform: "uppercase",
          whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis"
        }}>{product.title || "Untitled"}</div>
        <div style={{ fontSize: 12, color: "#6b6b72", marginTop: 2 }}>${price}</div>
      </div>
      <button
        onClick={onRemove}
        title="Remove from bundle"
        style={{
          background: "transparent", border: 0, padding: 4,
          color: "#e1251b", cursor: "pointer", display: "inline-flex"
        }}>
        <IconX size={16} style={{ strokeWidth: 2.2 }} />
      </button>
    </div>);

}

// ─────────── Bundle pricing model ───────────
// Mirrors the individual-item model one level up: members’ base cost + dealer
// profit (+ fundraising when the item collects it) is what the
// shopper pays. Precedence follows the item model, which has exactly one profit
// set per priced thing: the bundle’s own profit REPLACES the summed per-member
// profit on the bundle line. Per-member profit still governs an item sold alone.
// One profit set for the whole bundle — base cost is what varies by size, the
// profit doesn’t, so the item model’s per-size rows collapse to a single set.
function defaultBundleProfit() {
  return { dealerUnit: "$", dealer: "0.00", fundUnit: "$", fund: "0.00" };
}
function bnum(v) { const n = parseFloat(v); return isNaN(n) ? 0 : n; }
function bmoney(v) { return "$" + v.toFixed(2); }
function brange(s) { return s.lo === s.hi ? bmoney(s.lo) : bmoney(s.lo) + " – " + bmoney(s.hi); }

// One size’s derived numbers. All-Items → a single combined price;
// Choice → one price per option (the shopper buys exactly one member).
function bundleSizeMath(items, pricings, profit, size, bundleType, fundOn) {
  const bases = items.map((it) => bnum(((pricings || {})[it.id] || it.pricing || defaultPricing())[size]?.base));
  if (bases.length === 0) return null;
  const combos = bundleType === "choice" ? bases : [bases.reduce((a, b) => a + b, 0)];
  const p = profit || defaultBundleProfit();
  const fundActive = p.fundraisingOn != null ? p.fundraisingOn : fundOn;
  const amt = (unit, val, base) => unit === "%" ? base * bnum(val) / 100 : bnum(val);
  const rows = combos.map((base) => {
    const dealer = amt(p.dealerUnit, p.dealer, base);
    const fund = fundActive ? amt(p.fundUnit, p.fund, base) : 0;
    return { base, dealer, fund, customer: base + dealer + fund };
  });
  const span = (k) => ({ lo: Math.min.apply(null, rows.map((r) => r[k])), hi: Math.max.apply(null, rows.map((r) => r[k])) });
  return { rows, base: span("base"), dealer: span("dealer"), fund: span("fund"), customer: span("customer") };
}
// Card snapshot — the catalog card’s "from $X" reads the low end of this.
function bundleCardPricing(items, pricings, profit, bundleType, supplied, fundOn) {
  const one = (size) => {
    if (supplied) return "0.00";
    const m = bundleSizeMath(items, pricings, profit, size, bundleType, fundOn);
    if (!m) return "—";
    return m.customer.lo === m.customer.hi ?
    m.customer.lo.toFixed(2) :
    m.customer.lo.toFixed(2) + " - " + m.customer.hi.toFixed(2);
  };
  return { youth: { retail: one("youth") }, adult: { retail: one("adult") } };
}

function BundleLockChip() {
  return (
    <span style={{
      fontSize: 10, fontWeight: 800, letterSpacing: ".06em", background: "#ececf0",
      color: "#52525b", borderRadius: 4, padding: "3px 8px", flexShrink: 0
    }}>LOCKED</span>);

}

// ─────────── BundlePriceSettings ─────────── (per-bundle profit, mirrors items)
function BundlePriceSettings({ items, pricings, profit, onChange, bundleType, supplied, fundOn, locked }) {
  const choice = bundleType === "choice";
  const p = profit || defaultBundleProfit();
  const math = {
    youth: bundleSizeMath(items, pricings, profit, "youth", bundleType, fundOn),
    adult: bundleSizeMath(items, pricings, profit, "adult", bundleType, fundOn)
  };
  const fundActive = p.fundraisingOn != null ? p.fundraisingOn : fundOn;
  const fields = [
  { label: "DEALER PROFIT", unit: p.dealerUnit, value: p.dealer, u: "dealerUnit", v: "dealer" }].
  concat(fundActive ? [{ label: "FUNDRAISING", unit: p.fundUnit || "$", value: p.fund ?? "0.00", u: "fundUnit", v: "fund" }] : []);
  const readoutCols = "70px minmax(130px,240px) minmax(130px,240px)";
  const bpMode = (p.dealerUnit === "%" || p.fundUnit === "%") ? "%" : "$";
  const setBpMode = (m2) => {
    if (m2 === bpMode) return;
    // One mode governs the section; switching clears the entered values (Governing rule 3).
    onChange("dealerUnit", m2); onChange("fundUnit", m2);
    onChange("dealer", ""); onChange("fund", "");
  };
  const setBundleFund = (on) => onChange("fundraisingOn", on);
  const savedBundleFund = bnum(p.fund) > 0;
  return (
    <div style={{ border: "1px solid #ececf0", borderRadius: 8, padding: 24, marginBottom: 16 }}>
      <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 4 }}>
        <span style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".10em", color: "#0e0e10" }}>PRICE SETTINGS</span>
        {locked && <BundleLockChip />}
        {!supplied && items.length > 0 && <span style={{ marginLeft: "auto" }}><PSModeControl mode={bpMode} onChange={setBpMode} locked={locked} /></span>}
      </div>
      <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, marginBottom: 16 }}>
        {supplied ?
        "Team Supplied — the team has already covered this bundle, so shoppers pay $0 and these inputs don’t apply." :
        "Members’ base cost + dealer profit + fundraising, derived exactly the way it is on an individual item. The bundle’s profit replaces the summed per-member profit on the bundle line; per-member profit still applies when an item is sold on its own."}
      </div>

      {supplied ? null : items.length === 0 ?
      <div style={{ padding: "24px 16px", textAlign: "center", color: "#9a9aa1", fontSize: 12.5, border: "1px dashed #e5e5e9", borderRadius: 8 }}>
          Add products to set bundle pricing.
        </div> :

      <React.Fragment>
          <div style={locked ? { pointerEvents: "none", opacity: 0.5 } : undefined}>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(150px, 220px))", gap: 16 }}>
              {fields.map((f) =>
            <div key={f.label}>
                  <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".10em", color: "#9a9aa1", marginBottom: 8 }}>{f.label}</div>
                  <ProfitInput
                unit={f.unit}
                value={f.value}
                onUnit={(u) => onChange(f.u, u)}
                onValue={(v) => onChange(f.v, v)} />
                </div>
            )}
            </div>
            <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 10 }}>
              One profit for the bundle — base cost is what varies by size. Switching $ / % clears the entered values; percentages apply to base cost.
            </div>
          </div>

          {choice &&
        <div style={{ fontSize: 12.5, color: "#6b6b72", background: "#fafafb", border: "1px solid #f1f1f3", borderRadius: 6, padding: "10px 12px", marginTop: 16, lineHeight: 1.45 }}>
              Choice Bundle — the shopper buys one option, so the same profit produces a different customer price per option.
            </div>
        }

          <div style={{ marginTop: 20, paddingTop: 16, borderTop: "1px solid #ececf0" }}>
            <div style={{ display: "grid", gridTemplateColumns: readoutCols, gap: 12, fontSize: 11, fontWeight: 700, letterSpacing: ".10em", color: "#9a9aa1", marginBottom: 10 }}>
              <div>SIZE</div>
              <div>MEMBERS’ BASE COST</div>
              <div>BUNDLE RETAIL</div>
            </div>
            {["youth", "adult"].map((size, i) =>
          <div key={size} style={{ display: "grid", gridTemplateColumns: readoutCols, gap: 12, alignItems: "center", padding: "8px 0", borderTop: i > 0 ? "1px solid #f1f1f3" : 0 }}>
                <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{size.toUpperCase()}</div>
                <div style={{ fontSize: 13, color: "#6b6b72" }}>{brange(math[size].base)}</div>
                <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10" }}>{brange(math[size].customer)}</div>
              </div>
          )}
            <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 8, fontStyle: "italic" }}>
              Bundle retail is what customers see.
            </div>
          </div>

          <div style={{ display: "flex", alignItems: "center", gap: 14, marginTop: 16, paddingTop: 14, borderTop: "1px solid #ececf0", ...(locked ? { opacity: 0.55, pointerEvents: "none" } : {}) }}>
            <div style={{ flex: 1, minWidth: 0 }}>
              <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Fundraising on this bundle</div>
              <div style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.45, marginTop: 2 }}>
                {fundActive
                  ? "Added on top of bundle retail. Defaults to the store's Team Fundraising setting."
                  : savedBundleFund
                    ? "Off — saved amount (" + (p.fundUnit === "%" ? bnum(p.fund) + "%" : bmoney(bnum(p.fund))) + ") kept and restored if re-enabled."
                    : "Off — this bundle doesn't collect fundraising."}
              </div>
            </div>
            <button type="button" onClick={() => setBundleFund(!fundActive)} style={{ background: "none", border: 0, padding: 0, cursor: "pointer", flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8, fontFamily: "inherit" }}>
        <span style={{ width: 34, height: 19, borderRadius: 999, position: "relative", background: fundActive ? "#1f8a55" : "#d4d4d8", transition: "background .14s", display: "inline-block" }}>
          <span style={{ position: "absolute", top: 2, left: fundActive ? 17 : 2, width: 15, height: 15, borderRadius: 999, background: "#fff", transition: "left .14s" }} />
        </span>
        <span style={{ fontSize: 11.5, fontWeight: 700, width: 24, textAlign: "left", color: fundActive ? "#1f8a55" : "#71717a" }}>{fundActive ? "On" : "Off"}</span>
      </button>
          </div>
        </React.Fragment>
      }
    </div>);

}

// ─────────── BundleOptions ─────────── (storefront + shipping flags)
// Visibility reuses the one storefront Visible/Hidden control every catalog
// product carries (product.hidden) — surfaced here so a rep never has to leave
// the panel. Display flags stay editable in every lock state; free shipping
// changes order economics, so it locks with the commercial fields.
function BundleOptions({ hidden, onHidden, hideRetail, onHideRetail, freeShipping, onFreeShipping, locked }) {
  const rowStyle = (i) => ({
    display: "flex", alignItems: "center", gap: 20, padding: "14px 0",
    borderTop: i > 0 ? "1px solid #f1f1f3" : 0
  });
  return (
    <div style={{
      border: "1px solid " + (locked ? "#0e0e10" : "#ececf0"), borderRadius: 8,
      padding: 24, marginBottom: 16, position: "relative"
    }}>
      {locked &&
      <span style={{
        position: "absolute", top: -9, left: 16, background: "#0e0e10", color: "#fff",
        fontSize: 10, fontWeight: 800, letterSpacing: ".06em", borderRadius: 4, padding: "2px 8px"
      }}>STILL EDITABLE</span>
      }
      <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 4 }}>Bundle Options</div>
      <div style={{ fontSize: 12.5, color: "#6b6b72", marginBottom: 4 }}>
        How this bundle shows up on the storefront, and how it ships.
      </div>

      <div style={rowStyle(0)}>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>Storefront visibility</div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45, marginTop: 2 }}>
            {hidden ?
            "Hidden — shoppers won’t see this bundle in the catalog." :
            "Visible — shoppers see this bundle in the catalog while the store is live."}
          </div>
        </div>
        <button
          onClick={() => onHidden(!hidden)}
          title={hidden ? "Hidden from the storefront — click to show" : "Visible on the storefront — click to hide"}
          style={{
            display: "inline-flex", alignItems: "center", gap: 9, background: "none",
            border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit", flexShrink: 0
          }}>
          <span style={{
            width: 34, height: 19, borderRadius: 999, position: "relative", flexShrink: 0,
            background: hidden ? "#d4d4d8" : "#1f8a55", transition: "background .14s"
          }}>
            <span style={{
              position: "absolute", top: 2, left: hidden ? 2 : 17, width: 15, height: 15,
              borderRadius: 999, background: "#fff", transition: "left .14s"
            }} />
          </span>
          <span style={{ fontSize: 11.5, fontWeight: 700, width: 44, textAlign: "left", color: hidden ? "#71717a" : "#1f8a55" }}>
            {hidden ? "Hidden" : "Visible"}
          </span>
        </button>
      </div>

      <div style={rowStyle(1)}>
        <div style={{ flex: 1, minWidth: 0 }}>
          <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>Hide retail price</div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45, marginTop: 2 }}>
            The catalog card and product page won’t show a price. Shoppers still pay the configured price at checkout — this hides the number, it doesn’t zero it.
          </div>
        </div>
        <BundleToggle on={hideRetail} onChange={onHideRetail} />
      </div>

      <div style={rowStyle(2)}>
        <div style={{ flex: 1, minWidth: 0, ...(locked ? { opacity: 0.5 } : {}) }}>
          <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10", display: "flex", alignItems: "center", gap: 8 }}>
            Free shipping for the bundle{locked && <BundleLockChip />}
          </div>
          <div style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.45, marginTop: 2 }}>
            Waives shipping on the whole bundle order line at checkout — not on the rest of the cart.
          </div>
        </div>
        <span style={locked ? { pointerEvents: "none", opacity: 0.5, display: "inline-flex" } : { display: "inline-flex" }}>
          <BundleToggle on={freeShipping} onChange={onFreeShipping} />
        </span>
      </div>
    </div>);

}

// ─────────── BundlePricingInsights ─────────── (read-only, recalculates live)
function BundlePricingInsights({ items, pricings, profit, bundleType, supplied, freeShipping, fundOn }) {
  const my = bundleSizeMath(items, pricings, profit, "youth", bundleType, fundOn);
  const ma = bundleSizeMath(items, pricings, profit, "adult", bundleType, fundOn);
  const shell = (children) => (
    <div style={{ background: "#fafafb", border: "1px solid #ececf0", borderRadius: 8, padding: "18px 20px" }}>
      <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, flexWrap: "wrap", marginBottom: 14 }}>
        <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", textTransform: "uppercase", color: "#6b6b72" }}>
          Bundle Pricing Information
        </span>
        <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>Updates as members, profit and options change</span>
      </div>
      {children}
    </div>);

  if (!my || !ma) return shell(
    <div style={{ fontSize: 12.5, color: "#9a9aa1" }}>Add products to see the bundle’s pricing breakdown.</div>);

  const choice = bundleType === "choice";
  const anyFund = my.fund.hi > 0 || ma.fund.hi > 0;
  const rows = [
  { label: "Store price", hint: "Members’ base cost", y: brange(my.base), a: brange(ma.base) },
  { label: "Dealer profit", y: brange(my.dealer), a: brange(ma.dealer) },
  anyFund && { label: "Fundraising", y: brange(my.fund), a: brange(ma.fund) },
  {
    label: "Customer price",
    hint: supplied ? "Paid by the team" : choice ? "Per option" : "One combined price",
    y: supplied ? "Covered" : brange(my.customer),
    a: supplied ? "Covered" : brange(ma.customer),
    strong: true
  },
  freeShipping && { label: "Est. shipping", y: "Free", a: "Free", free: true }].
  filter(Boolean);
  const grid = { display: "grid", gridTemplateColumns: "minmax(150px,1.4fr) 1fr 1fr", gap: 12, alignItems: "baseline" };
  return shell(
    <React.Fragment>
      <div style={{ ...grid, fontSize: 10, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", paddingBottom: 8, borderBottom: "1px solid #ececf0" }}>
        <div></div><div style={{ textAlign: "right" }}>YOUTH</div><div style={{ textAlign: "right" }}>ADULT</div>
      </div>
      {rows.map((r, i) =>
      <div key={r.label} style={{ ...grid, padding: "9px 0", borderTop: i > 0 ? "1px solid #f1f1f3" : 0 }}>
          <div style={{ minWidth: 0 }}>
            <span style={{ fontSize: 12.5, fontWeight: r.strong ? 700 : 500, color: r.strong ? "#0e0e10" : "#6b6b72" }}>{r.label}</span>
            {r.hint && <span style={{ fontSize: 11.5, color: "#9a9aa1", marginLeft: 8 }}>{r.hint}</span>}
          </div>
          {[r.y, r.a].map((v, k) =>
        <div key={k} style={{
          textAlign: "right", fontSize: r.strong ? 13.5 : 12.5,
          fontWeight: r.strong || r.free ? 700 : 500,
          color: r.free || v === "Covered" ? "#1f8a55" : r.strong ? "#0e0e10" : "#3f3f46"
        }}>{v}</div>
        )}
        </div>
      )}
      {choice && !supplied &&
      <div style={{ marginTop: 12, paddingTop: 12, borderTop: "1px solid #ececf0", fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.45 }}>
          Catalog card reads “from {bmoney(Math.min(my.customer.lo, ma.customer.lo))}” — the lowest-priced option.
        </div>
      }
    </React.Fragment>);

}

// ─────────── CreateBundleSection ─────────── (inline card in Products tab)
function CreateBundleSection({ store, draftProducts, onCancel, onCreate }) {
  const [name, setName] = React.useState("");
  const [btype, setBtype] = React.useState(null); // "all" | "choice" — immutable after creation
  const [setting, setSetting] = React.useState(null); // "team-required" | "team-supplied" | null
  const [cat, setCat] = React.useState("");
  const [selectedIds, setSelectedIds] = React.useState([]);
  const [search, setSearch] = React.useState("");
  const [showModal, setShowModal] = React.useState(false);
  const [bundleProfit, setBundleProfit] = React.useState(defaultBundleProfit());
  const [hiddenFlag, setHiddenFlag] = React.useState(false);
  const [memberPrompt, setMemberPrompt] = React.useState(false); // Epic J — hide members?
  const [hideRetail, setHideRetail] = React.useState(false);
  const [freeShipping, setFreeShipping] = React.useState(false);
  const updateBundleProfit = (field, value) =>
  setBundleProfit((prev) => ({ ...prev, [field]: value }));
  const fundOn = store?.fundraisingEnabled === true;

  const selectedSet = new Set(selectedIds);
  const selectedProducts = selectedIds
    .map((id) => draftProducts.find((p) => p.id === id))
    .filter(Boolean);


  // Available products = drafts NOT already selected, filtered by search.
  const q = search.trim().toLowerCase();
  const availableAll = draftProducts.filter(
    (p) => !q || (p.title || "").toLowerCase().includes(q)
  );
  // First 8 shown in the inline grid (selected products are shown disabled / faded
  // so users get a sense of "what's in vs. what's available" at a glance).
  const QUICK = 8;
  const visibleQuick = availableAll.slice(0, QUICK);

  const addProduct = (id) => {
    if (!selectedSet.has(id)) setSelectedIds((s) => [...s, id]);
  };
  const removeProduct = (id) => {
    setSelectedIds((s) => s.filter((x) => x !== id));
  };

  const canCreate = name.trim().length > 0 && selectedIds.length >= 2 && !!btype && (setting != null || cat !== "");

  const handleCreate = () => {
    if (!canCreate) return;
    // Epic J — after members are chosen, ask whether to hide the individual
    // member items from the store (writes each member's Visibility flag).
    setMemberPrompt(true);
  };

  const finishCreate = (hideMembers) => {
    setMemberPrompt(false);
    onCreate({
      name: name.trim(),
      teamSetting: setting,
      bundleType: btype,
      category: setting ? "Team Gear" : cat,
      productIds: selectedIds,
      bundleProfit,
      hidden: hiddenFlag,
      hideRetail,
      freeShipping,
      hideMembers
    });
  };

  const hasSelection = selectedIds.length > 0;

  return (
    <div style={{ paddingBottom: 100 }}>
      <div style={{
        background: "#fff", border: "1px solid #ececf0", borderRadius: 12,
        boxShadow: "0 1px 2px rgba(20,20,30,.03)",
        padding: 28
      }}>
        {/* Header */}
        <div style={{
          display: "flex", alignItems: "flex-start", justifyContent: "space-between",
          gap: 16, marginBottom: 24
        }}>
          <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
            <div style={{
              width: 44, height: 44, borderRadius: 8, background: "#f4f4f6",
              display: "inline-flex", alignItems: "center", justifyContent: "center",
              flexShrink: 0
            }}>
              <BundleStackIcon size={22} />
            </div>
            <div>
              <h2 style={{ fontSize: 18, fontWeight: 700, margin: 0, color: "#0e0e10" }}>
                Create Bundle
              </h2>
              <div style={{ fontSize: 13, color: "#6b6b72", marginTop: 2 }}>
                Add products to create a team bundle
              </div>
            </div>
          </div>

          <div style={{ display: "inline-flex", alignItems: "center", gap: 12, flexShrink: 0 }}>
            <button onClick={onCancel} style={{
              background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 24px", fontSize: 13, fontWeight: 600, color: "#0e0e10",
              cursor: "pointer"
            }}>Cancel</button>
            <button
              onClick={handleCreate}
              disabled={!canCreate}
              title={!canCreate ? "Pick a bundle type, name the bundle, set a distinction or category, and add at least 2 products" : ""}
              style={{
                background: canCreate ? "#0e0e10" : "#ececf0",
                color: canCreate ? "#fff" : "#9a9aa1",
                border: 0, borderRadius: 6, padding: "12px 24px",
                fontSize: 13, fontWeight: 700,
                cursor: canCreate ? "pointer" : "not-allowed",
                transition: "background .15s, color .15s"
              }}>Create Bundle</button>
          </div>
        </div>

        {/* Epic J — member visibility prompt (reversible; writes each member's Visibility flag) */}
        {memberPrompt && (
          <Modal open onClose={() => setMemberPrompt(false)} width={520}>
            <div style={{ padding: "24px 28px 28px" }}>
              <h3 style={{ margin: "0 0 6px", fontSize: 18, fontWeight: 800, color: "#0e0e10" }}>Hide the individual items from the store?</h3>
              <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.55, marginBottom: 14 }}>
                Hiding them makes "{name.trim()}" the only way to buy this gear; keeping them visible leaves each item individually purchasable alongside the bundle. This sets each item's Visibility toggle — reversible anytime from its detail page.
              </div>
              <div style={{ border: "1px solid #ececf0", borderRadius: 8, padding: "2px 14px", marginBottom: 18, maxHeight: 240, overflowY: "auto" }}>
                {selectedProducts.map((it, i) => (
                  <div key={it.id} style={{ display: "flex", alignItems: "center", gap: 10, padding: "8px 0", borderTop: i > 0 ? "1px solid #f4f4f6" : 0, fontSize: 13, color: "#0e0e10" }}>
                    <ProductMiniThumb product={it} store={store} size={30} />
                    <span style={{ minWidth: 0, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{it.title}</span>
                  </div>
                ))}
              </div>
              <div style={{ display: "flex", justifyContent: "flex-end", gap: 12 }}>
                <button onClick={() => finishCreate(false)} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 20px", fontSize: 13, fontWeight: 600, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Keep items visible</button>
                <button onClick={() => finishCreate(true)} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "12px 20px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Hide member items</button>
              </div>
            </div>
          </Modal>
        )}

        {/* Bundle Type — set once, immutable after creation */}
        <div style={{ marginBottom: 24 }}>
          <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 4 }}>Bundle Type</div>
          <div style={{ fontSize: 12, color: "#9a9aa1", marginBottom: 10 }}>Set once — the type can’t be changed after the bundle is created.</div>
          <div style={{ display: "flex", gap: 12, flexWrap: "wrap" }}>
            {[
            { id: "all", label: "All-Items Bundle", desc: "Shoppers buy every item in the bundle at one combined price." },
            { id: "choice", label: "Choice Bundle", desc: "Shoppers must pick exactly one item — e.g. required pants: full-length or cuffed." }].
            map((t) => (
              <button key={t.id} type="button" onClick={() => setBtype(t.id)} style={{ flex: "1 1 260px", textAlign: "left", display: "flex", gap: 12, alignItems: "flex-start", background: btype === t.id ? "#fafafb" : "#fff", border: `1.5px solid ${btype === t.id ? "#0e0e10" : "#ececf0"}`, borderRadius: 8, padding: "14px 16px", cursor: "pointer", fontFamily: "inherit" }}>
                <span style={{ width: 16, height: 16, borderRadius: "50%", flexShrink: 0, marginTop: 1, border: `1.5px solid ${btype === t.id ? "#0e0e10" : "#c4c4ca"}`, background: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>{btype === t.id && <span style={{ width: 8, height: 8, borderRadius: "50%", background: "#0e0e10" }} />}</span>
                <span><span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>{t.label}</span><span style={{ display: "block", fontSize: 12, color: "#6b6b72", marginTop: 2, lineHeight: 1.45 }}>{t.desc}</span></span>
              </button>
            ))}
          </div>
        </div>

        {/* Bundle Name + Product Settings + Category — aligned field set */}
        <div style={{ marginBottom: 24, display: "grid", gridTemplateColumns: "repeat(2, minmax(0, 1fr))", gap: "24px 32px", alignItems: "start" }}>
          <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
            <label style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>Bundle Name</label>
            <input
              value={name}
              onChange={(e) => setName(e.target.value)}
              placeholder="Name this bundle"
              style={{
                height: 40, boxSizing: "border-box",
                border: "1px solid #d4d4d8", borderRadius: 6,
                padding: "0 14px", fontSize: 13.5, color: "#0e0e10", outline: 0,
                background: "#fff", fontFamily: "inherit"
              }} />
            <span style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.45 }}>Shown to shoppers wherever the bundle appears.</span>
          </div>

          <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
            <label style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>Product Settings</label>
            <div style={{ display: "inline-flex", alignItems: "center", gap: 3, height: 40, boxSizing: "border-box", padding: 3, background: "#f4f4f6", border: "1px solid #ececf0", borderRadius: 8, width: "fit-content" }}>
              {[
              { id: null, label: "None" },
              { id: "team-required", label: "Team Required" },
              { id: "team-supplied", label: "Team Supplied" }].
              map((opt) => {
                const on = setting === opt.id;
                return (
                  <button key={opt.label} type="button" onClick={() => setSetting(opt.id)} style={{
                    height: 32, padding: "0 12px", borderRadius: 6, border: 0, cursor: "pointer",
                    fontSize: 12.5, fontWeight: 600, fontFamily: "inherit", whiteSpace: "nowrap",
                    background: on ? "#fff" : "transparent",
                    color: on ? "#0e0e10" : "#6b6b72",
                    boxShadow: on ? "0 1px 2px rgba(20,20,30,.12)" : "none",
                    transition: "background .14s cubic-bezier(0.2,0,0,1), color .14s cubic-bezier(0.2,0,0,1)"
                  }}>{opt.label}</button>);
              })}
            </div>
            <span style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.45 }}>How the team buys this bundle — separate from its store category.</span>
          </div>

          {setting == null ?
          <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
            <label style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>Category <span style={{ color: "#e1251b" }}>*</span></label>
            <select value={cat} onChange={(e) => setCat(e.target.value)} style={{ height: 40, boxSizing: "border-box", border: "1px solid " + (cat ? "#d4d4d8" : "#e1251b"), borderRadius: 6, padding: "0 10px", fontSize: 13.5, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10", width: "100%" }}>
              <option value="">Select a category</option>
              {(window.__prodCats || []).map((c) => <option key={c.id} value={c.name}>{c.name}</option>)}
            </select>
            <span style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.45 }}>Required when no product setting is applied.</span>
          </div> :
          <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
            <label style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>Category</label>
            <div style={{ display: "inline-flex", alignItems: "center", gap: 8, height: 40, boxSizing: "border-box", padding: "0 12px", border: "1px dashed #d4d4d8", borderRadius: 6, background: "#fafafb", width: "fit-content" }}>
              <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>Team Gear</span>
              <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>set by product settings</span>
            </div>
            <span style={{ fontSize: 12, color: "#9a9aa1", lineHeight: 1.45 }}>Turn off the product setting to choose your own category.</span>
          </div>}
        </div>

        {/* MOQ warning — choice bundles splitting made-to-order demand */}
        {btype === "choice" && selectedProducts.length > 0 && selectedProducts.some((p) => pteRestricted(p)) && (
          <div style={{ display: "flex", gap: 10, alignItems: "flex-start", background: "#fff8ef", border: "1px solid #f2e2c9", borderRadius: 8, padding: "12px 16px", marginBottom: 24 }}>
            <span style={{ color: "#b06f1e", display: "inline-flex", marginTop: 1 }}><IconAlert size={15} /></span>
            <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>Splitting a required purchase across options may prevent individual options from reaching minimum order quantity.</span>
          </div>
        )}

        {/* Selected products row (only when items selected) */}
        {hasSelection &&
        <div style={{ marginBottom: 24 }}>
            <div style={{
            display: "flex", alignItems: "baseline", gap: 12, marginBottom: 12
          }}>
              <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10" }}>
                Selected Products
              </span>
              <span style={{ fontSize: 13, color: "#6b6b72" }}>
                ({selectedIds.length})
              </span>
            </div>
            <div style={{
            display: "grid",
            gridTemplateColumns: "repeat(auto-fill, minmax(220px, 1fr))",
            gap: 12
          }}>
              {selectedProducts.map((p) =>
            <SelectedChip
              key={p.id}
              product={p}
              store={store}
              onRemove={() => removeProduct(p.id)} />
            )}
            </div>
          </div>
        }

        {/* Add Products */}
        <div style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", marginBottom: 12 }}>
          Add Products
        </div>
        <div style={{ position: "relative", marginBottom: 16 }}>
          <span style={{
            position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)",
            color: "#9a9aa1", display: "inline-flex"
          }}>
            <IconSearch size={16} />
          </span>
          <input
            value={search}
            onChange={(e) => setSearch(e.target.value)}
            placeholder="Find products"
            style={{
              width: "100%", border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 16px 12px 40px", fontSize: 13, color: "#0e0e10", outline: 0
            }} />
        </div>

        {visibleQuick.length === 0 ?
        <div style={{
          padding: "32px 16px", textAlign: "center", color: "#6b6b72", fontSize: 13,
          border: "1px dashed #e5e5e9", borderRadius: 8
        }}>
            {draftProducts.length === 0 ?
          "Add draft products to your store before bundling." :
          "No products match your search."}
          </div> :

        <div style={{
          display: "grid",
          gridTemplateColumns: "repeat(auto-fill, minmax(240px, 1fr))",
          gap: 12
        }}>
            {visibleQuick.map((p) =>
          <PickerCard
            key={p.id}
            product={p}
            store={store}
            selected={selectedSet.has(p.id)}
            disabled={selectedSet.has(p.id)}
            onAdd={() => addProduct(p.id)} />
          )}
          </div>
        }

        {availableAll.length > 0 &&
        <button
          onClick={() => setShowModal(true)}
          style={{
            background: "transparent", border: 0, color: "#2563eb",
            fontSize: 13, fontWeight: 600, cursor: "pointer", padding: 0,
            marginTop: 16, display: "inline-flex", alignItems: "center", gap: 4
          }}>
            See All Products <IconChevronRight size={14} />
          </button>
        }

        <div style={{ marginTop: 28, paddingTop: 24, borderTop: "1px solid #ececf0" }}>
          <BundlePriceSettings
            items={selectedProducts}
            pricings={null}
            profit={bundleProfit}
            onChange={updateBundleProfit}
            bundleType={btype || "all"}
            supplied={setting === "team-supplied"}
            fundOn={fundOn}
            locked={false} />
          <BundleOptions
            hidden={hiddenFlag}
            onHidden={setHiddenFlag}
            hideRetail={hideRetail}
            onHideRetail={setHideRetail}
            freeShipping={freeShipping}
            onFreeShipping={setFreeShipping}
            locked={false} />
          <BundlePricingInsights
            items={selectedProducts}
            pricings={null}
            profit={bundleProfit}
            bundleType={btype || "all"}
            supplied={setting === "team-supplied"}
            freeShipping={freeShipping}
            fundOn={fundOn} />
        </div>
      </div>

      <SelectProductsModal
        open={showModal}
        store={store}
        products={draftProducts}
        initialSelectedIds={selectedIds}
        onClose={() => setShowModal(false)}
        onConfirm={(ids) => {setSelectedIds(ids);setShowModal(false);}} />
      
    </div>);

}

// ─────────── SelectProductsModal ─────────── (full product picker)
function SelectProductsModal({ open, store, products, initialSelectedIds, onClose, onConfirm }) {
  const [localIds, setLocalIds] = React.useState(initialSelectedIds || []);
  const [filter, setFilter] = React.useState("all");
  const [search, setSearch] = React.useState("");

  // Sync incoming selection whenever the modal is (re)opened.
  React.useEffect(() => {
    if (open) {
      setLocalIds(initialSelectedIds || []);
      setSearch("");
      setFilter("all");
    }
  }, [open]);

  const toggle = (id) => {
    setLocalIds((s) => s.includes(id) ? s.filter((x) => x !== id) : [...s, id]);
  };

  const q = search.trim().toLowerCase();
  const filtered = products.filter((p) => {
    if (q && !(p.title || "").toLowerCase().includes(q)) return false;
    if (filter === "team-required" && p.teamSetting !== "team-required") return false;
    if (filter === "team-supplied" && p.teamSetting !== "team-supplied") return false;
    return true;
  });

  const filterChips = [
  { id: "all", label: "All" },
  { id: "team-required", label: "Team Required" },
  { id: "team-supplied", label: "Team Supplied" }];


  return (
    <Modal open={open} onClose={onClose} width={780}>
      <div style={{ padding: "24px 24px 0" }}>
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          marginBottom: 16
        }}>
          <h3 style={{ fontSize: 18, fontWeight: 700, margin: 0 }}>Select Products</h3>
          <button onClick={onClose} style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72"
          }}>
            <IconX size={20} />
          </button>
        </div>

        {/* Search */}
        <div style={{ position: "relative", marginBottom: 16 }}>
          <span style={{
            position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)",
            color: "#9a9aa1", display: "inline-flex"
          }}>
            <IconSearch size={16} />
          </span>
          <input
            value={search}
            onChange={(e) => setSearch(e.target.value)}
            placeholder="Find products"
            style={{
              width: "100%", border: "1px solid #d4d4d8", borderRadius: 6,
              padding: "12px 16px 12px 40px", fontSize: 13, color: "#0e0e10", outline: 0
            }} />
        </div>

        {/* Filter chips */}
        <div style={{ display: "flex", gap: 8, marginBottom: 16 }}>
          {filterChips.map((c) => {
            const active = filter === c.id;
            return (
              <button key={c.id} onClick={() => setFilter(c.id)} style={{
                background: active ? "#0e0e10" : "#fff",
                color: active ? "#fff" : "#0e0e10",
                border: `1px solid ${active ? "#0e0e10" : "#d4d4d8"}`,
                padding: "8px 16px", borderRadius: 999,
                fontSize: 12, fontWeight: 600, cursor: "pointer", transition: "all .12s"
              }}>
                {c.label}
              </button>);

          })}
        </div>
      </div>

      {/* List */}
      <div style={{
        maxHeight: "55vh", overflowY: "auto", padding: "0 24px",
        display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12
      }}>
        {filtered.length === 0 ?
        <div style={{
          gridColumn: "1 / -1",
          padding: "48px 12px", textAlign: "center", color: "#6b6b72", fontSize: 13
        }}>
            No products match.
          </div> :

        filtered.map((p) => {
          const checked = localIds.includes(p.id);
          return (
            <div
              key={p.id}
              onClick={() => toggle(p.id)}
              style={{
                display: "flex", alignItems: "center", gap: 12,
                padding: "12px 12px",
                border: "1px solid #ececf0", borderRadius: 8,
                background: checked ? "#f6fbf8" : "#fff",
                borderColor: checked ? "#bfe5d0" : "#ececf0",
                cursor: "pointer", transition: "border-color .12s, background .12s"
              }}>
                <ProductMiniThumb product={p} store={store} size={44} />
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{
                  fontSize: 12, fontWeight: 700, color: "#0e0e10",
                  letterSpacing: ".04em", textTransform: "uppercase",
                  whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis"
                }}>{p.title || "Untitled"}</div>
                  <div style={{ marginTop: 2 }}>
                    <SettingPill teamSetting={p.teamSetting} small />
                  </div>
                </div>
                <span style={{
                width: 24, height: 24, borderRadius: "50%",
                border: `1.5px solid ${checked ? "#1f9d55" : "#d4d4d8"}`,
                background: checked ? "#1f9d55" : "#fff",
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                flexShrink: 0, transition: "all .12s"
              }}>
                  {checked && <IconCheck size={14} style={{ color: "#fff", strokeWidth: 3 }} />}
                </span>
              </div>);

        })
        }
      </div>

      {/* Footer */}
      <div style={{
        borderTop: "1px solid #ececf0", marginTop: 16,
        padding: "16px 24px",
        display: "flex", alignItems: "center", justifyContent: "space-between"
      }}>
        <div style={{ fontSize: 13, color: "#6b6b72" }}>
          {localIds.length} {localIds.length === 1 ? "product" : "products"} selected
        </div>
        <button
          onClick={() => onConfirm(localIds)}
          style={{
            background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
            padding: "12px 24px", fontSize: 13, fontWeight: 700, cursor: "pointer"
          }}>
          Add to Bundle
        </button>
      </div>
    </Modal>);

}

// ─────────── BundleThumbCollage ─────────── (2x2 mini grid shown on bundle cards)
function BundleThumbCollage({ items, store, slotSize = 90 }) {
  const slots = items.slice(0, 4);
  while (slots.length < 4) slots.push(null);
  return (
    <div style={{
      display: "grid", gridTemplateColumns: "1fr 1fr", gap: 12,
      width: "100%", maxWidth: slotSize * 2 + 10
    }}>
      {slots.map((it, i) =>
      <div key={i} style={{
        width: "100%", aspectRatio: "1 / 1",
        background: it ? "#fff" : "transparent",
        border: it ? "1px solid #ececf0" : "1px dashed #e5e5e9",
        borderRadius: 6,
        display: "flex", alignItems: "center", justifyContent: "center",
        padding: 8
      }}>
          {it ?
        <div style={{ width: "82%", height: "82%" }}>
              {it.thumbnail || <DefaultProductThumb store={store} product={it} />}
            </div> :

        null}
        </div>
      )}
    </div>);

}

// Helper used by ProductCard to compute price range.
function bundlePriceRange(items, size) {
  const vals = items.
  map((p) => parseFloat(p?.pricing?.[size]?.retail)).
  filter((v) => !isNaN(v));
  if (vals.length === 0) return "—";
  const min = Math.min(...vals);
  const max = Math.max(...vals);
  if (min === max) return `$${min.toFixed(2)}`;
  return `$${min.toFixed(2)} - $${max.toFixed(2)}`;
}

// ─────────── BundleToggle ─────────── (iOS-style switch)
function BundleToggle({ on, onChange }) {
  return (
    <span
      onClick={() => onChange(!on)}
      role="switch"
      aria-checked={on}
      style={{
        width: 44, height: 24, borderRadius: 999,
        background: on ? "#0e0e10" : "#d4d4d8",
        position: "relative", cursor: "pointer",
        transition: "background .15s", flexShrink: 0,
        display: "inline-block"
      }}>
      <span style={{
        position: "absolute", top: 2, left: on ? 22 : 2,
        width: 20, height: 20, borderRadius: "50%", background: "#fff",
        transition: "left .15s",
        boxShadow: "0 1px 3px rgba(0,0,0,.18)"
      }} />
    </span>);

}

// ─────────── BundleEditBody ─────────── (drawer body for editing a bundle)
function BundleEditBody({
  items, store, locked, fundOn, bundleType,
  bundleRequired, bundleSupplied,
  onBundleRequired, onBundleSupplied,
  bundleProfit, onBundleProfit,
  bundleHidden, onBundleHidden,
  hideRetail, onHideRetail,
  freeShipping, onFreeShipping,
  itemPricings, expandedId, onToggleExpanded,
  onUpdateItemPricing, onRemoveMember, onAddItems, onDeleteBundle
}) {
  return (
    <React.Fragment>
      {/* Bundle Settings */}
      <div style={{
        border: "1px solid #ececf0", borderRadius: 8, padding: 24, marginBottom: 16
      }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 16 }}>
          <span style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Bundle Settings</span>
          {locked && <BundleLockChip />}
        </div>
        <div style={locked ? { pointerEvents: "none", opacity: 0.5 } : undefined}>
        {[
        { id: "req", label: "Team Required", on: bundleRequired, set: onBundleRequired,
          tip: "Mandatory gear that rostered players must purchase to participate." },
        { id: "sup", label: "Team Supplied", on: bundleSupplied, set: onBundleSupplied,
          tip: "Mandatory participation gear that has already been paid for collectively (e.g. through sign-up fees or fundraisers). Players pay $0 at checkout." }].
        map((row) =>
        <div key={row.id} style={{
          display: "flex", alignItems: "center", gap: 12, padding: "8px 0"
        }}>
            <BundleToggle on={row.on} onChange={row.set} />
            <span style={{ fontSize: 14, color: "#0e0e10" }}>{row.label}</span>
            <InfoTooltip title={row.label} body={row.tip} />
          </div>
        )}
        </div>
      </div>

      <BundlePriceSettings
        items={items}
        pricings={itemPricings}
        profit={bundleProfit}
        onChange={onBundleProfit}
        bundleType={bundleType}
        supplied={bundleSupplied}
        fundOn={fundOn}
        locked={locked} />

      <BundleOptions
        hidden={bundleHidden}
        onHidden={onBundleHidden}
        hideRetail={hideRetail}
        onHideRetail={onHideRetail}
        freeShipping={freeShipping}
        onFreeShipping={onFreeShipping}
        locked={locked} />

      {/* Bundle members (accordion) — per-member pricing for items sold alone.
          Membership is editable until the bundle's first non-cancelled order (Epic J). */}
      <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 12 }}>
        <span style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Bundle Members</span>
        {locked && <BundleLockChip />}
        {onAddItems && !locked && (
          <button onClick={onAddItems} style={{ marginLeft: "auto", background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 14px", fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>+ Add items</button>
        )}
      </div>
      {items.length === 1 && (
        <div style={{ display: "flex", alignItems: "flex-start", gap: 10, background: "#fff8ef", border: "1px solid #f2e2c9", borderRadius: 8, padding: "12px 16px", marginBottom: 12, fontSize: 12.5, color: "#6b5314", lineHeight: 1.5 }}>
          <IconAlert size={15} style={{ color: "#b06f1e", flexShrink: 0, marginTop: 1 }} />
          <span><strong>Incomplete — needs a second item.</strong> The bundle is held off the storefront until it has at least 2 members; its name, type, pricing, and settings are all saved.{" "}
            {onDeleteBundle && <span role="button" onClick={onDeleteBundle} style={{ color: "#b81d14", fontWeight: 700, textDecoration: "underline", textUnderlineOffset: 2, cursor: "pointer" }}>Delete bundle instead</span>}
          </span>
        </div>
      )}
      <div style={{ display: "flex", flexDirection: "column", gap: 12, ...locked ? { pointerEvents: "none", opacity: 0.5 } : {} }}>
        {items.length === 0 &&
        <div style={{
          padding: "32px 16px", textAlign: "center", color: "#6b6b72", fontSize: 13,
          border: "1px dashed #e5e5e9", borderRadius: 8
        }}>
            No products in this bundle.
          </div>
        }
        {items.map((it) => {
          const expanded = expandedId === it.id;
          const pricing = itemPricings[it.id] || defaultPricing();
          const itemSetting = {
            "team-required": { label: "TEAM REQUIRED", color: "#e8590c" },
            "team-supplied": { label: "TEAM SUPPLIED", color: "#2944a8" },
            "fan-gear": { label: "FAN GEAR", color: "#1f7a44" }
          }[it.teamSetting] || { label: "FAN GEAR", color: "#1f7a44" };
          return (
            <div key={it.id} style={{
              border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden"
            }}>
              {/* Row header */}
              <button
                onClick={() => onToggleExpanded(it.id)}
                style={{
                  width: "100%", background: "#fff", border: 0, cursor: "pointer",
                  display: "flex", alignItems: "center", gap: 12, padding: "12px 16px",
                  textAlign: "left"
                }}>
                <ProductMiniThumb product={it} store={store} size={40} />
                <div style={{ flex: 1, minWidth: 0 }}>
                  <div style={{
                    fontSize: 13, fontWeight: 700, color: "#0e0e10",
                    whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis"
                  }}>{it.title || "[product-name]"}</div>
                  <span style={{
                    display: "inline-block", marginTop: 4,
                    background: "#fff4e6", color: itemSetting.color,
                    padding: "2px 8px", borderRadius: 3,
                    fontSize: 10, fontWeight: 800, letterSpacing: ".06em"
                  }}>{itemSetting.label}</span>
                </div>
                {onRemoveMember && !locked && (
                  <span role="button" title="Remove from bundle" onClick={(e) => { e.stopPropagation(); onRemoveMember(it.id); }} style={{ display: "inline-flex", color: "#b81d14", padding: 4, cursor: "pointer" }}><IconTrash size={14} /></span>
                )}
                <span style={{
                  display: "inline-flex", color: "#6b6b72",
                  transform: `rotate(${expanded ? 180 : 0}deg)`, transition: "transform .15s"
                }}>
                  <IconChevron size={18} />
                </span>
              </button>

              {/* Expanded pricing */}
              {expanded &&
              <div style={{ borderTop: "1px solid #ececf0", padding: 16, background: "#fafafb" }}>
                  <div style={{ display: "flex", justifyContent: "flex-end", marginBottom: 10 }}>
                    <PSModeControl mode={pricingMode(pricing)} onChange={(m2) => { if (m2 === pricingMode(pricing)) return; ["youth", "adult"].forEach((sz) => { onUpdateItemPricing(it.id, sz, "dealerUnit", m2); onUpdateItemPricing(it.id, sz, "dealer", ""); }); }} />
                  </div>
                  {["youth", "adult"].map((size, i) =>
                <div key={size} style={{
                  paddingTop: i > 0 ? 18 : 0,
                  borderTop: i > 0 ? "1px solid #ececf0" : 0,
                  marginTop: i > 0 ? 18 : 0
                }}>
                      <div style={{
                    display: "grid",
                    gridTemplateColumns: "56px 70px 1fr 70px",
                    gap: 12, fontSize: 10, fontWeight: 700, letterSpacing: ".10em",
                    color: "#9a9aa1", marginBottom: 8
                  }}>
                        <div>SIZE</div>
                        <div>BASE COST</div>
                        <div>DEALER PROFIT</div>
                        <div>RETAIL</div>
                      </div>
                      <div style={{
                    display: "grid",
                    gridTemplateColumns: "56px 70px 1fr 70px",
                    gap: 12, alignItems: "center"
                  }}>
                        <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{size.toUpperCase()}</div>
                        <div style={{ fontSize: 12, color: "#6b6b72" }}>${pricing[size].base}</div>
                        <ProfitInput
                      unit={pricing[size].dealerUnit}
                      value={pricing[size].dealer}
                      onUnit={(u) => onUpdateItemPricing(it.id, size, "dealerUnit", u)}
                      onValue={(v) => onUpdateItemPricing(it.id, size, "dealer", v)} />
                        <div style={{
                      border: "1px solid #d4d4d8", borderRadius: 4, background: "#fff",
                      padding: "8px 8px", fontSize: 12, fontWeight: 600, color: "#0e0e10",
                      textAlign: "center"
                    }}>${pricing[size].retail}</div>
                      </div>
                    </div>
                )}
                  <div style={{
                  marginTop: 16, paddingTop: 12, borderTop: "1px solid #ececf0",
                  display: "flex", justifyContent: "space-between", alignItems: "center",
                  fontSize: 11, color: "#6b6b72"
                }}>
                    <span style={{ fontStyle: "italic" }}>Retail price visible to customers</span>
                    <span style={{ display: "inline-flex", gap: 16 }}>
                      <span><span style={{ fontWeight: 700, color: "#9a9aa1", marginRight: 8 }}>YOUTH</span><span style={{ fontWeight: 700, color: "#0e0e10" }}>${pricing.youth.retail}</span></span>
                      <span><span style={{ fontWeight: 700, color: "#9a9aa1", marginRight: 8 }}>ADULT</span><span style={{ fontWeight: 700, color: "#0e0e10" }}>${pricing.adult.retail}</span></span>
                    </span>
                  </div>
                </div>
              }
            </div>);

        })}
      </div>

      <div style={{ marginTop: 16 }}>
        <BundlePricingInsights
          items={items}
          pricings={itemPricings}
          profit={bundleProfit}
          bundleType={bundleType}
          supplied={bundleSupplied}
          freeShipping={freeShipping}
          fundOn={fundOn} />
      </div>
    </React.Fragment>);

}

Object.assign(window, {
  CreateBundleSection, SelectProductsModal,
  BundleThumbCollage, bundlePriceRange, BundleStackIcon,
  BundleEditBody, BundleToggle, BulkPricingModal
});

// ─────────── BulkPricingModal ─────────── (eligibility: zero non-cancelled orders)
// Two mutually-exclusive modes:
//   • "all"      — apply the entered Dealer Profit / Fundraising to every draft product
//   • "selected" — reveal a product picker (mirrors Create Bundle) so the user
//                   chooses which drafts to re-price
// onApply(targetIds, { dealer, fund }) is called with the resolved id list.
function CircleDollarIcon({ size = 18, color = "#9a9aa1" }) {
  return (
    <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke={color} strokeWidth="1.7" strokeLinecap="round" strokeLinejoin="round">
      <circle cx="12" cy="12" r="9" />
      <path d="M14.5 9.5a2.2 2.2 0 0 0-2-1.5h-1a1.8 1.8 0 0 0 0 3.6h1.2a1.8 1.8 0 0 1 0 3.6H11.5a2.2 2.2 0 0 1-2-1.5" />
      <path d="M12 6.5v1.5M12 16v1.5" />
    </svg>);

}

function BulkInfoDot({ text }) {
  return (
    <span title={text} style={{
      width: 16, height: 16, borderRadius: "50%", border: "1.5px solid #c4c4cb",
      color: "#9a9aa1", fontSize: 10, fontWeight: 700, fontStyle: "italic",
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      cursor: "help", flexShrink: 0
    }}>i</span>);

}

// Formats a clean numeric string into a thousands-separated, 2-decimal display
// value (e.g. "10005" -> "10,005.00"). Returns "" for empty/invalid input.
function formatMoneyDisplay(raw) {
  if (raw === "" || raw == null) return "";
  const n = parseFloat(raw);
  if (isNaN(n)) return "";
  return n.toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 });
}

function BulkProfitField({ placeholder, value, onChange, unit }) {
  const [focused, setFocused] = React.useState(false);
  // While editing, show the raw numeric value for easy typing; once focus is
  // lost, show the formatted value. The stored `value` stays comma-free so the
  // downstream parseFloat() pricing math keeps working.
  const display = focused ? value : formatMoneyDisplay(value);
  return (
    <div style={{
      display: "flex", alignItems: "center", gap: 8,
      border: "1px solid #d4d4d8", borderRadius: 8, padding: "0 16px",
      height: 56, background: "#fff"
    }}>
      <span style={{
        fontSize: 15, color: "#9a9aa1", fontWeight: 600, flexShrink: 0,
        lineHeight: 1
      }}>{unit === "%" ? "%" : "$"}</span>
      <input
        type="text"
        inputMode="decimal"
        value={display}
        onChange={(e) => {
          // Numeric enforcement: digits + a single decimal point, max 2 dp.
          let v = e.target.value.replace(/[^0-9.]/g, "");
          const parts = v.split(".");
          if (parts.length > 2) v = parts[0] + "." + parts.slice(1).join("");
          const [int, dec] = v.split(".");
          if (dec !== undefined) v = int + "." + dec.slice(0, 2);
          onChange(v);
        }}
        onFocus={() => setFocused(true)}
        onBlur={() => setFocused(false)}
        placeholder={placeholder}
        style={{
          flex: 1, border: 0, outline: 0, background: "transparent",
          fontSize: 15, color: "#0e0e10", fontFamily: "inherit"
        }} />
    </div>);

}

function BulkPricingModal({ open, store, draftProducts, onClose, onApply }) {
  const [mode, setMode] = React.useState("all"); // "all" | "selected"
  const [dealer, setDealer] = React.useState("");
  const [fund, setFund] = React.useState("");
  const [selectedIds, setSelectedIds] = React.useState([]);
  const [search, setSearch] = React.useState("");
  const [showPicker, setShowPicker] = React.useState(false);
  const [punit, setPunit] = React.useState("$"); // one $ / % mode for both inputs (Governing rule 3)

  React.useEffect(() => {
    if (open) {
      setMode("all"); setDealer(""); setFund(""); setSelectedIds([]); setSearch(""); setShowPicker(false); setPunit("$");
    }
  }, [open]);

  const pool = (draftProducts || []).filter((p) => !p.isBundle);
  // Eligible = zero non-cancelled orders; Vault / catalog items are always
  // eligible. Locked products are excluded from "apply to all" and render
  // disabled in the picker.
  const eligible = pool.filter((p) => pteLockState(p, store) !== "full");
  const lockedCount = pool.length - eligible.length;
  const selectedSet = new Set(selectedIds);
  const selectedProducts = selectedIds.map((id) => eligible.find((p) => p.id === id)).filter(Boolean);

  const q = search.trim().toLowerCase();
  const availableAll = pool.filter((p) => !q || (p.title || "").toLowerCase().includes(q));
  const QUICK = 8;
  const visibleQuick = availableAll.slice(0, QUICK);

  const addProduct = (id) => {if (!selectedSet.has(id)) setSelectedIds((s) => [...s, id]);};
  const removeProduct = (id) => setSelectedIds((s) => s.filter((x) => x !== id));

  const hasProfit = dealer.trim() !== "" || fund.trim() !== "";
  const targetIds = mode === "all" ? eligible.map((p) => p.id) : selectedIds.filter((id) => eligible.some((p) => p.id === id));
  const canApply = hasProfit && targetIds.length > 0;

  const setModeExclusive = (m) => setMode((cur) => cur === m ? (m === "all" ? "selected" : "all") : m);

  const wide = mode === "selected";

  const handleApply = () => {
    if (!canApply) return;
    onApply(targetIds, { dealer: dealer.trim(), fund: fund.trim(), mode: punit });
  };

  return (
    <Modal open={open} onClose={onClose} width={wide ? 940 : 560}>
      <div style={{ padding: "28px 32px 32px" }}>
        {/* Header */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 28 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
            <h2 style={{ fontSize: 24, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Bulk Pricing</h2>
            <PSModeControl mode={punit} onChange={(m2) => { if (m2 !== punit) { setPunit(m2); setDealer(""); setFund(""); } }} />
          </div>
          <button onClick={onClose} aria-label="Close" style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#0e0e10", display: "inline-flex"
          }}>
            <IconX size={22} style={{ strokeWidth: 2 }} />
          </button>
        </div>

        {/* Toggles */}
        <div style={{ display: "flex", flexDirection: "column", gap: 16, marginBottom: 24 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
            <BundleToggle on={mode === "all"} onChange={() => setModeExclusive("all")} />
            <span style={{ fontSize: 16, color: "#0e0e10" }}>Apply pricing to all products</span>
            <BulkInfoDot text="Sets the entered Dealer Profit and Fundraising on every eligible product — items with orders are locked and excluded. Vault items are always eligible." />
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
            <BundleToggle on={mode === "selected"} onChange={() => setModeExclusive("selected")} />
            <span style={{ fontSize: 16, color: "#0e0e10" }}>Apply pricing to selected products</span>
            <BulkInfoDot text="Choose specific products to re-price below. Locked items (with orders) can't be selected." />
          </div>
          {lockedCount > 0 && (
            <div style={{ display: "flex", alignItems: "center", gap: 8, fontSize: 12.5, color: "#9a9aa1" }}>
              <IconLock size={13} />
              {lockedCount} product{lockedCount === 1 ? "" : "s"} with orders {lockedCount === 1 ? "is" : "are"} locked and excluded from bulk pricing.
            </div>
          )}
        </div>

        {/* Profit fields */}
        <div style={{ display: "flex", flexDirection: "column", gap: 16, marginBottom: 24 }}>
          <BulkProfitField unit={punit} placeholder="Dealer Profit" value={dealer} onChange={setDealer} />
          {store?.fundraisingEnabled === true && <BulkProfitField unit={punit} placeholder="Fundraising" value={fund} onChange={setFund} />}
        </div>

        {/* Selected-products picker (only in "selected" mode) */}
        {mode === "selected" &&
        <div style={{ marginBottom: 24, borderTop: "1px solid #ececf0", paddingTop: 24 }}>
            {selectedProducts.length > 0 &&
          <div style={{ marginBottom: 24 }}>
                <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 12 }}>
                  Selected Products <span style={{ color: "#6b6b72", fontWeight: 600 }}>({selectedProducts.length})</span>
                </div>
                <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(220px, 1fr))", gap: 12 }}>
                  {selectedProducts.map((p) =>
              <SelectedChip key={p.id} product={p} store={store} onRemove={() => removeProduct(p.id)} />
              )}
                </div>
              </div>
          }

            <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", marginBottom: 12 }}>Add Products</div>
            <div style={{ position: "relative", marginBottom: 16 }}>
              <span style={{ position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)", color: "#9a9aa1", display: "inline-flex" }}>
                <IconSearch size={16} />
              </span>
              <input
              value={search}
              onChange={(e) => setSearch(e.target.value)}
              placeholder="Find products"
              style={{ width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 16px 12px 40px", fontSize: 13, color: "#0e0e10", outline: 0 }} />
            </div>

            {visibleQuick.length === 0 ?
          <div style={{ padding: "32px 16px", textAlign: "center", color: "#6b6b72", fontSize: 13, border: "1px dashed #e5e5e9", borderRadius: 8 }}>
                {pool.length === 0 ? "Add draft products before bulk pricing." : "No products match your search."}
              </div> :

          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(240px, 1fr))", gap: 12 }}>
                {visibleQuick.map((p) => {
              const locked = pteLockState(p, store) === "full";
              return (
            <PickerCard
              key={p.id}
              product={p}
              store={store}
              selected={selectedSet.has(p.id)}
              disabled={selectedSet.has(p.id) || locked}
              lockNote={locked ? PTE_LOCK_TIPS.full : null}
              onAdd={() => addProduct(p.id)} />
              );
            })}
              </div>
          }

            {availableAll.length > QUICK &&
          <button onClick={() => setShowPicker(true)} style={{
            background: "transparent", border: 0, color: "#2563eb", fontSize: 13, fontWeight: 600,
            cursor: "pointer", padding: 0, marginTop: 16, display: "inline-flex", alignItems: "center", gap: 4
          }}>
                See All Products <IconChevronRight size={14} />
              </button>
          }
          </div>
        }

        {/* Set Pricing */}
        <button onClick={handleApply} disabled={!canApply} style={{
          width: "100%", background: canApply ? "#0e0e10" : "#cfcfd4", color: "#fff",
          border: 0, borderRadius: 8, padding: "16px", fontSize: 16, fontWeight: 700,
          cursor: canApply ? "pointer" : "not-allowed", fontFamily: "inherit"
        }}>
          Set Pricing{mode === "selected" && selectedIds.length > 0 ? ` (${selectedIds.length})` : ""}
        </button>
      </div>

      <SelectProductsModal
        open={showPicker}
        store={store}
        products={eligible}
        initialSelectedIds={selectedIds}
        onClose={() => setShowPicker(false)}
        onConfirm={(ids) => {setSelectedIds(ids);setShowPicker(false);}} />
    </Modal>);

}


/* ===== ASSET 22 : 42f7e1ad-1326-42c5-a8fc-fcf4b8ce0245 ===== */
// Orders tab — sub-tabs (My Orders / Pending / Players / Fan), tables, and right-side detail drawer.

// ─────────── Status badge ───────────
const STATUS_STYLES = {
  "artwork-production": { label: "Artwork Production", bg: "#fff1e6", fg: "#c2410c", border: "#fdd6b4" },
  "order-confirmed": { label: "Order Confirmed", bg: "#eef3fb", fg: "#1e40af", border: "#dbe7f8" },
  "factory-production": { label: "Factory Production", bg: "#f3eefb", fg: "#6d28d9", border: "#e6dcf6" },
  "shipped": { label: "Shipped", bg: "#e7f0fc", fg: "#1d4ed8", border: "#cfddf9" },
  "delivered": { label: "Delivered", bg: "#e7f5ed", fg: "#1f7a3d", border: "#bfe6cf" },
  "on-hold": { label: "On Hold", bg: "#e1251b", fg: "#fff", border: "#e1251b" },
  "submitted": { label: "Submitted", bg: "#eef3fb", fg: "#1e40af", border: "#dbe7f8" }
};

// Human-readable definition for each order status, shown in the Status info modal.
// Ordered to follow the natural order lifecycle.
const STATUS_DEFINITIONS = [
  { key: "submitted", desc: "The order has been placed and submitted into the production queue." },
  { key: "order-confirmed", desc: "Payment cleared and the order is confirmed — queued for artwork." },
  { key: "artwork-production", desc: "Logos and artwork are being prepared and proofed before manufacturing." },
  { key: "factory-production", desc: "Items are actively being manufactured at the factory." },
  { key: "shipped", desc: "The order has left the factory. A tracking number is available." },
  { key: "delivered", desc: "The order has been delivered to the recipient." },
  { key: "on-hold", desc: "The order is paused and needs attention — a payment, approval, or production issue must be resolved." }
];

function StatusBadge({ status, tracking }) {
  const s = STATUS_STYLES[status] || STATUS_STYLES["order-confirmed"];
  return (
    <div style={{ display: "inline-flex", flexDirection: "column", alignItems: "flex-start", gap: 4 }}>
      <span style={{
        display: "inline-flex", alignItems: "center",
        background: s.bg, color: s.fg, border: `1px solid ${s.border}`,
        borderRadius: 999, padding: "4px 12px", fontSize: 12, fontWeight: 600,
        whiteSpace: "nowrap"
      }}>{s.label}</span>
      {tracking &&
      <a href="#" onClick={(e) => e.preventDefault()} style={{ fontSize: 12, color: "#2563eb", textDecoration: "none" }}>{tracking}</a>
      }
    </div>);

}

function DueDateCell({ date, status }) {
  // overdue if status is artwork-production AND we're past date — pretend
  const overdue = status === "artwork-production";
  const ok = status === "delivered" || status === "shipped";
  const color = overdue ? "#e1251b" : ok ? "#1f7a3d" : "#0e0e10";
  return (
    <div style={{ display: "inline-flex", alignItems: "center", gap: 8, color, fontSize: 13, fontWeight: 500, whiteSpace: "nowrap" }}>
      {date}
      {overdue &&
      <span title="Past due" style={{ width: 16, height: 16, borderRadius: "50%", background: "#e1251b", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 11 }}>!</span>
      }
      {ok &&
      <span title="On schedule" style={{ width: 16, height: 16, borderRadius: "50%", background: "#1f9d55", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 10 }}>
          <IconCheck size={11} style={{ strokeWidth: 3 }} />
        </span>
      }
    </div>);

}

// ─────────── Tiny product thumbnails ───────────
// Abstract jersey/shorts/backpack silhouettes — colored from store palette.
function JerseyMini({ colors, label, variant = "front" }) {
  const c1 = colors?.[0] || "#1d3a8a";
  const c2 = colors?.[1] || "#0e0e10";
  const c3 = colors?.[2] || "#ffffff";
  const trim = c1.toLowerCase() === "#ffffff" || c1.toLowerCase() === "#fff" ? c2 : c1;
  return (
    <svg width="56" height="56" viewBox="0 0 56 56" aria-hidden="true">
      {/* jersey silhouette */}
      <path d="M16 12 L22 8 L34 8 L40 12 L46 18 L42 22 L40 18 L40 46 L16 46 L16 18 L14 22 L10 18 Z"
      fill={c1.toLowerCase() === "#ffffff" || c1.toLowerCase() === "#fff" ? "#f1f1f3" : c1}
      stroke={trim} strokeWidth="1.2" strokeLinejoin="round" />
      {/* neckline */}
      <path d={variant === "back" ?
      "M22 8 Q28 12 34 8 L34 11 Q28 13 22 11 Z" :
      "M22 8 Q28 14 34 8 L34 11 Q28 18 22 11 Z"}
      fill={c3} stroke={trim} strokeWidth="1" />
      {/* shoulder stripes */}
      <path d="M16 13 L22 11 L22 14 L16 16 Z" fill={c2} opacity=".85" />
      <path d="M40 13 L34 11 L34 14 L40 16 Z" fill={c2} opacity=".85" />
      {/* label (number or "BEARS") */}
      <text x="28" y={variant === "back" ? 34 : 30} textAnchor="middle"
      fontFamily="JetBrains Mono, monospace" fontWeight="800"
      fontSize={label && label.length <= 2 ? 14 : 8}
      fill={c3}>{label || ""}</text>
    </svg>);

}

function ShortsMini({ colors }) {
  const c1 = colors?.[0] || "#1d3a8a";
  const c2 = colors?.[1] || "#0e0e10";
  const fill = c1.toLowerCase() === "#ffffff" || c1.toLowerCase() === "#fff" ? "#f1f1f3" : c1;
  return (
    <svg width="56" height="56" viewBox="0 0 56 56" aria-hidden="true">
      <path d="M12 16 L44 16 L42 44 L31 44 L28 26 L25 44 L14 44 Z" fill={fill} stroke={c2} strokeWidth="1.2" strokeLinejoin="round" />
      <path d="M14 18 L42 18" stroke={c2} strokeWidth=".8" opacity=".4" />
      <path d="M40 24 L42 40" stroke={c2} strokeWidth="2" opacity=".6" />
    </svg>);

}

function BackpackMini({ colors }) {
  const c1 = colors?.[0] || "#1d3a8a";
  const c2 = colors?.[1] || "#0e0e10";
  return (
    <svg width="56" height="56" viewBox="0 0 56 56" aria-hidden="true">
      <path d="M18 12 Q18 6 28 6 Q38 6 38 12" fill="none" stroke={c2} strokeWidth="1.6" />
      <rect x="12" y="14" width="32" height="34" rx="4" fill={c1} stroke={c2} strokeWidth="1.2" />
      <rect x="18" y="26" width="20" height="14" rx="2" fill="#f1f1f3" stroke={c2} strokeWidth=".8" />
      <rect x="22" y="30" width="12" height="2" rx="1" fill={c2} opacity=".4" />
    </svg>);

}

// Real uniform photos, grouped by garment type, mapped deterministically so a
// given product always shows the same thumbnail.
const ORDER_JERSEY_PHOTOS = [
"assets/products/jersey-grizzlies.png",
"assets/products/jersey-lancers.png",
"assets/products/jersey-padres.png",
"assets/products/jersey-warriors.png",
"assets/products/jersey-asteroid.png"];

const ORDER_PANTS_PHOTOS = ["assets/products/pants-navy.png", "assets/products/pants-orange.png"];
const ORDER_SHORTS_PHOTOS = ["assets/products/shorts-green.png", "assets/products/shorts-navy.png"];

function orderPhotoFor(product) {
  const kind = product?.kind;
  const title = (product?.title || "").toLowerCase();
  let pool;
  if (kind === "shorts") {
    pool = title.includes("pant") ? ORDER_PANTS_PHOTOS : ORDER_SHORTS_PHOTOS;
  } else {
    // jersey, backpack, and anything else fall back to the jersey set
    pool = ORDER_JERSEY_PHOTOS;
  }
  const key = String(product?.title || product?.thumbLabel || "uniform");
  let h = 0;
  for (let i = 0; i < key.length; i++) h = h * 31 + key.charCodeAt(i) >>> 0;
  return pool[h % pool.length];
}

function OrderThumb({ product, size = "row" }) {
  const big = size === "big";
  const w = big ? 180 : 50;
  const h = big ? 240 : 66;
  return (
    <div style={{
      width: w, height: h, background: "#f1f2f4", borderRadius: big ? 8 : 6,
      display: "flex", alignItems: "center", justifyContent: "center",
      flexShrink: 0, overflow: "hidden", padding: big ? 12 : 4
    }}>
      <img
        src={orderPhotoFor(product)}
        alt={product?.title || "Uniform"}
        style={{ width: "100%", height: "100%", objectFit: "contain", display: "block" }} />
      
    </div>);

}

function ProductThumbPair({ product, store, kind, variant }) {
  const merged = { ...(product || {}), kind: product?.kind || kind };
  return <OrderThumb product={merged} />;
}

function ProductCell({ product, store }) {
  return (
    <div style={{ display: "flex", alignItems: "center", gap: 16, minWidth: 0 }}>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 4, flexShrink: 0 }}>
        <ProductThumbPair product={product} store={store} kind={product.kind} />
        <div style={{ fontSize: 9, color: "#9a9aa1", fontWeight: 500, letterSpacing: ".02em", textAlign: "center", lineHeight: 1.2, maxWidth: 124 }}>
          {product.title?.toUpperCase()}
        </div>
      </div>
    </div>);

}

// ─────────── Sub-tab pill row ───────────
// Clean segmented control — gray track, raised white active segment (inspo: dashboard-inspo-1).
function SegmentedControl({ tabs, value, onChange, anchor }) {
  return (
    <div style={{ display: "inline-flex", alignItems: "center", alignSelf: "flex-start", gap: 2, background: "#f4f4f5", border: "1px solid #e4e4e7", borderRadius: 10, padding: 3 }} data-comment-anchor={anchor}>
      {tabs.map((t) => {
        const active = value === t.id;
        return (
          <button key={t.id} onClick={() => onChange(t.id)} style={{
            display: "inline-flex", alignItems: "center", gap: 8, height: 32, boxSizing: "border-box",
            padding: "0 20px", borderRadius: 7, border: 0, cursor: "pointer", fontFamily: "inherit",
            background: active ? "#fff" : "transparent",
            color: active ? "#0e0e10" : "#6b6b72",
            fontSize: 13, fontWeight: 600, whiteSpace: "nowrap",
            boxShadow: active ? "0 1px 3px rgba(20,20,30,.12), 0 0 0 1px #e4e4e7" : "none",
            transition: "background .12s, color .12s, box-shadow .12s"
          }}
          onMouseEnter={(e) => { if (!active) e.currentTarget.style.color = "#0e0e10"; }}
          onMouseLeave={(e) => { if (!active) e.currentTarget.style.color = "#6b6b72"; }}>
            {t.label}
            {t.count != null && <span style={{ fontSize: 11.5, fontWeight: 700, fontVariantNumeric: "tabular-nums", borderRadius: 5, padding: "2px 6px", background: active ? "#f4f4f5" : "#e9e9ec", color: active ? "#52525b" : "#8e8e96" }}>{t.count}</span>}
          </button>);
      })}
    </div>);
}

// Shared sub-nav chip row — single source of truth for the Products / Orders /
// Settings tab pills. tabs: [{ id, label, Icon?, count? }]
function SubNavChips({ tabs, value, onChange, anchor }) {
  return (
    <div style={{ display: "flex", gap: 12 }} data-comment-anchor={anchor}>
      {tabs.map((t) => {
        const active = value === t.id;
        const pillBg = active ? "#0e0e10" : "#fff";
        return (
          <button key={t.id} onClick={() => onChange(t.id)} style={{
            background: pillBg,
            color: active ? "#fff" : "#0e0e10",
            border: `1px solid ${active ? "#0e0e10" : "#d4d4d8"}`,
            padding: "8px 24px", borderRadius: 999,
            fontSize: 13, fontWeight: 600, cursor: "pointer",
            display: "inline-flex", alignItems: "center", gap: 8,
            whiteSpace: "nowrap", transition: "all .12s", fontFamily: "inherit"
          }}>
            {t.Icon && <t.Icon size={16} color={active ? "#fff" : "#0e0e10"} bg={pillBg} />}
            {t.label}{t.count != null && <CountBadge n={t.count} active={active} />}
          </button>);
      })}
    </div>);
}

// Small count badge (gray circle) used in sub-tab pills.
function CountBadge({ n, active }) {
  return (
    <span style={{
      minWidth: 22, height: 22, borderRadius: 999, padding: "0 7px", boxSizing: "border-box",
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      background: active ? "rgba(255,255,255,.22)" : "#ececf0",
      color: active ? "#fff" : "#52525a",
      fontSize: 12, fontWeight: 700, lineHeight: 1,
    }}>{n}</span>
  );
}

function OrderSubTabs({ value, onChange, pendingCount }) {
  const tabs = [
  { id: "all", label: "All Orders" },
  { id: "pending", label: "Pending Orders", count: pendingCount }];

  return (
    <SegmentedControl anchor="e9ed887639-div-181-5" tabs={tabs} value={value} onChange={onChange} />);

}

function OrdersSearchBar({ value, onChange }) {
  return (
    <div style={{ display: "flex", gap: 12, alignItems: "center" }}>
      <div style={{ position: "relative", width: 360 }}>
        <span style={{ position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)", color: "#9a9aa1" }}>
          <IconSearch size={16} />
        </span>
        <input
          type="text"
          placeholder="Search by name, email, or order number"
          value={value || ""}
          onChange={(e) => onChange(e.target.value)}
          style={{
            width: "100%", height: 40,
            border: "1px solid #d4d4d8", borderRadius: 6,
            padding: "0 12px 0 36px", fontSize: 13, outline: 0,
            background: "#fff"
          }} />
        
      </div>
      <button title="Filter & sort" style={{
        width: 40, height: 40, borderRadius: 6, background: "#fff",
        border: "1px solid #d4d4d8", display: "inline-flex", alignItems: "center",
        justifyContent: "center", cursor: "pointer", color: "#0e0e10"
      }}
      onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
      onMouseLeave={(e) => e.currentTarget.style.background = "#fff"}>
        <IconSliders size={18} />
      </button>
    </div>);

}

// ─────────── All Orders top-level controls (Sort / Filter / Date / Search) ───────────
// Dropdown control styled as a text pill with a trailing chevron. `filled`
// gives it the light-gray grouped background (leftmost control in the design).
function ControlDropdown({ label, value, options, onChange, filled }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = (e) => {if (ref.current && !ref.current.contains(e.target)) setOpen(false);};
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);

  const current = options.find((o) => o.id === value) || options[0];

  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button
        onClick={() => setOpen((o) => !o)}
        style={{
          display: "inline-flex", alignItems: "center", gap: 8,
          background: open ? "#fafafb" : "#fff",
          border: `1px solid ${open ? "#c9c9d0" : "#d8d8de"}`, borderRadius: 8,
          padding: "12px 16px",
          fontSize: 13.5, fontWeight: 600, color: "#0e0e10",
          cursor: "pointer", whiteSpace: "nowrap"
        }}>
        {label === "Sort By" ? <IconSort size={16} /> : <IconFilter size={16} />}
        <span>{label}: <span style={{ color: value && value !== "none" ? "#0e0e10" : "#71717a" }}>{current.label}</span></span>
      </button>
      {open && (
      <div style={{
        position: "absolute", top: "calc(100% + 8px)", left: 0, zIndex: 40,
        background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10,
        boxShadow: "0 10px 30px rgba(0,0,0,.12)", padding: 8, minWidth: 192
      }}>
        {options.map((o) => {
          const active = o.id === value;
          return (
            <button key={o.id} onClick={() => {onChange(o.id);setOpen(false);}} style={{
              display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12,
              width: "100%", textAlign: "left", background: active ? "#f4f4f5" : "transparent",
              border: "none", borderRadius: 7, padding: "8px 12px", fontSize: 13,
              fontWeight: active ? 600 : 500, color: "#18181b", cursor: "pointer"
            }}
            onMouseEnter={(e) => {if (!active) e.currentTarget.style.background = "#fafafa";}}
            onMouseLeave={(e) => {if (!active) e.currentTarget.style.background = "transparent";}}>
              {o.label}
              {active && <IconCheck size={15} style={{ color: "#18181b" }} />}
            </button>);

        })}
      </div>)}

    </div>);

}

// Start/End Date control — text pill that opens a Start / End / Due-Ship date
// popover (matches the Figma date dropdown), with a Clear Selection action.
function DateRangeControl({ range, onChange }) {
  const [open, setOpen] = React.useState(false);
  const [alignRight, setAlignRight] = React.useState(false);
  const ref = React.useRef(null);
  const POPOVER_W = 320;
  React.useEffect(() => {
    if (!open) return;
    // Keep the popover on-screen: if left-aligned it would spill past the
    // viewport's right edge, anchor it to the control's right edge instead.
    if (ref.current) {
      const rect = ref.current.getBoundingClientRect();
      setAlignRight(rect.left + POPOVER_W > window.innerWidth - 12);
    }
    const onDoc = (e) => {if (ref.current && !ref.current.contains(e.target)) setOpen(false);};
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);

  const { start = "", end = "", due = "" } = range || {};
  const hasRange = start || end || due;
  const label = start || end ?
  `${start ? frShortDate(new Date(start)) : "Any"} – ${end ? frShortDate(new Date(end)) : "Any"}` :
  due ? `Due ${frShortDate(new Date(due))}` : "Start/End Date";
  const field = (lbl, key) =>
  <label style={{ display: "block", minWidth: 0, fontSize: 12, fontWeight: 600, color: "#52525b" }}>
      {lbl}
      <input type="date" value={range[key] || ""} onChange={(e) => onChange({ ...range, [key]: e.target.value })}
      style={{ display: "block", width: "100%", minWidth: 0, boxSizing: "border-box", marginTop: 4, height: 36, border: "1px solid #d4d4d8", borderRadius: 7, padding: "0 10px", fontSize: 13, fontFamily: "inherit", color: "#18181b", background: "#fff", outline: 0 }} />
    </label>;


  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button
        onClick={() => setOpen((o) => !o)}
        style={{
          display: "inline-flex", alignItems: "center", gap: 8,
          background: open ? "#fafafb" : "#fff",
          border: `1px solid ${open ? "#c9c9d0" : "#d8d8de"}`, borderRadius: 8,
          padding: "12px 16px", fontSize: 13.5, fontWeight: 600,
          color: "#0e0e10", cursor: "pointer", whiteSpace: "nowrap"
        }}>
        <IconCalendar size={16} />
        <span style={{ color: hasRange ? "#0e0e10" : "#71717a" }}>{label}</span>
      </button>
      {open && (
      <div style={{
        position: "absolute", top: "calc(100% + 8px)", zIndex: 40,
        ...(alignRight ? { right: 0 } : { left: 0 }),
        background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10,
        boxShadow: "0 10px 30px rgba(0,0,0,.12)", padding: 16, width: POPOVER_W
      }}>
        <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
          <div style={{ display: "flex", gap: 12 }}>
            <div style={{ flex: 1, minWidth: 0 }}>{field("Start Date", "start")}</div>
            <div style={{ flex: 1, minWidth: 0 }}>{field("End Date", "end")}</div>
          </div>
          {field("Due/Ship Date", "due")}
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: 2 }}>
            <button onClick={() => onChange({ start: "", end: "", due: "" })} style={{
              background: "transparent", border: "none", color: "#71717a", fontSize: 12, fontWeight: 600,
              cursor: "pointer", padding: 0
            }}>Clear Selection</button>
            <button onClick={() => setOpen(false)} style={{
              background: "#18181b", border: "none", color: "#fff", fontSize: 12, fontWeight: 600,
              cursor: "pointer", padding: "8px 16px", borderRadius: 999
            }}>Apply</button>
          </div>
        </div>
      </div>)}

    </div>);

}

// Verbatim Figma option lists ───────────────────────────────────────────────
const ALL_ORDERS_SORT_OPTIONS = [
{ id: "none", label: "None" },
{ id: "organization", label: "Organization" },
{ id: "factoryOrderNum", label: "Factory Order #" },
{ id: "webOrderNum", label: "Web Order #" },
{ id: "cartName", label: "Cart Name" },
{ id: "product", label: "Product" },
{ id: "shipTo", label: "Ship To" },
{ id: "rep", label: "Rep" },
{ id: "sport", label: "Sport" },
{ id: "quantity", label: "Quantity" },
{ id: "shippingGroup", label: "Shipping Group" },
{ id: "dueDate", label: "Due/Ship Date" },
{ id: "tempShipDate", label: "Temporary Ship Date" },
{ id: "status", label: "Status" }];


const ALL_ORDERS_FILTER_OPTIONS = [
{ id: "none", label: "None" },
{ id: "rework", label: "Rework Needed" },
{ id: "submitted", label: "Submitted" },
{ id: "order-confirmed", label: "Order Confirmed" },
{ id: "artwork-production", label: "Artwork Production" },
{ id: "factory-production", label: "Factory Production" },
{ id: "deleted", label: "Deleted" },
{ id: "ontime", label: "Due/Ship - On Time" },
{ id: "early", label: "Due/Ship - Early" },
{ id: "late", label: "Due/Ship - Late" },
{ id: "shipped", label: "Shipped" },
{ id: "delivered", label: "Delivered" }];


// MM/DD/YYYY → epoch ms (0 when absent) for date sorting / range filters.
function parseOrderDate(s) {
  if (!s) return 0;
  const [m, d, y] = String(s).split("/").map(Number);
  return new Date(y, (m || 1) - 1, d || 1).getTime();
}

// Lateness bucket for the Due/Ship filter options, derived from status
// (mirrors DueDateCell's overdue logic).
function dueShipBucket(o) {
  if (o.status === "artwork-production" || o.status === "on-hold") return "late";
  if (o.status === "factory-production") return "early";
  return "ontime";
}

function orderMatchesFilter(o, filter) {
  switch (filter) {
    case "none":return true;
    case "rework":return o.status === "on-hold";
    case "deleted":return false;
    case "ontime":case "early":case "late":return dueShipBucket(o) === filter;
    default:return o.status === filter;}

}

// Value used when sorting by a given key.
function orderSortValue(o, key) {
  switch (key) {
    case "product":return o.product?.title || "";
    case "shipTo":return o.customer || o.delivery?.name || o.rep || "";
    case "quantity":return o.quantity || 0;
    case "dueDate":return parseOrderDate(o.dueDate);
    case "tempShipDate":return parseOrderDate(o.tempShipDate);
    default:return o[key] ?? "";}

}

// ─────────── Column organizer (show/hide) ───────────
function ColumnOrganizer({ columns, hidden, onToggle, onRestore }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = (e) => {if (ref.current && !ref.current.contains(e.target)) setOpen(false);};
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, [open]);

  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button onClick={() => setOpen((o) => !o)} title="Organize columns" aria-label="Organize columns" style={{
        width: 36, height: 36, borderRadius: 8, background: open ? "#f4f4f5" : "transparent",
        border: "1px solid #e4e4e7", display: "inline-flex", alignItems: "center",
        justifyContent: "center", cursor: "pointer", color: "#18181b"
      }}>
        <IconColumns size={17} />
      </button>
      {open && (
      <div style={{
        position: "absolute", top: "calc(100% + 8px)", right: 0, zIndex: 45,
        background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10,
        boxShadow: "0 10px 30px rgba(0,0,0,.14)", padding: 8, width: 220
      }}>
        <div style={{ padding: "8px 12px 8px", fontSize: 11, fontWeight: 700, color: "#71717a", letterSpacing: ".05em", textTransform: "uppercase" }}>Columns</div>
        <div style={{ maxHeight: 320, overflowY: "auto" }}>
          {columns.map((c) => {
            const locked = c.alwaysOn;
            const shown = !hidden[c.key];
            return (
              <button key={c.key} disabled={locked} onClick={() => onToggle(c.key)} style={{
                display: "flex", alignItems: "center", gap: 12, width: "100%", textAlign: "left",
                background: "transparent", border: "none", borderRadius: 7, padding: "8px 12px",
                fontSize: 13, fontWeight: 500, color: locked ? "#a1a1aa" : "#18181b",
                cursor: locked ? "default" : "pointer"
              }}
              onMouseEnter={(e) => {if (!locked) e.currentTarget.style.background = "#fafafa";}}
              onMouseLeave={(e) => {e.currentTarget.style.background = "transparent";}}>
                <span style={{
                  width: 16, height: 16, borderRadius: 4, flexShrink: 0,
                  border: `1.5px solid ${shown ? "#18181b" : "#c4c4cb"}`,
                  background: shown ? "#18181b" : "#fff",
                  display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#fff"
                }}>{shown && <IconCheck size={11} />}</span>
                {c.label}
              </button>);

          })}
        </div>
        <div style={{ borderTop: "1px solid #f1f1f3", marginTop: 4, paddingTop: 4 }}>
          <button onClick={onRestore} style={{
            width: "100%", textAlign: "left", background: "transparent", border: "none",
            borderRadius: 7, padding: "8px 12px", fontSize: 13, fontWeight: 600, color: "#18181b", cursor: "pointer"
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#fafafa"}
          onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
            Restore to Default
          </button>
        </div>
      </div>)}

    </div>);

}

// ─────────── All Orders toolbar (Sort / Filter / Date / Search / Export) ──────
function AllOrdersToolbar({ sort, onSort, filter, onFilter, range, onRange, search, onSearch, onExport, showFilter = true, showDate = true, showExport = true }) {
  return (
    <div style={{ display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 12, flexWrap: "wrap", flex: 1 }}>
      <ControlDropdown label="Sort By" value={sort} options={ALL_ORDERS_SORT_OPTIONS} onChange={onSort} filled />
      {showFilter && <ControlDropdown label="Filter By" value={filter} options={ALL_ORDERS_FILTER_OPTIONS} onChange={onFilter} />}
      {showDate && <DateRangeControl range={range} onChange={onRange} />}
      <div style={{ position: "relative", display: "flex", alignItems: "center", minWidth: 240, border: "1px solid #d8d8de", borderRadius: 8, background: "#fff" }}>
        <input
          type="text"
          placeholder="Search Keyword"
          value={search || ""}
          onChange={(e) => onSearch(e.target.value)}
          style={{ width: "100%", border: 0, outline: 0, background: "transparent", padding: "12px 40px 12px 16px", fontSize: 13.5 }} />
        <IconSearch size={16} style={{ position: "absolute", right: 12, color: "#9a9aa1", pointerEvents: "none" }} />
      </div>
      {showExport &&
      <button onClick={onExport} style={{
        padding: "12px 24px", borderRadius: 8, background: "#fff", color: "#0e0e10",
        border: "1px solid #d8d8de", fontSize: 13.5, fontWeight: 600, cursor: "pointer", whiteSpace: "nowrap"
      }}
      onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
      onMouseLeave={(e) => e.currentTarget.style.background = "#fff"}>
        Export
      </button>}
    </div>);

}

// ─────────── Rich table for the All Orders surfaces ───────────
// Adds a leading checkbox column, colored row states, and a plain header.
const ALL_ORDERS_RICH_MIN_WIDTH = 1480;

function AllOrdersRichTable({ columns, rows, keyFor, store, onOpen, onOpenOrderView, selected, onToggleRow, onToggleAll, allColumns, hidden, onToggleCol, onRestoreCols }) {
  const [statusInfoOpen, setStatusInfoOpen] = React.useState(false);
  const allSelected = rows.length > 0 && rows.every((r) => selected[keyFor(r)]);
  const someSelected = rows.some((r) => selected[keyFor(r)]);
  const gridTemplate = "40px " + columns.map((c) => c.w || "1fr").join(" ");

  const Cbox = ({ on, indeterminate, onClick }) =>
  <span onClick={(e) => {e.stopPropagation();onClick();}} style={{
    width: 16, height: 16, borderRadius: 4, flexShrink: 0, cursor: "pointer",
    border: `1.5px solid ${on || indeterminate ? "#18181b" : "#c4c4cb"}`,
    background: on || indeterminate ? "#18181b" : "#fff",
    display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#fff"
  }}>{on ? <IconCheck size={12} /> : indeterminate ? <span style={{ width: 8, height: 2, background: "#fff", borderRadius: 1 }} /> : null}</span>;


  return (
    <div style={{ background: "#fafafb", border: "1px solid #ececf0", borderRadius: 10, padding: 4, overflowX: "auto", position: "relative" }}>
      {allColumns &&
      <div style={{ position: "absolute", top: 12, right: 16, zIndex: 30 }}>
        <ColumnOrganizer columns={allColumns} hidden={hidden} onToggle={onToggleCol} onRestore={onRestoreCols} />
      </div>}
      <div style={{ minWidth: ALL_ORDERS_RICH_MIN_WIDTH }}>
        <div style={{
          display: "grid", gridTemplateColumns: gridTemplate, gap: 16,
          padding: "16px 24px", fontSize: 11, fontWeight: 700, color: "#9a9aa1",
          letterSpacing: ".08em", textTransform: "uppercase", alignItems: "end"
        }}>
          <div style={{ display: "inline-flex", alignItems: "center" }}>
            <Cbox on={allSelected} indeterminate={!allSelected && someSelected} onClick={onToggleAll} />
          </div>
          {columns.map((c) =>
          c.key === "status" ?
          <div key={c.key} style={{ textAlign: c.align || "left", display: "inline-flex", alignItems: "center", gap: 4 }}>
              {c.label}
              <button onClick={() => setStatusInfoOpen(true)} title="What do these statuses mean?" aria-label="Status definitions" style={{
              display: "inline-flex", alignItems: "center", justifyContent: "center", padding: 0,
              background: "transparent", border: 0, color: "#9a9aa1", cursor: "pointer", lineHeight: 0
            }}>
                <IconInfo size={14} />
              </button>
            </div> :
          <div key={c.key} style={{ textAlign: c.align || "left" }}>{c.label}</div>
          )}
        </div>
        <div style={{ display: "flex", flexDirection: "column", gap: 12, paddingBottom: 4 }}>
          {rows.map((o) => {
            const k = keyFor(o);
            const isSel = !!selected[k];
            const isIssue = o.status === "on-hold";
            const bg = isSel ? "#e8f0fe" : isIssue ? "#fdecea" : o.highlight ? "#fcecec" : "#fff";
            return (
              <div key={k} onClick={() => onOpen && onOpen(o)} style={{
                display: "grid", gridTemplateColumns: gridTemplate, gap: 16,
                padding: "16px 24px", alignItems: "center", background: bg,
                borderRadius: 8, cursor: "pointer",
                boxShadow: isSel ? "inset 0 0 0 1px #b9d0fb" : isIssue ? "inset 0 0 0 1px #f6c9c4" : "none",
                transition: "background .12s"
              }}
              onMouseEnter={(e) => {if (!isSel && !isIssue) e.currentTarget.style.background = "#fafafb";}}
              onMouseLeave={(e) => {if (!isSel && !isIssue) e.currentTarget.style.background = o.highlight ? "#fcecec" : "#fff";}}>
                <div style={{ display: "inline-flex", alignItems: "center" }}>
                  <Cbox on={isSel} onClick={() => onToggleRow(k)} />
                </div>
                {columns.map((c) =>
                <div key={c.key} style={{
                  fontSize: 13, color: "#0e0e10", minWidth: 0, textAlign: c.align || "left",
                  overflow: c.noClip ? "visible" : "hidden", textOverflow: "ellipsis",
                  whiteSpace: c.wrap ? "normal" : "nowrap"
                }}>
                    {c.key === "factoryOrderNum" ?
                  <a href="#" title="Open order view" onClick={(e) => {e.preventDefault();e.stopPropagation();onOpenOrderView && onOpenOrderView(o);}}
                    style={{ color: "#2563eb", textDecoration: "none", fontVariantNumeric: "tabular-nums" }}>{o.factoryOrderNum}</a> :
                  c.render ? c.render(o, store) : o[c.key] ?? "—"}
                  </div>
                )}
              </div>);

          })}
        </div>
      </div>
      <Modal open={statusInfoOpen} onClose={() => setStatusInfoOpen(false)} width={520}>
        <div style={{ padding: "28px 28px" }}>
          <h2 style={{ margin: "0 0 4px", fontSize: 19, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>Order Status Guide</h2>
          <p style={{ margin: "0 0 20px", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.5 }}>Each order moves through these stages from placement to delivery.</p>
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            {STATUS_DEFINITIONS.map((d) => {
              const s = STATUS_STYLES[d.key];
              return (
                <div key={d.key} style={{ display: "grid", gridTemplateColumns: "150px 1fr", gap: 16, alignItems: "start" }}>
                  <span style={{
                    display: "inline-flex", alignItems: "center", justifySelf: "start",
                    background: s.bg, color: s.fg, border: `1px solid ${s.border}`,
                    borderRadius: 999, padding: "4px 12px", fontSize: 12, fontWeight: 600, whiteSpace: "nowrap"
                  }}>{s.label}</span>
                  <span style={{ fontSize: 13, color: "#3f3f46", lineHeight: 1.5 }}>{d.desc}</span>
                </div>);

            })}
          </div>
        </div>
      </Modal>
    </div>);

}

// ─────────── Bulk actions bar (appears when rows are selected) ───────────
function AllOrdersBulkBar({ count, onExport, onClear }) {
  if (!count) return null;
  return (
    <div style={{
      display: "flex", alignItems: "center", gap: 16, marginBottom: 16,
      background: "#18181b", color: "#fff", borderRadius: 10, padding: "12px 16px"
    }}>
      <span style={{ fontSize: 13, fontWeight: 600 }}>{count} selected</span>
      <div style={{ marginLeft: "auto", display: "flex", gap: 12 }}>
        <button onClick={onExport} style={{
          background: "#fff", color: "#18181b", border: "none", borderRadius: 999,
          padding: "8px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer"
        }}>Export Selected</button>
        <button onClick={onClear} style={{
          background: "transparent", color: "#d4d4d8", border: "1px solid #3f3f46", borderRadius: 999,
          padding: "8px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer"
        }}>Clear</button>
      </div>
    </div>);

}

// ─────────── Rows-per-page + pagination footer ───────────
function AllOrdersPager({ total, page, pageSize, onPage, onPageSize }) {
  const pageCount = Math.max(1, Math.ceil(total / pageSize));
  const from = total === 0 ? 0 : page * pageSize + 1;
  const to = Math.min(total, (page + 1) * pageSize);
  const nums = [];
  for (let i = 0; i < pageCount; i++) {
    if (i === 0 || i === pageCount - 1 || Math.abs(i - page) <= 1) nums.push(i);else
    if (nums[nums.length - 1] !== "…") nums.push("…");
  }
  const navBtn = { width: 32, height: 32, borderRadius: 8, border: "1px solid #e4e4e7", background: "#fff", fontSize: 13, cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#18181b" };
  return (
    <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginTop: 16, flexWrap: "wrap" }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12, fontSize: 13, color: "#71717a" }}>
        <span>Rows per page</span>
        <select value={pageSize} onChange={(e) => onPageSize(Number(e.target.value))} style={{
          height: 36, border: "1px solid #e4e4e7", borderRadius: 8, padding: "0 8px", fontSize: 13, background: "#fff", color: "#18181b", cursor: "pointer"
        }}>
          {[10, 25, 50, 100].map((n) => <option key={n} value={n}>{n}</option>)}
        </select>
        <span style={{ marginLeft: 8 }}>{from}–{to} of {total}</span>
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
        <button style={{ ...navBtn, opacity: page === 0 ? .4 : 1 }} disabled={page === 0} onClick={() => onPage(page - 1)}><IconChevronLeft size={16} /></button>
        {nums.map((n, i) => n === "…" ?
        <span key={"e" + i} style={{ padding: "0 4px", color: "#a1a1aa" }}>…</span> :
        <button key={n} onClick={() => onPage(n)} style={{
          ...navBtn, background: n === page ? "#18181b" : "#fff", color: n === page ? "#fff" : "#18181b",
          fontWeight: n === page ? 700 : 500, borderColor: n === page ? "#18181b" : "#e4e4e7"
        }}>{n + 1}</button>
        )}
        <button style={{ ...navBtn, opacity: page >= pageCount - 1 ? .4 : 1 }} disabled={page >= pageCount - 1} onClick={() => onPage(page + 1)}><IconChevronRight size={16} /></button>
      </div>
    </div>);

}

// ─────────── Shared All Orders experience ───────────
// Owns search / sort / filter / date / column-visibility / selection /
// pagination and renders the toolbar + rich table + pager. Used by both the
// workspace Orders → All Orders tab and the cross-store Overview All Orders page.
function AllOrdersExperience({ orders, columns, store, onOpen, storeFor, toolbarLeft, csvName, exportApi }) {
  const [search, setSearch] = React.useState("");
  const [sort, setSort] = React.useState("none");
  const [filter, setFilter] = React.useState("none");
  const [range, setRange] = React.useState({ start: "", end: "", due: "" });
  const [hidden, setHidden] = React.useState({});
  const [selected, setSelected] = React.useState({});
  const [page, setPage] = React.useState(0);
  const [pageSize, setPageSize] = React.useState(25);
  const [toast, setToast] = React.useState(null);

  const keyFor = (o) => (o.__store?.id ? o.__store.id + "-" : "") + o.id;

  const matches = (o) => {
    if (!search) return true;
    const q = search.toLowerCase();
    return [o.__store?.teamName, o.__store?.storeTitle, o.organization, o.customer, o.rep,
    o.webOrderNum, o.factoryOrderNum, o.cartName, o.orderNum, o.sport, o.shippingGroup, o.product?.title].
    filter(Boolean).some((v) => String(v).toLowerCase().includes(q));
  };

  const view = React.useMemo(() => {
    let out = orders.filter((o) => matches(o) && orderMatchesFilter(o, filter));
    if (range.start) out = out.filter((o) => parseOrderDate(o.dueDate) >= new Date(range.start).getTime());
    if (range.end) out = out.filter((o) => parseOrderDate(o.dueDate) <= new Date(range.end).getTime());
    if (range.due) out = out.filter((o) => parseOrderDate(o.dueDate) === new Date(range.due).getTime());
    if (sort !== "none") {
      out = [...out].sort((a, b) => {
        const va = orderSortValue(a, sort),bv = orderSortValue(b, sort);
        if (typeof va === "number" && typeof bv === "number") return va - bv;
        return String(va).localeCompare(String(bv));
      });
    }
    return out;
    // eslint-disable-next-line
  }, [orders, search, filter, sort, range]);

  // Keep the page in range when the result set shrinks.
  const pageCount = Math.max(1, Math.ceil(view.length / pageSize));
  React.useEffect(() => {if (page > pageCount - 1) setPage(0);}, [pageCount, page]);
  const paged = view.slice(page * pageSize, page * pageSize + pageSize);

  const visibleCols = columns.filter((c) => !hidden[c.key]);
  const selCount = Object.values(selected).filter(Boolean).length;

  const toCSV = (rows) => {
    const head = visibleCols.map((c) => '"' + c.label + '"').join(",");
    const cell = (o, c) => {
      let v;
      if (c.csv) v = c.csv(o, storeFor ? storeFor(o) : store);else
      if (c.key === "status") v = STATUS_STYLES[o.status]?.label || o.status;else
      if (c.key === "store") v = o.__store?.teamName || o.__store?.storeTitle || "";else
      if (c.key === "product") v = o.product?.title || "";else
      v = o[c.key] ?? "";
      return '"' + String(v).replace(/"/g, '""') + '"';
    };
    const body = rows.map((o) => visibleCols.map((c) => cell(o, c)).join(",")).join("\n");
    return head + "\n" + body;
  };
  const download = (rows, name) => {
    try {
      const blob = new Blob([toCSV(rows)], { type: "text/csv;charset=utf-8;" });
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;a.download = name;
      document.body.appendChild(a);a.click();a.remove();
      setTimeout(() => URL.revokeObjectURL(url), 1000);
      setToast(`Exported ${rows.length} order${rows.length === 1 ? "" : "s"}`);
    } catch (e) {setToast("Export failed");}
  };

  const selectedRows = view.filter((o) => selected[keyFor(o)]);
  // Lets an embedding view (the Store Close Report) fire the same CSV export,
  // honoring the current column-visibility selection.
  if (exportApi) exportApi.current = () => download(view, csvName || "all-orders.csv");

  // All line items that belong to the same web order (used by the Order View).
  const siblingsOf = (o) => {
    const sib = orders.filter((x) => x.webOrderNum === o.webOrderNum && (x.__store?.id || "") === (o.__store?.id || ""));
    return sib.length ? sib : [o];
  };
  const openWith = (o, initialView) => onOpen({ ...o, __initialView: initialView, __siblings: siblingsOf(o) });

  return (
    <div>
      <div style={{ marginBottom: 16, display: "flex", alignItems: "center", gap: 24, flexWrap: "wrap" }}>
        {toolbarLeft}
        <AllOrdersToolbar
          sort={sort} onSort={setSort}
          filter={filter} onFilter={setFilter}
          range={range} onRange={setRange}
          search={search} onSearch={setSearch}
          onExport={() => download(view, csvName || "all-orders.csv")} />
      </div>

      <AllOrdersBulkBar
        count={selCount}
        onExport={() => download(selectedRows, "selected-orders.csv")}
        onClear={() => setSelected({})} />

      {view.length === 0 ?
      <OrdersEmpty sub="all" search={search} /> :
      <React.Fragment>
        <AllOrdersRichTable
          columns={visibleCols} rows={paged} keyFor={keyFor}
          store={store} onOpen={(o) => openWith(o, "item")} onOpenOrderView={(o) => openWith(o, "order")} storeFor={storeFor}
          allColumns={columns} hidden={hidden}
          onToggleCol={(k) => setHidden((h) => ({ ...h, [k]: !h[k] }))}
          onRestoreCols={() => setHidden({})}
          selected={selected}
          onToggleRow={(k) => setSelected((s) => ({ ...s, [k]: !s[k] }))}
          onToggleAll={() => {
            const allOn = paged.length > 0 && paged.every((r) => selected[keyFor(r)]);
            setSelected((s) => {
              const next = { ...s };
              paged.forEach((r) => {next[keyFor(r)] = !allOn;});
              return next;
            });
          }} />
        <AllOrdersPager
          total={view.length} page={page} pageSize={pageSize}
          onPage={setPage} onPageSize={(n) => {setPageSize(n);setPage(0);}} />
      </React.Fragment>}

      <Toast message={toast} onDone={() => setToast(null)} />
    </div>);

}

// ─────────── Table primitives ───────────
const ORDERS_TABLE_MIN_WIDTH = 1480;

function OrdersTable({ columns, children, footer }) {
  // columns: [{ key, label, w }]
  const [statusInfoOpen, setStatusInfoOpen] = React.useState(false);
  const gridTemplate = columns.map((c) => c.w || "1fr").join(" ") + " 36px";
  return (
    <div style={{
      background: "#fafafb", border: "1px solid #ececf0", borderRadius: 10,
      padding: "4px", overflowX: "auto"
    }}>
      <div style={{ minWidth: ORDERS_TABLE_MIN_WIDTH }}>
        {/* header */}
        <div style={{
          display: "grid", gridTemplateColumns: gridTemplate, gap: 16,
          padding: "16px 24px", fontSize: 11, fontWeight: 700, color: "#9a9aa1",
          letterSpacing: ".08em", textTransform: "uppercase", alignItems: "end",
          borderBottom: "1px solid transparent"
        }}>
          {columns.map((c) =>
          c.key === "status" ?
          <div key={c.key} style={{ textAlign: c.align || "left", display: "inline-flex", alignItems: "center", gap: 4 }}>
            {c.label}
            <button onClick={() => setStatusInfoOpen(true)} title="What do these statuses mean?" aria-label="Status definitions" style={{
              display: "inline-flex", alignItems: "center", justifyContent: "center", padding: 0,
              background: "transparent", border: 0, color: "#9a9aa1", cursor: "pointer", lineHeight: 0
            }}
              onMouseEnter={(e) => e.currentTarget.style.color = "#0e0e10"}
              onMouseLeave={(e) => e.currentTarget.style.color = "#9a9aa1"}>
              <IconInfo size={14} />
            </button>
          </div> :
          <div key={c.key} style={{ textAlign: c.align || "left" }}>{c.label}</div>
          )}
          <div style={{ display: "inline-flex", justifyContent: "flex-end", color: "#9a9aa1", cursor: "pointer" }} title="Add column">
            <IconPlus size={14} />
          </div>
        </div>
        <div style={{ display: "flex", flexDirection: "column", gap: 12, paddingBottom: 4 }}>
          {children}
        </div>
        {footer}
      </div>
      <Modal open={statusInfoOpen} onClose={() => setStatusInfoOpen(false)} width={520}>
        <div style={{ padding: "28px 28px" }}>
          <h2 style={{ margin: "0 0 4px", fontSize: 19, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>Order Status Guide</h2>
          <p style={{ margin: "0 0 20px", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.5 }}>Each order moves through these stages from placement to delivery.</p>
          <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
            {STATUS_DEFINITIONS.map((d) => {
              const s = STATUS_STYLES[d.key];
              return (
                <div key={d.key} style={{ display: "grid", gridTemplateColumns: "150px 1fr", gap: 16, alignItems: "start" }}>
                  <span style={{
                    display: "inline-flex", alignItems: "center", justifySelf: "start",
                    background: s.bg, color: s.fg, border: `1px solid ${s.border}`,
                    borderRadius: 999, padding: "4px 12px", fontSize: 12, fontWeight: 600, whiteSpace: "nowrap"
                  }}>{s.label}</span>
                  <span style={{ fontSize: 13, color: "#3f3f46", lineHeight: 1.5 }}>{d.desc}</span>
                </div>
              );
            })}
          </div>
        </div>
      </Modal>
    </div>);

}

function OrdersRow({ columns, order, store, onOpen, accent }) {
  const gridTemplate = columns.map((c) => c.w || "1fr").join(" ") + " 36px";
  return (
    <div
      onClick={() => onOpen && onOpen(order)}
      style={{
        display: "grid", gridTemplateColumns: gridTemplate, gap: 16,
        padding: "16px 24px", alignItems: "center",
        background: accent === "highlight" ? "#fcecec" : "#fff",
        borderRadius: 8, cursor: "pointer",
        transition: "background .12s, transform .12s"
      }}
      onMouseEnter={(e) => {if (accent !== "highlight") e.currentTarget.style.background = "#fafafb";}}
      onMouseLeave={(e) => {if (accent !== "highlight") e.currentTarget.style.background = "#fff";}}>
      {columns.map((c) =>
      <div key={c.key} style={{
        fontSize: 13, color: "#0e0e10", minWidth: 0,
        textAlign: c.align || "left",
        overflow: c.noClip ? "visible" : "hidden",
        textOverflow: "ellipsis",
        whiteSpace: c.wrap ? "normal" : "nowrap"
      }}>
          {c.render ? c.render(order, store) : order[c.key] ?? "—"}
        </div>
      )}
      <div />
    </div>);

}

// ─────────── Column definitions ───────────
function commonRightCols({ onOpen }) {
  return [
  { key: "sport", label: "Sport", w: ".9fr" },
  { key: "quantity", label: "Quantity", w: ".7fr", render: (o) => <span style={{ fontVariantNumeric: "tabular-nums" }}>{o.quantity}</span> },
  { key: "shippingGroup", label: "Shipping Group", w: ".9fr" },
  { key: "dueDate", label: "Due/Ship Date", w: "1.2fr", render: (o) => <DueDateCell date={o.dueDate} status={o.status} /> },
  { key: "status", label: "Status", w: "1.3fr", render: (o) => <StatusBadge status={o.status} tracking={o.tracking} /> }];

}

// "All Orders" merges rep-placed (My) and fan-placed (Fan) orders into one
// table. It carries both the Rep and Customer columns; rep-placed rows show
// "—" for Customer since there's no external buyer.
function makeAllOrdersColumns() {
  return [
  { key: "product", label: "Product", w: "1.5fr", alwaysOn: true, render: (o, store) => <ProductCell product={o.product} store={store} />, csv: (o) => o.product?.title || "" },
  { key: "organization", label: "Organization", w: "1.1fr", render: (o) =>
    <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
        <span>{o.organization}</span>
      </div>
  },
  { key: "factoryOrderNum", label: "Factory Order #", w: "1fr", render: (o) => <BlueLink>{o.factoryOrderNum}</BlueLink> },
  { key: "webOrderNum", label: "Web Order #", w: "1fr", render: (o) => <BlueLink>{o.webOrderNum}</BlueLink> },
  { key: "cartName", label: "Cart Name", w: "1fr" },
  { key: "customer", label: "Customer", w: "1fr", render: (o) => o.customer || "—" },
  { key: "rep", label: "Rep", w: ".9fr" },
  ...commonRightCols({})];

}

function ReorderBadge() {
  return (
    <span style={{
      display: "inline-flex", padding: "2px 8px", borderRadius: 999,
      background: "#f1f1f3", color: "#6b6b72", fontSize: 11, fontWeight: 600,
      width: "fit-content"
    }}>Reorder</span>);

}

// Distinguishes rep-placed ("My") vs fan-placed ("Fan") rows inside All Orders.
function OrderTypeBadge({ type }) {
  const fan = type === "fan";
  return (
    <span style={{
      display: "inline-flex", padding: "4px 12px", borderRadius: 999,
      background: fan ? "#eef3fb" : "#f1f1f3", color: fan ? "#1e40af" : "#52525a",
      fontSize: 11, fontWeight: 700, letterSpacing: ".02em", width: "fit-content"
    }}>{fan ? "Fan" : "My Order"}</span>);

}

function BlueLink({ children }) {
  return (
    <a href="#" onClick={(e) => {e.preventDefault();e.stopPropagation();}} style={{
      color: "#2563eb", textDecoration: "none", fontVariantNumeric: "tabular-nums"
    }}>{children}</a>);

}

// ─────────── Lists ───────────
function AllOrdersList({ orders, store, onOpen }) {
  const cols = makeAllOrdersColumns();
  return (
    <OrdersTable columns={cols}>
      {orders.map((o) =>
      <OrdersRow key={o.id} columns={cols} order={o} store={store} onOpen={onOpen} accent={o.highlight ? "highlight" : undefined} />
      )}
    </OrdersTable>);

}

// ─────────── Pending Orders ───────────
function MinQuantityRing({ pct, size = 36, strokeWidth = 4, track = "#f1d4d4", color = "#e1251b" }) {
  const r = (size - strokeWidth) / 2;
  const c = 2 * Math.PI * r;
  const dash = c * (pct / 100);
  return (
    <svg width={size} height={size} viewBox={`0 0 ${size} ${size}`}>
      <circle cx={size / 2} cy={size / 2} r={r} fill="none" stroke={track} strokeWidth={strokeWidth} />
      <circle cx={size / 2} cy={size / 2} r={r} fill="none" stroke={color} strokeWidth={strokeWidth}
      strokeDasharray={`${dash} ${c - dash}`} strokeDashoffset={c * 0.25}
      transform={`rotate(-90 ${size / 2} ${size / 2})`} strokeLinecap="round" />
    </svg>);

}

function PendingOrdersList({ groups, store, org, onOpen, onSubmit }) {
  // groups: [{ shippingGroup, title, subtitle, pct, fee, rows: [...] }]
  // Containers key off parent-or-team: league stores get one container per
  // division plus one per unassigned team; single-team stores get one container.
  const cols = [
  { key: "check", label: "", w: "24px" },
  { key: "item", label: "Item", w: "1.4fr" },
  { key: "orderDate", label: "Order Date", w: "1fr" },
  { key: "customer", label: "Customer", w: "1.4fr" },
  { key: "teamInfo", label: "Team Info", w: "1.2fr" },
  { key: "player", label: "Player", w: "1fr" },
  { key: "type", label: "Type", w: ".8fr" },
  { key: "size", label: "Size", w: ".6fr" },
  { key: "factory", label: "Factory", w: ".7fr" }];

  const gridTemplate = cols.map((c) => c.w || "1fr").join(" ");
  const [checked, setChecked] = React.useState({});

  // Normalize a pending row into a submission line item.
  const toLine = (r) => ({ id: r.id, product: r.product, size: r.size, qty: r.quantity || 1, customer: r.customer });
  // Where this container's orders ship, from the group's fulfillment mode.
  const shipLabel = (g) => {
    const key = String(g.shippingGroup || "").toLowerCase();
    const grp = ((org && org.groups) || []).find((x) => x.id.toLowerCase() === key);
    if (!grp) return "Shipping to league manager"; // unassigned teams
    const mode = ((org && org.modes) || {})[grp.id] || "league";
    return mode === "teams" ? "Shipping to team managers" : "Shipping to league manager";
  };
  // Build submission groups (dropping empty groups) from a row-predicate.
  const buildGroups = (keep) =>
  groups.
  map((g) => ({ shippingGroup: g.shippingGroup, title: g.title, fee: g.fee, pct: g.pct, rows: g.rows.filter(keep).map(toLine) })).
  filter((g) => g.rows.length > 0);

  const selectedCount = Object.values(checked).filter(Boolean).length;
  const submitRow = (r) => onSubmit && onSubmit(buildGroups((x) => x.id === r.id));
  const submitGroup = (g) => onSubmit && onSubmit([{ shippingGroup: g.shippingGroup, title: g.title, fee: g.fee, pct: g.pct, rows: g.rows.map(toLine) }]);
  const submitSelected = () => onSubmit && onSubmit(buildGroups((x) => checked[x.id]));
  const submitAll = () => onSubmit && onSubmit(buildGroups(() => true));

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 24, paddingBottom: 80 }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12, background: "#fafafb", border: "1px solid #ececf0", borderRadius: 8, padding: "12px 16px" }}>
        <span style={{ width: 30, height: 30, borderRadius: 7, flexShrink: 0, background: "#fdf6e7", color: "#8a5f10", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconBankInbox size={15} /></span>
        <span style={{ fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5 }}>Containers are keyed to a division or team. Team-gear orders here also create <strong style={{ color: "#0e0e10" }}>bank-zone captures</strong> — review and commit proposed players in the My Teams tab.</span>
      </div>
      {groups.map((g) => {
        const allChecked = g.rows.every((r) => checked[r.id]);
        return (
          <div key={g.shippingGroup} style={{
            background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8,
            overflow: "hidden"
          }}>
            {/* group header — team code + rosters */}
            <div style={{ padding: "16px 24px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 24, flexWrap: "wrap" }}>
              <div style={{ display: "flex", flexDirection: "column", gap: 8, minWidth: 240 }}>
                <div style={{ fontSize: 14, fontWeight: 700, letterSpacing: ".03em", color: "#09090b", textTransform: "uppercase" }}>
                  {g.title || `SHIPPING GROUP: ${g.shippingGroup}`}
                </div>
                {g.subtitle && g.subtitle !== "Unassigned team" &&
                <div style={{ fontSize: 14, fontWeight: 500, letterSpacing: ".03em", color: "#71717a" }}>{g.subtitle}</div>
                }
                <div style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, fontWeight: 700, color: "#52525a" }}>
                  <IconVan size={15} style={{ flexShrink: 0, color: "#71717a" }} />
                  {shipLabel(g)}
                </div>
              </div>
              <div style={{ display: "flex", alignItems: "center", gap: 24 }}>
                {g.pct < 100 &&
                <div style={{ display: "flex", alignItems: "flex-start", gap: 4 }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                    <MinQuantityRing pct={g.pct} size={16} strokeWidth={4} track="#e5e7eb" color="#f87171" />
                    <div style={{ display: "flex", flexDirection: "column" }}>
                      <span style={{ fontSize: 12, fontWeight: 500, lineHeight: "16px", letterSpacing: ".03em", color: "#f87171", whiteSpace: "nowrap" }}>Minimum Quantity</span>
                      <span style={{ fontSize: 10, color: "#f87171" }}>{g.pct}%</span>
                    </div>
                  </div>
                  <span title={`Below the factory minimum — a $${(g.fee || 0).toFixed(2)} fee applies.`} style={{
                    width: 16, height: 16, borderRadius: "50%", background: "#fff",
                    border: "1px solid #09090b", color: "#09090b", flexShrink: 0,
                    display: "inline-flex", alignItems: "center", justifyContent: "center",
                    fontSize: 9, fontWeight: 800, cursor: "help"
                  }}>i</span>
                </div>
                }
                <button onClick={() => submitGroup(g)} style={{
                  background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, height: 36,
                  padding: "0 20px", fontSize: 13, fontWeight: 700, letterSpacing: ".02em",
                  cursor: "pointer", whiteSpace: "nowrap", transition: "background .12s"
                }}
                  onMouseEnter={(e) => e.currentTarget.style.background = "#e1251b"}
                  onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>Submit Group</button>
              </div>
            </div>

            {/* column headers */}
            <div style={{
              display: "grid", gridTemplateColumns: gridTemplate, gap: 12,
              padding: "8px 24px", borderBottom: "1px solid #f4f4f5",
              background: "#fff", fontSize: 11, fontWeight: 700, color: "#71717a",
              letterSpacing: ".06em", textTransform: "uppercase", alignItems: "center"
            }}>
              <Checkbox
                checked={allChecked}
                onChange={(v) => {
                  setChecked((c) => {
                    const n = { ...c };
                    g.rows.forEach((r) => {n[r.id] = v;});
                    return n;
                  });
                }} />
              
              {cols.slice(1).map((c) =>
              <div key={c.key} style={{ textAlign: c.align || "left" }}>{c.label}</div>
              )}
            </div>

            {/* rows */}
            <div style={{ background: "#fff" }}>
              {g.rows.map((r, i) =>
              <div key={r.id}
              onClick={() => onOpen && onOpen(r)}
              style={{
                display: "grid", gridTemplateColumns: gridTemplate, gap: 12,
                padding: "16px 24px", alignItems: "center",
                borderTop: i === 0 ? "none" : "1px solid #f4f4f5",
                cursor: "pointer", transition: "background .12s"
              }}
              onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
              onMouseLeave={(e) => e.currentTarget.style.background = "#fff"}>
                  <div onClick={(e) => e.stopPropagation()}>
                    <Checkbox checked={!!checked[r.id]} onChange={(v) => setChecked((c) => ({ ...c, [r.id]: v }))} />
                  </div>
                  <div style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 8, maxWidth: 150 }}>
                    <ProductThumbPair product={r.product} store={store} kind={r.product?.kind || "jersey"} />
                    <div style={{ textAlign: "center" }}>
                      <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".03em", color: "#09090b", lineHeight: "16px" }}>{r.itemName || r.product?.title}</div>
                      {r.itemName &&
                      <div style={{ fontSize: 12, color: "#71717a", lineHeight: "16px", letterSpacing: ".01em", marginTop: 2 }}>{r.product?.title}</div>
                      }
                    </div>
                  </div>
                  <div style={{ fontSize: 13, color: "#71717a" }}>{r.orderDate}</div>
                  <div style={{ fontSize: 12 }}>
                    <div style={{ fontWeight: 700, color: "#09090b", letterSpacing: ".03em" }}>{r.customer}</div>
                    <div style={{ color: "#71717a", marginTop: 2 }}>{r.customerEmail}</div>
                  </div>
                  <div style={{ fontSize: 12 }}>
                    <div style={{ fontWeight: 700, color: "#09090b", letterSpacing: ".03em" }}>{r.teamInfo || store?.teamName}</div>
                    {r.teamRoster && <div style={{ color: "#71717a", marginTop: 2 }}>{r.teamRoster}</div>}
                  </div>
                  <div style={{ fontSize: 13, color: "#71717a" }}>{r.player || "—"}</div>
                  <div>{(() => {
                    const t = r.type || "Team Gear";
                    const meta = { "Team Required": ["REQUIRED", PTE_C.ink, "#fff"], "Team Supplied": ["SUPPLIED", PTE_C.z100, PTE_C.z700], "Fan Gear": ["FAN GEAR", "#fff", PTE_C.z500, { border: "1px solid " + PTE_C.z200 }] }[t] || ["TEAM GEAR", PTE_C.z100, PTE_C.z700];
                    return <span style={pteChip(meta[1], meta[2], meta[3])}>{meta[0]}</span>;
                  })()}</div>
                  <div style={{ fontSize: 13, color: "#71717a" }}>{r.size}</div>
                  <div style={{ fontSize: 13, color: "#71717a" }}>{r.factory}</div>
                </div>
              )}
            </div>
          </div>);

      })}

      <FloatingActionBar>
        {selectedCount > 0 &&
        <button onClick={submitSelected} style={{
          background: "#fff", color: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6,
          padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
          cursor: "pointer", whiteSpace: "nowrap"
        }}>Submit Selected ({selectedCount})</button>
        }
        <button onClick={submitAll} style={{
          background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
          padding: "12px 24px", fontSize: 13, fontWeight: 700, letterSpacing: ".04em",
          cursor: "pointer", whiteSpace: "nowrap"
        }}>Submit All</button>
      </FloatingActionBar>
    </div>);

}

// ─────────── Submit Order modal ───────────
// Scalable order-review modal shown when a Rep submits one pending order or all
// of them. `groups` is an array of { shippingGroup, fee, rows:[{product,size,qty}] }.
const SUBMIT_UNIT_PRICE = (p) =>
p?.kind === "jersey" ? 48.95 :
p?.kind === "shorts" ? 32.00 :
p?.kind === "backpack" ? 40.00 : 45.00;

function SubmitOrderModal({ open, groups = [], store, onClose, onConfirm }) {
  const [po, setPo] = React.useState("8084640218");
  if (!open || !groups.length) return null;

  const groupSubtotal = (g) => g.rows.reduce((s, r) => s + SUBMIT_UNIT_PRICE(r.product) * (r.qty || 1), 0);
  const subtotal = groups.reduce((s, g) => s + groupSubtotal(g), 0);
  const feeTotal = groups.reduce((s, g) => s + (g.fee || 0), 0);
  const total = subtotal + feeTotal;
  const itemCount = groups.reduce((s, g) => s + g.rows.reduce((n, r) => n + (r.qty || 1), 0), 0);
  const fmt = (n) => `$${n.toFixed(2).replace(/\B(?=(\d{3})+(?!\d))/g, ",")}`;
  const plural = itemCount === 1 ? "order" : "orders";

  const shipName = store?.contactName || "George Harrison";

  const label = { fontSize: 12, fontWeight: 800, letterSpacing: ".10em", color: "#0e0e10", textTransform: "uppercase" };
  const subLabel = { fontSize: 12, fontWeight: 700, letterSpacing: ".06em", color: "#9a9aa1", fontStyle: "italic", textTransform: "uppercase" };

  return (
    <div style={{
      position: "fixed", inset: 0, zIndex: 120,
      background: "rgba(20,20,30,.5)",
      display: "flex", alignItems: "center", justifyContent: "center", padding: 24,
      animation: "fadeIn .15s ease"
    }} onClick={onClose}>
      <div onClick={(e) => e.stopPropagation()} style={{
        background: "#fff", borderRadius: 12, width: 1080, maxWidth: "100%", maxHeight: "92vh",
        display: "flex", flexDirection: "column", overflow: "hidden",
        boxShadow: "0 28px 64px rgba(20,20,30,.28)", animation: "popIn .18s cubic-bezier(.2,.9,.3,1.2)"
      }}>
        {/* Header */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "space-between",
          padding: "24px 32px", borderBottom: "1px solid #ececf0", flexShrink: 0
        }}>
          <div style={{ display: "flex", alignItems: "baseline", gap: 16 }}>
            <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 21, fontWeight: 800, margin: 0, letterSpacing: "-.01em" }}>
              Submit {groups.length > 1 ? "Orders" : "Order"}
            </h2>
            <span style={{ fontSize: 13, color: "#6b6b72", fontWeight: 500 }}>
              {itemCount} {itemCount === 1 ? "item" : "items"} · {groups.length} shipping {groups.length === 1 ? "group" : "groups"}
            </span>
          </div>
          <button onClick={onClose} aria-label="Close" style={{
            width: 36, height: 36, borderRadius: 8, border: 0, background: "transparent",
            cursor: "pointer", color: "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center"
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#f4f4f6"}
          onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
            <IconX size={20} />
          </button>
        </div>

        {/* Body */}
        <div style={{ display: "grid", gridTemplateColumns: "320px 1fr", gap: 0, flex: 1, minHeight: 0 }}>
          {/* Left rail — shipping / payment / billing */}
          <div style={{ padding: "28px 28px 28px 32px", borderRight: "1px solid #ececf0", overflowY: "auto" }}>
            <div style={{ ...label, marginBottom: 16 }}>Shipping Details</div>
            <div style={{ display: "flex", alignItems: "flex-start", gap: 12, marginBottom: 32 }}>
              <div style={{ fontSize: 14, lineHeight: 1.6, fontWeight: 700, color: "#0e0e10" }}>
                {shipName}<br />
                <span style={{ fontWeight: 600 }}>101760 Beatle Dr.</span><br />
                <span style={{ fontWeight: 600 }}>Bakersfield, CA 90564</span>
              </div>
              <button aria-label="Edit shipping" style={{
                background: "transparent", border: 0, cursor: "pointer", color: "#9a9aa1",
                padding: 2, marginTop: 2, display: "inline-flex"
              }}
              onMouseEnter={(e) => e.currentTarget.style.color = "#0e0e10"}
              onMouseLeave={(e) => e.currentTarget.style.color = "#9a9aa1"}>
                <IconEdit size={15} />
              </button>
            </div>

            <div style={{ ...label, marginBottom: 12 }}>Payment Method</div>
            <label style={{ display: "block", fontSize: 13, color: "#6b6b72", marginBottom: 8 }}>PO Number</label>
            <input value={po} onChange={(e) => setPo(e.target.value)} style={{
              width: "100%", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 12px",
              fontSize: 14, color: "#0e0e10", outline: "none", marginBottom: 32
            }}
            onFocus={(e) => e.currentTarget.style.borderColor = "#0e0e10"}
            onBlur={(e) => e.currentTarget.style.borderColor = "#d4d4d8"} />

            <div style={{ ...label, marginBottom: 12 }}>Billing Address</div>
            <div style={{ fontSize: 13.5, lineHeight: 1.6, color: "#6b6b72" }}>
              Same as shipping address
            </div>
          </div>

          {/* Right rail — summary + grouped details */}
          <div style={{ display: "flex", flexDirection: "column", minHeight: 0 }}>
            <div style={{ padding: "28px 32px 16px", borderBottom: "1px solid #f1f1f3", flexShrink: 0 }}>
              <div style={{ ...label, marginBottom: 16 }}>Order Summary</div>
              <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                <SummaryLine label="Subtotal" value={fmt(subtotal)} />
                {feeTotal > 0 && <SummaryLine label="Minimum Quantity Fee" value={fmt(feeTotal)} accent />}
                <div style={{ borderTop: "1px solid #ececf0", margin: "4px 0" }} />
                <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline" }}>
                  <span style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10" }}>TOTAL</span>
                  <span style={{ fontSize: 19, fontWeight: 800, color: "#0e0e10", fontVariantNumeric: "tabular-nums" }}>{fmt(total)}</span>
                </div>
              </div>
            </div>

            <div style={{ padding: "20px 32px 28px", overflowY: "auto", flex: 1, minHeight: 0 }}>
              <div style={{ ...label, marginBottom: 16 }}>Order Details</div>
              <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
                {groups.map((g) =>
                <div key={g.shippingGroup} style={{
                  border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden", background: "#fff"
                }}>
                    <div style={{
                    display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12,
                    padding: "16px 16px", background: "#fafafb", borderBottom: "1px solid #f1f1f3"
                  }}>
                      <div>
                        <div style={subLabel}>Shipping Group</div>
                        <div style={{ fontSize: 15, fontWeight: 800, color: "#0e0e10", marginTop: 2, textTransform: "uppercase" }}>{g.title || g.shippingGroup}</div>
                      </div>
                      {g.fee > 0 &&
                    <div style={{ textAlign: "right" }}>
                          <span style={{ fontSize: 11.5, fontWeight: 700, color: "#e1251b", marginRight: 8 }}>Minimum Quantity Fee</span>
                          <span style={{ fontSize: 15, fontWeight: 800, color: "#e1251b", fontVariantNumeric: "tabular-nums" }}>{fmt(g.fee)}</span>
                        </div>
                    }
                    </div>
                    <div>
                      {g.rows.map((r, i) =>
                    <div key={r.id || i} style={{
                      display: "grid", gridTemplateColumns: "1fr 52px 36px 88px", gap: 16,
                      alignItems: "center", padding: "16px 16px",
                      borderTop: i === 0 ? "none" : "1px solid #f3f3f4"
                    }}>
                          <div style={{ display: "flex", alignItems: "center", gap: 12, minWidth: 0 }}>
                            <OrderThumb product={r.product} store={store} />
                            <div style={{ fontSize: 11, fontWeight: 700, color: "#0e0e10", letterSpacing: ".02em", lineHeight: 1.35 }}>
                              {(r.product?.title || "Item").toUpperCase()}
                            </div>
                          </div>
                          <div style={{ fontSize: 13, color: "#6b6b72", textAlign: "center" }}>{r.size || "—"}</div>
                          <div style={{ fontSize: 13, color: "#6b6b72", textAlign: "center", fontVariantNumeric: "tabular-nums" }}>{r.qty || 1}</div>
                          <div style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10", textAlign: "right", fontVariantNumeric: "tabular-nums" }}>
                            {fmt(SUBMIT_UNIT_PRICE(r.product) * (r.qty || 1))}
                          </div>
                        </div>
                    )}
                    </div>
                  </div>
                )}
              </div>
            </div>
          </div>
        </div>

        {/* Footer */}
        <div style={{
          display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 16,
          padding: "16px 32px", borderTop: "1px solid #ececf0", flexShrink: 0
        }}>
          <button onClick={onClose} style={{
            background: "transparent", border: 0, cursor: "pointer",
            fontSize: 13.5, fontWeight: 700, letterSpacing: ".04em", color: "#0e0e10", padding: "12px 16px"
          }}>Cancel</button>
          <button onClick={() => onConfirm && onConfirm({ po, total, itemCount, groups })} style={{
            background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
            padding: "12px 32px", fontSize: 13.5, fontWeight: 700, letterSpacing: ".04em", cursor: "pointer",
            display: "inline-flex", alignItems: "center", gap: 8
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#26262b"}
          onMouseLeave={(e) => e.currentTarget.style.background = "#0e0e10"}>
            Confirm · {fmt(total)}
          </button>
        </div>
        <style>{`
          @keyframes fadeIn { from { opacity:0 } to { opacity:1 } }
          @keyframes popIn { from { opacity:0; transform: translateY(12px) scale(.98) } to { opacity:1; transform: translateY(0) scale(1) } }
        `}</style>
      </div>
    </div>);

}

function SummaryLine({ label, value, accent }) {
  return (
    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "baseline" }}>
      <span style={{ fontSize: 13.5, fontWeight: 700, color: accent ? "#e1251b" : "#0e0e10" }}>{label}</span>
      <span style={{ fontSize: 13.5, fontWeight: 700, color: accent ? "#e1251b" : "#0e0e10", fontVariantNumeric: "tabular-nums" }}>{value}</span>
    </div>);

}

// ─────────── Order detail drawer ───────────
function OrderDetailDrawer({ order, store, onClose }) {
  const [view, setView] = React.useState(order?.__initialView || "item"); // 'order' or 'item'
  const [activeItem, setActiveItem] = React.useState(order);
  // Sync when a different order is opened (new object each time the drawer opens).
  React.useEffect(() => {
    setView(order?.__initialView || "item");
    setActiveItem(order);
  }, [order]);
  if (!order) return null;
  const items = order.__siblings && order.__siblings.length ? order.__siblings : [activeItem];
  return (
    <div style={{
      position: "fixed", inset: 0, zIndex: 90,
      background: "rgba(20,20,30,.4)",
      display: "flex", justifyContent: "flex-end",
      animation: "drawerFade .2s ease"
    }} onClick={onClose}>
      <div onClick={(e) => e.stopPropagation()} style={{
        width: "min(944px, calc(100vw - 80px))", height: "100%",
        background: "#fff", overflowY: "auto",
        boxShadow: "-12px 0 32px rgba(20,20,30,.18)",
        animation: "drawerSlide .25s cubic-bezier(.2,.9,.3,1)"
      }}>
        <DrawerHeader order={order} onClose={onClose} />
        <DrawerCrumbs view={view} onView={setView} />
        {view === "order" ?
        <DrawerOrderView order={order} items={items} store={store} onOpenItem={(it) => {setActiveItem(it);setView("item");}} /> :
        <DrawerBody order={activeItem || order} store={store} />}
        <DrawerActions order={order} />
      </div>
      <style>{`
        @keyframes drawerFade { from { opacity: 0 } to { opacity: 1 } }
        @keyframes drawerSlide { from { transform: translateX(40px); opacity: .4 } to { transform: translateX(0); opacity: 1 } }
      `}</style>
    </div>);

}

function DrawerHeader({ order, onClose }) {
  return (
    <div style={{ padding: "28px 36px 16px", position: "relative" }}>
      <button onClick={onClose} style={{
        position: "absolute", top: 24, right: 28,
        background: "transparent", border: 0, cursor: "pointer", color: "#0e0e10", padding: 8
      }} title="Close (Esc)">
        <IconX size={22} />
      </button>
      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
        <div style={{ fontSize: 22, fontWeight: 700 }}>
          Web Order #:{" "}
          <a href="#" onClick={(e) => e.preventDefault()} style={{ color: "#2563eb", textDecoration: "none" }}>{order.webOrderNum}</a>
        </div>
        <div style={{ fontSize: 16, fontWeight: 600 }}>
          Factory Order #:{" "}
          <a href="#" onClick={(e) => e.preventDefault()} style={{ color: "#2563eb", textDecoration: "none" }}>{order.factoryOrderNum}</a>
        </div>
        <div style={{ marginTop: 12, fontSize: 18, fontWeight: 700, color: "#0e0e10" }}>{order.organization}</div>
        <div style={{ fontSize: 13, color: "#6b6b72" }}>
          <span style={{ fontWeight: 600, color: "#0e0e10" }}>Cart Name:</span> {order.cartName}
        </div>
      </div>
    </div>);

}

function DrawerCrumbs({ view, onView }) {
  return (
    <div style={{ padding: "0 36px 16px", display: "flex", alignItems: "center", gap: 8, fontSize: 13 }}>
      {view === "item" ?
      <React.Fragment>
        <a href="#" onClick={(e) => {e.preventDefault();onView("order");}} style={{
          color: "#6b6b72", textDecoration: "none", fontWeight: 500
        }}>Order View</a>
        <IconChevronRight size={14} style={{ color: "#9a9aa1" }} />
        <span style={{ color: "#0e0e10", fontWeight: 700 }}>Item View</span>
      </React.Fragment> :
      <span style={{ color: "#0e0e10", fontWeight: 700 }}>Order View</span>}
    </div>);

}

// ─────────── Order View (whole web order: summary + grouped line items) ───────────
function OrderSummaryCell({ label, value, info }) {
  return (
    <div>
      <div style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10", display: "inline-flex", alignItems: "center", gap: 8 }}>
        {label}{info && <IconInfo size={14} style={{ color: "#0e0e10" }} />}
      </div>
      <div style={{ fontSize: 14, color: "#0e0e10", marginTop: 4 }}>{value || "—"}</div>
    </div>);

}

function DrawerOrderView({ order, items, store, onOpenItem }) {
  // Group the web order's line items by shipping group, in first-seen order.
  const groups = [];
  const byKey = {};
  (items || []).forEach((it) => {
    const k = it.shippingGroup || "—";
    if (!byKey[k]) {byKey[k] = { group: k, rows: [] };groups.push(byKey[k]);}
    byKey[k].rows.push(it);
  });
  const subtotal = order.pricing?.subtotal;
  return (
    <div style={{ padding: "0 36px 24px" }}>
      {/* Summary card */}
      <div style={{
        background: "#fafafb", border: "1px solid #ececf0", borderRadius: 10,
        padding: "28px 28px", display: "grid",
        gridTemplateColumns: "1.1fr 1fr 1.2fr 1.4fr", gap: 0
      }}>
        <div style={{ display: "flex", flexDirection: "column", gap: 24, paddingRight: 24 }}>
          <OrderSummaryCell label="Order Submitted" value={order.submitDate} />
          <OrderSummaryCell label="Order Subtotal" value={subtotal != null ? `$${subtotal.toFixed(2)}` : "—"} info />
          <OrderSummaryCell label="PO #" value={order.poNumber} />
        </div>
        <div style={{ paddingRight: 24 }}>
          <OrderSummaryCell label="Payment Terms" value={order.paymentTerms || "Net 30"} />
        </div>
        <div style={{ paddingLeft: 28, paddingRight: 24, borderLeft: "1px solid #e4e4e7" }}>
          <RepBlock rep={order.repInfo} />
        </div>
        <div style={{ paddingLeft: 28, borderLeft: "1px solid #e4e4e7" }}>
          <DeliveryBlock delivery={order.delivery} />
        </div>
      </div>

      {/* Line items grouped by shipping group */}
      <div style={{ marginTop: 28, display: "flex", flexDirection: "column", gap: 28 }}>
        {groups.map((g) =>
        <div key={g.group}>
          <div style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10", marginBottom: 8 }}>Shipping Group: {g.group}</div>
          {g.rows.map((it, i) =>
          <React.Fragment key={it.id || i}>
            {i > 0 && <div style={{ height: 1, background: "#ececf0", margin: "4px 0" }} />}
            <OrderViewItemRow item={it} store={store} onOpen={() => onOpenItem(it)} />
          </React.Fragment>
          )}
        </div>
        )}
      </div>
    </div>);

}

// Right-hand status/date column varies by the item's lifecycle stage.
function OrderViewItemStatus({ item }) {
  const s = item.status;
  const tracking = item.tracking &&
  <a href="#" onClick={(e) => {e.preventDefault();e.stopPropagation();}} style={{ color: "#2563eb", textDecoration: "none", display: "inline-flex", alignItems: "center", gap: 4, fontVariantNumeric: "tabular-nums" }}>
      {item.tracking}<IconExternal size={13} />
    </a>;

  const row = (label, value, color) =>
  <div style={{ fontSize: 14 }}>
      <span style={{ fontWeight: 700, color: "#0e0e10" }}>{label}</span>{" "}
      <span style={{ color: color || "#0e0e10" }}>{value}</span>
    </div>;

  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 12, alignItems: "flex-start" }}>
      <StatusBadge status={s} />
      {s === "shipped" || s === "delivered" ?
      <React.Fragment>
        {row(s === "delivered" ? "Delivered" : "Shipped", item.shipDate || item.dueDate, "#1f7a3d")}
        {tracking}
      </React.Fragment> :
      s === "artwork-production" ?
      <React.Fragment>
        {row("Ship Date", item.shipDate || item.tempShipDate || item.dueDate)}
        {row("Now Estimated", item.dueDate, "#b91c1c")}
        {tracking}
      </React.Fragment> :
      s === "on-hold" ?
      row("Ship Date", item.shipDate || item.tempShipDate || item.dueDate) :
      <React.Fragment>
        {row("Ship Date", item.shipDate || item.tempShipDate || item.dueDate)}
        {tracking}
      </React.Fragment>}
    </div>);

}

function OrderViewItemRow({ item, store, onOpen }) {
  const isIssue = item.status === "on-hold";
  const pill = (label) =>
  <span key={label} style={{
    display: "inline-flex", padding: "4px 12px", borderRadius: 999,
    background: "#f1f1f3", color: "#6b6b72", fontSize: 11, fontWeight: 600
  }}>{label}</span>;

  return (
    <div onClick={onOpen} style={{
      display: "grid", gridTemplateColumns: "220px 1.1fr .9fr .9fr", gap: 20,
      padding: "16px 0", alignItems: "start", cursor: "pointer"
    }}>
      <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
        {item.isReorder &&
        <div style={{ display: "flex", gap: 8 }}>{pill("Reorder")}{pill("Legacy")}</div>}
        <div style={{
          width: 128, height: 152, background: "#f1f2f4", borderRadius: 8,
          display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 12
        }}>
          <img src={orderPhotoFor(item.product)} alt={item.product?.title || "Uniform"} style={{ width: "100%", height: "100%", objectFit: "contain" }} />
        </div>
        <div style={{ fontSize: 12, color: "#9a9aa1", fontWeight: 500, lineHeight: 1.3, maxWidth: 128 }}>{item.product?.title}</div>
        {isIssue &&
        <button onClick={(e) => e.stopPropagation()} style={{
          background: "#b91c1c", color: "#fff", border: 0, borderRadius: 6,
          padding: "12px 16px", fontSize: 13, fontWeight: 700, cursor: "pointer", width: 128
        }}>Fix Issue</button>}
      </div>

      <div style={{ display: "flex", flexDirection: "column", gap: 16, fontSize: 14 }}>
        <div><span style={{ fontWeight: 700 }}>Order Part ID #</span> <span>{item.partId}</span></div>
        <div><span style={{ fontWeight: 700 }}>Description</span> <span>{item.product?.colorway || "—"}</span></div>
        <div><span style={{ fontWeight: 700 }}>Sport</span> <span>{item.sport}</span></div>
      </div>

      <div style={{ display: "flex", flexDirection: "column", gap: 16, fontSize: 14 }}>
        <div><span style={{ fontWeight: 700 }}>Quantity</span> <span>{item.quantity}</span></div>
        <div><span style={{ fontWeight: 700 }}>Item Price</span> <span>{item.pricing?.subtotal != null ? `$${item.pricing.subtotal.toFixed(2)}` : "—"}</span></div>
        {item.originalOrderId && <div><span style={{ fontWeight: 700 }}>Original Order ID</span> <span>{item.originalOrderId}</span></div>}
        {item.originalPartId && <div><span style={{ fontWeight: 700 }}>Original Order Part ID</span> <span>{item.originalPartId}</span></div>}
      </div>

      <OrderViewItemStatus item={item} />
    </div>);

}

function DrawerBody({ order, store }) {
  return (
    <div style={{ padding: "0 36px 24px" }}>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 40, alignItems: "flex-start" }}>
        {/* left: art + stepper */}
        <div>
          <div style={{
            background: "#fafafb", border: "1px solid #ececf0", borderRadius: 8,
            padding: "20px 24px", display: "flex", justifyContent: "center", gap: 20
          }}>
            <OrderThumb product={order.product || {}} size="big" />
          </div>
          <button style={{
            width: "100%", marginTop: 16, background: "#fff", border: "1px solid #d4d4d8",
            borderRadius: 999, padding: "12px", fontSize: 13, fontWeight: 600,
            cursor: "pointer", letterSpacing: ".02em"
          }}
          onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
          onMouseLeave={(e) => e.currentTarget.style.background = "#fff"}>
            View PDF
          </button>
        </div>

        {/* right: title + meta */}
        <div>
          <div style={{ fontSize: 18, fontWeight: 700, marginBottom: 8 }}>{order.product?.title}</div>
          <div style={{ fontSize: 13, color: "#6b6b72", marginBottom: 12 }}>{order.product?.colorway || "Navy Jerseys"}</div>
          <div style={{ fontSize: 13, color: "#9a9aa1", marginBottom: 16 }}>
            <span style={{ fontWeight: 600 }}>Order Part ID#:</span>{" "}
            <span style={{ color: "#6b6b72" }}>{order.partId}</span>
          </div>
          <StatusBadge status={order.status} />

          <div style={{ marginTop: 20, display: "flex", flexDirection: "column", gap: 12 }}>
            <MetaRow label="Quantity" value={order.quantity} />
            <MetaRow label="Sport" value={order.sport} />
            <MetaRow label="PO Number" value={order.poNumber} />
            <MetaRow label="Tracking Number(s)" value={order.tracking ? <BlueLink>{order.tracking}</BlueLink> : "—"} />
            <MetaRow label="Temporary Ship Date" value={order.tempShipDate} />
          </div>
        </div>
      </div>

      {/* stepper full width */}
      <div style={{ marginTop: 32 }}>
        <ProgressStepper order={order} />
      </div>

      {/* delivery / rep / pricing */}
      <div style={{ marginTop: 32, display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: 32 }}>
        <DeliveryBlock delivery={order.delivery} />
        <RepBlock rep={order.repInfo} />
        <PricingBlock pricing={order.pricing} />
      </div>
    </div>);

}

function BigJersey({ colors, label, variant }) {
  const c1 = colors?.[0] || "#1d3a8a";
  const c2 = colors?.[1] || "#0e0e10";
  const c3 = colors?.[2] || "#f5b400";
  return (
    <svg width="220" height="240" viewBox="0 0 220 240">
      <path d="M40 30 L72 14 L148 14 L180 30 L210 60 L188 84 L180 60 L180 220 L40 220 L40 60 L32 84 L10 60 Z"
      fill={c1.toLowerCase() === "#ffffff" ? "#f1f1f3" : c1}
      stroke={c2} strokeWidth="2" strokeLinejoin="round" />
      {/* neckline */}
      <path d="M72 14 Q110 36 148 14 L148 26 Q110 46 72 26 Z" fill={c2} />
      {/* shoulder stripes */}
      <rect x="50" y="36" width="50" height="6" fill={c3} opacity=".95" transform="rotate(-12 75 39)" />
      <rect x="50" y="46" width="50" height="6" fill="#fff" opacity=".85" transform="rotate(-12 75 49)" />
      <rect x="50" y="56" width="50" height="6" fill={c3} opacity=".95" transform="rotate(-12 75 59)" />
      <rect x="120" y="36" width="50" height="6" fill={c3} opacity=".95" transform="rotate(12 145 39)" />
      <rect x="120" y="46" width="50" height="6" fill="#fff" opacity=".85" transform="rotate(12 145 49)" />
      <rect x="120" y="56" width="50" height="6" fill={c3} opacity=".95" transform="rotate(12 145 59)" />
      {/* label */}
      <text x="110" y="140" textAnchor="middle" fontFamily="JetBrains Mono, monospace" fontWeight="800"
      fontSize={label && label.length <= 2 ? 70 : label && label.length < 6 ? 40 : 28}
      fill={c3}
      style={{ filter: "drop-shadow(2px 2px 0 rgba(0,0,0,.4))" }}>{label || ""}</text>
    </svg>);

}

function MetaRow({ label, value }) {
  return (
    <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", gap: 16, fontSize: 14 }}>
      <span style={{ color: "#0e0e10", fontWeight: 600 }}>{label}</span>
      <span style={{ color: "#0e0e10", fontVariantNumeric: "tabular-nums" }}>{value || "—"}</span>
    </div>);

}

function ProgressStepper({ order }) {
  const steps = [
  { id: "submit", label: "Submit Date", sub: order.submitDate },
  { id: "confirm", label: "Order Confirmed", sub: order.confirmedDate },
  { id: "artwork", label: "Artwork Production", sub: order.artworkDate },
  { id: "factory", label: "Factory Production", sub: order.factoryDate },
  { id: "ship", label: "Ship Date", sub: order.shipDate || `Temporary: ${order.tempShipDate || ""}` },
  { id: "delivery", label: "Delivery", sub: order.deliveryDate }];

  const stageIndex = {
    "submitted": 0,
    "order-confirmed": 1,
    "artwork-production": 2,
    "factory-production": 3,
    "shipped": 4,
    "delivered": 5,
    "on-hold": 2
  }[order.status] ?? 0;

  return (
    <div style={{ position: "relative", display: "grid", gridTemplateColumns: `repeat(${steps.length}, 1fr)`, gap: 0 }}>
      {/* connecting line */}
      <div style={{
        position: "absolute", left: `${100 / steps.length / 2}%`, right: `${100 / steps.length / 2}%`,
        top: 16, height: 2, background: "#ececf0"
      }} />
      <div style={{
        position: "absolute", left: `${100 / steps.length / 2}%`,
        width: `calc((100% - ${100 / steps.length}%) * ${stageIndex / (steps.length - 1)})`,
        top: 16, height: 2, background: "#0e0e10", transition: "width .3s"
      }} />
      {steps.map((s, i) => {
        const active = i === stageIndex;
        const past = i < stageIndex;
        return (
          <div key={s.id} style={{ display: "flex", flexDirection: "column", alignItems: "center", gap: 8, position: "relative" }}>
            <div style={{
              width: 32, height: 32, borderRadius: "50%",
              background: active ? "#0e0e10" : past ? "#0e0e10" : "#ececf0",
              color: active || past ? "#fff" : "#6b6b72",
              display: "inline-flex", alignItems: "center", justifyContent: "center",
              fontSize: 13, fontWeight: 700
            }}>{past ? <IconCheck size={14} style={{ strokeWidth: 3 }} /> : i + 1}</div>
            <div style={{ fontSize: 12, fontWeight: active ? 700 : 500, color: active ? "#0e0e10" : "#6b6b72", textAlign: "center" }}>{s.label}</div>
            {s.sub && <div style={{ fontSize: 11, color: "#9a9aa1", textAlign: "center" }}>{s.sub}</div>}
          </div>);

      })}
    </div>);

}

function DeliveryBlock({ delivery }) {
  if (!delivery) return <div />;
  return (
    <div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 12 }}>
        <span style={{ fontSize: 14, fontWeight: 700 }}>Delivery Information</span>
        <button title="Edit" style={{ background: "transparent", border: 0, color: "#6b6b72", cursor: "pointer", padding: 2 }}>
          <IconEdit size={14} />
        </button>
      </div>
      <div style={{ fontSize: 13, color: "#0e0e10", lineHeight: 1.7 }}>
        <div style={{ fontWeight: 600 }}>{delivery.name}</div>
        <div>{delivery.street}</div>
        <div>{delivery.cityLine}</div>
        <div style={{ marginTop: 12 }}>
          <a href="#" onClick={(e) => e.preventDefault()} style={{ color: "#2563eb", textDecoration: "none" }}>{delivery.email}</a>
        </div>
        <div>{delivery.phone}</div>
      </div>
    </div>);

}

function RepBlock({ rep }) {
  if (!rep) return <div />;
  return (
    <div>
      <div style={{ fontSize: 14, fontWeight: 700, marginBottom: 12 }}>Rep/Dealership</div>
      <div style={{ fontSize: 13, color: "#0e0e10", lineHeight: 1.7 }}>
        <div style={{ fontWeight: 600 }}>{rep.name}</div>
        <div><a href="#" onClick={(e) => e.preventDefault()} style={{ color: "#2563eb", textDecoration: "none" }}>{rep.email}</a></div>
        <div style={{ marginTop: 12 }}>{rep.dealership}</div>
        <div>{rep.phone}</div>
      </div>
    </div>);

}

function PricingBlock({ pricing }) {
  if (!pricing) return <div />;
  return (
    <div>
      <div style={{ fontSize: 14, fontWeight: 700, marginBottom: 12 }}>Item Pricing</div>
      <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
        {pricing.lines.map((l, i) =>
        <div key={i} style={{ display: "flex", justifyContent: "space-between", fontSize: 13, color: "#0e0e10" }}>
            <span>{l.label} <span style={{ color: "#6b6b72" }}>({l.detail})</span></span>
            <span style={{ fontVariantNumeric: "tabular-nums", fontWeight: 600 }}>${l.amount.toFixed(2)}</span>
          </div>
        )}
        {pricing.charges?.length > 0 &&
        <>
            <div style={{ fontSize: 11, color: "#6b6b72", textAlign: "center", margin: "8px 0", letterSpacing: ".02em" }}>Production Charges</div>
            {pricing.charges.map((l, i) =>
          <div key={i} style={{ display: "flex", justifyContent: "space-between", fontSize: 13, color: "#0e0e10" }}>
                <span>{l.label} <span style={{ color: "#6b6b72" }}>{l.detail ? `(${l.detail})` : ""}</span></span>
                <span style={{ fontVariantNumeric: "tabular-nums" }}>${l.amount.toFixed(2)}</span>
              </div>
          )}
          </>
        }
        <div style={{ borderTop: "1px solid #ececf0", marginTop: 8, paddingTop: 12, display: "flex", justifyContent: "space-between", fontSize: 14, fontWeight: 700 }}>
          <span>Item Subtotal</span>
          <span style={{ fontVariantNumeric: "tabular-nums" }}>${pricing.subtotal.toFixed(2)}</span>
        </div>
      </div>
    </div>);

}

function DrawerActions({ order }) {
  const base = ["Contact Support"];
  const actions = base;
  return (
    <div style={{
      position: "sticky", bottom: 0, background: "#fff",
      padding: "16px 36px", borderTop: "1px solid #ececf0",
      display: "grid", gridTemplateColumns: `repeat(${actions.length}, 1fr)`, gap: 16
    }}>
      {actions.map((label) =>
      <button key={label} style={{
        background: "#fff", border: "1px solid #d4d4d8", borderRadius: 999,
        padding: "12px 16px", fontSize: 13, fontWeight: 600, color: "#0e0e10",
        cursor: "pointer", whiteSpace: "nowrap",
        transition: "all .12s"
      }}
      onMouseEnter={(e) => {e.currentTarget.style.background = "#0e0e10";e.currentTarget.style.color = "#fff";e.currentTarget.style.borderColor = "#0e0e10";}}
      onMouseLeave={(e) => {e.currentTarget.style.background = "#fff";e.currentTarget.style.color = "#0e0e10";e.currentTarget.style.borderColor = "#d4d4d8";}}>
          {label}
        </button>
      )}
    </div>);

}

// ─────────── Empty state (no orders / not seeded) ───────────
function OrdersEmpty({ sub, search }) {
  const map = {
    all: { title: "No Orders Yet", body: "Rep-placed and fan orders will appear here once purchases are made through your team store." },
    pending: { title: "No Pending Orders", body: "When customers place orders through your team store, they'll appear here for review." },
    fan: { title: "No Fan Orders", body: "Fan and supporter orders will show up here as your store generates sales." }
  };
  const m = map[sub] || map.all;
  const searching = !!search;
  return (
    <div style={{
      display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center",
      padding: "120px 20px", textAlign: "center"
    }}>
      <div style={{
        width: 56, height: 56, borderRadius: "50%", background: "#dff5ea", color: "#1f9d55",
        display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16
      }}>
        <svg width={22} height={22} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
          <rect x="3" y="3" width="18" height="18" rx="2" />
          <line x1="8" y1="16" x2="8" y2="12" /><line x1="12" y1="16" x2="12" y2="8" /><line x1="16" y1="16" x2="16" y2="10" />
        </svg>
      </div>
      <div style={{ fontSize: 15, fontWeight: 700, marginBottom: 8 }}>{searching ? "No matching orders" : m.title}</div>
      <div style={{ fontSize: 13, color: "#6b6b72", maxWidth: 440, lineHeight: 1.55 }}>
        {searching ? "Try a different name, email, or order number." : m.body}
      </div>
    </div>);

}

// ─────────── Top-level Orders tab ───────────
function OrdersTabV2({ store, org, setOrg, pendingJump, allOrdersJump, pendingTabJump, onSubView }) {
  const [sub, setSub] = React.useState("all");
  // Freight gate: submitting pending orders from a group whose fulfillment
  // routing is unresolved opens the assign-managers modal instead.
  const [assignGate, setAssignGate] = React.useState(null); // { gate, grps }
  const requestSubmit = (grps, orgOverride) => {
    const o = orgOverride || org;
    for (const g of grps) {
      const gate = freightGateFor(o, store, g.shippingGroup);
      if (gate) { setAssignGate({ gate, grps }); return; }
    }
    setSubmitGroups(grps);
  };
  const [search, setSearch] = React.useState("");
  const [sort, setSort] = React.useState("none");
  const [filter, setFilter] = React.useState("none");
  const [openOrder, setOpenOrder] = React.useState(null);
  const [toast, setToast] = React.useState(null);
  const [submitGroups, setSubmitGroups] = React.useState(null);

  // Report each viewed sub-tab up so the Orders badge can clear once all
  // have been seen. Fires on mount with the default sub and on changes.
  React.useEffect(() => {onSubView && onSubView(sub);}, [sub]);

  const data = store?.seeded ?
  makeOrdersDataset(store) :
  { myOrders: [], fanOrders: [], pending: [] };

  // When the Products tab asks us to jump to a pending order (e.g. after the
  // user clicks "Go to Pending Orders" in the active-orders warning), switch to
  // the Pending sub-tab and open the first pending order's drawer.
  React.useEffect(() => {
    if (!pendingJump) return;
    setSub("pending");
    const firstGroup = data.pending?.[0];
    const firstRow = firstGroup?.rows?.[0];
    if (firstRow) {
      setOpenOrder({ ...firstRow, shippingGroup: firstGroup.shippingGroup, kind: "pending" });
    }
  }, [pendingJump]);

  // Jump to the All Orders sub-tab when requested (e.g. from the Reports KPI).
  React.useEffect(() => {
    if (!allOrdersJump) return;
    setSub("all");
  }, [allOrdersJump]);

  // Jump to the Pending Orders sub-tab when requested (e.g. from the Reports KPI).
  React.useEffect(() => {
    if (!pendingTabJump) return;
    setSub("pending");
  }, [pendingTabJump]);

  const filterFn = (o) => {
    if (!search) return true;
    const s = search.toLowerCase();
    return [o.organization, o.customer, o.playerName, o.rep, o.webOrderNum, o.factoryOrderNum, o.cartName, o.product?.title].
    filter(Boolean).some((v) => String(v).toLowerCase().includes(s));
  };

  // All Orders = rep-placed (My) + fan-placed (Fan), tagged so the table can
  // badge each row's origin.
  const allOrders = [
  ...(data.myOrders || []).map((o) => ({ ...o, orderType: "my" })),
  ...(data.fanOrders || []).map((o) => ({ ...o, orderType: "fan" }))];

  const allOrdersCols = makeAllOrdersColumns();

  // Pending groups: search-filter rows, then sort within each group.
  const pendingGroups = (data.pending || [])
    .map((g) => {
      let rows = (g.rows || []).filter(filterFn);
      if (sort !== "none") rows = [...rows].sort((a, b) => {
        const va = orderSortValue(a, sort), vb = orderSortValue(b, sort);
        if (typeof va === "number" && typeof vb === "number") return va - vb;
        return String(va).localeCompare(String(vb));
      });
      return { ...g, rows };
    })
    .filter((g) => g.rows.length > 0);

  // Pending count for the sub-tab badge (all rows across shipping groups, unfiltered).
  const pendingCount = (data.pending || []).reduce((n, g) => n + ((g.rows || []).length), 0);

  const orderNav = [
  { id: "all", label: "All Orders", desc: "Every rep and fan order" },
  { id: "pending", label: "Pending Orders", desc: "Awaiting MOQ & submission", count: pendingCount }];

  return (
    <div style={{ paddingBottom: 60 }}>
      <div style={{ display: "flex", gap: 36, alignItems: "flex-start" }}>
      <SectionSideNav items={orderNav} value={sub} onChange={setSub} />
      <div style={{ flex: 1, minWidth: 0 }}>
      {sub !== "all" &&
      <div style={{ display: "flex", alignItems: "center", gap: 24, marginBottom: 16, flexWrap: "wrap" }}>
        <AllOrdersToolbar
          sort={sort} onSort={setSort}
          filter={filter} onFilter={setFilter}
          search={search} onSearch={setSearch}
          showFilter={false} showDate={false} showExport={false} />
      </div>}

      {sub === "all" && (
      <AllOrdersExperience
        orders={allOrders}
        columns={allOrdersCols}
        store={store}
        onOpen={(o) => setOpenOrder({ ...o, kind: o.orderType })} />)
      }
      {sub === "pending" && (
      pendingGroups.length === 0 ?
      <OrdersEmpty sub="pending" search={search} /> :
      <PendingOrdersList
        groups={pendingGroups}
        store={store}
        org={org}
        onOpen={(o) => setOpenOrder({ ...o, kind: "pending" })}
        onSubmit={(grps) => {if (grps && grps.length) requestSubmit(grps);}} />)

      }
      </div>
      </div>

      {openOrder && <OrderDetailDrawer order={openOrder} store={store} onClose={() => setOpenOrder(null)} />}
      {assignGate &&
      <AssignManagersModal
        gate={assignGate.gate}
        org={org}
        store={store}
        onClose={() => setAssignGate(null)}
        onComplete={(entries) => {
          const merged = { ...org, managers: { ...((org && org.managers) || {}), ...entries } };
          setOrg && setOrg((o) => ({ ...o, managers: { ...(o.managers || {}), ...entries } }));
          const grps = assignGate.grps;
          setAssignGate(null);
          requestSubmit(grps, merged); // auto-continue — next gate or the review modal
        }} />}
      <SubmitOrderModal
        open={!!submitGroups}
        groups={submitGroups || []}
        store={store}
        onClose={() => setSubmitGroups(null)}
        onConfirm={({ itemCount }) => {
          setSubmitGroups(null);
          setToast(itemCount === 1 ? "Order submitted" : `${itemCount} orders submitted`);
        }} />
      
      <Toast message={toast} onDone={() => setToast(null)} />
    </div>);

}

// ─────────── Demo dataset ───────────
function makeOrdersDataset(store) {
  const teamColors = store?.teamColors || ["#1d3a8a", "#0e0e10", "#ffffff"];
  const teamName = (store?.teamName || "Bears").toUpperCase();
  // All orders in a store belong to that store's organization.
  const orgName = store?.teamName || store?.storeTitle || "Jazz Bantum Basketball";
  const rep = "Jared Winter";
  const dealership = "BSN Sports";
  const deliveryEmail = "jenegrin@email.com";
  const repEmail = "jwinter@email.com";

  const sharedPricing = {
    lines: [
    { label: "Adult", detail: "$77.78 x 18 items", amount: 933.36 },
    { label: "Youth", detail: "$77.78 x 18 items", amount: 933.36 }],

    charges: [
    { label: "Minimum Order Fee", detail: "", amount: 30.00 },
    { label: "90-Day Promo", detail: "$5.77/item", amount: 23.08 },
    { label: "Logo Removal", detail: "$2.44/item", amount: 29.34 }],

    subtotal: 1949.14
  };

  const sharedDelivery = {
    name: "James Orlando",
    street: "32 South, Carterville Road",
    cityLine: "Pleasant Grove, Utah 89205",
    email: deliveryEmail,
    phone: "+1 (801) 456-1234"
  };

  const sharedRep = {
    name: rep,
    email: repEmail,
    dealership,
    phone: "(801) 456-1234"
  };

  const baseOrderShape = {
    poNumber: "4500805069",
    tempShipDate: "10/18/2025",
    submitDate: "10/10/2025",
    delivery: sharedDelivery,
    repInfo: sharedRep,
    pricing: sharedPricing,
    partId: "45678-76543"
  };

  const products = {
    jerseyBears: { title: "Waist Cut, FB V-Neck Jersey BEARS", kind: "jersey", colors: teamColors, colorway: "Navy Jerseys", thumbLabel: "BEARS", thumbLabelBack: "LASTNAME" },
    jerseyBuffalo: { title: "Waist Cut, FB V-Neck Jersey BUFFALO", kind: "jersey", colors: ["#dadcd9", "#0e0e10", "#1d3a8a"], colorway: "Whte Jerseys", thumbLabel: "15", thumbLabelBack: "15" },
    jerseyLake: { title: "Waist Cut, FB V-Neck Jersey LAKE DALLAS", kind: "jersey", colors: ["#1a1f33", "#0e0e10", "#ffffff"], colorway: "Navy Jerseys", thumbLabel: "25", thumbLabelBack: "25" },
    integratedPant: { title: "Integrated Pant BEARS", kind: "shorts", colors: teamColors },
    sfnFaux: { title: "Set-In, Faux Full Button Jersey DIAMOND", kind: "shorts", colors: ["#dadcd9", "#0e0e10"] },
    loretta: { title: "Loretta Backpack BEARS", kind: "backpack", colors: ["#dadcd9", "#1d3a8a"] },
    sfnLite: { title: "SFN Lite, V-Neck Jersey", kind: "jersey", colors: ["#dadcd9", "#1d3a8a", "#0e0e10"], colorway: "Whte Jerseys", thumbLabel: "15", thumbLabelBack: "15" },
    sfnShort: { title: "SFN Lite Short HOYAS", kind: "shorts", colors: ["#dadcd9", "#1d3a8a"] }
  };

  const myOrders = [
  {
    id: "mo-1", product: products.jerseyBears, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} BB`,
    rep, sport: "Basketball", quantity: 12, shippingGroup: "PMP",
    dueDate: "10/14/2025", status: "artwork-production",
    ...baseOrderShape
  },
  {
    id: "mo-2", product: products.jerseyBuffalo, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} BB`,
    rep, sport: "Basketball", quantity: 12, shippingGroup: "PMP",
    dueDate: "10/10/2025", status: "shipped", tracking: "ZXY356TJGSAMAK23A",
    isReorder: true,
    ...baseOrderShape
  },
  {
    id: "mo-3", product: products.jerseyLake, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} BB`,
    rep, sport: "Basketball", quantity: 12, shippingGroup: "PMP",
    dueDate: "10/10/2025", status: "on-hold",
    highlight: true,
    ...baseOrderShape
  },
  {
    id: "mo-4", product: products.integratedPant, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} BB`,
    rep, sport: "Basketball", quantity: 12, shippingGroup: "PMP",
    dueDate: "10/09/2025", status: "delivered", tracking: "ZXY356TJGSAMAK23A",
    ...baseOrderShape
  }];


  const fanOrders = [
  {
    id: "fo-1", product: products.jerseyBears, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} FB`,
    customer: "Jared Winter", sport: "Basketball", quantity: 1, shippingGroup: "PMP",
    dueDate: "10/14/2025", status: "artwork-production",
    ...baseOrderShape, rep
  },
  {
    id: "fo-2", product: products.jerseyBuffalo, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} FB`,
    customer: "Jared Winter", sport: "Basketball", quantity: 2, shippingGroup: "PMP",
    dueDate: "10/10/2025", status: "shipped", tracking: "ZXY356TJGSAMAK23A", isReorder: true,
    ...baseOrderShape, rep
  },
  {
    id: "fo-3", product: products.jerseyLake, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} FB`,
    customer: "Jared Winter", sport: "Basketball", quantity: 1, shippingGroup: "PMP",
    dueDate: "10/10/2025", status: "on-hold", highlight: true,
    ...baseOrderShape, rep
  },
  {
    id: "fo-4", product: products.integratedPant, organization: orgName,
    factoryOrderNum: "267891235122", webOrderNum: "08a9fc6312f3", cartName: `PG Junior ${teamName.charAt(0)}${teamName.toLowerCase().slice(1)} FB`,
    customer: "Jared Winter", sport: "Basketball", quantity: 1, shippingGroup: "PMP",
    dueDate: "10/09/2025", status: "delivered", tracking: "ZXY356TJGSAMAK23A",
    ...baseOrderShape, rep
  },
  {
    id: "fo-5", product: products.loretta, organization: orgName,
    factoryOrderNum: "26091234514", webOrderNum: "39d1fb6827e4", cartName: "Loretta Baseball",
    customer: "Kyle Morris", sport: "Basketball", quantity: 1, shippingGroup: "BLB",
    dueDate: "10/5/2025", status: "shipped", tracking: "ZXY356TJGSAMAK23A",
    ...baseOrderShape, rep
  }];


  // Pending orders — mirrors the flattened structure: league stores get one
  // container per division plus one per unassigned team; single-team stores
  // get a single container for all orders.
  const mkPending = (id, product, itemName, orderDate, customer, customerEmail, teamInfo, teamRoster, player, type, size) => ({
    id, product, itemName, orderDate, customer, customerEmail,
    teamInfo, teamRoster, player, type, size,
    factory: "PMP", quantity: 1, status: "submitted",
    ...baseOrderShape, rep,
  });
  const PENDING_CUSTOMERS = [
  ["Susan Quinones", "susanquinones@gmail.com"], ["Frank Wright", "fwright@outlook.com"],
  ["Kyle Morris", "kmorris@aol.com"], ["Irene George", "igeorge@gmail.com"],
  ["Kate Hall", "khall@aol.com"], ["Aaron Brown", "abrown@hotmail.com"]];
  const PENDING_PLAYERS = ["Jalen Hurts", "Omar Calzoni", "Cooper Curtis", "Davis Aminoff", "Lincoln Herwitz", "Corey Stanton"];
  const PENDING_TYPES = ["Team Required", "Team Supplied", "Fan Gear"];
  const prodCycle = [products.jerseyBears, products.integratedPant, products.jerseyBuffalo, products.sfnLite, products.sfnShort, products.jerseyLake];
  const pendSizes = ["YL", "AM", "AL", "YM", "AS", "AXL"];
  let pendId = 0;
  const pendingRows = (teamInfo, teamRoster, count, seed) => Array.from({ length: count }, (_, i) => {
    const c = PENDING_CUSTOMERS[(seed + i) % PENDING_CUSTOMERS.length];
    return mkPending(`pd-${++pendId}`, prodCycle[(seed + i) % prodCycle.length],
      (seed + i) % 3 === 0 ? "Home Game Jersey" : null,
      `10/0${((seed + i) % 8) + 1}/2025`, c[0], c[1], teamInfo, teamRoster,
      PENDING_PLAYERS[(seed + i) % PENDING_PLAYERS.length],
      PENDING_TYPES[(seed + i) % PENDING_TYPES.length],
      pendSizes[(seed + i) % pendSizes.length]);
  });
  const isLeague = (store?.storeType || "league") === "league";
  const pending = isLeague ?
  [
  ...ROSTER_GROUP_SEED.map((g, gi) => ({
    shippingGroup: g.id.toUpperCase(),
    title: `${g.name} • ${rTeamCode(g.id)}`,
    subtitle: g.rosters.map((r) => r.name).join(", "),
    pct: gi % 2 === 0 ? 54 + gi * 9 : 100,
    fee: gi % 2 === 0 ? 30.00 : 0,
    rows: pendingRows(g.name, g.rosters[gi % g.rosters.length].name, 2 + (gi % 2), gi * 3)
  })),
  ...STANDALONE_ROSTER_SEED.map((r, ri) => ({
    shippingGroup: r.id.replace("loose-", "").toUpperCase(),
    title: `${r.name} • ${rTeamCode(r.id)}`,
    subtitle: "Unassigned team",
    pct: ri === 0 ? 100 : 45,
    fee: ri === 0 ? 0 : 30.00,
    rows: pendingRows(r.name, null, 2, 11 + ri * 2)
  }))] :
  [{
    shippingGroup: "PMP", title: `${teamName} • ${rTeamCode(store?.id || "single")}`,
    pct: 62, fee: 30.00,
    rows: pendingRows(store?.teamName || "Team Roster", null, 3, 0)
  }];

  return { myOrders, fanOrders, pending };
}

Object.assign(window, { OrdersTabV2, OrderDetailDrawer, makeOrdersDataset, SubmitOrderModal });

/* ===== ASSET 23 : 5ccc15c0-9ce3-4436-b2f8-a12dc47560f2 ===== */
// Customizer 3.0 — sport / category picker.
// Matches the Team Store "choose a sport" design: Men / Women tabs over a
// 4-up grid of sport cards (label + athlete image). Each card's artwork is the
// gendered SVG export in assets/sport-cards/.

const CARD_DIR = "assets/sport-cards/";

// Men's catalog (order matches the reference design).
const MEN_CATEGORIES = [
  { id: "accessories",  label: "Accessories",   sport: "Accessories",   img: "accessories.svg" },
  { id: "apparel",      label: "Apparel",        sport: "Apparel",       img: "apparel-m.svg" },
  { id: "basketball",   label: "Basketball",     sport: "Basketball",    img: "basketball-m.svg" },
  { id: "baseball",     label: "Baseball",       sport: "Baseball",      img: "baseball-m.svg" },
  { id: "cheer",        label: "Cheer",          sport: "Cheer",         img: "cheer-m.svg" },
  { id: "football",     label: "Football",       sport: "Football",      img: "football-m.svg" },
  { id: "lacrosse",     label: "Lacrosse",       sport: "Lacrosse",      img: "lacrosse-m.svg" },
  { id: "soccer",       label: "Soccer",         sport: "Soccer",        img: "soccer-m.svg" },
  { id: "tennis",       label: "Tennis",         sport: "Tennis",        img: "tennis-m.svg" },
  { id: "track-field",  label: "Track & Field",  sport: "Track & Field", img: "trackfield-m.svg" },
  { id: "volleyball",   label: "Volleyball",     sport: "Volleyball",    img: "volleyball-m.svg" },
  { id: "wrestling",    label: "Wrestling",      sport: "Wrestling",     img: "wrestling-m.svg" },
];

// Women's catalog (order matches the reference design).
const WOMEN_CATEGORIES = [
  { id: "accessories",  label: "Accessories",   sport: "Accessories",   img: "accessories.svg" },
  { id: "apparel",      label: "Apparel",        sport: "Apparel",       img: "apparel-w.svg" },
  { id: "basketball",   label: "Basketball",     sport: "Basketball",    img: "basketball-w.svg" },
  { id: "cheer",        label: "Cheer",          sport: "Cheer",         img: "cheer-w.svg" },
  { id: "fastpitch",    label: "Fastpitch",      sport: "Softball",      img: "fastpitch-w.svg" },
  { id: "field-hockey", label: "Field Hockey",   sport: "Field Hockey",  img: "field-hockey-w.svg" },
  { id: "soccer",       label: "Soccer",         sport: "Soccer",        img: "soccer-w.svg" },
  { id: "tennis",       label: "Tennis",         sport: "Tennis",        img: "tennis-w.svg" },
  { id: "track-field",  label: "Track & Field",  sport: "Track & Field", img: "trackfield-w.svg" },
  { id: "volleyball",   label: "Volleyball",     sport: "Volleyball",    img: "volleyball-w.svg" },
  { id: "wrestling",    label: "Wrestling",      sport: "Wrestling",     img: "wrestling-w.svg" },
];

// Sports that only exist in the women's catalog — used to pick a sensible
// default tab when the store's sport implies a gender.
const WOMEN_ONLY = ["Softball", "Field Hockey"];

const SPORT_STYLE_COUNTS = { accessories: 18, apparel: 32, basketball: 24, baseball: 28, cheer: 12, football: 30, lacrosse: 14, soccer: 22, tennis: 10, "track-field": 16, volleyball: 20, wrestling: 8, fastpitch: 18, "field-hockey": 10 };

function GenderTabs({ gender, onChange }) {
  const Tab = ({ value, children }) => {
    const active = gender === value;
    return (
      <button
        type="button"
        onClick={() => onChange(value)}
        style={{
          height: 44, padding: "0 32px", border: 0, cursor: "pointer",
          display: "inline-flex", alignItems: "center",
          background: active ? "#0e0e10" : "#fff",
          color: active ? "#fff" : "#0e0e10",
          fontSize: 14, fontWeight: 700, letterSpacing: ".005em",
          transition: "background .15s, color .15s",
        }}
      >
        {children}
      </button>
    );
  };
  return (
    <div style={{ display: "inline-flex", border: "1px solid #d4d4d8", borderRadius: 8, overflow: "hidden", background: "#fff" }}>
      <Tab value="men">Men</Tab>
      <Tab value="women">Women</Tab>
    </div>
  );
}

function SportCard({ item, onPick }) {
  const [hover, setHover] = React.useState(false);
  return (
    <button
      type="button"
      onClick={() => onPick(item)}
      onMouseEnter={() => setHover(true)}
      onMouseLeave={() => setHover(false)}
      onFocus={() => setHover(true)}
      onBlur={() => setHover(false)}
      aria-label={item.label}
      style={{
        position: "relative", overflow: "hidden", textAlign: "left", width: "100%",
        background: "#fff", border: hover ? "1px solid #0e0e10" : "1px solid #e4e4e7",
        borderRadius: 12, padding: 18, height: 140, boxSizing: "border-box",
        display: "flex", flexDirection: "column", justifyContent: "flex-end",
        cursor: "pointer", boxShadow: hover ? "0 8px 20px rgba(9,9,11,.08)" : "none",
        transition: "border-color .14s ease, box-shadow .14s ease", fontFamily: "inherit",
      }}
    >
      <span aria-hidden="true" style={{ position: "absolute", top: -18, right: 4, fontSize: 96, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#f1f1f3", letterSpacing: "-.04em", lineHeight: 1, pointerEvents: "none", whiteSpace: "nowrap" }}>{item.label.charAt(0)}</span>
      <span style={{ position: "relative", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#0e0e10" }}>{item.label}</span>
      <span style={{ position: "relative", fontSize: 12, color: "#9a9aa1", marginTop: 3 }}>{(SPORT_STYLE_COUNTS[item.id] || 12) + " styles"}</span>
    </button>
  );
}

// ─────────── Customize-New-Product XL workflow steps ───────────

// Step 0 – Sport picker (Men / Women tabs + sport grid)
function CustSportPickerStep({ storeSport, onPick }) {
  const [gender, setGender] = React.useState(
    WOMEN_ONLY.includes(storeSport) ? "women" : "men"
  );
  const items = gender === "women" ? WOMEN_CATEGORIES : MEN_CATEGORIES;
  return (
    <div style={{ height: "100%", overflowY: "auto", padding: "22px 28px 28px" }}>
      <div style={{ maxWidth: 980, margin: "0 auto" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 20, flexWrap: "wrap" }}>
          <GenderTabs gender={gender} onChange={setGender} />
          <span style={{ fontSize: 13, color: "#9a9aa1" }}>{items.length + " categories · pick one to start customizing"}</span>
        </div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 14 }}>
          {items.map((it) => (
            <SportCard key={gender + "-" + it.id} item={it} onPick={() => onPick({ ...it, gender })} />
          ))}
        </div>
      </div>
    </div>
  );
}

// Step 1 – Product picker for the selected sport
const CUST_BASKETBALL_PRODUCTS = [
  { id: "bb-hoyas",    name: "Hoyas",      photo: "assets/products/jersey-grizzlies.png" },
  { id: "bb-huskies",  name: "Huskies",    photo: "assets/products/jersey-warriors.png"  },
  { id: "bb-wolves",   name: "Wolves",     photo: "assets/products/jersey-asteroid.png"  },
  { id: "bb-gold",     name: "Gold State", photo: "assets/products/jersey-padres.png"    },
  { id: "bb-lancers",  name: "Lancers",    photo: "assets/products/jersey-lancers.png"   },
  { id: "bb-grizzly",  name: "Grizzlies",  photo: "assets/products/jersey-grizzlies.png" },
  { id: "bb-warriors", name: "Warriors",   photo: "assets/products/jersey-warriors.png"  },
  { id: "bb-memphis",  name: "Memphis",    photo: "assets/products/jersey-asteroid.png"  },
];

function CustProductPickerStep({ sport, onBack, onPick }) {
  const [search, setSearch] = React.useState("");
  const sportLabel   = sport ? sport.label  : "Basketball";
  const genderLabel  = sport && sport.gender === "women" ? "Women and Girls" : "Men and Boys";
  const products = CUST_BASKETBALL_PRODUCTS.filter((p) =>
    p.name.toLowerCase().includes(search.toLowerCase())
  );
  return (
    <div style={{ height: "100%", display: "flex", flexDirection: "column", overflow: "hidden" }}>
      {/* Toolbar */}
      <div style={{ padding: "16px 28px 16px", borderBottom: "1px solid #ececf0", flexShrink: 0 }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16 }}>
          <div>
            <h2 style={{ fontSize: 22, fontWeight: 800, margin: 0, letterSpacing: "-.01em" }}>{sportLabel}</h2>
            <div style={{ fontSize: 12.5, color: "#6b6b72", marginTop: 2 }}>{genderLabel}</div>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
            <button style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 12px", fontSize: 12.5, fontWeight: 600, cursor: "pointer", color: "#0e0e10" }}>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><circle cx="12" cy="12" r="3"/><path d="M12 2v2m0 16v2M4.22 4.22l1.42 1.42m12.72 12.72 1.42 1.42M2 12h2m16 0h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
              Customize Your Look
            </button>
            <button style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 12px", fontSize: 12.5, fontWeight: 600, cursor: "pointer", color: "#0e0e10" }}>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><line x1="4" y1="6" x2="20" y2="6"/><line x1="8" y1="12" x2="20" y2="12"/><line x1="12" y1="18" x2="20" y2="18"/></svg>
              Show Filters
            </button>
            <button style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 12px", fontSize: 12.5, fontWeight: 600, cursor: "pointer", color: "#0e0e10" }}>
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d="M7 16V4m0 0L3 8m4-4 4 4M17 8v12m0 0 4-4m-4 4-4-4"/></svg>
              Sort By: Default
            </button>
            <div style={{ position: "relative" }}>
              <input value={search} onChange={(e) => setSearch(e.target.value)}
                placeholder={"Search " + sportLabel}
                style={{ border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 12px 8px 32px", fontSize: 12.5, width: 200, outline: 0 }} />
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="#9a9aa1" strokeWidth="2" strokeLinecap="round" style={{ position: "absolute", left: 12, top: "50%", transform: "translateY(-50%)" }}><circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/></svg>
            </div>
          </div>
        </div>
      </div>

      {/* Product list */}
      <div style={{ flex: 1, overflowY: "auto", padding: "20px 28px 32px" }}>
        {/* Group header */}
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 16 }}>
          <div>
            <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 4 }}>
              <span style={{ fontSize: 15, fontWeight: 700, color: "#0e0e10" }}>SFN Lite Round Neck Jerseys</span>
              <span style={{ background: "#0e0e10", color: "#fff", fontSize: 10.5, fontWeight: 700, letterSpacing: ".08em", padding: "2px 8px", borderRadius: 3 }}>PRO SIZING</span>
            </div>
            <div style={{ fontSize: 12, color: "#6b6b72" }}>
              SFN Lite &nbsp;·&nbsp; {genderLabel} &nbsp;·&nbsp; QXSFNLJ &nbsp;·&nbsp;
              <span style={{ textDecoration: "underline", cursor: "pointer" }}>Size Guide</span>
            </div>
          </div>
          <div style={{ display: "flex", flexDirection: "column", alignItems: "flex-end", gap: 4 }}>
            <div style={{ display: "flex", gap: 8 }}>
              <span style={{ background: "#e7f0ff", color: "#2563eb", fontSize: 11, fontWeight: 600, padding: "4px 8px", borderRadius: 4 }}>Sublimation</span>
              <span style={{ background: "#fef3e2", color: "#b45309", fontSize: 11, fontWeight: 600, padding: "4px 8px", borderRadius: 4 }}>QXX</span>
              <span style={{ background: "#edfbf3", color: "#1f9d55", fontSize: 11, fontWeight: 600, padding: "4px 8px", borderRadius: 4 }}>Estimated ship date 3/25/2026</span>
            </div>
            <div style={{ fontSize: 12, color: "#0e0e10" }}>
              <span style={{ fontWeight: 500 }}>MSRP Pricing:</span>
              <span>  Adult </span><strong>$89.45</strong>
              <span>  Youth </span><strong>$89.45</strong>
            </div>
            <div style={{ fontSize: 11.5, color: "#6b6b72" }}>Team pricing available</div>
          </div>
        </div>

        {/* Grid */}
        <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 20 }}>
          {products.map((p) => (
            <button key={p.id} onClick={() => onPick(p)}
              style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 8, padding: "16px 16px 16px", cursor: "pointer", textAlign: "center", transition: "box-shadow .14s, border-color .14s" }}
              onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 16px rgba(20,20,30,.10)"; e.currentTarget.style.borderColor = "#c7c7cd"; }}
              onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#ececf0"; }}>
              <div style={{ height: 160, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 12 }}>
                <img src={p.photo} alt={p.name} style={{ maxHeight: 148, maxWidth: "100%", objectFit: "contain" }} />
              </div>
              <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{p.name}</div>
            </button>
          ))}
        </div>
      </div>

      {/* Footer back */}
      <div style={{ padding: "12px 28px", borderTop: "1px solid #ececf0", flexShrink: 0 }}>
        <button onClick={onBack} style={{ background: "transparent", border: 0, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, fontWeight: 600, color: "#6b6b72", padding: 0 }}>
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
          Back to Sport Picker
        </button>
      </div>
    </div>
  );
}

// Step 2 – Builder
function CustBuilderStep({ sport, product, onBack, onAddToStore }) {
  const [tab, setTab] = React.useState("details");
  const [activeThumb, setActiveThumb] = React.useState(0);
  const sportLabel = sport ? sport.label : "Basketball";
  const photo = product ? product.photo : "assets/products/jersey-warriors.png";
  const thumbs = [photo, photo, photo, photo];

  const sidebarItems = [
    { key: "details",      label: "Details",      icon: <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><circle cx="12" cy="12" r="9"/><line x1="12" y1="9" x2="12" y2="13"/><circle cx="12" cy="16" r=".6" fill="currentColor"/></svg> },
    { key: "options",      label: "Options",      icon: <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><path d="M20.38 3.46 16 2a4 4 0 0 1-8 0L3.62 3.46a2 2 0 0 0-1.34 2.23l.58 3.57a1 1 0 0 0 .99.86H5v10h2v-5a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v5h2V10h1.15a1 1 0 0 0 .99-.86l.58-3.57a2 2 0 0 0-1.34-2.23z"/></svg> },
    { key: "customize",    label: "Customize",    icon: <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg> },
    { key: "applications", label: "Applications", icon: <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><path d="M12 2 2 7l10 5 10-5-10-5z"/><path d="M2 17l10 5 10-5"/><path d="M2 12l10 5 10-5"/></svg> },
    { key: "summary",      label: "Summary",      icon: <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round"><line x1="8" y1="6" x2="21" y2="6"/><line x1="8" y1="12" x2="21" y2="12"/><line x1="8" y1="18" x2="21" y2="18"/><circle cx="3" cy="6" r="1"/><circle cx="3" cy="12" r="1"/><circle cx="3" cy="18" r="1"/></svg> },
  ];

  return (
    <div style={{ height: "100%", display: "flex", overflow: "hidden" }}>
      {/* Sidebar */}
      <div style={{ width: 80, flexShrink: 0, borderRight: "1px solid #ececf0", display: "flex", flexDirection: "column", alignItems: "center", paddingTop: 12, gap: 2, overflowY: "auto" }}>
        {sidebarItems.map((item) => {
          const active = tab === item.key;
          return (
            <button key={item.key} onClick={() => setTab(item.key)} style={{
              width: 64, padding: "12px 4px", border: 0, borderRadius: 8,
              background: active ? "#f1f1f3" : "transparent",
              color: active ? "#0e0e10" : "#9a9aa1",
              cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", gap: 4,
              transition: "background .12s, color .12s",
            }}
              onMouseEnter={(e) => { e.currentTarget.style.background = active ? "#f1f1f3" : "#fafafa"; e.currentTarget.style.color = "#0e0e10"; }}
              onMouseLeave={(e) => { e.currentTarget.style.background = active ? "#f1f1f3" : "transparent"; e.currentTarget.style.color = active ? "#0e0e10" : "#9a9aa1"; }}
            >
              {item.icon}
              <span style={{ fontSize: 10.5, fontWeight: active ? 700 : 500, letterSpacing: ".01em" }}>{item.label}</span>
            </button>
          );
        })}
      </div>

      {/* Main */}
      <div style={{ flex: 1, display: "flex", flexDirection: "column", overflow: "hidden" }}>
        {/* Builder toolbar */}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "12px 20px", borderBottom: "1px solid #ececf0", flexShrink: 0 }}>
          <button onClick={onBack} style={{ background: "transparent", border: 0, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, fontWeight: 600, color: "#6b6b72", padding: 0 }}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round"><path d="M15 18l-6-6 6-6"/></svg>
            Back to {sportLabel}
          </button>
          <div style={{ display: "flex", alignItems: "center", gap: 20 }}>
            {[{ label: "Undo", d: "M9 14 4 9l5-5M4 9h10.5a5.5 5.5 0 0 1 0 11H11" }, { label: "Redo", d: "M15 14l5-5-5-5M19 9H8.5a5.5 5.5 0 0 0 0 11H13" }].map(({ label, d }) => (
              <button key={label} style={{ background: "transparent", border: 0, cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", gap: 4, color: "#6b6b72", padding: 0 }}>
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d={d}/></svg>
                <span style={{ fontSize: 10, fontWeight: 500 }}>{label}</span>
              </button>
            ))}
            <button style={{ background: "transparent", border: 0, cursor: "pointer", display: "flex", flexDirection: "column", alignItems: "center", gap: 4, color: "#6b6b72", padding: 0 }}>
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><circle cx="12" cy="12" r="10"/><path d="M12 8v4m0 4h.01"/></svg>
              <span style={{ fontSize: 10, fontWeight: 500 }}>Help</span>
            </button>
          </div>
          <div style={{ display: "flex", gap: 12 }}>
            <button onClick={() => onAddToStore()} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "8px 20px", fontSize: 13, fontWeight: 700, cursor: "pointer" }}>Add to Store</button>
          </div>
        </div>

        {/* Builder body */}
        <div style={{ flex: 1, display: "flex", overflow: "hidden" }}>
          {/* Details panel */}
          <div style={{ width: 312, flexShrink: 0, borderRight: "1px solid #ececf0", overflowY: "auto", padding: "16px 20px" }}>
            {tab === "details" && (
              <div>
                <div style={{ fontSize: 14, fontWeight: 800, marginBottom: 16, color: "#0e0e10" }}>BLB, Set-In, Full Button Jersey</div>
                {[["Price Item","BSBQT2BJ"],["Product Line","Sublimated"],["Block","Set-In Full Button Jersey"],["Style","Astros"],["Gender", sport && sport.gender === "women" ? "Women/Girls" : "Men/Boys"],["Youth","$76.67"],["XS","$76.67"],["S \u2013 3XL","$79.00"],["4XL \u2013 5XL","$80.34"]].map(([k, v]) => (
                  <div key={k} style={{ display: "flex", justifyContent: "space-between", padding: "8px 0", borderBottom: "1px solid #f1f1f3", fontSize: 13 }}>
                    <span style={{ color: "#6b6b72", fontWeight: 500 }}>{k}</span>
                    <span style={{ color: "#0e0e10", fontWeight: 500, textAlign: "right" }}>{v}</span>
                  </div>
                ))}
                <div style={{ display: "flex", gap: 16, margin: "16px 0 16px" }}>
                  <span style={{ fontSize: 12.5, color: "#0e0e10", textDecoration: "underline", cursor: "pointer" }}>Sizing Chart</span>
                  <span style={{ fontSize: 12.5, color: "#0e0e10", textDecoration: "underline", cursor: "pointer" }}>How to Measure</span>
                </div>
                <div style={{ fontSize: 14, fontWeight: 700, marginBottom: 12, color: "#0e0e10" }}>Materials</div>
                {[{ label: "Fabric Option 1 (Suggested)", extra: "", desc: "100% Polyester \u00b7 7.96 oz\nCircular knitted, midweight fabric with high stretch in all directions." }, { label: "Fabric Option 2", extra: "+$2.00/item", desc: "100% Polyester \u00b7 7.96 oz\nCircular knitted, midweight fabric with advanced moisture management." }].map((f, i) => (
                  <div key={i} style={{ display: "flex", gap: 12, marginBottom: 16 }}>
                    <div style={{ width: 48, height: 48, borderRadius: 4, flexShrink: 0, overflow: "hidden", background: "repeating-linear-gradient(45deg,#d4d4d8 0px,#d4d4d8 2px,#e8e8ec 2px,#e8e8ec 8px)" }} />
                    <div>
                      <div style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10" }}>{f.label} {f.extra && <span style={{ color: "#6b6b72", fontWeight: 500 }}>{f.extra}</span>}</div>
                      <div style={{ fontSize: 11.5, color: "#6b6b72", marginTop: 4, lineHeight: 1.5, whiteSpace: "pre-line" }}>{f.desc}</div>
                    </div>
                  </div>
                ))}
              </div>
            )}
            {tab !== "details" && (
              <div style={{ padding: "60px 0", textAlign: "center", color: "#9a9aa1", fontSize: 13 }}>
                {(sidebarItems.find((s) => s.key === tab) || {}).label} options
              </div>
            )}
          </div>

          {/* Preview panel */}
          <div style={{ flex: 1, display: "flex", flexDirection: "column", background: "#fafafa", overflow: "hidden" }}>
            <div style={{ flex: 1, display: "flex", alignItems: "center", justifyContent: "center", padding: "24px 32px" }}>
              <img src={thumbs[activeThumb]} alt={product ? product.name : "Jersey"} style={{ maxHeight: "100%", maxWidth: "100%", objectFit: "contain" }} />
            </div>
            <div style={{ display: "flex", gap: 12, padding: "12px 20px", borderTop: "1px solid #ececf0", justifyContent: "center", flexShrink: 0 }}>
              {thumbs.map((src, i) => (
                <button key={i} onClick={() => setActiveThumb(i)} style={{ width: 64, height: 64, borderRadius: 4, border: activeThumb === i ? "2px solid #0e0e10" : "1px solid #e5e5e9", background: "#fff", cursor: "pointer", overflow: "hidden", padding: 4, flexShrink: 0 }}>
                  <img src={src} alt="" style={{ width: "100%", height: "100%", objectFit: "contain" }} />
                </button>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
}

// Step 3 – Review & Finalize
function CustReviewStep({ sport, product, onBack, onCancel, onAddToStore }) {
  const [teamSetting, setTeamSetting] = React.useState("fan-gear");
  const [pricing, setPricing] = React.useState(() => defaultPricing());
  const [sizes, setSizes] = React.useState(() => defaultApplicationSizes());
  const sizeOptions = ["AS SHOWN", "XS", "S", "M", "L", "XL", "XXL"];
  const photo = product ? product.photo : "assets/products/jersey-warriors.png";

  const updatePricing = (size, field, value) => {
    setPricing((prev) => {
      const next = { ...prev, [size]: { ...prev[size], [field]: value } };
      const baseNum = parseFloat(next[size].base) || 0;
      const dealerNum = parseFloat(next[size].dealer) || 0;
      const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
      next[size].retail = (baseNum + dealerAmt).toFixed(2);
      return next;
    });
  };

  const updateRow = (id, patch) => setSizes((rows) => rows.map((r) => r.id === id ? { ...r, ...patch } : r));

  const handleAddToStore = () => {
    const ts = Date.now();
    const photoKey = photo.replace("assets/products/", "").replace(".png", "");
    onAddToStore({
      id: `wx-${product ? product.id : "custom"}-${ts}`,
      title: ((product ? product.name : "Custom")) + " \u2014 Custom",
      style: (sport ? sport.label : "Sport") + " \u00b7 Uniform Jersey",
      photo: photoKey,
      adult: pricing.adult.retail,
      youth: pricing.youth.retail,
      teamProfit: parseFloat(pricing.adult.team) || 15,
      teamSetting,
      applicationSizes: sizes,
      pricing,
      sizesConfirmed: true,
    });
  };

  return (
    <div style={{ height: "100%", display: "flex", flexDirection: "column", overflow: "hidden" }}>
      <div style={{ flex: 1, display: "flex", overflow: "hidden" }}>
        {/* Left: product card */}
        <div style={{ width: 268, flexShrink: 0, borderRight: "1px solid #ececf0", padding: "24px 20px", display: "flex", flexDirection: "column" }}>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden" }}>
            <div style={{ height: 188, display: "flex", alignItems: "center", justifyContent: "center", background: "#f7f7f8", padding: 16 }}>
              <img src={photo} alt={product ? product.name : "Product"} style={{ maxHeight: "100%", maxWidth: "100%", objectFit: "contain" }} />
            </div>
            <div style={{ padding: "12px 16px 16px" }}>
              <div style={{ fontSize: 13, fontWeight: 800, color: "#0e0e10", letterSpacing: ".04em" }}>{product ? product.name.toUpperCase() : "CUSTOM"}</div>
              <div style={{ fontSize: 12, color: "#6b6b72", marginTop: 2 }}>{sport ? sport.label : "Sport"} \u00b7 Uniform Jersey</div>
              <button onClick={onBack} style={{ marginTop: 12, width: "100%", background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 0", fontSize: 12.5, fontWeight: 700, cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center", gap: 8, color: "#0e0e10" }}>
                <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg>
                Customize
              </button>
            </div>
          </div>
        </div>

        {/* Right: settings */}
        <div style={{ flex: 1, overflowY: "auto", padding: "24px 28px" }}>
          {/* Product Settings */}
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "16px 24px", marginBottom: 16 }}>
            <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 12, color: "#0e0e10" }}>Product Settings</div>
            {[{ key: "team-required", label: "Team Required", tip: "Mandatory gear rostered players must purchase to participate." }, { key: "team-supplied", label: "Team Supplied", tip: "Mandatory gear already paid for collectively — players pick sizes but pay $0." }].map((opt) => (
              <div key={opt.key} style={{ display: "flex", alignItems: "center", gap: 12, padding: "8px 0" }}>
                <BundleToggle on={teamSetting === opt.key} onChange={(v) => setTeamSetting(v ? opt.key : "fan-gear")} />
                <span style={{ fontSize: 14, color: "#0e0e10" }}>{opt.label}</span>
                <InfoTooltip title={opt.label} body={opt.tip} />
              </div>
            ))}
          </div>

          {/* Price settings — shared Governing-rule-3 card ($ / % mode) */}
          <PriceSettingsCard pricing={pricing} onChange={setPricing} fundOn={false} covered={teamSetting === "team-supplied"} />

          {/* Confirm Application Sizes */}
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "16px 24px" }}>
            <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 12, color: "#0e0e10" }}>Confirm Application Sizes</div>
            <div style={{ display: "grid", gridTemplateColumns: "140px 1fr 100px 1.3fr 1fr", gap: 12, fontSize: 11, fontWeight: 700, letterSpacing: ".10em", color: "#9a9aa1", paddingBottom: 12, borderBottom: "1px solid #ececf0" }}>
              <div>TYPE</div><div>DETAILS</div><div>COLORS</div><div>SELECT SIZES</div><div>NOTES</div>
            </div>
            {sizes.map((row) => (
              <div key={row.id} style={{ display: "grid", gridTemplateColumns: "140px 1fr 100px 1.3fr 1fr", gap: 12, padding: "16px 0", borderBottom: "1px solid #ececf0", alignItems: "flex-start" }}>
                <div>
                  <div style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10", letterSpacing: ".05em" }}>{row.type}</div>
                  <div style={{ fontSize: 11, color: "#6b6b72", marginTop: 4 }}>( {row.placement} )</div>
                </div>
                <div style={{ display: "flex", gap: 12, alignItems: "flex-start" }}>
                  <div style={{ width: 48, height: 48, border: "1px solid #ececf0", borderRadius: 4, display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 20, fontWeight: 700, color: "#0e0e10", flexShrink: 0 }}>
                    {row.iconKind === "hash" ? "#" : "T"}
                  </div>
                  <div style={{ fontSize: 11, color: "#0e0e10", lineHeight: 1.6, fontWeight: 500, letterSpacing: ".04em" }}>
                    {Object.entries(row.details).map(([k, v]) => (
                      <div key={k}><span style={{ color: "#9a9aa1", fontWeight: 600 }}>{k}</span><span style={{ color: "#9a9aa1" }}> : </span><span>{v}</span></div>
                    ))}
                  </div>
                </div>
                <div style={{ display: "flex", gap: 4, flexWrap: "wrap" }}>
                  {row.colors.map((c, i) => (
                    <div key={i} style={{ width: 32, height: 32, borderRadius: 4, background: c, border: c && c.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none" }} />
                  ))}
                </div>
                <div style={{ display: "flex", flexDirection: "column", gap: 4 }}>
                  <SizeRow label="ADULT" value={row.adultSize} options={sizeOptions} onChange={(v) => updateRow(row.id, { adultSize: v })} />
                  <SizeRow label="YOUTH" value={row.youthSize} options={sizeOptions} onChange={(v) => updateRow(row.id, { youthSize: v })} />
                </div>
                <div>
                  <textarea placeholder="Notes Here" value={row.notes} onChange={(e) => updateRow(row.id, { notes: e.target.value })}
                    style={{ width: "100%", minHeight: 64, resize: "vertical", border: "1px solid #d4d4d8", borderRadius: 4, padding: "8px 8px", fontSize: 12, fontFamily: "inherit", color: "#0e0e10", outline: 0 }} />
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      {/* Footer */}
      <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, padding: "16px 28px", borderTop: "1px solid #ececf0", background: "#fff", flexShrink: 0 }}>
        <button onClick={onCancel} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, color: "#0e0e10", cursor: "pointer" }}>Cancel</button>
        <button onClick={handleAddToStore} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "12px 28px", fontSize: 13, fontWeight: 800, cursor: "pointer" }}>Add to Store</button>
      </div>
    </div>
  );
}

function Customizer({ storeSport, onPick, onClose }) {
  const [gender, setGender] = React.useState(
    WOMEN_ONLY.includes(storeSport) ? "women" : "men"
  );

  const items = gender === "women" ? WOMEN_CATEGORIES : MEN_CATEGORIES;

  const handlePick = (item) => {
    onPick({ ...item, gender });
  };

  return (
    <div style={{ flex: 1, padding: "28px 36px 80px", background: "#fafafa", position: "relative", minHeight: "100%" }}>
      <div style={{ position: "absolute", top: 24, left: 28 }}>
        <button onClick={onClose} style={{
          background: "transparent", border: 0, cursor: "pointer", padding: 8,
          color: "#0e0e10", display: "inline-flex", alignItems: "center", gap: 8,
          fontSize: 14, fontWeight: 700, whiteSpace: "nowrap",
        }}>
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
          Back to store
        </button>
      </div>

      <div style={{ maxWidth: 1080, margin: "44px auto 0" }}>
        <GenderTabs gender={gender} onChange={setGender} />

        <div style={{
          display: "grid", gridTemplateColumns: "repeat(4, 1fr)",
          gap: 14, marginTop: 24,
        }}>
          {items.map((it) => (
            <SportCard key={gender + "-" + it.id} item={it} onPick={handlePick} />
          ))}
        </div>
      </div>
    </div>
  );
}

Object.assign(window, { Customizer });


/* ===== ASSET 24 : 0d86a7c2-f456-4a01-930c-12c3fd60a13d ===== */
// My Carts — the "Add From Cart" picker.
// Reached from the Add Product modal → "Add From Cart".

const SAMPLE_CARTS = [
{
  id: "c1", date: "March 2, 2026", time: "10:05 MST",
  name: "SKU Item Test", qty: 3, total: 156.00,
  statuses: ["rework", "active"], favorite: false
},
{
  id: "c2", date: "May 21, 2026", time: "14:52 MST",
  name: "Test", qty: 0, total: 0,
  statuses: [], favorite: false
},
{
  id: "c3", date: "May 21, 2026", time: "14:20 MST",
  name: "Test Team Store", qty: 2, total: 178.90,
  statuses: ["active"], favorite: false
},
{
  id: "c4", date: "April 14, 2026", time: "09:31 MST",
  name: "Carlsbad Hoyas — Jersey + Hat Bundle", qty: 12, total: 624.00,
  statuses: ["active"], favorite: true
},
{
  id: "c5", date: "April 02, 2026", time: "16:12 MST",
  name: "Spring Practice Tees", qty: 8, total: 312.50,
  statuses: [], favorite: false
}];


function MyCarts({ onClose, onLoadCart, onAddToStore, stores = [], activeStoreId }) {
  const [tab, setTab] = React.useState("saved");
  const [carts, setCarts] = React.useState(SAMPLE_CARTS);
  const [sort, setSort] = React.useState("None");
  const [search, setSearch] = React.useState("");
  const [toast, setToast] = React.useState(null);
  const [sortOpen, setSortOpen] = React.useState(false);
  const [addModalCart, setAddModalCart] = React.useState(null);
  const sortRef = React.useRef();
  React.useEffect(() => {
    const h = (e) => {if (sortRef.current && !sortRef.current.contains(e.target)) setSortOpen(false);};
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);

  const visible = React.useMemo(() => {
    let arr = carts.filter((c) => c.name.toLowerCase().includes(search.toLowerCase()));
    if (sort === "Newest") arr = [...arr].sort((a, b) => new Date(b.date) - new Date(a.date));
    if (sort === "Oldest") arr = [...arr].sort((a, b) => new Date(a.date) - new Date(b.date));
    if (sort === "Total (High → Low)") arr = [...arr].sort((a, b) => b.total - a.total);
    if (sort === "Total (Low → High)") arr = [...arr].sort((a, b) => a.total - b.total);
    return arr;
  }, [carts, search, sort]);

  const setFavorite = (id, v) => setCarts((cs) => cs.map((c) => c.id === id ? { ...c, favorite: v } : c));
  const remove = (id) => setCarts((cs) => cs.filter((c) => c.id !== id));

  return (
    <div style={{ flex: 1, padding: "28px 36px 80px", background: "#fff" }}>
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 24 }}>
        <div>
          <button onClick={onClose} style={{
            background: "transparent", border: 0, cursor: "pointer", padding: "4px 0",
            color: "#6b6b72", display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, marginBottom: 8
          }}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M15 18l-6-6 6-6" /></svg>
            Back to store
          </button>
          <h1 style={{ fontSize: 22, fontWeight: 800, letterSpacing: ".02em", margin: 0 }}>MY CARTS</h1>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <div ref={sortRef} style={{ position: "relative" }}>
            <button onClick={() => setSortOpen((v) => !v)} style={{
              background: "transparent", border: 0, cursor: "pointer", padding: "8px 0",
              display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, color: "#0e0e10"
            }}>
              <span style={{ color: "#6b6b72" }}>Sort by</span>
              <span style={{ fontWeight: 600 }}>{sort}</span>
              <IconChevron size={14} />
            </button>
            {sortOpen &&
            <div style={{ position: "absolute", top: "100%", right: 0, background: "#fff", border: "1px solid #ececf0", borderRadius: 6, boxShadow: "0 8px 24px rgba(20,20,30,.10)", minWidth: 180, padding: 4, zIndex: 30, marginTop: 4 }}>
                {["None", "Newest", "Oldest", "Total (High → Low)", "Total (Low → High)"].map((o) =>
              <div key={o} onClick={() => {setSort(o);setSortOpen(false);}} style={{
                padding: "8px 12px", fontSize: 13, cursor: "pointer", borderRadius: 4,
                background: sort === o ? "#fafafb" : "transparent"
              }}
              onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
              onMouseLeave={(e) => e.currentTarget.style.background = sort === o ? "#fafafb" : "transparent"}>{o}</div>
              )}
              </div>
            }
          </div>
          <input
            value={search}
            onChange={(e) => setSearch(e.target.value)}
            placeholder="Keyword search"
            style={{
              border: "1px solid #d4d4d8", borderRadius: 4, background: "#fff",
              padding: "12px 12px", fontSize: 13, width: 240, outline: 0
            }} />
          
        </div>
      </div>

      {
      <div>
          {/* Column headers */}
          <div style={{
          display: "grid",
          gridTemplateColumns: "1fr 2fr .6fr .8fr 1.4fr",
          gap: 16, padding: "16px 20px", fontSize: 11, fontWeight: 700, letterSpacing: ".10em", color: "#6b6b72",
          borderBottom: "1px solid #ececf0"
        }}>
            <div>DATE/TIME</div>
            <div style={{ textAlign: "center" }}>CART NAME</div>
            <div style={{ textAlign: "center" }}>QUANTITY</div>
            <div style={{ textAlign: "center" }}>TOTAL</div>
            <div style={{ textAlign: "center" }}>ACTIONS</div>
          </div>
          {visible.length === 0 ?
        <div style={{ padding: "80px 20px", textAlign: "center", color: "#6b6b72" }}>
              No carts match your search.
            </div> :

        visible.map((c) =>
        <CartRow key={c.id} cart={c}
        onFavorite={(v) => setFavorite(c.id, v)}
        onLoad={() => {onLoadCart(c);setToast(`Loaded "${c.name}"`);}}
        onAddToStore={() => setAddModalCart(c)}
        onDelete={() => remove(c.id)} />
        )
        }
        </div>
      }

      <AddCartToStoreModal
        open={!!addModalCart}
        cart={addModalCart}
        carts={carts}
        stores={stores}
        defaultStoreId={activeStoreId}
        onClose={() => setAddModalCart(null)}
        onConfirm={(payload) => {
          setAddModalCart(null);
          onAddToStore && onAddToStore(payload);
        }} />
      

      <Toast message={toast} onDone={() => setToast(null)} />
    </div>);

}

function AddCartToStoreModal({ open, cart, carts, stores, defaultStoreId, onClose, onConfirm }) {
  const [selectedCartId, setSelectedCartId] = React.useState(null);
  const [selectedStoreId, setSelectedStoreId] = React.useState(null);
  React.useEffect(() => {
    if (open) {
      setSelectedCartId(cart?.id || carts[0]?.id);
      setSelectedStoreId(defaultStoreId || stores[0]?.id);
    }
  }, [open, cart, defaultStoreId]);

  const cartOptions = carts.map((c) => ({ value: c.id, label: c.name }));
  const storeOptions = stores.map((s) => ({
    value: s.id,
    label: s.teamName || s.storeTitle || "Team Store",
    color: s.teamColors?.[0] || "#e1251b",
    logo: s.teamLogo?.url
  }));

  const selectedCart = carts.find((c) => c.id === selectedCartId);
  const selectedStore = stores.find((s) => s.id === selectedStoreId);

  return (
    <Modal open={open} onClose={onClose} width={560}>
      <div style={{ padding: "28px 28px 28px" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 28 }}>
          <h3 style={{ fontSize: 18, fontWeight: 700, margin: 0 }}>Add Saved Cart to Team Store</h3>
          <button onClick={onClose} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
            <IconX size={20} />
          </button>
        </div>

        <div style={{ marginBottom: 16 }}>
          <SelectField
            label="Saved Cart"
            value={selectedCartId}
            options={cartOptions}
            onChange={setSelectedCartId} />
          
        </div>

        <div style={{ marginBottom: 28 }}>
          <TeamStoreSelect
            value={selectedStoreId}
            options={storeOptions}
            onChange={setSelectedStoreId} />
          
        </div>

        <div style={{ display: "flex", justifyContent: "center" }}>
          <Button
            variant="primary"
            disabled={!selectedCartId || !selectedStoreId}
            onClick={() => onConfirm({ cart: selectedCart, store: selectedStore })}
            style={{ padding: "16px 28px", textTransform: "none", letterSpacing: 0, fontSize: 14, borderRadius: 6 }}>
            Add to Selected Store
          </Button>
        </div>
      </div>
    </Modal>);

}

function TeamStoreSelect({ value, options, onChange }) {
  const [open, setOpen] = React.useState(false);
  const [focus, setFocus] = React.useState(false);
  const ref = React.useRef();
  React.useEffect(() => {
    const h = (e) => {if (ref.current && !ref.current.contains(e.target)) setOpen(false);};
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  const filled = !!value;
  const float = focus || filled || open;
  const selected = options.find((o) => o.value === value);
  return (
    <div ref={ref} style={{ position: "relative" }}>
      <div
        tabIndex={0}
        onFocus={() => setFocus(true)}
        onBlur={() => setFocus(false)}
        onClick={() => setOpen((v) => !v)}
        style={{
          position: "relative",
          border: `1px solid ${open || focus ? "#0e0e10" : "#d4d4d8"}`,
          borderRadius: 4, background: "#fff",
          padding: "16px 40px 16px 16px",
          cursor: "pointer", userSelect: "none", minHeight: 56,
          display: "flex", alignItems: "center", gap: 12
        }}>
        <label style={{
          position: "absolute", left: 12, top: float ? -7 : 18,
          fontSize: float ? 11 : 13,
          color: open || focus ? "#0e0e10" : "#6b6b72",
          background: "#fff", padding: float ? "0 4px" : 0,
          pointerEvents: "none", transition: "all .12s ease",
          fontWeight: float ? 500 : 400
        }}>Select Team Store</label>
        {selected && <TeamBadge option={selected} />}
        <div style={{ fontSize: 14, color: "#0e0e10" }}>{selected?.label || ""}</div>
        <IconChevron size={18} style={{ position: "absolute", right: 12, top: "50%", transform: `translateY(-50%) rotate(${open ? 180 : 0}deg)`, transition: "transform .15s", color: "#6b6b72" }} />
      </div>
      {open &&
      <div style={{
        position: "absolute", top: "calc(100% + 4px)", left: 0, right: 0,
        background: "#fff", border: "1px solid #e5e5e9", borderRadius: 6,
        boxShadow: "0 8px 24px rgba(20,20,30,.10)", zIndex: 30,
        maxHeight: 280, overflowY: "auto"
      }}>
          {options.length === 0 ?
        <div style={{ padding: 16, color: "#9a9aa1", fontSize: 13 }}>No team stores yet.</div> :
        options.map((o) =>
        <div key={o.value}
        onMouseDown={(e) => {e.preventDefault();onChange(o.value);setOpen(false);}}
        style={{
          padding: "12px 16px", cursor: "pointer", fontSize: 14,
          background: o.value === value ? "#f7f7f8" : "transparent",
          display: "flex", alignItems: "center", gap: 12
        }}
        onMouseEnter={(e) => {if (o.value !== value) e.currentTarget.style.background = "#fafafb";}}
        onMouseLeave={(e) => {if (o.value !== value) e.currentTarget.style.background = "transparent";}}>
              <TeamBadge option={o} />
              <span>{o.label}</span>
              {o.value === value && <IconCheck size={16} style={{ marginLeft: "auto" }} />}
            </div>
        )}
        </div>
      }
    </div>);

}

function TeamBadge({ option }) {
  if (option.logo) {
    return <img src={option.logo} alt="" style={{ width: 28, height: 28, borderRadius: "50%", objectFit: "cover", border: "1px solid #ececf0", flexShrink: 0 }} />;
  }
  return (
    <span style={{
      width: 28, height: 28, borderRadius: "50%", background: option.color || "#e1251b",
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      color: "#fff", fontWeight: 800, fontSize: 11, flexShrink: 0,
      border: (option.color || "").toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "none"
    }}>{option.label?.[0]?.toUpperCase() || "T"}</span>);

}

function CartRow({ cart, onFavorite, onLoad, onAddToStore, onDelete }) {
  const statusStyle = {
    rework: { background: "#e1251b", color: "#fff" },
    active: { background: "#cfe8fb", color: "#1c5d99" }
  };
  return (
    <div style={{
      display: "grid",
      gridTemplateColumns: "1fr 2fr .6fr .8fr 1.4fr",
      gap: 16, padding: "24px 20px", alignItems: "center",
      borderBottom: "1px solid #f1f1f3",
      transition: "background .12s"
    }}
    onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
    onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>
      <div>
        <div style={{ fontSize: 14, fontWeight: 600, color: "#0e0e10" }}>{cart.date}</div>
        <div style={{ fontSize: 12, color: "#6b6b72", marginTop: 2 }}>{cart.time}</div>
      </div>
      <div style={{ textAlign: "center" }}>
        <div style={{ fontSize: 14, color: "#0e0e10", marginBottom: 8 }}>{cart.name}</div>
        <button style={{ background: "transparent", border: 0, cursor: "pointer", color: "#6b6b72", fontSize: 12, display: "inline-flex", alignItems: "center", gap: 4 }}>
          <IconEdit size={12} /> EDIT
        </button>
      </div>
      <div style={{ textAlign: "center", fontSize: 14, color: "#0e0e10" }}>
        {cart.qty || "—"}
      </div>
      <div style={{ textAlign: "center", fontSize: 14, color: "#0e0e10", fontWeight: 500 }}>
        ${cart.total.toFixed(2)}
      </div>
      <div style={{ display: "flex", flexDirection: "column", alignItems: "stretch", gap: 8, minWidth: 0 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 8, justifyContent: "flex-end" }}>
          <button onClick={() => onFavorite(!cart.favorite)} title="Favorite" style={{
            background: "transparent", border: 0, cursor: "pointer", padding: 4,
            display: "inline-flex", flexDirection: "column", alignItems: "center", color: cart.favorite ? "#f5b400" : "#9a9aa1"
          }}>
            <svg width="18" height="18" viewBox="0 0 24 24" fill={cart.favorite ? "currentColor" : "none"} stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round">
              <polygon points="12 2 15.1 8.3 22 9.3 17 14.1 18.2 21 12 17.8 5.8 21 7 14.1 2 9.3 8.9 8.3 12 2" />
            </svg>
            <span style={{ fontSize: 10, color: "#9a9aa1", marginTop: 2 }}>Favorite</span>
          </button>
          <div style={{ display: "flex", gap: 8, flexWrap: "wrap", justifyContent: "flex-end" }}>
            {cart.statuses.map((s) =>
            <span key={s} style={{
              ...statusStyle[s],
              padding: "4px 12px", borderRadius: 4, fontSize: 11, fontWeight: 700,
              textTransform: "capitalize"
            }}>{s}</span>
            )}
          </div>
        </div>
        <button onClick={onLoad} style={{
          background: "#fafafb", border: "1px solid #ececf0", borderRadius: 4,
          padding: "8px 16px", fontSize: 12, color: "#0e0e10", cursor: "pointer",
          display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, fontWeight: 600,
          transition: "background .12s"
        }}
        onMouseEnter={(e) => e.currentTarget.style.background = "#f1f1f3"}
        onMouseLeave={(e) => e.currentTarget.style.background = "#fafafb"}>
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M7 17L17 7" /><path d="M9 7h8v8" /></svg>
          LOAD CART
        </button>
        <button onClick={onAddToStore} style={{
          background: "#fafafb", border: "1px solid #ececf0", borderRadius: 4,
          padding: "8px 16px", fontSize: 12, color: "#0e0e10", cursor: "pointer",
          display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, fontWeight: 600,
          transition: "background .12s"
        }}
        onMouseEnter={(e) => {e.currentTarget.style.background = "#0e0e10";e.currentTarget.style.color = "#fff";e.currentTarget.style.borderColor = "#0e0e10";}}
        onMouseLeave={(e) => {e.currentTarget.style.background = "#fafafb";e.currentTarget.style.color = "#0e0e10";e.currentTarget.style.borderColor = "#ececf0";}}>
          <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M7 17L17 7" /><path d="M9 7h8v8" /></svg>
          ADD TO TEAM STORE
        </button>
        <button onClick={onDelete} style={{
          background: "#fafafb", border: "1px solid #ececf0", borderRadius: 4,
          padding: "8px 16px", fontSize: 12, color: "#0e0e10", cursor: "pointer",
          display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, fontWeight: 600,
          transition: "background .12s"
        }}
        onMouseEnter={(e) => {e.currentTarget.style.background = "#fff3f2";e.currentTarget.style.color = "#b81d14";e.currentTarget.style.borderColor = "#f5d0cd";}}
        onMouseLeave={(e) => {e.currentTarget.style.background = "#fafafb";e.currentTarget.style.color = "#0e0e10";e.currentTarget.style.borderColor = "#ececf0";}}>
          <IconTrash size={13} />
          DELETE
        </button>
      </div>
    </div>);

}

Object.assign(window, { MyCarts });

/* ===== ASSET 25 : 9b2c98d4-2f7a-4608-bb44-eacab3741176 ===== */
// Saved Designs — reached from Add Product modal → "Add Saved Design"

// Jersey / shorts SVG placeholders, parameterized by color.
function JerseyFront({ primary, secondary, accent, text = "HOYAS", number = "15", textColor }) {
  return (
    <svg viewBox="0 0 120 140" width="100%" height="100%">
      <path d="M30 18 L46 12 L54 22 L66 22 L74 12 L90 18 L96 36 L84 42 L82 122 L38 122 L36 42 L24 36 Z" fill={primary} stroke="#0e0e10" strokeWidth="0.6" />
      <path d="M30 18 L24 36 L36 42 L40 38 L30 18 Z" fill={secondary} />
      <path d="M90 18 L96 36 L84 42 L80 38 L90 18 Z" fill={secondary} />
      <path d="M46 12 L54 22 L66 22 L74 12 L72 18 L66 28 L54 28 L48 18 Z" fill={secondary} />
      <text x="60" y="62" textAnchor="middle" fontSize="11" fontWeight="800" fill={textColor || "#fff"} fontFamily="Inter, sans-serif">{text}</text>
      <text x="60" y="92" textAnchor="middle" fontSize="22" fontWeight="800" fill={textColor || "#fff"} fontFamily="Inter, sans-serif">{number}</text>
    </svg>
  );
}
function JerseyBack({ primary, secondary, accent, number = "15", textColor }) {
  return (
    <svg viewBox="0 0 120 140" width="100%" height="100%">
      <path d="M30 18 L46 12 L54 22 L66 22 L74 12 L90 18 L96 36 L84 42 L82 122 L38 122 L36 42 L24 36 Z" fill={primary} stroke="#0e0e10" strokeWidth="0.6" />
      <path d="M30 18 L24 36 L36 42 L40 38 L30 18 Z" fill={secondary} />
      <path d="M90 18 L96 36 L84 42 L80 38 L90 18 Z" fill={secondary} />
      <path d="M46 12 L54 22 L66 22 L74 12 L72 18 L66 28 L54 28 L48 18 Z" fill={secondary} />
      <text x="60" y="78" textAnchor="middle" fontSize="32" fontWeight="800" fill={textColor || "#fff"} fontFamily="Inter, sans-serif">{number}</text>
    </svg>
  );
}
function ShortsFront({ primary, secondary, accent }) {
  return (
    <svg viewBox="0 0 120 140" width="100%" height="100%">
      <ellipse cx="60" cy="18" rx="20" ry="10" fill="#fff" stroke="#0e0e10" strokeWidth="0.6" />
      <path d="M40 22 L80 22 L92 130 L66 130 L60 90 L54 130 L28 130 Z" fill={primary} stroke="#0e0e10" strokeWidth="0.6" />
      <path d="M40 26 L40 50 L42 130 L28 130 Z" fill={secondary} />
      <path d="M80 26 L80 50 L78 130 L92 130 Z" fill={secondary} />
      <rect x="50" y="30" width="20" height="6" rx="1" fill={accent || "#fff"} opacity="0.4" />
    </svg>
  );
}
function ShortsBack({ primary, secondary, accent }) {
  return (
    <svg viewBox="0 0 120 140" width="100%" height="100%">
      <ellipse cx="60" cy="18" rx="20" ry="10" fill="#fff" stroke="#0e0e10" strokeWidth="0.6" />
      <path d="M40 22 L80 22 L92 130 L66 130 L60 90 L54 130 L28 130 Z" fill={primary} stroke="#0e0e10" strokeWidth="0.6" />
      <path d="M40 26 L40 50 L42 130 L28 130 Z" fill={secondary} />
      <path d="M80 26 L80 50 L78 130 L92 130 Z" fill={secondary} />
    </svg>
  );
}

const SAVED_DESIGNS = [
  {
    id: "d1", title: "HOYAS WHITE", user: "Cody Scott",
    date: "March 18, 2026", time: "00:07 MST", salesRep: "—",
    sport: "Basketball", category: "Uniform Jersey",
    palette: { primary: "#e6e7e2", secondary: "#1d3a8a", textColor: "#1d3a8a", num: "15" },
    thumbs: [
      "assets/products/jersey-grizzlies.png",
      "assets/products/jersey-lancers.png",
      "assets/products/shorts-green.png",
      "assets/products/shorts-navy.png",
    ],
  },
  {
    id: "d2", title: "HOYAS DARK", user: "Cody Scott",
    date: "March 18, 2026", time: "00:11 MST", salesRep: "—",
    sport: "Basketball", category: "Uniform Jersey",
    palette: { primary: "#1d3a8a", secondary: "#142659", textColor: "#cfd6e6", num: "25" },
    thumbs: [
      "assets/products/jersey-warriors.png",
      "assets/products/jersey-asteroid.png",
      "assets/products/pants-navy.png",
      "assets/products/shorts-navy.png",
    ],
  },
  {
    id: "d3", title: "Basketball Sample", user: "Cody Scott",
    date: "February 02, 2026", time: "12:48 MST", salesRep: "Jordan Lee",
    sport: "Basketball", category: "Uniform Jersey",
    palette: { primary: "#0e0e10", secondary: "#e1251b", textColor: "#fff", num: "22", text: "FALCONS" },
    thumbs: [
      "assets/products/jersey-padres.png",
      "assets/products/jersey-grizzlies.png",
      "assets/products/pants-orange.png",
      "assets/products/shorts-green.png",
    ],
  },
];

function SavedDesigns({ onClose, onAddToStore, storeTeamName, stores = [], activeStoreId }) {
  const [sort, setSort] = React.useState("None");
  const [filter, setFilter] = React.useState("All");
  const [search, setSearch] = React.useState("");
  const [favs, setFavs] = React.useState({});
  const [toast, setToast] = React.useState(null);
  const [addModalDesign, setAddModalDesign] = React.useState(null);

  const visible = SAVED_DESIGNS.filter((d) => d.title.toLowerCase().includes(search.toLowerCase()));

  return (
    <div style={{ flex: 1, padding: "28px 36px 80px", background: "#fff" }}>
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", marginBottom: 28 }}>
        <div>
          <button onClick={onClose} style={{
            background: "transparent", border: 0, cursor: "pointer", padding: "4px 0",
            color: "#6b6b72", display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13, marginBottom: 8,
          }}>
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M15 18l-6-6 6-6"/></svg>
            Back to store
          </button>
          <h1 style={{ fontSize: 22, fontWeight: 800, letterSpacing: ".02em", margin: 0 }}>SAVED DESIGNS</h1>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <InlineDropdown label="Sort by" value={sort} options={["None", "Newest", "Oldest", "Title (A→Z)"]} onChange={setSort} />
          <InlineDropdown label="Filter by" value={filter} options={["All", "Basketball", "Baseball", "Football", "Soccer"]} onChange={setFilter} />
          <div style={{
            position: "relative", border: "1px solid #d4d4d8", borderRadius: 4,
            background: "#fff", width: 260, display: "flex", alignItems: "center",
          }}>
            <IconSearch size={14} style={{ position: "absolute", left: 12, color: "#9a9aa1" }} />
            <input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Keyword Search" style={{
              width: "100%", border: 0, outline: 0, background: "transparent",
              padding: "12px 12px 12px 36px", fontSize: 13,
            }} />
          </div>
        </div>
      </div>

      <div style={{
        display: "grid", gridTemplateColumns: "1.4fr 1.6fr 1fr",
        gap: 16, padding: "16px 20px", fontSize: 12, fontWeight: 700, letterSpacing: ".10em",
        color: "#6b6b72", borderBottom: "1px solid #ececf0", textAlign: "center",
      }}>
        <div style={{ textAlign: "left" }}>DESIGN INFORMATION</div>
        <div>THUMBNAILS</div>
        <div>ACTIONS</div>
      </div>

      {visible.map((d) => (
        <DesignRow key={d.id} design={d}
          fav={!!favs[d.id]}
          onFav={(v) => setFavs((f) => ({ ...f, [d.id]: v }))}
          onAddToStore={() => setAddModalDesign(d)} />
      ))}

      <AddDesignToStoreModal
        open={!!addModalDesign}
        design={addModalDesign}
        stores={stores}
        defaultStoreId={activeStoreId}
        onClose={() => setAddModalDesign(null)}
        onConfirm={(payload) => {
          setAddModalDesign(null);
          onAddToStore && onAddToStore(payload);
        }}
      />

      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
}

function AddDesignToStoreModal({ open, design, stores, defaultStoreId, onClose, onConfirm }) {
  const [selectedStoreId, setSelectedStoreId] = React.useState(null);
  React.useEffect(() => {
    if (open) setSelectedStoreId(defaultStoreId || stores[0]?.id);
  }, [open, defaultStoreId]);

  const storeOptions = stores.map((s) => ({
    value: s.id,
    label: s.teamName || s.storeTitle || "Team Store",
    color: s.teamColors?.[0] || "#e1251b",
    logo: s.teamLogo?.url,
  }));
  const selectedStore = stores.find((s) => s.id === selectedStoreId);

  return (
    <Modal open={open} onClose={onClose} width={520}>
      <div style={{ padding: "28px 28px 28px" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 28 }}>
          <h3 style={{ fontSize: 18, fontWeight: 700, margin: 0 }}>Add Product to Team Store</h3>
          <button onClick={onClose} style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#6b6b72" }}>
            <IconX size={20} />
          </button>
        </div>
        <div style={{ marginBottom: 28 }}>
          <TeamStoreSelect
            value={selectedStoreId}
            options={storeOptions}
            onChange={setSelectedStoreId}
          />
        </div>
        <div style={{ display: "flex", justifyContent: "center" }}>
          <Button
            variant="primary"
            disabled={!selectedStoreId}
            onClick={() => onConfirm({ design, store: selectedStore })}
            style={{ padding: "16px 28px", textTransform: "none", letterSpacing: 0, fontSize: 14, borderRadius: 6 }}>
            Add to Selected Store
          </Button>
        </div>
      </div>
    </Modal>
  );
}

function InlineDropdown({ label, value, options, onChange }) {
  const [open, setOpen] = React.useState(false);
  const ref = React.useRef();
  React.useEffect(() => {
    const h = (e) => { if (ref.current && !ref.current.contains(e.target)) setOpen(false); };
    document.addEventListener("mousedown", h);
    return () => document.removeEventListener("mousedown", h);
  }, []);
  return (
    <div ref={ref} style={{ position: "relative" }}>
      <button onClick={() => setOpen((v) => !v)} style={{
        background: "transparent", border: 0, cursor: "pointer",
        display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13,
        padding: "8px 0",
      }}>
        <span style={{ color: "#6b6b72" }}>{label}</span>
        <span style={{ color: "#0e0e10", fontWeight: 600 }}>{value}</span>
        <IconChevron size={14} style={{ color: "#6b6b72" }} />
      </button>
      {open && (
        <div style={{
          position: "absolute", top: "100%", left: 0, marginTop: 8,
          background: "#fff", border: "1px solid #ececf0", borderRadius: 6,
          boxShadow: "0 8px 24px rgba(20,20,30,.10)", minWidth: 160, padding: 4, zIndex: 30,
        }}>
          {options.map((o) => (
            <div key={o} onClick={() => { onChange(o); setOpen(false); }} style={{
              padding: "8px 12px", fontSize: 13, cursor: "pointer", borderRadius: 4,
              background: value === o ? "#fafafb" : "transparent",
            }}
              onMouseEnter={(e) => e.currentTarget.style.background = "#fafafb"}
              onMouseLeave={(e) => e.currentTarget.style.background = value === o ? "#fafafb" : "transparent"}>{o}</div>
          ))}
        </div>
      )}
    </div>
  );
}

function DesignRow({ design, fav, onFav, onAddToStore }) {
  const [hovering, setHovering] = React.useState(false);
  const p = design.palette;
  return (
    <div style={{
      display: "grid", gridTemplateColumns: "1.4fr 1.6fr 1fr",
      gap: 16, padding: "28px 20px", borderBottom: "1px solid #ececf0",
      alignItems: "flex-start",
      background: hovering ? "#fafafb" : "transparent",
      transition: "background .12s",
    }}
      onMouseEnter={() => setHovering(true)}
      onMouseLeave={() => setHovering(false)}>
      {/* Design info */}
      <div>
        <div style={{ fontSize: 15, fontWeight: 800, marginBottom: 16, letterSpacing: ".02em" }}>{design.title}</div>
        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, fontSize: 12 }}>
          <DesignField label="USER" value={design.user} />
          <DesignField label="SPORTS" value={design.sport} />
          <DesignField label="DATE / TIME" value={<>{design.date}<br/>{design.time}</>} />
          <DesignField label="CATEGORY" value={
            <span style={{ display: "inline-flex", alignItems: "center", gap: 8 }}>
              {design.category}
              <span title="Category info" style={{
                width: 16, height: 16, borderRadius: "50%", background: "#9a9aa1",
                color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center",
                fontSize: 9, fontWeight: 700, cursor: "help",
              }}>i</span>
            </span>
          } />
          <DesignField label="SALES REP" value={design.salesRep} />
        </div>
      </div>

      {/* Thumbnails */}
      <div style={{
        display: "grid", gridTemplateColumns: "1fr 1fr", gap: 0,
        maxWidth: 280, margin: "0 auto",
      }}>
        {(design.thumbs || []).map((src, i) => (
          <div key={i} style={{ height: 132, display: "flex", alignItems: "center", justifyContent: "center" }}>
            <img src={src} alt={design.title + " thumbnail " + (i + 1)}
              style={{ width: "100%", height: "100%", objectFit: "contain", display: "block" }} />
          </div>
        ))}
      </div>

      {/* Actions */}
      <div style={{ display: "flex", flexDirection: "column", gap: 8, alignItems: "center" }}>
        <button onClick={() => onFav(!fav)} style={{
          background: "transparent", border: 0, cursor: "pointer", padding: 4,
          display: "inline-flex", flexDirection: "column", alignItems: "center",
          color: fav ? "#f5b400" : "#9a9aa1", marginBottom: 4,
        }}>
          <svg width="18" height="18" viewBox="0 0 24 24" fill={fav ? "currentColor" : "none"} stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" strokeLinejoin="round">
            <polygon points="12 2 15.1 8.3 22 9.3 17 14.1 18.2 21 12 17.8 5.8 21 7 14.1 2 9.3 8.9 8.3 12 2" />
          </svg>
          <span style={{ fontSize: 10, color: "#9a9aa1", marginTop: 2 }}>Favorite</span>
        </button>
        <DesignActionButton icon={
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M7 17L17 7"/><path d="M9 7h8v8"/></svg>
        }>LOAD DESIGN</DesignActionButton>
        <DesignActionButton icon={
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><circle cx="18" cy="5" r="3"/><circle cx="6" cy="12" r="3"/><circle cx="18" cy="19" r="3"/><path d="M8.6 13.5l6.8 4M8.6 10.5l6.8-4"/></svg>
        }>SHARE DESIGN</DesignActionButton>
        <DesignActionButton icon={<IconCart size={14} />}>MOVE TO CART</DesignActionButton>
        <DesignActionButton icon={
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><rect x="3" y="4" width="18" height="4" rx="1"/><path d="M5 8v11a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V8"/><path d="M10 12h4"/></svg>
        }>ARCHIVE</DesignActionButton>
        <DesignActionButton primary onClick={onAddToStore} icon={
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"><path d="M3 9l1.5-5h15L21 9"/><path d="M4 9v10a1 1 0 0 0 1 1h14a1 1 0 0 0 1-1V9"/><path d="M3 9h18"/></svg>
        }>ADD TO TEAM STORE</DesignActionButton>
      </div>
    </div>
  );
}

function DesignField({ label, value }) {
  return (
    <div>
      <div style={{ fontSize: 10, fontWeight: 700, letterSpacing: ".10em", color: "#9a9aa1", marginBottom: 4 }}>{label}</div>
      <div style={{ fontSize: 12, color: "#0e0e10" }}>{value || "—"}</div>
    </div>
  );
}

function DesignActionButton({ children, icon, primary, onClick }) {
  const base = {
    background: "#fafafb", border: "1px solid #ececf0", borderRadius: 4,
    padding: "8px 16px", fontSize: 11, color: "#0e0e10", cursor: "pointer",
    display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8, fontWeight: 700,
    transition: "all .12s", width: "100%", letterSpacing: ".04em",
  };
  return (
    <button onClick={onClick} style={base}
      onMouseEnter={(e) => {
        if (primary) {
          e.currentTarget.style.background = "#0e0e10";
          e.currentTarget.style.color = "#fff";
          e.currentTarget.style.borderColor = "#0e0e10";
        } else {
          e.currentTarget.style.background = "#f1f1f3";
        }
      }}
      onMouseLeave={(e) => {
        e.currentTarget.style.background = "#fafafb";
        e.currentTarget.style.color = "#0e0e10";
        e.currentTarget.style.borderColor = "#ececf0";
      }}>
      {icon}
      {children}
    </button>
  );
}

Object.assign(window, { SavedDesigns });


/* ===== ASSET 26 : de13a619-155d-4d4e-9fcc-58e57b88f807 ===== */
// Overview mode — renders every major screen side-by-side as scaled iframes.
//
// Each cell loads index.html#frame=<id> in an iframe; app.jsx reads that hash
// on boot and jumps the state machine straight to that screen with seed data.

const OVERVIEW_FRAMES = [
  { id: "portal",                      label: "00 · Team Stores portal" },
  { id: "empty",                       label: "01 · Empty state" },
  { id: "form",                        label: "02 · Create form" },
  { id: "form-tax",                    label: "03 · Sales tax confirm" },
  { id: "loading",                     label: "04 · Creating store…" },
  { id: "list",                        label: "05 · Store list" },
  { id: "dashboard-general",           label: "06 · Dashboard / General" },
  { id: "dashboard-products-empty",    label: "07 · Products (empty)" },
  { id: "customizer",                  label: "08 · Customizer picker" },
  { id: "my-carts",                    label: "09 · My Carts" },
  { id: "saved-designs",               label: "10 · Saved Designs" },
  { id: "dashboard-products-configure",label: "11 · Configure pending" },
  { id: "dashboard-products-drafts",   label: "12 · Drafts / Published" },
];

// Native iframe viewport — the App is min-width: 1280
const FRAME_W = 1280;
const FRAME_H = 820;

function OverviewGrid({ onExit }) {
  // Responsive: pick a thumb width based on viewport (3 per row at >= 1500, 2 below)
  const [cols, setCols] = React.useState(() => (window.innerWidth >= 1500 ? 3 : 2));
  React.useEffect(() => {
    const onResize = () => setCols(window.innerWidth >= 1500 ? 3 : 2);
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, []);

  // Compute thumb width to fit cols across the available width
  const [containerW, setContainerW] = React.useState(window.innerWidth);
  React.useEffect(() => {
    const onResize = () => setContainerW(window.innerWidth);
    window.addEventListener("resize", onResize);
    return () => window.removeEventListener("resize", onResize);
  }, []);

  const GAP = 22;
  const PAD = 36;
  const usable = containerW - PAD * 2 - GAP * (cols - 1);
  const thumbW = Math.max(380, Math.floor(usable / cols));
  const scale = thumbW / FRAME_W;
  const thumbH = Math.round(FRAME_H * scale);

  return (
    <div style={{
      position: "fixed", inset: 0, background: "#f7f7f8",
      overflow: "auto", zIndex: 5,
    }}>
      {/* Top bar */}
      <div style={{
        position: "sticky", top: 0, zIndex: 2,
        background: "rgba(255,255,255,.92)",
        backdropFilter: "saturate(140%) blur(8px)",
        borderBottom: "1px solid #ececf0",
        padding: "16px 36px",
        display: "flex", alignItems: "center", justifyContent: "space-between",
      }}>
        <div style={{ display: "flex", alignItems: "baseline", gap: 16 }}>
          <strong style={{ fontSize: 14, letterSpacing: ".08em" }}>FRAME OVERVIEW</strong>
          <span style={{ color: "#6b6b72", fontSize: 12 }}>
            {OVERVIEW_FRAMES.length} screens · click any frame to open it
          </span>
        </div>
        <button onClick={onExit} style={{
          background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6,
          padding: "8px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer",
        }}>Exit overview</button>
      </div>

      {/* Grid */}
      <div style={{
        padding: `${PAD}px`,
        display: "grid",
        gridTemplateColumns: `repeat(${cols}, ${thumbW}px)`,
        gap: GAP,
        justifyContent: "center",
      }}>
        {OVERVIEW_FRAMES.map((f) => (
          <FrameCard key={f.id} frame={f} width={thumbW} height={thumbH} scale={scale} />
        ))}
      </div>
    </div>
  );
}

function FrameCard({ frame, width, height, scale }) {
  const src = `${location.pathname}#frame=${frame.id}`;
  const openFull = () => window.open(src, "_blank");
  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
      <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between" }}>
        <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".06em", color: "#0e0e10" }}>
          {frame.label.toUpperCase()}
        </span>
        <button onClick={openFull} style={{
          background: "transparent", border: 0, color: "#6b6b72",
          fontSize: 11, cursor: "pointer", padding: 0,
        }}>Open ↗</button>
      </div>

      <div
        onClick={openFull}
        title="Click to open in a new tab"
        style={{
          width, height,
          background: "#fff",
          border: "1px solid #e5e5e9",
          borderRadius: 10,
          overflow: "hidden",
          boxShadow: "0 1px 2px rgba(20,20,30,.04), 0 12px 32px rgba(20,20,30,.06)",
          cursor: "pointer",
          position: "relative",
        }}
      >
        <iframe
          src={src}
          title={frame.label}
          style={{
            width: FRAME_W,
            height: FRAME_H,
            border: 0,
            transform: `scale(${scale})`,
            transformOrigin: "top left",
            pointerEvents: "none",
            background: "#fff",
          }}
        />
        {/* Hover overlay */}
        <div className="frame-card-hover" style={{
          position: "absolute", inset: 0,
          background: "rgba(14,14,16,0)",
          transition: "background .15s",
          pointerEvents: "none",
        }} />
      </div>
    </div>
  );
}

Object.assign(window, { OverviewGrid, OVERVIEW_FRAMES });


/* ===== ASSET 27 : a34ed382-fee3-4ca2-8130-f42a4d6b7bcb ===== */
// onboarding-checklist.jsx
// "Kickoff Checklist" — a persistent, non-blocking onboarding panel that replaces
// the old guided-tooltip tour. It docks in a corner, never covers the working
// area, and lets a first-time Rep complete setup at their own pace.
//
// The four steps are the SAME milestones the old tour walked through, tracked by
// the exact same app CustomEvents (so no app wiring had to change):
//   tour:product-ready      → "Add & verify a product"  becomes complete
//   tour:product-published  → "Publish your product"     becomes complete
//   tour:roster-added       → "Build your roster"        becomes complete
//   tour:store-live         → "Launch your store"        becomes complete
//
// Completion is ALSO derived from the live store object (products / live flag)
// and persisted per-store to localStorage, so a refresh restores real progress.
//
// Clicking an incomplete step routes the Rep to the feature that completes it
// (via the onStepAction callback the app supplies).
//
// Exposes <OnboardingChecklist run store screen accent position onStepAction
//          onDismiss onComplete />.

// ─────────── Steps (verbatim from the old tooltip flow) ───────────
const CHECKLIST_STEPS = [
{
  id: "product-added",
  title: "Add & verify your first product",
  desc: "Customize a new design, pull from a saved cart, or drop in a saved design — then confirm the logo application sizes. Your draft becomes Ready.",
  cta: "Add a product"
},
{
  id: "product-published",
  title: "Publish your product",
  desc: "Your product is Ready. Publish it so it appears on your storefront — a store needs at least one published product before it can go live.",
  cta: "Publish a product"
},
{
  id: "roster",
  title: "Add your teams (optional)",
  desc: "Rosters are optional — front-load players by CSV, manual entry, or a share link, or launch now and let rosters back-fill from team-gear orders.",
  cta: "Open My Teams"
},
{
  id: "launch",
  title: "Launch your store",
  desc: "Take your store live. Share the link and team codes — team-gear orders can build your roster from there.",
  cta: "Launch your store"
}];


// ─────────── Accent presets (PROLOOK palette) ───────────
const CHECKLIST_ACCENTS = {
  red: { base: "#e1251b", deep: "#b81d14", soft: "#fbe7e5" },
  navy: { base: "#1d3a8a", deep: "#152c6b", soft: "#e4e9f6" },
  green: { base: "#1f9d55", deep: "#177a41", soft: "#e7f5ed" }
};
function resolveAccent(name) {
  return CHECKLIST_ACCENTS[name] || CHECKLIST_ACCENTS.red;
}

// ─────────── Derive completion from the live store object ───────────
function deriveDone(store) {
  const done = {};
  const products = store && store.products || [];
  if (products.length > 0) done["product-added"] = true;
  if (products.some((p) => p.status === "published")) {
    done["product-added"] = true;
    done["product-published"] = true;
  }
  if (store && store.live) {
    done["product-added"] = true;
    done["product-published"] = true;
    done["launch"] = true;
  }
  return done;
}

const ckKey = (id) => id ? "prolook_checklist_" + id : null;
function loadPersisted(id) {
  const k = ckKey(id);
  if (!k) return {};
  try {return JSON.parse(localStorage.getItem(k) || "{}");} catch (e) {return {};}
}
function savePersisted(id, done) {
  const k = ckKey(id);
  if (!k) return;
  try {localStorage.setItem(k, JSON.stringify(done));} catch (e) {}
}
const dismissKey = (id) => id ? "prolook_checklist_dismissed_" + id : null;

// ─────────── Circular completion indicator ───────────
function StepCircle({ done, accent, hovered }) {
  return (
    <span style={{
      width: 28, height: 28, borderRadius: "50%", flexShrink: 0,
      display: "inline-flex", alignItems: "center", justifyContent: "center",
      background: done ? accent.base : "#fff",
      border: done ? `1px solid ${accent.base}` : `2px solid ${hovered ? accent.base : "#cdcdd4"}`,
      color: "#fff", marginTop: 1,
      transition: "background .25s ease, border-color .2s ease, transform .25s ease",
      transform: done ? "scale(1)" : "scale(1)",
      boxShadow: done ? `0 2px 6px ${accent.base}40` : "none"
    }}>
      {done && <IconCheck size={14} style={{ strokeWidth: 3 }} />}
    </span>);

}

// ─────────── A single step row ───────────
function StepRow({ step, done, accent, last, onClick }) {
  const [hover, setHover] = React.useState(false);
  const clickable = !done;
  return (
    <div
      role={clickable ? "button" : undefined}
      tabIndex={clickable ? 0 : undefined}
      onClick={clickable ? onClick : undefined}
      onKeyDown={clickable ? (e) => {if (e.key === "Enter" || e.key === " ") {e.preventDefault();onClick();}} : undefined}
      onMouseEnter={() => setHover(true)}
      onMouseLeave={() => setHover(false)}
      style={{
        display: "flex", alignItems: "flex-start", gap: 12,
        padding: "12px 16px",
        borderBottom: last ? "none" : "1px solid #f1f1f3",
        cursor: clickable ? "pointer" : "default",
        background: clickable && hover ? "#faf9f9" : "transparent",
        transition: "background .15s ease",
        position: "relative"
      }}>
      
      <StepCircle done={done} accent={accent} hovered={clickable && hover} />
      <div style={{ flex: 1, minWidth: 0 }}>
        <div style={{
          display: "flex", alignItems: "center", gap: 8,
          fontSize: 14, fontWeight: 700, lineHeight: 1.3,
          color: done ? "#9a9aa1" : "#0e0e10",
          textDecoration: done ? "line-through" : "none",
          textDecorationColor: done ? "#c2c2c8" : "transparent"
        }}>
          {step.title}
        </div>
        <div style={{
          fontSize: 12.5, lineHeight: 1.5, marginTop: 4,
          color: done ? "#b6b6bd" : "#6b6b72", textWrap: "pretty"
        }}>
          {step.desc}
        </div>
        {clickable &&
        <span style={{
          display: "inline-flex", alignItems: "center", gap: 4,
          marginTop: 8, fontSize: 12, fontWeight: 700,
          color: accent.base,
          opacity: hover ? 1 : 0.82, transition: "opacity .15s, gap .15s",
          gap: hover ? 6 : 4
        }}>
            {step.cta}
            <IconChevronRight size={13} style={{ strokeWidth: 2.6 }} />
          </span>
        }
      </div>
    </div>);

}

// ─────────── Collapsed launcher (a compact progress pill) ───────────
function ChecklistLauncher({ pct, total, completed, accent, position, onExpand, allDone, bottom = 24 }) {
  const [hover, setHover] = React.useState(false);
  const size = 38,stroke = 4,r = (size - stroke) / 2,c = 2 * Math.PI * r;
  const corner = position === "left" ? { left: 24 } : { right: 24 };
  return (
    <button
      onClick={onExpand}
      onMouseEnter={() => setHover(true)}
      onMouseLeave={() => setHover(false)}
      style={{
        position: "fixed", bottom, ...corner, zIndex: 90,
        display: "flex", alignItems: "center", gap: 12,
        background: "#fff", border: "1px solid #e5e5e9", borderRadius: 999,
        padding: "8px 16px 8px 8px",
        boxShadow: hover ? "0 12px 32px rgba(20,20,30,.16)" : "0 6px 20px rgba(20,20,30,.10)",
        transform: hover ? "translateY(-1px)" : "translateY(0)",
        transition: "box-shadow .2s, transform .2s",
        fontFamily: '"Inter", system-ui, sans-serif'
      }}>
      
      <span style={{ position: "relative", width: size, height: size, flexShrink: 0 }}>
        <svg width={size} height={size} style={{ transform: "rotate(-90deg)" }}>
          <circle cx={size / 2} cy={size / 2} r={r} fill="none" stroke="#ececf0" strokeWidth={stroke} />
          <circle cx={size / 2} cy={size / 2} r={r} fill="none" stroke={accent.base} strokeWidth={stroke}
          strokeLinecap="round" strokeDasharray={c} strokeDashoffset={c * (1 - pct / 100)}
          style={{ transition: "stroke-dashoffset .5s cubic-bezier(.4,.9,.3,1)" }} />
        </svg>
        <span style={{
          position: "absolute", inset: 0, display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}>
          {allDone ?
          <IconCheck size={16} style={{ strokeWidth: 3, color: accent.base }} /> :
          <span style={{ fontSize: 10.5, fontWeight: 800, color: "#0e0e10" }}>{pct}%</span>}
        </span>
      </span>
      <span style={{ textAlign: "left" }}>
        <span style={{ display: "block", fontSize: 13, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>
          {allDone ? "Setup complete" : "Setup checklist"}
        </span>
        <span style={{ display: "block", fontSize: 11.5, fontWeight: 600, color: "#9a9aa1", marginTop: 1 }}>
          {completed} of {total} steps done
        </span>
      </span>
      <span style={{ display: "inline-flex", color: "#9a9aa1", marginLeft: 2, transform: "rotate(180deg)" }}>
        <IconChevron size={16} style={{ strokeWidth: 2.4 }} />
      </span>
    </button>);

}

// ─────────── The expanded checklist card ───────────
function ChecklistCard({ store, done, accent, position, onStep, onCollapse, onDismiss, bottom = 24 }) {
  const total = CHECKLIST_STEPS.length;
  const completed = CHECKLIST_STEPS.filter((s) => done[s.id]).length;
  const pct = Math.round(completed / total * 100);
  const allDone = completed === total;
  const teamName = store && (store.teamName || store.storeTitle) || "your team";
  const corner = position === "left" ? { left: 24 } : { right: 24 };

  return (
    <div style={{
      position: "fixed", bottom, ...corner, zIndex: 90,
      width: 380, maxWidth: "calc(100vw - 48px)", maxHeight: `calc(100vh - ${bottom + 24}px)`,
      display: "flex", flexDirection: "column",
      background: "#fff", borderRadius: 14, overflow: "hidden",
      boxShadow: "0 18px 50px rgba(20,20,30,.20), 0 0 0 1px rgba(20,20,30,.06)",
      fontFamily: '"Inter", system-ui, sans-serif',
      animation: "ckPop .28s cubic-bezier(.2,.9,.3,1.1)"
    }}>
      {/* Header */}
      <div style={{ ...{
          background: `linear-gradient(180deg, ${accent.base} 0%, ${accent.deep} 130%)`,
          color: "#fff", padding: "16px 20px 32px", position: "relative", flexShrink: 0
        }, background: "linear-gradient(rgb(225, 37, 27) 0%, rgb(184, 29, 20) 130%) center center" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 12 }}>
          <div style={{ minWidth: 0 }}>
            <div style={{
              fontSize: 10.5, fontWeight: 800, letterSpacing: ".14em", textTransform: "uppercase",
              opacity: 0.82, marginBottom: 8
            }}>
              Draft store · Setup
            </div>
            <h3 style={{ margin: 0, fontSize: 17, fontWeight: 800, letterSpacing: "-.01em", lineHeight: 1.25 }}>
              Let's get {teamName} ready for kickoff!
            </h3>
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 4, flexShrink: 0 }}>
            <span style={{ fontSize: 19, fontWeight: 800, lineHeight: 1, letterSpacing: "-.02em" }}>{pct}%</span>
            <button onClick={onCollapse} aria-label="Collapse checklist" style={{
              background: "rgba(255,255,255,.16)", border: 0, cursor: "pointer",
              color: "#fff", borderRadius: 7, padding: 8, display: "inline-flex", marginLeft: 8
            }}
            onMouseEnter={(e) => e.currentTarget.style.background = "rgba(255,255,255,.28)"}
            onMouseLeave={(e) => e.currentTarget.style.background = "rgba(255,255,255,.16)"}>
              <IconChevron size={16} style={{ strokeWidth: 2.6 }} />
            </button>
          </div>
        </div>
      </div>

      {/* Progress bar — a white pill straddling the header/body seam */}
      <div style={{ position: "relative", height: 0, flexShrink: 0 }}>
        <div style={{
          position: "absolute", left: 20, right: 20, top: -12,
          height: 12, borderRadius: 999, background: "#fff",
          padding: 4, boxShadow: "0 2px 8px rgba(20,20,30,.12)"
        }}>
          <div style={{
            height: "100%", borderRadius: 999,
            width: `${pct}%`, minWidth: pct > 0 ? 6 : 0,
            background: `linear-gradient(90deg, ${accent.base}, ${accent.deep})`,
            transition: "width .55s cubic-bezier(.4,.9,.3,1)"
          }} />
        </div>
      </div>

      {/* Steps */}
      <div style={{ paddingTop: 16, flex: 1, minHeight: 0, overflowY: "auto" }}>
        {CHECKLIST_STEPS.map((s, i) =>
        <StepRow
          key={s.id}
          step={s}
          done={!!done[s.id]}
          accent={accent}
          last={i === CHECKLIST_STEPS.length - 1}
          onClick={() => onStep(s.id)} />

        )}
      </div>

      {/* Done banner */}
      {allDone &&
      <div style={{
        display: "flex", alignItems: "center", gap: 12, margin: "0 16px 4px",
        padding: "12px 16px", borderRadius: 10, background: accent.soft
      }}>
          <span style={{
          width: 32, height: 32, borderRadius: 9, flexShrink: 0, background: accent.base, color: "#fff",
          display: "inline-flex", alignItems: "center", justifyContent: "center"
        }}>
            <IconTrophy size={17} style={{ strokeWidth: 1.9 }} />
          </span>
          <div style={{ fontSize: 12.5, lineHeight: 1.45, color: accent.deep, fontWeight: 600 }}>
            {teamName} is live and ready for kickoff. Nicely done — you cleared every setup step.
          </div>
        </div>
      }

      {/* Footer */}
      <div style={{ padding: "12px 16px 16px", textAlign: "center", borderTop: "1px solid #f1f1f3", marginTop: 8, flexShrink: 0 }}>
        <button onClick={onDismiss} style={{
          background: "transparent", border: 0, cursor: "pointer",
          fontSize: 12.5, fontWeight: 600, color: "#9a9aa1", fontFamily: "inherit", padding: "4px 8px"
        }}
        onMouseEnter={(e) => e.currentTarget.style.color = "#6b6b72"}
        onMouseLeave={(e) => e.currentTarget.style.color = "#9a9aa1"}>
          {allDone ? "Dismiss checklist" : "Dismiss checklist"}
        </button>
      </div>
    </div>);

}

// ─────────── Controller ───────────
function OnboardingChecklist({ run, store, screen, accent: accentName = "red", position = "right", onStepAction, onDismiss, onComplete }) {
  const accent = React.useMemo(() => resolveAccent(accentName), [accentName]);
  const storeId = store && store.id;

  const [open, setOpen] = React.useState(false); // controller active?
  const [collapsed, setCollapsed] = React.useState(false); // expanded vs launcher pill
  const [done, setDone] = React.useState({});
  const completedRef = React.useRef(0);

  // Start / reset when the run flag (a fresh first store) flips.
  React.useEffect(() => {
    if (run) {
      const dismissed = (() => {try {return localStorage.getItem(dismissKey(storeId)) === "true";} catch (e) {return false;}})();
      if (dismissed) {setOpen(false);return;}
      const t = setTimeout(() => {
        setDone({ ...deriveDone(store), ...loadPersisted(storeId) });
        setCollapsed(false);
        setOpen(true);
      }, 450);
      return () => clearTimeout(t);
    }
    setOpen(false);
    // eslint-disable-next-line
  }, [run, storeId]);

  // Re-merge whenever the live store object changes (products added, went live).
  React.useEffect(() => {
    if (!open) return;
    setDone((cur) => {
      const merged = { ...cur, ...deriveDone(store), ...loadPersisted(storeId) };
      // keep anything already true
      Object.keys(cur).forEach((k) => {if (cur[k]) merged[k] = true;});
      return merged;
    });
    // eslint-disable-next-line
  }, [store, open]);

  // Mark a step done + persist.
  const mark = React.useCallback((ids) => {
    setDone((cur) => {
      const next = { ...cur };
      (Array.isArray(ids) ? ids : [ids]).forEach((id) => {next[id] = true;});
      savePersisted(storeId, next);
      return next;
    });
  }, [storeId]);

  // Listen to the SAME completion events the old tour used.
  React.useEffect(() => {
    if (!open) return;
    const onReady = () => mark("product-added");
    const onPublished = () => mark(["product-added", "product-published"]);
    const onRoster = () => mark("roster");
    const onLive = () => mark(["product-added", "product-published", "launch"]);
    window.addEventListener("tour:product-ready", onReady);
    window.addEventListener("tour:product-published", onPublished);
    window.addEventListener("tour:roster-added", onRoster);
    window.addEventListener("tour:store-live", onLive);
    return () => {
      window.removeEventListener("tour:product-ready", onReady);
      window.removeEventListener("tour:product-published", onPublished);
      window.removeEventListener("tour:roster-added", onRoster);
      window.removeEventListener("tour:store-live", onLive);
    };
  }, [open, mark]);

  // Fire onComplete once all four are done.
  const completed = CHECKLIST_STEPS.filter((s) => done[s.id]).length;
  React.useEffect(() => {
    if (open && completed === CHECKLIST_STEPS.length && completedRef.current < CHECKLIST_STEPS.length) {
      onComplete && onComplete();
    }
    completedRef.current = completed;
  }, [completed, open, onComplete]);

  const handleStep = React.useCallback((id) => {
    onStepAction && onStepAction(id);
  }, [onStepAction]);

  const dismiss = React.useCallback(() => {
    try {localStorage.setItem(dismissKey(storeId), "true");} catch (e) {}
    setOpen(false);
    onDismiss && onDismiss();
  }, [storeId, onDismiss]);

  if (!open) return <ChecklistStyles />;

  const total = CHECKLIST_STEPS.length;
  const pct = Math.round(completed / total * 100);
  const allDone = completed === total;
  // The dashboard has a fixed action/launch bar pinned to the bottom (~70px).
  // Lift the checklist above it so the corner button is never covered.
  const bottom = screen === "dashboard" ? 96 : 24;

  return (
    <>
      <ChecklistStyles />
      {collapsed ?
      <ChecklistLauncher
        pct={pct} total={total} completed={completed} accent={accent}
        position={position} allDone={allDone} bottom={bottom}
        onExpand={() => setCollapsed(false)} /> :


      <ChecklistCard
        store={store} done={done} accent={accent} position={position} bottom={bottom}
        onStep={handleStep}
        onCollapse={() => setCollapsed(true)}
        onDismiss={dismiss} />

      }
    </>);

}

function ChecklistStyles() {
  return (
    <style>{`
      @keyframes ckPop { from { transform: translateY(12px) scale(.985) } to { transform: none } }
    `}</style>);

}

// ─────────── Store Setup Guide — informational modal ───────────
// Plain-language orientation, not a checklist: no progress tracking, never a
// gate. Auto-shows once per rep; reopenable from the workspace header.
function StoreGuideModal({ open, onClose }) {
  if (!open) return null;
  const sec = (title, body) => (
    <div key={title} style={{ display: "flex", gap: 14, alignItems: "flex-start" }}>
      <span style={{ width: 6, height: 6, borderRadius: 999, background: "#e1251b", marginTop: 7, flexShrink: 0 }} />
      <div>
        <div style={{ fontSize: 13.5, fontWeight: 800, color: "#0e0e10", marginBottom: 3 }}>{title}</div>
        <div style={{ fontSize: 12.5, color: "#52525b", lineHeight: 1.6 }}>{body}</div>
      </div>
    </div>
  );
  return (
    <Modal open onClose={onClose} width={640}>
      <div style={{ padding: "28px 32px 28px" }}>
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 12 }}>
          <div>
            <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".12em", color: "#9a9aa1", textTransform: "uppercase", marginBottom: 6 }}>Store Setup Guide</div>
            <h2 style={{ margin: 0, fontSize: 22, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>How your store comes together</h2>
          </div>
          <button onClick={onClose} aria-label="Close" style={{ background: "none", border: 0, cursor: "pointer", color: "#6b6b72", padding: 4 }}><IconX size={20} /></button>
        </div>
        <div style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.55, margin: "10px 0 20px" }}>
          A quick orientation — nothing here is required, and you can reopen it anytime from the workspace header.
        </div>
        <div style={{ display: "flex", flexDirection: "column", gap: 16, marginBottom: 24 }}>
          {sec("The tabs", "Products holds your catalog. Orders tracks buyer purchases. My Team(s) manages rosters and team codes. Reports covers sales and fundraising. Settings edits everything you set at creation.")}
          {sec("How a product reaches the storefront", "Add it (Add Products), configure it (sizes, category, pricing), turn its Visibility on, then launch the store. Hidden items stay in your catalog but never show to buyers.")}
          {sec("Team Required vs. Team Supplied", "Team Required gear must be bought by rostered players at their own cost. Team Supplied gear is already paid for — players pick sizes and pay $0. Both file under the Team Gear category.")}
          {sec("The Stock Vault vs. a bundle", "The Stock Vault is your library of blank stock gear — Pro Stock and Sideline Stock. A bundle is buyer-facing: several catalog items sold together as one storefront line. A Catalog Template is a saved set of this store's own gear you can reuse in a later store.")}
          {sec("Launching, closing, re-opening", "Launch controls live in the workspace on the Settings tab. A store needs at least one visible product to launch; you can close and re-open it anytime.")}
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end" }}>
          <button onClick={onClose} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "12px 24px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Got it</button>
        </div>
      </div>
    </Modal>
  );
}

Object.assign(window, { OnboardingChecklist, CHECKLIST_STEPS, StoreGuideModal });

/* ===== ASSET 28 : 682236e4-0198-4f7b-9360-37fa1d19e7d3 ===== */
// Top-level app — state machine for the create flow + Tweaks integration.

const TWEAK_DEFAULTS = /*EDITMODE-BEGIN*/{
  "overviewMode": false,
  "overviewEmpty": false,
  "zeroState": false,
  "seedData": true,
  "hasStoreAccess": true,
  "bypassGate": false,
  "checklistAccent": "red",
  "checklistPosition": "right",
  "portalHeadline": "fulfillment",
  "portalHero": "cinematic",
  "portalCta": "manager",
  "portalCtaColor": "black",
  "wizardAccent": "red",
  "wizardAthletes": 30,
  "templateLayout": "sidebar",
  "ssoWelcome": true,
  "ssoMultiBrand": false,
  "ssoOriginBrand": "prolook"
}/*EDITMODE-END*/;

// Read the frame route from URL hash (used by overview iframes)
function getFrameRoute() {
  const m = (window.location.hash || "").match(/frame=([\w-]+)/);
  return m ? m[1] : null;
}

/* ===== My Account · Unified Profile ===== */
// Read-only profile mirrored from the Product Customizer identity the rep used
// to sign in. Personal Info & Security are strictly read-only; the only editable
// surface is Brand Access (manual brand authentication). "Edit Profile" is an
// external redirect to the Customizer account dashboard (new tab).

const ACCOUNT_PROFILE = {
  firstName: "Hingle",
  lastName: "McCringleberry",
  email: "hinglemycringle@prolook.com",
  countryCode: "+1",
  countryFlag: "🇺🇸",
  phone: "801 123 4567",
  country: "United States",
  state: "Utah",
  city: "Salt Lake City",
  zip: "84117",
  dealership: "Team Gear Athletics ABC",
  dealershipId: "TGA-40912",
};

// MVP showcase: ProLook only. Re-add entries to demo multi-brand connections:
// { id: "clout", name: "Clout", desc: "Performance & sublimation line", connected: true, sso: false },
// { id: "vantage", name: "Vantage Athletics", desc: "Sideline & spirit wear", connected: false, sso: false },
const ACCOUNT_BRANDS = [
  { id: "prolook", name: "Prolook", desc: "Primary customizer & catalog", connected: true, sso: true },
];

// Read-only labeled field mirroring the Customizer profile layout.
function ProfileReadField({ label, value, required, mono, dropdown, children }) {
  return (
    <div style={{ display: "flex", flexDirection: "column", gap: 8, minWidth: 0 }}>
      <label style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase", color: "#3f3f46", display: "inline-flex", alignItems: "center", gap: 4 }}>
        {label}
      </label>
      {children || (
        <div style={{
          position: "relative", background: "#f5f5f7", border: "1px solid #ececf0", borderRadius: 8,
          padding: "12px 16px", fontSize: 15, color: "#52525b", minHeight: 48, display: "flex", alignItems: "center",
          fontFamily: mono ? "JetBrains Mono, monospace" : "inherit", cursor: "not-allowed",
        }}>
          <span style={{ overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{value}</span>
          {dropdown && <IconChevron size={18} style={{ position: "absolute", right: 16, top: "50%", transform: "translateY(-50%)", color: "#a1a1aa" }} />}
        </div>
      )}
    </div>
  );
}

function AccountProfile({ profile = ACCOUNT_PROFILE, brands: brandsProp = ACCOUNT_BRANDS, onEditProfile, onToast, archivedStores = [], onManageStore, onUnarchive, onCreateTemplate }) {
  const [brands, setBrands] = React.useState(brandsProp);
  const [primaryId, setPrimaryId] = React.useState("prolook"); // brand profile info is pulled from
  const [hoverStar, setHoverStar] = React.useState(null);
  const [loginBrand, setLoginBrand] = React.useState(null); // brand pending sign-in
  const [creds, setCreds] = React.useState({ email: "", password: "" });
  const [signingIn, setSigningIn] = React.useState(false);
  const primaryBrand = brands.find((b) => b.id === primaryId) || CUSTOMIZER_BRANDS[primaryId] || { name: "Product Customizer" };

  const openLogin = (b) => { setCreds({ email: "", password: "" }); setLoginBrand(b); };
  const closeLogin = () => { if (!signingIn) setLoginBrand(null); };
  const submitLogin = (e) => {
    e && e.preventDefault();
    if (!loginBrand || !creds.email || !creds.password) return;
    setSigningIn(true);
    setTimeout(() => {
      const b = loginBrand;
      setBrands((bs) => bs.map((x) => x.id === b.id ? { ...x, connected: true, justAuthed: true } : x));
      setSigningIn(false);
      setLoginBrand(null);
      onToast && onToast(`${b.name} authenticated — you can now sell across this brand`);
    }, 850);
  };

  return (
    <div style={{ flex: 1, padding: "32px 40px 120px", background: "#fafafb" }}>
      <div style={{ maxWidth: 1120, margin: "0 auto" }}>
        {/* Page header */}
        <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 20, flexWrap: "wrap" }}>
          <div>
            <h1 style={{ fontSize: 32, fontWeight: 800, margin: 0, color: "#0e0e10", letterSpacing: "-.01em" }}>My Account</h1>
            <p style={{ fontSize: 15, color: "#6b6b72", margin: "8px 0 0", maxWidth: 620, lineHeight: 1.5 }}>
              Your profile is synced from the Product Customizer account you signed in with. Review your details below.
            </p>
          </div>
          <button onClick={onEditProfile} style={{
            border: "1px solid #d4d4d8", background: "#fff", color: "#0e0e10", borderRadius: 9,
            padding: "12px 20px", cursor: "pointer", fontSize: 14, fontWeight: 700,
            display: "inline-flex", alignItems: "center", gap: 8, flexShrink: 0,
          }}
            onMouseEnter={(e) => { e.currentTarget.style.borderColor = "#0e0e10"; }}
            onMouseLeave={(e) => { e.currentTarget.style.borderColor = "#d4d4d8"; }}>
            <IconEdit size={16} /> Edit Profile <IconExternal size={15} style={{ color: "#9a9aa1" }} />
          </button>
        </div>

        {/* Identity strip */}
        <div style={{ display: "flex", alignItems: "center", gap: 16, margin: "28px 0 28px" }}>
          <div style={{
            width: 64, height: 64, borderRadius: "50%", background: "#0e0e10", color: "#fff",
            display: "inline-flex", alignItems: "center", justifyContent: "center",
            fontSize: 22, fontWeight: 800, fontFamily: "Archivo, sans-serif", flexShrink: 0,
          }}>{(profile.firstName[0] || "") + (profile.lastName[0] || "")}</div>
          <div style={{ minWidth: 0 }}>
            <div style={{ fontSize: 19, fontWeight: 800, color: "#0e0e10" }}>{profile.firstName} {profile.lastName}</div>
            <div style={{ fontSize: 14, color: "#6b6b72", marginTop: 2, display: "inline-flex", alignItems: "center", gap: 8 }}>
              <IconMail size={15} /> {profile.email}
            </div>
          </div>
        </div>

        <div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
          {/* ─── Card A — Personal Info & Security (read-only) ─── */}
          <SettingsCard style={{ padding: "32px 36px" }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 4 }}>
              <h2 style={{ fontSize: 20, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Personal Info</h2>
              <span style={{
                display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12, fontWeight: 700,
                letterSpacing: ".03em", color: "#6b6b72", background: "#f1f1f3", border: "1px solid #e5e5e9",
                borderRadius: 999, padding: "4px 12px",
              }}>
                <IconLock size={13} /> Read-only
              </span>
            </div>
            <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 28px" }}>Pulled directly from your primary brand, <strong style={{ color: "#09090b", fontWeight: 700 }}>{primaryBrand.name}</strong>. To edit these details, update them in your {primaryBrand.name} Customizer account or change your primary brand below.</p>

            <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "24px 36px" }}>
              <ProfileReadField label="First Name" value={profile.firstName} required />
              <ProfileReadField label="Last Name" value={profile.lastName} required />
              <ProfileReadField label="Email Address" value={profile.email} required />
              <ProfileReadField label="Phone Number" required>
                <div style={{ display: "flex", background: "#f5f5f7", border: "1px solid #ececf0", borderRadius: 8, overflow: "hidden", minHeight: 48, cursor: "not-allowed" }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 8, padding: "0 16px", borderRight: "1px solid #e5e5e9" }}>
                    <span style={{ fontSize: 18, lineHeight: 1 }}>{profile.countryFlag}</span>
                    <span style={{ fontSize: 15, color: "#52525b" }}>{profile.countryCode}</span>
                    <IconChevron size={16} style={{ color: "#a1a1aa" }} />
                  </div>
                  <div style={{ display: "flex", alignItems: "center", padding: "0 16px", fontSize: 15, color: "#52525b" }}>{profile.phone}</div>
                </div>
              </ProfileReadField>
              <ProfileReadField label="Country" value={profile.country} required dropdown />
              <ProfileReadField label="State" value={profile.state} required dropdown />
              <ProfileReadField label="City" value={profile.city} required dropdown />
              <ProfileReadField label="Zip Code" value={profile.zip} required mono />
            </div>

            {/* Security context notice */}
            <div style={{
              display: "flex", alignItems: "flex-start", gap: 12, marginTop: 28,
              background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 10, padding: "16px 16px",
            }}>
              <span style={{ width: 32, height: 32, borderRadius: 8, background: "#0e0e10", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
                <IconLock size={16} />
              </span>
              <div style={{ fontSize: 13.5, color: "#52525b", lineHeight: 1.55, paddingTop: 2 }}>
                To update your password or change your personal data, please update your account settings directly in the{" "}
                <button onClick={onEditProfile} style={{ border: 0, background: "transparent", padding: 0, color: "#0e0e10", fontWeight: 700, cursor: "pointer", textDecoration: "underline", textUnderlineOffset: 2, fontSize: 13.5 }}>Product Customizer platform</button>.
              </div>
            </div>
          </SettingsCard>

          {/* ─── Card B — Brand Access & Authentication (editable) ─── */}
          <SettingsCard style={{ padding: "32px 36px" }}>
            <h2 style={{ fontSize: 20, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Brand Access &amp; Authentication</h2>
            <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 24px" }}>The corporate entity you're attached to and the brands you're authorized to sell.</p>

            {/* Dealership callout */}
            <div style={{
              display: "flex", alignItems: "center", gap: 16, background: "#0e0e10", borderRadius: 12, padding: "16px 24px",
            }}>
              <span style={{ width: 44, height: 44, borderRadius: 10, background: "rgba(255,255,255,.12)", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
                <IconShield size={22} />
              </span>
              <div style={{ minWidth: 0, flex: 1 }}>
                <div style={{ fontSize: 11.5, fontWeight: 700, letterSpacing: ".1em", textTransform: "uppercase", color: "#a1a1aa" }}>Dealership</div>
                <div style={{ fontSize: 17, fontWeight: 800, color: "#fff", marginTop: 4 }}>{profile.dealership}</div>
              </div>
              <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12.5, fontWeight: 700, color: "#4ade80", background: "rgba(74,222,128,.12)", borderRadius: 999, padding: "8px 12px", flexShrink: 0 }}>
                <IconCircleCheck size={15} /> Verified
              </span>
            </div>

            {/* Brand list */}
            <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".06em", textTransform: "uppercase", color: "#6b6b72", margin: "28px 0 12px" }}>Authorized Brands</div>
            <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
              {brands.map((b) => {
                const isPrimary = primaryId === b.id;
                const canPrimary = b.connected;
                const hovered = hoverStar === b.id;
                return (
                <div key={b.id} style={{
                  display: "flex", alignItems: "center", gap: 16,
                  border: `1px solid ${isPrimary ? "#f0c64a" : "#ececf0"}`, borderRadius: 12, padding: "16px 20px",
                  background: isPrimary ? "#fffdf5" : "#fff",
                }}>
                  {/* Primary-brand star */}
                  <div style={{ position: "relative", flexShrink: 0, display: "flex" }}
                    onMouseEnter={() => setHoverStar(b.id)} onMouseLeave={() => setHoverStar(null)}>
                    <button
                      onClick={() => canPrimary && setPrimaryId(b.id)}
                      aria-label={isPrimary ? "Primary brand" : "Set as primary brand"}
                      style={{
                        border: 0, background: "transparent", padding: 4, lineHeight: 0,
                        cursor: canPrimary ? "pointer" : "not-allowed",
                        opacity: canPrimary ? 1 : 0.4,
                      }}>
                      <svg width="22" height="22" viewBox="0 0 24 24"
                        fill={isPrimary ? "#f5b301" : "none"}
                        stroke={isPrimary ? "#f5b301" : "#a1a1aa"} strokeWidth="1.8" strokeLinejoin="round">
                        <path d="M12 2.5l2.9 6.1 6.6.6-5 4.4 1.5 6.5L12 17.2 5.5 20.6l1.5-6.5-5-4.4 6.6-.6L12 2.5z" />
                      </svg>
                    </button>
                    {hovered && (
                      <div style={{
                        position: "absolute", left: "50%", bottom: "calc(100% + 8px)", transform: "translateX(-50%)",
                        width: 212, background: "#0e0e10", color: "#fff", borderRadius: 8, padding: "8px 12px",
                        fontSize: 12.5, lineHeight: 1.45, fontWeight: 500, textAlign: "center", zIndex: 20,
                        boxShadow: "0 8px 22px rgba(20,20,30,.28)", pointerEvents: "none",
                      }}>
                        {!canPrimary
                          ? "Connect this brand to set it as your primary."
                          : isPrimary
                            ? "Your primary brand. Your profile details are pulled from here."
                            : "Make this your primary brand — the account your profile details are pulled from."}
                        <span style={{ position: "absolute", left: "50%", top: "100%", transform: "translateX(-50%)", width: 0, height: 0, borderLeft: "6px solid transparent", borderRight: "6px solid transparent", borderTop: "6px solid #0e0e10" }} />
                      </div>
                    )}
                  </div>

                  <span style={{
                    width: 48, height: 48, borderRadius: 10, background: "#f1f1f3", border: "1px solid #e5e5e9",
                    color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center",
                    fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", flexShrink: 0,
                  }}>{b.name[0]}</span>
                  <div style={{ minWidth: 0, flex: 1 }}>
                    <div style={{ fontSize: 15.5, fontWeight: 800, color: "#0e0e10", display: "inline-flex", alignItems: "center", gap: 8 }}>
                      {b.name}
                      {isPrimary && <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".05em", textTransform: "uppercase", color: "#a67c00", background: "#fdf1cf", border: "1px solid #f0d98a", borderRadius: 999, padding: "2px 8px" }}>Primary</span>}
                    </div>
                    <div style={{ fontSize: 13, color: "#6b6b72", marginTop: 2 }}>{b.desc}{b.connected && b.sso ? " · Single sign-on" : ""}</div>
                  </div>
                  {b.connected ? (
                    <span style={{ display: "inline-flex", alignItems: "center", gap: 8, fontSize: 13.5, fontWeight: 700, color: "#1f9d55", background: "#e7f5ed", border: "1px solid #c7ead4", borderRadius: 999, padding: "8px 16px", flexShrink: 0 }}>
                      <IconCheck size={16} /> Connected
                    </span>
                  ) : (
                    <button onClick={() => openLogin(b)} style={{
                      border: "1px solid #0e0e10", background: "#fff", color: "#0e0e10", borderRadius: 9,
                      padding: "8px 16px", cursor: "pointer", fontSize: 13.5, fontWeight: 700, flexShrink: 0,
                      display: "inline-flex", alignItems: "center", gap: 8,
                    }}
                      onMouseEnter={(e) => { e.currentTarget.style.background = "#0e0e10"; e.currentTarget.style.color = "#fff"; }}
                      onMouseLeave={(e) => { e.currentTarget.style.background = "#fff"; e.currentTarget.style.color = "#0e0e10"; }}>
                      <IconLock size={15} /> Log In to Brand
                    </button>
                  )}
                </div>
                );
              })}
            </div>
          </SettingsCard>

          {/* ─── Card C — Archived Stores (restore) ─── */}
          <SettingsCard style={{ padding: "32px 36px" }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, flexWrap: "wrap" }}>
              <div style={{ minWidth: 0 }}>
                <h2 style={{ fontSize: 20, fontWeight: 800, margin: 0, color: "#0e0e10" }}>Archived Stores</h2>
                <p style={{ fontSize: 14, color: "#6b6b72", margin: "8px 0 0", maxWidth: 620, lineHeight: 1.5 }}>
                  Permanently shut-down stores, kept as read-only records. Order history and reports are retained. Restore a store to move it back into your active stores list.
                </p>
              </div>
              {archivedStores.length > 0 && (
                <span style={{
                  display: "inline-flex", alignItems: "center", gap: 8, fontSize: 12.5, fontWeight: 700,
                  color: "#52525b", background: "#f1f1f3", border: "1px solid #e5e5e9", borderRadius: 999,
                  padding: "6px 14px", flexShrink: 0,
                }}>
                  {archivedStores.length} archived
                </span>
              )}
            </div>

            <div style={{ display: "flex", alignItems: "flex-start", gap: 12, margin: "20px 0 4px", background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 10, padding: "14px 16px" }}>
              <span style={{ width: 30, height: 30, borderRadius: 8, background: "#fdf1cf", color: "#92670b", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}>
                <IconLock size={15} />
              </span>
              <div style={{ fontSize: 13, color: "#52525b", lineHeight: 1.55, paddingTop: 2 }}>
                Archiving can&rsquo;t be self-reversed. Restoring an archived store is a <strong style={{ color: "#09090b", fontWeight: 700 }}>managing rep</strong> action.
              </div>
            </div>

            {archivedStores.length === 0 ? (
              <div style={{ display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", textAlign: "center", padding: "40px 20px 28px", color: "#9a9aa1" }}>
                <div style={{ width: 64, height: 64, borderRadius: "50%", background: "#f5f5f7", border: "1px solid #ececf0", display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}>
                  <svg width="30" height="30" viewBox="0 0 24 24" fill="none" stroke="#a1a1aa" strokeWidth="1.9" strokeLinecap="round" strokeLinejoin="round" aria-hidden="true">
                    <rect x="3" y="4" width="18" height="4" rx="1" /><path d="M5 8v11a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V8" /><path d="M10 12h4" />
                  </svg>
                </div>
                <div style={{ fontSize: 14.5, fontWeight: 700, color: "#52525b" }}>No archived stores</div>
                <div style={{ fontSize: 13, color: "#9a9aa1", marginTop: 4 }}>Stores you archive from the Stores tab will appear here.</div>
              </div>
            ) : (
              <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(248px, 1fr))", gap: 16, marginTop: 20 }}>
                {archivedStores.map((s) => (
                  <StoreCard
                    key={s.id}
                    store={s}
                    archived
                    onManage={() => onManageStore && onManageStore(s.id)}
                    onView={() => onManageStore && onManageStore(s.id)}
                    onUnarchive={() => onUnarchive && onUnarchive(s.id)}
                    onCreateTemplate={onCreateTemplate}
                  />
                ))}
              </div>
            )}
          </SettingsCard>
        </div>
      </div>

      {/* Brand sign-in modal */}
      <Modal open={!!loginBrand} onClose={closeLogin} width={440}>
        {loginBrand && (
          <form onSubmit={submitLogin} style={{ padding: "32px 36px 32px", position: "relative" }}>
            <button type="button" onClick={closeLogin} style={{
              position: "absolute", top: 16, right: 16, border: 0, background: "transparent", cursor: "pointer",
              color: "#9a9aa1", width: 32, height: 32, borderRadius: 7, display: "inline-flex", alignItems: "center", justifyContent: "center",
            }} title="Close">
              <IconX size={18} />
            </button>

            {/* Brand logo + name */}
            <div style={{ display: "flex", flexDirection: "column", alignItems: "center", textAlign: "center", gap: 16, marginBottom: 24 }}>
              <span style={{
                width: 60, height: 60, borderRadius: 14, background: "#0e0e10", color: "#fff",
                display: "inline-flex", alignItems: "center", justifyContent: "center",
                fontSize: 26, fontWeight: 800, fontFamily: "Archivo, sans-serif",
              }}>{loginBrand.name[0]}</span>
              <div>
                <div style={{ fontSize: 20, fontWeight: 800, color: "#0e0e10" }}>Sign in to {loginBrand.name}</div>
                <div style={{ fontSize: 13.5, color: "#6b6b72", marginTop: 4, lineHeight: 1.5, maxWidth: 320 }}>
                  Enter your {loginBrand.name} credentials to authorize selling across this brand.
                </div>
              </div>
            </div>

            <div style={{ display: "flex", flexDirection: "column", gap: 16 }}>
              <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                <label style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".05em", textTransform: "uppercase", color: "#3f3f46" }}>Email</label>
                <input
                  type="email" autoFocus value={creds.email} disabled={signingIn}
                  onChange={(e) => setCreds((c) => ({ ...c, email: e.target.value }))}
                  placeholder={`you@${loginBrand.id}.com`}
                  style={{ border: "1px solid #d4d4d8", borderRadius: 8, padding: "12px 16px", fontSize: 15, outline: "none", width: "100%" }}
                  onFocus={(e) => e.target.style.borderColor = "#0e0e10"}
                  onBlur={(e) => e.target.style.borderColor = "#d4d4d8"} />
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                <label style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".05em", textTransform: "uppercase", color: "#3f3f46" }}>Password</label>
                <input
                  type="password" value={creds.password} disabled={signingIn}
                  onChange={(e) => setCreds((c) => ({ ...c, password: e.target.value }))}
                  placeholder="••••••••"
                  style={{ border: "1px solid #d4d4d8", borderRadius: 8, padding: "12px 16px", fontSize: 15, outline: "none", width: "100%" }}
                  onFocus={(e) => e.target.style.borderColor = "#0e0e10"}
                  onBlur={(e) => e.target.style.borderColor = "#d4d4d8"} />
              </div>
            </div>

            <button type="submit" disabled={!creds.email || !creds.password || signingIn} style={{
              marginTop: 24, width: "100%", border: 0, borderRadius: 9, padding: "16px",
              background: (!creds.email || !creds.password || signingIn) ? "#e5e5e9" : "#0e0e10",
              color: (!creds.email || !creds.password || signingIn) ? "#9a9aa1" : "#fff",
              fontSize: 15, fontWeight: 700, cursor: (!creds.email || !creds.password || signingIn) ? "default" : "pointer",
              display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 8,
            }}>
              {signingIn ? "Authenticating…" : <React.Fragment><IconLock size={16} /> Sign In</React.Fragment>}
            </button>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: 8, marginTop: 16, fontSize: 12.5, color: "#9a9aa1" }}>
              <IconLock size={12} /> Secured by {loginBrand.name} SSO
            </div>
          </form>
        )}
      </Modal>
    </div>
  );
}

/* ===== Auto-sign-in · Welcome & Primary Brand modal ===== */
// Interrupts the first landing on the Team Stores overview per session. Explains
// the silent SSO hand-off from the originating brand customizer and lets multi-
// brand accounts pick their primary brand before entering the workspace.
function WelcomeSSOModal({ open, originId = "prolook", accessibleIds = ACCESSIBLE_CUSTOMIZERS, multiBrand = true, onConfirm }) {
  const origin = CUSTOMIZER_BRANDS[originId] || CUSTOMIZER_BRANDS.prolook;
  const brands = (multiBrand ? accessibleIds : [originId]).map((id) => ({ id, ...CUSTOMIZER_BRANDS[id] })).filter((b) => b.name);
  const [primary, setPrimary] = React.useState(originId);
  React.useEffect(() => { if (open) setPrimary(originId); }, [open, originId]);
  const isMulti = multiBrand && brands.length > 1;

  const brandGlyph = (b, size) => (
    b.logoBlack || b.logoInk
      ? <img src={b.logoBlack || b.logoInk} alt={b.name} style={{ height: size, width: "auto", objectFit: "contain" }} />
      : <span style={{ fontSize: Math.round(size * 0.62), fontWeight: 800, letterSpacing: "-.3px", color: "#0e0e10", lineHeight: 1, textAlign: "center" }}>{b.wordmark || b.name}</span>
  );

  return (
    <Modal open={open} onClose={() => {}} width={isMulti ? 640 : 520}>
      <div style={{ padding: "40px 40px 32px", position: "relative" }}>
        {/* SSO badge */}
        <div style={{ display: "flex", justifyContent: "center", marginBottom: 20 }}>
          <span style={{
            display: "inline-flex", alignItems: "center", gap: 8, background: "#e7f5ed", border: "1px solid #c7ead4",
            color: "#1f9d55", borderRadius: 999, padding: "8px 16px", fontSize: 12.5, fontWeight: 700, letterSpacing: ".03em",
          }}>
            <IconCircleCheck size={15} /> Signed in automatically
          </span>
        </div>

        <h2 style={{ margin: "0 0 12px", fontSize: 28, fontWeight: 800, letterSpacing: "-.4px", color: "#09090b", textAlign: "center", fontFamily: "Archivo, sans-serif" }}>
          Welcome to Team Stores
        </h2>
        <p style={{ margin: "0 auto", maxWidth: 440, fontSize: 15, lineHeight: 1.6, color: "#52525b", textAlign: "center" }}>
          We've automatically signed you in using your <strong style={{ color: "#09090b", fontWeight: 700 }}>{origin.name}</strong> Customizer account and set it as your primary brand for this workspace session.
        </p>

        {isMulti ? (
          <div style={{ marginTop: 32 }}>
            <div style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".1em", textTransform: "uppercase", color: "#71717a", marginBottom: 16 }}>Primary Brand Selection</div>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12 }}>
              {brands.map((b) => {
                const on = primary === b.id;
                return (
                  <button key={b.id} onClick={() => setPrimary(b.id)} title={b.name} style={{
                    position: "relative", aspectRatio: "1 / 1", display: "flex", flexDirection: "column",
                    alignItems: "center", justifyContent: "center", gap: 8, padding: 12,
                    background: on ? "#fff" : "#f4f4f5", border: `2px solid ${on ? "#09090b" : "#f4f4f5"}`,
                    borderRadius: 14, cursor: "pointer", transition: "border-color .12s, background .12s",
                  }}
                    onMouseEnter={(e) => { if (!on) { e.currentTarget.style.borderColor = "#d4d4d8"; e.currentTarget.style.background = "#fff"; } }}
                    onMouseLeave={(e) => { if (!on) { e.currentTarget.style.borderColor = "#f4f4f5"; e.currentTarget.style.background = "#f4f4f5"; } }}>
                    {on && <span style={{ position: "absolute", top: 8, right: 8, width: 20, height: 20, borderRadius: "50%", background: "#09090b", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center" }}><IconCheck size={13} /></span>}
                    <span style={{ height: 32, display: "flex", alignItems: "center" }}>{brandGlyph(b, 22)}</span>
                    <span style={{ fontSize: 11.5, fontWeight: 600, color: "#52525b", textAlign: "center", lineHeight: 1.2 }}>{b.name}</span>
                  </button>
                );
              })}
            </div>
            <p style={{ margin: "16px 0 0", fontSize: 12.5, color: "#a1a1aa", textAlign: "center" }}>You can change your primary brand anytime from My Account.</p>
          </div>
        ) : (
          <div style={{ marginTop: 28 }}>
            <div style={{ fontSize: 11.5, fontWeight: 800, letterSpacing: ".1em", textTransform: "uppercase", color: "#71717a", marginBottom: 12, textAlign: "center" }}>Your Primary Brand</div>
            <div style={{
              display: "flex", alignItems: "center", justifyContent: "center", gap: 16,
              background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 14, padding: "16px 24px", maxWidth: 340, margin: "0 auto",
            }}>
              <span style={{ height: 32, display: "flex", alignItems: "center" }}>{brandGlyph(origin, 24)}</span>
              <span style={{ width: 1, height: 28, background: "#e4e4e7" }} />
              <span style={{ fontSize: 15.5, fontWeight: 800, color: "#0e0e10" }}>{origin.name}</span>
              <span style={{ display: "inline-flex", alignItems: "center", gap: 4, color: "#1f9d55", fontSize: 12.5, fontWeight: 700 }}><IconCircleCheck size={15} /> Primary</span>
            </div>
          </div>
        )}

        {/* CTA */}
        <div style={{ display: "flex", justifyContent: "flex-end", marginTop: 32 }}>
          <button onClick={() => onConfirm(primary)} style={{
            border: 0, borderRadius: 9, padding: "16px 32px", background: "#09090b", color: "#fff",
            fontSize: 15, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 8,
          }}
            onMouseEnter={(e) => e.currentTarget.style.background = "#dc2626"}
            onMouseLeave={(e) => e.currentTarget.style.background = "#09090b"}>
            {isMulti ? "Confirm & Enter" : "Continue to Workspace"} <IconArrowRight size={17} />
          </button>
        </div>
      </div>
    </Modal>
  );
}

// ─────────── Created-store persistence ───────────
// User-created stores survive a reload: they serialize into localStorage on
// every change (React-element fields stripped) and rehydrate on boot.
const CREATED_STORES_KEY = "ts_created_stores_v1";
function loadCreatedStores() {
  try {
    const raw = JSON.parse(localStorage.getItem(CREATED_STORES_KEY) || "[]");
    return Array.isArray(raw) ? raw.map((s) => ({ ...s, __fresh: false })) : [];
  } catch (e) { return []; }
}
function saveCreatedStores(list) {
  try {
    localStorage.setItem(CREATED_STORES_KEY, JSON.stringify(list, (k, v) => {
      if (k === "thumbnail") return undefined; // React elements don't serialize
      if (v && typeof v === "object" && v.$$typeof) return undefined;
      return v;
    }));
  } catch (e) {}
}

function App() {
  const [tweaks, setTweak] = useTweaks(TWEAK_DEFAULTS);
  const frameRoute = React.useMemo(getFrameRoute, []);
  const isFrameRoute = !!frameRoute;

  // expose a global so deep components can route here without prop-drilling
  React.useEffect(() => {
    window.__openMyCarts = () => setScreen("my-carts");
    window.__openSavedDesigns = () => setScreen("saved-designs");
    return () => { delete window.__openMyCarts; delete window.__openSavedDesigns; };
  });

  // Global hook to kick off the onboarding demo (also used by the Tweak button).
  React.useEffect(() => {
    window.__startOnboardingDemo = () => startOnboardingDemo();
    return () => { delete window.__startOnboardingDemo; };
  });

  // State machine: overview | gate | list | leagues | empty | form | loading | dashboard | customizer
  // The Team Stores landing page is the entry point; reps arrive already on Overview.
  // A signed-in rep whose dealership has no access is handed off from the landing
  // page with ?access=none and lands straight on the business-details form.
  const [screen, setScreen] = React.useState(() => {
    try { if (new URLSearchParams(window.location.search).get("access") === "none") return "access-form"; } catch (e) {}
    return "overview";
  });
  const [navTab, setNavTab] = React.useState("overview");
  // Auto-sign-in welcome: originating brand comes from the ?brand= hand-off param
  // (falls back to the tweak). Interrupts the first overview landing per session.
  const ssoOriginId = React.useMemo(() => {
    try { const b = new URLSearchParams(window.location.search).get("brand"); if (b && CUSTOMIZER_BRANDS[b]) return b; } catch (e) {}
    return tweaks.ssoOriginBrand || "prolook";
  }, [tweaks.ssoOriginBrand]);
  const [showWelcome, setShowWelcome] = React.useState(false);
  const [showLogoutModal, setShowLogoutModal] = React.useState(false);
  const [ordersOrigin, setOrdersOrigin] = React.useState("overview"); // which tab the All-Stores Orders page was opened from
  const [primaryBrand, setPrimaryBrand] = React.useState(ssoOriginId);
  React.useEffect(() => {
    if (!tweaks.ssoWelcome) return;
    let seen = false;
    try { seen = sessionStorage.getItem("ts_sso_welcomed") === "1"; } catch (e) {}
    if (!seen) setShowWelcome(true);
  }, []);
  const confirmWelcome = (chosen) => {
    setPrimaryBrand(chosen || ssoOriginId);
    setShowWelcome(false);
    try { sessionStorage.setItem("ts_sso_welcomed", "1"); } catch (e) {}
  };
  // Session grant: a no-access user who passes the splash unlocks Team Stores
  // for the rest of the session.
  const [gatePassed, setGatePassed] = React.useState(false);
  const [showTaxModal, setShowTaxModal] = React.useState(false);
  const [showCancelModal, setShowCancelModal] = React.useState(false);
  const [pendingNav, setPendingNav] = React.useState(null);
  const [pendingData, setPendingData] = React.useState(null);
  const [stores, setStores] = React.useState(() => (frameRoute ? makeSeededStores() : makeSeededStores().concat(loadCreatedStores())));
  const [activeStoreId, setActiveStoreId] = React.useState(null);
  // Persist user-created stores (and any edits to them) across reloads.
  React.useEffect(() => { if (!frameRoute) saveCreatedStores(stores.filter((s) => s.__userCreated)); }, [stores, frameRoute]);
  const [toast, setToast] = React.useState(null);
  const [presetData, setPresetData] = React.useState(null);
  const [portalView, setPortalView] = React.useState(null); // TM portal preview (2.1)
  React.useEffect(() => { window.__openTmPortal = (inv, st) => setPortalView({ invite: inv, store: st }); return () => { delete window.__openTmPortal; }; }, []);
  const [showTemplateModal, setShowTemplateModal] = React.useState(false);
  const [templateMode, setTemplateMode] = React.useState(false);
  const [dashboardTab, setDashboardTab] = React.useState("general");
  const [leagues, setLeagues] = React.useState(() => makeSampleLeagues());
  const [activeLeagueId, setActiveLeagueId] = React.useState(null);
  const [activeGroupId, setActiveGroupId] = React.useState(null);
  const [showLeagueModal, setShowLeagueModal] = React.useState(false);
  const [showGroupModal, setShowGroupModal] = React.useState(false);
  const [userTemplates, setUserTemplates] = React.useState(() => loadUserTemplates());
  const [createTplStore, setCreateTplStore] = React.useState(null);

  // ─── Onboarding "Launchpad" tour ───
  // is_first_store: global flag. The guided tour runs only while it's true; it
  // flips permanently to false once the tour is dismissed or completed.
  const [isFirstStore, setIsFirstStore] = React.useState(() => {
    try { return localStorage.getItem("prolook_is_first_store") !== "false"; } catch (e) { return true; }
  });
  const [tourRun, setTourRun] = React.useState(false);
  // Store Setup Guide — informational modal; dismissal is per rep, not per store.
  const [guideOpen, setGuideOpen] = React.useState(false);
  const dismissGuide = () => { setGuideOpen(false); try { localStorage.setItem("ts_setup_guide_seen_v1", "1"); } catch (e) {} };
  React.useEffect(() => {
    if (screen !== "dashboard" || isFrameRoute) return;
    let seen = false;
    try { seen = localStorage.getItem("ts_setup_guide_seen_v1") === "1"; } catch (e) {}
    if (!seen) setGuideOpen(true);
  }, [screen]);
  React.useEffect(() => {
    window.__openStoreGuide = () => setGuideOpen(true);
    return () => { delete window.__openStoreGuide; };
  }, []);
  const persistFirstStore = (v) => {
    setIsFirstStore(v);
    try { localStorage.setItem("prolook_is_first_store", v ? "true" : "false"); } catch (e) {}
  };
  const endTour = () => { setTourRun(false); persistFirstStore(false); };

  // Route a checklist step to the feature that completes it.
  const handleChecklistStep = (stepId) => {
    setScreen("dashboard");
    if (stepId === "product-added" || stepId === "product-published") {
      setDashboardTab("products");
      setTimeout(() => window.__goToDashTab && window.__goToDashTab("products"), 30);
    } else if (stepId === "roster") {
      setDashboardTab("roster");
      setTimeout(() => window.__goToDashTab && window.__goToDashTab("roster"), 30);
    } else if (stepId === "launch") {
      setDashboardTab("general");
      setTimeout(() => { window.__goToDashTab && window.__goToDashTab("general"); window.__openLaunchModal && window.__openLaunchModal(); }, 60);
    }
  };

  // ─── Frame routing: hydrate state for the requested deep-link ───
  React.useEffect(() => {
    if (!frameRoute) return;
    const seeded = makeSeedStores();
    const firstId = seeded[0]?.id;

    switch (frameRoute) {
      case "empty":
        setStores([]);
        setScreen("empty");
        break;
      case "form":
        setStores([]);
        setPresetData(makePrefill());
        setScreen("form");
        break;
      case "form-tax":
        setStores([]);
        setPresetData(makePrefill());
        setScreen("form");
        setPendingData(makePrefill());
        setShowTaxModal(true);
        break;
      case "loading":
        setStores([]);
        setScreen("loading");
        break;
      case "portal":
        setStores(seeded);
        setScreen("portal");
        break;
      case "list":
        setStores(seeded);
        setScreen("list");
        break;
      case "dashboard-general":
        setStores(seeded);
        setActiveStoreId(firstId);
        setDashboardTab("general");
        setScreen("dashboard");
        break;
      case "dashboard-products-empty": {
        setStores(seeded);
        setActiveStoreId(firstId);
        setDashboardTab("products");
        setScreen("dashboard");
        break;
      }
      case "dashboard-products-configure": {
        const withPending = seeded.map((s, i) =>
          i === 0
            ? {
                ...s,
                pendingProducts: [
                  { __pendingId: "p-1", title: "Custom Football Jersey — Home", subtitle: "Sublimated Set-In, Full Button Jersey", source: "cart" },
                  { __pendingId: "p-2", title: "Custom Football Jersey — Away", subtitle: "Sublimated Set-In, 2-Button Jersey", source: "cart" },
                ],
              }
            : s
        );
        setStores(withPending);
        setActiveStoreId(firstId);
        setDashboardTab("products");
        setScreen("dashboard");
        break;
      }
      case "dashboard-products-drafts": {
        const withDrafts = seeded.map((s, i) =>
          i === 0
            ? {
                ...s,
                products: [
                  { id: "pr-1", title: "Custom Football Jersey — Home", subtitle: "Sublimated Set-In, Full Button Jersey", status: "published", teamSetting: "team-required" },
                  { id: "pr-2", title: "Custom Football Jersey — Away", subtitle: "Sublimated Set-In, 2-Button Jersey", status: "draft" },
                  { id: "pr-3", title: "Performance Hoodie",          subtitle: "Sublimated Traditional, Full-Length, Elastic Cuff Pant", status: "draft" },
                ],
              }
            : s
        );
        setStores(withDrafts);
        setActiveStoreId(firstId);
        setDashboardTab("products");
        setScreen("dashboard");
        break;
      }
      case "customizer":
        setStores(seeded);
        setActiveStoreId(firstId);
        setScreen("customizer");
        break;
      case "my-carts":
        setStores(seeded);
        setActiveStoreId(firstId);
        setScreen("my-carts");
        break;
      case "saved-designs":
        setStores(seeded);
        setActiveStoreId(firstId);
        setScreen("saved-designs");
        break;
      default:
        break;
    }
    // eslint-disable-next-line
  }, [frameRoute]);

  const updateStore = (id, patch) => {
    setStores((arr) => arr.map((s) => s.id === id ? { ...s, ...patch } : s));
  };

  // Archive confirmation triggered from a store card's actions menu.
  const [archiveTargetId, setArchiveTargetId] = React.useState(null);
  const archiveTargetStore = stores.find((s) => s.id === archiveTargetId);
  // Restore confirmation triggered from the My Account archived-stores list.
  const [restoreTargetId, setRestoreTargetId] = React.useState(null);
  const restoreTargetStore = stores.find((s) => s.id === restoreTargetId);
  const activeStores = stores.filter((s) => !s.archived);
  const archivedStores = stores.filter((s) => s.archived);

  const handleLaunch = (id) => {
    updateStore(id, { live: true, status: "active" });
    setToast("Your store is now live");
    // Milestone 3 signal for the onboarding tour.
    window.dispatchEvent(new CustomEvent("tour:store-live"));
  };
  const handleSetStatus = (id, status) => {
    updateStore(id, { status });
    setToast(status === "hold" ? "Store put on hold" : "Store is active again");
  };
  const handleViewStore = (s) => {
    setToast(`Opening ${s.teamName || s.storeTitle} storefront…`);
  };

  const addPendingToStore = (storeId, pendingItems) => {
    setStores((arr) => arr.map((s) => {
      if (s.id !== storeId) return s;
      const withIds = pendingItems.map((p, i) => ({
        ...p,
        __pendingId: p.__pendingId || `pending-${Date.now()}-${i}-${Math.floor(Math.random() * 1e6)}`,
      }));
      return { ...s, pendingProducts: [...(s.pendingProducts || []), ...withIds] };
    }));
  };

  const goCreate = () => {
    setPresetData(makePrefill());
    setTemplateMode(false);
    setScreen("form");
  };

  const handleCreateLeague = (league) => {
    // Normalize step-2 group names into group objects so they share a shape
    // with groups created later from inside the league via "Add Group".
    const groups = (league.groups || []).map((g, i) =>
      typeof g === "string"
        ? { id: "group-" + Date.now() + "-" + i, name: g, storeIds: [] }
        : g
    );
    setLeagues((arr) => [...arr, {
      ...league,
      groups,
      location: league.location || "Perrisville, CA",
      avatarColor: "#1d3a8a",
      members: league.members || [
        { id: "m-owner", name: "Matt Passmore", email: "matt@prolook.com", role: "Owner", color: "#1f9d55" },
        { id: "m-admin", name: "Stevie Nicks", email: "stevie@prolook.com", role: "Admin", color: "#9a6a1e" },
        { id: "m-mgr", name: "George Lin", email: "george@prolook.com", role: "Manager", color: "#1d3a8a" },
      ],
    }]);
    setShowLeagueModal(false);
    setActiveLeagueId(league.id);
    setActiveGroupId(null);
    setScreen("list");
    setToast(`League "${league.name}" created`);
  };

  const handleCreateGroup = (leagueId, group) => {
    setLeagues((arr) => arr.map((l) =>
      l.id === leagueId ? { ...l, groups: [...(l.groups || []), group] } : l
    ));
    setShowGroupModal(false);
    setToast(`Group "${group.name}" created`);
  };

  const handleUpdateLeague = (leagueId, patch, silent) => {
    setLeagues((arr) => arr.map((l) => l.id === leagueId ? { ...l, ...patch } : l));
  };

  const handleDeleteLeague = (leagueId) => {
    setLeagues((arr) => arr.filter((l) => l.id !== leagueId));
    setActiveLeagueId(null);
    setActiveGroupId(null);
    setScreen("list");
    setToast("League deleted");
  };

  const seedDummyData = () => {
    setStores(makeSeededStores());
    setActiveStoreId(null);
    setLeagues(makeSampleLeagues());
    setActiveLeagueId(null);
    setActiveGroupId(null);
    setScreen("overview");
    setNavTab("overview");
  };

  const clearAll = () => {
    setStores([]);
    setLeagues([]);
    setActiveLeagueId(null);
    setActiveGroupId(null);
    setActiveStoreId(null);
    setScreen("empty");
  };

  const toggleSeed = (v) => {
    setTweak("seedData", v);
    if (v) seedDummyData(); else clearAll();
  };

  // Wipe everything — stores, leagues, active selection, and any running tour —
  // and return to the empty state.
  const clearAllData = () => {
    setTweak("seedData", false);
    setTourRun(false);
    setLeagues([]);
    setActiveLeagueId(null);
    setActiveGroupId(null);
    clearAll();
    setToast("Cleared all dummy data");
  };

  // The 6-step wizard collects compliance + tax in-flow (Step 4) and audits it
  // (Step 6), so deploying goes straight to the loading → finalize path. The
  // standalone SalesTaxModal is retained for the "form-tax" deep-link frame.
  const handleFormSubmit = (data) => {
    setPendingData(data);
    setScreen("loading");
    setTimeout(() => finalizeStore(data), 1800);
  };

  const handleTaxConfirm = () => {
    setShowTaxModal(false);
    setScreen("loading");
    setTimeout(() => finalizeStore(pendingData), 1800);
  };

  const finalizeStore = (data) => {
    const id = "store-" + Date.now();
    // Step 2 products land in the store catalog as live records (Epic A).
    const carried = (data.curatedProducts || []).map((c, i) => ({
      ...c,
      id: "prod-" + id + "-" + i,
      status: "live",
      ready: true,
      teamSetting: c.teamSetting || "fan-gear",
      optRequired: c.optRequired ?? c.teamSetting === "team-required",
      optSupplied: c.optSupplied ?? c.teamSetting === "team-supplied",
      pricing: c.pricing || defaultPricing(),
    }));
    const newStore = {
      id, ...data,
      products: carried,
      pendingProducts: [],
      __fresh: true,
      __userCreated: true, // persisted to localStorage so the store survives reloads
      ownership: "mine", // created by the active session user
      revenue: 0,
    };
    setStores((s) => [...s, newStore]);
    setActiveStoreId(id);
    setDashboardTab(carried.length ? "products" : "general");
    setScreen("dashboard");
    setToast("Store created successfully");
    // First-ever store → launch the contextual onboarding tour.
    if (isFirstStore) setTourRun(true);
  };

  // Tweak helper: spin up a blank fresh store and start the tour immediately,
  // so the onboarding can be demoed without walking the whole create form.
  const startOnboardingDemo = () => {
    const id = "store-" + Date.now();
    const newStore = { ...makePrefill(), id, __fresh: true, products: [], pendingProducts: [], revenue: 0 };
    setStores((s) => [...s, newStore]);
    setActiveStoreId(id);
    setDashboardTab("general");
    setScreen("dashboard");
    persistFirstStore(true);
    setTourRun(true);
  };

  const handleCancel = () => setShowCancelModal(true);

  const handleSaveTemplate = (tpl) => {
    setUserTemplates((list) => { const next = [tpl, ...list]; saveUserTemplates(next); return next; });
    setCreateTplStore(null);
    setToast(`"${tpl.name}" saved to your templates`);
  };
  const confirmCancel = () => {
    setShowCancelModal(false);
    setTemplateMode(false);
    if (pendingNav) { const fn = pendingNav; setPendingNav(null); fn(); return; }
    setScreen(stores.length > 0 ? "list" : "empty");
  };

  const activeStore = stores.find((s) => s.id === activeStoreId);
  const sidebarTeams = stores.map((s) => ({
    id: s.id,
    name: s.teamName || s.storeTitle,
    color: s.teamColors?.[0] || "#e1251b",
    initial: (s.teamName || s.storeTitle || "T")[0],
  }));

  // ─── prolook.com header navigation ───
  const canAccessStores = tweaks.hasStoreAccess || gatePassed || tweaks.bypassGate;
  const TEAM_STORES_SCREENS = ["gate", "access-form", "access-pending", "stripe-verify", "portal", "overview", "orders", "list", "leagues", "league-settings", "empty", "form", "template", "loading", "account"];

  const goTeamStores = () => {
    // Reps land straight on the Team Stores Overview (the standalone landing page replaced the portal).
    setNavTab("overview"); setActiveLeagueId(null); setActiveGroupId(null); setScreen("overview");
  };
  // Header items: Discover + the (unbuilt) Build menus return to the Overview.
  const onNavigate = (id) => {
    if (id === "team-stores") goTeamStores();
    else setScreen("overview");
  };

  // ─── Team Stores section sub-tabs (Overview / Stores / Leagues) ───
  const onTabChange = (id) => {
    if (id === "overview") { setNavTab("overview"); setScreen("overview"); }
    else if (id === "stores") { setNavTab("stores"); setActiveLeagueId(null); setActiveGroupId(null); setScreen((tweaks.zeroState || stores.length) ? "list" : "empty"); }
    else if (id === "leagues") { setNavTab("leagues"); setActiveLeagueId(null); setActiveGroupId(null); setScreen("leagues"); }
    else if (id === "vault") { setNavTab("vault"); setScreen("vault"); }
    else if (id === "wsettings") { setNavTab("wsettings"); setActiveLeagueId(null); setActiveGroupId(null); setScreen("wsettings"); }
  };
  // ─── Account menu routing (header dropdown) ───
  const onAccountMenu = (id) => {
    if (id === "account") { setActiveLeagueId(null); setActiveGroupId(null); setScreen("account"); }
    else if (id === "customizer") { window.open("https://customizer.prolook.com/", "_blank", "noopener"); setToast("Opening the PROLOOK Customizer in a new tab…"); }
    else if (id === "orders") { const origin = navTab === "stores" || navTab === "vault" ? navTab : "overview"; setOrdersOrigin(origin); setNavTab(origin); setActiveLeagueId(null); setActiveGroupId(null); setScreen("orders"); }
    else if (id === "wsettings") { setActiveLeagueId(null); setActiveGroupId(null); setScreen("wsettings"); }
    else if (id === "logout") { setShowLogoutModal(true); }
  };
  const goStores = () => onTabChange("stores");
  const goLeagues = () => onTabChange("leagues");

  // Toggling the access tweak should be immediately demonstrable: revoking it
  // drops a user sitting in Team Stores back to the splash; granting it lets
  // them in from the splash.
  const setStoreAccess = (v) => {
    setTweak("hasStoreAccess", v);
    setGatePassed(false);
    if (!v && TEAM_STORES_SCREENS.includes(screen)) { setNavTab("overview"); setScreen("gate"); }
    if (v && (screen === "gate" || screen === "access-form" || screen === "access-pending")) { setNavTab("overview"); setScreen("overview"); }
  };
  const unlockFromGate = (msg) => { setGatePassed(true); setNavTab("overview"); setActiveLeagueId(null); setActiveGroupId(null); setScreen("overview"); setToast(msg); };

  // Store-editor screens drop the storefront chrome entirely.
  const inEditor = screen === "dashboard" || screen === "customizer";
  const headerActive = inEditor ? "none" : (screen === "home" ? "none" : "team-stores");
  const showSubTabs = !inEditor && (screen === "wsettings" || screen === "vault" || screen === "overview" || screen === "orders" || screen === "leagues" || screen === "empty" || screen === "account" || screen === "form" || screen === "template" || screen === "template-repo" || (screen === "list" && !activeLeagueId));
  const activeSubTab = screen === "wsettings" ? "none" : screen === "vault" ? "vault" : (screen === "form" || screen === "template" || screen === "template-repo") ? "stores" : (screen === "account" ? "none" : (screen === "orders" ? ordersOrigin : (screen === "overview" ? "overview" : (screen === "leagues" ? "leagues" : "stores"))));
  const topLevelStoreCount = stores.filter((s) => s.kind !== "fan").length;
  const inCreateFlow = screen === "form" || screen === "template" || screen === "template-repo";
  const guardNav = (fn) => { if (inCreateFlow) { setPendingNav(() => fn); setShowCancelModal(true); } else { fn(); } };

  // ─── Overview mode short-circuit (top-level app only, never inside an iframe) ───
  if (tweaks.overviewMode && !isFrameRoute) {
    return (
      <React.Fragment>
        <OverviewGrid onExit={() => setTweak("overviewMode", false)} />
        <TweaksPanel title="Tweaks">
          <TweakSection label="View">
            <TweakToggle label="Frame overview" value={tweaks.overviewMode} onChange={(v) => setTweak("overviewMode", v)} />
            <TweakToggle label="Has Team Stores access" value={tweaks.hasStoreAccess} onChange={setStoreAccess} />
            <TweakToggle label="Bypass compliance gate" value={tweaks.bypassGate} onChange={(v) => setTweak("bypassGate", v)} />
            <TweakToggle label="Seed dummy data" value={tweaks.seedData} onChange={toggleSeed} />
            <TweakButton label="Clear all dummy data" secondary onClick={clearAllData} />
          </TweakSection>
        </TweaksPanel>
      </React.Fragment>
    );
  }

  // ─── No-access flow short-circuit ───
  // The gate, the application form, and the manual-review pending state are
  // full-viewport standalone pages (no prolook.com shell) — they sit BETWEEN
  // the brand site and the tools.
  if ((screen === "gate" || screen === "access-form" || screen === "access-pending" || screen === "stripe-verify") && !isFrameRoute) {
    return (
      <React.Fragment>
        {screen === "gate" && (
          <div style={{ minHeight: "100vh", background: "#fbfbfc", minWidth: 1280, display: "flex", flexDirection: "column" }}>
            <TeamStoresHeader
              active=""
              onChange={() => {}}
              accountName={`${ACCOUNT_PROFILE.firstName} ${ACCOUNT_PROFILE.lastName}`}
              onMenuSelect={() => { window.location.href = encodeURI("Team Stores Landing.dc.html"); }}
              onGoLanding={() => { window.location.href = encodeURI("Team Stores Landing.dc.html"); }}
            />
            <div style={{ display: "flex", minHeight: "calc(100vh - 92px)" }}>
              <FeatureGate
                embedded
                onApply={() => setScreen("access-form")}
                onBypass={() => unlockFromGate("Verified — welcome to Team Stores")}
                onBack={() => { window.location.href = encodeURI("Team Stores Landing.dc.html"); }}
              />
            </div>
          </div>
        )}
        {screen === "access-form" && (
          <AccessApplyForm
            onSubmit={() => setScreen("stripe-verify")}
            onCancel={() => { window.location.href = encodeURI("Team Stores Landing.dc.html"); }}
          />
        )}
        {screen === "stripe-verify" && (
          <StripeVerifying
            onDone={() => unlockFromGate("Verified with Stripe \u2014 welcome to Team Stores")}
          />
        )}
        {screen === "access-pending" && (
          <AccessPending
            email="jane@prolook.com"
            onApprove={() => unlockFromGate("Access approved — welcome to Team Stores")}
            onBack={() => { window.location.href = encodeURI("Team Stores Landing.dc.html"); }}
          />
        )}
        <Toast message={toast} onDone={() => setToast(null)} />
        <TweaksPanel title="Tweaks">
          <TweakSection label="Access">
            <TweakToggle label="Has Team Stores access" value={tweaks.hasStoreAccess} onChange={setStoreAccess} />
            <TweakToggle label="Bypass compliance gate" value={tweaks.bypassGate} onChange={(v) => setTweak("bypassGate", v)} />
          </TweakSection>
          <TweakSection label="No-access flow">
            <TweakButton label="▶  Gate landing" onClick={() => setScreen("gate")} />
            <TweakButton label="▶  Business details form" onClick={() => setScreen("access-form")} />
            <TweakButton label="▶  Stripe verification" onClick={() => setScreen("stripe-verify")} />
          </TweakSection>
        </TweaksPanel>
      </React.Fragment>
    );
  }

  return (
    <div style={{ minHeight: "100vh", background: "#fbfbfc", minWidth: 1280 }}>
      {portalView && <TeamManagerPortal invite={portalView.invite} store={portalView.store} onExit={() => setPortalView(null)} />}
      {!inEditor && !showSubTabs && <UtilityBar />}
      <PlatformVaultOverlay />
      {!inEditor && !showSubTabs && <GlobalHeader active={headerActive} onNavigate={(id) => guardNav(() => onNavigate(id))} hideAccountCart={showSubTabs} />}
      {showSubTabs && (
        <TeamStoresHeader
          active={activeSubTab}
          onChange={(id) => guardNav(() => onTabChange(id))}
          accountName={`${ACCOUNT_PROFILE.firstName} ${ACCOUNT_PROFILE.lastName}`}
          onMenuSelect={(id) => guardNav(() => onAccountMenu(id))}
          onGoLanding={() => guardNav(() => { window.location.href = encodeURI("Team Stores Landing.dc.html"); })}
        />
      )}
      <div key={screen} style={{ display: "flex", minHeight: inEditor ? "100vh" : "calc(100vh - 114px)", animation: "pageIn .22s ease-out both" }}>
        {screen === "home" && <ProlookHome onShopTeamStores={goTeamStores} />}
        {screen === "portal" && (
          <TeamStoresPortal
            onEnter={() => { if (canAccessStores) { setNavTab("overview"); setScreen("overview"); } else { setScreen("gate"); } }}
            headlineKey={tweaks.portalHeadline}
            heroVariant={tweaks.portalHero}
            ctaLabel={tweaks.portalCta === "dashboard" ? "Go to Dashboard" : "Enter Store Manager"}
            ctaColor={tweaks.portalCtaColor}
          />
        )}
        {screen === "vault" && (() => {
          const vaultToProducts = (items) => items.map((v, i) => ({
            id: "dep-" + v.id + "-" + (Date.now() + i), title: v.title, sku: v.sku, brand: "Prolook", source: "ProStock",
            category: v.vaultCategory, stock: "in", deco: "none", decoratable: true, photo: v.photo,
            colorway: v.colorway, colorways: v.colorways, status: "live", srcKind: "vault", teamSetting: "fan-gear",
            subtitle: v.sku + " · Prolook ProStock",
            pricing: vaultPricing(v.base, v.retail),
          }));
          return (
          <ProStockVaultPage
            stores={activeStores}
            onDeployToStore={(dest, items) => {
              const target = stores.find((s) => s.id === dest.id);
              const added = vaultToProducts(items);
              updateStore(dest.id, { products: [...((target && target.products) || []), ...added] });
              setToast(added.length + (added.length === 1 ? " ProStock item" : " ProStock items") + " added to " + (dest.storeTitle || dest.teamName) + " — confirm pricing, team setting & sizes");
            }}
            onDeployToStores={(dests, items) => {
              dests.forEach((dest) => {
                const target = stores.find((s) => s.id === dest.id);
                updateStore(dest.id, { products: [...((target && target.products) || []), ...vaultToProducts(items)] });
              });
              setToast(items.length + (items.length === 1 ? " item" : " items") + " added to " + dests.length + " stores — confirm pricing, team setting & sizes in each");
            }}
            onStartStore={(selItems) => {
              // Vault multi-select → store creation wizard with products pre-loaded.
              setPresetData({
                ...makePrefill(),
                _fromVault: true,
                curatedProducts: selItems.map((v) => {
                  const cw = hcmDefaultCw(v);
                  return { id: "wv-" + v.id, srcId: v.id, title: v.title, sku: v.sku, subtitle: v.sku + " · Prolook " + v.source, status: "draft", teamSetting: "fan-gear", rosters: [], photo: cw.front, colorway: cw.name, colorways: v.colorways, brand: "Prolook", source: v.source, retail: v.retail, base: v.base, pricing: vaultPricing(v.base, v.retail) };
                }),
              });
              setTemplateMode(false);
              setNavTab("stores");
              setScreen("form");
            }}
          />
          );
        })()}
        {screen === "overview" && (
          <OverviewDashboard
            isEmpty={tweaks.overviewEmpty || tweaks.zeroState}
            stores={activeStores}
            leagues={leagues}
            onManageStore={(id, tab) => { recordStoreVisit(id); setActiveStoreId(id); setDashboardTab(tab || "home"); setScreen("dashboard"); }}
            onGoStores={goStores}
            onViewOrders={() => { setOrdersOrigin("overview"); setNavTab("overview"); setScreen("orders"); }}
            onAdd={goCreate}
          />
        )}
        {screen === "orders" && (
          <OrdersOverview
            stores={stores}
            origin={ordersOrigin}
            onBack={() => onTabChange(ordersOrigin)}
          />
        )}
        {screen === "leagues" && (
          <LeaguesGrid
            leagues={leagues}
            stores={stores}
            onOpenLeague={(id) => { setNavTab("leagues"); setActiveLeagueId(id); setActiveGroupId(null); setScreen("list"); }}
            onAddLeague={() => setShowLeagueModal(true)}
          />
        )}
        {screen === "empty" && <EmptyState onCreate={goCreate} />}
        {screen === "wsettings" && <WorkspaceSettings stores={stores} />}
        {screen === "account" && (
          <AccountProfile
            onEditProfile={() => { window.open("https://customizer.prolook.com/account", "_blank", "noopener"); setToast("Opening Product Customizer account settings in a new tab…"); }}
            onToast={setToast}
            archivedStores={archivedStores}
            onManageStore={(id) => { setActiveStoreId(id); setDashboardTab("home"); setScreen("dashboard"); }}
            onUnarchive={(id) => setRestoreTargetId(id)}
            onCreateTemplate={(s) => setCreateTplStore(s)}
          />
        )}        {screen === "list" && (
          <StoreList
            stores={activeStores}
            zeroState={tweaks.zeroState}
            onAdd={goCreate}
            onAddLeague={() => setShowLeagueModal(true)}
            onAddGroup={() => setShowGroupModal(true)}
            onManage={(id) => { recordStoreVisit(id); setActiveStoreId(id); setDashboardTab("home"); setScreen("dashboard"); }}
            onArchive={(id) => setArchiveTargetId(id)}
            onCreateTemplate={(s) => setCreateTplStore(s)}
            leagues={leagues}
            activeLeagueId={activeLeagueId}
            onSelectLeague={(id) => { setActiveLeagueId(id); setActiveGroupId(null); }}
            activeGroupId={activeGroupId}
            onOpenGroup={(gid) => setActiveGroupId(gid)}
            onCloseGroup={() => setActiveGroupId(null)}
            onOpenSettings={() => setScreen("league-settings")}
            onBackToLeagues={goLeagues}
          />
        )}
        {screen === "league-settings" && (
          <LeagueSettings
            league={leagues.find((l) => l.id === activeLeagueId)}
            onBack={() => setScreen("list")}
            onSave={handleUpdateLeague}
            onDelete={handleDeleteLeague}
            onToast={setToast}
          />
        )}
        {screen === "form" && (
          <CreateForm
            onCancel={handleCancel}
            onSubmit={handleFormSubmit}
            presetData={presetData}
            accent={tweaks.wizardAccent === "navy" ? "#1d3a8a" : "#e1251b"}
            athletes={tweaks.wizardAthletes || 30}
            templateMode={templateMode}
            liveStores={activeStores}
          />
        )}

        {screen === "loading" && (
          <div style={{ flex: 1, background: "#fff" }} />
        )}
        {screen === "dashboard" && activeStore && (
          <ManageStoreWorkspace
            store={activeStore}
            readOnly={!!activeStore.archived}
            onBack={goStores}
            onCustomize={() => setScreen("customizer")}
            initialTab={dashboardTab}
            liveStores={stores.filter((s) => s.live)}
            onLaunch={handleLaunch}
            onSetStatus={handleSetStatus}
            onViewStore={handleViewStore}
            onSwitchStore={(id) => { setActiveStoreId(id); setScreen("dashboard"); }}
            onProductsChange={(changes) => updateStore(activeStore.id, changes)}
            onUpdateStore={(patch) => updateStore(activeStore.id, patch)}
            onCreateTemplate={(s) => setCreateTplStore(s)}
          />
        )}
        {screen === "customizer" && activeStore && (
          <Customizer
            storeSport={activeStore.sport}
            onClose={() => setScreen("dashboard")}
            onPick={(item) => { setScreen("dashboard"); setToast(`Selected ${item.label} — opening Customizer 3.0…`); }}
          />
        )}
        {screen === "my-carts" && (
          <MyCarts
            stores={stores}
            activeStoreId={activeStoreId}
            onClose={() => setScreen("dashboard")}
            onLoadCart={(cart) => { setScreen("dashboard"); setToast(`Loaded "${cart.name}" into the store`); }}
            onAddToStore={({ cart, store }) => {
              // Build pending products from the cart (1-3 items for demo)
              const itemCount = Math.min(Math.max(cart.qty || 1, 1), 3);
              const items = Array.from({ length: itemCount }).map((_, i) => ({
                title: cart.name + (itemCount > 1 ? ` — Item ${i + 1}` : ""),
                subtitle: (store.teamName || store.storeTitle || "").toUpperCase(),
                source: "cart",
              }));
              addPendingToStore(store.id, items);
              setActiveStoreId(store.id);
              setDashboardTab("products");
              setScreen("dashboard");
              setToast(`Added "${cart.name}" — configure ${itemCount} item${itemCount > 1 ? "s" : ""} below`);
            }}
          />
        )}
        {screen === "saved-designs" && (
          <SavedDesigns
            stores={stores}
            activeStoreId={activeStoreId}
            onClose={() => setScreen("dashboard")}
            storeTeamName={activeStore?.teamName}
            onAddToStore={({ design, store }) => {
              addPendingToStore(store.id, [{
                title: design.title,
                subtitle: (store.teamName || store.storeTitle || "").toUpperCase(),
                source: "design",
                designPalette: design.palette,
              }]);
              setActiveStoreId(store.id);
              setDashboardTab("products");
              setScreen("dashboard");
              setToast(`Added "${design.title}" — configure pricing below`);
            }}
          />
        )}
      </div>

      <SalesTaxModal open={showTaxModal} onClose={() => setShowTaxModal(false)} onConfirm={handleTaxConfirm} />
      <CancelConfirmModal open={showCancelModal} onClose={() => { setShowCancelModal(false); setPendingNav(null); }} onConfirm={confirmCancel} />

      <CreateLeagueModal
        open={showLeagueModal}
        stores={stores}
        onClose={() => setShowLeagueModal(false)}
        onCreate={handleCreateLeague}
      />
      <CreateGroupModal
        open={showGroupModal}
        league={leagues.find((l) => l.id === activeLeagueId)}
        stores={stores}
        onClose={() => setShowGroupModal(false)}
        onCreate={handleCreateGroup}
      />
      <LoadingOverlay visible={screen === "loading"} />
      <ArchiveStoreModal
        open={!!archiveTargetStore}
        store={archiveTargetStore}
        onClose={() => setArchiveTargetId(null)}
        onConfirm={() => { updateStore(archiveTargetId, { status: "archived", live: false, archived: true }); setArchiveTargetId(null); setToast("Store archived"); }}
      />
      <RestoreStoreModal
        open={!!restoreTargetStore}
        store={restoreTargetStore}
        onClose={() => setRestoreTargetId(null)}
        onConfirm={() => { updateStore(restoreTargetId, { status: "active", live: false, archived: false }); setRestoreTargetId(null); setToast("Store restored — now a draft in your active stores"); }}
      />
      {/* Logout confirmation — confirm lands on the Team Stores landing page */}
      <Modal open={showLogoutModal} onClose={() => setShowLogoutModal(false)} width={440}>
        <div style={{ padding: "28px 32px 8px" }}>
          <h2 style={{ margin: 0, fontFamily: "Archivo, sans-serif", fontSize: 20, fontWeight: 800, color: "#0e0e10", letterSpacing: "-.01em" }}>Log out of Team Stores?</h2>
          <p style={{ margin: "10px 0 0", fontSize: 14, color: "#6b6b72", lineHeight: 1.55 }}>You're about to sign out of your Team Stores workspace on this device.</p>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 12, padding: "24px 32px 28px" }}>
          <button onClick={() => setShowLogoutModal(false)} style={{ border: "1px solid #d8d8de", background: "#fff", color: "#0e0e10", borderRadius: 8, padding: "12px 20px", cursor: "pointer", fontSize: 13.5, fontWeight: 700, fontFamily: "inherit" }}>Cancel</button>
          <button onClick={() => { setShowLogoutModal(false); window.location.href = encodeURI("Team Stores Landing.dc.html"); }} style={{ border: 0, background: "#0e0e10", color: "#fff", borderRadius: 8, padding: "12px 20px", cursor: "pointer", fontSize: 13.5, fontWeight: 700, fontFamily: "inherit" }}>Yes, log out</button>
        </div>
      </Modal>
      <Toast message={toast} onDone={() => setToast(null)} />

      {/* Store Setup Guide — informational modal, replaces the corner checklist */}
      <StoreGuideModal
        open={guideOpen && !isFrameRoute && !tweaks.overviewMode}
        onClose={dismissGuide}
        onComplete={() => persistFirstStore(false)}
      />

      <WelcomeSSOModal
        open={showWelcome && !isFrameRoute && !tweaks.overviewMode}
        originId={ssoOriginId}
        multiBrand={tweaks.ssoMultiBrand}
        onConfirm={confirmWelcome}
      />

      <TweaksPanel title="Tweaks">
        <TweakSection label="Access">
          <TweakToggle label="Has Team Stores access" value={tweaks.hasStoreAccess} onChange={setStoreAccess} />
          <TweakToggle label="Bypass compliance gate" value={tweaks.bypassGate} onChange={(v) => setTweak("bypassGate", v)} />
        </TweakSection>
        <TweakSection label="Auto sign-in welcome">
          <TweakButton label="▶  Preview welcome modal" onClick={() => setShowWelcome(true)} />
          <TweakToggle label="Multi-brand account" value={tweaks.ssoMultiBrand} onChange={(v) => setTweak("ssoMultiBrand", v)} />
          <TweakToggle label="Show on session start" value={tweaks.ssoWelcome} onChange={(v) => setTweak("ssoWelcome", v)} />
          <TweakSelect label="Origin brand" value={tweaks.ssoOriginBrand}
            options={ACCESSIBLE_CUSTOMIZERS.map((id) => ({ value: id, label: CUSTOMIZER_BRANDS[id].name }))}
            onChange={(v) => setTweak("ssoOriginBrand", v)} />
        </TweakSection>
        <TweakSection label="View">
          <TweakToggle label="Frame overview" value={tweaks.overviewMode} onChange={(v) => setTweak("overviewMode", v)} />
          <TweakToggle label="Overview empty state" value={tweaks.overviewEmpty} onChange={(v) => setTweak("overviewEmpty", v)} />
          <TweakToggle label="Zero state usability test" value={tweaks.zeroState} onChange={(v) => setTweak("zeroState", v)} />
          <TweakToggle label="Seed dummy data" value={tweaks.seedData} onChange={toggleSeed} />
          <TweakButton label="Clear all dummy data" secondary onClick={clearAllData} />
        </TweakSection>
        <TweakSection label="Store Close Report">
          <TweakButton label="▶  Replay first-after-close" onClick={() => window.dispatchEvent(new CustomEvent("closereport:replay"))} />
        </TweakSection>
        <TweakSection label="Store Setup Guide">
          <TweakButton label="▶  Open · Individual (6 cards)" onClick={() => window.dispatchEvent(new CustomEvent("setupguide:open", { detail: { storeType: "single" } }))} />
          <TweakButton label="▶  Open · League (7 cards)" secondary onClick={() => window.dispatchEvent(new CustomEvent("setupguide:open", { detail: { storeType: "league" } }))} />
        </TweakSection>
        <TweakSection label="Team Stores portal">
          <TweakButton label="▶  Preview portal landing" onClick={() => { setNavTab("overview"); setScreen("portal"); }} />
          <TweakRadio label="Hero treatment" value={tweaks.portalHero}
            options={[{ value: "cinematic", label: "Cinematic" }, { value: "split", label: "Split panel" }]}
            onChange={(v) => setTweak("portalHero", v)} />
          <TweakRadio label="Headline" value={tweaks.portalHeadline}
            options={[{ value: "fulfillment", label: "Total fulfillment" }, { value: "teamstores", label: "Team stores" }, { value: "geared", label: "Geared up" }]}
            onChange={(v) => setTweak("portalHeadline", v)} />
          <TweakRadio label="Gateway CTA" value={tweaks.portalCta}
            options={[{ value: "manager", label: "Store Manager" }, { value: "dashboard", label: "Dashboard" }]}
            onChange={(v) => setTweak("portalCta", v)} />
          <TweakRadio label="CTA color" value={tweaks.portalCtaColor}
            options={[{ value: "black", label: "Black" }, { value: "red", label: "Red" }]}
            onChange={(v) => setTweak("portalCtaColor", v)} />
        </TweakSection>
        <TweakSection label="Create-store wizard">
          <TweakButton label="▶  Open create wizard" onClick={() => { setPresetData(makePrefill()); setTemplateMode(false); setScreen("form"); }} />
          <TweakRadio label="Template layout" value={tweaks.templateLayout}
            options={[{ value: "sidebar", label: "Sidebar" }, { value: "stacked", label: "Stacked" }]}
            onChange={(v) => setTweak("templateLayout", v)} />
          <TweakRadio label="Accent" value={tweaks.wizardAccent}
            options={[{ value: "red", label: "PROLOOK Red" }, { value: "navy", label: "Navy" }]}
            onChange={(v) => setTweak("wizardAccent", v)} />
          <TweakSlider label="Projected athletes" min={10} max={60} step={1}
            value={tweaks.wizardAthletes} onChange={(v) => setTweak("wizardAthletes", v)} />
        </TweakSection>
        <TweakSection label="Onboarding checklist">
          <TweakButton label="Open Store Setup Guide" onClick={() => setGuideOpen(true)} />
          <TweakButton label="Reset guide (auto-shows again)" secondary onClick={() => { try { localStorage.removeItem("ts_setup_guide_seen_v1"); } catch (e) {} setToast("Guide reset — it will auto-open on the next workspace visit"); }} />
          <TweakRadio label="Accent" value={tweaks.checklistAccent}
            options={[{ value: "red", label: "Red" }, { value: "navy", label: "Navy" }, { value: "green", label: "Green" }]}
            onChange={(v) => setTweak("checklistAccent", v)} />
          <TweakRadio label="Position" value={tweaks.checklistPosition}
            options={[{ value: "right", label: "Bottom right" }, { value: "left", label: "Bottom left" }]}
            onChange={(v) => setTweak("checklistPosition", v)} />
          <TweakToggle label="Run on next new store" value={isFirstStore} onChange={persistFirstStore} />
          {activeStore && !tourRun && (
            <TweakButton label="Replay checklist on this store" secondary onClick={() => { try { localStorage.removeItem("prolook_checklist_dismissed_" + activeStore.id); } catch (e) {} persistFirstStore(true); setTourRun(true); }} />
          )}
        </TweakSection>
      </TweaksPanel>
    </div>
  );
}

// Demo prefill values to make the flow tangible
const USER_TPL_KEY = "prolook_user_templates";
function loadUserTemplates() {
  try { const raw = localStorage.getItem(USER_TPL_KEY); return raw ? JSON.parse(raw) : []; } catch (e) { return []; }
}
function saveUserTemplates(list) {
  try { localStorage.setItem(USER_TPL_KEY, JSON.stringify(list)); } catch (e) {}
}

function makePrefill() {
  return {
    storeTitle: "",
    teamName: "",
    teamColors: [],
    teamLogo: null,
    connectTeam: "",
    sport: "",
    contactPref: "Email & Phone",
    email: "",
    phone: "",
    storeOpenMode: "set-window",
    storeOpenStart: "",
    storeOpenEnd: "",
    shipFirstName: "",
    shipLastName: "",
    shipName: "",
    shipStreet: "",
    shipApt: "",
    shipCountry: "United States",
    shipState: "",
    shipCity: "",
    shipZip: "",
    shipOption: "Ship to Coach",
    saveToAddressBook: true,
    collectTax: true,
    taxRate: "",
    // 6-step wizard fields
    storeSlug: "",
    connectedTeam: "",
    sports: [],
    contactEmailMode: "",
    contactPhoneMode: "",
    managerName: "",
    urgencyTimers: true,
    curatedProducts: [],
    collectPlayerName: true,
    collectRosterNumber: true,
    taxMode: "zip",
    flatTaxRate: "",
    heroTheme: "pitch",
    primaryColor: "#e1251b",
  };
}

function makeSeedStores() {
  const base = [
    // ownership: "mine" → created by the logged-in user; "joined" → granted admin access by others / inherited via org role.
    { name: "Jazz Bantum Boys Basketball", city: "Riverside", state: "California", color: "#7c1d2b", initials: "JB", ownership: "mine", createdBy: "Hingle McCringleberry", storeType: "league", dist: "coach", announcement: "<p><strong>Orders close June 19</strong> \u2014 uniforms arrive about 3 weeks after the store closes. Jerseys run small; most players size up one. All custom gear is made to order, so no returns or exchanges on personalized items.</p>" },
    { name: "Rams", city: "Perrisville", state: "California", color: "#ec6c2a", initials: "RA", ownership: "mine", createdBy: "George Harrison" },
    { name: "Gators", city: "Perrisville", state: "California", color: "#1f9d55", initials: "GA", ownership: "joined", createdBy: "Dwayne Hoover" },
    { name: "Honey Badgers", city: "Perrisville", state: "California", color: "#f5b400", initials: "HB", ownership: "joined", createdBy: "Marcy Feldman" },
    // Roster-optional real-world stores: team mapping done but zero rosters (fills from checkout)…
    { name: "Wasatch Elite Lacrosse", city: "Draper", state: "Utah", color: "#1e3a6e", initials: "WL", ownership: "mine", createdBy: "Hingle McCringleberry", storeType: "league", dist: "coach", sport: "Lacrosse", rosterProfile: "teams-only", revenue: 18240.5 },
    // …and no teams at all — fan-gear only; team gear is locked until teams or divisions exist.
    { name: "Bayshore Summer Classic", city: "Huntington Beach", state: "California", color: "#0f766e", initials: "BC", ownership: "mine", createdBy: "Hingle McCringleberry", storeType: "league", dist: "coach", sport: "Volleyball", rosterProfile: "unstructured", revenue: 46870.25 },
    // Marucci-seller store (Products Tab Evolution) — multi-source canonical catalog:
    // Prolook custom + Marucci/Victus stock + external-customizer + decorated stock.
    { name: "Lincoln High Baseball — Fall 2026", city: "Lincoln", state: "Nebraska", color: "#1e3a6e", initials: "LH", ownership: "mine", createdBy: "Hingle McCringleberry", sport: "Baseball", seller: "Marucci Team Stores", entitledBrands: ["Marucci", "Victus", "Prolook"], revenue: 23140.75 },
  ];
  return base.map((b, i) => ({
    id: "seed-" + i,
    ownership: b.ownership,
    createdByName: b.createdBy,
    seller: b.seller || "Marucci Team Stores",
    entitledBrands: b.entitledBrands || ["Marucci", "Victus", "Prolook"],
    storeTitle: b.name,
    teamName: b.name,
    storeType: b.storeType || "single",
    rosterProfile: b.rosterProfile,
    teamColors: [b.color, "#0e0e10", "#ffffff"],
    teamLogo: null,
    sport: b.sport || (b.storeType === "league" ? "Basketball" : "Football"),
    contactPref: "Email & Phone",
    email: "coach@example.com",
    phone: "555-123-4567",
    storeOpenMode: "set-window",
    // Staggered per-team windows — distinct start dates, spans of 30 / 60 / 90 days.
    storeOpenStart: ["2026-05-20", "2026-05-01", "2026-04-10", "2026-05-25"][i % 4],
    storeOpenEnd: ["2026-06-19", "2026-06-30", "2026-07-09", "2026-07-24"][i % 4],
    shipFirstName: "Bismo",
    shipLastName: "Funyuns",
    shipName: "Bismo Funyuns",
    shipStreet: "123 Sports Way",
    shipCity: b.city,
    shipState: b.state,
    shipZip: "92009",
    shipCountry: "United States",
    shipOption: "Ship to Coach",
    distributionModel: b.dist || "coach",
    saveToAddressBook: true,
    collectTax: true,
    taxRate: "7.75",
    fundraisingEnabled: true,
    announcement: b.announcement || "",
    revenue: b.revenue != null ? b.revenue : Math.round((Math.random() * 1800 + 200) * 100) / 100,
  }));
}

// Enriched demo stores — a couple are live with products so Overview / workspace
// states (revenue, active stores, launched controls) all have something to show.
function makeSeededStores() {
  const seeded = makeSeedStores();
  const active = seeded.map((s, i) => {
    const TEAM = (s.teamName || "").toUpperCase();
    if (i === 0) {
      return { ...s, seeded: true, live: true, status: "active", products: [
        { id: s.id + "-p1", title: "Custom Basketball Jersey — Home", photo: "jersey-grizzlies", subtitle: "SFN Lite, V-Neck Jersey", sku: "QXSFNLJ", status: "published", teamSetting: "team-required", source: "design", hasOrders: true, storeCategory: "Team Gear", storeSubcategory: "Uniforms", rosters: [] },
        { id: s.id + "-p2", title: "City A Green Alternate Jersey", photo: "jersey-asteroid", subtitle: "Sublimated Set-In, 2-Button Jersey", status: "published", teamSetting: "team-required", rosters: ["sf-8", "sf-10", "sf-12", "sf-14"] },
        { id: s.id + "-p3", title: "Corner Canyon Shooting Shirt",        photo: "hooded-tech-tee", subtitle: "Sublimated Tapered, Full-Length, Elastic Cuff Pant", status: "draft",     teamSetting: "team-required", rosters: ["af-12", "af-14"] },
        { id: s.id + "-p4", title: "League Warm-Up Hoodie",         photo: "performance-hoodie", subtitle: "Sublimated Traditional, Full-Length, Elastic Cuff Pant", status: "draft",     teamSetting: "team-supplied", rosters: [] },
        { id: s.id + "-p5", title: "Fleece Hoodie — Maroon", subtitle: "MAFH-300 · decorated blank", sku: "MAFH-300", status: "published", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "in", deco: "approved", decoratable: true, photo: "performance-hoodie", pricing: { youth: { retail: "48" }, adult: { retail: "54" } } },
        { id: s.id + "-p6", title: "Team Backpack", subtitle: "VBP-30 · blank, ready to decorate", sku: "VBP-30", status: "draft", teamSetting: "fan-gear", brand: "Victus", source: "Marucci stock", category: "Bags", stock: "in", deco: "none", decoratable: true, photo: "squad-bag", pricing: { youth: { retail: "52" }, adult: { retail: "52" } } },
      ] };
    }
    if (i === 1) {
      return { ...s, seeded: true, live: true, status: "active", products: [
        { id: s.id + "-p1", title: "Team Tee", subtitle: "Sublimated Set-In, Full Button Jersey", status: "published", teamSetting: "team-supplied", source: "cart", hidden: true },
        { id: s.id + "-p2", title: "Performance Tee — Rams", subtitle: "MPT-100 · decorated blank", sku: "MPT-100", status: "published", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Tees", stock: "in", deco: "approved", decoratable: true, photo: "hooded-tech-tee", pricing: { youth: { retail: "22" }, adult: { retail: "26" } } },
        { id: s.id + "-p3", title: "Coach's Quarter-Zip", subtitle: "MQZ-70 · art awaiting approval", sku: "MQZ-70", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "in", deco: "submitted", decoratable: true, pricing: { youth: { retail: "56" }, adult: { retail: "62" } } },
      ] };
    }
    if (i === 4) {
      // Wasatch Elite Lacrosse — live, teams mapped, rosters back-filling from orders.
      return { ...s, seeded: true, live: true, status: "active", products: [
        { id: s.id + "-p1", title: "Lacrosse Game Jersey — Home", subtitle: "Sublimated Set-In Lacrosse Jersey", status: "published", teamSetting: "team-required", rosters: [] },
        { id: s.id + "-p2", title: "Lacrosse Game Shorts", subtitle: "Sublimated Lacrosse Short", status: "published", teamSetting: "team-required", rosters: [] },
        { id: s.id + "-p3", title: "Shooter Shirt — Practice", subtitle: "Sublimated Long-Sleeve Shooter", status: "published", teamSetting: "team-supplied", rosters: [] },
        { id: s.id + "-p4", title: "Wasatch Elite Fan Tee", subtitle: "Cotton-Blend Fan Tee", status: "published", teamSetting: "fan-gear", rosters: [] },
        { id: s.id + "-p5", title: "Team Duffel — Wasatch", subtitle: "MTD-40 · decorated blank", sku: "MTD-40", status: "published", teamSetting: "team-required", brand: "Marucci", source: "Decorated stock", category: "Bags", stock: "in", deco: "approved", decoratable: true, photo: "duffel-bag", pricing: { youth: { retail: "44" }, adult: { retail: "44" } } },
        { id: s.id + "-p6", title: "Sideline Polo", subtitle: "MCP-55 · render queued", sku: "MCP-55", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Polos", stock: "in", deco: "render", decoratable: true, pricing: { youth: { retail: "38" }, adult: { retail: "42" } } },
      ] };
    }
    if (i === 5) {
      // Bayshore Summer Classic — live, fan-gear only. No teams or divisions exist,
      // so Team Required / Team Supplied can't be set and the roster bank stays empty.
      return { ...s, seeded: true, live: true, status: "active", products: [
        { id: s.id + "-p1", title: "Bayshore Classic Fan Tee", subtitle: "Cotton-Blend Event Tee", status: "published", teamSetting: "fan-gear", rosters: [] },
        { id: s.id + "-p2", title: "Bayshore Classic Hoodie", subtitle: "Midweight Fleece Hoodie", status: "published", teamSetting: "fan-gear", rosters: [] },
        { id: s.id + "-p3", title: "Event Beach Towel", subtitle: "Oversized Logo Towel", status: "published", teamSetting: "fan-gear", rosters: [] },
        { id: s.id + "-p4", title: "Tournament Game Jersey", subtitle: "Sublimated Volleyball Jersey", status: "published", teamSetting: "fan-gear", rosters: [] },
        { id: s.id + "-p5", title: "Dugout Pullover — Bayshore", subtitle: "MDP-210 · decorated blank", sku: "MDP-210", status: "published", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "out", deco: "approved", decoratable: true, photo: "sfn-warmup", pricing: { youth: { retail: "52" }, adult: { retail: "58" } } },
        { id: s.id + "-p6", title: "Bat Pack", subtitle: "MBP-20 · blank", sku: "MBP-20", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Marucci stock", category: "Bags", stock: "in", deco: "none", decoratable: true, pricing: { youth: { retail: "46" }, adult: { retail: "46" } } },
      ] };
    }
    if (i === 6) {
      // Lincoln High Baseball — Marucci seller-of-record store exercising the
      // Products Tab Evolution spec: four sources, decoration lifecycle states,
      // requirement group with opt-out, margin erosion, OOS variants.
      const pr = (y, a) => ({ youth: { retail: String(y) }, adult: { retail: String(a) } });
      return { ...s, seeded: true, live: true, status: "active",
        storeOpenStart: "2026-06-01", storeOpenEnd: "2026-08-14",
        artPolicy: "block-publish", oosTreatment: "hidden",
        reqGroups: [{ name: "Bag", count: 2 }],
        products: [
          { id: s.id + "-p1", title: "Home Game Jersey", subtitle: "Sublimated · Customizer design QX-4471", status: "published", teamSetting: "team-required", teams: "Varsity, JV", brand: "Prolook", source: "Prolook custom", category: "Uniforms", stock: "na", deco: "na", decoratable: false, hasOrders: true, photo: "bb-jersey-home", pricing: pr("58.13", "65.41") },
          { id: s.id + "-p2", title: "Away Game Jersey", subtitle: "Sublimated · Customizer design QX-4472", status: "published", teamSetting: "team-required", teams: "Varsity, JV", brand: "Prolook", source: "Prolook custom", category: "Uniforms", stock: "na", deco: "na", decoratable: false, hasOrders: true, photo: "bb-jersey-away", pricing: pr("58.13", "65.41") },
          { id: s.id + "-p3", title: "Fleece Hoodie — Charcoal", subtitle: "MAFH-300 · decorated blank", sku: "MAFH-300", status: "published", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "in", deco: "approved", decoratable: true, photo: "performance-hoodie", pricing: pr("48", "54") },
          { id: s.id + "-p4", title: "Performance Tee", subtitle: "MPT-100 · decorated blank", sku: "MPT-100", status: "published", teamSetting: "team-supplied", brand: "Marucci", source: "Decorated stock", category: "Tees", stock: "in", deco: "approved", decoratable: true, photo: "hooded-tech-tee", pricing: pr("22", "26") },
          { id: s.id + "-p5", title: "Dugout Pullover", subtitle: "MDP-210 · decorated blank", sku: "MDP-210", status: "published", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "out", deco: "approved", decoratable: true, erode: 3.10, photo: "sfn-warmup", pricing: pr("52", "58") },
          { id: s.id + "-p6", title: "CAT X Custom Bat", subtitle: "Configured in Marucci bat builder", status: "published", teamSetting: "fan-gear", brand: "Marucci", source: "External customizer", category: "Hard goods", stock: "in", deco: "na", decoratable: false, pricing: { adult: { retail: "349" } } },
          { id: s.id + "-p7", title: "Team Duffel", subtitle: "MTD-40 · decorated blank", sku: "MTD-40", status: "published", teamSetting: "team-required", brand: "Marucci", source: "Decorated stock", category: "Bags", stock: "in", deco: "approved", decoratable: true, group: "Bag · 1 of 2", optOut: true, photo: "duffel-bag", pricing: pr("44", "44") },
          { id: s.id + "-p8", title: "Wheeled Team Bag", subtitle: "MWB-60 · decorated blank", sku: "MWB-60", status: "published", teamSetting: "team-required", brand: "Marucci", source: "Decorated stock", category: "Bags", stock: "in", deco: "approved", decoratable: true, group: "Bag · 1 of 2", optOut: true, photo: "team-bag", pricing: pr("78", "78") },
          { id: s.id + "-p9", title: "Practice Tee", subtitle: "MPT-90 · art not yet submitted", sku: "MPT-90", status: "draft", teamSetting: "team-supplied", brand: "Marucci", source: "Decorated stock", category: "Tees", stock: "in", deco: "draft", decoratable: true, pricing: pr("20", "24") },
          { id: s.id + "-p10", title: "Hitting Jacket", subtitle: "MHJ-120 · art awaiting approval", sku: "MHJ-120", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "in", deco: "submitted", decoratable: true, pricing: pr("64", "72") },
          { id: s.id + "-p11", title: "Travel Polo", subtitle: "VTP-15 · art rejected — see notes", sku: "VTP-15", status: "draft", teamSetting: "fan-gear", brand: "Victus", source: "Decorated stock", category: "Polos", stock: "in", deco: "rejected", decoratable: true, pricing: pr("38", "42") },
          { id: s.id + "-p12", title: "Fleece Hoodie — Navy", subtitle: "MAFH-300N · render queued", sku: "MAFH-300N", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Decorated stock", category: "Fleece", stock: "in", deco: "render", decoratable: true, photo: "performance-hoodie", pricing: pr("48", "54") },
          { id: s.id + "-p13", title: "Custom Fielding Glove", subtitle: "Configured in Marucci glove builder", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "External customizer", category: "Hard goods", stock: "in", deco: "na", decoratable: false, pricing: { adult: { retail: "289" } } },
          { id: s.id + "-p14", title: "Warm-Up Pant", subtitle: "Customizer design QX-4488", status: "draft", teamSetting: "team-supplied", brand: "Prolook", source: "Prolook custom", category: "Pants", stock: "na", deco: "na", decoratable: false, photo: "pro-jogger", pricing: pr("34", "38") },
          { id: s.id + "-p15", title: "Coach's Quarter-Zip", subtitle: "MQZ-70 · blank, ready to decorate", sku: "MQZ-70", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Marucci stock", category: "Fleece", stock: "in", deco: "none", decoratable: true, pricing: pr("56", "62") },
          { id: s.id + "-p16", title: "Team Backpack", subtitle: "VBP-30 · blank", sku: "VBP-30", status: "draft", teamSetting: "fan-gear", brand: "Victus", source: "Marucci stock", category: "Bags", stock: "out", deco: "none", decoratable: true, photo: "squad-bag", pricing: pr("52", "52") },
          { id: s.id + "-p17", title: "Bat Pack", subtitle: "MBP-20 · blank", sku: "MBP-20", status: "draft", teamSetting: "fan-gear", brand: "Marucci", source: "Marucci stock", category: "Bags", stock: "in", deco: "none", decoratable: true, pricing: pr("46", "46") },
        ] };
    }
    return { ...s, seeded: true };
  });
  return [...active, ...makeArchivedStores()];
}

// A few permanently shut-down stores, kept as read-only records and surfaced
// in the My Account · Archived Stores section (never in the active stores list).
function makeArchivedStores() {
  const base = [
    { name: "Falcons 2024 Fall Locker", city: "Escondido", state: "California", color: "#3b3f8c", initials: "FA", createdBy: "Hingle McCringleberry", sport: "Football", start: "2024-08-01", end: "2024-10-30", revenue: 4820.5 },
    { name: "Titans Spring Spirit Shop", city: "Vista", state: "California", color: "#0f766e", initials: "TI", createdBy: "George Harrison", sport: "Soccer", start: "2024-03-04", end: "2024-04-20", revenue: 1975.0 },
    { name: "Wildcats Holiday Pop-Up", city: "Carlsbad", state: "California", color: "#9a3412", initials: "WC", createdBy: "Marcy Feldman", sport: "Basketball", start: "2023-11-15", end: "2023-12-24", revenue: 6310.75 },
  ];
  return base.map((b, i) => ({
    id: "archived-" + i,
    ownership: "mine",
    createdByName: b.createdBy,
    storeTitle: b.name,
    teamName: b.name,
    storeType: "single",
    teamColors: [b.color, "#0e0e10", "#ffffff"],
    teamLogo: null,
    sport: b.sport,
    contactPref: "Email & Phone",
    email: "coach@example.com",
    phone: "555-123-4567",
    storeOpenMode: "set-window",
    storeOpenStart: b.start,
    storeOpenEnd: b.end,
    shipFirstName: "Bismo",
    shipLastName: "Funyuns",
    shipName: "Bismo Funyuns",
    shipStreet: "123 Sports Way",
    shipCity: b.city,
    shipState: b.state,
    shipZip: "92009",
    shipCountry: "United States",
    shipOption: "Ship to Coach",
    distributionModel: "coach",
    saveToAddressBook: true,
    collectTax: true,
    taxRate: "7.75",
    fundraisingEnabled: true,
    revenue: b.revenue,
    seeded: true,
    live: false,
    status: "archived",
    archived: true,
    archivedOn: ["2024-11-02", "2024-05-01", "2024-01-10"][i],
    products: [],
  }));
}

function makeSampleLeagues() {
  return []; // Leagues management is out of MVP scope — tab + create/group flows hidden
  return [{
    id: "league-socal",
    name: "SoCal Youth Football League",
    location: "Perrisville, CA",
    avatarColor: "#1d3a8a",
    storeIds: ["seed-0", "seed-1", "seed-2"],
    groups: [
      { id: "grp-8u", name: "8U Division", storeIds: ["seed-0"] },
      { id: "grp-12u", name: "12U Division", storeIds: ["seed-1"] }
    ],
    members: [
      { id: "m-owner", name: "Matt Passmore", email: "matt@prolook.com", role: "Owner", color: "#1f9d55" }
    ]
  }];
}

/* ===== Products Tab Evolution — Confluence spec 4231856129 ===== */
// One canonical product record across four sources (source is an attribute,
// never a type). Table-at-scale list, decoration lifecycle + art approval,
// batch decoration, Stock Vault, three new intake channels, policy touches.
// Zinc-ramp literals match the PROLOOK DS tokens the prototype uses.
const PTE_C = { ink: "#09090b", z800: "#27272a", z700: "#3f3f46", z600: "#52525b", z500: "#71717a", z400: "#a1a1aa", z300: "#d4d4d8", z200: "#e4e4e7", z100: "#f4f4f5", z50: "#fafafa", red: "#e1251b", redInk: "#b81d14", ok: "#1f8a55", okBg: "#e7f5ed", warn: "#b5760a", warnBg: "#fff4e0", blue: "#1d5fbf", blueBg: "#e8f1fd", indigo: "#4353b8", indigoBg: "#eef2ff", rejBg: "#fdeaea" };

// Third-party integration is platform-wide on this fork: every store is seller-of-record Marucci.
function isMultiSourceStore(store) { return true; }

function pteCategoryOf(p) {
  if (p.category) return p.category;
  const t = (p.title || "").toLowerCase();
  if (/(pant|short|legging|tight|jogger)/.test(t)) return "Pants";
  if (/(bag|backpack|towel)/.test(t)) return "Bags";
  if (/(bat|glove)/.test(t)) return "Hard goods";
  if (/(hoodie|fleece|pullover|quarter)/.test(t)) return "Fleece";
  if (/polo/.test(t)) return "Polos";
  if (/(tee|shirt(?! jersey))/.test(t)) return "Tees";
  return "Uniforms";
}
// Canonical-record defaults — legacy single-brand products read as Prolook custom.
function pteMeta(p) {
  return {
    brand: p.brand || "Prolook",
    source: p.source || "Prolook custom",
    category: pteCategoryOf(p),
    stock: p.stock || "na",
    deco: p.deco || "na",
    decoratable: !!p.decoratable,
  };
}
const PTE_DECO = {
  none: { label: "Not decorated", bg: PTE_C.z100, ink: PTE_C.z600, dot: PTE_C.z400 },
  render: { label: "Render pending", bg: PTE_C.indigoBg, ink: PTE_C.indigo, dot: PTE_C.indigo },
  draft: { label: "Art: Draft", bg: PTE_C.warnBg, ink: PTE_C.warn, dot: PTE_C.warn },
  submitted: { label: "Art: Submitted", bg: PTE_C.blueBg, ink: PTE_C.blue, dot: PTE_C.blue },
  approved: { label: "Art: Approved", bg: PTE_C.okBg, ink: PTE_C.ok, dot: PTE_C.ok },
  rejected: { label: "Art: Rejected", bg: PTE_C.rejBg, ink: PTE_C.redInk, dot: PTE_C.redInk },
  na: { label: "—", bg: "transparent", ink: PTE_C.z300, dot: "transparent" },
};
// ── Lifecycle removal (July 2026): single live list, source-keyed locks ──
// Source badge — the third-party build adds Vault and the seller catalog.
function pteSourceBadge(p) {
  const s = String(p.srcKind || p.source || "").toLowerCase();
  const sub = String(p.subtitle || "").toLowerCase() + " " + String(p.style || "").toLowerCase();
  if (s === "vault" || s.includes("vault") || sub.includes("vault")) return "Vault";
  if (s.includes("marucci") || s.includes("stock")) return "Marucci Catalog";
  if (s.includes("design")) return "Saved Design";
  if (s.includes("cart")) return "Saved Cart";
  return "Custom";
}
// Vault / seller-catalog items are not made to order — no design or order locks.
function pteRestricted(p) {
  const b = pteSourceBadge(p);
  return b !== "Vault" && b !== "Marucci Catalog";
}
// Lock trigger = any non-cancelled order (data-driven via p.hasOrders); a bundle
// with orders also locks its restricted members.
function pteHasOrders(p, store) {
  if (p.hasOrders) return true;
  return !!(store && (store.products || []).some((b) => b.isBundle && b.hasOrders && (b.bundleItemIds || []).includes(p.id)));
}
function pteLockState(p, store) {
  if (!pteRestricted(p)) return "none";
  return pteHasOrders(p, store) ? "full" : "design";
}
const PTE_LOCK_TIPS = {
  design: "Design is locked once an item is added to the store.",
  full: "This item has orders — changes are locked because it is made to order.",
};
// Send for Approval renders only when the store has an Active team manager.
function pteHasActiveTM(store) {
  try { return seedTmInvites(store).some((i) => i.status === "active"); } catch (e) { return false; }
}

// Publish gates: render-set existence is policy-independent; art approval
// blocks publish only under the strict (default) policy.
function ptePublishBlocked(p, store) {
  const m = pteMeta(p);
  if (m.deco === "render") return "renders";
  if ((store.artPolicy || "block-publish") !== "block-publish") return null;
  return ["draft", "submitted", "rejected"].includes(m.deco) ? "art" : null;
}
function pteChip(bg, ink, extra) { return Object.assign({ fontSize: 10.5, fontWeight: 700, letterSpacing: ".4px", padding: "3px 9px", borderRadius: 999, background: bg, color: ink, whiteSpace: "nowrap", display: "inline-block" }, extra || {}); }
function pteRetail(p, tier) { const v = parseFloat(String((p.pricing && p.pricing[tier] && p.pricing[tier].retail) != null ? p.pricing[tier].retail : "").replace(/[^0-9.]/g, "")); return isNaN(v) ? null : v; }
function ptePriceText(p) {
  if (p.teamSetting === "team-supplied") return "Covered";
  if (p.isBundle && p.bundleType === "choice") {
    const vals = [pteRetail(p, "youth"), pteRetail(p, "adult")].filter((v) => v != null);
    if (!vals.length) return "—";
    return "from $" + Math.min(...vals).toFixed(2);
  }
  const y = pteRetail(p, "youth"), a = pteRetail(p, "adult");
  if (y == null && a == null) return "—";
  const f = (v) => v == null ? "—" : "$" + (v % 1 ? v.toFixed(2) : v.toFixed(0));
  return f(y) + " / " + f(a);
}
function PteThumb({ product, store, size = 34, radius = 6 }) {
  const m = pteMeta(product);
  if (product.photo) return (
    <span style={{ width: size, height: size, borderRadius: radius, flexShrink: 0, background: "#fafafa", border: "1px solid " + PTE_C.z100, display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 2 }}>
      <CatalogPhoto photo={product.photo} alt={product.title} />
    </span>
  );
  if (product.thumbnail) return <span style={{ width: size, height: size, flexShrink: 0, display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden" }}>{product.thumbnail}</span>;
  return (
    <span style={{ width: size, height: size, borderRadius: radius, flexShrink: 0, background: PTE_C.z100, display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 12, fontWeight: 700, color: PTE_C.z400 }}>
      {store && store.__letterThumbs === false ? "" : m.category.slice(0, 1)}
    </span>
  );
}

// ─────────── Delete / removal — keyed to source + order state ───────────
function ProductDeleteModal({ product, store, onClose, onConfirm, onGoToOrders, onGoToBundle }) {
  if (!product) return null;
  const locked = pteLockState(product, store) === "full";
  // Epic C — bundle protection, keyed to membership. Affected bundles are always named.
  const holdingBundles = product.isBundle ? [] : ((store && store.products) || []).filter((b) => b.isBundle && (b.bundleItemIds || []).includes(product.id));
  const twoMember = holdingBundles.filter((b) => (b.bundleItemIds || []).length === 2);
  const escalate = !locked && twoMember.length > 0;
  const names = (list) => list.map((b) => '“' + (b.title || "Bundle") + '”').join(", ");
  const btn = (kind) => ({
    border: kind === "secondary" ? "1px solid " + PTE_C.z300 : 0, borderRadius: 6, padding: "11px 20px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
    background: kind === "danger" ? PTE_C.red : kind === "primary" ? PTE_C.ink : "#fff",
    color: kind === "secondary" ? PTE_C.ink : "#fff",
  });
  return (
    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 400, padding: 24 }}>
      <div onClick={(e) => e.stopPropagation()} style={{ width: 480, maxWidth: "100%", background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.2)", padding: 28 }}>
        <div style={{ display: "flex", gap: 14, alignItems: "flex-start", marginBottom: 20 }}>
          <span style={{ width: 40, height: 40, borderRadius: "50%", flexShrink: 0, background: locked ? PTE_C.rejBg : PTE_C.z100, color: locked ? PTE_C.redInk : PTE_C.ink, display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
            {locked ? <IconAlert size={18} /> : <IconTrash size={18} />}
          </span>
          <div>
            <div style={{ fontSize: 16, fontWeight: 800, color: PTE_C.ink, fontFamily: "Archivo, sans-serif", marginBottom: 6 }}>
              {locked ? "This item has orders" : escalate ? "This leaves a bundle with one item" : "Delete this product?"}
            </div>
            <div style={{ fontSize: 13, color: PTE_C.z600, lineHeight: 1.55 }}>
              {locked
                ? `“${product.title}” is made to order and can’t be deleted while it has open orders. Cancel the blocking orders from the Orders tab first — once every order is cancelled, delete is permitted.`
                : escalate
                ? `“${product.title}” is one of two items in ${names(twoMember)}. Removing it leaves that bundle with a single member — it becomes Incomplete and drops off the storefront until a second item is added. The bundle itself is not deleted.`
                : holdingBundles.length > 0
                ? `“${product.title}” will be removed from this store’s catalog and will also leave ${names(holdingBundles)}. ${holdingBundles.length === 1 ? "That bundle stays" : "Those bundles stay"} complete. This can’t be undone.`
                : `“${product.title}” will be removed from this store’s catalog. This can’t be undone.`}
            </div>
          </div>
        </div>
        <div style={{ display: "flex", justifyContent: "flex-end", gap: 10 }}>
          {locked ? (
            <React.Fragment>
              <button onClick={onClose} style={btn("secondary")}>Keep in Store</button>
              <button onClick={onGoToOrders} style={btn("primary")}>Go to Orders</button>
            </React.Fragment>
          ) : (
            <React.Fragment>
              <button onClick={onClose} style={btn("secondary")}>Cancel</button>
              {escalate && onGoToBundle && <button onClick={() => onGoToBundle(twoMember[0].id)} style={btn("primary")}>Go to bundle</button>}
              <button onClick={onConfirm} style={btn("danger")}>{escalate ? "Remove anyway" : "Delete"}</button>
            </React.Fragment>
          )}
        </div>
      </div>
    </div>
  );
}

// ─────────── 4 — Store-level category curation ───────────
// Lives on the Products tab (a Settings card links here): the rep orders the
// categories, orders products inside them, pins required uniforms to the top and
// features one item. The order IS the storefront's tab order and browse order,
// so the panel carries a live preview of the result.
function storeCatCfg(store) {
  window.__storeCatCfg = window.__storeCatCfg || {};
  const id = (store && store.id) || "store";
  if (!window.__storeCatCfg[id]) window.__storeCatCfg[id] = { order: null, subs: {}, pins: {}, feat: {}, prodOrder: {}, added: [], subOf: {} };
  return window.__storeCatCfg[id];
}
const CAT_SYS = "Team Gear";
const CAT_NONE = "Uncategorized";
// Same category the catalog table shows: the explicit store category, else the
// product record's own category. Team Gear is a store category like any other —
// what makes it special is that it is system-managed and carries the team
// settings, so team-required / team-supplied items always belong to it.
function productCat(p) {
  const explicit = p.storeCategory || (typeof pteMeta === "function" ? pteMeta(p).category : "");
  const t = p.teamSetting || "fan-gear";
  if (!explicit) return (t === "team-required" || t === "team-supplied") ? CAT_SYS : CAT_NONE;
  return explicit;
}
function isTeamGearItem(p) {
  const t = p.teamSetting || "fan-gear";
  return t === "team-required" || t === "team-supplied" || productCat(p) === CAT_SYS;
}
function storeCatList(store, products) {
  const cfg = storeCatCfg(store);
  const fromWorkspace = (window.__prodCats || []).map((c) => c.name).filter((n) => n !== CAT_SYS);
  const used = Array.from(new Set((products || []).map(productCat)));
  const all = Array.from(new Set([CAT_SYS, ...fromWorkspace, ...cfg.added, ...used.filter((n) => n !== CAT_NONE)]));
  const order = cfg.order || all;
  const sorted = order.filter((n) => all.includes(n)).concat(all.filter((n) => !order.includes(n)));
  if (used.includes(CAT_NONE)) sorted.push(CAT_NONE);
  return sorted;
}
function storeCatProducts(store, products, cat) {
  const cfg = storeCatCfg(store);
  // Team Gear collects every team-required / team-supplied item wherever it is
  // categorized — that section keeps its special behavior.
  const list = (products || []).filter((p) => cat === CAT_SYS ? isTeamGearItem(p) : productCat(p) === cat);
  const ord = cfg.prodOrder[cat] || [];
  const byOrder = (a, b) => {
    const pa = cfg.pins[a.id] ? 0 : 1, pb = cfg.pins[b.id] ? 0 : 1;
    if (pa !== pb) return pa - pb;
    const ia = ord.indexOf(a.id), ib = ord.indexOf(b.id);
    return (ia < 0 ? 999 : ia) - (ib < 0 ? 999 : ib);
  };
  return list.slice().sort(byOrder);
}
function StoreCategoryManager({ store, products, onBack, onToast, onUpdateProduct }) {
  const cfg = storeCatCfg(store);
  const [tick, setTick] = React.useState(0);
  const bump = () => setTick((n) => n + 1);
  const cats = storeCatList(store, products);
  const [sel, setSel] = React.useState(cats[0] || CAT_SYS);
  const [newCat, setNewCat] = React.useState("");
  const [newSub, setNewSub] = React.useState("");
  const [drag, setDrag] = React.useState(null);
  const active = cats.includes(sel) ? sel : cats[0];
  const items = storeCatProducts(store, products, active);
  const countOf = (c) => storeCatProducts(store, products, c).length;
  const publishedIn = (c) => storeCatProducts(store, products, c).filter((p) => p.status !== "draft" && !p.hidden).length;
  const tabs = cats.filter((c) => countOf(c) > 0 && publishedIn(c) > 0);
  const overflow = tabs.slice(4);
  const shown = tabs.slice(0, 4);

  const reorderCats = (from, to) => {
    const list = cats.filter((c) => c !== CAT_NONE);
    if (from < 0 || to < 0 || from >= list.length || to >= list.length) return;
    const next = list.slice();
    next.splice(to, 0, next.splice(from, 1)[0]);
    cfg.order = next;
    bump();
  };
  const reorderProds = (from, to) => {
    const ids = items.map((p) => p.id);
    if (from < 0 || to < 0 || from >= ids.length || to >= ids.length) return;
    const next = ids.slice();
    next.splice(to, 0, next.splice(from, 1)[0]);
    cfg.prodOrder[active] = next;
    bump();
  };
  const addCat = () => {
    const n = newCat.trim();
    if (!n || cats.some((c) => c.toLowerCase() === n.toLowerCase())) return;
    cfg.added = [...cfg.added, n];
    cfg.order = (cfg.order || cats.filter((c) => c !== CAT_NONE)).concat([n]);
    setNewCat(""); setSel(n); bump();
    onToast && onToast("Category \u201c" + n + "\u201d added to this store");
  };
  const addSub = () => {
    const n = newSub.trim();
    if (!n) return;
    cfg.subs[active] = [...(cfg.subs[active] || []), n];
    setNewSub(""); bump();
    onToast && onToast("Subcategory \u201c" + n + "\u201d added under " + active);
  };
  const removeCat = (c) => {
    if (c === CAT_SYS || c === CAT_NONE) return;
    cfg.added = cfg.added.filter((x) => x !== c);
    cfg.order = (cfg.order || cats).filter((x) => x !== c);
    delete cfg.subs[c];
    bump();
    onToast && onToast(countOf(c) > 0
      ? "\u201c" + c + "\u201d removed \u2014 its " + countOf(c) + " products moved to Uncategorized"
      : "\u201c" + c + "\u201d removed");
  };
  const togglePin = (p) => { cfg.pins[p.id] = !cfg.pins[p.id]; bump(); onToast && onToast(cfg.pins[p.id] ? "\u201c" + p.title + "\u201d pinned to the top of " + active : "Unpinned \u201c" + p.title + "\u201d"); };
  const toggleFeat = (p) => { cfg.feat[p.id] = !cfg.feat[p.id]; bump(); onToast && onToast(cfg.feat[p.id] ? "\u201c" + p.title + "\u201d featured in " + active : "Removed the feature treatment"); };

  const handle = (
    <svg width="12" height="12" viewBox="0 0 12 12" fill="currentColor" style={{ flexShrink: 0, opacity: .5 }}>
      <circle cx="4" cy="2.5" r="1.1" /><circle cx="8" cy="2.5" r="1.1" /><circle cx="4" cy="6" r="1.1" /><circle cx="8" cy="6" r="1.1" /><circle cx="4" cy="9.5" r="1.1" /><circle cx="8" cy="9.5" r="1.1" />
    </svg>
  );
  const col = { background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: 16, minWidth: 0 };
  const hdr = (t, s) => (
    <div style={{ marginBottom: 12 }}>
      <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".06em", color: PTE_C.ink }}>{t}</div>
      <div style={{ fontSize: 11.5, color: PTE_C.z500, marginTop: 3, lineHeight: 1.45 }}>{s}</div>
    </div>
  );
  return (
    <div style={{ paddingBottom: 100 }}>
      <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 6 }}>
        {onBack &&
          <button onClick={onBack} style={{ display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "7px 12px", fontSize: 12.5, fontWeight: 700, color: PTE_C.ink, cursor: "pointer", fontFamily: "inherit" }}>
            <IconChevronLeft size={13} /> Catalog
          </button>}
        <h2 style={{ margin: 0, fontSize: 20, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Categories &amp; Sorting</h2>
        <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.z600, background: PTE_C.z100, borderRadius: 999, padding: "3px 9px" }}>THIS STORE ONLY</span>
      </div>
      <p style={{ margin: "0 0 18px", fontSize: 13, color: PTE_C.z500, maxWidth: 780, lineHeight: 1.55 }}>
        This order is what buyers see: categories become the storefront header tabs, and the product order inside a category is its browse order. Workspace categories are the starting set — anything you add here belongs to this store.
      </p>
      <div style={{ display: "grid", gridTemplateColumns: "minmax(240px,270px) minmax(420px,1.6fr) minmax(300px,1fr)", gap: 16, alignItems: "start" }}>
        {/* Categories */}
        <div style={col}>
          {hdr("CATEGORY ORDER", "Drag to reorder. Team Gear is system-managed \u2014 it can move, but not be renamed or removed.")}
          {cats.map((c, i) => {
            const on = c === active;
            const sys = c === CAT_SYS, none = c === CAT_NONE;
            const n = countOf(c), pub = publishedIn(c);
            return (
              <div key={c}
                draggable={!none}
                onDragStart={() => setDrag({ kind: "cat", i })}
                onDragOver={(e) => { e.preventDefault(); if (drag && drag.kind === "cat" && drag.i !== i && !none) { reorderCats(drag.i, i); setDrag({ kind: "cat", i }); } }}
                onDragEnd={() => setDrag(null)}
                onClick={() => setSel(c)}
                style={{ display: "flex", alignItems: "center", gap: 9, padding: "9px 10px", marginBottom: 5, borderRadius: 7, cursor: "pointer", background: on ? PTE_C.z50 : "#fff", border: "1px solid " + (on ? PTE_C.ink : PTE_C.z200) }}>
                {!none && handle}
                <span style={{ minWidth: 0, flex: 1 }}>
                  <span style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: none ? PTE_C.z500 : PTE_C.ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{c}</span>
                  <span style={{ display: "block", fontSize: 10.5, color: PTE_C.z400, marginTop: 1 }}>
                    {n === 0 ? "Empty \u2014 no storefront tab" : pub === 0 ? n + " item" + (n === 1 ? "" : "s") + " \u00b7 none published" : n + " item" + (n === 1 ? "" : "s")}
                  </span>
                </span>
                {sys && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.z600, background: PTE_C.z100, borderRadius: 999, padding: "2px 7px", flexShrink: 0 }}>SYSTEM</span>}
                {!sys && !none &&
                  <button onClick={(e) => { e.stopPropagation(); removeCat(c); }} title={"Remove " + c} style={{ background: "none", border: 0, padding: 2, cursor: "pointer", color: PTE_C.z400, display: "inline-flex", flexShrink: 0 }}><IconTrash size={12} /></button>}
              </div>
            );
          })}
          <div style={{ display: "flex", gap: 6, marginTop: 10 }}>
            <input value={newCat} onChange={(e) => setNewCat(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") addCat(); }} placeholder="New category"
              style={{ flex: 1, minWidth: 0, height: 32, boxSizing: "border-box", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "0 10px", fontSize: 12.5, fontFamily: "inherit", outline: "none" }} />
            <button onClick={addCat} disabled={!newCat.trim()} style={{ background: newCat.trim() ? PTE_C.ink : PTE_C.z100, color: newCat.trim() ? "#fff" : PTE_C.z400, border: 0, borderRadius: 6, padding: "0 12px", height: 32, fontSize: 12, fontWeight: 700, cursor: newCat.trim() ? "pointer" : "default", fontFamily: "inherit" }}>Add</button>
          </div>
        </div>

        {/* Products in the selected category */}
        <div style={col}>
          {hdr(String(active || "").toUpperCase() + " \u00b7 PRODUCT ORDER", "Drag to reorder. Pin moves an item to the top and keeps it there; Feature only changes emphasis.")}
          {/* Subcategories — one level, never deeper */}
          <div style={{ display: "flex", alignItems: "center", gap: 6, flexWrap: "wrap", marginBottom: 12, paddingBottom: 12, borderBottom: "1px solid " + PTE_C.z100 }}>
            <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.z500 }}>SUBCATEGORIES</span>
            {(cfg.subs[active] || []).map((s) => (
              <span key={s} style={{ display: "inline-flex", alignItems: "center", gap: 5, fontSize: 11.5, fontWeight: 600, color: PTE_C.z700, background: PTE_C.z50, border: "1px solid " + PTE_C.z200, borderRadius: 999, padding: "3px 9px" }}>
                {s}
                <button onClick={() => { cfg.subs[active] = (cfg.subs[active] || []).filter((x) => x !== s); bump(); }} style={{ background: "none", border: 0, padding: 0, cursor: "pointer", color: PTE_C.z400, display: "inline-flex" }}><IconX size={10} /></button>
              </span>
            ))}
            {(cfg.subs[active] || []).length === 0 && <span style={{ fontSize: 11.5, color: PTE_C.z400 }}>None</span>}
            <span style={{ display: "inline-flex", gap: 5, marginLeft: "auto" }}>
              <input value={newSub} onChange={(e) => setNewSub(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") addSub(); }} placeholder="Add subcategory"
                style={{ width: 132, height: 28, boxSizing: "border-box", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "0 9px", fontSize: 11.5, fontFamily: "inherit", outline: "none" }} />
            </span>
          </div>
          {items.length === 0 &&
            <div style={{ padding: "34px 12px", textAlign: "center", fontSize: 12.5, color: PTE_C.z400, lineHeight: 1.5 }}>
              Nothing in {active} yet. {active === CAT_NONE ? "Assign a category on each product to get it out of this bucket." : "Set this category on a product in the catalog and it appears here."}
            </div>}
          {items.length > 0 && publishedIn(active) === 0 &&
            <div style={{ display: "flex", gap: 8, alignItems: "flex-start", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 7, padding: "9px 11px", marginBottom: 10, fontSize: 11.5, color: "#6b5314", lineHeight: 1.45 }}>
              <IconAlert size={12} style={{ flexShrink: 0, marginTop: 1, color: "#8a5f10" }} /> Every item here is a draft or hidden, so this category won’t render a storefront tab yet.
            </div>}
          {items.map((p, i) => {
            const pinned = !!cfg.pins[p.id];
            const feat = !!cfg.feat[p.id];
            const draft = p.status === "draft";
            return (
              <div key={p.id}
                draggable
                onDragStart={() => setDrag({ kind: "prod", i })}
                onDragOver={(e) => { e.preventDefault(); if (drag && drag.kind === "prod" && drag.i !== i) { reorderProds(drag.i, i); setDrag({ kind: "prod", i }); } }}
                onDragEnd={() => setDrag(null)}
                style={{ display: "flex", alignItems: "flex-start", gap: 10, padding: "10px 11px", marginBottom: 6, borderRadius: 7, background: "#fff", border: "1px solid " + (feat ? PTE_C.red : PTE_C.z200) }}>
                {handle}
                <span style={{ width: 20, fontSize: 11, fontWeight: 800, color: PTE_C.z400, fontVariantNumeric: "tabular-nums", flexShrink: 0 }}>{i + 1}</span>
                <PteThumb product={p} store={store} />
                <span style={{ minWidth: 0, flex: 1 }}>
                  <span title={p.title} style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: PTE_C.ink, lineHeight: 1.3, textWrap: "pretty" }}>{p.title}</span>
                  <span style={{ display: "flex", gap: 6, marginTop: 3, alignItems: "center", flexWrap: "wrap" }}>
                    {pinned && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.ink, background: PTE_C.z100, borderRadius: 3, padding: "1px 5px" }}>PINNED</span>}
                    {feat && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#fff", background: PTE_C.red, borderRadius: 3, padding: "1px 5px" }}>FEATURED</span>}
                    {draft && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.z500, background: PTE_C.z100, borderRadius: 3, padding: "1px 5px" }}>DRAFT</span>}
                    {p.hidden && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.z500, background: PTE_C.z100, borderRadius: 3, padding: "1px 5px" }}>HIDDEN</span>}
                    {!pinned && !feat && !draft && !p.hidden && <span style={{ fontSize: 10.5, color: PTE_C.z400 }}>{p.subtitle || "Published"}</span>}
                  </span>
                  <span style={{ display: "flex", gap: 6, marginTop: 7 }}>
                    <button onClick={() => togglePin(p)} title="Pin to the top of this category" style={{ background: pinned ? PTE_C.ink : "#fff", color: pinned ? "#fff" : PTE_C.z600, border: "1px solid " + (pinned ? PTE_C.ink : PTE_C.z300), borderRadius: 6, padding: "5px 10px", fontSize: 11, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{pinned ? "Pinned" : "Pin"}</button>
                    <button onClick={() => toggleFeat(p)} title="Feature this product — emphasis only, position unchanged" style={{ background: feat ? PTE_C.red : "#fff", color: feat ? "#fff" : PTE_C.z600, border: "1px solid " + (feat ? PTE_C.red : PTE_C.z300), borderRadius: 6, padding: "5px 10px", fontSize: 11, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{feat ? "Featured" : "Feature"}</button>
                  </span>
                </span>
              </div>
            );
          })}
        </div>

        {/* Storefront preview */}
        <div style={{ ...col, position: "sticky", top: 12 }}>
          {hdr("STOREFRONT PREVIEW", "Tabs render every category that has a published item; the rest fold into More.")}
          <div style={{ border: "1px solid " + PTE_C.z200, borderRadius: 8, overflow: "hidden" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 6, padding: "10px 12px", borderBottom: "1px solid " + PTE_C.z200, background: PTE_C.z50, flexWrap: "wrap" }}>
              {shown.map((c) => (
                <span key={c} onClick={() => setSel(c)} style={{ cursor: "pointer", fontSize: 10.5, fontWeight: 800, letterSpacing: ".05em", textTransform: "uppercase", color: c === active ? PTE_C.red : PTE_C.z600, borderBottom: "2px solid " + (c === active ? PTE_C.red : "transparent"), paddingBottom: 2 }}>{c}</span>
              ))}
              {overflow.length > 0 && <span title={overflow.join(", ")} style={{ fontSize: 10.5, fontWeight: 800, letterSpacing: ".05em", textTransform: "uppercase", color: PTE_C.z500 }}>More ▾ ({overflow.length})</span>}
              {tabs.length === 0 && <span style={{ fontSize: 11.5, color: PTE_C.z400 }}>No published items yet — no tabs.</span>}
            </div>
            <div style={{ padding: 12 }}>
              {active === CAT_SYS &&
                <div style={{ display: "flex", gap: 7, alignItems: "flex-start", background: "#eef3fb", border: "1px solid #cfe0f5", borderRadius: 7, padding: "8px 10px", marginBottom: 10, fontSize: 11, color: "#1e4e8c", lineHeight: 1.45 }}>
                  <IconInfo size={12} style={{ flexShrink: 0, marginTop: 1 }} /> Team Gear keeps its required-gear nudge — required for rostered players, visible but optional for fans.
                </div>}
              {items.filter((p) => p.status !== "draft" && !p.hidden).length === 0
                ? <div style={{ fontSize: 11.5, color: PTE_C.z400, padding: "18px 0", textAlign: "center" }}>Nothing published in {active}.</div>
                : items.filter((p) => p.status !== "draft" && !p.hidden).map((p, i) => {
                  const feat = !!cfg.feat[p.id];
                  return (
                    <div key={p.id} style={{ display: "flex", alignItems: "center", gap: 9, padding: feat ? "10px 10px" : "7px 10px", marginBottom: 6, borderRadius: 7, background: feat ? "#fff7f6" : PTE_C.z50, border: "1px solid " + (feat ? PTE_C.red : PTE_C.z200) }}>
                      <span style={{ width: 16, fontSize: 10.5, fontWeight: 800, color: PTE_C.z400 }}>{i + 1}</span>
                      <span style={{ flex: 1, minWidth: 0, fontSize: feat ? 12.5 : 11.5, fontWeight: feat ? 800 : 600, color: PTE_C.ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{p.title}</span>
                      {cfg.pins[p.id] && <span style={{ fontSize: 8.5, fontWeight: 800, letterSpacing: ".05em", color: PTE_C.z600 }}>PINNED</span>}
                      {feat && <span style={{ fontSize: 8.5, fontWeight: 800, letterSpacing: ".05em", color: "#fff", background: PTE_C.red, borderRadius: 3, padding: "1px 5px" }}>FEATURED</span>}
                    </div>
                  );
                })}
            </div>
          </div>
          <div style={{ marginTop: 10, fontSize: 11, color: PTE_C.z400, lineHeight: 1.5 }}>
            Featured treatment is a badge and accent border in place — pending stakeholder sign-off on how loud it should be, and how many items may carry it.
          </div>
        </div>
      </div>
    </div>
  );
}

// ─────────── Products list V2 — table at scale (Epic A) ───────────
function ProductsListV2({ onManageCategories, products, store, multiSource, onPublish, onUnpublish, onSendForApproval, onRemove, onAddProducts, onCreateBundle, onUpdateProduct, onBulkPrice, onOpenImages, onOpenEditor, onDecorateBatch, onPatchStore, onToast }) {
  const [q, setQ] = React.useState("");
  const [deleting, setDeleting] = React.useState(null); // product pending delete confirmation
  const [sel, setSel] = React.useState({});
  const [menuId, setMenuId] = React.useState(null);
  const [colOpen, setColOpen] = React.useState(false);
  const [editingId, setEditingId] = React.useState(null);
  const [bulkOpen, setBulkOpen] = React.useState(false);
  const [cols, setCols] = React.useState(() => multiSource
    ? { brand: true, source: true, cat: true, stock: true, deco: true, teams: false }
    : { brand: false, source: false, cat: true, stock: false, deco: false, teams: true });
  const [facets, setFacets] = React.useState({ brand: "All", source: "All", cat: "All", stock: "All", deco: "All", set: "All", vis: "All", fund: "All" });
  const setFacet = (k, v) => setFacets((f) => ({ ...f, [k]: v }));
  // Deep link from Settings — filter to items still collecting fundraising.
  React.useEffect(() => {
    if (window.__pteFilterCollecting) { window.__pteFilterCollecting = false; setFacet("fund", "Collecting"); }
  }, []);

  const reqGroups = store.reqGroups || [];
  const metaOf = pteMeta;
  const match = (p) => {
    const m = metaOf(p);
    if (q && !((p.title || "") + " " + (p.subtitle || "") + " " + (p.sku || "")).toLowerCase().includes(q.toLowerCase())) return false;
    if (facets.brand !== "All" && m.brand !== facets.brand) return false;
    if (facets.source !== "All" && m.source !== facets.source) return false;
    if (facets.cat !== "All" && (p.storeCategory || m.category) !== facets.cat) return false;
    if (facets.stock !== "All" && ({ "In stock": "in", "Out of stock": "out", "N/A (made to order)": "na" })[facets.stock] !== m.stock) return false;
    if (facets.deco !== "All") {
      const dm = { "Not decorated": "none", "Render pending": "render", "Art: Draft": "draft", "Art: Submitted": "submitted", "Art: Approved": "approved", "Art: Rejected": "rejected" };
      if (dm[facets.deco] !== m.deco) return false;
    }
    if (facets.set !== "All" && ({ "Team Required": "team-required", "Team Supplied": "team-supplied", "Fan Gear": "fan-gear" })[facets.set] !== (p.teamSetting || "fan-gear")) return false;
    if (facets.vis !== "All" && (facets.vis === "Hidden") !== !!p.hidden) return false;
    if (facets.fund !== "All" && !(itemFundOn(p, store) && itemFundAmount(p) > 0)) return false;
    return true;
  };
  const filtered = products.filter(match);
  const visible = filtered;
  const anyFilter = !!q || Object.values(facets).some((v) => v !== "All");
  const total = products.length;

  const presentCats = Array.from(new Set(products.map((p) => p.storeCategory || metaOf(p).category)));
  const facetDefs = [
    multiSource && { k: "brand", label: "Brand", opts: ["All", "Marucci", "Victus", "Prolook"] },
    multiSource && { k: "source", label: "Source", opts: ["All", "Prolook custom", "Marucci stock", "Decorated stock", "External customizer"] },
    { k: "cat", label: "Category", opts: ["All"].concat(presentCats) },
    multiSource && { k: "stock", label: "Stock", opts: ["All", "In stock", "Out of stock", "N/A (made to order)"] },
    multiSource && { k: "deco", label: "Decoration", opts: ["All", "Not decorated", "Render pending", "Art: Draft", "Art: Submitted", "Art: Approved", "Art: Rejected"] },
    { k: "set", label: "Setting", opts: ["All", "Team Required", "Team Supplied", "Fan Gear"] },
    { k: "vis", label: "Visibility", opts: ["All", "Visible", "Hidden"] },
    { k: "fund", label: "Fundraising", opts: ["All", "Collecting"] },
  ].filter(Boolean);
  const colDefs = [
    multiSource && ["brand", "Brand"], multiSource && ["source", "Source"], ["cat", "Category"],
    multiSource && ["stock", "Stock"], multiSource && ["deco", "Decoration"], ["teams", "Teams"],
  ].filter(Boolean);

  const gridCols = ["36px", "minmax(230px,1.6fr)", cols.brand && "minmax(86px,.7fr)", cols.source && "minmax(112px,.9fr)", cols.cat && "minmax(90px,.8fr)", cols.stock && "minmax(96px,.8fr)", cols.deco && "minmax(120px,1fr)", "minmax(100px,.9fr)", cols.teams && "minmax(96px,.8fr)", "minmax(96px,.8fr)", "minmax(100px,.9fr)", "76px"].filter(Boolean).join(" ");
  const rowGrid = { display: "grid", gridTemplateColumns: gridCols, gap: 14, alignItems: "center", padding: "7px 20px" };

  const selIds = visible.filter((p) => sel[p.id]).map((p) => p.id);
  const selProds = visible.filter((p) => sel[p.id]);
  const decoSel = selProds.filter((p) => metaOf(p).decoratable);
  const reqSel = selProds.filter((p) => p.teamSetting === "team-required");
  const allChecked = visible.length > 0 && visible.every((p) => sel[p.id]);
  const editingProduct = products.find((p) => p.id === editingId) || null;
  const lockedSel = selProds.filter((p) => pteLockState(p, store) === "full");
  const hasActiveTM = pteHasActiveTM(store);
  const saveToVault = (arr) => {
    window.__vaultItems = window.__vaultItems || [];
    arr.forEach((p) => {
      const m = metaOf(p);
      window.__vaultItems.push({ id: "v-" + p.id + "-" + Date.now(), title: p.title, brand: m.brand, source: m.source, photo: p.photo, category: m.category, deco: (store.teamName || "Team") + " mark · Left chest 3.5in · Embroidery · " + (m.deco === "approved" ? "Approved" : "Art " + m.deco), used: "This store" });
    });
  };
  const groupSelected = () => {
    const name = metaOf(reqSel[0]).category === "Bags" ? "Bag" : "Gear";
    const tag = name + " · 1 of " + reqSel.length;
    const ids = new Set(reqSel.map((p) => p.id));
    onPatchStore({
      products: products.map((p) => ids.has(p.id) ? { ...p, group: tag } : p),
      reqGroups: [...reqGroups, { name, count: reqSel.length }],
    });
    setSel({});
    onToast && onToast("Requirement group created — buyers pick 1 of " + reqSel.length);
  };

  const pillStyle = (on) => ({ background: on ? PTE_C.ink : "#fff", color: on ? "#fff" : PTE_C.ink, border: "1px solid " + (on ? PTE_C.ink : PTE_C.z300), padding: "8px 20px", borderRadius: 999, fontSize: 13, fontWeight: 600, cursor: "pointer", fontFamily: "inherit", display: "inline-flex", alignItems: "center", gap: 8 });
  const cntStyle = (on) => ({ fontSize: 11, fontWeight: 700, borderRadius: 999, padding: "1px 8px", background: on ? "rgba(255,255,255,.18)" : PTE_C.z100, color: on ? "#fff" : PTE_C.z600 });
  const selArrow = (active) => "url('data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2210%22 height=%226%22%3E%3Cpath d=%22M1 1l4 4 4-4%22 stroke=%22" + (active ? "%23fff" : "%23666") + "%22 fill=%22none%22 stroke-width=%221.5%22/%3E%3C/svg%3E')";
  const facetStyle = (active) => ({ appearance: "none", WebkitAppearance: "none", border: "1px solid " + (active ? PTE_C.ink : PTE_C.z300), background: (active ? PTE_C.ink : "#fff") + " " + selArrow(active) + " no-repeat right 10px center", color: active ? "#fff" : PTE_C.z700, borderRadius: 999, padding: "0 30px 0 12px", height: 32, boxSizing: "border-box", fontSize: 12.5, fontWeight: 600, fontFamily: "inherit", cursor: "pointer" });
  const headCell = { fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z400 };
  const bulkBtn = (kind) => ({ background: kind === "primary" ? "#fff" : "transparent", color: kind === "primary" ? PTE_C.ink : "#fff", border: kind === "primary" ? 0 : "1px solid rgba(255,255,255,.25)", borderRadius: 6, padding: "8px 13px", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" });

  return (
    <div style={{ paddingBottom: 100 }}>
      {(menuId || colOpen) && <div onClick={() => { setMenuId(null); setColOpen(false); }} style={{ position: "fixed", inset: 0, zIndex: 30 }} />}

      {/* Pills + result line + primary actions */}
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, flexWrap: "wrap", marginBottom: 18 }}>
        <div style={{ display: "flex", gap: 12, alignItems: "center" }}>
          <span style={{ fontSize: 16, fontWeight: 800, color: PTE_C.ink, fontFamily: "Archivo, sans-serif" }}>Catalog</span>
          <span style={{ fontSize: 11.5, fontWeight: 700, borderRadius: 999, padding: "2px 10px", background: PTE_C.z100, color: PTE_C.z600 }}>{total}</span>
          {anyFilter
            ? <span style={{ fontSize: 12, color: PTE_C.z400, marginLeft: 8 }}>{visible.length} of {total} match filters</span>
            : <span style={{ fontSize: 12, color: PTE_C.z400, marginLeft: 8 }}>Items are live on the storefront once the store launches.</span>}
        </div>
        <div style={{ display: "flex", gap: 10 }}>
          <button onClick={onAddProducts} style={{ background: PTE_C.ink, color: "#fff", border: 0, borderRadius: 6, padding: "10px 20px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>+ Add Products</button>
          <button onClick={onCreateBundle} style={{ background: "#fff", color: PTE_C.ink, border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "10px 16px", fontSize: 13, fontWeight: 600, cursor: "pointer", fontFamily: "inherit" }}>Create Bundle</button>
        </div>
      </div>

      {/* Search + faceted filters + column control */}
      <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap", marginBottom: 10 }}>
        <input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search products…" style={{ width: 220, height: 32, boxSizing: "border-box", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "0 12px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff" }} />
        {facetDefs.map((f) => (
          <select key={f.k} value={facets[f.k] === "All" ? f.label + ": All" : facets[f.k]} onChange={(e) => setFacet(f.k, e.target.value.startsWith(f.label + ": ") ? e.target.value.split(": ")[1] : e.target.value)} style={facetStyle(facets[f.k] !== "All")}>
            {f.opts.map((o) => <option key={o} value={o === "All" ? f.label + ": All" : o}>{o === "All" ? f.label + ": All" : o}</option>)}
          </select>
        ))}
        {anyFilter && <button onClick={() => { setQ(""); setFacets({ brand: "All", source: "All", cat: "All", stock: "All", deco: "All", set: "All", vis: "All" }); }} style={{ background: "none", border: 0, fontSize: 12, fontWeight: 600, color: PTE_C.z400, cursor: "pointer", fontFamily: "inherit" }}>Clear</button>}
        <div style={{ flex: 1 }} />
        <div style={{ position: "relative" }}>
          <button onClick={() => setColOpen((v) => !v)} style={{ background: "#fff", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "0 14px", height: 32, boxSizing: "border-box", fontSize: 12.5, fontWeight: 600, cursor: "pointer", fontFamily: "inherit", color: PTE_C.z700 }}>Columns ▾</button>
          {colOpen && (
            <div style={{ position: "absolute", right: 0, top: "calc(100% + 6px)", zIndex: 40, background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 8, boxShadow: "0 8px 24px rgba(20,20,30,.12)", padding: 8, minWidth: 170 }}>
              {colDefs.map(([k, label]) => (
                <button key={k} onClick={() => setCols((c) => ({ ...c, [k]: !c[k] }))} style={{ display: "flex", gap: 8, alignItems: "center", width: "100%", textAlign: "left", background: "none", border: 0, borderRadius: 6, padding: "7px 10px", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: PTE_C.z800, cursor: "pointer" }}>
                  <span style={{ width: 14, display: "inline-block", color: cols[k] ? PTE_C.ink : "transparent", fontWeight: 700 }}>✓</span>{label}
                </button>
              ))}
            </div>
          )}
        </div>
      </div>

      {/* Requirement-groups strip (Epic E) */}
      {reqGroups.length > 0 && (
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 12, padding: "10px 14px", background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 8, flexWrap: "wrap" }}>
          <span style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z500 }}>Requirement groups</span>
          {reqGroups.map((g, i) => <span key={i} style={{ fontSize: 12, fontWeight: 600, background: PTE_C.z100, borderRadius: 999, padding: "4px 12px", color: PTE_C.z800 }}>{g.name} — pick 1 of {g.count}</span>)}
          <span style={{ fontSize: 12, color: PTE_C.z400 }}>Buyers satisfy the requirement with any one member. Select 2+ Required items to create a group.</span>
        </div>
      )}

      {/* Table */}
      <div style={{ background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, overflowX: "auto" }}>
        <div style={{ ...rowGrid, padding: "10px 20px", borderBottom: "1px solid " + PTE_C.z200, background: PTE_C.z50 }}>
          <span><input type="checkbox" checked={allChecked} onChange={() => { const next = { ...sel }; visible.forEach((p) => { next[p.id] = !allChecked; }); setSel(next); }} style={{ width: 15, height: 15, accentColor: PTE_C.ink, cursor: "pointer" }} /></span>
          <span style={headCell}>Product</span>
          {cols.brand && <span style={headCell}>Brand</span>}
          {cols.source && <span style={headCell}>Source</span>}
          {cols.cat && <span style={headCell}>Category</span>}
          {cols.stock && <span style={headCell}>Stock</span>}
          {cols.deco && <span style={headCell}>Decoration</span>}
          <span style={headCell}>Setting</span>
          {cols.teams && <span style={headCell}>Teams</span>}
          <span style={headCell}>Storefront</span>
          <span style={{ ...headCell, textAlign: "right" }}>Youth / Adult</span>
          <span />
        </div>
        {visible.length === 0 && (
          products.length === 0 && !anyFilter ? (
            <div style={{ display: "flex", flexDirection: "column", alignItems: "center", padding: "88px 20px 96px", textAlign: "center" }}>
              <div style={{ width: 56, height: 56, borderRadius: "50%", background: PTE_C.z100, color: PTE_C.ink, display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}>
                <IconShirt size={26} />
              </div>
              <div style={{ fontSize: 20, fontWeight: 800, color: PTE_C.ink, marginBottom: 8, fontFamily: "Archivo, sans-serif" }}>No products yet</div>
              <div style={{ color: PTE_C.z600, fontSize: 14, maxWidth: 420, lineHeight: 1.55, marginBottom: 22 }}>
                Pull products from your saved designs and carts, or customize a new one, to start building this store.
              </div>
              <button type="button" onClick={onAddProducts} style={{ background: PTE_C.ink, color: "#fff", border: 0, borderRadius: 6, padding: "12px 28px", fontSize: 14, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>+ Add Products</button>
            </div>
          ) : (
            <div style={{ padding: "72px 20px", textAlign: "center", color: PTE_C.z500, fontSize: 14 }}>
              {anyFilter ? "No products match these filters." : "No products yet — added items appear here."}
            </div>
          )
        )}
        {visible.map((p) => {
          const m = metaOf(p);
          const dm = PTE_DECO[m.deco] || PTE_DECO.na;
          const lock = pteLockState(p, store);
          const setMeta = { "team-required": ["REQUIRED", PTE_C.ink, "#fff"], "team-supplied": ["SUPPLIED", PTE_C.z100, PTE_C.z700], "fan-gear": ["FAN GEAR", "#fff", PTE_C.z500] }[p.teamSetting || "fan-gear"];
          const stockMeta = m.stock === "in" ? ["In stock", PTE_C.okBg, PTE_C.ok] : m.stock === "out" ? [(store.oosTreatment || "hidden") === "hidden" ? "Out · hidden" : "Out of stock", PTE_C.z800, "#fff"] : ["Made to order", PTE_C.z50, PTE_C.z400];
          const teamsText = p.teams || (p.rosters && p.rosters.length ? p.rosters.length + (p.rosters.length === 1 ? " team" : " teams") : "Available league-wide");
          const menuItems = [];
          menuItems.push({ label: "Update Product", act: () => setEditingId(p.id) });
          if (hasActiveTM) menuItems.push({ label: "Send for Approval", note: "Optional — reviewed by this store's team manager", act: () => onSendForApproval && onSendForApproval(p.id) });
          // Customize (Artwork Editor) — Pro Stock / Sideline Stock only; brand-catalog items are made to order.
          if (!pteRestricted(p) || m.decoratable) menuItems.push({ label: "Customize", act: () => onOpenEditor && onOpenEditor(p.id) });
          if (p.teamSetting === "team-required") menuItems.push({ label: "Allow opt-out: " + (p.optOut ? "On" : "Off"), note: "“I already own this” counts as satisfied", act: () => { onUpdateProduct({ ...p, optOut: !p.optOut }); onToast && onToast(p.optOut ? "Opt-out disabled" : "Opt-out enabled — counts as satisfied in required-gear tracking"); } });
          if (multiSource) menuItems.push({ label: "Save to Vault", note: "Carries source, brand, decoration record", act: () => { saveToVault([p]); onToast && onToast("Saved “" + p.title + "” to Stock Vault"); } });
          menuItems.push({ label: "Delete", danger: true, act: () => setDeleting(p) });
          return (
            <div key={p.id} style={{ ...rowGrid, borderBottom: "1px solid " + PTE_C.z100, minHeight: 48, background: sel[p.id] ? PTE_C.z50 : "#fff" }}>
              <span><input type="checkbox" checked={!!sel[p.id]} onChange={() => setSel((s) => ({ ...s, [p.id]: !s[p.id] }))} style={{ width: 15, height: 15, accentColor: PTE_C.ink, cursor: "pointer" }} /></span>
              <span style={{ display: "flex", alignItems: "center", gap: 10, minWidth: 0, opacity: p.hidden ? 0.55 : 1 }}>
                <PteThumb product={p} store={store} />
                <span style={{ minWidth: 0 }}>
                  <button onClick={() => (!pteRestricted(p) || m.decoratable) && onOpenEditor ? onOpenEditor(p.id) : setEditingId(p.id)} style={{ background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 13.5, fontWeight: 600, color: PTE_C.ink, cursor: "pointer", textAlign: "left", display: "block", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", maxWidth: 230 }}
                    onMouseEnter={(e) => e.currentTarget.style.color = PTE_C.redInk} onMouseLeave={(e) => e.currentTarget.style.color = PTE_C.ink}>{p.title}</button>
                  <span style={{ fontSize: 11, color: PTE_C.z400, display: "flex", gap: 6, alignItems: "center" }}>
                    {lock !== "none" && <span title={PTE_LOCK_TIPS[lock]} style={{ display: "inline-flex", alignItems: "center", color: lock === "full" ? PTE_C.redInk : PTE_C.z500, cursor: "help", flexShrink: 0 }}><IconLock size={11} style={{ strokeWidth: 2 }} /></span>}
                    <span style={{ whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis", maxWidth: 190 }}>{p.subtitle || p.sku || m.source}</span>
                    {p.group && <span style={{ fontSize: 10, fontWeight: 700, color: PTE_C.z600, background: PTE_C.z100, borderRadius: 4, padding: "1px 6px", whiteSpace: "nowrap" }}>{p.group}</span>}
                    {p.teamSetting === "team-required" && p.optOut && <span style={{ fontSize: 10, fontWeight: 700, color: PTE_C.blue, background: PTE_C.blueBg, borderRadius: 4, padding: "1px 6px", whiteSpace: "nowrap" }}>OPT-OUT OK</span>}
                  </span>
                </span>
              </span>
              {cols.brand && <span><span style={pteChip(m.brand === "Prolook" ? PTE_C.ink : PTE_C.z100, m.brand === "Prolook" ? "#fff" : PTE_C.z700)}>{m.brand.toUpperCase()}</span></span>}
              {cols.source && <span><span style={pteChip(PTE_C.z100, PTE_C.z700)}>{pteSourceBadge(p).toUpperCase()}</span></span>}
              {cols.cat && <span style={{ fontSize: 12.5, color: PTE_C.z600 }}>{p.storeCategory || m.category}{p.storeSubcategory ? <span style={{ color: PTE_C.z400 }}> · {p.storeSubcategory}</span> : null}</span>}
              {cols.stock && <span><span style={pteChip(stockMeta[1], stockMeta[2])}>{stockMeta[0]}</span></span>}
              {cols.deco && <span>{m.deco === "na" ? <span style={{ fontSize: 12, color: PTE_C.z300 }}>—</span> : <span style={pteChip(dm.bg, dm.ink, { display: "inline-flex", alignItems: "center", gap: 5 })}><span style={{ width: 5, height: 5, borderRadius: 999, background: dm.dot, display: "inline-block" }} />{dm.label}</span>}</span>}
              <span><span style={pteChip(setMeta[1], setMeta[2], p.teamSetting === "fan-gear" || !p.teamSetting ? { border: "1px solid " + PTE_C.z200 } : null)}>{setMeta[0]}</span></span>
              {cols.teams && <span style={{ fontSize: 12.5, color: PTE_C.z600 }}>{teamsText}</span>}
              <span>
                <button onClick={() => { onUpdateProduct({ ...p, hidden: !p.hidden }); onToast && onToast(p.hidden ? `"${p.title}" is visible on the storefront` : `"${p.title}" hidden from the storefront — orders, category and lock state are kept`); }}
                  title={p.hidden ? "Hidden from the storefront — click to show" : "Visible on the storefront — click to hide"}
                  style={{ display: "inline-flex", alignItems: "center", gap: 7, background: "none", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit" }}>
                  <span style={{ width: 30, height: 17, borderRadius: 999, position: "relative", flexShrink: 0, background: p.hidden ? PTE_C.z300 : PTE_C.ok, transition: "background .14s" }}>
                    <span style={{ position: "absolute", top: 2, left: p.hidden ? 2 : 15, width: 13, height: 13, borderRadius: 999, background: "#fff", transition: "left .14s" }} />
                  </span>
                  <span style={{ fontSize: 11.5, fontWeight: 700, color: p.hidden ? PTE_C.z500 : PTE_C.ok }}>{p.hidden ? "Hidden" : "Visible"}</span>
                </button>
              </span>
              <span style={{ textAlign: "right", fontVariantNumeric: "tabular-nums", fontSize: 13, color: PTE_C.z700 }}>
                {ptePriceText(p)}
                {p.erode != null && <span style={{ display: "block", fontSize: 10, fontWeight: 700, color: PTE_C.warn }}>Margin −${Number(p.erode).toFixed(2)}</span>}
              </span>
              <span style={{ display: "flex", alignItems: "center", justifyContent: "flex-end", gap: 4, position: "relative" }}>
                {(!pteRestricted(p) || m.decoratable) && (
                  <button title="Edit decoration in the Artwork Editor" onClick={() => onOpenEditor && onOpenEditor(p.id)} style={{ width: 28, height: 28, border: "1px solid " + PTE_C.z200, borderRadius: 6, display: "inline-flex", alignItems: "center", justifyContent: "center", color: PTE_C.z600, fontSize: 13, background: "#fff", cursor: "pointer", fontFamily: "inherit" }}>✎</button>
                )}
                <button onClick={() => setMenuId(menuId === p.id ? null : p.id)} style={{ width: 28, height: 28, border: "1px solid transparent", borderRadius: 6, background: menuId === p.id ? PTE_C.z100 : "none", cursor: "pointer", color: PTE_C.z500, fontSize: 15, fontFamily: "inherit" }}>⋯</button>
                {menuId === p.id && (
                  <div style={{ position: "absolute", right: 0, top: "calc(100% + 4px)", zIndex: 50, background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 8, boxShadow: "0 8px 24px rgba(20,20,30,.14)", padding: 6, minWidth: 210, textAlign: "left" }}>
                    {menuItems.map((mi, i) => (
                      <button key={i} disabled={mi.disabled} onClick={() => { setMenuId(null); mi.act(); }} style={{ display: "block", width: "100%", textAlign: "left", background: "none", border: 0, borderRadius: 6, padding: "8px 10px", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: mi.disabled ? PTE_C.z300 : mi.danger ? PTE_C.redInk : PTE_C.z800, cursor: mi.disabled ? "default" : "pointer" }}>
                        {mi.label}
                        {mi.note && <span style={{ display: "block", fontSize: 10.5, color: PTE_C.z400, fontWeight: 400, marginTop: 2 }}>{mi.note}</span>}
                      </button>
                    ))}
                  </div>
                )}
              </span>
            </div>
          );
        })}
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "12px 20px", borderTop: "1px solid " + PTE_C.z100, fontSize: 12, color: PTE_C.z400 }}>
          <span>Showing 1–{visible.length} of {visible.length}</span>
          <span>List virtualizes past 50 rows — pagination appears at 100+</span>
        </div>
      </div>

      {/* Bulk actions bar */}
      {selIds.length > 0 && (
        <div style={{ position: "fixed", left: "50%", transform: "translateX(-50%)", bottom: 24, zIndex: 60, background: PTE_C.ink, color: "#fff", borderRadius: 12, boxShadow: "0 12px 32px rgba(20,20,30,.3)", padding: "12px 18px", display: "flex", alignItems: "center", gap: 14 }}>
          <span style={{ fontSize: 13, fontWeight: 700 }}>{selIds.length} selected</span>
          <span style={{ width: 1, height: 20, background: "rgba(255,255,255,.2)" }} />
          <button onClick={() => setBulkOpen(true)} title={lockedSel.length ? lockedSel.length + " locked item" + (lockedSel.length === 1 ? "" : "s") + " will be excluded" : "Bulk price selected"} style={bulkBtn("primary")}>Bulk price{lockedSel.length ? ` (${selIds.length - lockedSel.length} of ${selIds.length})` : ""}</button>
          <button onClick={() => { onToast && onToast("Team assignment updated for " + selIds.length + " products"); setSel({}); }} style={bulkBtn()}>Assign to teams</button>
          {multiSource && <button disabled={decoSel.length === 0} onClick={() => { if (!decoSel.length) return; onDecorateBatch(decoSel.map((p) => ({ fromStore: true, id: p.id, title: p.title, photo: p.photo, brand: metaOf(p).brand, category: metaOf(p).category, ok: metaOf(p).category !== "Hard goods" }))); setSel({}); }} title="Batch decoration via the Image Editor zone map" style={{ ...bulkBtn(), ...(decoSel.length === 0 ? { opacity: .4, cursor: "default" } : { borderColor: PTE_C.red, color: "#ffb3ae" }) }}>Decorate selected ({decoSel.length})</button>}
          {reqSel.length >= 2 && <button onClick={groupSelected} style={bulkBtn()}>Group as requirement</button>}
          {multiSource && <button onClick={() => { saveToVault(selProds); onToast && onToast(selIds.length + " saved to Stock Vault"); setSel({}); }} style={bulkBtn()}>Save to Vault</button>}
          <button onClick={() => {
            const ok = selProds.filter((p) => pteLockState(p, store) !== "full");
            ok.forEach((p) => onRemove(p.id));
            setSel({});
            const blockedN = selProds.length - ok.length;
            onToast && onToast(ok.length + " deleted" + (blockedN ? " — " + blockedN + " with orders kept (cancel their orders first)" : ""));
          }} style={{ ...bulkBtn(), color: "#ff9d96" }}>Delete</button>
        </div>
      )}

      <EditProductDrawer
        open={!!editingProduct}
        product={editingProduct}
        store={store}
        onClose={() => setEditingId(null)}
        onSave={(updated) => { onUpdateProduct && onUpdateProduct(updated); setEditingId(null); }}
        onDelete={() => { if (editingProduct) { setEditingId(null); setDeleting(editingProduct); } }} />

      <BulkPricingModal
        open={bulkOpen}
        store={store}
        draftProducts={selProds.length ? selProds : visible.filter((p) => !p.isBundle)}
        onClose={() => setBulkOpen(false)}
        onApply={(ids, profit) => {
          onBulkPrice && onBulkPrice(ids, profit);
          setBulkOpen(false);
          if (multiSource) onToast && onToast("Bulk pricing applied — 1 held at MAP floor $24.00");
          setSel({});
        }} />

      <ProductDeleteModal
        product={deleting}
        store={store}
        onClose={() => setDeleting(null)}
        onConfirm={() => { if (deleting) { onRemove(deleting.id); onToast && onToast(`"${deleting.title}" deleted`); } setDeleting(null); }}
        onGoToBundle={typeof onOpenDetail === "function" ? (bid) => { setDeleting(null); onOpenDetail(bid); } : undefined}
        onGoToOrders={() => { setDeleting(null); window.__goToPendingOrder && window.__goToPendingOrder(); }} />
    </div>
  );
}

// ─────────── Add Products — six intake channels (Epic G) ───────────
function AddChannelsView({ onBack, onChannel }) {
  const channels = [
    { k: "marucci", label: "Add Marucci Products", desc: "Browse the ingested Marucci / Victus catalog by sport, category and product line. Availability visible at selection.", isNew: true },
    { k: "external", label: "External Customizer", desc: "Hand off to Marucci's bat or glove builder; the configured item returns here as a draft.", isNew: true },
    { k: "vault", label: "Add from Vault", desc: "Pull previously vaulted items — including pre-decorated blanks — into this store.", isNew: true },
    { k: "designs", label: "Saved Designs", desc: "Add products from your saved Customizer designs.", isNew: false },
    { k: "carts", label: "Saved Carts", desc: "Pull products from a saved cart.", isNew: false },
    { k: "custom", label: "Custom Products", desc: "Design a new product in a brand Customizer.", isNew: false },
  ];
  return (
    <div style={{ paddingBottom: 60 }}>
      <button onClick={onBack} style={{ background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: PTE_C.z500, cursor: "pointer", marginBottom: 14, display: "inline-block" }}>← Back to Products</button>
      <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.4px", margin: "0 0 6px", fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Add Products</h2>
      <p style={{ fontSize: 14, color: PTE_C.z500, margin: "0 0 24px", maxWidth: 640, lineHeight: 1.5 }}>Every channel writes into the same canonical product record — the source is an attribute, not a separate type. Items land as drafts and go through the same publish gate.</p>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 16, maxWidth: 1080 }}>
        {channels.map((ch) => (
          <button key={ch.k} onClick={() => onChannel(ch.k)} style={{ position: "relative", textAlign: "left", background: "#fff", border: "1px solid " + (ch.isNew ? PTE_C.z300 : PTE_C.z200), borderRadius: 10, padding: 22, cursor: "pointer", fontFamily: "inherit", minHeight: 120, boxShadow: ch.isNew ? "0 1px 3px rgba(20,20,30,.05)" : "none" }}>
            {ch.isNew && <span style={{ position: "absolute", top: 14, right: 14, fontSize: 9.5, fontWeight: 700, letterSpacing: ".8px", background: PTE_C.red, color: "#fff", borderRadius: 4, padding: "2px 7px" }}>NEW</span>}
            <span style={{ fontSize: 15, fontWeight: 700, display: "block", marginBottom: 6, color: PTE_C.ink }}>{ch.label}</span>
            <span style={{ fontSize: 12.5, color: PTE_C.z500, lineHeight: 1.5, display: "block" }}>{ch.desc}</span>
          </button>
        ))}
      </div>
    </div>
  );
}

// ─────────── Marucci catalog browse + Decorate Selected exit ───────────
const MARUCCI_CATALOG = [
  { id: "c1", title: "Fleece Hoodie", sku: "MAFH-300", cat: "Fleece", cost: "$31.20", oos: false, photo: "performance-hoodie", dec: true },
  { id: "c2", title: "Dugout Pullover", sku: "MDP-210", cat: "Fleece", cost: "$34.60", oos: false, photo: "sfn-warmup", dec: true },
  { id: "c3", title: "Performance Tee", sku: "MPT-100", cat: "Tees", cost: "$12.40", oos: false, photo: "hooded-tech-tee", dec: true },
  { id: "c4", title: "Long-Sleeve Tee", sku: "MLT-110", cat: "Tees", cost: "$14.90", oos: true, letter: "T", dec: true },
  { id: "c5", title: "Coaches Polo", sku: "MCP-55", cat: "Polos", cost: "$21.00", oos: false, letter: "P", dec: true },
  { id: "c6", title: "Team Backpack", sku: "VBP-30", cat: "Bags", cost: "$29.80", oos: false, photo: "team-bag", dec: true },
  { id: "c7", title: "Bat Pack", sku: "MBP-20", cat: "Bags", cost: "$24.10", oos: false, photo: "squad-bag", dec: true },
  { id: "c8", title: "CAT X2 Bat (blank)", sku: "MCX2-33", cat: "Hard goods", cost: "$289.00", oos: false, letter: "H", dec: false },
];
function MarucciCatalogView({ store, onBack, onAddDrafts, onDecorate }) {
  const [cat, setCat] = React.useState("All");
  const [sel, setSel] = React.useState({});
  const items = MARUCCI_CATALOG.filter((c) => cat === "All" || c.cat === cat);
  const selected = MARUCCI_CATALOG.filter((c) => sel[c.id]);
  const toItem = (c) => ({ id: c.id, title: c.title, sku: c.sku, photo: c.photo || null, letter: c.letter, brand: c.sku.startsWith("V") ? "Victus" : "Marucci", source: "Marucci stock", category: c.cat, stock: c.oos ? "out" : "in", deco: "none", decoratable: c.dec, subtitle: c.sku + " · blank — supplier cost " + c.cost });
  return (
    <div style={{ paddingBottom: 100 }}>
      <button onClick={onBack} style={{ background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: PTE_C.z500, cursor: "pointer", marginBottom: 14, display: "inline-block" }}>← Back to channels</button>
      <div style={{ display: "flex", alignItems: "baseline", gap: 12, marginBottom: 6 }}>
        <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.4px", margin: 0, fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Add Marucci Products</h2>
        <span style={{ fontSize: 12, color: PTE_C.z400 }}>Ingested catalog · synced 2h ago · availability live</span>
      </div>
      <p style={{ fontSize: 14, color: PTE_C.z500, margin: "0 0 20px", lineHeight: 1.5 }}>Select blanks to add as drafts, or select and decorate in one pass. Out-of-stock variants shown for planning; they publish hidden from the storefront.</p>
      <div style={{ display: "flex", gap: 8, marginBottom: 18, flexWrap: "wrap" }}>
        {["All", "Fleece", "Tees", "Polos", "Bags", "Hard goods"].map((c) => (
          <button key={c} onClick={() => setCat(c)} style={{ background: cat === c ? PTE_C.ink : "#fff", color: cat === c ? "#fff" : PTE_C.z700, border: "1px solid " + (cat === c ? PTE_C.ink : PTE_C.z300), borderRadius: 999, padding: "7px 16px", fontSize: 12.5, fontWeight: 600, cursor: "pointer", fontFamily: "inherit" }}>{c}</button>
        ))}
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 14, maxWidth: 1200 }}>
        {items.map((c) => (
          <button key={c.id} onClick={() => setSel((s) => ({ ...s, [c.id]: !s[c.id] }))} style={{ position: "relative", textAlign: "left", background: "#fff", border: "2px solid " + (sel[c.id] ? PTE_C.ink : PTE_C.z200), borderRadius: 10, padding: 16, cursor: "pointer", fontFamily: "inherit" }}>
            <span style={{ position: "absolute", top: 10, right: 10, width: 20, height: 20, borderRadius: 999, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 11, fontWeight: 700, background: sel[c.id] ? PTE_C.ink : PTE_C.z100, color: sel[c.id] ? "#fff" : "transparent" }}>✓</span>
            {c.oos && <span style={{ position: "absolute", top: 12, left: 12, fontSize: 9.5, fontWeight: 700, letterSpacing: ".6px", background: PTE_C.z800, color: "#fff", borderRadius: 4, padding: "2px 7px" }}>OUT OF STOCK</span>}
            <span style={{ height: 110, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 10 }}>
              {c.photo
                ? <span style={{ width: "100%", height: 106, display: "flex", alignItems: "center", justifyContent: "center" }}><CatalogPhoto photo={c.photo} alt={c.title} /></span>
                : <span style={{ width: 76, height: 96, borderRadius: 8, background: PTE_C.z100, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 20, fontWeight: 700, color: PTE_C.z300 }}>{c.letter}</span>}
            </span>
            <span style={{ fontSize: 13, fontWeight: 700, display: "block", color: PTE_C.ink }}>{c.title}</span>
            <span style={{ fontSize: 11.5, color: PTE_C.z400, display: "block", marginTop: 2 }}>{c.sku} · {c.cat}</span>
            <span style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: 8 }}>
              <span style={{ fontSize: 12, color: PTE_C.z500 }}>Cost <strong style={{ color: PTE_C.ink }}>{c.cost}</strong></span>
              <span style={{ fontSize: 10, fontWeight: 700, letterSpacing: ".4px", padding: "2px 8px", borderRadius: 999, background: c.dec ? PTE_C.indigoBg : PTE_C.z100, color: c.dec ? PTE_C.indigo : PTE_C.z400 }}>{c.dec ? "Decoratable" : "No zone map"}</span>
            </span>
          </button>
        ))}
      </div>
      {selected.length > 0 && (
        <div style={{ position: "fixed", left: "50%", transform: "translateX(-50%)", bottom: 24, zIndex: 60, background: PTE_C.ink, color: "#fff", borderRadius: 12, boxShadow: "0 12px 32px rgba(20,20,30,.3)", padding: "12px 18px", display: "flex", alignItems: "center", gap: 14 }}>
          <span style={{ fontSize: 13, fontWeight: 700 }}>{selected.length} selected</span>
          <button onClick={() => onAddDrafts(selected.map(toItem))} style={{ background: "#fff", color: PTE_C.ink, border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Add as drafts</button>
          <button onClick={() => onDecorate(selected.map((c) => ({ ...toItem(c), fromStore: false, ok: c.dec })))} style={{ background: PTE_C.red, color: "#fff", border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Decorate selected →</button>
        </div>
      )}
    </div>
  );
}

// ─────────── Batch decoration — review grid (Epic D) ───────────
function BatchDecorateView({ store, items, onCancel, onSave }) {
  const [rows, setRows] = React.useState(() => (items || []).map((b) => ({ ...b, ok: b.ok !== false, included: true })));
  const included = rows.filter((b) => b.included && b.ok);
  const mark = (store.teamName || "Team").split(/[\s—-]/).filter(Boolean).slice(0, 2).map((w) => w[0]).join("").toUpperCase();
  return (
    <div style={{ paddingBottom: 60 }}>
      <button onClick={onCancel} style={{ background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: PTE_C.z500, cursor: "pointer", marginBottom: 14, display: "inline-block" }}>← Cancel batch</button>
      <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.4px", margin: "0 0 6px", fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Batch decoration — review</h2>
      <p style={{ fontSize: 14, color: PTE_C.z500, margin: "0 0 18px", maxWidth: 720, lineHeight: 1.5 }}>One placement, resolved per product through each template's imprint-zone map. Spot-check the grid; override or exclude before committing. Renders are produced server-side after save.</p>
      <div style={{ display: "flex", alignItems: "center", gap: 14, background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: "14px 18px", marginBottom: 18, maxWidth: 720 }}>
        <span style={{ width: 44, height: 44, borderRadius: 8, background: (store.teamColors && store.teamColors[0]) || PTE_C.ink, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 16, fontWeight: 800, fontFamily: "Archivo, sans-serif", flexShrink: 0 }}>{mark}</span>
        <div>
          <div style={{ fontSize: 13, fontWeight: 700, color: PTE_C.ink }}>{store.teamName || "Team"} — Primary mark</div>
          <div style={{ fontSize: 12, color: PTE_C.z500 }}>Left chest · 3.5 in wide · Embroidery · From team artwork library</div>
        </div>
        <span style={{ marginLeft: "auto", fontSize: 10.5, fontWeight: 700, letterSpacing: ".6px", textTransform: "uppercase", background: PTE_C.okBg, color: PTE_C.ok, borderRadius: 999, padding: "3px 10px" }}>Production-ready</span>
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 14, maxWidth: 1200, marginBottom: 20 }}>
        {rows.map((b, i) => (
          <div key={b.id} style={{ background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: 16, opacity: b.included && b.ok ? 1 : .55 }}>
            <span style={{ height: 100, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 10, position: "relative" }}>
              {b.photo
                ? <span style={{ width: "100%", height: 96, display: "flex", alignItems: "center", justifyContent: "center", opacity: .9 }}><CatalogPhoto photo={b.photo} alt={b.title} /></span>
                : <span style={{ width: 70, height: 88, borderRadius: 8, background: PTE_C.z100, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 18, fontWeight: 700, color: PTE_C.z300 }}>{(b.category || "P").slice(0, 1)}</span>}
              {b.ok && <span style={{ position: "absolute", left: "calc(50% - 26px)", top: 34, width: 18, height: 18, borderRadius: 4, background: (store.teamColors && store.teamColors[0]) || PTE_C.ink, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 8, fontWeight: 800 }}>{mark}</span>}
            </span>
            <div style={{ fontSize: 13, fontWeight: 700, color: PTE_C.ink }}>{b.title}</div>
            <div style={{ fontSize: 11.5, color: PTE_C.z500, margin: "3px 0 8px" }}>{b.ok ? "Left chest · resolves to 3.5 in · Embroidery" : "No imprint zone on this template"}</div>
            <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
              <span style={{ fontSize: 10.5, fontWeight: 700, padding: "3px 10px", borderRadius: 999, background: !b.ok ? PTE_C.warnBg : b.included ? PTE_C.okBg : PTE_C.z100, color: !b.ok ? PTE_C.warn : b.included ? PTE_C.ok : PTE_C.z400 }}>{b.ok ? (b.included ? "Resolved" : "Excluded") : "Needs review"}</span>
              <button onClick={() => setRows((r) => r.map((x, j) => j === i ? { ...x, included: !x.included } : x))} style={{ background: "none", border: 0, fontSize: 11.5, fontWeight: 600, color: PTE_C.z400, cursor: "pointer", fontFamily: "inherit", textDecoration: "underline", textUnderlineOffset: 2 }}>{b.included ? "Exclude" : "Include"}</button>
            </div>
          </div>
        ))}
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 14 }}>
        <button onClick={() => onSave(included)} disabled={included.length === 0} style={{ background: included.length ? PTE_C.ink : PTE_C.z100, color: included.length ? "#fff" : PTE_C.z400, border: 0, borderRadius: 6, padding: "12px 24px", fontSize: 13.5, fontWeight: 700, cursor: included.length ? "pointer" : "default", fontFamily: "inherit" }}>Save all — {included.length} decorated drafts</button>
        <span style={{ fontSize: 12.5, color: PTE_C.z500 }}>Creates placement records → server render queued → art enters approval as <strong>Draft</strong></span>
      </div>
    </div>
  );
}

// ─────────── Product Images view — renders, imprint record, approval (Epic C) ───────────
function ProductImagesView({ product, store, onBack, onOpenEditor, onUpdateProduct, onToast }) {
  const p = product;
  const m = pteMeta(p);
  const dm = PTE_DECO[m.deco] || PTE_DECO.na;
  const stateLabel = { none: "Not decorated", render: "Render pending", draft: "Draft", submitted: "Submitted", approved: "Approved", rejected: "Rejected", na: "—" }[m.deco];
  const vChipC = (state) => ({ Approved: [PTE_C.okBg, PTE_C.ok], Draft: [PTE_C.warnBg, PTE_C.warn], Submitted: [PTE_C.blueBg, PTE_C.blue], Rejected: [PTE_C.rejBg, PTE_C.redInk], "Render pending": [PTE_C.indigoBg, PTE_C.indigo] })[state] || [PTE_C.z100, PTE_C.z500];
  const variants = [
    { label: "Primary · Front", photo: p.photo, state: stateLabel },
    { label: "Primary · Back", photo: p.photo, state: stateLabel },
    { label: "Alt colorway · Front", photo: null, state: "Render pending" },
    { label: "Alt colorway · Back", photo: null, state: "Render pending" },
  ];
  const stepIdx = { none: -1, render: 0, draft: 0, submitted: 1, approved: 2, rejected: 1, na: -1 }[m.deco];
  const steps = ["Draft", "Submitted", m.deco === "rejected" ? "Rejected" : "Approved"].map((label, i) => ({
    label, line: i < 2,
    note: i === 0 ? "Placement saved from the editor" : i === 1 ? "Awaiting seller art review" : m.deco === "rejected" ? "Logo too small for embroidery at 3.5in — resize to 4in" : "Cleared for production & storefront",
    on: i <= stepIdx, red: m.deco === "rejected" && i === 1,
  }));
  const canSubmit = ["draft", "rejected"].includes(m.deco);
  const imprint = [
    ["Zone", "Left chest"], ["Size", "3.5 in wide"], ["Method", "Embroidery"],
    ["Artwork", (store.teamName || "Team") + " primary (vector)"], ["Colors", "PMS 289C · 152C"],
    ["Personalization", m.source === "Prolook custom" ? "Name & number (roster pre-fill)" : "Not supported (stock)"],
    ["Record", "IMP-" + String(p.id).replace(/[^A-Z0-9]/gi, "").slice(-6).toUpperCase()],
  ];
  return (
    <div style={{ paddingBottom: 60 }}>
      <button onClick={onBack} style={{ background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: PTE_C.z500, cursor: "pointer", marginBottom: 14, display: "inline-block" }}>← Back to Products</button>
      <div style={{ display: "flex", alignItems: "baseline", gap: 12, marginBottom: 18 }}>
        <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.4px", margin: 0, fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>{p.title}</h2>
        <span style={{ fontSize: 12.5, color: PTE_C.z400 }}>{m.brand} · {m.source} · {dm.label}</span>
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 340px", gap: 20, alignItems: "start", maxWidth: 1200 }}>
        <div style={{ background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: 20 }}>
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 14 }}>
            <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z500 }}>Variant image set — composited renders</span>
            <button onClick={() => onOpenEditor && onOpenEditor()} style={{ background: PTE_C.ink, color: "#fff", border: 0, borderRadius: 6, padding: "9px 18px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>✎ Edit in Image Editor</button>
          </div>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(4,1fr)", gap: 12 }}>
            {variants.map((v, i) => (
              <div key={i} style={{ border: "1px solid " + PTE_C.z200, borderRadius: 8, padding: 10, textAlign: "center" }}>
                <span style={{ height: 96, display: "flex", alignItems: "center", justifyContent: "center", marginBottom: 8 }}>
                  {v.photo
                    ? <span style={{ width: "100%", height: 92, display: "flex", alignItems: "center", justifyContent: "center" }}><CatalogPhoto photo={v.photo} alt={v.label} /></span>
                    : <span style={{ width: 64, height: 84, borderRadius: 6, background: "linear-gradient(90deg," + PTE_C.z100 + " 25%," + PTE_C.z50 + " 50%," + PTE_C.z100 + " 75%)" }} />}
                </span>
                <div style={{ fontSize: 11.5, fontWeight: 600, color: PTE_C.ink }}>{v.label}</div>
                <span style={{ display: "inline-block", marginTop: 5, fontSize: 9.5, fontWeight: 700, letterSpacing: ".4px", padding: "2px 8px", borderRadius: 999, background: vChipC(v.state)[0], color: vChipC(v.state)[1] }}>{v.state}</span>
              </div>
            ))}
          </div>
          <div style={{ marginTop: 14, fontSize: 12, color: PTE_C.z400, lineHeight: 1.5 }}>Storefront imagery is derived — the server compositor renders every image from the placement record. A change to artwork or placement invalidates and re-queues the set.</div>
        </div>
        <div style={{ display: "flex", flexDirection: "column", gap: 14 }}>
          <div style={{ background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: 18 }}>
            <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z500, marginBottom: 12 }}>Production imprint record</div>
            {imprint.map(([k, v]) => (
              <div key={k} style={{ display: "flex", justifyContent: "space-between", gap: 12, padding: "7px 0", borderBottom: "1px solid " + PTE_C.z100, fontSize: 12.5 }}>
                <span style={{ color: PTE_C.z500 }}>{k}</span><span style={{ fontWeight: 600, textAlign: "right", color: PTE_C.ink }}>{v}</span>
              </div>
            ))}
          </div>
          <div style={{ background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: 18 }}>
            <div style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z500, marginBottom: 12 }}>Art approval</div>
            {steps.map((s, i) => (
              <div key={i} style={{ display: "flex", gap: 10, alignItems: "flex-start" }}>
                <span style={{ display: "flex", flexDirection: "column", alignItems: "center" }}>
                  <span style={{ width: 10, height: 10, borderRadius: 999, marginTop: 3, background: s.on ? (s.red ? PTE_C.red : PTE_C.ink) : PTE_C.z200 }} />
                  {s.line && <span style={{ width: 2, height: 22, background: PTE_C.z200 }} />}
                </span>
                <span style={{ fontSize: 12.5, paddingBottom: 10 }}>
                  <strong style={{ color: s.on ? PTE_C.ink : PTE_C.z400 }}>{s.label}</strong>
                  <span style={{ display: "block", color: PTE_C.z400, fontSize: 11.5 }}>{s.note}</span>
                </span>
              </div>
            ))}
            <button disabled={!canSubmit} onClick={() => { onUpdateProduct({ ...p, deco: "submitted" }); onToast && onToast("Art submitted — entered the seller art approver's review queue"); }} style={{ width: "100%", marginTop: 12, border: 0, borderRadius: 8, padding: 11, fontSize: 12.5, fontWeight: 700, fontFamily: "inherit", cursor: canSubmit ? "pointer" : "default", background: canSubmit ? PTE_C.ink : PTE_C.z100, color: canSubmit ? "#fff" : PTE_C.z400 }}>
              {m.deco === "submitted" ? "Awaiting review" : m.deco === "approved" ? "Approved" : m.deco === "rejected" ? "Resubmit for approval" : "Submit for approval"}
            </button>
            <div style={{ fontSize: 11.5, color: PTE_C.z400, marginTop: 8, lineHeight: 1.5 }}>
              {(store.artPolicy || "block-publish") === "block-publish" ? "Policy: unapproved art blocks publish (strict, default). Flip in Store Settings." : "Policy: publishable while in review; order submission is gated. Flip in Store Settings."}
            </div>
          </div>
          <button onClick={() => { window.__vaultItems = window.__vaultItems || []; window.__vaultItems.push({ id: "v-" + p.id + "-" + Date.now(), title: p.title, brand: m.brand, source: m.source, photo: p.photo, category: m.category, deco: (store.teamName || "Team") + " primary · Left chest 3.5in · Embroidery · " + stateLabel, used: "This store" }); onToast && onToast("Saved to Stock Vault with its decoration record"); }} style={{ background: "#fff", border: "1px solid " + PTE_C.z300, borderRadius: 8, padding: 11, fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", color: PTE_C.z800 }}>Save to Stock Vault</button>
        </div>
      </div>
    </div>
  );
}

// ─────────── Image Editor — artwork placement across the variant image set ───────────
// Placement is the record: layers are stored per variant (colorway × view), the
// server compositor derives every storefront render from them. Copy a layer once
// and paste it into the ticked variants; ink flips for contrast on dark bodies.
const IE_FONTS = [
  { id: "block", label: "Block", stack: "Archivo, sans-serif", weight: 800, ls: "1px" },
  { id: "sans", label: "Sans", stack: "Inter, sans-serif", weight: 700, ls: ".5px" },
  { id: "mono", label: "Mono", stack: "'JetBrains Mono', ui-monospace, Menlo, monospace", weight: 500, ls: "1.5px" },
];
const IE_VIEWS = [{ id: "front", name: "Front" }, { id: "back", name: "Back" }];
// Only an em/en-dash suffix is a colorway ("Fleece Hoodie — Charcoal"); a hyphen
// inside a product word is not ("Quarter-Zip"). Alternates that collide with the
// derived primary drop out, so every label in the variant strip stays unique.
function ieColorways(product) {
  const seg = /[—–]\s*([A-Za-z][A-Za-z ]*)$/.exec(product.title || "");
  const raw = seg ? seg[1].trim() : "";
  const primary = { id: "primary", name: raw || "Primary", filter: "none", dark: false };
  const alts = [
    { id: "navy", name: "Navy", filter: "brightness(.45) sepia(1) hue-rotate(190deg) saturate(3.2)", dark: true },
    { id: "black", name: "Black", filter: "brightness(.32) saturate(.12)", dark: true },
  ];
  return [primary].concat(alts.filter((a) => a.name.toLowerCase() !== primary.name.toLowerCase()));
}
// Store titles carry a season ("Lincoln High Baseball — Fall 2026"); artwork never
// does, so season and year tokens are stripped before a mark is derived.
function ieTeamWords(store) {
  const name = String(store.teamName || store.storeTitle || "Team").split(/[—–]/)[0];
  const drop = /^(fall|spring|summer|winter|season|\d{2,4}(-\d{2,4})?)$/i;
  const words = name.split(/[\s\-_/]+/).filter((w) => w && !drop.test(w));
  return words.length ? words : ["Team"];
}
function ieTeamName(store) {
  const w = ieTeamWords(store);
  const two = w.slice(0, 2).join(" ");
  return (two.length <= 14 ? two : w[0]).toUpperCase();
}
function ieTeamArt(store) {
  const words = ieTeamWords(store);
  const initials = words.slice(0, 2).map((w) => w[0]).join("").toUpperCase();
  const word = words[0].toUpperCase();
  return [
    { id: "primary", name: "team-primary.svg", mark: initials, meta: "Vector · 2 color", w: 14 },
    { id: "wordmark", name: "team-wordmark.svg", mark: word, meta: "Vector · 1 color", w: 34 },
    { id: "alt", name: "alternate-mark.png", mark: "ALT", meta: "PNG · needs vector", w: 20 },
  ];
}
// Garment reads ~20in across the render, so width% maps to imprint inches.
function ieZone(l) {
  if (l.w >= 45) return "Full front";
  if (l.y > 66) return "Lower front";
  if (l.y < 30) return "Collar · upper";
  if (l.x < 42) return "Left chest";
  if (l.x > 58) return "Right chest";
  return "Center chest";
}
function ieInches(w) { return (Math.round(w * 0.2 * 10) / 10).toFixed(1) + " in"; }

function ProductImageEditor({ product, store, onBack, onSave }) {
  const p = product;
  const m = pteMeta(p);
  const colorways = React.useMemo(() => ieColorways(p), [p.id, p.title]);
  const library = React.useMemo(() => ieTeamArt(store), [store.teamName, store.storeTitle]);
  const swatches = React.useMemo(() => {
    // Near-blacks and near-whites collapse so the row never shows two swatches
    // a printer would treat as the same ink.
    const norm = (c) => { const h = String(c).toLowerCase(); if (/^#(0[0-9a-f]|1[0-8])/.test(h)) return "#09090b"; if (/^#(f[0-9a-f]){1,3}/.test(h)) return "#ffffff"; return h; };
    const all = ["#ffffff", "#09090b"].concat(store.teamColors || []);
    return all.filter((c, i) => c && all.findIndex((x) => norm(x) === norm(c)) === i).slice(0, 5);
  }, [store.teamColors]);
  const teamInk = (store.teamColors && store.teamColors[0]) || PTE_C.ink;

  const initial = React.useMemo(() => {
    const by = {};
    colorways.forEach((c) => IE_VIEWS.forEach((v) => { by[c.id + "-" + v.id] = []; }));
    if (["draft", "submitted", "approved", "rejected"].includes(m.deco)) {
      by["primary-front"] = [{ id: "l1", type: "art", src: "primary", name: library[0].name, mark: library[0].mark, x: 34, y: 43, w: 14, rot: 0, fx: 1, fy: 1, color: teamInk, removeWhite: true }];
      by["primary-back"] = [{ id: "l2", type: "text", name: library[1].mark, text: library[1].mark, x: 50, y: 40, w: 38, rot: 0, fx: 1, fy: 1, color: teamInk, font: "block" }];
    }
    return by;
  }, [p.id]);

  const [layersBy, setLayersBy] = React.useState(initial);
  const [active, setActive] = React.useState("primary-front");
  const [selectedId, setSelectedId] = React.useState(() => ((initial["primary-front"] || [])[0] || {}).id || null);
  const [checked, setChecked] = React.useState({});
  const [clipboard, setClipboard] = React.useState(null);
  const [panel, setPanel] = React.useState(null);
  const [pick, setPick] = React.useState(library[0].id);
  const [undoStack, setUndoStack] = React.useState([]);
  const [redoStack, setRedoStack] = React.useState([]);
  const [toast, setToast] = React.useState(null);
  const [saved, setSaved] = React.useState(true);

  const canvasRef = React.useRef(null);
  const toastTimer = React.useRef(null);
  // Layer type is drawn in px, so the canvas width has to be real: a % width can
  // only stay truthful against the inch readout if the glyphs fit their box.
  const [canvasW, setCanvasW] = React.useState(520);
  React.useEffect(() => {
    const el = canvasRef.current;
    if (!el) return;
    const read = () => { const w = el.getBoundingClientRect().width; if (w) setCanvasW(w); };
    read();
    if (!window.ResizeObserver) { window.addEventListener("resize", read); return () => window.removeEventListener("resize", read); }
    const ro = new ResizeObserver(read);
    ro.observe(el);
    return () => ro.disconnect();
  }, []);
  const fitPx = (text, wPct, inset, cap, ratio) => {
    const n = Math.max(1, String(text == null ? "" : text).length);
    return Math.max(6, Math.min(cap, (canvasW * wPct / 100) * inset / (n * ratio)));
  };
  const live = React.useRef({});
  live.current = { layersBy, active };
  React.useEffect(() => () => clearTimeout(toastTimer.current), []);
  const flash = (msg) => { clearTimeout(toastTimer.current); setToast(msg); toastTimer.current = setTimeout(() => setToast(null), 2400); };

  const snap = () => { const o = {}; const cur = live.current.layersBy; Object.keys(cur).forEach((k) => { o[k] = cur[k].map((l) => ({ ...l })); }); return o; };
  const pushUndo = () => { setUndoStack((u) => [...u, snap()].slice(-40)); setRedoStack([]); };
  const commit = (next) => { pushUndo(); setLayersBy(next); setSaved(false); };

  const cw = colorways.find((c) => c.id === active.split("-")[0]) || colorways[0];
  const view = IE_VIEWS.find((v) => v.id === active.split("-")[1]) || IE_VIEWS[0];
  const layers = layersBy[active] || [];
  const sel = layers.find((l) => l.id === selectedId) || null;
  const checkedIds = Object.keys(checked).filter((k) => checked[k]);
  const setLayers = (list, nextSel) => { commit({ ...layersBy, [active]: list }); if (nextSel !== undefined) setSelectedId(nextSel); };
  const patchSel = (patch) => { if (!sel) return; setLayers(layers.map((l) => l.id === sel.id ? { ...l, ...patch } : l)); };
  const needSel = () => { if (!sel) { flash("Select a layer first"); return true; } return false; };

  const clamp = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
  const startDrag = (e, layer, mode, corner) => {
    e.preventDefault(); e.stopPropagation();
    const rect = canvasRef.current && canvasRef.current.getBoundingClientRect();
    if (!rect) return;
    const start = { mx: e.clientX, my: e.clientY, x: layer.x, y: layer.y, w: layer.w };
    setSelectedId(layer.id);
    pushUndo();
    const move = (ev) => {
      const dx = ((ev.clientX - start.mx) / rect.width) * 100;
      const dy = ((ev.clientY - start.my) / rect.height) * 100;
      const key = live.current.active;
      setLayersBy((cur) => ({ ...cur, [key]: (cur[key] || []).map((l) => {
        if (l.id !== layer.id) return l;
        if (mode === "move") return { ...l, x: clamp(start.x + dx, 3, 97), y: clamp(start.y + dy, 3, 97) };
        const sign = (corner === "se" || corner === "ne") ? 1 : -1;
        return { ...l, w: clamp(start.w + dx * 2 * sign, 6, 90) };
      }) }));
      setSaved(false);
    };
    const up = () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  const startGrip = (e, layer) => {
    e.preventDefault(); e.stopPropagation();
    const list = layers;
    const from = list.findIndex((l) => l.id === layer.id);
    const startY = e.clientY;
    let to = from;
    const move = (ev) => { to = clamp(from - Math.round((ev.clientY - startY) / 56), 0, list.length - 1); };
    const up = () => {
      window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up);
      if (to !== from) { const next = list.slice(); const [item] = next.splice(from, 1); next.splice(to, 0, item); setLayers(next); }
    };
    setSelectedId(layer.id);
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };

  const photoSrc = p.photo ? `assets/products/${p.photo}.png` : null;
  const imgStyle = { position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", pointerEvents: "none", filter: cw.filter, transform: view.id === "back" ? "scaleX(-1)" : "none" };
  const toolBtn = { display: "flex", alignItems: "center", gap: 6, padding: "7px 10px", borderRadius: 6, border: 0, background: "none", fontFamily: "inherit", fontSize: 12.5, color: PTE_C.z700, cursor: "pointer", whiteSpace: "nowrap", flexShrink: 0 };
  const divider = <span style={{ width: 1, height: 22, background: PTE_C.z200, margin: "0 6px", flexShrink: 0 }} />;
  const sectionLabel = { fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z500 };
  const swatchStyle = (c, big) => ({ width: big ? 28 : 20, height: big ? 28 : 20, borderRadius: 5, background: c, cursor: "pointer", flexShrink: 0, padding: 0,
    border: "1px solid " + (String(c).toLowerCase() === "#ffffff" ? PTE_C.z300 : "transparent"),
    boxShadow: sel && String(sel.color).toLowerCase() === String(c).toLowerCase() ? "0 0 0 2px " + PTE_C.ink : "none" });

  const doCopy = () => { if (needSel()) return; setClipboard({ ...sel }); flash("Copied " + sel.name); };
  const doPaste = () => {
    if (!clipboard) { flash("Nothing in the clipboard — copy a layer first"); return; }
    if (!checkedIds.length) { flash("Tick the variants to paste into"); return; }
    const next = { ...layersBy };
    checkedIds.forEach((vid, i) => {
      const dark = (colorways.find((c) => c.id === vid.split("-")[0]) || {}).dark;
      const lc = String(clipboard.color).toLowerCase();
      const color = lc === "#ffffff" && !dark ? PTE_C.ink : ((lc === "#09090b" || lc === "#0e0e10") && dark ? "#ffffff" : clipboard.color);
      next[vid] = [...(next[vid] || []), { ...clipboard, color, id: "l" + Date.now() + "_" + i }];
    });
    commit(next);
    flash("Pasted " + clipboard.name + " into " + checkedIds.length + " variant" + (checkedIds.length === 1 ? "" : "s") + " — ink flipped for contrast");
  };
  const doUndo = () => {
    if (!undoStack.length) { flash("Nothing to undo"); return; }
    const prev = undoStack[undoStack.length - 1];
    setRedoStack((r) => [...r, snap()]); setUndoStack((u) => u.slice(0, -1)); setLayersBy(prev); setSaved(false);
  };
  const doRedo = () => {
    if (!redoStack.length) { flash("Nothing to redo"); return; }
    const next = redoStack[redoStack.length - 1];
    setUndoStack((u) => [...u, snap()]); setRedoStack((r) => r.slice(0, -1)); setLayersBy(next); setSaved(false);
  };
  // A new layer never lands under one already on this variant.
  const freeY = (base) => clamp(base + (layers.length ? 12 * layers.length : 0), 12, 64);
  const doInsert = () => {
    const art = library.find((a) => a.id === pick);
    if (!art) { flash("Pick an artwork file first"); return; }
    const layer = { id: "l" + Date.now(), type: "art", src: art.id, name: art.name, mark: art.mark, x: 50, y: freeY(44), w: art.w, rot: 0, fx: 1, fy: 1, color: cw.dark ? "#ffffff" : teamInk, removeWhite: true };
    setLayers([...layers, layer], layer.id);
    setPanel(null);
    flash("Inserted " + art.name);
  };
  const doAddText = () => {
    const label = ieTeamName(store);
    const layer = { id: "l" + Date.now(), type: "text", name: label, text: label, x: 50, y: freeY(44), w: 34, rot: 0, fx: 1, fy: 1, color: cw.dark ? "#ffffff" : teamInk, font: "block" };
    setLayers([...layers, layer], layer.id);
  };
  const doSaveAll = () => {
    const images = Object.keys(layersBy).filter((k) => (layersBy[k] || []).length).length;
    const dirty = !saved;
    setSaved(true);
    onSave && onSave({ dirty, images, zone: sel ? ieZone(sel) : (layersBy["primary-front"] || [])[0] ? ieZone(layersBy["primary-front"][0]) : "Left chest" });
    flash(images ? "Saved placement on " + images + " product image" + (images === 1 ? "" : "s") + " — renders re-queued" : "Saved — no artwork placed yet");
  };

  return (
    <div style={{ position: "fixed", inset: 0, zIndex: 380, display: "flex", flexDirection: "column", background: PTE_C.z100, color: PTE_C.ink, fontFamily: '"Inter",system-ui,sans-serif', userSelect: "none", overflow: "hidden" }}>

      <div style={{ height: 56, flexShrink: 0, background: "#fff", borderBottom: "1px solid " + PTE_C.z200, display: "flex", alignItems: "center", justifyContent: "space-between", padding: "0 16px", gap: 16 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12, minWidth: 0 }}>
          <button onClick={onBack} style={{ display: "flex", alignItems: "center", gap: 8, padding: "7px 12px", border: "1px solid " + PTE_C.z300, borderRadius: 6, background: "#fff", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, cursor: "pointer", flexShrink: 0, color: PTE_C.ink }}>← Back</button>
          {divider}
          <span style={{ minWidth: 0 }}>
            <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{p.title}{p.sku ? " — " + p.sku : ""}</span>
            <span style={{ display: "block", fontSize: 11, color: PTE_C.z500, whiteSpace: "nowrap" }}>{store.storeTitle || store.teamName} · {m.brand} · Image editor</span>
          </span>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 12, flexShrink: 0 }}>
          <span style={{ fontSize: 12, color: saved ? PTE_C.z500 : PTE_C.warn, whiteSpace: "nowrap" }}>{saved ? "All changes saved" : "Unsaved changes"}</span>
          <button onClick={doSaveAll} style={{ background: PTE_C.ink, color: "#fff", border: 0, borderRadius: 6, padding: "10px 20px", fontSize: 12.5, fontWeight: 700, fontFamily: "inherit", cursor: "pointer" }}>Save all</button>
        </div>
      </div>

      <div style={{ height: 48, flexShrink: 0, background: "#fff", borderBottom: "1px solid " + PTE_C.z200, display: "flex", alignItems: "center", padding: "0 12px", gap: 2, overflowX: "auto", overflowY: "hidden" }}>
        <button onClick={doCopy} style={toolBtn}>Copy</button>
        <button onClick={doPaste} style={toolBtn}>Paste</button>
        {divider}
        <button onClick={doUndo} style={Object.assign({}, toolBtn, { color: undoStack.length ? PTE_C.z700 : PTE_C.z300 })}>↺ Undo</button>
        <button onClick={doRedo} style={Object.assign({}, toolBtn, { color: redoStack.length ? PTE_C.z700 : PTE_C.z300 })}>↻ Redo</button>
        {divider}
        <button onClick={() => { if (!needSel()) patchSel({ fx: sel.fx * -1 }); }} style={toolBtn}>Flip H</button>
        <button onClick={() => { if (!needSel()) patchSel({ fy: sel.fy * -1 }); }} style={toolBtn}>Flip V</button>
        {divider}
        <button onClick={() => { if (!needSel()) patchSel({ rot: sel.rot - 15 }); }} style={toolBtn}>⟲ 15°</button>
        <button onClick={() => { if (!needSel()) patchSel({ rot: sel.rot + 15 }); }} style={toolBtn}>⟳ 15°</button>
        <span style={{ fontSize: 11.5, color: PTE_C.z400, fontFamily: "'JetBrains Mono', ui-monospace, monospace", minWidth: 38, flexShrink: 0 }}>{sel ? sel.rot + "°" : "—"}</span>
        {divider}
        <span style={{ fontSize: 12.5, color: PTE_C.z700, whiteSpace: "nowrap", flexShrink: 0, marginRight: 4 }}>Recolor</span>
        <span style={{ display: "flex", gap: 6, flexShrink: 0 }}>
          {swatches.map((c) => <button key={c} onClick={() => { if (!needSel()) patchSel({ color: c }); }} title={c} style={swatchStyle(c, false)} />)}
        </span>
      </div>

      <div style={{ flex: 1, minHeight: 0, display: "flex" }}>

        <div style={{ flex: 1, minWidth: 0, display: "flex", flexDirection: "column" }}>
          <div style={{ flexShrink: 0, display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, padding: "12px 20px 0" }}>
            <span style={sectionLabel}>Canvas · {cw.name} · {view.name}</span>
            <span style={{ fontSize: 12, color: PTE_C.z500, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{sel ? "Selected: " + sel.name : "Nothing selected"}</span>
          </div>
          <div style={{ flex: 1, minHeight: 0, display: "flex", alignItems: "center", justifyContent: "center", padding: "14px 20px 18px" }}>
            <div ref={canvasRef} onPointerDown={() => setSelectedId(null)} style={{ position: "relative", aspectRatio: "10 / 11", height: "100%", maxWidth: "100%", background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, boxShadow: "0 4px 12px rgba(9,9,11,.06)", overflow: "hidden" }}>
              {photoSrc
                ? <img src={photoSrc} alt={p.title} draggable={false} style={imgStyle} />
                : <span style={{ position: "absolute", inset: "14%", border: "1.5px dashed " + PTE_C.z300, borderRadius: 12, display: "flex", alignItems: "center", justifyContent: "center", fontSize: 12.5, color: PTE_C.z400, background: cw.dark ? PTE_C.z800 : PTE_C.z50 }}>Blank {m.category.toLowerCase()} — render pending</span>}
              <span style={{ position: "absolute", left: 12, bottom: 10, fontSize: 10, color: PTE_C.z400, fontFamily: "'JetBrains Mono', ui-monospace, monospace" }}>{(p.sku || "IMG").replace(/[^A-Za-z0-9]/g, "")}_{cw.id}_{view.id}.png · 1000 × 1100</span>
              {layers.map((l, i) => {
                const isSel = l.id === selectedId;
                const f = IE_FONTS.find((x) => x.id === l.font) || IE_FONTS[0];
                return (
                  <div key={l.id} onPointerDown={(e) => startDrag(e, l, "move")} style={{ position: "absolute", left: l.x + "%", top: l.y + "%", width: l.w + "%", transform: `translate(-50%,-50%) rotate(${l.rot}deg) scale(${l.fx},${l.fy})`, display: "flex", alignItems: "center", justifyContent: "center", cursor: "move", zIndex: 10 + i, outline: isSel ? "1.5px solid " + PTE_C.red : "none", outlineOffset: 4 }}>
                    {l.type === "art" && !l.removeWhite && <span style={{ position: "absolute", inset: "-8%", background: "#fff", borderRadius: 2 }} />}
                    {l.type === "art"
                      ? <span style={{ position: "relative", display: "block", width: "100%", boxSizing: "border-box", textAlign: "center", fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: fitPx(l.mark, l.w, 0.8, 54, 0.66), letterSpacing: "1px", color: l.color, lineHeight: 1.05, padding: "9% 6%", border: "2px solid " + l.color, borderRadius: 3 }}>{l.mark}</span>
                      : <span style={{ position: "relative", display: "block", width: "100%", textAlign: "center", fontFamily: f.stack, fontWeight: f.weight, fontSize: fitPx(l.text, l.w, 0.98, 46, 0.78), letterSpacing: f.ls, color: l.color, lineHeight: 1.1, whiteSpace: "nowrap" }}>{l.text}</span>}
                    {isSel && ["nw", "ne", "se", "sw"].map((c) => (
                      <span key={c} onPointerDown={(e) => startDrag(e, l, "scale", c)} style={{ position: "absolute", width: 9, height: 9, background: "#fff", border: "2px solid " + PTE_C.red, borderRadius: 2, zIndex: 3,
                        cursor: (c === "nw" || c === "se") ? "nwse-resize" : "nesw-resize",
                        top: c[0] === "n" ? -9 : "auto", bottom: c[0] === "s" ? -9 : "auto", left: c[1] === "w" ? -9 : "auto", right: c[1] === "e" ? -9 : "auto" }} />
                    ))}
                  </div>
                );
              })}
            </div>
          </div>

          <div style={{ flexShrink: 0, background: "#fff", borderTop: "1px solid " + PTE_C.z200, padding: "14px 18px 16px" }}>
            <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 12 }}>
              <span style={{ display: "flex", alignItems: "center", gap: 10 }}>
                <span style={sectionLabel}>Variants</span>
                <span style={{ fontSize: 12, color: PTE_C.z400, whiteSpace: "nowrap" }}>{colorways.length} colorways × {IE_VIEWS.length} views · {checkedIds.length} ticked</span>
              </span>
              <span style={{ display: "flex", alignItems: "center", gap: 10, flexShrink: 0 }}>
                <span style={{ fontSize: 12, color: PTE_C.z500, whiteSpace: "nowrap" }}>{clipboard ? "Clipboard: " + clipboard.name : "Clipboard empty"}</span>
                <button onClick={doPaste} style={{ padding: "8px 14px", borderRadius: 6, border: 0, fontSize: 12.5, fontWeight: 700, fontFamily: "inherit", cursor: "pointer", background: PTE_C.ink, color: "#fff", whiteSpace: "nowrap" }}>Paste to {checkedIds.length} variant{checkedIds.length === 1 ? "" : "s"}</button>
              </span>
            </div>
            <div style={{ display: "flex", gap: 10, overflowX: "auto", paddingBottom: 4 }}>
              {colorways.map((c) => IE_VIEWS.map((v) => {
                const id = c.id + "-" + v.id;
                const on = id === active;
                const tick = !!checked[id];
                const count = (layersBy[id] || []).length;
                return (
                  <div key={id} onClick={() => { setActive(id); setSelectedId(((layersBy[id] || [])[0] || {}).id || null); }} style={{ display: "flex", alignItems: "center", gap: 10, padding: "8px 12px 8px 8px", border: "1px solid " + (on ? PTE_C.ink : PTE_C.z200), boxShadow: on ? "0 0 0 1px " + PTE_C.ink : "none", borderRadius: 8, cursor: "pointer", background: "#fff", flexShrink: 0 }}>
                    <span style={{ width: 38, height: 42, borderRadius: 5, flexShrink: 0, overflow: "hidden", background: PTE_C.z50, position: "relative", display: "block" }}>
                      {photoSrc && <img src={photoSrc} alt="" draggable={false} style={{ position: "absolute", inset: 0, width: "100%", height: "100%", objectFit: "contain", filter: c.filter, transform: v.id === "back" ? "scaleX(-1)" : "none" }} />}
                    </span>
                    <span style={{ display: "flex", flexDirection: "column", gap: 2, minWidth: 0 }}>
                      <span style={{ fontSize: 12.5, fontWeight: 600, whiteSpace: "nowrap" }}>{c.name}</span>
                      <span style={{ fontSize: 11, color: PTE_C.z500, whiteSpace: "nowrap" }}>{v.name} · {count === 1 ? "1 layer" : count + " layers"}</span>
                    </span>
                    <span onClick={(e) => { e.stopPropagation(); setChecked((s) => ({ ...s, [id]: !s[id] })); }} style={{ width: 20, height: 20, borderRadius: 5, flexShrink: 0, border: "1px solid " + (tick ? PTE_C.ink : PTE_C.z300), background: tick ? PTE_C.ink : "#fff", color: "#fff", fontSize: 11, display: "flex", alignItems: "center", justifyContent: "center" }}>{tick ? "✓" : ""}</span>
                  </div>
                );
              }))}
            </div>
          </div>
        </div>

        <div style={{ width: 340, flexShrink: 0, background: "#fff", borderLeft: "1px solid " + PTE_C.z200, display: "flex", flexDirection: "column", minHeight: 0 }}>
          <div style={{ padding: 16, borderBottom: "1px solid " + PTE_C.z200, display: "flex", gap: 10 }}>
            <button onClick={() => setPanel("upload")} style={{ flex: 1, padding: 10, border: "1px solid " + PTE_C.z300, borderRadius: 6, background: "#fff", fontSize: 12.5, fontWeight: 600, fontFamily: "inherit", cursor: "pointer", color: PTE_C.ink }}>Upload image</button>
            <button onClick={doAddText} style={{ flex: 1, padding: 10, border: "1px solid " + PTE_C.z300, borderRadius: 6, background: "#fff", fontSize: 12.5, fontWeight: 600, fontFamily: "inherit", cursor: "pointer", color: PTE_C.ink }}>Add text</button>
          </div>

          <div style={{ flex: 1, minHeight: 0, overflowY: "auto" }}>
            <div style={{ padding: "16px 16px 8px", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 10 }}>
              <span style={sectionLabel}>Layers</span>
              <span style={{ fontSize: 11, color: PTE_C.z400 }}>{layers.length === 1 ? "1 layer" : layers.length + " layers"}</span>
            </div>
            <div style={{ padding: "0 16px 8px", display: "flex", flexDirection: "column", gap: 8 }}>
              {layers.slice().reverse().map((l) => {
                const isSel = l.id === selectedId;
                return (
                  <div key={l.id} onClick={() => setSelectedId(l.id)} style={{ display: "flex", alignItems: "center", gap: 9, padding: "9px 10px", border: "1px solid " + (isSel ? PTE_C.red : PTE_C.z200), background: isSel ? "#fdf3f2" : "#fff", borderRadius: 8, cursor: "pointer" }}>
                    <span onPointerDown={(e) => startGrip(e, l)} title="Drag to reorder" style={{ display: "flex", flexDirection: "column", gap: 2, padding: "2px 3px", cursor: "grab", flexShrink: 0 }}>
                      {[0, 1, 2].map((r) => <span key={r} style={{ display: "flex", gap: 2 }}><span style={{ width: 3, height: 3, borderRadius: 999, background: PTE_C.z300 }} /><span style={{ width: 3, height: 3, borderRadius: 999, background: PTE_C.z300 }} /></span>)}
                    </span>
                    <span style={{ width: 34, height: 34, borderRadius: 5, flexShrink: 0, display: "flex", alignItems: "center", justifyContent: "center", background: cw.dark ? PTE_C.z800 : PTE_C.z100, color: l.color, fontSize: 9, fontWeight: 800, letterSpacing: ".5px", border: "1px solid " + PTE_C.z200, fontFamily: "Archivo, sans-serif", overflow: "hidden" }}>{l.type === "art" ? l.mark : "T"}</span>
                    <span style={{ display: "flex", flexDirection: "column", gap: 2, minWidth: 0, flex: 1 }}>
                      <span style={{ fontSize: 12.5, fontWeight: 600, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{l.name}</span>
                      <span style={{ fontSize: 11, color: PTE_C.z500, whiteSpace: "nowrap" }}>{l.type === "art" ? "Artwork" : "Text · " + (IE_FONTS.find((f) => f.id === l.font) || IE_FONTS[0]).label} · {ieZone(l)} {ieInches(l.w)}</span>
                    </span>
                    <span style={{ display: "flex", gap: 2, flexShrink: 0 }}>
                      <button title="Duplicate" onClick={(e) => { e.stopPropagation(); const clone = { ...l, id: "l" + Date.now(), x: clamp(l.x + 4, 3, 97), y: clamp(l.y + 4, 3, 97) }; setLayers([...layers, clone], clone.id); }} style={{ width: 26, height: 26, borderRadius: 5, border: 0, background: "none", color: PTE_C.z600, fontSize: 13, cursor: "pointer", fontFamily: "inherit" }}>⧉</button>
                      <button title="Delete" onClick={(e) => { e.stopPropagation(); setLayers(layers.filter((x) => x.id !== l.id), null); }} style={{ width: 26, height: 26, borderRadius: 5, border: 0, background: "none", color: PTE_C.z600, fontSize: 13, cursor: "pointer", fontFamily: "inherit" }}>✕</button>
                    </span>
                  </div>
                );
              })}
              {!layers.length && (
                <div style={{ padding: "22px 16px", border: "1px dashed " + PTE_C.z300, borderRadius: 8, textAlign: "center" }}>
                  <div style={{ fontSize: 12.5, color: PTE_C.z600, marginBottom: 4 }}>No artwork on this image</div>
                  <div style={{ fontSize: 11.5, color: PTE_C.z400, lineHeight: 1.5 }}>Upload a mark, add text, or paste from another variant.</div>
                </div>
              )}
            </div>

            {sel && (
              <div style={{ margin: "12px 16px 16px", border: "1px solid " + PTE_C.z200, borderRadius: 10, overflow: "hidden" }}>
                <div style={{ padding: "11px 14px", background: PTE_C.z50, borderBottom: "1px solid " + PTE_C.z200 }}>
                  <span style={sectionLabel}>{sel.type === "art" ? "Artwork properties" : "Text properties"}</span>
                </div>
                <div style={{ padding: 14 }}>
                  <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 10, marginBottom: 8 }}>
                    <div>
                      <div style={{ fontSize: 11, color: PTE_C.z500, marginBottom: 5 }}>Position</div>
                      <div style={{ padding: "8px 10px", border: "1px solid " + PTE_C.z200, borderRadius: 6, fontSize: 12, fontFamily: "'JetBrains Mono', ui-monospace, monospace" }}>{Math.round(sel.x)}% · {Math.round(sel.y)}%</div>
                    </div>
                    <div>
                      <div style={{ fontSize: 11, color: PTE_C.z500, marginBottom: 5 }}>Size</div>
                      <div style={{ padding: "8px 10px", border: "1px solid " + PTE_C.z200, borderRadius: 6, fontSize: 12, fontFamily: "'JetBrains Mono', ui-monospace, monospace" }}>{Math.round(sel.w)}% wide</div>
                    </div>
                  </div>
                  <div style={{ fontSize: 11.5, color: PTE_C.z500, marginBottom: 14 }}>Reads as <strong style={{ color: PTE_C.ink, fontWeight: 600 }}>{ieZone(sel)} · {ieInches(sel.w)}</strong> on the imprint record</div>

                  {sel.type === "text" && (
                    <div style={{ marginBottom: 14 }}>
                      <div style={{ fontSize: 11, color: PTE_C.z500, marginBottom: 5 }}>Text</div>
                      <input value={sel.text} onChange={(e) => { const v = e.target.value; setLayersBy((cur) => ({ ...cur, [active]: (cur[active] || []).map((l) => l.id === sel.id ? { ...l, text: v, name: v || "Text" } : l) })); setSaved(false); }}
                        style={{ width: "100%", boxSizing: "border-box", padding: "9px 11px", border: "1px solid " + PTE_C.z300, borderRadius: 6, fontSize: 13, fontFamily: "inherit", outline: "none" }} />
                      <div style={{ display: "flex", gap: 8, marginTop: 10 }}>
                        {IE_FONTS.map((f) => (
                          <button key={f.id} onClick={() => patchSel({ font: f.id })} style={{ padding: "7px 12px", borderRadius: 6, fontSize: 12, cursor: "pointer", fontFamily: f.stack, border: "1px solid " + (sel.font === f.id ? PTE_C.ink : PTE_C.z200), background: sel.font === f.id ? PTE_C.ink : "#fff", color: sel.font === f.id ? "#fff" : PTE_C.z700 }}>{f.label}</button>
                        ))}
                      </div>
                    </div>
                  )}

                  {sel.type === "art" && (
                    <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, padding: "2px 0 14px", borderBottom: "1px solid " + PTE_C.z100, marginBottom: 14 }}>
                      <span style={{ fontSize: 12.5, color: PTE_C.z700 }}>Remove white background</span>
                      <span onClick={() => patchSel({ removeWhite: !sel.removeWhite })} style={{ width: 38, height: 22, borderRadius: 999, position: "relative", flexShrink: 0, cursor: "pointer", background: sel.removeWhite ? PTE_C.ink : PTE_C.z300 }}>
                        <span style={{ position: "absolute", top: 2, left: sel.removeWhite ? 18 : 2, width: 18, height: 18, borderRadius: 999, background: "#fff", transition: "left .14s cubic-bezier(.2,0,0,1)" }} />
                      </span>
                    </div>
                  )}

                  <div style={{ fontSize: 11, color: PTE_C.z500, marginBottom: 8 }}>Recolor</div>
                  <div style={{ display: "flex", gap: 8 }}>
                    {swatches.map((c) => <button key={c} onClick={() => patchSel({ color: c })} title={c} style={swatchStyle(c, true)} />)}
                  </div>
                </div>
              </div>
            )}
          </div>

          <div style={{ flexShrink: 0, borderTop: "1px solid " + PTE_C.z200, padding: "14px 16px" }}>
            <div style={{ marginBottom: 8 }}><span style={sectionLabel}>Image caption</span></div>
            <div style={{ padding: "9px 11px", border: "1px solid " + PTE_C.z200, borderRadius: 6, fontSize: 12.5, color: PTE_C.z600, background: PTE_C.z50, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{p.title} — {cw.name} ({view.name.toLowerCase()})</div>
          </div>
        </div>
      </div>

      {panel === "upload" && (
        <div onClick={() => setPanel(null)} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 40, padding: 24 }}>
          <div onClick={(e) => e.stopPropagation()} style={{ width: 620, maxWidth: "100%", background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.18)", overflow: "hidden" }}>
            <div style={{ padding: "18px 22px", borderBottom: "1px solid " + PTE_C.z200, display: "flex", alignItems: "center", justifyContent: "space-between" }}>
              <span style={{ fontSize: 16, fontWeight: 700, fontFamily: "Archivo, sans-serif" }}>Upload image</span>
              <button onClick={() => setPanel(null)} style={{ border: 0, background: "none", color: PTE_C.z500, fontSize: 15, cursor: "pointer", fontFamily: "inherit" }}>✕</button>
            </div>
            <div style={{ padding: 22 }}>
              <div style={{ border: "1.5px dashed " + PTE_C.z300, borderRadius: 10, padding: 30, textAlign: "center", background: PTE_C.z50, marginBottom: 22 }}>
                <div style={{ fontSize: 14.5, fontWeight: 600, marginBottom: 6 }}>Drag and drop artwork here</div>
                <div style={{ fontSize: 12.5, color: PTE_C.z500 }}>or click to browse · .gif, .jpg, .png, .svg</div>
              </div>
              <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 12 }}>
                <span style={sectionLabel}>Team artwork library</span>
                <span style={{ fontSize: 11, color: PTE_C.z400 }}>Lives on the team — persists across seasons</span>
              </div>
              <div style={{ display: "grid", gridTemplateColumns: "repeat(3,minmax(0,1fr))", gap: 12, marginBottom: 24 }}>
                {library.map((a) => (
                  <div key={a.id} onClick={() => setPick(a.id)} style={{ padding: 10, borderRadius: 8, cursor: "pointer", border: "1px solid " + (pick === a.id ? PTE_C.ink : PTE_C.z200), background: pick === a.id ? PTE_C.z50 : "#fff" }}>
                    <span style={{ display: "flex", alignItems: "center", justifyContent: "center", height: 64, marginBottom: 8, borderRadius: 6, background: PTE_C.z100, fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: 15, letterSpacing: "1px", color: PTE_C.z700, overflow: "hidden" }}>{a.mark}</span>
                    <span style={{ display: "block", fontSize: 12, fontWeight: 600, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{a.name}</span>
                    <span style={{ fontSize: 11, color: PTE_C.z500 }}>{a.meta}</span>
                  </div>
                ))}
              </div>
              <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12 }}>
                <button onClick={() => setPick(null)} style={{ border: 0, background: "none", fontSize: 12.5, color: PTE_C.redInk, cursor: "pointer", fontFamily: "inherit", padding: 0 }}>{pick ? "Remove " + (library.find((a) => a.id === pick) || {}).name : "Nothing selected"}</button>
                <button onClick={doInsert} style={{ background: PTE_C.ink, color: "#fff", border: 0, borderRadius: 6, padding: "10px 20px", fontSize: 12.5, fontWeight: 700, fontFamily: "inherit", cursor: "pointer" }}>Insert image</button>
              </div>
            </div>
          </div>
        </div>
      )}

      {toast && <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: PTE_C.ink, color: "#fff", padding: "12px 20px", borderRadius: 8, fontSize: 12.5, boxShadow: "0 12px 32px rgba(9,9,11,.25)", zIndex: 50 }}>{toast}</div>}
    </div>
  );
}

// ─────────── Stock Vault — workspace-level overlay (Epic F) ───────────
// Vault categories are data-driven per third-party vendor (this seller uses
// Stock · Custom); refPrice is a reference/default only — pricing is
// re-confirmed on every deploy.
window.__vaultItems = window.__vaultItems || [
  { id: "v1", title: "Fleece Hoodie — Lions left chest", sku: "MAFLHD-BK", refPrice: 54, vaultCategory: "Stock", brand: "Marucci", source: "Decorated stock", photo: "performance-hoodie", category: "Fleece", deco: "Lincoln Lions primary · Left chest 3.5in · Embroidery · Approved", used: "3 stores" },
  { id: "v2", title: "Performance Tee — Lions full front", sku: "MATEE-PF-GR", refPrice: 28, vaultCategory: "Stock", brand: "Marucci", source: "Decorated stock", photo: "hooded-tech-tee", category: "Tees", deco: "Lincoln Lions wordmark · Full front 10in · Screen print · Approved", used: "2 stores" },
  { id: "v3", title: "Coaches Polo — Wordmark", sku: "VCPOLO-NV", refPrice: 46, vaultCategory: "Stock", brand: "Victus", source: "Decorated stock", letter: "P", category: "Polos", deco: "Wordmark · Left chest 3in · Embroidery · Approved", used: "1 store" },
  { id: "v4", title: "Sideline Cap — Alternate mark", sku: "VCCAP-ALT", refPrice: 24, vaultCategory: "Stock", brand: "Victus", source: "Decorated stock", letter: "C", category: "Accessories", deco: "Alternate mark · Cap front 2.5in · Embroidery · Approved", used: "4 stores" },
  { id: "v5", title: "Legacy Dugout Jacket — Lions", sku: "MADGJ-LEG", refPrice: 89, vaultCategory: "Stock", brand: "Marucci", source: "Decorated stock", letter: "J", category: "Fleece", deco: "Lions primary · Left chest 3.5in · Embroidery · Approved", used: "2 stores", discontinued: true },
  { id: "v6", title: "Custom Sublimated Game Jersey — Lions", sku: "CUS-LJ-24", refPrice: 72, vaultCategory: "Custom", brand: "Marucci", source: "Custom design", letter: "G", category: "Jerseys", deco: "Full sublimation · Lions home set · Approved", used: "2 stores" },
  { id: "v7", title: "Custom Warm-Up Jacket — Lions script", sku: "CUS-WU-11", refPrice: 64, vaultCategory: "Custom", brand: "Marucci", source: "Custom design", letter: "W", category: "Fleece", deco: "Lions script · Full back 12in · Sublimation · Approved", used: "1 store" },
];

// Shared Vault search — matches product name or SKU; a pasted exact SKU
// returns its item as the top result.
function vaultSearch(items, q) {
  const s = (q || "").trim().toLowerCase();
  if (!s) return items;
  const hits = items.filter((v) => [v.title, v.brand, v.source, v.deco, v.sku].filter(Boolean).some((x) => String(x).toLowerCase().includes(s)));
  return hits.slice().sort((a, b) => (((b.sku || "").toLowerCase() === s) ? 1 : 0) - (((a.sku || "").toLowerCase() === s) ? 1 : 0));
}

// ─────────── Stock Vault, sectioned by vendor ───────────
// The Vault IS each enabled vendor's catalog: vendor stock and the decorated
// items saved from it live under one section per enabled vendor.
function vaultVendorGroups(vaultItems, entitled, q) {
  const stock = MARUCCI_CATALOG.map((c) => ({
    key: "cat-" + c.id, raw: c, id: c.id, sku: c.sku, title: c.title,
    subtitle: c.sku + " · " + c.cat + " · Cost " + c.cost,
    photo: c.photo, letter: c.letter, kind: "catalog",
    brand: String(c.sku || "").startsWith("V") ? "Victus" : "Marucci",
    reason: c.oos ? "Out of stock" : null,
  }));
  const saved = (vaultItems || []).map((v) => ({
    key: "vlt-" + v.id, raw: v, id: v.id, sku: v.sku, title: v.title,
    subtitle: (v.refPrice != null ? "ref. $" + v.refPrice.toFixed(2) + " · " : "") + (v.deco || ""),
    photo: v.photo, letter: v.letter, kind: "vault",
    brand: v.brand || "Prolook", source: v.source, deco: v.deco,
    reason: v.discontinued ? "SKU discontinued" : null,
  }));
  const all = vaultSearch(saved.concat(stock), q);
  const brands = (entitled && entitled.length ? entitled : ["Marucci", "Victus", "Prolook"]);
  return brands
    .map((b) => ({ brand: b, vendor: b !== "Prolook", items: all.filter((i) => i.brand === b) }))
    .filter((g) => g.items.length);
}

function VaultTabContent({ store, accent, committedIds, onAddVault, onAddCatalog }) {
  const [q, setQ] = React.useState("");
  const entitled = ((store || {}).entitledBrands) || ["Marucci", "Victus", "Prolook"];
  const groups = vaultVendorGroups(window.__vaultItems || [], entitled, q);
  const total = groups.reduce((n, g) => n + g.items.length, 0);
  return (
    <div>
      <VaultSearchField value={q} onChange={setQ} style={{ marginBottom: 24 }} />
      {total === 0 && (
        <div style={{ padding: "56px 20px", textAlign: "center", color: "#9a9aa1", fontSize: 13.5 }}>
          {q ? "No Vault items match “" + q + "” — try a product name or exact SKU."
             : "Nothing in the Vault yet — enable a vendor or save a decorated product to reuse it here."}
        </div>
      )}
      <div style={{ display: "grid", gap: 36 }}>
        {groups.map((g) => (
          <div key={g.brand}>
            <div style={{ display: "flex", alignItems: "center", gap: 10, paddingBottom: 10, marginBottom: 18, borderBottom: "1px solid #ececf0" }}>
              <span style={{ fontSize: 15, fontWeight: 800, letterSpacing: ".01em", color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>{g.brand}</span>
              {g.vendor && <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".08em", textTransform: "uppercase", color: "#6b6b72", background: "#f1f1f3", border: "1px solid #ececf0", borderRadius: 4, padding: "3px 6px" }}>Vendor</span>}
              <span style={{ fontSize: 12, fontWeight: 600, color: "#9a9aa1" }}>{g.items.length} product{g.items.length === 1 ? "" : "s"}</span>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(220px, 1fr))", gap: 16 }}>
              {g.items.map((it) => (
                <SavedAssetCard key={it.key} kind="design" accent={accent}
                  title={it.title} subtitle={it.subtitle} photo={it.photo} letter={it.letter}
                  badge={it.reason} disabledReason={it.reason}
                  committed={committedIds.has(it.id)}
                  onAdd={() => it.kind === "vault" ? onAddVault(it.raw) : onAddCatalog(it.raw)}
                  onEdit={() => {}} onRemove={() => {}} />
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

// ─────────── Vault row actions (main Stock Vault page) ───────────
function VaultRowMenu({ onRename, onDelete, onDeploy }) {
  const [open, setOpen] = React.useState(false);
  const [pos, setPos] = React.useState({ top: 0, left: 0 });
  const btnRef = React.useRef(null);
  const popRef = React.useRef(null);
  React.useEffect(() => {
    if (!open) return;
    const onDoc = (e) => {
      if (popRef.current && popRef.current.contains(e.target)) return;
      if (btnRef.current && btnRef.current.contains(e.target)) return;
      setOpen(false);
    };
    const onKey = (e) => { if (e.key === "Escape") setOpen(false); };
    const onScroll = () => setOpen(false);
    document.addEventListener("mousedown", onDoc);
    document.addEventListener("keydown", onKey);
    window.addEventListener("scroll", onScroll, true);
    return () => { document.removeEventListener("mousedown", onDoc); document.removeEventListener("keydown", onKey); window.removeEventListener("scroll", onScroll, true); };
  }, [open]);
  const toggle = () => {
    if (open) { setOpen(false); return; }
    const r = btnRef.current.getBoundingClientRect();
    setPos({ top: r.bottom + 6, left: Math.max(12, r.right - 176) });
    setOpen(true);
  };
  const Item = ({ label, danger, onClick }) => (
    <button type="button" onClick={() => { setOpen(false); onClick(); }} style={{
      display: "flex", alignItems: "center", width: "100%", boxSizing: "border-box", gap: 10,
      background: "transparent", border: 0, padding: "9px 14px", cursor: "pointer", fontFamily: "inherit",
      fontSize: 12.5, fontWeight: 600, textAlign: "left", color: danger ? PTE_C.redInk : PTE_C.z800,
    }}
      onMouseEnter={(e) => (e.currentTarget.style.background = danger ? "#fdf3f2" : PTE_C.z50)}
      onMouseLeave={(e) => (e.currentTarget.style.background = "transparent")}>{label}</button>
  );
  return (
    <React.Fragment>
      <button ref={btnRef} type="button" aria-label="Item actions" aria-haspopup="menu" aria-expanded={open} onClick={toggle} style={{
        width: 32, height: 32, borderRadius: 6, display: "inline-flex", alignItems: "center", justifyContent: "center",
        border: "1px solid " + (open ? PTE_C.z300 : "transparent"), background: open ? PTE_C.z100 : "transparent",
        color: PTE_C.z600, cursor: "pointer", fontFamily: "inherit", flexShrink: 0,
      }}
        onMouseEnter={(e) => { if (!open) e.currentTarget.style.background = PTE_C.z100; }}
        onMouseLeave={(e) => { if (!open) e.currentTarget.style.background = "transparent"; }}>
        <svg width="16" height="16" viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
          <circle cx="3" cy="8" r="1.5" /><circle cx="8" cy="8" r="1.5" /><circle cx="13" cy="8" r="1.5" />
        </svg>
      </button>
      {open && (
        <div ref={popRef} role="menu" style={{
          position: "fixed", top: pos.top, left: pos.left, width: 176, zIndex: 500,
          background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10,
          boxShadow: "0 16px 36px rgba(9,9,11,.16)", padding: "6px 0",
        }}>
          {onDeploy && <Item label="Add to a store…" onClick={onDeploy} />}
          <Item label="Rename" onClick={onRename} />
          <Item label="Delete" danger onClick={onDelete} />
        </div>
      )}
    </React.Fragment>
  );
}

function VaultModalShell({ title, children, footer, onClose }) {
  return (
    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 600, padding: 24 }}>
      <div onClick={(e) => e.stopPropagation()} style={{ width: 440, maxWidth: "100%", background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.18)", overflow: "hidden" }}>
        <div style={{ padding: "16px 20px", borderBottom: "1px solid " + PTE_C.z200, display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12 }}>
          <span style={{ fontSize: 15, fontWeight: 700, fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>{title}</span>
          <button type="button" onClick={onClose} aria-label="Close" style={{ border: 0, background: "none", color: PTE_C.z500, fontSize: 14, cursor: "pointer", fontFamily: "inherit" }}>✕</button>
        </div>
        <div style={{ padding: "20px" }}>{children}</div>
        <div style={{ padding: "14px 20px", borderTop: "1px solid " + PTE_C.z200, background: PTE_C.z50, display: "flex", justifyContent: "flex-end", gap: 10 }}>{footer}</div>
      </div>
    </div>
  );
}
const vaultBtnGhost = { background: "#fff", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, color: PTE_C.z700, cursor: "pointer", fontFamily: "inherit" };
const vaultBtnInk = { background: PTE_C.ink, color: "#fff", border: 0, borderRadius: 6, padding: "9px 18px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" };
const vaultBtnDanger = { background: PTE_C.red, color: "#fff", border: 0, borderRadius: 6, padding: "9px 18px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" };

// 9a — ONE search component, two placements (Vault page + Add Products → Product
// Vault tab). Same matching (name, brand, source, decoration, exact SKU first).
function VaultSearchField({ value, onChange, width = 300, style }) {
  return (
    <input value={value} onChange={(e) => onChange(e.target.value)} placeholder="Search the Vault by name or SKU"
      style={{ width, height: 36, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13, fontFamily: "inherit", outline: "none", display: "block", ...style }} />
  );
}

// 9b — which stores an item is deployed in. Deterministic per item so the list
// matches the "used in N stores" figure every time it opens.
function vaultUsedStores(v, stores) {
  const n = parseInt(String(v.used || "0"), 10) || 0;
  const pool = (stores || []).filter((s) => s.kind !== "fan");
  if (!n || !pool.length) return [];
  let h = 5381; const s = String(v.id || "");
  for (let i = 0; i < s.length; i++) h = ((h * 33) ^ s.charCodeAt(i)) >>> 0;
  const out = [];
  let guard = 0;
  while (out.length < Math.min(n, pool.length) && guard++ < 200) {
    h = (Math.imul(h, 1664525) + 1013904223) >>> 0;
    const cand = pool[h % pool.length];
    if (!out.includes(cand)) out.push(cand);
  }
  return out;
}
function VaultUsedInModal({ item, stores, onClose, onOpenStore }) {
  const used = vaultUsedStores(item, stores);
  return (
    <VaultModalShell title={"Used in " + used.length + (used.length === 1 ? " store" : " stores")} onClose={onClose}
      footer={<button type="button" onClick={onClose} style={vaultBtnGhost}>Close</button>}>
      <div style={{ fontSize: 13, color: PTE_C.z600, lineHeight: 1.55, marginBottom: 14 }}>
        <strong style={{ color: PTE_C.ink }}>{item.title}</strong> is deployed in the stores below. Each copy is its own store product — opening one takes you to that store’s catalog.
      </div>
      {used.length === 0 && <div style={{ fontSize: 13, color: PTE_C.z400 }}>Not deployed anywhere yet.</div>}
      {used.map((s) => (
        <div key={s.id} style={{ display: "flex", alignItems: "center", gap: 12, padding: "11px 12px", border: "1px solid " + PTE_C.z200, borderRadius: 8, marginBottom: 8 }}>
          <span style={{ width: 30, height: 30, borderRadius: 8, flexShrink: 0, background: (s.teamColors && s.teamColors[0]) || PTE_C.ink, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 11, fontWeight: 800 }}>{String(s.storeTitle || s.teamName || "S").slice(0, 2).toUpperCase()}</span>
          <span style={{ minWidth: 0, flex: 1 }}>
            <span style={{ display: "block", fontSize: 13, fontWeight: 700, color: PTE_C.ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s.storeTitle || s.teamName}</span>
            <span style={{ display: "block", fontSize: 11.5, color: PTE_C.z500, marginTop: 1 }}>{s.teamName || "—"} · {s.status === "closed" ? "Closed" : s.status === "archived" ? "Archived" : s.live ? "Active" : "Draft"}</span>
          </span>
          <button type="button" onClick={() => { onClose(); onOpenStore && onOpenStore(s.id); }} style={{ ...vaultBtnGhost, whiteSpace: "nowrap" }}>Open store</button>
        </div>
      ))}
    </VaultModalShell>
  );
}

// 9c — a Vault selection can start a NEW store or land in an EXISTING one. The
// store list is searchable and shows enough to pick confidently; entitlement and
// discontinued SKUs are resolved before the deploy button unlocks.
function VaultDeployModal({ items, stores, onClose, onConfirm, onStartNew }) {
  const [q, setQ] = React.useState("");
  const [pick, setPick] = React.useState(null);
  const list = (stores || []).filter((s) => s.kind !== "fan" && s.status !== "archived").filter((s) => {
    const t = (q || "").trim().toLowerCase();
    return !t || [s.storeTitle, s.teamName, s.sport].filter(Boolean).some((x) => String(x).toLowerCase().includes(t));
  });
  const dest = list.find((s) => s.id === pick) || null;
  const entitled = dest ? (dest.entitledBrands || ["Marucci", "Victus", "Prolook"]) : null;
  const blocked = items.filter((v) => v.discontinued);
  const notEntitled = dest ? items.filter((v) => !v.discontinued && !entitled.includes(v.brand)) : [];
  const ok = items.filter((v) => !v.discontinued && (!dest || entitled.includes(v.brand)));
  return (
    <VaultModalShell title={"Deploy " + items.length + (items.length === 1 ? " item" : " items")} onClose={onClose}
      footer={<React.Fragment>
        <button type="button" onClick={onClose} style={vaultBtnGhost}>Cancel</button>
        <button type="button" onClick={() => { onClose(); onStartNew && onStartNew(); }} style={vaultBtnGhost}>Start a new store instead</button>
        <button type="button" disabled={!dest || ok.length === 0} onClick={() => onConfirm(dest, ok)}
          style={{ ...vaultBtnInk, opacity: dest && ok.length ? 1 : .45, cursor: dest && ok.length ? "pointer" : "default" }}>
          {dest ? "Add " + ok.length + (ok.length === 1 ? " item to " : " items to ") + (dest.storeTitle || dest.teamName) : "Pick a store"}
        </button>
      </React.Fragment>}>
      <div style={{ fontSize: 12.5, color: PTE_C.z600, lineHeight: 1.55, marginBottom: 12 }}>
        Items land as <strong style={{ color: PTE_C.ink }}>drafts flagged for confirmation</strong> — pricing, team setting, roster assignment and sizes are re-confirmed in the destination store, and the normal publish gates still apply.
      </div>
      <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 14 }}>
        {items.map((v) => (
          <span key={v.id} style={{ fontSize: 11.5, fontWeight: 600, color: v.discontinued ? PTE_C.warn : PTE_C.z700, background: v.discontinued ? "#fff9ec" : PTE_C.z50, border: "1px solid " + (v.discontinued ? "#f0dcb4" : PTE_C.z200), borderRadius: 999, padding: "4px 10px" }}>
            {v.title}{v.discontinued ? " · SKU discontinued" : ""}
          </span>
        ))}
      </div>
      <VaultSearchField value={q} onChange={setQ} width="100%" style={{ marginBottom: 12 }} />
      <div style={{ maxHeight: 260, overflowY: "auto", marginBottom: 4 }}>
        {list.length === 0 && <div style={{ fontSize: 12.5, color: PTE_C.z400, padding: "14px 0" }}>No stores match “{q}”.</div>}
        {list.map((s) => {
          const on = s.id === pick;
          const ent = s.entitledBrands || ["Marucci", "Victus", "Prolook"];
          const lockedHere = items.filter((v) => !v.discontinued && !ent.includes(v.brand)).length;
          return (
            <button key={s.id} type="button" onClick={() => setPick(s.id)} style={{ display: "flex", alignItems: "center", gap: 11, width: "100%", textAlign: "left", cursor: "pointer", background: on ? PTE_C.z50 : "#fff", border: "1px solid " + (on ? PTE_C.ink : PTE_C.z200), borderRadius: 8, padding: "10px 12px", marginBottom: 6, fontFamily: "inherit" }}>
              <span style={{ width: 28, height: 28, borderRadius: 7, flexShrink: 0, background: (s.teamColors && s.teamColors[0]) || PTE_C.ink, color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 10.5, fontWeight: 800 }}>{String(s.storeTitle || s.teamName || "S").slice(0, 2).toUpperCase()}</span>
              <span style={{ minWidth: 0, flex: 1 }}>
                <span style={{ display: "block", fontSize: 13, fontWeight: 700, color: PTE_C.ink, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s.storeTitle || s.teamName}</span>
                <span style={{ display: "block", fontSize: 11.5, color: PTE_C.z500, marginTop: 1 }}>{s.teamName || "—"} · {s.status === "closed" ? "Closed" : s.live ? "Active" : "Draft"} · {(s.products || []).length} products</span>
              </span>
              {lockedHere > 0 && <span title="This store's seller is not entitled to that brand" style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".04em", color: PTE_C.z500, background: PTE_C.z100, borderRadius: 999, padding: "3px 8px", flexShrink: 0 }}>{lockedHere} LOCKED</span>}
            </button>
          );
        })}
      </div>
      {(blocked.length > 0 || notEntitled.length > 0) &&
        <div style={{ display: "flex", gap: 9, alignItems: "flex-start", background: "#fff9ec", border: "1px solid #f0dcb4", borderRadius: 8, padding: "10px 12px", marginTop: 8, fontSize: 11.5, color: "#6b5314", lineHeight: 1.5 }}>
          <IconAlert size={13} style={{ flexShrink: 0, marginTop: 1, color: PTE_C.warn }} />
          <span>
            {blocked.length > 0 && <React.Fragment>{blocked.length} item{blocked.length === 1 ? "" : "s"} can’t deploy — the referenced SKU is discontinued. </React.Fragment>}
            {notEntitled.length > 0 && <React.Fragment>{notEntitled.length} item{notEntitled.length === 1 ? "" : "s"} are locked for this store’s seller entitlement. </React.Fragment>}
            The rest deploy normally — nothing fails silently.
          </span>
        </div>}
    </VaultModalShell>
  );
}

function PlatformVaultBody({ onDeployed, rowActions, onItemsChange, onStartStore, stores, onDeployToStore, onOpenStore }) {
  const [rev, setRev] = React.useState(0);
  const [renameTarget, setRenameTarget] = React.useState(null);
  const [renameText, setRenameText] = React.useState("");
  const [deleteTarget, setDeleteTarget] = React.useState(null);
  const [rowToast, setRowToast] = React.useState(null);
  React.useEffect(() => { if (!rowToast) return; const t = setTimeout(() => setRowToast(null), 2600); return () => clearTimeout(t); }, [rowToast]);
  const startRename = (v) => { setRenameText(v.title); setRenameTarget(v); };
  const commitRename = () => {
    const name = renameText.trim();
    if (!name) return;
    const it = (window.__vaultItems || []).find((x) => x.id === renameTarget.id);
    if (it) it.title = name;
    setRenameTarget(null); setRev((n) => n + 1); onItemsChange && onItemsChange(); setRowToast("Renamed to “" + name + "”");
  };
  const commitDelete = () => {
    window.__vaultItems = (window.__vaultItems || []).filter((x) => x.id !== deleteTarget.id);
    setRowToast("“" + deleteTarget.title + "” deleted from the Vault");
    setDeleteTarget(null); setRev((n) => n + 1); onItemsChange && onItemsChange();
  };
  const [noticeOpen, setNoticeOpen] = React.useState(true);
  // 9a — the same search component the Add Products Vault tab uses.
  const [q, setQ] = React.useState("");
  const [usedTarget, setUsedTarget] = React.useState(null);
  const [deployItems, setDeployItems] = React.useState(null);
  const allItems = vaultSearch(window.__vaultItems || [], q);
  // Category facet — data-driven per vendor; "All" is always present.
  const cats = ["All"].concat(Array.from(new Set(allItems.map((v) => v.vaultCategory).filter(Boolean))));
  const [cat, setCat] = React.useState("All");
  const items = cat === "All" ? allItems : allItems.filter((v) => v.vaultCategory === cat);
  const selectable = !!(rowActions && onStartStore);
  const [sel, setSel] = React.useState({});
  const selIds = Object.keys(sel).filter((k) => sel[k]);
  const selectableItems = items.filter((v) => !v.discontinued);
  const allChecked = selectableItems.length > 0 && selectableItems.every((v) => sel[v.id]);
  const toggleAll = () => setSel(allChecked ? {} : Object.fromEntries(selectableItems.map((v) => [v.id, true])));
  const target = window.__vaultDeployTarget || null;
  const grid = (selectable ? "36px " : "") + "minmax(240px,1.4fr) 100px 130px 1.5fr 100px 90px " + (rowActions ? "56px" : "200px");
  return (
    <React.Fragment>
      <p style={{ fontSize: 14, color: PTE_C.z500, margin: "0 0 20px", maxWidth: 760, lineHeight: 1.55 }}>Spans every store on your platform. A Vault item carries its <strong style={{ color: PTE_C.z800 }}>source, brand, and decoration record only</strong> — pricing, team setting, roster assignment and sizes are re-confirmed each time it deploys into a store. Decorate a blank once, deploy it store after store.</p>
      {!rowActions && (target
        ? <div style={{ fontSize: 12.5, color: PTE_C.z600, marginBottom: 14 }}>Deploy target: <strong style={{ color: PTE_C.ink }}>{target.label}</strong> · Entitled: {target.entitled.join(", ")}</div>
        : <div style={{ fontSize: 12.5, color: PTE_C.z600, background: PTE_C.z50, border: "1px solid " + PTE_C.z200, borderRadius: 8, padding: "10px 14px", marginBottom: 14, maxWidth: 760 }}>Open a store's Products tab to deploy Vault items into it.</div>)}
      <VaultSearchField value={q} onChange={setQ} style={{ marginBottom: 14 }} />
      {cats.length > 1 && (
        <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 14, flexWrap: "wrap" }}>
          {cats.map((c) => {
            const on = c === cat;
            const n = c === "All" ? allItems.length : allItems.filter((v) => v.vaultCategory === c).length;
            return (
              <button key={c} type="button" onClick={() => { setCat(c); }} style={{ border: "1px solid " + (on ? PTE_C.ink : PTE_C.z300), background: on ? PTE_C.ink : "#fff", color: on ? "#fff" : PTE_C.z700, borderRadius: 999, padding: "7px 14px", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{c} <span style={{ opacity: .6, marginLeft: 2, fontFamily: "'JetBrains Mono', ui-monospace, monospace", fontSize: 11 }}>{n}</span></button>
            );
          })}
        </div>
      )}
      <div style={{ background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, overflowX: "auto", maxWidth: 1376 }}>
        <div style={{ display: "grid", gridTemplateColumns: grid, gap: 16, padding: "12px 20px", borderBottom: "1px solid " + PTE_C.z200, fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z400, background: PTE_C.z50, alignItems: "center" }}>
          {selectable && <input type="checkbox" aria-label="Select all" checked={allChecked} onChange={toggleAll} style={{ width: 16, height: 16, accentColor: PTE_C.ink, cursor: "pointer", margin: 0 }} />}
          <span>Item</span><span>Brand</span><span>Source</span><span>Decoration record</span><span>Ref. price</span><span>Used in</span><span />
        </div>
        {items.map((v) => {
          const entitledOk = !target || target.entitled.includes(v.brand);
          const deployable = entitledOk && !v.discontinued && !!target;
          return (
            <div key={v.id} style={{ display: "grid", gridTemplateColumns: grid, gap: 16, padding: "12px 20px", borderBottom: "1px solid " + PTE_C.z100, alignItems: "center", opacity: entitledOk ? 1 : .65, background: sel[v.id] ? PTE_C.z50 : "transparent" }}>
              {selectable && (v.discontinued
                ? <span title="Discontinued SKUs can't start a store" />
                : <input type="checkbox" aria-label={"Select " + v.title} checked={!!sel[v.id]} onChange={() => setSel((s) => ({ ...s, [v.id]: !s[v.id] }))} style={{ width: 16, height: 16, accentColor: PTE_C.ink, cursor: "pointer", margin: 0 }} />)}
              <span style={{ display: "flex", alignItems: "center", gap: 10, minWidth: 0 }}>
                <PteThumb product={v} />
                <span style={{ minWidth: 0 }}>
                  <span style={{ display: "block", fontSize: 13.5, fontWeight: 600, color: PTE_C.ink }}>{v.title}</span>
                  {v.sku && <span style={{ display: "block", fontSize: 10.5, color: PTE_C.z400, fontFamily: "'JetBrains Mono', ui-monospace, monospace", marginTop: 2 }}>{v.sku}</span>}
                  {rowActions && v.discontinued && <span style={{ display: "inline-block", marginTop: 4, fontSize: 10, fontWeight: 700, letterSpacing: ".4px", textTransform: "uppercase", color: PTE_C.warn, border: "1px solid #f0dcb4", background: "#fff9ec", borderRadius: 999, padding: "2px 8px" }}>SKU discontinued</span>}
                </span>
              </span>
              <span><span style={pteChip(entitledOk ? PTE_C.z100 : PTE_C.z50, entitledOk ? PTE_C.z700 : PTE_C.z400)}>{v.brand}</span></span>
              <span style={{ fontSize: 12.5, color: PTE_C.z600 }}>{v.source}</span>
              <span style={{ fontSize: 12, color: PTE_C.z600, display: "-webkit-box", WebkitLineClamp: 2, WebkitBoxOrient: "vertical", overflow: "hidden" }}>{v.deco}</span>
              <span title="Reference price only — pricing is re-confirmed on every deploy" style={{ fontSize: 12.5, color: PTE_C.z500, fontFamily: "'JetBrains Mono', ui-monospace, monospace", whiteSpace: "nowrap" }}>{v.refPrice != null ? "$" + v.refPrice.toFixed(2) : "—"}{v.refPrice != null && <span style={{ fontSize: 10, color: PTE_C.z400, marginLeft: 4, fontFamily: "Inter, sans-serif" }}>ref.</span>}</span>
              <span>
                {parseInt(String(v.used || "0"), 10) > 0
                  ? <button type="button" onClick={() => setUsedTarget(v)} title={"See the stores using " + v.title}
                      style={{ background: "none", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit", fontSize: 12.5, fontWeight: 700, color: "#2563eb", textAlign: "left" }}
                      onMouseEnter={(e) => (e.currentTarget.style.textDecoration = "underline")}
                      onMouseLeave={(e) => (e.currentTarget.style.textDecoration = "none")}>{v.used}</button>
                  : <span style={{ fontSize: 12.5, color: PTE_C.z400 }}>Not deployed</span>}
              </span>
              <span style={{ display: "flex", justifyContent: "flex-end" }}>
                {rowActions
                  ? <VaultRowMenu onRename={() => startRename(v)} onDelete={() => setDeleteTarget(v)} onDeploy={onDeployToStore && !v.discontinued ? () => setDeployItems([v]) : null} />
                  : v.discontinued
                    ? <span title="The referenced SKU is no longer in the seller's catalog — deploy is blocked with reason, never a silent partial load" style={{ fontSize: 11, fontWeight: 700, color: PTE_C.warn, border: "1px solid #f0dcb4", background: "#fff9ec", borderRadius: 6, padding: "8px 12px", display: "inline-block" }}>SKU discontinued</span>
                    : !entitledOk
                      ? <span title="This store's seller is not entitled to this brand" style={{ fontSize: 11, fontWeight: 700, color: PTE_C.z400, border: "1px solid " + PTE_C.z200, borderRadius: 6, padding: "8px 12px", display: "inline-block" }}>Not entitled</span>
                      : <button disabled={!deployable} onClick={() => { target.deploy(v); onDeployed && onDeployed(); }} style={{ background: deployable ? PTE_C.ink : PTE_C.z100, color: deployable ? "#fff" : PTE_C.z400, border: 0, borderRadius: 6, padding: "8px 14px", fontSize: 12, fontWeight: 700, cursor: deployable ? "pointer" : "default", fontFamily: "inherit" }}>Deploy to this store</button>}
              </span>
            </div>
          );
        })}
      </div>
      {selectable && selIds.length > 0 && (
        <div style={{ position: "fixed", bottom: 28, left: "50%", transform: "translateX(-50%)", zIndex: 300, display: "flex", alignItems: "center", gap: 14, background: "#0e0e10", borderRadius: 10, padding: "12px 12px 12px 18px", boxShadow: "0 16px 40px rgba(9,9,11,.35)" }}>
          <span style={{ fontSize: 12.5, fontWeight: 700, color: "#fff", whiteSpace: "nowrap" }}>{selIds.length} selected</span>
          <button type="button" onClick={() => setSel({})} style={{ background: "transparent", border: 0, padding: "8px 4px", fontSize: 12, fontWeight: 700, color: "rgba(255,255,255,.7)", cursor: "pointer", fontFamily: "inherit" }}>Clear</button>
          {onDeployToStore &&
            <button type="button" onClick={() => setDeployItems(allItems.filter((v) => sel[v.id]))} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #fff", borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" }}>Add to existing store</button>}
          <button type="button" onClick={() => onStartStore(allItems.filter((v) => sel[v.id]))} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#e1251b", border: "1px solid #e1251b", borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" }}>Start a new store ({selIds.length})</button>
        </div>
      )}
      <div style={{ fontSize: 12, color: PTE_C.z400, marginTop: 12, maxWidth: 760 }}>{rowActions
        ? "Vault records are managed here. Select items to start a new store with them, or open a store's Products tab and add from the Vault — pricing, team setting, sizes and roster assignment are confirmed at that point."
        : "Deploying opens the standard per-item confirm (pricing, team setting, sizes, roster assignment) before the item lands as a draft. Entitlement is enforced server-side against the destination store's seller."}</div>
      {renameTarget && (
        <VaultModalShell title="Rename Vault item" onClose={() => setRenameTarget(null)}
          footer={<React.Fragment>
            <button type="button" onClick={() => setRenameTarget(null)} style={vaultBtnGhost}>Cancel</button>
            <button type="button" disabled={!renameText.trim()} onClick={commitRename} style={{ ...vaultBtnInk, opacity: renameText.trim() ? 1 : .45, cursor: renameText.trim() ? "pointer" : "default" }}>Save name</button>
          </React.Fragment>}>
          <label style={{ display: "block", fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: PTE_C.z400, marginBottom: 7 }}>Item name</label>
          <input autoFocus value={renameText} onChange={(e) => setRenameText(e.target.value)}
            onKeyDown={(e) => { if (e.key === "Enter") commitRename(); }}
            style={{ width: "100%", boxSizing: "border-box", padding: "10px 12px", border: "1.5px solid " + PTE_C.z300, borderRadius: 6, fontSize: 13.5, fontFamily: "inherit", outline: "none", color: PTE_C.ink }} />
          <div style={{ fontSize: 11.5, color: PTE_C.z400, marginTop: 8, lineHeight: 1.5 }}>Renaming affects the Vault record only — products already deployed into stores keep their own names.</div>
        </VaultModalShell>
      )}
      {deleteTarget && (
        <VaultModalShell title="Delete Vault item" onClose={() => setDeleteTarget(null)}
          footer={<React.Fragment>
            <button type="button" onClick={() => setDeleteTarget(null)} style={vaultBtnGhost}>Cancel</button>
            <button type="button" onClick={commitDelete} style={vaultBtnDanger}>Delete item</button>
          </React.Fragment>}>
          <div style={{ fontSize: 13.5, color: PTE_C.z700, lineHeight: 1.6 }}>
            Delete <strong style={{ color: PTE_C.ink, fontWeight: 700 }}>{deleteTarget.title}</strong> from the Stock Vault?
          </div>
          <div style={{ display: "flex", alignItems: "center", gap: 10, marginTop: 14, padding: "11px 13px", border: "1px solid " + PTE_C.z200, borderRadius: 8, background: PTE_C.z50 }}>
            <PteThumb product={deleteTarget} />
            <span style={{ minWidth: 0 }}>
              <span style={{ display: "block", fontSize: 12.5, fontWeight: 600, color: PTE_C.ink }}>Used in {deleteTarget.used || "0 stores"}</span>
              <span style={{ display: "block", fontSize: 11.5, color: PTE_C.z500, marginTop: 2 }}>{deleteTarget.brand} · {deleteTarget.source}</span>
            </span>
          </div>
          <div style={{ fontSize: 11.5, color: PTE_C.z500, marginTop: 12, lineHeight: 1.55 }}>Products already deployed into those stores are unaffected — this only removes the reusable Vault record. This can’t be undone.</div>
        </VaultModalShell>
      )}
      {usedTarget && <VaultUsedInModal item={usedTarget} stores={stores} onClose={() => setUsedTarget(null)} onOpenStore={onOpenStore} />}
      {deployItems && deployItems.length > 0 &&
        <VaultDeployModal items={deployItems} stores={stores}
          onClose={() => setDeployItems(null)}
          onStartNew={() => onStartStore && onStartStore(deployItems)}
          onConfirm={(dest, ok) => {
            setDeployItems(null);
            setSel({});
            onDeployToStore && onDeployToStore(dest, ok);
          }} />}
      {rowToast && <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: PTE_C.ink, color: "#fff", padding: "12px 20px", borderRadius: 8, fontSize: 12.5, boxShadow: "0 12px 32px rgba(9,9,11,.25)", zIndex: 700 }}>{rowToast}</div>}
    </React.Fragment>
  );
}

// Dedicated Vault page — reached from the main OVERVIEW / STORES / VAULT nav.
function PlatformVaultPage({ onStartStore, stores, onDeployToStore, onOpenStore }) {
  const [rev, setRev] = React.useState(0);
  const count = (window.__vaultItems || []).length;
  return (
    <div style={{ flex: 1, minWidth: 0 }}>
      <div style={{ maxWidth: 1440, margin: "0 auto", padding: "28px 32px 120px" }}>
        <div style={{ marginBottom: 18 }}>
          <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.3px", margin: "0 0 8px", fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Stock Vault</h2>
          <div style={{ fontSize: 14, color: PTE_C.z500 }} data-rev={rev}>{count} item{count === 1 ? "" : "s"}</div>
        </div>
        <PlatformVaultBody rowActions onItemsChange={() => setRev((n) => n + 1)} onStartStore={onStartStore} stores={stores} onDeployToStore={onDeployToStore} onOpenStore={onOpenStore} />
      </div>
    </div>
  );
}

function PlatformVaultOverlay() {
  const [open, setOpen] = React.useState(false);
  React.useEffect(() => {
    window.__openVault = () => setOpen(true);
    return () => { delete window.__openVault; };
  }, []);
  if (!open) return null;
  return (
    <div style={{ position: "fixed", inset: 0, zIndex: 400, background: "#fbfbfc", overflowY: "auto", fontFamily: '"Inter",system-ui,sans-serif' }}>
      <div style={{ background: "#fff", borderBottom: "1px solid " + PTE_C.z200, position: "sticky", top: 0, zIndex: 5 }}>
        <div style={{ maxWidth: 1240, margin: "0 auto", padding: "18px 32px", display: "flex", alignItems: "center", gap: 12 }}>
          <span style={{ width: 9, height: 9, background: PTE_C.red, borderRadius: 2 }} />
          <h2 style={{ fontSize: 20, fontWeight: 800, letterSpacing: "-.3px", margin: 0, fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Stock Vault</h2>
          <button onClick={() => setOpen(false)} style={{ marginLeft: "auto", background: "#fff", border: "1px solid " + PTE_C.z300, borderRadius: 6, padding: "8px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", color: PTE_C.z800 }}>✕ Close</button>
        </div>
      </div>
      <div style={{ maxWidth: 1240, margin: "0 auto", padding: "24px 32px 120px" }}>
        <PlatformVaultBody onDeployed={() => setOpen(false)} />
      </div>
    </div>
  );
}

// ─────────── Store Settings touches (Epic H) — multi-source stores only ───────────
function CatalogPolicySection({ store, onUpdate }) {
  if (!isMultiSourceStore(store)) return null;
  const policy = store.artPolicy || "block-publish";
  const oos = store.oosTreatment || "hidden";
  const card = { background: "#fff", border: "1px solid " + PTE_C.z200, borderRadius: 10, padding: 22, transition: "box-shadow .12s, border-color .12s" };
  const radio = (on) => ({ width: 16, height: 16, borderRadius: 999, border: "2px solid " + (on ? PTE_C.red : PTE_C.z300), background: on ? PTE_C.red : "#fff", boxShadow: "inset 0 0 0 3px #fff", flexShrink: 0, marginTop: 2 });
  const optBtn = (on) => ({ display: "flex", gap: 12, alignItems: "flex-start", width: "100%", textAlign: "left", background: "none", border: "1px solid " + (on ? PTE_C.ink : PTE_C.z200), borderRadius: 8, padding: "12px 14px", cursor: "pointer", fontFamily: "inherit", marginBottom: 8 });
  const mark = (store.teamName || "Team").split(/[\s—-]/).filter(Boolean).slice(0, 2).map((w) => w[0]).join("").toUpperCase();
  const artMarks = [
    { label: "Primary", tile: mark, ok: true }, { label: "Wordmark", tile: "W", ok: true }, { label: "Alternate", tile: "A", ok: false },
  ];
  return (
    <div style={{ marginTop: 32 }}>
      <h3 style={{ fontSize: 17, fontWeight: 800, margin: "0 0 4px", fontFamily: "Archivo, sans-serif", color: PTE_C.ink }}>Catalog & Decoration</h3>
      <p style={{ fontSize: 13, color: PTE_C.z500, margin: "0 0 18px" }}>Only the sections the multi-Customizer touches; the rest of Settings is unchanged.</p>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
        <div style={card} onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)"; e.currentTarget.style.borderColor = "#c9c9cf"; }} onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#e4e4e7"; }}>
          <div style={{ fontSize: 13, fontWeight: 700, marginBottom: 4, color: PTE_C.ink }}>Seller &amp; brand scope</div>
          <div style={{ fontSize: 12, color: PTE_C.z500, marginBottom: 14 }}>Set at store creation; drives catalog access, checkout merchant, and Vault entitlement.</div>
          <div style={{ display: "flex", justifyContent: "space-between", padding: "9px 0", borderBottom: "1px solid " + PTE_C.z100, fontSize: 13 }}>
            <span style={{ color: PTE_C.z500 }}>Seller of record</span><span style={{ fontWeight: 600 }}>{store.seller || "Marucci Team Stores"}</span>
          </div>
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: "9px 0", fontSize: 13 }}>
            <span style={{ color: PTE_C.z500 }}>Entitled brands</span>
            <span style={{ display: "flex", gap: 6 }}>
              {(store.entitledBrands || []).map((b, i) => (
                <span key={b} style={{ fontSize: 11, fontWeight: 700, background: i === 0 ? PTE_C.ink : PTE_C.z100, color: i === 0 ? "#fff" : PTE_C.z700, borderRadius: 4, padding: "3px 8px" }}>{b.toUpperCase()}</span>
              ))}
            </span>
          </div>
        </div>
        <div style={card} onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)"; e.currentTarget.style.borderColor = "#c9c9cf"; }} onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#e4e4e7"; }}>
          <div style={{ fontSize: 13, fontWeight: 700, marginBottom: 4, color: PTE_C.ink }}>Team artwork library</div>
          <div style={{ fontSize: 12, color: PTE_C.z500, marginBottom: 14 }}>Lives on the Team, not the store — persists across seasons. The Artwork Editor reads from here.</div>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(3,1fr)", gap: 10 }}>
            {artMarks.map((am) => (
              <div key={am.label} style={{ border: "1px solid " + PTE_C.z200, borderRadius: 8, padding: 12, textAlign: "center" }}>
                <span style={{ display: "inline-flex", width: 40, height: 40, borderRadius: 6, background: am.label === "Primary" ? ((store.teamColors && store.teamColors[0]) || PTE_C.ink) : PTE_C.z100, alignItems: "center", justifyContent: "center", fontWeight: 800, color: am.label === "Primary" ? "#fff" : PTE_C.z400, marginBottom: 8, fontFamily: "Archivo, sans-serif" }}>{am.tile}</span>
                <div style={{ fontSize: 11.5, fontWeight: 600 }}>{am.label}</div>
                <span style={{ display: "inline-block", marginTop: 5, fontSize: 9, fontWeight: 700, letterSpacing: ".4px", padding: "2px 7px", borderRadius: 999, background: am.ok ? PTE_C.okBg : PTE_C.warnBg, color: am.ok ? PTE_C.ok : PTE_C.warn }}>{am.ok ? "Production-ready" : "Needs vector"}</span>
              </div>
            ))}
          </div>
        </div>
        <div style={card} onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)"; e.currentTarget.style.borderColor = "#c9c9cf"; }} onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#e4e4e7"; }}>
          <div style={{ fontSize: 13, fontWeight: 700, marginBottom: 4, color: PTE_C.ink }}>Decoration &amp; art approval policy</div>
          <div style={{ fontSize: 12, color: PTE_C.z500, marginBottom: 14 }}>Default is strict — matches the existing publish-gate discipline. Flippable per seller.</div>
          {[
            { key: "block-publish", label: "Unapproved art blocks publish (default)", desc: "A decorated product cannot go live until its placement is approved. Strict — matches the factory-bound PO discipline." },
            { key: "block-submission", label: "Publishable; blocks order submission", desc: "Store can open while art is in review; the close-out gate holds any line whose art is unapproved." },
          ].map((o) => (
            <button key={o.key} onClick={() => onUpdate && onUpdate({ artPolicy: o.key })} style={optBtn(policy === o.key)}>
              <span style={radio(policy === o.key)} />
              <span><strong style={{ fontSize: 13 }}>{o.label}</strong><span style={{ display: "block", fontSize: 11.5, color: PTE_C.z500, marginTop: 2 }}>{o.desc}</span></span>
            </button>
          ))}
        </div>
        <div style={card} onMouseEnter={(e) => { e.currentTarget.style.boxShadow = "0 4px 14px rgba(20,20,30,.08)"; e.currentTarget.style.borderColor = "#c9c9cf"; }} onMouseLeave={(e) => { e.currentTarget.style.boxShadow = "none"; e.currentTarget.style.borderColor = "#e4e4e7"; }}>
          <div style={{ fontSize: 13, fontWeight: 700, marginBottom: 4, color: PTE_C.ink }}>Out-of-stock treatment &amp; Vault</div>
          <div style={{ fontSize: 12, color: PTE_C.z500, marginBottom: 14 }}>Stock is binary per variant — no quantities. Bundle members stay purchasable regardless.</div>
          {[
            { key: "hidden", label: "Hidden from storefront (default)", desc: "Out-of-stock items disappear from buyers; reps still see them with an OUT badge." },
            { key: "shown", label: "Shown, disabled", desc: "Visible with an OUT OF STOCK badge and a disabled buy action." },
          ].map((o) => (
            <button key={o.key} onClick={() => onUpdate && onUpdate({ oosTreatment: o.key })} style={optBtn(oos === o.key)}>
              <span style={radio(oos === o.key)} />
              <span><strong style={{ fontSize: 13 }}>{o.label}</strong><span style={{ display: "block", fontSize: 11.5, color: PTE_C.z500, marginTop: 2 }}>{o.desc}</span></span>
            </button>
          ))}
          <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginTop: 12, paddingTop: 12, borderTop: "1px solid " + PTE_C.z100, fontSize: 12.5 }}>
            <span style={{ color: PTE_C.z600 }}>Suggest “Save to Vault” after each decoration save</span>
            <span style={{ fontSize: 11, fontWeight: 700, background: PTE_C.okBg, color: PTE_C.ok, borderRadius: 999, padding: "3px 10px" }}>ON</span>
          </div>
        </div>
      </div>
    </div>
  );
}

Object.assign(window, { ProductsListV2, AddChannelsView, MarucciCatalogView, BatchDecorateView, ProductImagesView, ProductImageEditor, PlatformVaultOverlay, CatalogPolicySection, isMultiSourceStore });

window.App = App; /* mounted by DC runtime */

/* ===== SETTINGS EXPANSION — sectioned Settings, Workspace Settings, TM portal ===== */

// Generic sectioned side-nav. Used anywhere a tab needs sub-navigation with a
// label + short description (store Settings, Workspace Settings, Orders).
// items: [{ id, label, desc?, count? }]
function SectionSideNav({ items, value, onChange, width = 232 }) {
  return (
    <nav style={{ width, flexShrink: 0, position: "sticky", top: 24, display: "flex", flexDirection: "column", gap: 2 }}>
      {items.map((it) => {
        const on = it.id === value;
        return (
          <button key={it.id} type="button" onClick={() => onChange(it.id)} style={{
            display: "flex", flexDirection: "column", gap: 2, textAlign: "left",
            background: on ? "#f1f1f3" : "transparent", border: 0, borderLeft: "3px solid " + (on ? "#e1251b" : "transparent"),
            borderRadius: "0 8px 8px 0", padding: "10px 14px 10px 13px", cursor: "pointer", fontFamily: "inherit",
          }}
            onMouseEnter={(e) => { if (!on) e.currentTarget.style.background = "#f7f7f8"; }}
            onMouseLeave={(e) => { if (!on) e.currentTarget.style.background = "transparent"; }}>
            <span style={{ display: "flex", alignItems: "center", gap: 8 }}>
              <span style={{ fontSize: 13.5, fontWeight: on ? 800 : 600, color: on ? "#0e0e10" : "#3f3f46" }}>{it.label}</span>
              {it.count > 0 && <span style={{
                fontSize: 10.5, fontWeight: 800, borderRadius: 999, padding: "1px 7px",
                background: on ? "#0e0e10" : "#e4e4e7", color: on ? "#fff" : "#52525b",
              }}>{it.count}</span>}
            </span>
            {it.desc && <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{it.desc}</span>}
          </button>
        );
      })}
    </nav>
  );
}

function StoreSettingsNav({ store, value, onChange }) {
  const items = [
    { id: "store", label: "General", desc: "Details, branding & contact" },
    { id: "schedule", label: "Schedule & Status", desc: "Order window & close dates" },
    { id: "team", label: "Team & Roster", desc: "Roster rules & jersey numbers" },
    { id: "customization", label: "Store Customization & Design", desc: "Storefront color & announcement" },
    { id: "people", label: "People & Access", desc: "Invites & portal access" },
    { id: "distribution", label: "Distribution & Fulfillment", desc: "Fulfillment method & destination" },
  ];
  if (typeof isMultiSourceStore === "function" && isMultiSourceStore(store)) items.push({ id: "catalog", label: "Catalog & Decoration", desc: "Seller scope & art policy" });
  return <SectionSideNav items={items} value={value} onChange={onChange} />;
}

// Small iOS-style toggle used across the new settings sections.
function SettingsToggle({ value, onChange, disabled }) {
  return (
    <span role="switch" aria-checked={!!value} onClick={() => !disabled && onChange(!value)} style={{ width: 38, height: 22, borderRadius: 999, position: "relative", flexShrink: 0, cursor: disabled ? "default" : "pointer", background: value ? "#0e0e10" : "#d4d4d8", opacity: disabled ? .5 : 1, display: "inline-block", transition: "background .14s" }}>
      <span style={{ position: "absolute", top: 2, left: value ? 18 : 2, width: 18, height: 18, borderRadius: 999, background: "#fff", boxShadow: "0 1px 2px rgba(0,0,0,.2)", transition: "left .14s cubic-bezier(.2,0,0,1)" }} />
    </span>
  );
}

// ── 3.4 Jersey number selection — the option set is shared with wizard Step 1
// (Team & Roster panel) so both surfaces look and behave identically. ──
const NUMBER_SELECTION_OPTS = [
  { key: "manager", title: "Team manager chooses on behalf of players", desc: "The manager assigns each player's jersey number on the roster. Buyers see their assigned number on the product page — no number entry at checkout." },
  { key: "player", title: "Players choose their own numbers", desc: "Buyers enter a jersey number on the product page when ordering numbered gear. The roster fills in as orders come through." },
];
function numberSelectionHelper(mode) {
  return mode === "manager" ? "Number entry lives on the roster (My Team tab). Duplicate-number rules from Roster Rules still apply." : "Number entry appears on the buyer product page. Duplicate-number rules from Roster Rules still apply.";
}
function NumberSelectionOptions({ value, onChange }) {
  const radio = (on) => ({ width: 16, height: 16, borderRadius: 999, border: "2px solid " + (on ? "#e1251b" : "#d4d4d8"), background: on ? "#e1251b" : "#fff", boxShadow: "inset 0 0 0 3px #fff", flexShrink: 0, marginTop: 2 });
  return (
    <React.Fragment>
      {NUMBER_SELECTION_OPTS.map((o) => {
        const on = value === o.key;
        return (
          <button key={o.key} type="button" onClick={() => onChange(o.key)} style={{ display: "flex", gap: 12, alignItems: "flex-start", width: "100%", textAlign: "left", background: "none", border: "1px solid " + (on ? "#0e0e10" : "#ececf0"), borderRadius: 8, padding: "12px 14px", cursor: "pointer", fontFamily: "inherit", marginBottom: 8, boxSizing: "border-box" }}>
            <span style={radio(on)} />
            <span><strong style={{ fontSize: 13, color: "#0e0e10" }}>{o.title}</strong><span style={{ display: "block", fontSize: 11.5, color: "#6b6b72", marginTop: 2, lineHeight: 1.5 }}>{o.desc}</span></span>
          </button>
        );
      })}
    </React.Fragment>
  );
}
function NumberSelectionCard({ store, onUpdateStore, onToast }) {
  const mode = store.numberSelection || "manager";
  return (
    <div style={{ marginTop: 16 }}>
    <DetailCard title="Jersey number selection" desc="Decide who chooses jersey numbers for numbered team gear.">
      <NumberSelectionOptions value={mode} onChange={(k) => { onUpdateStore && onUpdateStore({ numberSelection: k }); onToast && onToast("Number selection updated"); }} />
      <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 4, lineHeight: 1.5 }}>
        {numberSelectionHelper(mode)}
      </div>
    </DetailCard>
    </div>
  );
}

// ── 3.2 Repeating close dates ──
function nextCloseAfter(iso, cadence) {
  const d = new Date(iso + "T12:00:00");
  if (isNaN(d)) return null;
  if (cadence === "weekly") d.setDate(d.getDate() + 7);
  else if (cadence === "biweekly") d.setDate(d.getDate() + 14);
  else if (cadence === "monthly") d.setMonth(d.getMonth() + 1);
  else { // end of season/quarter — next quarter end after the current close
    const q = Math.floor(d.getMonth() / 3) + 1;
    d.setMonth(q * 3, 0); // last day of this quarter
    const base = new Date(iso + "T12:00:00");
    if (d <= base) d.setMonth(d.getMonth() + 3 + 1, 0);
  }
  return d.toISOString().slice(0, 10);
}
const REPEAT_CADENCES = [
  { key: "weekly", label: "Weekly" },
  { key: "biweekly", label: "Every 2 weeks" },
  { key: "monthly", label: "Monthly" },
  { key: "season", label: "End of season" },
];
// 2 — the full derived schedule. Every cadence resolves to explicit dates between
// the window's close and the season end the rep set; "End of season" is not a magic
// value, it IS the season end date.
function buildCloseSchedule(firstISO, cadence, seasonEndISO) {
  if (!firstISO || !seasonEndISO) return [];
  if (cadence === "season") return seasonEndISO >= firstISO ? [seasonEndISO] : [];
  const out = [];
  let cur = firstISO;
  let guard = 0;
  while (cur && cur <= seasonEndISO && guard++ < 200) {
    out.push(cur);
    cur = nextCloseAfter(cur, cadence);
  }
  return out;
}
const CAD_PHRASE = { weekly: "Weekly", biweekly: "Every 2 weeks", monthly: "Monthly", season: "One close at the end of the season" };
function RepeatingCloseBlock({ store, onUpdateStore, onToast }) {
  const on = !!store.repeatClose;
  const cad = store.repeatCadence || "monthly";
  const isLeague = store.storeType === "league";
  const end = store.storeOpenEnd;
  const [draft, setDraft] = React.useState({ cad, seasonStart: store.seasonStart || store.storeOpenStart || "", seasonEnd: store.seasonEnd || "" });
  const [showAll, setShowAll] = React.useState(false);
  React.useEffect(() => { setDraft({ cad: store.repeatCadence || "monthly", seasonStart: store.seasonStart || store.storeOpenStart || "", seasonEnd: store.seasonEnd || "" }); }, [store.id, store.repeatCadence, store.seasonEnd, store.seasonStart]);
  const dirty = draft.cad !== cad || draft.seasonEnd !== (store.seasonEnd || "") || draft.seasonStart !== (store.seasonStart || store.storeOpenStart || "");
  const schedule = buildCloseSchedule(end, draft.cad, draft.seasonEnd);
  const todayISO = new Date().toISOString().slice(0, 10);
  const upcoming = schedule.filter((d) => d >= todayISO);
  const nextClose = upcoming[0] || schedule[0] || null;
  const badRange = !!draft.seasonEnd && !!end && draft.seasonEnd < end;
  const canSave = !!draft.seasonEnd && !badRange && schedule.length > 0;
  const save = () => {
    if (!canSave) return;
    onUpdateStore && onUpdateStore({ repeatCadence: draft.cad, seasonStart: draft.seasonStart, seasonEnd: draft.seasonEnd });
    onToast && onToast(schedule.length === 1 ? "Schedule saved — 1 close through " + formatStoreDate(draft.seasonEnd) : "Schedule saved — " + schedule.length + " closes through " + formatStoreDate(draft.seasonEnd));
  };
  const dateField = (label, key, min) => (
    <label style={{ display: "block", flex: "1 1 170px", minWidth: 0 }}>
      <span style={{ display: "block", fontSize: 11, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", marginBottom: 5 }}>{label}</span>
      <input type="date" value={draft[key] || ""} min={min || undefined}
        onChange={(e) => { setDraft((d) => ({ ...d, [key]: e.target.value })); setShowAll(false); }}
        style={{ display: "block", width: "100%", boxSizing: "border-box", height: 38, border: "1px solid " + (key === "seasonEnd" && (!draft.seasonEnd || badRange) ? "#e8b4b0" : "#d4d4d8"), borderRadius: 7, padding: "0 10px", fontSize: 13, fontFamily: "inherit", color: "#0e0e10", background: "#fff", outline: "none" }} />
    </label>
  );
  const visible = showAll ? schedule : schedule.slice(0, 6);
  return (
    <div style={{ marginTop: 18, paddingTop: 16, borderTop: "1px solid #ececf0" }}>
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 16 }}>
        <div style={{ minWidth: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}>
            <span style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10" }}>REPEATING CLOSE</span>
            {isLeague && <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#2563eb", background: "#eef3fb", borderRadius: 999, padding: "3px 8px" }}>SUB-OPTION OF THE LEAGUE WINDOW</span>}
          </div>
          <p style={{ margin: 0, fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, maxWidth: 520 }}>
            {isLeague
              ? "The league sets one window; repetition is configured within it. When the window closes, it re-opens on the cadence below and every team follows the same close."
              : "Instead of one fixed close, the store closes and re-opens on a recurring cadence. Orders batch per cycle."}
          </p>
        </div>
        <SettingsToggle value={on} onChange={(v) => { onUpdateStore && onUpdateStore({ repeatClose: v }); onToast && onToast(v ? "Repeating close enabled" : "Repeating close turned off"); }} disabled={!end} />
      </div>
      {!end && <div style={{ marginTop: 10, fontSize: 12, color: "#9a9aa1" }}>Set a closing date on the window first — always-open stores have nothing to repeat.</div>}
      {on && end && (
        <div style={{ marginTop: 14 }}>
          <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", marginBottom: 6 }}>CLOSE CADENCE</div>
          <div style={{ display: "inline-flex", background: "#f1f1f3", borderRadius: 8, padding: 4, marginBottom: 14 }}>
            {REPEAT_CADENCES.map((c) => {
              const sel = draft.cad === c.key;
              return <button key={c.key} type="button" onClick={() => { setDraft((d) => ({ ...d, cad: c.key })); setShowAll(false); }} style={{ padding: "7px 14px", border: 0, borderRadius: 6, cursor: "pointer", fontSize: 12, fontWeight: 700, background: sel ? "#fff" : "transparent", color: sel ? "#0e0e10" : "#6b6b72", boxShadow: sel ? "0 1px 3px rgba(20,20,30,.12)" : "none", fontFamily: "inherit" }}>{c.label}</button>;
            })}
          </div>

          {/* Season date range — the horizon every cadence resolves against. */}
          <div style={{ background: "#fff", border: "1px solid " + (draft.seasonEnd && !badRange ? "#e4e4e7" : "#f0d4d1"), borderRadius: 10, padding: "14px 16px" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 4 }}>
              <IconCalendar size={14} style={{ color: "#6b6b72" }} />
              <span style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".05em", color: "#0e0e10" }}>SEASON DATE RANGE</span>
              {draft.cad === "season" && <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#b45309", background: "#fff7e8", border: "1px solid #f0dcb4", borderRadius: 999, padding: "3px 8px" }}>REQUIRED FOR END OF SEASON</span>}
            </div>
            <p style={{ margin: "0 0 12px", fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, maxWidth: 560 }}>
              {draft.cad === "season"
                ? "\u201cEnd of season\u201d is the date you set here \u2014 the store closes once, on the season end."
                : "Repetition stops at the season end. Set it so the cadence resolves to a finite list of close dates."}
            </p>
            <div style={{ display: "flex", gap: 12, flexWrap: "wrap", alignItems: "flex-end" }}>
              {dateField("SEASON START", "seasonStart")}
              {dateField("SEASON END", "seasonEnd", draft.seasonStart || end)}
            </div>
            {!draft.seasonEnd && <div style={{ marginTop: 10, display: "flex", gap: 8, alignItems: "flex-start", fontSize: 12.5, color: "#b81d14", lineHeight: 1.5 }}><IconAlert size={14} style={{ flexShrink: 0, marginTop: 1 }} /><span>No season end set — the cadence can’t be saved and no closes are scheduled.</span></div>}
            {badRange && <div style={{ marginTop: 10, display: "flex", gap: 8, alignItems: "flex-start", fontSize: 12.5, color: "#b81d14", lineHeight: 1.5 }}><IconAlert size={14} style={{ flexShrink: 0, marginTop: 1 }} /><span>The season ends before the window’s first close ({formatStoreDate(end)}). Move the season end later.</span></div>}
          </div>

          {/* Resolved schedule — plain language + every date the cadence produces. */}
          <div style={{ marginTop: 12, background: "#f7f7f8", border: "1px solid #ececf0", borderRadius: 10, padding: "14px 16px" }}>
            {canSave ? (
              <React.Fragment>
                <div style={{ display: "flex", alignItems: "flex-start", gap: 14, flexWrap: "wrap" }}>
                  <div style={{ minWidth: 200 }}>
                    <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", marginBottom: 3 }}>NEXT CLOSE</div>
                    <div style={{ fontSize: 19, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif", letterSpacing: "-.01em" }}>{nextClose ? formatStoreDate(nextClose) : "\u2014"}</div>
                  </div>
                  <div style={{ minWidth: 160 }}>
                    <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", marginBottom: 3 }}>REMAINING THIS SEASON</div>
                    <div style={{ fontSize: 19, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>{upcoming.length} <span style={{ fontSize: 13, fontWeight: 600, color: "#6b6b72" }}>of {schedule.length} {schedule.length === 1 ? "close" : "closes"}</span></div>
                  </div>
                </div>
                <div style={{ marginTop: 10, fontSize: 12.5, color: "#3f3f46", lineHeight: 1.55 }}>
                  {draft.cad === "season"
                    ? <span><strong style={{ color: "#0e0e10" }}>End of season</strong> resolves to <strong style={{ color: "#0e0e10" }}>{formatStoreDate(draft.seasonEnd)}</strong> — one close, then the store stays closed.</span>
                    : <span><strong style={{ color: "#0e0e10" }}>{CAD_PHRASE[draft.cad]}</strong> from {formatStoreDate(end)} through the season end on <strong style={{ color: "#0e0e10" }}>{formatStoreDate(draft.seasonEnd)}</strong> — {schedule.length} {schedule.length === 1 ? "close" : "closes"}, re-opening after each one.</span>}
                </div>
                <div style={{ marginTop: 12, paddingTop: 12, borderTop: "1px solid #e6e6ea" }}>
                  <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", marginBottom: 8 }}>ALL CLOSE DATES</div>
                  <div style={{ display: "flex", flexWrap: "wrap", gap: 8 }}>
                    {visible.map((d, i) => {
                      const isNext = d === nextClose;
                      const past = d < todayISO;
                      return (
                        <span key={d + i} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: isNext ? "#0e0e10" : "#fff", border: "1px solid " + (isNext ? "#0e0e10" : "#e4e4e7"), color: isNext ? "#fff" : past ? "#a1a1aa" : "#3f3f46", borderRadius: 999, padding: "6px 12px", fontSize: 12, fontWeight: 600, textDecoration: past ? "line-through" : "none" }}>
                          <span style={{ fontSize: 10, fontWeight: 800, opacity: .6 }}>{i + 1}</span>{formatStoreDate(d)}
                        </span>
                      );
                    })}
                    {schedule.length > 6 && (
                      <button type="button" onClick={() => setShowAll(!showAll)} style={{ background: "none", border: 0, color: "#e1251b", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", padding: "6px 4px" }}>{showAll ? "Show fewer" : "Show all " + schedule.length}</button>
                    )}
                  </div>
                </div>
              </React.Fragment>
            ) : (
              <div style={{ display: "flex", alignItems: "center", gap: 10, fontSize: 12.5, color: "#6b6b72" }}>
                <IconClock size={15} style={{ flexShrink: 0, color: "#9a9aa1" }} />
                <span>No close dates yet. Set a season end above and the full schedule appears here before you save.</span>
              </div>
            )}
          </div>

          <div style={{ marginTop: 12, display: "flex", alignItems: "center", gap: 12 }}>
            <Button variant="primary" disabled={!canSave || !dirty} onClick={save}>Save schedule</Button>
            {dirty && <button type="button" onClick={() => setDraft({ cad, seasonStart: store.seasonStart || store.storeOpenStart || "", seasonEnd: store.seasonEnd || "" })} style={{ background: "none", border: 0, fontSize: 12.5, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit" }}>Discard changes</button>}
            <span style={{ fontSize: 12, color: dirty ? "#b45309" : "#9a9aa1", marginLeft: "auto" }}>{dirty ? "Unsaved cadence \u2014 the schedule above is a preview." : store.seasonEnd ? "Saved \u00b7 season ends " + formatStoreDate(store.seasonEnd) : "Not scheduled yet."}</span>
          </div>
        </div>
      )}
    </div>
  );
}

// ── 3.3 Product categories & subcategories (workspace defaults) ──
window.__prodCats = window.__prodCats || [
  { id: "team-gear", name: "Team Gear", subs: ["Uniforms", "Practice Gear"] },
  { id: "fan-gear", name: "Fan Gear", subs: ["Apparel", "Accessories"] },
];

// ── 2.2 Team Manager Access (store-level — lives with the other team manager settings) ──
function seedTmInvites(store) {
  window.__tmInvites = window.__tmInvites || [];
  if (!window.__tmInvites.some((i) => i.storeId === store.id)) {
    const nm = store.storeTitle || store.teamName || "Team Store";
    window.__tmInvites.push(
      { id: "tm-" + store.id + "-1", email: "d.hargrove@lincolnathletics.org", storeId: store.id, storeName: nm, invited: "Jun 12, 2026", status: "active", lastLogin: "Jul 28, 2026" },
      { id: "tm-" + store.id + "-2", email: "coach.ramos@westsidewolves.com", storeId: store.id, storeName: nm, invited: "Jul 21, 2026", status: "invited" }
    );
  }
  return window.__tmInvites.filter((i) => i.storeId === store.id);
}

function TMAccessSection({ store }) {
  const [invites, setInvites] = React.useState(() => seedTmInvites(store).slice());
  const sync = (nextForStore) => { window.__tmInvites = (window.__tmInvites || []).filter((i) => i.storeId !== store.id).concat(nextForStore); setInvites(nextForStore); };
  const [email, setEmail] = React.useState("");
  const [toast, setToast] = React.useState(null);
  const [revokeTarget, setRevokeTarget] = React.useState(null);
  React.useEffect(() => { if (!toast) return; const t = setTimeout(() => setToast(null), 2600); return () => clearTimeout(t); }, [toast]);
  const valid = /.+@.+\..+/.test(email.trim());
  const send = () => {
    if (!valid) return;
    sync([{ id: "tm" + Date.now(), email: email.trim(), storeId: store.id, storeName: store.storeTitle || store.teamName || "Team Store", invited: "Jul 30, 2026", status: "invited" }, ...invites]);
    setToast("Invite sent to " + email.trim());
    setEmail("");
  };
  const resend = (inv) => setToast("Invite re-sent to " + inv.email);
  const revoke = () => {
    if (!revokeTarget) return;
    sync(invites.filter((i) => i.id !== revokeTarget.id));
    setToast("Access revoked for " + revokeTarget.email);
    setRevokeTarget(null);
  };
  const grid = "minmax(240px,1.6fr) 120px 170px 210px";
  return (
    <div>
      <h3 style={{ margin: "0 0 4px", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Team Manager Access</h3>
      <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 18px", maxWidth: 640, lineHeight: 1.55 }}>Invite this store's team manager to a slimmed-down portal with exactly two areas — Reporting and Rosters. No products, orders, settings, or vault. Access is scoped to this store only.</p>
      <div style={{ background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, padding: 20, marginBottom: 16 }}>
        <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", marginBottom: 12 }}>SEND AN INVITE</div>
        <div style={{ display: "flex", gap: 10, flexWrap: "wrap" }}>
          <input value={email} onChange={(e) => setEmail(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") send(); }} placeholder="manager@team.org" type="email" style={{ flex: "1 1 240px", height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13.5, fontFamily: "inherit", outline: "none" }} />
          <Button variant="primary" disabled={!valid} onClick={send}>Send Invite</Button>
        </div>
        <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 10 }}>The invitee gets an email link to set a password for the Team Manager Portal.</div>
      </div>
      <div style={{ background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, overflow: "hidden" }}>
        <div style={{ display: "grid", gridTemplateColumns: grid, gap: 16, padding: "12px 20px", borderBottom: "1px solid #e4e4e7", fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: "#a1a1aa", background: "#fafafa" }}>
          <span>Invitee</span><span>Invited</span><span>Status</span><span />
        </div>
        {invites.length === 0 && <div style={{ padding: "40px 20px", textAlign: "center", color: "#9a9aa1", fontSize: 13.5 }}>No team managers invited yet.</div>}
        {invites.map((inv) => (
          <div key={inv.id} style={{ display: "grid", gridTemplateColumns: grid, gap: 16, padding: "13px 20px", borderBottom: "1px solid #f4f4f5", alignItems: "center" }}>
            <span style={{ display: "flex", alignItems: "center", gap: 10, minWidth: 0 }}>
              <span style={{ width: 32, height: 32, borderRadius: "50%", background: inv.status === "active" ? "#0e0e10" : "#f1f1f3", color: inv.status === "active" ? "#fff" : "#6b6b72", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 12.5, flexShrink: 0 }}>{inv.email[0].toUpperCase()}</span>
              <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{inv.email}</span>
            </span>
            <span style={{ fontSize: 12.5, color: "#52525b" }}>{inv.invited}</span>
            <span>
              {inv.status === "active"
                ? <span style={{ display: "inline-flex", flexDirection: "column", gap: 2 }}>
                    <span style={{ display: "inline-flex", alignItems: "center", gap: 6, fontSize: 11.5, fontWeight: 700, color: "#1f9d55" }}><span style={{ width: 7, height: 7, borderRadius: 999, background: "#1f9d55" }} />Active</span>
                    {inv.lastLogin && <span style={{ fontSize: 10.5, color: "#9a9aa1" }}>Last login {inv.lastLogin}</span>}
                  </span>
                : <span style={{ display: "inline-flex", alignItems: "center", gap: 6, fontSize: 11.5, fontWeight: 700, color: "#b45309" }}><span style={{ width: 7, height: 7, borderRadius: 999, background: "#d97706" }} />Invited · not logged in</span>}
            </span>
            <span style={{ display: "flex", justifyContent: "flex-end", gap: 8 }}>
              {inv.status === "active"
                ? <button type="button" onClick={() => window.__openTmPortal && window.__openTmPortal(inv, store)} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "7px 12px", fontSize: 11.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" }}>View portal as</button>
                : <button type="button" onClick={() => resend(inv)} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "7px 12px", fontSize: 11.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Resend</button>}
              <button type="button" onClick={() => setRevokeTarget(inv)} style={{ background: "#fff", border: "1px solid #f3c9c5", borderRadius: 6, padding: "7px 12px", fontSize: 11.5, fontWeight: 700, color: "#b81d14", cursor: "pointer", fontFamily: "inherit" }}>Revoke</button>
            </span>
          </div>
        ))}
      </div>
      <Modal open={!!revokeTarget} onClose={() => setRevokeTarget(null)} width={440}>
        <div style={{ padding: "28px 28px 8px" }}>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800, fontFamily: "Archivo, sans-serif" }}>Revoke access?</h2>
          <p style={{ margin: "12px 0 0", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>
            {revokeTarget && (revokeTarget.email + " will immediately lose access to Reporting and Rosters for this store. You can re-invite them at any time.")}
          </p>
        </div>
        <div style={{ padding: "20px 28px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
          <Button variant="secondary" onClick={() => setRevokeTarget(null)}>Cancel</Button>
          <Button variant="primary" onClick={revoke}>Revoke Access</Button>
        </div>
      </Modal>
      {toast && <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: "#0e0e10", color: "#fff", padding: "12px 20px", borderRadius: 8, fontSize: 12.5, boxShadow: "0 12px 32px rgba(9,9,11,.25)", zIndex: 700 }}>{toast}</div>}
    </div>
  );
}

// ── 3.1 Emails — inventory from the Confluence "System Emails" spec ──
const SYSTEM_EMAILS = [
  { group: "Reps & Team Managers", note: "Sent to the store-owning rep and the team manager on record. With no manager on file, only the rep receives it.", rows: [
    { id: "status-changed", name: "Store Status Changed", purpose: "Any store status transition — one email per change.", subject: "{{store}} is now {{newStatus}}", body: "Hi {{firstName}},\n\n{{store}} changed from {{prevStatus}} to {{newStatus}} on {{timestamp}}, triggered by {{actor}}.\n\nArchived stores can't be restored without support.\n\nOpen the store workspace: {{workspaceLink}}" },
    { id: "buyer-created", name: "New Buyer Account Created", purpose: "A consumer creates a storefront account for the first time.", subject: "New buyer on {{store}}: {{buyerName}}", body: "Hi {{firstName}},\n\n{{buyerName}} ({{buyerEmail}}) created an account on {{store}} as {{role}} at {{timestamp}}.\n\nView roster & orders: {{storeLink}}" },
    { id: "order-placed", name: "New Storefront Order Placed", purpose: "Any consumer completes checkout and an order is created.", subject: "New order #{{orderNo}} on {{store}} — {{total}}", body: "Hi {{firstName}},\n\n{{buyerName}} placed order #{{orderNo}} on {{store}}.\n\n{{itemList}}\n\nOrder total: {{total}}\n\nView the order: {{orderLink}}" },
    { id: "rostered-created", name: "Rostered Player Account Created", purpose: "Account creation matched to a roster entry — replaces the general buyer email.", subject: "Rostered player joined {{store}}: {{playerName}}", body: "Hi {{firstName}},\n\n{{playerName}} ({{playerEmail}}) created an account and matched roster entry {{rosterEntry}} on {{store}} at {{timestamp}}." },
    { id: "rostered-gear", name: "Rostered Player Ordered Team Gear", purpose: "A rostered player orders Team Required or Team Supplied gear.", subject: "{{playerName}} ordered team gear — #{{orderNo}}", body: "Hi {{firstName}},\n\n{{playerName}} ({{rosterEntry}}) placed order #{{orderNo}} containing team gear.\n\n{{itemList}}\n\nOrder total: {{total}}\n\nReview pending orders: {{pendingLink}}" },
  ]},
  { group: "Reps only", note: "Sent exclusively to the store-owning rep.", rows: [
    { id: "cancel-request", name: "Consumer Cancellation Request", purpose: "A buyer requests a cancellation — the rep must action it manually.", subject: "Cancellation requested on order #{{orderNo}}", body: "Hi {{firstName}},\n\n{{buyerName}} requested cancellation of order #{{orderNo}} ({{total}}).\nReason: {{reason}}\n\nThis order has NOT been cancelled — review and action the request: {{orderLink}}" },
    { id: "moq-ready", name: "Pending Batch Ready (MOQ Reached)", purpose: "Pending orders reach the configured MOQ threshold.", subject: "{{store}} hit MOQ — {{pendingCount}} pending orders ready", body: "Hi {{firstName}},\n\nPending orders on {{store}} reached your MOQ ({{pendingCount}} of {{moq}}), worth {{pendingValue}}.\n\n{{productBreakdown}}\n\nProcess the batch: {{pendingLink}}" },
  ]},
  { group: "Consumers", note: "Sent to the buyer whose account or order triggered the event. All include an unsubscribe/preference link.", rows: [
    { id: "order-confirm", name: "Order Confirmation", purpose: "Checkout complete — sent within 2 minutes.", subject: "Your {{store}} order #{{orderNo}} is confirmed", body: "Hi {{buyerFirst}},\n\nThanks for your order from {{store}}!\n\n{{itemizedList}}\n\nTotal: {{total}}\nEstimated timeline: {{timeline}}\n\nView your order: {{accountLink}}" },
    { id: "order-status", name: "Order Status Update", purpose: "One email per order status transition (e.g. Shipped).", subject: "Order #{{orderNo}} update: {{newStatus}}", body: "Hi {{buyerFirst}},\n\nYour {{store}} order #{{orderNo}} is now {{newStatus}}.\n{{statusDetail}}\n\nTrack it here: {{orderLink}}" },
    { id: "store-closing", name: "Store Closing Notice", purpose: "Store transitions to Closed — sent to all account holders.", subject: "{{store}} has closed", body: "Hi {{buyerFirst}},\n\n{{store}} closed on {{closeDate}}. {{closureMessage}}\n\nOutstanding orders: {{outstandingStatus}}\n\nQuestions? Contact {{repContact}}." },
    { id: "window-closed", name: "Ordering Window Closed", purpose: "A pop-up window auto-closes — sent to account holders who haven't ordered.", subject: "Ordering has closed for {{store}}", body: "Hi {{buyerFirst}},\n\nThe ordering window for {{store}} closed on {{closeDate}} and new orders are no longer available.\n\nQuestions? Contact {{repContact}}." },
    { id: "window-closing", name: "Ordering Window Closing Soon", purpose: "48-hour advance notice to account holders who haven't ordered yet.", subject: "Last chance — {{store}} closes {{closeDateTime}}", body: "Hi {{buyerFirst}},\n\nThe ordering window for {{store}} closes {{closeDateTime}} — don't miss your gear.\n\nShop now: {{storefrontLink}}" },
    { id: "order-error", name: "Order Error", purpose: "A processing error needs buyer action — plain-language, never generic.", subject: "Action needed on your {{store}} order #{{orderNo}}", body: "Hi {{buyerFirst}},\n\nThere was a problem with order #{{orderNo}}: {{plainErrorDescription}}\n\nWhat to do next: {{nextSteps}}\n\nNeed help? Contact {{repContact}}." },
    { id: "cancel-confirm", name: "Cancellation Request Confirmation", purpose: "Confirms receipt of a cancellation request — not the cancellation itself.", subject: "We received your cancellation request for #{{orderNo}}", body: "Hi {{buyerFirst}},\n\nWe received your cancellation request for order #{{orderNo}}. Cancellation is NOT guaranteed — it's subject to review, usually within {{responseTimeframe}}.\n\nQuestions? Contact {{repContact}}." },
  ]},
];

function EmailsSection() {
  const [state, setState] = React.useState(() => {
    if (!window.__sysEmailState) {
      window.__sysEmailState = {};
      SYSTEM_EMAILS.forEach((g) => g.rows.forEach((r) => { window.__sysEmailState[r.id] = { on: true, subject: r.subject, body: r.body }; }));
    }
    return { ...window.__sysEmailState };
  });
  const patch = (id, p) => setState((s) => { const next = { ...s, [id]: { ...s[id], ...p } }; window.__sysEmailState = next; return next; });
  const allIds = React.useMemo(() => SYSTEM_EMAILS.flatMap((g) => g.rows.map((r) => r.id)), []);
  const enabledCount = allIds.filter((id) => state[id] && state[id].on).length;
  const allOn = enabledCount === allIds.length;
  const setAll = (v) => {
    setState((s) => { const next = { ...s }; allIds.forEach((id) => { next[id] = { ...next[id], on: v }; }); window.__sysEmailState = next; return next; });
    setToast(v ? "All emails enabled" : "All emails disabled — nothing will send");
  };
  const [open, setOpen] = React.useState(null);
  const [toast, setToast] = React.useState(null);
  React.useEffect(() => { if (!toast) return; const t = setTimeout(() => setToast(null), 2400); return () => clearTimeout(t); }, [toast]);
  return (
    <div>
      <h3 style={{ margin: "0 0 4px", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Emails</h3>
      <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 18px", maxWidth: 640, lineHeight: 1.55 }}>Workspace defaults for every transactional email the platform sends. Individual stores can override a template from that store's Settings. All emails are trigger-based — no schedules, no duplicates per event.</p>
      <div style={{ display: "flex", alignItems: "center", gap: 16, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, padding: "14px 20px", marginBottom: 24 }}>
        <span style={{ flex: 1, minWidth: 0 }}>
          <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>All Emails</span>
          <span style={{ display: "block", fontSize: 12, color: "#9a9aa1", marginTop: 2 }}>
            {allOn ? "Every system email is active." : enabledCount === 0 ? "All system emails are paused — the platform will send nothing." : enabledCount + " of " + allIds.length + " emails active."}
          </span>
        </span>
        {enabledCount === 0 && <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#b81d14", background: "#fdecea", borderRadius: 999, padding: "3px 9px", flexShrink: 0 }}>ALL PAUSED</span>}
        <span style={{ flexShrink: 0, display: "inline-flex" }}>
          <SettingsToggle value={enabledCount > 0} onChange={(v) => setAll(v)} />
        </span>
      </div>
      {SYSTEM_EMAILS.map((g) => (
        <div key={g.group} style={{ marginBottom: 24 }}>
          <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".08em", color: "#0e0e10", textTransform: "uppercase", marginBottom: 4 }}>{g.group}</div>
          <div style={{ fontSize: 12, color: "#9a9aa1", marginBottom: 10 }}>{g.note}</div>
          <div style={{ background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, overflow: "hidden" }}>
            {g.rows.map((r, ri) => {
              const st = state[r.id];
              const isOpen = open === r.id;
              return (
                <div key={r.id} style={{ borderTop: ri === 0 ? "none" : "1px solid #f4f4f5" }}>
                  <div style={{ display: "flex", alignItems: "center", gap: 14, padding: "13px 20px", cursor: "pointer", background: isOpen ? "#fafafa" : "#fff" }} onClick={() => setOpen(isOpen ? null : r.id)}>
                    <span style={{ flex: 1, minWidth: 0 }}>
                      <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: st.on ? "#0e0e10" : "#9a9aa1" }}>{r.name}</span>
                      <span style={{ display: "block", fontSize: 12, color: "#9a9aa1", marginTop: 2 }}>{r.purpose}</span>
                    </span>
                    {!st.on && <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#f1f1f3", borderRadius: 999, padding: "3px 9px", flexShrink: 0 }}>DISABLED</span>}
                    <span onClick={(e) => e.stopPropagation()} style={{ flexShrink: 0, display: "inline-flex" }}>
                      <SettingsToggle value={st.on} onChange={(v) => { patch(r.id, { on: v }); setToast(r.name + (v ? " enabled" : " disabled")); }} />
                    </span>
                    <span style={{ flexShrink: 0, color: "#9a9aa1", transform: isOpen ? "rotate(180deg)" : "none", transition: "transform .15s", display: "inline-flex" }}>
                      <svg width="14" height="14" viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M3 6l5 5 5-5" /></svg>
                    </span>
                  </div>
                  {isOpen && (
                    <div style={{ padding: "4px 20px 18px", background: "#fafafa", borderTop: "1px solid #f4f4f5" }}>
                      <label style={{ display: "block", fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: "#a1a1aa", margin: "12px 0 6px" }}>Subject</label>
                      <input value={st.subject} onChange={(e) => patch(r.id, { subject: e.target.value })} style={{ width: "100%", boxSizing: "border-box", height: 38, border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13, fontFamily: "'JetBrains Mono', ui-monospace, monospace", outline: "none", background: "#fff" }} />
                      <label style={{ display: "block", fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: "#a1a1aa", margin: "12px 0 6px" }}>Body</label>
                      <textarea value={st.body} onChange={(e) => patch(r.id, { body: e.target.value })} rows={Math.min(10, st.body.split("\n").length + 1)} style={{ width: "100%", boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "10px 12px", fontSize: 12.5, lineHeight: 1.6, fontFamily: "'JetBrains Mono', ui-monospace, monospace", outline: "none", resize: "vertical", background: "#fff" }} />
                      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: 10 }}>
                        <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{"{{placeholders}}"} are filled per event at send time.</span>
                        <span style={{ display: "flex", gap: 8 }}>
                          <button type="button" onClick={() => patch(r.id, { subject: r.subject, body: r.body })} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "8px 14px", fontSize: 12, fontWeight: 700, color: "#3f3f46", cursor: "pointer", fontFamily: "inherit" }}>Reset to default</button>
                          <button type="button" onClick={() => { setToast(r.name + " template saved"); setOpen(null); }} style={{ background: "#0e0e10", border: 0, borderRadius: 6, padding: "8px 16px", fontSize: 12, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>Save template</button>
                        </span>
                      </div>
                    </div>
                  )}
                </div>
              );
            })}
          </div>
        </div>
      ))}
      {toast && <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: "#0e0e10", color: "#fff", padding: "12px 20px", borderRadius: 8, fontSize: 12.5, boxShadow: "0 12px 32px rgba(9,9,11,.25)", zIndex: 700 }}>{toast}</div>}
    </div>
  );
}

function CategoriesSection({ stores }) {
  const [cats, setCats] = React.useState(() => window.__prodCats.map((c) => ({ ...c, subs: c.subs.slice() })));
  const sync = (next) => { window.__prodCats = next; setCats(next); };
  const [newCat, setNewCat] = React.useState("");
  const [newSub, setNewSub] = React.useState({});
  const [deleteTarget, setDeleteTarget] = React.useState(null);
  const [reassignTo, setReassignTo] = React.useState("");
  const [toast, setToast] = React.useState(null);
  React.useEffect(() => { if (!toast) return; const t = setTimeout(() => setToast(null), 2600); return () => clearTimeout(t); }, [toast]);
  const productCount = (catName) => {
    let n = 0;
    (stores || []).forEach((s) => (s.products || []).forEach((p) => { if (p.storeCategory === catName) n++; }));
    return n;
  };
  const addCat = () => {
    const name = newCat.trim();
    if (!name || cats.some((c) => c.name.toLowerCase() === name.toLowerCase())) return;
    sync([...cats, { id: name.toLowerCase().replace(/[^a-z0-9]+/g, "-"), name, subs: [] }]);
    setNewCat(""); setToast("Category \u201c" + name + "\u201d created");
  };
  const addSub = (cid) => {
    const name = (newSub[cid] || "").trim();
    if (!name) return;
    sync(cats.map((c) => c.id === cid ? (c.subs.some((x) => x.toLowerCase() === name.toLowerCase()) ? c : { ...c, subs: [...c.subs, name] }) : c));
    setNewSub((s) => ({ ...s, [cid]: "" }));
  };
  const removeSub = (cid, sub) => sync(cats.map((c) => c.id === cid ? { ...c, subs: c.subs.filter((x) => x !== sub) } : c));
  const confirmDelete = () => {
    if (!deleteTarget) return;
    const n = productCount(deleteTarget.name);
    sync(cats.filter((c) => c.id !== deleteTarget.id));
    setToast(n > 0
      ? ("\u201c" + deleteTarget.name + "\u201d deleted — " + n + " product" + (n === 1 ? "" : "s") + " reassigned to \u201c" + (reassignTo || "Uncategorized") + "\u201d")
      : ("\u201c" + deleteTarget.name + "\u201d deleted"));
    setDeleteTarget(null); setReassignTo("");
  };
  return (
    <div>
      <h3 style={{ margin: "0 0 4px", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Product Categories</h3>
      <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 18px", maxWidth: 640, lineHeight: 1.55 }}>Workspace defaults applied to every store — stores can override. Categories and subcategories appear as dropdowns when editing a store product; Vault items are not categorized here.</p>
      <div style={{ display: "flex", gap: 10, marginBottom: 16 }}>
        <input value={newCat} onChange={(e) => setNewCat(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") addCat(); }} placeholder="New category name" style={{ flex: "0 1 280px", height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13.5, fontFamily: "inherit", outline: "none" }} />
        <Button variant="secondary" disabled={!newCat.trim()} onClick={addCat}>Add Category</Button>
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16 }}>
        {cats.map((c) => {
          const n = productCount(c.name);
          return (
            <div key={c.id} style={{ background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, padding: 18 }}>
              <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, marginBottom: 12 }}>
                <span style={{ display: "flex", alignItems: "center", gap: 10, minWidth: 0 }}>
                  <span style={{ fontSize: 14.5, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>{c.name}</span>
                  <span style={{ fontSize: 11, fontWeight: 700, color: "#6b6b72", background: "#f1f1f3", borderRadius: 999, padding: "3px 9px", whiteSpace: "nowrap" }}>{n} product{n === 1 ? "" : "s"}</span>
                </span>
                <button type="button" title="Delete category" onClick={() => { setDeleteTarget(c); setReassignTo((cats.find((x) => x.id !== c.id) || {}).name || ""); }} style={{ width: 30, height: 30, borderRadius: 6, border: "1px solid #ececf0", background: "#fff", color: "#9a9aa1", cursor: "pointer", display: "inline-flex", alignItems: "center", justifyContent: "center", flexShrink: 0 }}><IconTrash size={14} /></button>
              </div>
              <div style={{ fontSize: 10.5, fontWeight: 700, letterSpacing: ".8px", textTransform: "uppercase", color: "#a1a1aa", marginBottom: 8 }}>Subcategories</div>
              <div style={{ display: "flex", flexWrap: "wrap", gap: 8, marginBottom: 12 }}>
                {c.subs.length === 0 && <span style={{ fontSize: 12, color: "#9a9aa1" }}>None yet</span>}
                {c.subs.map((s) => (
                  <span key={s} style={{ display: "inline-flex", alignItems: "center", gap: 6, fontSize: 12, fontWeight: 600, color: "#3f3f46", background: "#f4f4f5", borderRadius: 999, padding: "5px 6px 5px 12px" }}>
                    {s}
                    <button type="button" onClick={() => removeSub(c.id, s)} title={"Remove " + s} style={{ width: 18, height: 18, borderRadius: 999, border: 0, background: "#e4e4e7", color: "#52525b", cursor: "pointer", fontSize: 11, lineHeight: 1, display: "inline-flex", alignItems: "center", justifyContent: "center", padding: 0 }}>×</button>
                  </span>
                ))}
              </div>
              <div style={{ display: "flex", gap: 8 }}>
                <input value={newSub[c.id] || ""} onChange={(e) => setNewSub((s) => ({ ...s, [c.id]: e.target.value }))} onKeyDown={(e) => { if (e.key === "Enter") addSub(c.id); }} placeholder="Add subcategory" style={{ flex: 1, height: 34, boxSizing: "border-box", border: "1px solid #e4e4e7", borderRadius: 6, padding: "0 10px", fontSize: 12.5, fontFamily: "inherit", outline: "none" }} />
                <button type="button" onClick={() => addSub(c.id)} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 14px", fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Add</button>
              </div>
            </div>
          );
        })}
      </div>
      <Modal open={!!deleteTarget} onClose={() => setDeleteTarget(null)} width={480}>
        <div style={{ padding: "28px 28px 8px" }}>
          <h2 style={{ margin: 0, fontSize: 18, fontWeight: 800, fontFamily: "Archivo, sans-serif" }}>Delete “{deleteTarget && deleteTarget.name}”?</h2>
          {deleteTarget && productCount(deleteTarget.name) > 0 ? (
            <React.Fragment>
              <p style={{ margin: "12px 0 0", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>
                {productCount(deleteTarget.name)} product{productCount(deleteTarget.name) === 1 ? " uses" : "s use"} this category. Choose where they go:
              </p>
              <select value={reassignTo} onChange={(e) => setReassignTo(e.target.value)} style={{ marginTop: 14, width: "100%", height: 40, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 10px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff" }}>
                {cats.filter((c) => c.id !== deleteTarget.id).map((c) => <option key={c.id} value={c.name}>Reassign to {c.name}</option>)}
                <option value="">Leave uncategorized</option>
              </select>
            </React.Fragment>
          ) : (
            <p style={{ margin: "12px 0 0", fontSize: 13.5, color: "#6b6b72", lineHeight: 1.6 }}>No products use this category{deleteTarget && deleteTarget.subs.length > 0 ? " — its " + deleteTarget.subs.length + " subcategor" + (deleteTarget.subs.length === 1 ? "y" : "ies") + " will be removed with it" : ""}. This can't be undone.</p>
          )}
        </div>
        <div style={{ padding: "20px 28px", display: "flex", justifyContent: "flex-end", gap: 12 }}>
          <Button variant="secondary" onClick={() => setDeleteTarget(null)}>Cancel</Button>
          <Button variant="primary" onClick={confirmDelete}>Delete Category</Button>
        </div>
      </Modal>
      {toast && <div style={{ position: "fixed", bottom: 24, left: "50%", transform: "translateX(-50%)", background: "#0e0e10", color: "#fff", padding: "12px 20px", borderRadius: 8, fontSize: 12.5, boxShadow: "0 12px 32px rgba(9,9,11,.25)", zIndex: 700 }}>{toast}</div>}
    </div>
  );
}

// ── Workspace Settings screen (workspace-level, one step above stores) ──
function WorkspaceSettings({ stores }) {
  const [section, setSection] = React.useState("emails");
  const items = [
    { id: "emails", label: "Emails", desc: "Transactional email templates" },
    { id: "categories", label: "Product Categories", desc: "Categories & subcategories" },
  ];
  return (
    <div style={{ flex: 1, minWidth: 0 }}>
      <div style={{ maxWidth: 1440, margin: "0 auto", padding: "28px 32px 120px" }}>
        <div style={{ marginBottom: 22 }}>
          <h2 data-screen-label="Workspace Settings" style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.3px", margin: "0 0 8px", fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Workspace Settings</h2>
          <div style={{ fontSize: 14, color: "#71717a" }}>Applies across every store on your workspace. Store-specific settings live in each store's Settings tab.</div>
        </div>
        <div style={{ display: "flex", gap: 36, alignItems: "flex-start" }}>
          <SectionSideNav items={items} value={section} onChange={setSection} />
          <div style={{ flex: 1, minWidth: 0, maxWidth: 980 }}>
            {section === "emails" && <EmailsSection />}
            {section === "categories" && <CategoriesSection stores={stores} />}
          </div>
        </div>
      </div>
    </div>
  );
}

// ── 2.1 Team Manager Portal — slimmed workspace for invited team managers ──
function TeamManagerPortal({ invite, store, onExit }) {
  const [tab, setTab] = React.useState("payouts");
  const [org, setOrg] = React.useState(() => makeRosterOrg(store, false));
  const NAV = [{ id: "payouts", label: "REPORTING" }, { id: "roster", label: "ROSTERS" }];
  // 1 — the manager reads the SAME Store Close Report as the rep. Role-tagging the
  // store keeps the seen-flag separate (a manager's dismissal is not the rep's) and
  // keeps owner-only lifecycle actions out; the report itself is at full parity.
  const mstore = React.useMemo(() => ({ ...store, role: "manager" }), [store]);
  return (
    <div style={{ position: "fixed", inset: 0, zIndex: 900, background: "#fbfbfc", overflowY: "auto", fontFamily: '"Inter",system-ui,sans-serif', minWidth: 1100 }}>
      {/* Rep-preview strip — this is the in-file view switch, not part of the manager's UI */}
      <div style={{ background: "#fff9ec", borderBottom: "1px solid #f0dcb4", padding: "9px 24px", display: "flex", alignItems: "center", gap: 10, fontSize: 12.5, color: "#8a5a0a" }}>
        <span style={{ fontWeight: 700 }}>Rep preview</span>
        <span>You're viewing the portal as {invite.email}. Team managers see only Reporting and Rosters for {store.storeTitle || store.teamName}.</span>
        <button type="button" onClick={onExit} style={{ marginLeft: "auto", background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "7px 14px", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" }}>Exit preview</button>
      </div>
      {/* Portal shell — visibly reduced workspace: two nav items, no create/catalog affordances */}
      <header style={{ position: "sticky", top: 0, zIndex: 50, height: 76, background: "#fff", borderBottom: "1px solid #e8e8ec", display: "flex", alignItems: "center", gap: 28, padding: "0 32px" }}>
        <span style={{ display: "inline-flex", alignItems: "center", gap: 14, color: "#0e0e10" }}>
          <BrandMark variant="ink" height={30} />
          <span style={{ width: 1, height: 24, background: "#e4e4e7" }} />
          <span style={{ fontSize: 14, fontWeight: 800, letterSpacing: ".14em", whiteSpace: "nowrap" }}>TEAM MANAGER PORTAL</span>
        </span>
        <nav style={{ display: "flex", gap: 4, marginLeft: 16 }}>
          {NAV.map((n) => {
            const on = tab === n.id;
            return <button key={n.id} type="button" onClick={() => setTab(n.id)} style={{ background: "transparent", border: 0, borderRadius: 0, padding: "26px 16px", fontSize: 12.5, fontWeight: 800, letterSpacing: ".1em", color: on ? "#e1251b" : "#3f3f46", cursor: "pointer", fontFamily: "inherit", borderBottom: on ? "3px solid #e1251b" : "3px solid transparent" }}>{n.label}</button>;
          })}
        </nav>
        <span style={{ marginLeft: "auto", display: "inline-flex", alignItems: "center", gap: 10 }}>
          <span style={{ width: 32, height: 32, borderRadius: "50%", background: "#0e0e10", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: 13 }}>{invite.email[0].toUpperCase()}</span>
          <span style={{ display: "inline-flex", flexDirection: "column" }}>
            <span style={{ fontSize: 12.5, fontWeight: 700, color: "#0e0e10" }}>{invite.email}</span>
            <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".08em", color: "#1f9d55" }}>SIGNED IN AS TEAM MANAGER</span>
          </span>
        </span>
      </header>
      <div style={{ maxWidth: 1280, margin: "0 auto", padding: "28px 32px 120px" }}>
        <div style={{ display: "flex", alignItems: "baseline", gap: 12, marginBottom: 20 }}>
          <h1 data-screen-label={"TM Portal — " + (tab === "payouts" ? "Reporting" : "Rosters")} style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, letterSpacing: "-.01em", color: "#09090b", margin: 0 }}>{tab === "payouts" ? "Reporting" : (store.storeType === "league" ? "My Teams" : "My Team")}</h1>
          <span style={{ fontSize: 13, color: "#71717a" }}>{store.storeTitle || store.teamName}</span>
        </div>
        {tab === "payouts" && <PayoutsTab store={mstore} onViewAllOrders={() => {}} onViewPendingOrders={() => {}} />}
        {tab === "roster" && <RosterTab startEmpty={false} store={store} org={org} setOrg={setOrg} managerGated={false} onManageManagers={() => {}} />}
      </div>
      <StoreCloseReportHost store={mstore} />
    </div>
  );
}

Object.assign(window, { SectionSideNav, StoreSettingsNav, WorkspaceSettings, TeamManagerPortal, NumberSelectionCard, RepeatingCloseBlock, SettingsToggle });

/* ===== ASSET 29 : Store Close Report ===== */
// A complete review of a store's Active Order Window, generated per close event.
//   • Summary layer — card-paced hero figures (no auto-advance).
//   • Detail layer  — the operational record: order table, charts, breakdowns.
// Both layers read the SAME numbers the Reports tab shows (revenue, orders,
// pending, AOV, Team Revenue, Fundraising) so nothing can disagree.

const CR_INK = "#0e0e10";
const CR_MUTED = "#6b6b72";
const CR_LINE = "#ececf0";
const CR_GREEN = "#1f9d55";
const CR_RED = "#e1251b";
const CR_NUM = { fontVariantNumeric: "tabular-nums lining-nums" };

function crMoney(n) { return "$" + Math.round(n).toLocaleString("en-US"); }
function crMoney2(n) { return "$" + (Math.round(n * 100) / 100).toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 }); }
function crParseDate(v) {
  if (v instanceof Date) return v;
  const s = String(v || "");
  const m = s.match(/^(\d{4})-(\d{2})-(\d{2})/);
  if (m) return new Date(+m[1], +m[2] - 1, +m[3]);
  const d = new Date(s);
  return isNaN(d.getTime()) ? new Date() : d;
}
function crDate(v) { return crParseDate(v).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" }); }
function crDateShort(v) { return crParseDate(v).toLocaleDateString("en-US", { month: "short", day: "numeric" }); }
function crDateISO(v) { const d = crParseDate(v); const p = (n) => String(n).padStart(2, "0"); return `${d.getFullYear()}-${p(d.getMonth() + 1)}-${p(d.getDate())}`; }
function crDateUS(v) { const d = crParseDate(v); const p = (n) => String(n).padStart(2, "0"); return `${p(d.getMonth() + 1)}/${p(d.getDate())}/${d.getFullYear()}`; }
function crSlug(s) { return String(s || "Store").replace(/[^A-Za-z0-9]+/g, "") || "Store"; }
function crAscii(s) { return String(s).replace(/[\u2010-\u2015\u2212]/g, "-").replace(/[\u2018\u2019]/g, "'").replace(/[\u201C\u201D]/g, '"').replace(/[^\x20-\x7E]/g, " "); }

// Deterministic PRNG so a store's report is identical on every open.
function crSeed(str) { let h = 2166136261; const s = String(str); for (let i = 0; i < s.length; i++) { h ^= s.charCodeAt(i); h = Math.imul(h, 16777619); } return h >>> 0; }
function crRng(seed) { let s = (seed >>> 0) || 1; return () => (s = (Math.imul(s, 1664525) + 1013904223) >>> 0) / 4294967296; }

// A readable accent: team color 1 unless it's too light to sit type on.
function crLum(hex) {
  const h = String(hex || "#000").replace("#", "");
  const v = h.length === 3 ? h.split("").map((c) => c + c).join("") : h;
  const n = parseInt(v, 16); if (isNaN(n)) return 0;
  return (0.2126 * ((n >> 16) & 255) + 0.7152 * ((n >> 8) & 255) + 0.0722 * (n & 255)) / 255;
}
function crAccent(store) {
  const colors = (store?.teamColors || []).filter(Boolean);
  const pick = colors.find((c) => crLum(c) < 0.72);
  return pick || CR_INK;
}

// ─────────── Close-event bookkeeping (per store) + seen state (per user) ───────────
function crEventKey(store) { return "ts_close_event_" + (store?.id || "store"); }
function crReadJSON(key, fallback) { try { const v = JSON.parse(window.localStorage.getItem(key) || "null"); return v == null ? fallback : v; } catch (e) { return fallback; } }
function crWriteJSON(key, val) { try { window.localStorage.setItem(key, JSON.stringify(val)); } catch (e) {} }

// Reconciles the persisted close event with the store's current status.
// Closed → Open → Closed produces a NEW event (the trigger re-arms); prior
// events are retained, never overwritten.
function crSyncEvent(store) {
  if (!store) return null;
  const key = crEventKey(store);
  const prev = crReadJSON(key, null);
  const status = store.status;
  if (status === "closed") {
    if (prev && prev.active) return prev;
    const next = {
      id: (prev?.id || 0) + 1,
      closedAt: new Date().toISOString(),
      openedAt: store.storeOpenStart || null,
      active: true,
      auto: false,
    };
    crWriteJSON(key, next);
    return next;
  }
  if (status === "archived") {
    if (prev) return prev;
    const next = { id: 1, closedAt: store.storeOpenEnd || new Date().toISOString(), openedAt: store.storeOpenStart || null, active: true, auto: true };
    crWriteJSON(key, next);
    return next;
  }
  if (prev && prev.active) { const next = { ...prev, active: false }; crWriteJSON(key, next); return next; }
  return prev;
}
function crUserId(store) { return store?.role === "manager" ? "team-manager" : "rep"; }
function crSeenKey(store) { return "ts_close_report_seen_" + crUserId(store); }
function crIsSeen(store, ev) {
  if (!ev) return true;
  const seen = crReadJSON(crSeenKey(store), {});
  return !!seen[(store?.id || "store") + ":" + ev.id];
}
function crMarkSeen(store, ev) {
  if (!ev) return;
  const k = crSeenKey(store);
  const seen = crReadJSON(k, {});
  seen[(store?.id || "store") + ":" + ev.id] = Date.now();
  crWriteJSON(k, seen);
}
function crClearSeen(store, ev) {
  if (!ev) return;
  const k = crSeenKey(store);
  const seen = crReadJSON(k, {});
  delete seen[(store?.id || "store") + ":" + ev.id];
  crWriteJSON(k, seen);
}
function crOpenReport(layer) {
  window.dispatchEvent(new CustomEvent("closereport:open", { detail: { layer: layer || "summary" } }));
}
function crAvailable(store) { return store?.status === "closed" || store?.status === "archived"; }

// ─────────── Report data ───────────
const CR_CATALOG = [
  { title: "Home Game Jersey", price: 78, personalize: true },
  { title: "Away Game Jersey", price: 78, personalize: true },
  { title: "Integrated Pant", price: 54, personalize: false },
  { title: "Performance Hoodie", price: 68, personalize: true },
  { title: "Shooter Shirt", price: 42, personalize: true },
  { title: "Team Backpack", price: 62, personalize: true },
  { title: "Fan Spirit Tee", price: 34, personalize: false },
  { title: "Team Hat", price: 28, personalize: false },
];
const CR_SIZES = ["YM", "YL", "AS", "AM", "AL", "AXL"];
const CR_STAGES = [
  { key: "submitted", label: "Order placed", w: 0.06 },
  { key: "order-confirmed", label: "Confirmed", w: 0.14 },
  { key: "artwork-production", label: "Artwork", w: 0.18 },
  { key: "factory-production", label: "Factory production", w: 0.22 },
  { key: "shipped", label: "Shipped", w: 0.24 },
  { key: "delivered", label: "Delivered", w: 0.16 },
];
const CR_BUYERS = [
  ["Susan Quinones", "Bears 14U"], ["Frank Wright", "Bears 14U"], ["Kyle Morris", "Bears 16U"],
  ["Irene George", "Bears 16U"], ["Kate Hall", "Bears Varsity"], ["Aaron Brown", "Bears Varsity"],
  ["Maria Delgado", "Bears 14U"], ["Tom Ashby", "Bears JV"], ["Priya Raman", "Bears JV"],
  ["Devon Clark", "Bears Varsity"], ["Nicole Barrett", "Bears 16U"], ["Owen Vance", "Bears JV"],
];
const CR_PLAYERS = ["Jalen Hurts", "Omar Calzoni", "Cooper Curtis", "Davis Aminoff", "Lincoln Herwitz", "Corey Stanton", "Ava Chen", "Marcus Reed"];

function crBuildReport(store, ev) {
  // Same source figures the Reports tab reads.
  const revenue = store?.revenue || 0;
  const orderCount = revenue > 0 ? Math.max(Math.round(revenue / 46), 1) : 0;
  const pending = orderCount ? Math.max(Math.round(orderCount * 0.17), 1) : 0;
  const aov = orderCount ? revenue / orderCount : 0;
  const totalProfit = Math.round(revenue * 0.34);
  const profitData = buildProfit(revenue); // Team Revenue trend (gross)

  const fundraisingEnabled = store?.fundraisingEnabled === true;
  const collected = Math.round(revenue * 0.18 * 100) / 100;
  const platformFees = Math.round(collected * 0.05 * 100) / 100;
  const cardFees = Math.round((collected * 0.029 + orderCount * 0.3) * 100) / 100;
  const netFundraising = Math.round((collected - platformFees - cardFees) * 100) / 100;

  const openDate = crParseDate(store?.storeOpenStart || "2026-02-03");
  const closeDate = crParseDate(ev?.closedAt || store?.storeOpenEnd || "2026-07-06");
  const windowDays = Math.max(1, Math.round((closeDate - openDate) / 86400000));

  // Below-MOQ containers still open at close (from the same pending dataset the
  // Orders tab uses).
  const ds = store?.seeded ? makeOrdersDataset(store) : { pending: [] };
  const moqOpen = (ds.pending || []).filter((g) => (g.pct || 0) < 100).map((g) => ({
    title: g.title, subtitle: g.subtitle, pct: g.pct || 0, fee: g.fee || 0, units: (g.rows || []).length,
  }));
  const moqFeeTotal = moqOpen.reduce((a, g) => a + g.fee, 0);

  // Refunds / cancellations net OUT of revenue: gross - refunds = Total Revenue.
  // Very low order counts don't get a refund story imposed on them.
  const refundCount = orderCount >= 3 ? Math.max(1, Math.round(orderCount * 0.035)) : 0;
  const refundAmount = refundCount ? Math.round(revenue * 0.031 * 100) / 100 : 0;
  const gross = Math.round((revenue + refundAmount) * 100) / 100;

  // Line items — the grain the CSV exports at. Their totals are built to sum
  // exactly to gross, so the summary and the table can never diverge.
  const rnd = crRng(crSeed((store?.id || "s") + ":" + (ev?.id || 1)));
  const stagePlan = [];
  CR_STAGES.forEach((s) => { const n = Math.round(orderCount * s.w); for (let i = 0; i < n; i++) stagePlan.push(s.key); });
  while (stagePlan.length < orderCount) stagePlan.push("delivered");
  stagePlan.length = orderCount;

  const rows = [];
  let running = 0;
  const lineTarget = Math.round((gross - moqFeeTotal) * 100) / 100;
  for (let i = 0; i < orderCount; i++) {
    const buyer = CR_BUYERS[Math.floor(rnd() * CR_BUYERS.length)];
    const origin = rnd() < 0.52 ? "player" : rnd() < 0.72 ? "fan" : "my";
    const lines = 1 + (rnd() < 0.34 ? 1 : 0);
    const webOrderNum = "W" + String(104200 + i * 7 + (i % 3)).padStart(6, "0");
    const factoryOrderNum = "2678" + String(91235122 + i * 13);
    const day = new Date(openDate.getTime() + Math.floor(rnd() * windowDays) * 86400000);
    const poOpen = i % 17 === 5;
    const stage = stagePlan[i] || "delivered";
    for (let j = 0; j < lines; j++) {
      const prod = CR_CATALOG[Math.floor(rnd() * CR_CATALOG.length)];
      const qty = 1 + (rnd() < 0.18 ? 1 : 0);
      const player = CR_PLAYERS[Math.floor(rnd() * CR_PLAYERS.length)];
      rows.push({
        id: `cr-${i}-${j}`,
        webOrderNum, factoryOrderNum,
        orderType: origin,
        customer: buyer[0],
        teamRoster: buyer[1],
        product: { title: prod.title, kind: "jersey", colors: store?.teamColors || ["#1d3a8a", "#0e0e10"] },
        productTitle: prod.title,
        size: CR_SIZES[Math.floor(rnd() * CR_SIZES.length)],
        quantity: qty,
        unitPrice: prod.price,
        lineTotal: prod.price * qty,
        personalization: prod.personalize ? `${player.split(" ")[1] || player} / ${1 + Math.floor(rnd() * 55)}` : "—",
        status: stage,
        poNumber: "45008" + String(5069 + i),
        poOpen,
        tracking: stage === "shipped" || stage === "delivered" ? "ZXY356TJGSAMAK" + String(20 + i) : "",
        fulfillment: stage === "delivered" ? "Delivered" : stage === "shipped" ? "In transit" : "In production",
        dueDate: crDateUS(day),
        orderDate: crDateUS(day),
        moqFee: 0,
        sport: "Basketball",
        shippingGroup: "PMP",
        rep: "Jared Winter",
        organization: store?.teamName || store?.storeTitle || "Team Store",
        refunded: false,
      });
      running += prod.price * qty;
    }
  }
  // Scale line pricing so line totals + MOQ fees - refunds == Total Revenue
  // exactly. Nothing on the report is computed a second, divergent way.
  if (rows.length && running > 0) {
    const factor = lineTarget / running;
    let sum = 0;
    rows.forEach((r) => {
      r.unitPrice = Math.max(0.5, Math.round(r.unitPrice * factor * 100) / 100);
      r.lineTotal = Math.round(r.unitPrice * r.quantity * 100) / 100;
      sum += r.lineTotal;
    });
    const last = rows[rows.length - 1];
    const adjusted = Math.round((last.lineTotal + (lineTarget - sum)) * 100) / 100;
    if (adjusted > 0) {
      last.lineTotal = adjusted;
      last.unitPrice = Math.round((adjusted / last.quantity) * 100) / 100;
    }
  }
  // Attach the Minimum Quantity Fee to the first line of the containers that
  // never reached minimum.
  moqOpen.forEach((g, i) => { const r = rows[i * 3]; if (r) r.moqFee = g.fee; });
  // Flag the refunded / cancelled orders.
  for (let i = 0; i < refundCount && rows.length; i++) { const r = rows[rows.length - 1 - i]; if (r) r.refunded = true; }

  const stageMix = CR_STAGES.map((s) => ({
    ...s,
    count: rows.filter((r) => r.status === s.key).length,
  }));
  const unshipped = rows.filter((r) => ["submitted", "order-confirmed", "artwork-production", "factory-production"].includes(r.status)).length;
  const openPO = rows.filter((r) => r.poOpen).length;

  // Top product by quantity ordered — computed from the line items above.
  const byProduct = {};
  rows.forEach((r) => { byProduct[r.productTitle] = (byProduct[r.productTitle] || 0) + r.quantity; });
  const ranked = Object.entries(byProduct).map(([title, units]) => ({ title, units })).sort((a, b) => b.units - a.units);
  const totalUnits = rows.reduce((a, r) => a + r.quantity, 0);
  const topProduct = ranked[0] || null;
  const singleOrder = orderCount === 1;

  return {
    store, ev,
    storeName: store?.storeTitle || store?.teamName || "Team Store",
    teamName: store?.teamName || store?.storeTitle || "Team Store",
    accent: crAccent(store),
    openDate, closeDate, windowDays,
    revenue, gross, orderCount, pending, aov, totalProfit, profitData,
    fundraisingEnabled, collected, platformFees, cardFees, netFundraising,
    platformFeeLabel: store?.platformFeeLabel || "Platform Fees",
    refundCount, refundAmount,
    moqOpen, moqFeeTotal,
    rows, stageMix, unshipped, openPO,
    topProduct, rankedProducts: ranked, totalUnits, singleOrder,
    empty: orderCount === 0,
    archived: store?.status === "archived",
  };
}

// Hero views in order. Net Fundraising only when Team Fundraising is enabled;
// Avg. Order Value is promoted into that slot when it isn't.
function crHeroes(d) {
  if (d.empty) return [];
  const heroes = [
    {
      id: "revenue", eyebrow: "Total revenue", value: d.revenue, format: crMoney,
      unit: "USD · net of refunds & cancellations",
      sentence: `This store brought in ${crMoney(d.revenue)}.`,
      delta: { v: "12.4%", dir: "up" },
      foot: `Across ${d.windowDays} days of open ordering.`,
    },
    {
      id: "orders", eyebrow: "Orders", value: d.orderCount, format: (n) => Math.round(n).toLocaleString("en-US"),
      unit: d.orderCount === 1 ? "order" : "orders",
      sentence: d.singleOrder ? "One order came through." : `${d.orderCount.toLocaleString("en-US")} orders came through.`,
      delta: { v: "8.1%", dir: "up" },
      foot: `${d.totalUnits.toLocaleString("en-US")} items across ${d.rows.length} line items.`,
    },
  ];
  if (d.fundraisingEnabled) {
    heroes.push({
      id: "fundraising", eyebrow: "Net fundraising", value: d.netFundraising, format: crMoney,
      unit: "USD · after platform & card fees",
      sentence: `You raised ${crMoney(d.netFundraising)} for the team.`,
      delta: { v: "12.4%", dir: "up" },
      foot: `${crMoney2(d.collected)} collected, less ${crMoney2(d.platformFees + d.cardFees)} in fees.`,
    });
  } else {
    heroes.push({
      id: "aov", eyebrow: "Avg. order value", value: d.aov, format: crMoney,
      unit: "USD per order",
      sentence: `Buyers spent ${crMoney(d.aov)} per order on average.`,
      delta: { v: "3.2%", dir: "down" },
      foot: `${crMoney(d.revenue)} over ${d.orderCount.toLocaleString("en-US")} orders.`,
    });
  }
  if (d.topProduct) {
    heroes.push({
      id: "top", eyebrow: "Top product by quantity", value: d.topProduct.units, format: (n) => Math.round(n).toLocaleString("en-US"),
      unit: d.topProduct.units === 1 ? "unit ordered" : "units ordered",
      title: d.topProduct.title,
      sentence: d.singleOrder || d.rankedProducts.length === 1
        ? `The only product ordered was ${d.topProduct.title}.`
        : `Your top seller was ${d.topProduct.title} — ${d.topProduct.units} ordered.`,
      foot: d.rankedProducts.length === 1
        ? "One product was ordered in this window."
        : `${Math.round((d.topProduct.units / Math.max(d.totalUnits, 1)) * 100)}% of all units in the window.`,
    });
  }
  return heroes;
}

// ─────────── Motion ───────────
function crPrefersReduced() {
  try { return window.matchMedia && window.matchMedia("(prefers-reduced-motion: reduce)").matches; } catch (e) { return false; }
}
function CrCountUp({ value, format, reduce, duration = 900 }) {
  const [n, setN] = React.useState(reduce ? value : 0);
  React.useEffect(() => {
    if (reduce) { setN(value); return; }
    let raf, start;
    const tick = (t) => {
      if (start == null) start = t;
      const p = Math.min(1, (t - start) / duration);
      setN(value * (1 - Math.pow(1 - p, 3)));
      if (p < 1) raf = requestAnimationFrame(tick);
    };
    raf = requestAnimationFrame(tick);
    // Guarantee the exact figure lands even if frames are throttled.
    const settle = setTimeout(() => setN(value), duration + 120);
    return () => { cancelAnimationFrame(raf); clearTimeout(settle); };
  }, [value, reduce, duration]);
  return <React.Fragment>{format(n)}</React.Fragment>;
}
function CrFade({ k, reduce, children, style }) {
  const [on, setOn] = React.useState(reduce);
  React.useEffect(() => {
    if (reduce) { setOn(true); return; }
    setOn(false);
    const t = setTimeout(() => setOn(true), 20);
    return () => clearTimeout(t);
  }, [k, reduce]);
  return (
    <div style={{
      opacity: on ? 1 : 0,
      transform: on ? "none" : "scale(.985) translateY(6px)",
      transition: reduce ? "opacity .12s linear" : "opacity .26s cubic-bezier(.2,0,0,1), transform .26s cubic-bezier(.2,0,0,1)",
      ...style,
    }}>{children}</div>
  );
}

// ─────────── Small shared pieces ───────────
function CrDelta({ delta }) {
  if (!delta) return null;
  const up = delta.dir === "up";
  const T = up ? IconTrendUp : IconTrendDown;
  return (
    <span style={{ display: "inline-flex", alignItems: "center", gap: 5, color: up ? CR_GREEN : CR_RED, fontWeight: 700, fontSize: 13, ...CR_NUM }}>
      <T size={15} /> {delta.v} <span style={{ color: CR_MUTED, fontWeight: 500 }}>vs last month</span>
    </span>
  );
}
function CrLogo({ store, size = 34, radius = 8 }) {
  if (store?.teamLogo?.url) return <img src={store.teamLogo.url} alt="" style={{ width: size, height: size, borderRadius: radius, objectFit: "cover", flexShrink: 0 }} />;
  const bg = store?.teamLogo?.color || crAccent(store);
  const mono = store?.teamLogo?.mono || (store?.teamName || "T")[0].toUpperCase();
  return (
    <span style={{ width: size, height: size, borderRadius: radius, background: bg, color: "#fff", flexShrink: 0, display: "inline-flex", alignItems: "center", justifyContent: "center", fontWeight: 800, fontSize: size * 0.42, letterSpacing: ".02em" }}>{mono}</span>
  );
}
function CrOriginBadge({ type }) {
  const map = { fan: { l: "Fan", bg: "#eef3fb", fg: "#1e40af" }, player: { l: "Player", bg: "#f3eefb", fg: "#6d28d9" }, my: { l: "My Order", bg: "#f1f1f3", fg: "#52525a" } };
  const s = map[type] || map.my;
  return <span style={{ display: "inline-flex", padding: "3px 9px", borderRadius: 999, background: s.bg, color: s.fg, fontSize: 11, fontWeight: 700, width: "fit-content" }}>{s.l}</span>;
}
function CrPanel({ title, action, children, style }) {
  return (
    <div style={{ background: "#fff", border: `1px solid ${CR_LINE}`, borderRadius: 12, padding: "20px 24px", ...style }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, marginBottom: 16 }}>
        <h3 style={{ fontSize: 15.5, fontWeight: 800, margin: 0, color: CR_INK }}>{title}</h3>
        {action}
      </div>
      {children}
    </div>
  );
}
function CrStat({ label, value, sub, delta }) {
  return (
    <div style={{ background: "#fff", border: `1px solid ${CR_LINE}`, borderRadius: 12, padding: "16px 18px", display: "flex", flexDirection: "column", gap: 10 }}>
      <span style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".07em", color: CR_MUTED, textTransform: "uppercase" }}>{label}</span>
      <span style={{ fontFamily: "Archivo, sans-serif", fontSize: 26, fontWeight: 800, color: CR_INK, lineHeight: 1, ...CR_NUM }}>{value}</span>
      {delta ? <CrDelta delta={delta} /> : sub ? <span style={{ fontSize: 12.5, color: CR_MUTED }}>{sub}</span> : null}
    </div>
  );
}

// ─────────── Exports ───────────
function crPdfDoc() {
  const ops = [];
  const esc = (s) => crAscii(s).replace(/\\/g, "\\\\").replace(/\(/g, "\\(").replace(/\)/g, "\\)");
  const rgb = (hex) => {
    const h = String(hex || "#000000").replace("#", "");
    const v = h.length === 3 ? h.split("").map((c) => c + c).join("") : h;
    const n = parseInt(v, 16) || 0;
    return [((n >> 16) & 255) / 255, ((n >> 8) & 255) / 255, (n & 255) / 255].map((x) => x.toFixed(3)).join(" ");
  };
  return {
    text(x, y, size, str, opts) {
      const o = opts || {};
      ops.push(`BT /${o.bold ? "F2" : "F1"} ${size} Tf ${rgb(o.color || "#0e0e10")} rg 1 0 0 1 ${x} ${y} Tm (${esc(str)}) Tj ET`);
    },
    rect(x, y, w, h, color) { ops.push(`${rgb(color)} rg ${x} ${y} ${w} ${h} re f`); },
    line(x1, y1, x2, y2, color) { ops.push(`${rgb(color || "#dddddd")} RG 0.7 w ${x1} ${y1} m ${x2} ${y2} l S`); },
    build() {
      const content = ops.join("\n");
      const objs = [
        "<< /Type /Catalog /Pages 2 0 R >>",
        "<< /Type /Pages /Kids [3 0 R] /Count 1 >>",
        "<< /Type /Page /Parent 2 0 R /MediaBox [0 0 612 792] /Resources << /Font << /F1 5 0 R /F2 6 0 R >> >> /Contents 4 0 R >>",
        `<< /Length ${content.length} >>\nstream\n${content}\nendstream`,
        "<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica >>",
        "<< /Type /Font /Subtype /Type1 /BaseFont /Helvetica-Bold >>",
      ];
      let pdf = "%PDF-1.4\n";
      const offsets = [];
      objs.forEach((o, i) => { offsets.push(pdf.length); pdf += `${i + 1} 0 obj\n${o}\nendobj\n`; });
      const xref = pdf.length;
      pdf += `xref\n0 ${objs.length + 1}\n0000000000 65535 f \n`;
      offsets.forEach((off) => { pdf += String(off).padStart(10, "0") + " 00000 n \n"; });
      pdf += `trailer\n<< /Size ${objs.length + 1} /Root 1 0 R >>\nstartxref\n${xref}\n%%EOF`;
      return pdf;
    },
  };
}

function crDownload(blob, filename) {
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url; a.download = filename;
  document.body.appendChild(a); a.click(); a.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1000);
}
function crFileBase(d) { return `StoreCloseReport_${crSlug(d.storeName)}_${crDateISO(d.closeDate)}`; }

// Presentation-ready one-pager: team logo + colors, the window, the hero
// figures, and the Team Revenue trend.
function crDownloadPdf(d, opts) {
  crDownload(new Blob([crRenderPdf(d, opts)], { type: "application/pdf" }), crFileBase(d) + ".pdf");
}
function crRenderPdf(d, opts) {
  const o = opts || {};
  const doc = crPdfDoc();
  const accent = d.accent;
  const M = 54;
  // Header band
  doc.rect(0, 720, 612, 72, accent);
  doc.rect(M, 736, 40, 40, "#ffffff");
  doc.text(M + 12, 749, 18, (d.teamName || "T")[0].toUpperCase(), { bold: true, color: accent });
  doc.text(M + 54, 762, 9, "STORE CLOSE REPORT", { bold: true, color: "#ffffff" });
  doc.text(M + 54, 742, 17, d.storeName, { bold: true, color: "#ffffff" });
  // Window
  doc.text(M, 692, 9, "ACTIVE ORDER WINDOW", { bold: true, color: "#6b6b72" });
  doc.text(M, 674, 13, `${crDate(d.openDate)}  to  ${crDate(d.closeDate)}   (${d.windowDays} days)`, { color: "#0e0e10" });
  doc.text(M, 656, 9.5, `Figures as of ${crDate(d.closeDate)} - the store's close date.`, { color: "#6b6b72" });
  doc.line(M, 644, 612 - M, 644, "#e4e4e7");

  // Hero figures
  const heroes = crHeroes(d);
  const cellW = (612 - M * 2) / 2;
  heroes.slice(0, 4).forEach((h, i) => {
    const col = i % 2, row = Math.floor(i / 2);
    const x = M + col * cellW, y = 596 - row * 88;
    doc.text(x, y, 8.5, h.eyebrow.toUpperCase(), { bold: true, color: "#6b6b72" });
    doc.text(x, y - 30, 27, h.title ? h.title : h.format(h.value), { bold: true, color: "#0e0e10" });
    if (h.title) doc.text(x, y - 48, 11, `${h.format(h.value)} ${h.unit}`, { color: "#6b6b72" });
    else doc.text(x, y - 48, 10, h.unit, { color: "#6b6b72" });
  });

  // Team Revenue trend
  let y = 400;
  doc.text(M, y, 9, "TEAM REVENUE", { bold: true, color: "#6b6b72" });
  const bars = d.profitData || [];
  const max = Math.max(1, ...bars.map((b) => b.profit));
  const bw = (612 - M * 2) / Math.max(bars.length, 1);
  bars.forEach((b, i) => {
    const h = Math.max(2, (b.profit / max) * 96);
    doc.rect(M + i * bw + 3, y - 118, bw - 8, h, b.current ? accent : "#d4d4d8");
    doc.text(M + i * bw + 4, y - 132, 7.5, b.month, { color: "#9a9aa1" });
  });
  doc.text(M, y - 150, 10, `Total team revenue in window: ${crMoney(d.revenue)}`, { color: "#0e0e10" });

  // Optional sections
  y = 218;
  if (o.breakdown) {
    doc.text(M, y, 9, "DETAILED BREAKDOWN", { bold: true, color: "#6b6b72" });
    const rows = [
      ["Gross sales", crMoney2(d.gross)],
      ["Refunds / cancellations (" + d.refundCount + ")", "-" + crMoney2(d.refundAmount)],
      ["Total revenue (net)", crMoney2(d.revenue)],
      ["Orders / Avg. order value", `${d.orderCount} / ${crMoney2(d.aov)}`],
    ].concat(d.fundraisingEnabled && o.summary !== "gross" ? [
      ["Fundraising collected", crMoney2(d.collected)],
      [d.platformFeeLabel + " + card fees", "-" + crMoney2(d.platformFees + d.cardFees)],
      ["Net fundraising", crMoney2(d.netFundraising)],
    ] : d.fundraisingEnabled ? [["Fundraising collected (gross)", crMoney2(d.collected)]] : []);
    rows.forEach((r, i) => {
      const ry = y - 18 - i * 15;
      doc.text(M, ry, 10, r[0], { color: "#3f3f46" });
      doc.text(612 - M - 90, ry, 10, r[1], { bold: i === 2 || r[0] === "Net fundraising", color: "#0e0e10" });
    });
    y = y - 26 - rows.length * 15;
  }
  if (o.dateRange) {
    doc.text(M, y, 10, `Selected date range: ${crDateUS(d.openDate)} - ${crDateUS(d.closeDate)}`, { color: "#3f3f46" });
    y -= 18;
  }
  if (o.notes && o.notesText) {
    doc.text(M, y, 9, "REPORT NOTES", { bold: true, color: "#6b6b72" });
    String(o.notesText).slice(0, 320).match(/.{1,88}(\s|$)/g)?.forEach((ln, i) => doc.text(M, y - 16 - i * 13, 10, ln.trim(), { color: "#3f3f46" }));
    y -= 60;
  }
  doc.line(M, 66, 612 - M, 66, "#e4e4e7");
  doc.text(M, 50, 8.5, `Refunds and cancellations are netted out of revenue, consistent with the Reports tab. Generated ${crDate(new Date())}.`, { color: "#9a9aa1" });
  return doc.build();
}

// ─────────── PDF settings step (mirrors Fundraising Report → Download PDF) ───────────
function CrPdfSettingsModal({ open, onClose, data, onDone }) {
  const [summary, setSummary] = React.useState("net");
  const [opts, setOpts] = React.useState({ breakdown: true, dateRange: true, notes: false });
  const [notesText, setNotesText] = React.useState("");
  React.useEffect(() => { if (open) { setSummary("net"); setOpts({ breakdown: true, dateRange: true, notes: false }); } }, [open]);
  const toggle = (k) => setOpts((p) => ({ ...p, [k]: !p[k] }));
  const rangeStr = `${crDateUS(data.openDate)} - ${crDateUS(data.closeDate)}`;
  return (
    <Modal open={open} onClose={onClose} width={720}>
      <div style={{ display: "flex", flexDirection: "column", maxHeight: "86vh" }}>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "28px 32px", borderBottom: `1px solid ${CR_LINE}` }}>
          <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, margin: 0, color: CR_INK, letterSpacing: "-.01em" }}>Summary PDF Settings</h2>
          <button onClick={onClose} aria-label="Close" style={{ background: "transparent", border: 0, cursor: "pointer", padding: 4, color: "#9a9aa1", display: "inline-flex" }}><IconX size={24} /></button>
        </div>
        <div style={{ padding: "28px 32px", overflowY: "auto" }}>
          <div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
            <FrRadio selected={summary === "net"} onSelect={() => setSummary("net")}
              title="Net Summary (Default)"
              sub="Revenue net of refunds and cancellations, and fundraising after all fees." />
            <FrRadio selected={summary === "gross"} onSelect={() => setSummary("gross")}
              title="Gross Total Summary"
              sub="Shows gross sales and fundraising collected before any fees are deducted." />
          </div>
          <div style={{ height: 1, background: CR_LINE, margin: "28px 0" }} />
          <div style={{ fontSize: 16, fontWeight: 800, color: CR_INK, marginBottom: 16 }}>Report Details:</div>
          <div style={{ display: "flex", flexDirection: "column", gap: 24 }}>
            <FrCheckbox checked={opts.breakdown} onToggle={() => toggle("breakdown")}
              title="Detailed Breakdown (Full Report)"
              sub="Adds gross sales, refunds, net revenue, order counts and the fundraising lines." />
            <FrCheckbox checked={opts.dateRange} onToggle={() => toggle("dateRange")}
              title="Selected Date Range"
              sub={`Adds the Active Order Window (${rangeStr}) to the report PDF.`} />
            <FrCheckbox checked={opts.notes} onToggle={() => toggle("notes")}
              title="Include PDF Report Notes"
              sub="Adds your notes to the printed report output." />
          </div>
          {opts.notes && (
            <textarea value={notesText} onChange={(e) => setNotesText(e.target.value)} rows={3}
              placeholder="Notes for the league director…"
              style={{ marginTop: 16, width: "100%", border: `1px solid #d4d4d8`, borderRadius: 8, padding: 12, fontSize: 13.5, fontFamily: "inherit", resize: "vertical" }} />
          )}
        </div>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, padding: "16px 32px", borderTop: `1px solid ${CR_LINE}` }}>
          <span style={{ fontSize: 12.5, color: CR_MUTED, ...CR_NUM }}>{crFileBase(data)}.pdf</span>
          <div style={{ display: "flex", gap: 16 }}>
            <button onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8, padding: "12px 24px", fontSize: 14, fontWeight: 700, color: CR_INK, cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
            <button onClick={() => { crDownloadPdf(data, { ...opts, summary, notesText }); onClose(); onDone && onDone(); }}
              style={{ display: "inline-flex", alignItems: "center", gap: 8, background: CR_INK, border: 0, borderRadius: 8, padding: "12px 24px", fontSize: 14, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>
              <IconDownload size={17} /> Download PDF
            </button>
          </div>
        </div>
      </div>
    </Modal>
  );
}

// ─────────── Detail-layer table columns (line-item grain) ───────────
function crMakeColumns() {
  return [
    { key: "webOrderNum", label: "Web Order #", w: "1fr", alwaysOn: true, render: (o) => <span style={{ ...CR_NUM, fontWeight: 600 }}>{o.webOrderNum}</span> },
    { key: "factoryOrderNum", label: "Factory Order #", w: "1.1fr" },
    { key: "orderType", label: "Origin", w: ".8fr", render: (o) => <CrOriginBadge type={o.orderType} />, csv: (o) => o.orderType === "fan" ? "Fan" : o.orderType === "player" ? "Player" : "My Order" },
    { key: "customer", label: "Buyer", w: "1fr" },
    { key: "teamRoster", label: "Roster / Team", w: "1fr" },
    { key: "productTitle", label: "Product", w: "1.3fr" },
    { key: "size", label: "Size", w: ".5fr" },
    { key: "quantity", label: "Qty", w: ".45fr", align: "right", render: (o) => <span style={CR_NUM}>{o.quantity}</span> },
    { key: "unitPrice", label: "Unit Price", w: ".7fr", align: "right", render: (o) => <span style={CR_NUM}>{crMoney2(o.unitPrice)}</span>, csv: (o) => o.unitPrice.toFixed(2) },
    { key: "lineTotal", label: "Line Total", w: ".75fr", align: "right", render: (o) => <span style={{ ...CR_NUM, fontWeight: 700 }}>{crMoney2(o.lineTotal)}</span>, csv: (o) => o.lineTotal.toFixed(2) },
    { key: "personalization", label: "Personalization", w: "1fr" },
    { key: "status", label: "Production Stage", w: "1.25fr", render: (o) => <StatusBadge status={o.status} tracking={o.tracking} />, csv: (o) => STATUS_STYLES[o.status]?.label || o.status },
    { key: "poNumber", label: "PO #", w: ".9fr", render: (o) => o.poOpen
      ? <span title="Unsettled payment" style={{ ...CR_NUM, color: "#b81d14", fontWeight: 700 }}>{o.poNumber} · open</span>
      : <span style={CR_NUM}>{o.poNumber}</span>, csv: (o) => o.poNumber + (o.poOpen ? " (open)" : "") },
    { key: "tracking", label: "Tracking", w: "1fr", render: (o) => o.tracking || "—" },
    { key: "fulfillment", label: "Fulfillment", w: ".9fr" },
    { key: "moqFee", label: "Min. Qty Fee", w: ".8fr", align: "right", render: (o) => o.moqFee ? <span style={{ ...CR_NUM, color: CR_RED, fontWeight: 700 }}>{crMoney2(o.moqFee)}</span> : "—", csv: (o) => o.moqFee ? o.moqFee.toFixed(2) : "" },
  ];
}

// ─────────── Summary layer views ───────────
function CrCover({ d, store, reduce, mode }) {
  return (
    <CrFade k="cover" reduce={reduce} style={{ textAlign: "center", maxWidth: 640 }}>
      <div style={{ display: "inline-flex", alignItems: "center", gap: 12, marginBottom: 28 }}>
        <CrLogo store={store} size={44} radius={10} />
        <span style={{ width: 1, height: 28, background: CR_LINE }} />
        <span style={{ fontSize: 13, fontWeight: 700, letterSpacing: ".14em", textTransform: "uppercase", color: CR_MUTED }}>Store Close Report</span>
      </div>
      <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 42, lineHeight: 1.1, fontWeight: 800, letterSpacing: "-.02em", color: CR_INK, margin: "0 0 16px", textWrap: "pretty" }}>
        {d.storeName}
      </h2>
      <p style={{ fontSize: 16, lineHeight: 1.6, color: CR_MUTED, margin: "0 0 28px" }}>
        {d.empty
          ? `The ordering window ran ${crDate(d.openDate)} – ${crDate(d.closeDate)} and closed with no orders placed.`
          : `The ordering window ran ${crDate(d.openDate)} – ${crDate(d.closeDate)}. Here's everything that happened while it was open.`}
      </p>
      <div style={{ display: "inline-flex", alignItems: "center", gap: 10, padding: "8px 16px", borderRadius: 999, background: "#f5f5f7", fontSize: 12.5, fontWeight: 600, color: CR_MUTED, ...CR_NUM }}>
        <span style={{ width: 7, height: 7, borderRadius: "50%", background: d.archived ? "#9a9aa1" : "#b81d14" }} />
        {d.archived ? "Archived" : "Closed"} · as of {crDate(d.closeDate)}
      </div>
      {mode === "auto" && (
        <div style={{ marginTop: 28, fontSize: 13, color: "#9a9aa1" }}>Existing orders keep processing normally — the workspace stays fully editable.</div>
      )}
    </CrFade>
  );
}

function CrHeroView({ hero, d, reduce }) {
  return (
    <CrFade k={hero.id} reduce={reduce} style={{ textAlign: "center", maxWidth: 720 }}>
      <div style={{ fontSize: 13, fontWeight: 700, letterSpacing: ".14em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 24 }}>{hero.eyebrow}</div>
      {hero.title ? (
        <React.Fragment>
          <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 52, fontWeight: 800, letterSpacing: "-.02em", color: CR_INK, lineHeight: 1.05, marginBottom: 12, textWrap: "balance" }}>{hero.title}</div>
          <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 72, fontWeight: 800, color: d.accent, lineHeight: 1, letterSpacing: "-.03em", ...CR_NUM }}>
            <CrCountUp value={hero.value} format={hero.format} reduce={reduce} />
          </div>
        </React.Fragment>
      ) : (
        <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 88, fontWeight: 800, color: CR_INK, lineHeight: 1, letterSpacing: "-.035em", ...CR_NUM }}>
          <CrCountUp value={hero.value} format={hero.format} reduce={reduce} />
        </div>
      )}
      <div style={{ fontSize: 13.5, color: CR_MUTED, marginTop: 14, letterSpacing: ".01em" }}>{hero.unit}</div>
      <div style={{ height: 3, width: 56, background: d.accent, borderRadius: 2, margin: "26px auto" }} />
      <p style={{ fontSize: 19, lineHeight: 1.5, fontWeight: 600, color: CR_INK, margin: "0 0 18px", textWrap: "pretty" }}>{hero.sentence}</p>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: 16, flexWrap: "wrap", fontSize: 13, color: CR_MUTED }}>
        {hero.delta && <CrDelta delta={hero.delta} />}
        {hero.delta && <span style={{ width: 1, height: 14, background: CR_LINE }} />}
        <span style={CR_NUM}>as of {crDate(d.closeDate)}</span>
      </div>
      <div style={{ marginTop: 12, fontSize: 13, color: "#9a9aa1" }}>{hero.foot}</div>
    </CrFade>
  );
}

function CrRecap({ d, reduce, onDetail }) {
  const tiles = [
    { label: "Total revenue", value: crMoney(d.revenue) },
    { label: "Orders", value: d.orderCount.toLocaleString("en-US") },
    { label: "Avg. order value", value: crMoney(d.aov) },
    { label: "Pending orders", value: d.pending.toLocaleString("en-US") },
    d.fundraisingEnabled ? { label: "Net fundraising", value: crMoney(d.netFundraising) } : null,
    { label: "Team revenue", value: crMoney(d.revenue) },
    { label: "Refunds / cancellations", value: `${d.refundCount} · ${crMoney(d.refundAmount)}` },
    { label: "Not yet shipped", value: d.unshipped.toLocaleString("en-US") },
  ].filter(Boolean);
  return (
    <CrFade k="recap" reduce={reduce} style={{ width: "100%", maxWidth: 820 }}>
      <div style={{ textAlign: "center", marginBottom: 28 }}>
        <div style={{ fontSize: 13, fontWeight: 700, letterSpacing: ".14em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 12 }}>The full recap</div>
        <h3 style={{ fontFamily: "Archivo, sans-serif", fontSize: 30, fontWeight: 800, letterSpacing: "-.02em", color: CR_INK, margin: 0 }}>
          {crDateShort(d.openDate)} – {crDate(d.closeDate)}
        </h3>
      </div>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 12 }}>
        {tiles.map((t) => (
          <div key={t.label} style={{ border: `1px solid ${CR_LINE}`, borderRadius: 10, padding: "14px 16px", background: "#fff" }}>
            <div style={{ fontSize: 10.5, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 8 }}>{t.label}</div>
            <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 21, fontWeight: 800, color: CR_INK, ...CR_NUM }}>{t.value}</div>
          </div>
        ))}
      </div>
      <div style={{ marginTop: 20, padding: "14px 18px", borderRadius: 10, background: "#f7f7f8", fontSize: 12.5, lineHeight: 1.55, color: "#4a4a52" }}>
        Gross sales {crMoney2(d.gross)} less {d.refundCount} refund{d.refundCount === 1 ? "" : "s"} / cancellation{d.refundCount === 1 ? "" : "s"} of {crMoney2(d.refundAmount)} equals the {crMoney2(d.revenue)} total revenue shown on the Reports tab. Every figure here is computed once, from the order line items in this window.
      </div>
      <div style={{ display: "flex", justifyContent: "center", marginTop: 26 }}>
        <button onClick={onDetail} style={{ display: "inline-flex", alignItems: "center", gap: 10, background: CR_INK, color: "#fff", border: 0, borderRadius: 8, padding: "14px 26px", fontSize: 14.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>
          Open the full report <IconChevronRight size={16} />
        </button>
      </div>
    </CrFade>
  );
}

function CrEmptyWindow({ d, reduce }) {
  return (
    <CrFade k="empty" reduce={reduce} style={{ maxWidth: 620, textAlign: "center" }}>
      <div style={{ fontSize: 13, fontWeight: 700, letterSpacing: ".14em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 20 }}>Window summary</div>
      <h3 style={{ fontFamily: "Archivo, sans-serif", fontSize: 30, fontWeight: 800, letterSpacing: "-.02em", color: CR_INK, margin: "0 0 14px" }}>No orders were placed</h3>
      <p style={{ fontSize: 15.5, lineHeight: 1.6, color: CR_MUTED, margin: "0 0 24px" }}>
        The store was open for {d.windowDays} days, {crDate(d.openDate)} – {crDate(d.closeDate)}. No orders, revenue, or fundraising were recorded in this window.
      </p>
      <div style={{ display: "grid", gridTemplateColumns: "repeat(3, 1fr)", gap: 12, textAlign: "left" }}>
        {[["Orders", "0"], ["Total revenue", "$0"], ["Refunds", "0"]].map(([l, v]) => (
          <div key={l} style={{ border: `1px solid ${CR_LINE}`, borderRadius: 10, padding: "14px 16px" }}>
            <div style={{ fontSize: 10.5, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 8 }}>{l}</div>
            <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 21, fontWeight: 800, color: CR_INK, ...CR_NUM }}>{v}</div>
          </div>
        ))}
      </div>
    </CrFade>
  );
}

// ─────────── Detail layer ───────────
function CrDetailLayer({ d, store, onPdf, exportApi, onCsv }) {
  const columns = React.useMemo(() => crMakeColumns(), []);
  const attention = [];
  if (d.unshipped) attention.push(`${d.unshipped} line item${d.unshipped === 1 ? "" : "s"} not yet shipped — these keep processing after close.`);
  if (d.openPO) attention.push(`${d.openPO} order${d.openPO === 1 ? "" : "s"} with an unsettled payment (open PO).`);
  if (d.moqOpen.length) attention.push(`${d.moqOpen.length} container${d.moqOpen.length === 1 ? "" : "s"} closed below minimum quantity.`);

  return (
    <div style={{ padding: "24px 28px 40px", overflowY: "auto", flex: 1, background: "#fbfbfc" }}>
      {/* Report header */}
      <div style={{ display: "flex", alignItems: "flex-start", justifyContent: "space-between", gap: 24, flexWrap: "wrap", marginBottom: 20 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 16 }}>
          <CrLogo store={store} size={48} radius={10} />
          <div>
            <div style={{ fontSize: 11, fontWeight: 700, letterSpacing: ".12em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 6 }}>Store Close Report</div>
            <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 24, fontWeight: 800, margin: 0, color: CR_INK, letterSpacing: "-.01em" }}>{d.storeName}</h2>
            <div style={{ fontSize: 13, color: CR_MUTED, marginTop: 6, ...CR_NUM }}>
              Active Order Window {crDate(d.openDate)} – {crDate(d.closeDate)} · {d.windowDays} days · as of {crDate(d.closeDate)}
            </div>
          </div>
        </div>
        <div style={{ display: "flex", gap: 12, flexWrap: "wrap" }}>
          <button onClick={onCsv} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8, padding: "12px 18px", fontSize: 13.5, fontWeight: 700, color: CR_INK, cursor: "pointer", fontFamily: "inherit" }}>
            <IconDownload size={16} /> Full order CSV
          </button>
          <button onClick={onPdf} style={{ display: "inline-flex", alignItems: "center", gap: 8, background: CR_INK, border: 0, borderRadius: 8, padding: "12px 18px", fontSize: 13.5, fontWeight: 700, color: "#fff", cursor: "pointer", fontFamily: "inherit" }}>
            <IconDownload size={16} /> Summary PDF
          </button>
        </div>
      </div>

      {attention.length > 0 && (
        <div style={{ display: "flex", gap: 14, alignItems: "flex-start", background: "#fff8ed", border: "1px solid #f6dcb4", borderRadius: 10, padding: "14px 18px", marginBottom: 16 }}>
          <span style={{ color: "#b45309", marginTop: 1, flexShrink: 0 }}><IconAlert size={17} /></span>
          <div>
            <div style={{ fontSize: 13, fontWeight: 800, color: "#92400e", marginBottom: 6 }}>Needs attention</div>
            <ul style={{ margin: 0, paddingLeft: 18, fontSize: 13, color: "#7c4a11", lineHeight: 1.7 }}>
              {attention.map((a) => <li key={a}>{a}</li>)}
            </ul>
          </div>
        </div>
      )}

      {/* KPI row — the same figures as the Reports tab */}
      <div style={{ display: "grid", gridTemplateColumns: `repeat(${d.fundraisingEnabled ? 5 : 4}, 1fr)`, gap: 12, marginBottom: 16 }}>
        <CrStat label="Total Revenue" value={crMoney(d.revenue)} delta={{ v: "12.4%", dir: "up" }} />
        <CrStat label="Orders" value={d.orderCount.toLocaleString("en-US")} delta={{ v: "8.1%", dir: "up" }} />
        <CrStat label="Pending Orders" value={d.pending.toLocaleString("en-US")} delta={{ v: "4.6%", dir: "up" }} />
        <CrStat label="Avg. Order Value" value={crMoney(d.aov)} delta={{ v: "3.2%", dir: "down" }} />
        {d.fundraisingEnabled && <CrStat label="Net Fundraising" value={crMoney(d.netFundraising)} sub={`from ${crMoney2(d.collected)} collected`} />}
      </div>

      {/* Reconciliation */}
      <div style={{ background: "#fff", border: `1px solid ${CR_LINE}`, borderRadius: 12, padding: "16px 20px", marginBottom: 16, fontSize: 13, color: "#4a4a52", lineHeight: 1.6 }}>
        <strong style={{ color: CR_INK, fontWeight: 800 }}>How these numbers reconcile.</strong>{" "}
        Gross sales <span style={CR_NUM}>{crMoney2(d.gross)}</span> − refunds &amp; cancellations <span style={CR_NUM}>{crMoney2(d.refundAmount)}</span> ({d.refundCount}) = total revenue <span style={{ ...CR_NUM, fontWeight: 700, color: CR_INK }}>{crMoney2(d.revenue)}</span>, the figure on the Reports tab. Minimum Quantity Fees of <span style={CR_NUM}>{crMoney2(d.moqFeeTotal)}</span> are included in gross. No figure on this report is computed a second way.
      </div>

      <div style={{ display: "grid", gridTemplateColumns: d.fundraisingEnabled ? "1.4fr 1fr" : "1fr", gap: 16, marginBottom: 16 }}>
        <CrPanel title="Team Revenue">
          <TeamProfitChart data={d.profitData} />
        </CrPanel>
        {d.fundraisingEnabled && (
          <CrPanel title="Fundraising breakdown">
            <div style={{ border: `1px solid ${CR_LINE}`, borderRadius: 10, overflow: "hidden" }}>
              {[
                { label: "Fundraising Collected", val: d.collected },
                { label: d.platformFeeLabel, val: d.platformFees },
                { label: "Card Processing Fees", val: d.cardFees },
                { label: "Net Fundraising", val: d.netFundraising, bold: true },
              ].map((r, i, arr) => (
                <div key={r.label} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "14px 16px", borderBottom: i < arr.length - 1 ? `1px solid ${CR_LINE}` : "none", background: r.bold ? "#fafafb" : "#fff" }}>
                  <span style={{ fontSize: 13.5, fontWeight: r.bold ? 800 : 500, color: CR_INK }}>{r.label}</span>
                  <span style={{ fontSize: 13.5, fontWeight: r.bold ? 800 : 500, color: CR_INK, ...CR_NUM }}>{crMoney2(r.val)}</span>
                </div>
              ))}
            </div>
            <p style={{ margin: "12px 2px 0", fontSize: 12.5, color: CR_MUTED, lineHeight: 1.55 }}>
              Only price additions marked as “Fundraising” on the product edit page are included — the same rule the Fundraising Report uses.
            </p>
          </CrPanel>
        )}
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: 16, marginBottom: 16 }}>
        <CrPanel title="Production-stage mix">
          <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
            {d.stageMix.map((s) => {
              const pct = d.rows.length ? Math.round((s.count / d.rows.length) * 100) : 0;
              return (
                <div key={s.key} style={{ display: "grid", gridTemplateColumns: "148px 1fr 74px", alignItems: "center", gap: 12 }}>
                  <span style={{ fontSize: 12.5, color: CR_INK }}>{s.label}</span>
                  <span style={{ height: 8, borderRadius: 4, background: "#f1f1f3", overflow: "hidden" }}>
                    <span style={{ display: "block", height: "100%", width: `${pct}%`, background: d.accent, borderRadius: 4 }} />
                  </span>
                  <span style={{ fontSize: 12.5, color: CR_MUTED, textAlign: "right", ...CR_NUM }}>{s.count} · {pct}%</span>
                </div>
              );
            })}
          </div>
          <div style={{ marginTop: 14, paddingTop: 12, borderTop: `1px solid ${CR_LINE}`, fontSize: 12.5, color: CR_MUTED, lineHeight: 1.55 }}>
            <strong style={{ color: CR_INK }}>{d.unshipped}</strong> line items are not yet shipped. Closing the store does not stop them — existing orders keep processing.
          </div>
        </CrPanel>

        <CrPanel title="Refunds, cancellations & minimums">
          <div style={{ display: "flex", gap: 12, marginBottom: 16 }}>
            <div style={{ flex: 1, border: `1px solid ${CR_LINE}`, borderRadius: 10, padding: "14px 16px" }}>
              <div style={{ fontSize: 10.5, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 8 }}>Refunds / cancellations</div>
              <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 21, fontWeight: 800, color: CR_INK, ...CR_NUM }}>{d.refundCount} · {crMoney2(d.refundAmount)}</div>
            </div>
            <div style={{ flex: 1, border: `1px solid ${CR_LINE}`, borderRadius: 10, padding: "14px 16px" }}>
              <div style={{ fontSize: 10.5, fontWeight: 700, letterSpacing: ".07em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 8 }}>Minimum Quantity Fees</div>
              <div style={{ fontFamily: "Archivo, sans-serif", fontSize: 21, fontWeight: 800, color: CR_INK, ...CR_NUM }}>{crMoney2(d.moqFeeTotal)}</div>
            </div>
          </div>
          {d.moqOpen.length ? (
            <div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
              <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".05em", textTransform: "uppercase", color: CR_MUTED }}>Below minimum at close</div>
              {d.moqOpen.map((g) => (
                <div key={g.title} style={{ border: `1px solid ${CR_LINE}`, borderRadius: 10, padding: "12px 14px" }}>
                  <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 8 }}>
                    <span style={{ fontSize: 13, fontWeight: 700, color: CR_INK }}>{g.title}</span>
                    <span style={{ fontSize: 12.5, fontWeight: 700, color: CR_RED, ...CR_NUM }}>{crMoney2(g.fee)}</span>
                  </div>
                  <div style={{ height: 7, borderRadius: 4, background: "#f1f1f3", overflow: "hidden", marginBottom: 6 }}>
                    <span style={{ display: "block", height: "100%", width: `${g.pct}%`, background: CR_RED, borderRadius: 4 }} />
                  </div>
                  <div style={{ fontSize: 11.5, color: CR_MUTED, ...CR_NUM }}>{g.pct}% of minimum · {g.units} orders in container</div>
                </div>
              ))}
            </div>
          ) : (
            <div style={{ fontSize: 12.5, color: CR_MUTED }}>Every container reached its minimum before the window closed.</div>
          )}
        </CrPanel>
      </div>

      <CrPanel title={`Full order table · ${d.rows.length} line items in this window`} style={{ padding: "20px 20px 24px" }}>
        {d.rows.length ? (
          <AllOrdersExperience
            orders={d.rows}
            columns={columns}
            store={store}
            onOpen={() => {}}
            csvName={crFileBase(d) + ".csv"}
            exportApi={exportApi} />
        ) : (
          <div style={{ padding: "48px 0", textAlign: "center", color: CR_MUTED, fontSize: 13.5 }}>
            No orders were placed during this window. Exports still produce valid files with headers and zero rows.
          </div>
        )}
      </CrPanel>
    </div>
  );
}

// ─────────── The report overlay ───────────
function StoreCloseReportOverlay({ store, ev, mode, initialLayer, onClose }) {
  const d = React.useMemo(() => crBuildReport(store, ev), [store, ev]);
  const [layer, setLayer] = React.useState(initialLayer || "summary");
  const [step, setStep] = React.useState(0);
  const reduce = crPrefersReduced();
  const [pdfOpen, setPdfOpen] = React.useState(false);
  const [toast, setToast] = React.useState(null);
  const exportApi = React.useRef(null);

  const views = React.useMemo(() => {
    const v = [{ kind: "cover" }];
    if (d.empty) v.push({ kind: "empty" });
    else crHeroes(d).forEach((h) => v.push({ kind: "hero", hero: h }));
    v.push({ kind: "recap" });
    return v;
  }, [d]);

  const last = step >= views.length - 1;
  const next = () => setStep((s) => Math.min(s + 1, views.length - 1));
  const prev = () => setStep((s) => Math.max(s - 1, 0));

  React.useEffect(() => {
    const h = (e) => {
      if (e.key === "Escape") { onClose(); return; }
      if (layer !== "summary") return;
      if (e.key === "ArrowRight" || e.key === " " || e.key === "Enter") { e.preventDefault(); next(); }
      if (e.key === "ArrowLeft") { e.preventDefault(); prev(); }
    };
    window.addEventListener("keydown", h);
    return () => window.removeEventListener("keydown", h);
  }, [layer, views.length, onClose]);

  const view = views[step] || views[0];
  const wide = layer === "detail";

  const csvExport = () => {
    if (exportApi.current) { exportApi.current(); return; }
    const cols = crMakeColumns();
    const head = cols.map((c) => '"' + c.label + '"').join(",");
    crDownload(new Blob([head + "\n"], { type: "text/csv;charset=utf-8;" }), crFileBase(d) + ".csv");
  };

  return (
    <div
      onMouseDown={(e) => { if (e.target === e.currentTarget) onClose(); }}
      style={{
        position: "fixed", inset: 0, zIndex: 4000, background: "rgba(9,9,11,.62)",
        backdropFilter: "blur(3px)", WebkitBackdropFilter: "blur(3px)",
        display: "flex", alignItems: "center", justifyContent: "center", padding: 24,
      }}>
      <div role="dialog" aria-modal="true" aria-label="Store Close Report" style={{
        width: wide ? "min(1320px, 97vw)" : "min(1040px, 94vw)",
        height: "min(760px, 94vh)", background: "#fff", borderRadius: 14,
        boxShadow: "0 30px 80px rgba(9,9,11,.34)", display: "flex", flexDirection: "column",
        overflow: "hidden", transition: "width .22s cubic-bezier(.2,0,0,1)",
      }}>
        {/* Top bar */}
        <div style={{ display: "flex", alignItems: "center", gap: 16, padding: "14px 20px", borderBottom: `1px solid ${CR_LINE}`, flexShrink: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12, minWidth: 0 }}>
            <CrLogo store={store} size={30} radius={7} />
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 13.5, fontWeight: 800, color: CR_INK, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{d.storeName}</div>
              <div style={{ fontSize: 11.5, color: CR_MUTED, ...CR_NUM }}>Store Close Report · {crDate(d.openDate)} – {crDate(d.closeDate)}</div>
            </div>
          </div>
          <div style={{ marginLeft: "auto", display: "flex", alignItems: "center", gap: 10 }}>
            {layer === "summary" ? (
              <button onClick={() => setLayer("detail")} style={{
                display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 999,
                padding: "7px 14px", fontSize: 12.5, fontWeight: 700, color: CR_INK, cursor: "pointer", fontFamily: "inherit",
              }}>Skip to full report <IconChevronRight size={14} /></button>
            ) : (
              <button onClick={() => setLayer("summary")} style={{
                display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 999,
                padding: "7px 14px", fontSize: 12.5, fontWeight: 700, color: CR_INK, cursor: "pointer", fontFamily: "inherit",
              }}>Back to summary</button>
            )}
            <button onClick={onClose} aria-label="Close report" style={{ background: "transparent", border: 0, cursor: "pointer", padding: 6, color: "#9a9aa1", display: "inline-flex" }}><IconX size={20} /></button>
          </div>
        </div>

        {layer === "summary" ? (
          <React.Fragment>
            {/* Segmented progress */}
            <div style={{ display: "flex", gap: 6, padding: "12px 20px 0", flexShrink: 0 }}>
              {views.map((v, i) => (
                <button key={i} onClick={() => setStep(i)} aria-label={`View ${i + 1}`} style={{
                  flex: 1, height: 4, borderRadius: 2, border: 0, padding: 0, cursor: "pointer",
                  background: i <= step ? d.accent : "#ececf0", opacity: i <= step ? 1 : 1,
                  transition: "background .2s ease",
                }} />
              ))}
            </div>
            {/* Stage */}
            <div onClick={(e) => { if (e.target === e.currentTarget || e.currentTarget.contains(e.target)) { if (!last) next(); } }}
              style={{ flex: 1, display: "flex", alignItems: "center", justifyContent: "center", padding: "24px 44px", cursor: last ? "default" : "pointer", overflowY: "auto" }}>
              {view.kind === "cover" ? <CrCover d={d} store={store} reduce={reduce} mode={mode} />
                : view.kind === "empty" ? <CrEmptyWindow d={d} reduce={reduce} />
                : view.kind === "hero" ? <CrHeroView hero={view.hero} d={d} reduce={reduce} />
                : <CrRecap d={d} reduce={reduce} onDetail={() => setLayer("detail")} />}
            </div>
            {/* Footer controls */}
            <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "14px 20px", borderTop: `1px solid ${CR_LINE}`, flexShrink: 0 }}>
              <button onClick={prev} disabled={step === 0} style={{
                display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8,
                padding: "10px 16px", fontSize: 13, fontWeight: 700, color: step === 0 ? "#b6b6bd" : CR_INK, cursor: step === 0 ? "not-allowed" : "pointer", fontFamily: "inherit",
              }}><IconChevronLeft size={15} /> Back</button>
              <span style={{ fontSize: 12.5, color: CR_MUTED, ...CR_NUM }}>{step + 1} / {views.length}</span>
              <button onClick={() => setStep(views.length - 1)} disabled={last} style={{
                background: "transparent", border: 0, fontSize: 12.5, fontWeight: 700, color: last ? "#b6b6bd" : CR_MUTED,
                cursor: last ? "default" : "pointer", fontFamily: "inherit", padding: "6px 4px",
              }}>Jump to end</button>
              <div style={{ marginLeft: "auto", display: "flex", alignItems: "center", gap: 12 }}>
                <button onClick={onClose} style={{ background: "transparent", border: 0, fontSize: 13, fontWeight: 700, color: CR_MUTED, cursor: "pointer", fontFamily: "inherit", padding: "10px 8px" }}>Done</button>
                {last ? (
                  <button onClick={() => setLayer("detail")} style={{
                    display: "inline-flex", alignItems: "center", gap: 8, background: CR_INK, color: "#fff", border: 0, borderRadius: 8,
                    padding: "11px 20px", fontSize: 13.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
                  }}>View full report <IconChevronRight size={15} /></button>
                ) : (
                  <button onClick={next} style={{
                    display: "inline-flex", alignItems: "center", gap: 8, background: CR_INK, color: "#fff", border: 0, borderRadius: 8,
                    padding: "11px 20px", fontSize: 13.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
                  }}>Next <IconChevronRight size={15} /></button>
                )}
              </div>
            </div>
          </React.Fragment>
        ) : (
          <CrDetailLayer d={d} store={store} exportApi={exportApi} onPdf={() => setPdfOpen(true)} onCsv={csvExport} />
        )}
      </div>

      <CrPdfSettingsModal open={pdfOpen} onClose={() => setPdfOpen(false)} data={d} onDone={() => setToast("Summary PDF downloaded")} />
      <Toast message={toast} onDone={() => setToast(null)} />
    </div>
  );
}

// ─────────── Trigger host + prototype demo control ───────────
// Mounted once inside the store workspace. Owns the close event, the per-user
// seen flag, the automatic first-after-close modal, and the on-demand opens
// dispatched by the two entry points.
function StoreCloseReportHost({ store }) {
  const [ev, setEv] = React.useState(() => crSyncEvent(store));
  const [open, setOpen] = React.useState(null);
  const [seenTick, setSeenTick] = React.useState(0);

  React.useEffect(() => { setEv(crSyncEvent(store)); }, [store?.id, store?.status]);

  // Automatic trigger: Closed + this user hasn't seen THIS close event yet.
  React.useEffect(() => {
    if (!ev || store?.status !== "closed") return;
    if (crIsSeen(store, ev)) return;
    const t = setTimeout(() => setOpen({ mode: "auto", layer: "summary" }), 350);
    return () => clearTimeout(t);
  }, [ev && ev.id, store?.status, seenTick]);

  React.useEffect(() => {
    const h = (e) => setOpen({ mode: "review", layer: (e.detail && e.detail.layer) || "summary" });
    window.addEventListener("closereport:open", h);
    return () => window.removeEventListener("closereport:open", h);
  }, []);

  // Prototype-only: the Tweaks panel fires this to replay the first-after-close
  // moment. window.__crCanReplay lets the panel disable itself when the store
  // isn't Closed.
  React.useEffect(() => {
    window.__crCanReplay = store?.status === "closed";
    const h = () => { if (store?.status === "closed") replay(); };
    window.addEventListener("closereport:replay", h);
    return () => { window.removeEventListener("closereport:replay", h); delete window.__crCanReplay; };
  }, [store?.status, store?.id]);

  const dismiss = () => { crMarkSeen(store, ev); setOpen(null); setSeenTick((n) => n + 1); };
  const replay = () => {
    const fresh = crSyncEvent(store);
    setEv(fresh);
    crClearSeen(store, fresh);
    setSeenTick((n) => n + 1);
    setOpen({ mode: "auto", layer: "summary" });
  };

  return (
    <React.Fragment>
      {open && <StoreCloseReportOverlay store={store} ev={ev} mode={open.mode} initialLayer={open.layer} onClose={dismiss} />}
    </React.Fragment>
  );
}

// ─────────── On-demand entry points ───────────
// variant "reports"  → top of the Reports tab (first tab in both store types)
// variant "settings" → Settings → Schedule & Status → Store Status card
function StoreCloseReportEntry({ store, variant }) {
  const available = crAvailable(store);
  const ev = available ? crSyncEvent(store) : crReadJSON(crEventKey(store), null);
  // Reports tab: only surface the entry once a close event has actually produced a report.
  if (variant !== "settings" && !(available && ev)) return null;
  const closeDate = ev?.closedAt || store?.storeOpenEnd;
  const windowStr = `${crDate(store?.storeOpenStart || "2026-02-03")} – ${crDate(closeDate || "2026-07-06")}`;

  if (variant === "settings") {
    return (
      <div style={{ marginTop: 24, paddingTop: 16, borderTop: "1px solid #ececf0" }}>
        <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".06em", color: "#0e0e10", marginBottom: 8 }}>STORE CLOSE REPORT</div>
        <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16 }}>
          <span style={{ fontSize: 13, color: "#6b6b72", lineHeight: 1.5 }}>
            {available
              ? `Every order and summary figure from the Active Order Window (${windowStr}).`
              : "Available when the store closes."}
          </span>
          <button onClick={available ? () => crOpenReport("summary") : undefined} disabled={!available} style={{
            flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8,
            background: "#fff", border: `1px solid ${available ? "#d4d4d8" : "#ececf0"}`, borderRadius: 6,
            padding: "12px 16px", fontSize: 13.5, fontWeight: 700, color: available ? "#0e0e10" : "#b6b6bd",
            cursor: available ? "pointer" : "not-allowed", fontFamily: "inherit",
          }}>
            <IconReceipt size={14} /> View Close Report
          </button>
        </div>
      </div>
    );
  }

  const accent = crAccent(store);
  return (
    <div style={{
      display: "flex", alignItems: "center", gap: 20, flexWrap: "wrap",
      background: "#fff", border: `1px solid ${available ? "#dcdce1" : CR_LINE}`, borderRadius: 12,
      padding: "18px 22px", marginBottom: 16, position: "relative", overflow: "hidden",
    }}>
      <span aria-hidden="true" style={{ position: "absolute", left: 0, top: 0, bottom: 0, width: 4, background: available ? accent : "#ececf0" }} />
      <span style={{ width: 40, height: 40, borderRadius: 10, flexShrink: 0, background: available ? "#f5f5f7" : "#fafafb", color: available ? CR_INK : "#c4c4cb", display: "inline-flex", alignItems: "center", justifyContent: "center", marginLeft: 6 }}>
        <IconReceipt size={19} />
      </span>
      <div style={{ minWidth: 220, flex: 1 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 4 }}>
          <span style={{ fontSize: 15.5, fontWeight: 800, color: available ? CR_INK : "#9a9aa1" }}>Store Close Report</span>
          {available && store?.status === "archived" && (
            <span style={{ fontSize: 10.5, fontWeight: 800, letterSpacing: ".06em", color: "#6b6b72", background: "#f1f1f3", borderRadius: 4, padding: "2px 8px" }}>READ-ONLY</span>
          )}
        </div>
        <div style={{ fontSize: 13, color: CR_MUTED, lineHeight: 1.5 }}>
          {available
            ? `Full recap of the Active Order Window — ${windowStr}. Summary figures, the complete order table, PDF and CSV export.`
            : "Available when the store closes. It recaps every order in the Active Order Window."}
        </div>
      </div>
      <button onClick={available ? () => crOpenReport("summary") : undefined} disabled={!available} style={{
        flexShrink: 0, display: "inline-flex", alignItems: "center", gap: 8,
        background: available ? CR_INK : "#f5f5f7", border: 0, borderRadius: 8,
        padding: "12px 20px", fontSize: 13.5, fontWeight: 700, color: available ? "#fff" : "#b6b6bd",
        cursor: available ? "pointer" : "not-allowed", fontFamily: "inherit",
      }}>
        View report <IconChevronRight size={15} />
      </button>
    </div>
  );
}

Object.assign(window, { StoreCloseReportHost, StoreCloseReportEntry, StoreCloseReportOverlay, crBuildReport, crRenderPdf, crMakeColumns });

/* ===== Store Setup Guide ===== */
// First-minute orientation modal. Reuses the Store Close Report shell verbatim —
// same chrome, proportions, and type scale; only the card payload differs.
// Variant (Individual 6 cards / League 7) is data-driven off storeType and
// overridable via prop/event so both can be reviewed side by side.
function sgSeenKey(store) { return "ts_setup_guide_seen_" + (store?.id || "store"); }
function sgStatusChip(store) {
  const live = !!store?.live;
  return live ? { label: "Open", dot: "#1f7a44" } : { label: "Draft · Not live yet", dot: "#9a9aa1" };
}
function sgCards(storeType) {
  const league = storeType === "league";
  return [
    { id: "cover", kind: "cover" },
    { id: "tabs", eyebrow: "The tabs", title: "Five tabs, five jobs.", lines: [
      ["Products", "everything the store can sell, visible or hidden."],
      ["Orders", "every order buyers place, in one table."],
      league ? ["My Teams", "your league's divisions and teams, each with its own roster."]
             : ["My Team", "your roster — the players this store serves."],
      ["Reports", "sales, fundraising, and profit numbers."],
      ["Settings", "schedule, contacts, and launch controls."],
    ] },
    { id: "products", eyebrow: "Products", title: "How a product reaches the storefront.", copy: "Add it, configure it, turn its visibility on, then launch the store. Hidden items stay in your catalog — they never show to buyers." },
    league ? { id: "org", eyebrow: "Divisions & teams", title: "How your league is organized.", copy: "Divisions sit under the league, and teams sit under divisions. Each team gets its own roster and team code. Products are assigned to specific teams, so a buyer only sees what their team ordered." } : null,
    { id: "gear", eyebrow: "Team gear", title: "Team Required vs. Team Supplied.", copy: "Required gear is bought by rostered players at their own cost. Supplied gear is already paid for — players just pick sizes and pay $0. Both file under Team Gear." },
    { id: "vault", eyebrow: "Stock Vault", title: "The Stock Vault vs. a bundle.", copy: "The Stock Vault is your library of blank stock gear. A bundle is buyer-facing — several catalog items sold together as one storefront line. A Catalog Template is a saved set of this store's own gear you can reuse in a later store." },
    { id: "live", eyebrow: "Going live", title: "Launching, closing, re-opening.", copy: "Launch controls live on the Settings tab, and a store needs at least one visible product to launch. You can close and re-open anytime." },
  ].filter(Boolean);
}
// 16:9 media slot — sized identically whether it holds a still or a looping
// clip, so media can drop in later without moving anything. Neutral for now.
function SgMediaSlot() {
  return (
    <div aria-hidden="true" style={{ width: "min(560px, 100%)", aspectRatio: "16 / 9", margin: "32px auto 0", borderRadius: 12, background: "#f5f5f7", border: "1px solid " + CR_LINE, display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", flexShrink: 0, color: "#c9c9cf" }}>
      <IconImage size={26} />
    </div>
  );
}
function SgCover({ store, reduce }) {
  const chip = sgStatusChip(store);
  return (
    <CrFade k="sg-cover" reduce={reduce} style={{ textAlign: "center", maxWidth: 640, margin: "auto" }}>
      <div style={{ display: "flex", justifyContent: "center", marginBottom: 22 }}><CrLogo store={store} size={64} radius={14} /></div>
      <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 42, lineHeight: 1.1, fontWeight: 800, letterSpacing: "-.02em", color: CR_INK, margin: "0 0 16px", textWrap: "pretty" }}>{store?.teamName || store?.storeTitle || "Your store"}</h2>
      <p style={{ fontSize: 16, lineHeight: 1.6, color: CR_MUTED, margin: "0 0 28px" }}>Your store is created. Here's how the workspace fits together.</p>
      <div style={{ display: "inline-flex", alignItems: "center", gap: 10, padding: "8px 16px", borderRadius: 999, background: "#f5f5f7", fontSize: 12.5, fontWeight: 600, color: CR_MUTED, ...CR_NUM }}>
        <span style={{ width: 7, height: 7, borderRadius: "50%", background: chip.dot }} />
        {chip.label}
      </div>
      <div style={{ marginTop: 28, fontSize: 13, color: "#9a9aa1" }}>Nothing here is required — you can reopen this guide anytime from the workspace header.</div>
    </CrFade>
  );
}
function SgCardView({ card, store, reduce }) {
  if (card.kind === "cover") return <SgCover store={store} reduce={reduce} />;
  return (
    <CrFade k={card.id} reduce={reduce} style={{ textAlign: "center", maxWidth: 720, width: "100%", margin: "auto" }}>
      <div style={{ fontSize: 13, fontWeight: 700, letterSpacing: ".14em", textTransform: "uppercase", color: CR_MUTED, marginBottom: 24 }}>{card.eyebrow}</div>
      <h2 style={{ fontFamily: "Archivo, sans-serif", fontSize: 40, lineHeight: 1.1, fontWeight: 800, letterSpacing: "-.02em", color: CR_INK, margin: "0 0 16px", textWrap: "balance" }}>{card.title}</h2>
      {card.lines ? (
        <div style={{ display: "flex", flexDirection: "column", gap: 6, maxWidth: 560, margin: "0 auto" }}>
          {card.lines.map((l) => (
            <p key={l[0]} style={{ fontSize: 15.5, lineHeight: 1.6, color: CR_MUTED, margin: 0 }}>
              <strong style={{ color: CR_INK, fontWeight: 700 }}>{l[0]}</strong> — {l[1]}
            </p>
          ))}
        </div>
      ) : (
        <p style={{ fontSize: 16, lineHeight: 1.6, color: CR_MUTED, margin: "0 auto", maxWidth: 560, textWrap: "pretty" }}>{card.copy}</p>
      )}
      <SgMediaSlot />
    </CrFade>
  );
}
function StoreSetupGuideOverlay({ store, storeType, onClose }) {
  const type = storeType || store?.storeType || "single";
  const cards = React.useMemo(() => sgCards(type), [type]);
  const accent = crAccent(store);
  const reduce = crPrefersReduced();
  const [step, setStep] = React.useState(0);
  const last = step >= cards.length - 1;
  const next = () => setStep((s) => Math.min(s + 1, cards.length - 1));
  const prev = () => setStep((s) => Math.max(s - 1, 0));
  React.useEffect(() => {
    const h = (e) => {
      if (e.key === "Escape") { onClose(); return; }
      if (e.key === "ArrowRight" || e.key === " " || e.key === "Enter") { e.preventDefault(); next(); }
      if (e.key === "ArrowLeft") { e.preventDefault(); prev(); }
    };
    window.addEventListener("keydown", h);
    return () => window.removeEventListener("keydown", h);
  }, [cards.length, onClose]);
  return (
    <div
      onMouseDown={(e) => { if (e.target === e.currentTarget) onClose(); }}
      style={{
        position: "fixed", inset: 0, zIndex: 4000, background: "rgba(9,9,11,.62)",
        backdropFilter: "blur(3px)", WebkitBackdropFilter: "blur(3px)",
        display: "flex", alignItems: "center", justifyContent: "center", padding: 24,
      }}>
      <div role="dialog" aria-modal="true" aria-label="Store Setup Guide" style={{
        width: "min(1040px, 94vw)", height: "min(760px, 94vh)", background: "#fff", borderRadius: 14,
        boxShadow: "0 30px 80px rgba(9,9,11,.34)", display: "flex", flexDirection: "column", overflow: "hidden",
      }}>
        {/* Top bar */}
        <div style={{ display: "flex", alignItems: "center", gap: 16, padding: "14px 20px", borderBottom: "1px solid " + CR_LINE, flexShrink: 0 }}>
          <div style={{ display: "flex", alignItems: "center", gap: 12, minWidth: 0 }}>
            <CrLogo store={store} size={30} radius={7} />
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 13.5, fontWeight: 800, color: CR_INK, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{store?.teamName || store?.storeTitle || "Your store"}</div>
              <div style={{ fontSize: 11.5, color: CR_MUTED }}>Store Setup Guide</div>
            </div>
          </div>
          <div style={{ marginLeft: "auto", display: "flex", alignItems: "center", gap: 10 }}>
            <button onClick={onClose} style={{
              display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 999,
              padding: "7px 14px", fontSize: 12.5, fontWeight: 700, color: CR_INK, cursor: "pointer", fontFamily: "inherit",
            }}>Skip to workspace</button>
            <button onClick={onClose} aria-label="Close guide" style={{ background: "transparent", border: 0, cursor: "pointer", padding: 6, color: "#9a9aa1", display: "inline-flex" }}><IconX size={20} /></button>
          </div>
        </div>
        {/* Segmented progress — position in the sequence, never completion */}
        <div style={{ display: "flex", gap: 6, padding: "12px 20px 0", flexShrink: 0 }}>
          {cards.map((c, i) => (
            <button key={c.id} onClick={() => setStep(i)} aria-label={"Card " + (i + 1)} style={{
              flex: 1, height: 4, borderRadius: 2, border: 0, padding: 0, cursor: "pointer",
              background: i <= step ? accent : "#ececf0", transition: "background .2s ease",
            }} />
          ))}
        </div>
        {/* Stage */}
        <div onClick={(e) => { if (e.target === e.currentTarget || e.currentTarget.contains(e.target)) { if (!last) next(); } }}
          style={{ flex: 1, display: "flex", padding: "24px 44px", cursor: last ? "default" : "pointer", overflowY: "auto" }}>
          <SgCardView card={cards[step] || cards[0]} store={store} reduce={reduce} />
        </div>
        {/* Footer controls */}
        <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "14px 20px", borderTop: "1px solid " + CR_LINE, flexShrink: 0 }}>
          <button onClick={prev} disabled={step === 0} style={{
            display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 8,
            padding: "10px 16px", fontSize: 13, fontWeight: 700, color: step === 0 ? "#b6b6bd" : CR_INK, cursor: step === 0 ? "not-allowed" : "pointer", fontFamily: "inherit",
          }}><IconChevronLeft size={15} /> Back</button>
          <span style={{ fontSize: 12.5, color: CR_MUTED, ...CR_NUM }}>{step + 1} / {cards.length}</span>
          <button onClick={() => setStep(cards.length - 1)} disabled={last} style={{
            background: "transparent", border: 0, fontSize: 12.5, fontWeight: 700, color: last ? "#b6b6bd" : CR_MUTED,
            cursor: last ? "default" : "pointer", fontFamily: "inherit", padding: "6px 4px",
          }}>Jump to end</button>
          <div style={{ marginLeft: "auto", display: "flex", alignItems: "center", gap: 12 }}>
            <button onClick={onClose} style={{ background: "transparent", border: 0, fontSize: 13, fontWeight: 700, color: CR_MUTED, cursor: "pointer", fontFamily: "inherit", padding: "10px 8px" }}>Done</button>
            {last ? (
              <button onClick={onClose} style={{
                display: "inline-flex", alignItems: "center", gap: 8, background: CR_INK, color: "#fff", border: 0, borderRadius: 8,
                padding: "11px 20px", fontSize: 13.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
              }}>Done</button>
            ) : (
              <button onClick={next} style={{
                display: "inline-flex", alignItems: "center", gap: 8, background: CR_INK, color: "#fff", border: 0, borderRadius: 8,
                padding: "11px 20px", fontSize: 13.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
              }}>Next <IconChevronRight size={15} /></button>
            )}
          </div>
        </div>
      </div>
    </div>
  );
}
// Reopen entry — lives in the workspace header, as the cover card promises.
function SetupGuideHeaderButton({ store }) {
  return (
    <button onClick={() => window.dispatchEvent(new CustomEvent("setupguide:open"))} title="Reopen the store setup guide" style={{
      display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #d4d4d8", borderRadius: 999,
      padding: "8px 14px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit",
    }}><IconInfo size={14} /> Setup guide</button>
  );
}
// Host — auto-opens once per store while it's still a draft (the just-created
// moment), then only on demand via the header button or Tweaks panel.
function StoreSetupGuideHost({ store }) {
  const [open, setOpen] = React.useState(null);
  React.useEffect(() => {
    if (!store || store.live) return;
    if (crReadJSON(sgSeenKey(store), false)) return;
    const t = setTimeout(() => setOpen({}), 400);
    return () => clearTimeout(t);
  }, [store?.id]);
  React.useEffect(() => {
    const h = (e) => setOpen({ storeType: e.detail && e.detail.storeType });
    window.addEventListener("setupguide:open", h);
    return () => window.removeEventListener("setupguide:open", h);
  }, []);
  const dismiss = () => { crWriteJSON(sgSeenKey(store), true); setOpen(null); };
  return open ? <StoreSetupGuideOverlay store={store} storeType={open.storeType} onClose={dismiss} /> : null;
}
Object.assign(window, { StoreSetupGuideHost, StoreSetupGuideOverlay, SetupGuideHeaderButton, sgCards });


/* ═══════════════ Products tab restructure (Aug 2026) ═══════════════
   1) Card catalog grouped by category sections with subcategory groups —
      drag to reorder sections, subcategories and products; categories are
      managed inline (the Categories & Sorting page is gone).
   2) Dedicated Product Detail sub-page — Product info / Sizes & color ways
      tabs — replaces the edit slide-out.
   3) Stock Vault (Prolook ProStock HCM blanks, real colorway renders):
      a Products sub-nav page per store + a workspace-level STOCK VAULT tab. */

// ── Vault stock data — Pro Stock (HCM) + Sideline Apparel (QXX), per the
// Vault Items doc. Product images on file are black colorways (front/back views). ──
const VAULT_COLOR_HEX = { "Black": "#1b1b1e", "White": "#f3f3f1", "Silver Gray": "#b9bcc2", "Forest Green": "#20463a", "Maroon": "#5e1f2d", "Red": "#c8102e", "Navy Blue": "#1c2a4a", "Royal Blue": "#2149c4", "Detroit Gray": "#5e6167", "Slate Gray": "#64748b", "Heather Gray": "#a9abae", "Khaki": "#b9a77c" };
function vaultItem(id, title, gender, cfg) {
  return {
    id, title, gender, youth: cfg.youth != null, priceAdult: cfg.adult, priceYouth: cfg.youth != null ? cfg.youth : null,
    sku: cfg.sku, retail: cfg.adult, base: cfg.adult, vaultCategory: cfg.cat, brand: "Prolook",
    source: cfg.stockType === "sideline" ? "Sideline Stock" : "ProStock", used: 0, stockType: cfg.stockType,
    colorways: cfg.colors.map((name) => { const slug = name.toLowerCase().replace(/ /g, "-"); return { name, slug, hex: VAULT_COLOR_HEX[name], front: cfg.cwDir ? cfg.cwDir + "/front-" + slug : "vault/" + cfg.img + "-front", back: cfg.cwDir ? cfg.cwDir + "/back-" + slug : "vault/" + cfg.img + "-back" }; }),
  };
}
const HCM_8 = ["Black", "Silver Gray", "Forest Green", "Maroon", "Red", "Navy Blue", "Royal Blue", "Detroit Gray"];
const HCM_9 = ["Black", "White", "Silver Gray", "Forest Green", "Maroon", "Red", "Navy Blue", "Royal Blue", "Detroit Gray"];
window.__prostockItems = window.__prostockItems || [
  // Pro Stock (HCM) — Men's
  vaultItem("hcm-scuba-m", "Scuba Hoodie", "Men", { stockType: "pro", cat: "Hoodies", sku: "HCM-SCB-M", img: "m-scuba-hoodie", cwDir: "hcm/lsh", colors: HCM_8, adult: 92, youth: 82.80 }),
  vaultItem("hcm-ss-scuba-m", "Short Sleeve Scuba Hoodie", "Men", { stockType: "pro", cat: "Hoodies", sku: "HCM-SSC-M", img: "m-ss-scuba-hoodie", cwDir: "hcm/ssh", colors: HCM_8, adult: 92, youth: 82.80 }),
  vaultItem("hcm-fz-scuba-m", "Full Zip Scuba Hoodie", "Men", { stockType: "pro", cat: "Hoodies", sku: "HCM-FZS-M", img: "m-fz-scuba-hoodie", colors: ["Black", "Detroit Gray", "Navy Blue"], adult: 86, youth: 79 }),
  vaultItem("hcm-rls-m", "Raglan Long Sleeve", "Men", { stockType: "pro", cat: "Raglans", sku: "HCM-RLS-M", img: "m-raglan-ls", cwDir: "hcm/rls", colors: HCM_9, adult: 36.80, youth: 34.50 }),
  vaultItem("hcm-rss-m", "Raglan Short Sleeve", "Men", { stockType: "pro", cat: "Raglans", sku: "HCM-RSS-M", img: "m-raglan-ss", cwDir: "hcm/rss", colors: HCM_9, adult: 32.20, youth: 29.90 }),
  vaultItem("hcm-woven-m", "Woven Short", "Men", { stockType: "pro", cat: "Bottoms", sku: "HCM-WVS-M", img: "m-woven-short", colors: ["Black", "Detroit Gray"], adult: 43.70, youth: 40.25 }),
  vaultItem("hcm-jogger-m", "Tapered Jogger", "Men", { stockType: "pro", cat: "Bottoms", sku: "HCM-TPJ-M", img: "m-tapered-jogger", colors: ["Black", "Detroit Gray", "Navy Blue"], adult: 92, youth: 82.80 }),
  // Pro Stock (HCM) — Women's
  vaultItem("hcm-scuba-w", "Scuba Hoodie", "Women", { stockType: "pro", cat: "Hoodies", sku: "HCM-SCB-W", img: "w-scuba-hoodie", cwDir: "hcm/scb-w", colors: HCM_8, adult: 92, youth: 82.80 }),
  vaultItem("hcm-fz-scuba-w", "Full Zip Scuba Hoodie", "Women", { stockType: "pro", cat: "Hoodies", sku: "HCM-FZS-W", img: "w-fz-scuba-hoodie", colors: ["Black", "Detroit Gray", "Navy Blue"], adult: 86, youth: 79 }),
  vaultItem("hcm-rls-w", "Raglan Long Sleeve", "Women", { stockType: "pro", cat: "Raglans", sku: "HCM-RLS-W", img: "w-raglan-ls", cwDir: "hcm/rls-w", colors: HCM_9, adult: 36.80, youth: 34.50 }),
  vaultItem("hcm-rss-w", "Raglan Short Sleeve", "Women", { stockType: "pro", cat: "Raglans", sku: "HCM-RSS-W", img: "w-raglan-ss", cwDir: "hcm/rss-w", colors: HCM_9, adult: 32.20, youth: 29.90 }),
  vaultItem("hcm-woven-w", "Woven Short", "Women", { stockType: "pro", cat: "Bottoms", sku: "HCM-WVS-W", img: "w-woven-short", colors: ["Black", "Detroit Gray"], adult: 43.70, youth: 40.25 }),
  vaultItem("hcm-jogger-w", "Tapered Jogger", "Women", { stockType: "pro", cat: "Bottoms", sku: "HCM-TPJ-W", img: "w-tapered-jogger", colors: ["Black", "Detroit Gray", "Navy Blue"], adult: 92 }),
  // Sideline Apparel (QXX) — Men's
  vaultItem("qxx-qz-m", "1/4-Zip Sideline Pullover", "Men", { stockType: "sideline", cat: "Outerwear", sku: "QXX-14Z-M", img: "m-qz-pullover", cwDir: "qxx/qz-m", colors: ["Black", "Heather Gray", "Red", "Royal Blue", "Navy Blue"], adult: 81.78 }),
  vaultItem("qxx-polo-m", "Sideline Polo", "Men", { stockType: "sideline", cat: "Polos", sku: "QXX-PLO-M", img: "m-sideline-polo", cwDir: "qxx/polo-m", colors: ["White", "Black", "Red", "Royal Blue", "Navy Blue", "Slate Gray"], adult: 74.11 }),
  vaultItem("qxx-pant-m", "Sideline Pant", "Men", { stockType: "sideline", cat: "Bottoms", sku: "QXX-PNT-M", img: "m-sideline-pant", colors: ["Black", "Silver Gray", "Khaki"], adult: 102.35 }),
  vaultItem("qxx-short-m", "Sideline Short", "Men", { stockType: "sideline", cat: "Bottoms", sku: "QXX-SHT-M", img: "m-sideline-short", colors: ["Black", "Silver Gray", "Khaki"], adult: 89.44 }),
  // Sideline Apparel (QXX) — Women's
  vaultItem("qxx-qz-w", "1/4-Zip Sideline Pullover", "Women", { stockType: "sideline", cat: "Outerwear", sku: "QXX-14Z-W", img: "w-qz-pullover", cwDir: "qxx/qz-w", colors: ["Black", "Heather Gray", "Red", "Royal Blue", "Navy Blue"], adult: 81.78 }),
  vaultItem("qxx-legging-w", "Stock Legging", "Women", { stockType: "sideline", cat: "Leggings", sku: "QXX-LEG-W", img: "w-stock-legging", cwDir: "qxx/leg-w", colors: ["Black", "Red", "Royal Blue", "Navy Blue"], adult: 58.78, youth: 58.78 }),
];
function vaultGenderLine(it) { return it.gender ? it.gender + (it.youth ? "/Youth" : "") : ""; }
function vaultPriceFmt(n) { return "$" + (Number(n) % 1 ? Number(n).toFixed(2) : String(Number(n))); }
function vaultPriceLine(it) {
  if (it.priceAdult == null) return "";
  return it.priceYouth != null ? "Adult " + vaultPriceFmt(it.priceAdult) + " · Youth " + vaultPriceFmt(it.priceYouth) : "Adult " + vaultPriceFmt(it.priceAdult);
}
// Catalog Templates — branded snapshots saved from a store's catalog (Products
// tab → Select → Create Catalog Template). Members arrive in a destination
// store wearing the origin store's artwork; locked members (source: Saved
// Design, Saved Cart, or Customize) can never be re-branded, stock members can
// via Edit artwork. Pricing, team setting, sizes, category and team assignment
// are re-confirmed per item on deploy — only artwork carries.
window.__catalogTemplates = window.__catalogTemplates || [
  { id: "tpl-grizzlies-2025", name: "Grizzlies Basketball 2025\u201326", originStore: "Grizzlies Basketball", members: [
    { id: "m-gz-jersey", title: "Home Jersey", styleBlock: "SFN Lite, Round Neck Jersey — Grizzlies", photo: "jersey-grizzlies", sku: "SFN-JR-100", price: 89.00, locked: true },
    { id: "m-gz-short", title: "Team Shorts", styleBlock: "Performance Mesh Short — Green", photo: "shorts-green", sku: "MSH-SH-200", price: 46.00, locked: false },
    { id: "m-gz-warm", title: "Warm-Up Jacket", styleBlock: "Full-Zip Travel Jacket — Grizzlies", photo: "jersey-lancers", sku: "WU-JK-300", price: 78.00, locked: true },
  ] },
  { id: "tpl-padres-spring", name: "Padres Spring Season", originStore: "Padres Baseball", members: [
    { id: "m-pd-home", title: "Home Jersey", styleBlock: "Set-in Faux, Full-Button Jersey — Padres", photo: "jersey-padres", sku: "SIF-JR-410", price: 92.00, locked: true },
    { id: "m-pd-pant", title: "Team Pants", styleBlock: "Pro-Cut Knit Bottom — Navy", photo: "pants-navy", sku: "PCK-PT-430", price: 54.00, locked: false },
  ] },
];
function hcmDefaultCw(item) {
  return item.colorways.find((c) => c.slug === "black") || item.colorways[0];
}
function vaultPricing(baseV, retailV) {
  const b = parseFloat(baseV) || 0, r = parseFloat(retailV) || 0;
  const dealer = Math.max(0, r - b);
  const tier = { base: b.toFixed(2), dealerUnit: "$", dealer: dealer.toFixed(2), fundUnit: "$", fund: "0.00", retail: (b + dealer).toFixed(2) };
  return { youth: { ...tier }, adult: { ...tier } };
}

// Deterministic order stats for the detail page (prototype data).
function productOrderStats(p, store) {
  if (!pteHasOrders(p, store)) return null;
  let h = 5381; const s = String(p.id || "");
  for (let i = 0; i < s.length; i++) h = ((h * 33) ^ s.charCodeAt(i)) >>> 0;
  const orders = 3 + (h % 9);
  const units = orders + ((h >> 3) % 14);
  const price = pteRetail(p, "adult") || pteRetail(p, "youth") || 32;
  return { orders, units, revenue: units * price };
}

const catalogGripDots = (
  <svg width="12" height="14" viewBox="0 0 12 14" fill="currentColor" aria-hidden="true">
    <circle cx="3.5" cy="2.5" r="1.3" /><circle cx="8.5" cy="2.5" r="1.3" /><circle cx="3.5" cy="7" r="1.3" /><circle cx="8.5" cy="7" r="1.3" /><circle cx="3.5" cy="11.5" r="1.3" /><circle cx="8.5" cy="11.5" r="1.3" />
  </svg>
);
const subcatDragIcon = (
  <svg width="14" height="10" viewBox="0 0 14 10" fill="none" stroke="currentColor" strokeWidth="1.6" strokeLinecap="round" aria-hidden="true">
    <path d="M1 3h12M1 7h12" />
  </svg>
);

// ── Vault item card (shared by store sub-page + workspace page) ──
function ProStockItemCard({ item, onClick, selectable, selected, onToggleSelect, selectMode }) {
  const [hover, setHover] = React.useState(false);
  const cw = hcmDefaultCw(item);
  const showBox = selectable && (hover || selected || selectMode);
  const activate = () => { if (selectMode && onToggleSelect) onToggleSelect(item); else onClick(item); };
  return (
    <div role="button" tabIndex={0}
      onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}
      onClick={activate}
      onKeyDown={(e) => { if (e.key === "Enter") activate(); }}
      style={{
        border: "1px solid " + (selected ? "#0e0e10" : hover ? "#c7c7cd" : "#ececf0"), borderRadius: 10, background: "#fff", cursor: "pointer",
        overflow: "hidden", transition: "border-color .14s, box-shadow .14s", boxShadow: selected ? "0 0 0 1px #0e0e10" : hover ? "0 6px 18px rgba(20,20,30,.08)" : "none",
      }}>
      <div style={{ position: "relative", margin: 12, marginBottom: 0, background: "#f7f7f8", borderRadius: 6, height: 196, display: "flex", alignItems: "center", justifyContent: "center", padding: 10 }}>
        {showBox && (
          <button type="button" aria-label={selected ? "Deselect item" : "Select item"} title={selected ? "Deselect" : "Select for a new store"}
            onClick={(e) => { e.stopPropagation(); onToggleSelect && onToggleSelect(item); }}
            style={{ position: "absolute", top: 8, left: 8, width: 22, height: 22, borderRadius: 6, cursor: "pointer", padding: 0, display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 12, fontWeight: 800, fontFamily: "inherit", border: "1.5px solid " + (selected ? "#0e0e10" : "#b6b6bd"), background: selected ? "#0e0e10" : "#fff", color: selected ? "#fff" : "transparent" }}>✓</button>
        )}
        <div style={{ width: 168, height: 176 }}><CatalogPhoto photo={cw.front} alt={item.title} /></div>
      </div>
      <div style={{ padding: "14px 16px 16px" }}>
        <div style={{ fontSize: 14.5, fontWeight: 800, color: "#0e0e10", letterSpacing: ".01em" }}>{item.title}</div>
        <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 4 }}>{vaultGenderLine(item)} · {item.colorways.length} color way{item.colorways.length === 1 ? "" : "s"}</div>
        <div style={{ fontSize: 12, color: "#3f3f46", marginTop: 3, fontVariantNumeric: "tabular-nums" }}>{vaultPriceLine(item)}</div>
      </div>
    </div>
  );
}

// Interstitial when adding a Pro Stock / Sideline item to a store: pick the
// sizes and color ways the store will offer (multi-select each).
const VAULT_OFFER_ADULT = ["S", "M", "L", "XL", "2XL", "3XL"];
const VAULT_OFFER_YOUTH = ["YS", "YM", "YL"];
function VaultOfferModal({ item, onClose, onConfirm }) {
  const sizes = (item.youth ? VAULT_OFFER_YOUTH : []).concat(VAULT_OFFER_ADULT);
  const [selSizes, setSelSizes] = React.useState(() => { const o = {}; sizes.forEach((s) => { o[s] = true; }); return o; });
  const [selCws, setSelCws] = React.useState(() => { const o = {}; item.colorways.forEach((c) => { o[c.slug] = true; }); return o; });
  const sizeList = sizes.filter((s) => selSizes[s]);
  const cwListSel = item.colorways.filter((c) => selCws[c.slug]);
  const allCw = cwListSel.length === item.colorways.length;
  const allSizes = sizeList.length === sizes.length;
  const ready = sizeList.length > 0 && cwListSel.length > 0;
  return (
    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 700, padding: 24 }}>
      <div onClick={(e) => e.stopPropagation()} style={{ width: 640, maxWidth: "100%", maxHeight: "calc(100vh - 48px)", display: "flex", flexDirection: "column", background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.18)", overflow: "hidden" }}>
        <div style={{ padding: "16px 20px", borderBottom: "1px solid #e4e4e7", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, flexShrink: 0 }}>
          <span style={{ fontSize: 15, fontWeight: 700, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>{item.title} — sizes & color ways</span>
          <button type="button" onClick={onClose} aria-label="Close" style={{ border: 0, background: "none", color: "#71717a", fontSize: 14, cursor: "pointer", fontFamily: "inherit" }}>✕</button>
        </div>
        <div style={{ padding: 20, overflowY: "auto", minHeight: 0, flex: 1 }}>
          <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 10 }}>
            <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1" }}>SIZES OFFERED</span>
            <span style={{ fontSize: 11.5, color: "#71717a" }}>
              {sizeList.length} of {sizes.length} sizes
              <button type="button" onClick={() => setSelSizes(() => { const o = {}; if (!allSizes) sizes.forEach((s) => { o[s] = true; }); return o; })} style={{ background: "none", border: 0, padding: 0, marginLeft: 10, fontSize: 11.5, fontWeight: 700, color: "#0e0e10", textDecoration: "underline", cursor: "pointer", fontFamily: "inherit" }}>{allSizes ? "Deselect all" : "Select all"}</button>
            </span>
          </div>
          <div style={{ display: "flex", gap: 8, flexWrap: "wrap", marginBottom: 20 }}>
            {sizes.map((s) => {
              const on = !!selSizes[s];
              return (
                <button key={s} type="button" aria-pressed={on} onClick={() => setSelSizes((m) => ({ ...m, [s]: !m[s] }))} style={{ minWidth: 56, padding: "12px 12px", borderRadius: 10, cursor: "pointer", fontFamily: "inherit", fontSize: 13.5, fontWeight: 700, display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 7, background: on ? "#0e0e10" : "#fff", color: on ? "#fff" : "#09090b", border: "1.5px solid " + (on ? "#0e0e10" : "#d4d4d8"), boxShadow: on ? "0 0 0 3px rgba(14,14,16,.14)" : "none", transition: "all .12s" }}>{on && <span aria-hidden="true" style={{ display: "inline-flex", alignItems: "center", justifyContent: "center", width: 15, height: 15, borderRadius: 999, background: "#fff", color: "#0e0e10", fontSize: 9.5, fontWeight: 800 }}>✓</span>}{s}</button>
              );
            })}
          </div>
          <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 10 }}>
            <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1" }}>COLOR WAYS OFFERED</span>
            <span style={{ fontSize: 11.5, color: "#71717a" }}>
              {cwListSel.length} of {item.colorways.length} selected
              <button type="button" onClick={() => setSelCws(() => { const o = {}; if (!allCw) item.colorways.forEach((c) => { o[c.slug] = true; }); return o; })} style={{ background: "none", border: 0, padding: 0, marginLeft: 10, fontSize: 11.5, fontWeight: 700, color: "#0e0e10", textDecoration: "underline", cursor: "pointer", fontFamily: "inherit" }}>{allCw ? "Deselect all" : "Select all"}</button>
            </span>
          </div>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 8 }}>
            {item.colorways.map((c) => {
              const on = !!selCws[c.slug];
              return (
                <button key={c.slug} type="button" title={c.name} onClick={() => setSelCws((m) => ({ ...m, [c.slug]: !m[c.slug] }))} style={{ position: "relative", display: "flex", flexDirection: "column", alignItems: "center", gap: 6, cursor: "pointer", background: on ? "#fafafa" : "#fff", border: "1px solid " + (on ? "#09090b" : "#e4e4e7"), borderRadius: 8, padding: "10px 6px 8px", fontFamily: "inherit" }}>
                  <span aria-hidden="true" style={{ position: "absolute", top: 6, right: 6, width: 16, height: 16, borderRadius: 999, display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 9, fontWeight: 800, border: "1px solid " + (on ? "#09090b" : "#d4d4d8"), background: on ? "#09090b" : "#fff", color: on ? "#fff" : "transparent" }}>✓</span>
                  <span style={{ width: 56, height: 62, display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden" }}><CatalogPhoto photo={c.front} alt={c.name} /></span>
                  <span style={{ display: "inline-flex", alignItems: "center", gap: 5, maxWidth: "100%" }}>
                    <span style={{ width: 10, height: 10, borderRadius: "50%", background: c.hex, flexShrink: 0, border: c.slug === "white" ? "1px solid #d4d4d8" : "1px solid rgba(9,9,11,.12)" }} />
                    <span style={{ fontSize: 10.5, fontWeight: 700, color: "#09090b", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{c.name}</span>
                  </span>
                </button>
              );
            })}
          </div>
        </div>
        <div style={{ padding: "14px 20px", borderTop: "1px solid #e4e4e7", background: "#fafafa", display: "flex", justifyContent: "flex-end", gap: 10, flexShrink: 0 }}>
          <button type="button" onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, color: "#3f3f46", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
          <button type="button" disabled={!ready} onClick={() => { if (ready) onConfirm(sizeList, cwListSel); }} style={{ background: ready ? "#0e0e10" : "#e4e4e7", color: ready ? "#fff" : "#a1a1aa", border: 0, borderRadius: 6, padding: "9px 18px", fontSize: 12.5, fontWeight: 700, cursor: ready ? "pointer" : "default", fontFamily: "inherit" }}>Continue</button>
        </div>
      </div>
    </div>
  );
}

// The "SELECT AN ITEM" panel from the reference.
function VaultSelectPanel({ items, onSelect, selectable, selected, onToggle }) {
  const selectMode = !!selectable && items.some((it) => selected && selected[it.id]);
  const groups = [
    { k: "pro-Men", label: "Pro Stock (HCM) — Men's" },
    { k: "pro-Women", label: "Pro Stock (HCM) — Women's" },
    { k: "sideline-Men", label: "Sideline Apparel (QXX) — Men's" },
    { k: "sideline-Women", label: "Sideline Apparel (QXX) — Women's" },
  ].map((g) => ({ ...g, items: items.filter((it) => it.stockType + "-" + (it.gender || "Men") === g.k) })).filter((g) => g.items.length > 0);
  const grid = (list) => (
    <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(250px, 1fr))", gap: 16 }}>
      {list.map((it) => <ProStockItemCard key={it.id} item={it} onClick={onSelect} selectable={selectable} selected={!!(selected && selected[it.id])} onToggleSelect={onToggle} selectMode={selectMode} />)}
    </div>
  );
  return (
    <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 12, padding: "18px 20px 20px", boxShadow: "0 1px 3px rgba(20,20,30,.04)" }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, marginBottom: 16 }}>
        <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".1em", color: "#9a9aa1", textTransform: "uppercase" }}>Select an item</span>
        <span style={{ fontSize: 12, color: "#9a9aa1" }}>{items.length} stock item{items.length === 1 ? "" : "s"} on file{selectable ? " · select several to start a new store" : ""}</span>
      </div>
      {groups.length === 0 ? grid(items) : groups.map((g, i) => (
        <div key={g.k} style={{ marginTop: i === 0 ? 0 : 26 }}>
          <div style={{ fontSize: 12, fontWeight: 800, letterSpacing: ".04em", color: "#3f3f46", marginBottom: 10 }}>{g.label}</div>
          {grid(g.items)}
        </div>
      ))}
    </div>
  );
}

// Store-level Products → Stock Vault sub-page ("Team Artwork").
function StoreVaultPage({ store, onOpenItem }) {
  const items = window.__prostockItems || [];
  return (
    <div style={{ paddingBottom: 60 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 8 }}>
          <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.3px", margin: 0, fontFamily: "Archivo, sans-serif", color: "#0e0e10" }}>Stock Vault</h2>
          <span style={{ fontSize: 10, fontWeight: 800, letterSpacing: ".08em", color: "#6b6b72", background: "#fff", border: "1px solid #e4e4e7", borderRadius: 999, padding: "4px 10px" }}>THIS STORE ONLY</span>
        </div>
        <p style={{ fontSize: 14, color: "#52525b", margin: "0 0 20px", lineHeight: 1.6, maxWidth: 720 }}>
          Pick the color ways and sizes this store offers for the item, then place your logo once and copy it across every color way.
        </p>
      <VaultSelectPanel items={items} onSelect={onOpenItem} />
    </div>
  );
}

// "Add to a store…" picker used by the workspace-level Vault page. multi turns the
// list into a multi-select (used from the Artwork Editor's "Add to store(s)").
function ProStockDeployModal({ item, items, stores, onClose, onConfirm, onOpenEditor, multi, title, z, initialPicks }) {
  const batch = !!(items && items.length > 1);
  const one = item || (items && items[0]);
  const [q, setQ] = React.useState("");
  const [picks, setPicks] = React.useState(() => initialPicks || {});
  const [ddOpen, setDdOpen] = React.useState(false);
  const ddRef = React.useRef(null);
  React.useEffect(() => {
    const onDoc = (e) => { if (ddRef.current && !ddRef.current.contains(e.target)) setDdOpen(false); };
    document.addEventListener("mousedown", onDoc);
    return () => document.removeEventListener("mousedown", onDoc);
  }, []);
  const [ci, setCi] = React.useState(() => Math.max(0, one.colorways.indexOf(hcmDefaultCw(one))));
  const cw = one.colorways[ci];
  const [selCws, setSelCws] = React.useState(() => { const o = {}; one.colorways.forEach((c) => { o[c.slug] = true; }); return o; });
  const cwList = one.colorways.filter((c) => selCws[c.slug]);
  const allOn = cwList.length === one.colorways.length;
  const toggleCw = (c, i) => setSelCws((m) => {
    const on = !m[c.slug];
    const next = { ...m, [c.slug]: on };
    if (on) setCi(i);
    else if (i === ci) { const rest = one.colorways.findIndex((x) => next[x.slug]); if (rest >= 0) setCi(rest); }
    return next;
  });
  const list = (stores || []).filter((s) => s.kind !== "fan" && s.status !== "archived").filter((s) => {
    const t = (q || "").trim().toLowerCase();
    return !t || [s.storeTitle, s.teamName, s.sport].filter(Boolean).some((x) => String(x).toLowerCase().includes(t));
  });
  const allStores = (stores || []).filter((s) => s.kind !== "fan" && s.status !== "archived");
  const picked = allStores.filter((s) => picks[s.id]);
  const ready = (batch || cwList.length > 0) && picked.length > 0;
  const confirmLabel = picked.length ? "Add to Store (" + picked.length + ")" : "Add to Store";
  return (
    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: z || 600, padding: 24 }}>
      <div onClick={(e) => e.stopPropagation()} style={{ width: 560, maxWidth: "100%", maxHeight: "calc(100vh - 48px)", display: "flex", flexDirection: "column", background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.18)", overflow: "hidden" }}>
        <div style={{ padding: "16px 20px", borderBottom: "1px solid #e4e4e7", display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, flexShrink: 0 }}>
          <span style={{ fontSize: 15, fontWeight: 700, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>{title || "Add to Store"}</span>
          <button type="button" onClick={onClose} aria-label="Close" style={{ border: 0, background: "none", color: "#71717a", fontSize: 14, cursor: "pointer", fontFamily: "inherit" }}>✕</button>
        </div>
        <div style={{ padding: 20, overflowY: "auto", minHeight: 0, flex: 1 }}>
          {batch ? (
            <div style={{ border: "1px solid #e4e4e7", borderRadius: 8, marginBottom: 14, background: "#fafafa", maxHeight: 170, overflowY: "auto", padding: "5px 12px" }}>
              {items.map((it) => { const c = hcmDefaultCw(it); return (
                <div key={it.id} style={{ display: "flex", alignItems: "center", gap: 10, padding: "6px 0" }}>
                  <span style={{ width: 34, height: 34, borderRadius: 6, background: "#fff", border: "1px solid #f0f0f2", display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 2, flexShrink: 0 }}>
                    <CatalogPhoto photo={c && c.front} alt={it.title} />
                  </span>
                  <span style={{ minWidth: 0, flex: 1 }}>
                    <span style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#09090b", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{it.title}</span>
                    <span style={{ display: "block", fontSize: 11, color: "#71717a", marginTop: 1 }}>{it.sku}{c ? " · " + c.name : ""} · {it.source}</span>
                  </span>
                </div>
              ); })}
            </div>
          ) : (
          <div style={{ border: "1px solid #e4e4e7", borderRadius: 10, marginBottom: 12, background: "#fafafa", overflow: "hidden" }}>
            <div style={{ height: 210, display: "flex", alignItems: "center", justifyContent: "center", padding: "16px 16px 8px" }}>
              <CatalogPhoto photo={cw.front} alt={one.title + " — " + cw.name} />
            </div>
            <div style={{ padding: "8px 14px 14px", textAlign: "center" }}>
              <span style={{ display: "block", fontSize: 14, fontWeight: 700, color: "#09090b" }}>{one.title}</span>
              <span style={{ display: "block", fontSize: 11.5, color: "#71717a", marginTop: 2 }}>{one.sku} · {cw.name} · {one.source}</span>
            </div>
          </div>
          )}
          {!batch && <div style={{ marginBottom: 14 }}>
            <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 8 }}>
              <span style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1" }}>COLOR WAYS OFFERED</span>
              <span style={{ fontSize: 11.5, color: "#71717a" }}>
                {cwList.length} of {one.colorways.length} selected
                <button type="button" onClick={() => setSelCws(() => { const o = {}; if (allOn) o[cw.slug] = true; else one.colorways.forEach((c) => { o[c.slug] = true; }); return o; })}
                  style={{ background: "none", border: 0, padding: 0, marginLeft: 10, fontSize: 11.5, fontWeight: 700, color: "#0e0e10", textDecoration: "underline", cursor: "pointer", fontFamily: "inherit" }}>
                  {allOn ? "Clear" : "Select all"}
                </button>
              </span>
            </div>
            <div style={{ display: "grid", gridTemplateColumns: "repeat(4, 1fr)", gap: 8 }}>
              {one.colorways.map((c, i) => {
                const on = !!selCws[c.slug];
                return (
                  <button key={c.slug} type="button" title={c.name} onClick={() => toggleCw(c, i)} style={{
                    position: "relative", display: "flex", flexDirection: "column", alignItems: "center", gap: 6, cursor: "pointer",
                    background: on ? "#fafafa" : "#fff", border: "1px solid " + (on ? "#09090b" : "#e4e4e7"), borderRadius: 8, padding: "10px 6px 8px", fontFamily: "inherit",
                  }}>
                    <span aria-hidden="true" style={{ position: "absolute", top: 6, right: 6, width: 16, height: 16, borderRadius: 999, display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 9, fontWeight: 800, border: "1px solid " + (on ? "#09090b" : "#d4d4d8"), background: on ? "#09090b" : "#fff", color: on ? "#fff" : "transparent" }}>✓</span>
                    <span style={{ width: 52, height: 56, display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden" }}>
                      <CatalogPhoto photo={c.front} alt={c.name} />
                    </span>
                    <span style={{ display: "inline-flex", alignItems: "center", gap: 5, maxWidth: "100%" }}>
                      <span style={{ width: 10, height: 10, borderRadius: "50%", background: c.hex, flexShrink: 0, border: c.slug === "white" ? "1px solid #d4d4d8" : "1px solid rgba(9,9,11,.12)" }} />
                      <span style={{ fontSize: 10.5, fontWeight: 700, color: "#09090b", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{c.name}</span>
                    </span>
                  </button>
                );
              })}
            </div>
          </div>}
          <div style={{ fontSize: 12, color: "#71717a", lineHeight: 1.55, marginBottom: 12 }}>
            {batch
              ? "Pick one or more stores — each item lands in every store's catalog in its default color way; pricing, team setting, sizes and roster assignment are confirmed per store."
              : multi
              ? "Pick one or more stores — the item lands in each store's catalog; pricing, team setting, sizes and roster assignment are confirmed per store."
              : "The item lands in the store's catalog — pricing, team setting, sizes and roster assignment are confirmed there."}
          </div>
          {picked.length > 0 && (
            <div style={{ display: "flex", flexWrap: "wrap", gap: 6, marginBottom: 10 }}>
              {picked.map((s) => (
                <span key={s.id} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: "#f4f4f5", border: "1px solid #e4e4e7", borderRadius: 999, padding: "5px 6px" }}>
                  <span style={{ width: 18, height: 18, borderRadius: 5, background: (s.teamColors && s.teamColors[0]) || "#09090b", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 8, fontWeight: 800 }}>{String(s.storeTitle || s.teamName || "S").slice(0, 2).toUpperCase()}</span>
                  <span style={{ fontSize: 12, fontWeight: 700, color: "#09090b", maxWidth: 160, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s.storeTitle || s.teamName}</span>
                  <button type="button" aria-label={"Remove " + (s.storeTitle || s.teamName)} onClick={() => setPicks((mm) => ({ ...mm, [s.id]: false }))} style={{ border: 0, background: "none", padding: "0 4px 0 0", color: "#71717a", fontSize: 12, cursor: "pointer", fontFamily: "inherit", lineHeight: 1 }}>✕</button>
                </span>
              ))}
            </div>
          )}
          <div ref={ddRef} style={{ position: "relative" }}>
            <input value={q} onChange={(e) => { setQ(e.target.value); setDdOpen(true); }} onFocus={() => setDdOpen(true)} placeholder={picked.length ? "Add another store" : "Search your stores"}
              style={{ width: "100%", height: 38, boxSizing: "border-box", border: "1px solid " + (ddOpen ? "#0e0e10" : "#d4d4d8"), borderRadius: 6, padding: "0 30px 0 12px", fontSize: 13, fontFamily: "inherit", outline: "none", display: "block" }} />
            <span aria-hidden="true" style={{ position: "absolute", right: 12, top: 13, pointerEvents: "none", color: "#71717a", fontSize: 9, display: "inline-block", transform: ddOpen ? "rotate(180deg)" : "none" }}>▼</span>
            {ddOpen && (
              <div style={{ position: "absolute", top: 42, left: 0, right: 0, zIndex: 20, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 8, boxShadow: "0 12px 28px rgba(9,9,11,.14)", maxHeight: 224, overflowY: "auto", padding: 6 }}>
                {list.length === 0 && <div style={{ fontSize: 12.5, color: "#a1a1aa", padding: "10px 8px" }}>No stores match “{q}”.</div>}
                {list.map((s) => {
                  const on = !!picks[s.id];
                  return (
                    <button key={s.id} type="button" onClick={() => setPicks((mm) => ({ ...mm, [s.id]: !mm[s.id] }))} style={{ display: "flex", alignItems: "center", gap: 11, width: "100%", textAlign: "left", cursor: "pointer", background: on ? "#fafafa" : "#fff", border: 0, borderRadius: 6, padding: "9px 10px", fontFamily: "inherit" }}>
                      <span style={{ width: 26, height: 26, borderRadius: 7, flexShrink: 0, background: (s.teamColors && s.teamColors[0]) || "#09090b", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 10, fontWeight: 800 }}>{String(s.storeTitle || s.teamName || "S").slice(0, 2).toUpperCase()}</span>
                      <span style={{ minWidth: 0, flex: 1 }}>
                        <span style={{ display: "block", fontSize: 12.5, fontWeight: 700, color: "#09090b", overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>{s.storeTitle || s.teamName}</span>
                        <span style={{ display: "block", fontSize: 11, color: "#71717a", marginTop: 1 }}>{s.teamName || "—"} · {s.status === "closed" ? "Closed" : s.live ? "Active" : "Draft"} · {(s.products || []).length} products</span>
                      </span>
                      <span aria-hidden="true" style={{ width: 18, height: 18, borderRadius: 5, flexShrink: 0, display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 10, fontWeight: 800, border: "1px solid " + (on ? "#09090b" : "#d4d4d8"), background: on ? "#09090b" : "#fff", color: on ? "#fff" : "transparent" }}>✓</span>
                    </button>
                  );
                })}
              </div>
            )}
          </div>
          <div style={{ height: ddOpen ? 236 : 8 }} />
        </div>
        <div style={{ padding: "14px 20px", borderTop: "1px solid #e4e4e7", background: "#fafafa", display: "flex", justifyContent: "flex-end", gap: 10, flexShrink: 0 }}>
          {onOpenEditor && (
            <button type="button" onClick={() => onOpenEditor(one, selCws[cw.slug] ? cw : (cwList[0] || cw), cwList, picked)} style={{ marginRight: "auto", background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "9px 14px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Customize</button>
          )}
          <button type="button" onClick={onClose} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, color: "#3f3f46", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
          <button type="button" disabled={!ready} onClick={() => { if (!ready) return; const cwPrimary = selCws[cw.slug] ? cw : (cwList[0] || cw); onConfirm(picked, cwPrimary, cwList); }} style={{ background: ready ? "#0e0e10" : "#e4e4e7", color: ready ? "#fff" : "#a1a1aa", border: 0, borderRadius: 6, padding: "9px 18px", fontSize: 12.5, fontWeight: 700, cursor: ready ? "pointer" : "default", fontFamily: "inherit" }}>
            {confirmLabel}
          </button>
        </div>
      </div>
    </div>
  );
}

// Workspace-level Stock Vault page (OVERVIEW / STORES / STOCK VAULT nav).
function ProStockVaultPage({ stores, onDeployToStore, onDeployToStores, onStartStore }) {
  const [deploy, setDeploy] = React.useState(null);
  const [vtab, setVtab] = React.useState("pro");
  const [vaultToast, setVaultToast] = React.useState(null);
  const [sel, setSel] = React.useState({});
  const [editorItem, setEditorItem] = React.useState(null);
  const [editorStores, setEditorStores] = React.useState([]);
  const [editorPick, setEditorPick] = React.useState(false);
  const [batchPick, setBatchPick] = React.useState(false);
  const items = window.__prostockItems || [];
  const proItems = items.filter((i) => i.stockType !== "sideline");
  const sideItems = items.filter((i) => i.stockType === "sideline");
  const selItems = items.filter((it) => sel[it.id]);
  const shape = (it, cw, cwList) => [{ id: it.id + "-" + cw.slug, title: it.title, sku: it.sku, vaultCategory: it.vaultCategory, photo: cw.front, colorway: cw.name, colorways: (cwList && cwList.length ? cwList : it.colorways), retail: it.retail, base: it.base }];
  const deployTo = (dests, it, cw, cwList) => {
    const payload = shape(it, cw, cwList);
    if (dests.length === 1 || !onDeployToStores) dests.forEach((d) => onDeployToStore(d, payload));
    else onDeployToStores(dests, payload);
  };
  return (
    <div style={{ flex: 1, minWidth: 0 }}>
      <div style={{ maxWidth: 1440, margin: "0 auto", padding: "28px 32px 120px" }}>
        <h2 style={{ fontSize: 24, fontWeight: 800, letterSpacing: "-.3px", margin: "0 0 8px", fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Stock Vault</h2>
        <Toast message={vaultToast} onDone={() => setVaultToast(null)} />
        <div style={{ fontSize: 14, color: "#71717a", marginBottom: 10 }}>{items.length} stock style{items.length === 1 ? "" : "s"} · Pro Stock (HCM) + Sideline Apparel (QXX)</div>
        <p style={{ fontSize: 14, color: "#71717a", margin: "0 0 22px", maxWidth: 760, lineHeight: 1.55 }}>
          The Stock Vault spans every store on your platform. It carries Prolook <strong style={{ color: "#27272a" }}>Pro Stock (HCM)</strong> and <strong style={{ color: "#27272a" }}>Sideline Apparel (QXX)</strong> items — in-stock blanks ready to drop into any store. Pricing, team setting, sizes and roster assignment are confirmed inside the destination store.{onStartStore ? " Select several items to start a new store with them pre-loaded." : ""}
        </p>
        <div style={{ display: "inline-flex", background: "#f1f1f3", borderRadius: 10, padding: 4, gap: 2, marginBottom: 18 }}>
          {[{ k: "pro", l: "Pro Stock" }, { k: "sideline", l: "Sideline Stock" }].map((t) => (
            <button key={t.k} type="button" onClick={() => setVtab(t.k)} style={{ border: 0, cursor: "pointer", fontFamily: "inherit", borderRadius: 7, padding: "10px 24px", fontSize: 14, fontWeight: 700, background: vtab === t.k ? "#fff" : "transparent", color: vtab === t.k ? "#09090b" : "#71717a", boxShadow: vtab === t.k ? "0 1px 3px rgba(9,9,11,.12)" : "none", transition: "background .14s, color .14s" }}>{t.l}</button>
          ))}
        </div>
        <VaultSelectPanel items={vtab === "pro" ? proItems : sideItems} onSelect={(it) => setDeploy(it)} selectable={!!onStartStore} selected={sel} onToggle={(it) => setSel((s) => ({ ...s, [it.id]: !s[it.id] }))} />
        <div style={{ fontSize: 12, color: "#a1a1aa", marginTop: 18, maxWidth: 760 }}>
          You can also work with ProStock items from inside a store: Products → Stock Vault picks color ways and sizes for that store only.
        </div>
      </div>
      {onStartStore && selItems.length > 0 && (
        <div style={{ position: "fixed", left: "50%", bottom: 24, transform: "translateX(-50%)", zIndex: 500, background: "#0e0e10", color: "#fff", borderRadius: 10, boxShadow: "0 12px 32px rgba(9,9,11,.28)", display: "flex", alignItems: "center", gap: 14, padding: "10px 12px 10px 18px", whiteSpace: "nowrap" }}>
          <span style={{ fontSize: 12.5, fontWeight: 700 }}>{selItems.length} item{selItems.length === 1 ? "" : "s"} selected</span>
          <button type="button" onClick={() => setSel({})} style={{ background: "none", border: 0, color: "#a1a1aa", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Clear</button>
          <button type="button" onClick={() => setBatchPick(true)} style={{ background: "#fff", color: "#0e0e10", border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Add to existing store(s)</button>
          <button type="button" onClick={() => onStartStore(selItems)} style={{ background: "#e1251b", color: "#fff", border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{"Start a new store with " + (selItems.length === 1 ? "this item" : "these " + selItems.length + " items")}</button>
        </div>
      )}
      {batchPick && selItems.length > 0 && (
        <ProStockDeployModal items={selItems} stores={stores} multi title={"Add " + (selItems.length === 1 ? "1 item" : selItems.length + " items") + " to stores"}
          onClose={() => setBatchPick(false)}
          onConfirm={(dests, cw, cwList) => {
            const its = selItems; setBatchPick(false); setSel({});
            const payload = its.map((it) => shape(it, (its.length === 1 && cw) ? cw : hcmDefaultCw(it), its.length === 1 ? cwList : null)[0]);
            if (dests.length === 1 || !onDeployToStores) dests.forEach((d) => onDeployToStore(d, payload));
            else onDeployToStores(dests, payload);
          }} />
      )}
      {deploy && (
        <ProStockDeployModal item={deploy} stores={stores}
          onClose={() => setDeploy(null)}
          onOpenEditor={(it, cw, cwList, pickedStores) => { setDeploy(null); setEditorItem(cwList && cwList.length ? { ...it, colorways: cwList } : it); setEditorStores(pickedStores || []); }}
          onConfirm={(dests, cw, cwList) => { const it = deploy; setDeploy(null); deployTo(dests, it, cw, cwList); }} />
      )}
      {editorItem && (
        <ArtworkBuilderPage item={editorItem} store={null} name={editorItem.title} styleNo={editorItem.sku}
          cwList={editorItem.colorways} backLabel="Back to Vault" pickedStores={editorStores}
          actionLabel={editorStores.length ? "Add to Store (" + editorStores.length + ")" : "Add to store(s)"} onAction={() => setEditorPick(true)}
          onClose={() => { setEditorItem(null); setEditorPick(false); setEditorStores([]); }} />
      )}
      {editorItem && editorPick && (
        <ProStockDeployModal item={editorItem} stores={stores} multi z={800} title="Add to stores"
          initialPicks={Object.fromEntries(editorStores.map((s) => [s.id, true]))}
          onClose={() => setEditorPick(false)}
          onConfirm={(dests, cw, cwList) => { const it = editorItem; setEditorPick(false); setEditorItem(null); setSel({}); deployTo(dests, it, cw, cwList); }} />
      )}
    </div>
  );
}

// ── One product card in the catalog sections (reference: image / title /
// SKU / divider / Base Price row; grip + kebab appear on hover) ──
function ProductCatalogCard({ p, store, pinned, onOpen, menuItems, menuOpen, onMenuToggle, draggable, onDragStart, onDragOver, onDragEnd, selectMode, selChecked, selLocked, onSelToggle }) {
  const [hover, setHover] = React.useState(false);
  const m = pteMeta(p);
  const lock = pteLockState(p, store);
  const basePrice = (() => {
    const b = parseFloat(p.pricing && p.pricing.adult && p.pricing.adult.base);
    if (!isNaN(b) && b > 0) return "$" + b.toFixed(2);
    const r = pteRetail(p, "adult") || pteRetail(p, "youth");
    return r != null ? "$" + r.toFixed(2) : "—";
  })();
  // Epic J — Incomplete bundles are held off the storefront; bundled items carry a tag.
  const incomplete = !!p.isBundle && (p.bundleItemIds || []).length < 2;
  const inBundle = !p.isBundle && (store?.products || []).find((b) => b.isBundle && (b.bundleItemIds || []).includes(p.id));
  return (
    <div
      data-product-card={p.id}
      draggable={selectMode ? false : draggable}
      onDragStart={onDragStart} onDragOver={onDragOver} onDragEnd={onDragEnd}
      onMouseEnter={() => setHover(true)} onMouseLeave={() => setHover(false)}
      onClick={() => (selectMode ? (onSelToggle && onSelToggle()) : onOpen(p.id))}
      style={{
        border: "1px solid " + (selectMode && selChecked ? "#0e0e10" : hover ? "#c7c7cd" : "#ececf0"), borderRadius: 8, background: "#fff", cursor: "pointer",
        display: "flex", flexDirection: "column", position: "relative", opacity: p.hidden ? .6 : 1,
        boxShadow: hover ? "0 6px 18px rgba(20,20,30,.08)" : "none", transition: "border-color .14s, box-shadow .14s",
      }}>
      <div style={{ position: "relative", height: 226, display: "flex", alignItems: "center", justifyContent: "center", padding: "26px 16px 10px" }}>
        {p.photo
          ? <div style={{ width: 160, height: 190 }}><CatalogPhoto photo={p.photo} alt={p.title} /></div>
          : p.thumbnail
            ? <span style={{ width: 120, height: 160, display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden" }}>{p.thumbnail}</span>
            : <span style={{ width: 90, height: 116, borderRadius: 8, background: "#f1f1f3", display: "flex", alignItems: "center", justifyContent: "center", fontSize: 24, fontWeight: 800, color: "#c4c4cb" }}>{(p.title || "P").slice(0, 1)}</span>}
        {selectMode && (
          <span onClick={(e) => e.stopPropagation()} title={selLocked ? "Included with its selected bundle" : undefined} style={{ position: "absolute", top: 10, left: 12, zIndex: 2, display: "inline-flex", alignItems: "center", gap: 4 }}>
            <input type="checkbox" checked={!!selChecked} disabled={!!selLocked} onChange={() => onSelToggle && onSelToggle()} style={{ width: 16, height: 16, accentColor: "#0e0e10", cursor: selLocked ? "default" : "pointer", margin: 0 }} />
            {selLocked && <IconLock size={11} style={{ color: "#9a9aa1" }} />}
          </span>
        )}
        {!selectMode && hover && draggable !== false && (
          <span title="Drag to reorder within this section" style={{ position: "absolute", top: 10, left: 12, color: "#9a9aa1", cursor: "grab" }}>{catalogGripDots}</span>
        )}
        <span style={{ position: "absolute", top: 6, right: 8, display: "flex", alignItems: "center", gap: 4 }} onClick={(e) => e.stopPropagation()}>
          {p.hidden && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#71717a", background: "#f4f4f5", borderRadius: 4, padding: "2px 7px" }}>HIDDEN</span>}
          {incomplete && <span title="Needs a second item — hidden from the storefront until then" style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#b06f1e", background: "#fff8ef", borderRadius: 4, padding: "2px 7px", cursor: "help" }}>INCOMPLETE</span>}
          {inBundle && <span title={"In bundle: " + inBundle.title + " — only un-bundled items can join another bundle. Click to open the bundle."} onClick={() => onOpen(inBundle.id)} style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#2563eb", background: "#e7f0fc", borderRadius: 4, padding: "2px 7px", cursor: "pointer" }}>IN BUNDLE</span>}
          {pinned && <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".05em", color: "#0e0e10", background: "#f4f4f5", borderRadius: 4, padding: "2px 7px" }}>PINNED</span>}
          {lock !== "none" && <span title={PTE_LOCK_TIPS[lock]} style={{ display: "inline-flex", alignItems: "center", color: lock === "full" ? "#b81d14" : "#9a9aa1", cursor: "help" }}><IconLock size={11} style={{ strokeWidth: 2 }} /></span>}
          <span style={{ position: "relative", visibility: hover || menuOpen ? "visible" : "hidden" }}>
            <button onClick={() => onMenuToggle(p.id)} aria-label="Product actions" style={{ width: 28, height: 28, border: 0, borderRadius: 6, background: menuOpen ? "#f4f4f5" : "none", cursor: "pointer", color: "#71717a", fontSize: 15, fontFamily: "inherit" }}>⋯</button>
            {menuOpen && (
              <div style={{ position: "absolute", right: 0, top: "calc(100% + 4px)", zIndex: 50, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 8, boxShadow: "0 8px 24px rgba(20,20,30,.14)", padding: 6, minWidth: 216, textAlign: "left" }}>
                {menuItems.map((mi, i) => (
                  <button key={i} onClick={() => { onMenuToggle(null); mi.act(); }} style={{ display: "block", width: "100%", textAlign: "left", background: "none", border: 0, borderRadius: 6, padding: "8px 10px", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: mi.danger ? "#b81d14" : "#27272a", cursor: "pointer" }}>
                    {mi.label}
                    {mi.note && <span style={{ display: "block", fontSize: 10.5, color: "#a1a1aa", fontWeight: 400, marginTop: 2 }}>{mi.note}</span>}
                  </button>
                ))}
              </div>
            )}
          </span>
        </span>
        {p.colorways && p.colorways.length > 1 && (
          <span style={{ position: "absolute", bottom: 6, left: 14, display: "flex", gap: 4, alignItems: "center" }}>
            {p.colorways.slice(0, 6).map((c) => (
              <span key={c.slug} title={c.name} style={{ width: 10, height: 10, borderRadius: "50%", background: c.hex, border: c.slug === "white" ? "1px solid #d4d4d8" : "1px solid rgba(9,9,11,.15)", boxShadow: p.colorway === c.name ? "0 0 0 1.5px #0e0e10" : "none" }} />
            ))}
            {p.colorways.length > 6 && <span style={{ fontSize: 9.5, fontWeight: 800, color: "#9a9aa1" }}>+{p.colorways.length - 6}</span>}
          </span>
        )}
      </div>
      <div style={{ padding: "10px 16px 0" }}>
        <div style={{ fontSize: 14.5, fontWeight: 700, color: "#0e0e10", lineHeight: 1.35, textWrap: "pretty" }}>{p.title}</div>
        <div style={{ fontSize: 11.5, color: "#9a9aa1", marginTop: 3, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{p.subtitle || p.sku || pteSourceBadge(p)}</div>
      </div>
      <div style={{ margin: "12px 16px 0", borderTop: "1px solid #ececf0", padding: "10px 0 14px", marginTop: "auto" }}>
        {incomplete ? (
          <div style={{ fontSize: 11.5, fontWeight: 700, color: "#b06f1e" }}>Incomplete — needs a second item</div>
        ) : (
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
            <span style={{ fontSize: 11, fontWeight: 700, color: "#71717a" }}>{p.teamSetting === "team-supplied" ? "Team Supplied" : "Base Price"}</span>
            <span style={{ fontSize: 13, fontWeight: 600, color: "#0e0e10", fontVariantNumeric: "tabular-nums" }}>{p.teamSetting === "team-supplied" ? "Covered" : basePrice}</span>
          </div>
        )}
        {p.srcKind === "vault" && (
          <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginTop: 6 }}>
            <span style={{ fontSize: 11, fontWeight: 700, color: "#71717a" }}>Used In</span>
            <span title="Deployed from the Stock Vault" style={{ fontSize: 12, fontWeight: 700, color: "#2563eb" }}>1 store</span>
          </div>
        )}
      </div>
    </div>
  );
}

// ── Card catalog grouped by category sections — replaces the table ──
function ProductsCatalogSections({ products, store, onSendForApproval, onRemove, onAddProducts, onCreateBundle, onUpdateProduct, onOpenImages, onOpenEditor, onPatchStore, onToast, onOpenDetail }) {
  const cfg = storeCatCfg(store);
  const [tick, setTick] = React.useState(0);
  const bump = () => setTick((n) => n + 1);
  const [q, setQ] = React.useState("");
  const [catFilter, setCatFilter] = React.useState("All");
  const [menuId, setMenuId] = React.useState(null);
  const [secMenu, setSecMenu] = React.useState(null);
  const [deleting, setDeleting] = React.useState(null);
  const [drag, setDrag] = React.useState(null); // {kind:'cat',i} | {kind:'sub',cat,i} | {kind:'prod',cat,id}
  const [renaming, setRenaming] = React.useState(null); // { cat, text }
  const [subAdding, setSubAdding] = React.useState(null); // { cat, text }
  const [newCat, setNewCat] = React.useState("");
  const hasActiveTM = pteHasActiveTM(store);

  const match = (p) => !q || ((p.title || "") + " " + (p.subtitle || "") + " " + (p.sku || "")).toLowerCase().includes(q.toLowerCase());
  const cats = storeCatList(store, products);
  const shownCats = catFilter === "All" ? cats : cats.filter((c) => c === catFilter);
  const total = products.length;
  // ── Catalog Template creation — multi-select mode (selection does nothing else) ──
  const [selMode, setSelMode] = React.useState(false);
  const [tsel, setTsel] = React.useState({});
  const [tplDialog, setTplDialog] = React.useState(false);
  const [tplName, setTplName] = React.useState("");
  const bundleMemberIds = React.useMemo(() => { const s = new Set(); products.forEach((b) => { if (b.isBundle && tsel[b.id]) (b.bundleItemIds || []).forEach((id) => s.add(id)); }); return s; }, [products, tsel]);
  const isChecked = (p) => !!tsel[p.id] || bundleMemberIds.has(p.id);
  const isLockedSel = (p) => !tsel[p.id] && bundleMemberIds.has(p.id);
  const selCount = products.filter(isChecked).length;
  const allSelected = products.length > 0 && products.every(isChecked);
  const toggleSel = (p) => { if (isLockedSel(p)) return; setTsel((s) => ({ ...s, [p.id]: !s[p.id] })); };
  const selectAll = () => setTsel(allSelected ? {} : Object.fromEntries(products.map((p) => [p.id, true])));
  const exitSelMode = () => { setSelMode(false); setTsel({}); setTplDialog(false); };
  const tplDesignLocked = (p) => !p.isBundle && p.srcKind !== "vault" && p.srcKind !== "other-store";
  const selectedItems = products.filter((p) => isChecked(p) && !p.isBundle);
  const lockedNames = selectedItems.filter(tplDesignLocked).map((p) => p.title);
  const saveTemplate = () => {
    const name = tplName.trim();
    if (!name || selectedItems.length === 0) return;
    const priceOf = (p) => { const b = parseFloat(p.pricing && p.pricing.adult && p.pricing.adult.retail) || parseFloat(p.pricing && p.pricing.adult && p.pricing.adult.base); if (!isNaN(b) && b > 0) return b; return pteRetail(p, "adult") || pteRetail(p, "youth") || 0; };
    const tpl = { id: "tpl-" + Date.now(), name, originStore: store.teamName || store.storeTitle || "This store", members: selectedItems.map((p) => ({ id: p.id, title: p.title, styleBlock: p.sku || p.subtitle || "Style block", photo: p.photo, sku: p.sku, price: priceOf(p), locked: tplDesignLocked(p) })) };
    window.__catalogTemplates = (window.__catalogTemplates || []).concat([tpl]);
    onToast && onToast("Template \u201c" + name + "\u201d saved — find it under Catalog Templates in Add Products");
    exitSelMode();
  };
  const dragEnabled = !q && catFilter === "All" && !selMode;

  // ── category management ──
  const reorderCats = (from, to) => {
    const list = cats.filter((c) => c !== CAT_NONE);
    if (from < 0 || to < 0 || from >= list.length || to >= list.length) return;
    const next = list.slice();
    next.splice(to, 0, next.splice(from, 1)[0]);
    cfg.order = next;
    bump();
  };
  const moveProd = (cat, catItems, fromId, toId) => {
    const ids = catItems.map((x) => x.id);
    const f = ids.indexOf(fromId), t = ids.indexOf(toId);
    if (f < 0 || t < 0 || f === t) return;
    ids.splice(t, 0, ids.splice(f, 1)[0]);
    cfg.prodOrder[cat] = ids;
    bump();
  };
  const reorderSubs = (cat, from, to) => {
    const list = (cfg.subs[cat] || []).slice();
    if (from < 0 || to < 0 || from >= list.length || to >= list.length) return;
    list.splice(to, 0, list.splice(from, 1)[0]);
    cfg.subs[cat] = list;
    bump();
  };
  const addCat = () => {
    const n = newCat.trim();
    if (!n || cats.some((c) => c.toLowerCase() === n.toLowerCase())) return;
    cfg.added = [...cfg.added, n];
    cfg.order = (cfg.order || cats.filter((c) => c !== CAT_NONE)).concat([n]);
    setNewCat(""); bump();
    onToast && onToast("Category \u201c" + n + "\u201d added — it renders a storefront tab once it holds a live product");
  };
  const removeCat = (c) => {
    if (c === CAT_SYS || c === CAT_NONE) return;
    const n = storeCatProducts(store, products, c).length;
    cfg.added = cfg.added.filter((x) => x !== c);
    cfg.order = (cfg.order || cats).filter((x) => x !== c);
    delete cfg.subs[c];
    delete cfg.prodOrder[c];
    window.__prodCats = (window.__prodCats || []).filter((x) => x.name !== c);
    if (n > 0) onPatchStore({ products: products.map((p) => productCat(p) === c ? { ...p, storeCategory: CAT_NONE, storeSubcategory: "" } : p) });
    bump();
    onToast && onToast(n > 0 ? "\u201c" + c + "\u201d removed — its " + n + " product" + (n === 1 ? "" : "s") + " moved to Uncategorized" : "\u201c" + c + "\u201d removed");
  };
  const commitRename = () => {
    if (!renaming) return;
    const oldName = renaming.cat, next = renaming.text.trim();
    setRenaming(null);
    if (!next || next === oldName || cats.some((c) => c.toLowerCase() === next.toLowerCase())) return;
    const wc = (window.__prodCats || []).find((c) => c.name === oldName);
    if (wc) wc.name = next;
    cfg.added = (cfg.added || []).map((c) => c === oldName ? next : c);
    if (cfg.order) cfg.order = cfg.order.map((c) => c === oldName ? next : c);
    if (cfg.subs[oldName]) { cfg.subs[next] = cfg.subs[oldName]; delete cfg.subs[oldName]; }
    if (cfg.prodOrder[oldName]) { cfg.prodOrder[next] = cfg.prodOrder[oldName]; delete cfg.prodOrder[oldName]; }
    if (products.some((p) => productCat(p) === oldName)) onPatchStore({ products: products.map((p) => productCat(p) === oldName ? { ...p, storeCategory: next } : p) });
    bump();
    onToast && onToast("Renamed \u201c" + oldName + "\u201d to \u201c" + next + "\u201d");
  };
  const commitAddSub = () => {
    if (!subAdding) return;
    const cat = subAdding.cat, n = subAdding.text.trim();
    setSubAdding(null);
    if (!n || (cfg.subs[cat] || []).some((s) => s.toLowerCase() === n.toLowerCase())) return;
    cfg.subs[cat] = [...(cfg.subs[cat] || []), n];
    bump();
    onToast && onToast("Subcategory \u201c" + n + "\u201d added under " + cat);
  };
  const removeSub = (cat, s) => {
    cfg.subs[cat] = (cfg.subs[cat] || []).filter((x) => x !== s);
    if (products.some((p) => productCat(p) === cat && p.storeSubcategory === s)) {
      onPatchStore({ products: products.map((p) => productCat(p) === cat && p.storeSubcategory === s ? { ...p, storeSubcategory: "" } : p) });
    }
    bump();
    onToast && onToast("Subcategory \u201c" + s + "\u201d removed — its products stay in " + cat);
  };
  const togglePin = (p, cat) => { cfg.pins[p.id] = !cfg.pins[p.id]; bump(); onToast && onToast(cfg.pins[p.id] ? "\u201c" + p.title + "\u201d pinned to the top of " + cat : "Unpinned \u201c" + p.title + "\u201d"); };

  const cardMenu = (p, cat) => {
    const m = pteMeta(p);
    const items = [];
    items.push({ label: "Open product page", act: () => onOpenDetail(p.id) });
    items.push({ label: cfg.pins[p.id] ? "Unpin" : "Pin to top of " + cat, act: () => togglePin(p, cat) });
    if (!pteRestricted(p) || m.decoratable) items.push({ label: "Customize", act: () => onOpenEditor && onOpenEditor(p.id) });
    if (hasActiveTM) items.push({ label: "Send for Approval", note: "Optional — reviewed by this store's team manager", act: () => onSendForApproval && onSendForApproval(p.id) });
    if (p.teamSetting === "team-required") items.push({ label: "Allow opt-out: " + (p.optOut ? "On" : "Off"), note: "\u201cI already own this\u201d counts as satisfied", act: () => { onUpdateProduct({ ...p, optOut: !p.optOut }); onToast && onToast(p.optOut ? "Opt-out disabled" : "Opt-out enabled — counts as satisfied in required-gear tracking"); } });
    items.push({ label: p.hidden ? "Show on storefront" : "Hide from storefront", note: p.hidden ? null : "Orders, category and lock state are kept", act: () => { onUpdateProduct({ ...p, hidden: !p.hidden }); onToast && onToast(p.hidden ? `"${p.title}" is visible on the storefront` : `"${p.title}" hidden from the storefront`); } });
    items.push({ label: "Delete", danger: true, act: () => setDeleting(p) });
    return items;
  };

  const grid = { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(236px, 1fr))", gap: 16 };
  const capLabel = { fontSize: 11, fontWeight: 800, letterSpacing: ".1em", color: "#9a9aa1", textTransform: "uppercase" };

  if (total === 0 && !q) {
    return (
      <div style={{ paddingBottom: 60 }}>
        <div style={{ display: "flex", flexDirection: "column", alignItems: "center", padding: "110px 20px 96px", textAlign: "center" }}>
          <div style={{ width: 56, height: 56, borderRadius: "50%", background: "#f1f1f3", color: "#0e0e10", display: "inline-flex", alignItems: "center", justifyContent: "center", marginBottom: 16 }}>
            <IconShirt size={26} />
          </div>
          <div style={{ fontSize: 20, fontWeight: 800, color: "#0e0e10", marginBottom: 8, fontFamily: "Archivo, sans-serif" }}>No products yet</div>
          <div style={{ color: "#52525b", fontSize: 14, maxWidth: 460, lineHeight: 1.55, marginBottom: 22 }}>
            Pull products from the Stock Vault, your saved designs and carts, or customize a new one to start building this store.
          </div>
          <div style={{ display: "flex", gap: 10 }}>
            <button type="button" onClick={onAddProducts} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "12px 28px", fontSize: 14, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>+ Add Products</button>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div style={{ paddingBottom: 100 }}>
      {(menuId || secMenu) && <div onClick={() => { setMenuId(null); setSecMenu(null); }} style={{ position: "fixed", inset: 0, zIndex: 30 }} />}

      {/* Title row + primary actions */}
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, flexWrap: "wrap", marginBottom: 16 }}>
        <div style={{ display: "flex", gap: 10, alignItems: "baseline" }}>
          <span style={{ fontSize: 17, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Catalog</span>
          <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>{total} item{total === 1 ? "" : "s"} — live on the storefront once the store launches.</span>
        </div>
        <div style={{ display: "flex", gap: 10 }}>
          <button onClick={() => (selMode ? exitSelMode() : setSelMode(true))} title="Select products and bundles to save as a Catalog Template" style={{ display: "inline-flex", alignItems: "center", gap: 7, background: selMode ? "#0e0e10" : "#fff", color: selMode ? "#fff" : "#0e0e10", border: "1px solid " + (selMode ? "#0e0e10" : "#d4d4d8"), borderRadius: 6, padding: "10px 18px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>{selMode ? "Cancel selection" : "Select"}</button>
          <button onClick={onCreateBundle} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: "#fff", color: "#0e0e10", border: "1px solid #d4d4d8", borderRadius: 6, padding: "10px 18px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}><IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Create Bundle</button>
          <button onClick={onAddProducts} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "10px 20px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}><IconPlus size={14} style={{ strokeWidth: 2.4 }} /> Add Products</button>
        </div>
      </div>

      {/* Search + category filter */}
      <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 22 }}>
        <input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search the catalog by name or SKU" style={{ width: 300, height: 38, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13, fontFamily: "inherit", outline: "none", background: "#fff" }} />
        <select value={catFilter} onChange={(e) => setCatFilter(e.target.value)} style={{
          appearance: "none", WebkitAppearance: "none",
          border: "1px solid " + (catFilter !== "All" ? "#0e0e10" : "#e4e4e7"),
          background: (catFilter !== "All" ? "#0e0e10" : "#f4f4f5") + " url('data:image/svg+xml,%3Csvg xmlns=%22http://www.w3.org/2000/svg%22 width=%2210%22 height=%226%22%3E%3Cpath d=%22M1 1l4 4 4-4%22 stroke=%22" + (catFilter !== "All" ? "%23fff" : "%23555") + "%22 fill=%22none%22 stroke-width=%221.5%22/%3E%3C/svg%3E') no-repeat right 12px center",
          color: catFilter !== "All" ? "#fff" : "#27272a", borderRadius: 999, padding: "9px 32px 9px 14px", fontSize: 12.5, fontWeight: 700, fontFamily: "inherit", cursor: "pointer",
        }}>
          <option value="All">Category: All</option>
          {cats.map((c) => <option key={c} value={c}>{c}</option>)}
        </select>
        {(q || catFilter !== "All") && <button onClick={() => { setQ(""); setCatFilter("All"); }} style={{ background: "none", border: 0, fontSize: 12, fontWeight: 600, color: "#9a9aa1", cursor: "pointer", fontFamily: "inherit" }}>Clear</button>}
        {!dragEnabled && <span style={{ fontSize: 12, color: "#a1a1aa" }}>Clear search and filters to drag &amp; reorder.</span>}
      </div>

      {selMode && (
        <div style={{ display: "flex", alignItems: "center", gap: 12, margin: "0 0 14px", padding: "12px 16px", background: "#fff", border: "1px solid #ececf0", borderRadius: 10 }}>
          <label style={{ display: "inline-flex", alignItems: "center", gap: 10, cursor: "pointer", fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>
            <input type="checkbox" checked={allSelected} onChange={selectAll} style={{ width: 16, height: 16, accentColor: "#0e0e10", cursor: "pointer", margin: 0 }} />
            Select all
          </label>
          <span style={{ fontSize: 12.5, color: "#9a9aa1" }}>Selecting a bundle includes its member items.</span>
        </div>
      )}
      {/* Category sections */}
      <div style={{ display: "grid", gap: 24 }} data-tick={tick}>
        {shownCats.map((cat) => {
          const ci = cats.indexOf(cat);
          const sys = cat === CAT_SYS, none = cat === CAT_NONE;
          const catItems = storeCatProducts(store, products, cat);
          const items = catItems.filter(match);
          if (q && items.length === 0) return null;
          const isRenaming = renaming && renaming.cat === cat;
          const subs = cfg.subs[cat] || [];
          const withCw = items.filter((p) => p.colorways && p.colorways.length).length;
          const subtitle = items.length === 0
            ? "Empty — no storefront tab until it holds a live product"
            : withCw === items.length && withCw > 0
              ? items.length + " item" + (items.length === 1 ? "" : "s") + " with 3D color ways on file"
              : items.length + " item" + (items.length === 1 ? "" : "s");
          const groups = [["", items.filter((p) => !p.storeSubcategory || !subs.includes(p.storeSubcategory))]]
            .concat(subs.map((s) => [s, items.filter((p) => p.storeSubcategory === s)]));
          const secMenuItems = [];
          if (!sys && !none) secMenuItems.push({ label: "Rename category", act: () => setRenaming({ cat, text: cat }) });
          if (!none) secMenuItems.push({ label: "Add subcategory", act: () => setSubAdding({ cat, text: "" }) });
          if (!sys && !none) secMenuItems.push({ label: "Remove category", danger: true, note: "Products move to Uncategorized", act: () => removeCat(cat) });
          return (
            <div key={cat}
              onDragOver={(e) => { if (drag && drag.kind === "cat" && dragEnabled) { e.preventDefault(); if (drag.i !== ci && !none) { reorderCats(drag.i, ci); setDrag({ kind: "cat", i: cats.indexOf(cat) }); } } }}
              style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 12, padding: "20px 24px 24px", boxShadow: "0 1px 3px rgba(20,20,30,.03)", opacity: drag && drag.kind === "cat" && drag.i === ci ? .55 : 1 }}>
              {/* Section header */}
              <div style={{ display: "flex", alignItems: "flex-start", gap: 10, marginBottom: 16 }}>
                {!none && (
                  <span
                    draggable={dragEnabled}
                    onDragStart={() => setDrag({ kind: "cat", i: ci })}
                    onDragEnd={() => setDrag(null)}
                    title={dragEnabled ? "Drag to reorder this section — the order is the storefront order" : "Clear search and filters to reorder"}
                    style={{ cursor: dragEnabled ? "grab" : "default", color: "#b6b6bd", display: "inline-flex", padding: "6px 2px 0 0" }}>{catalogGripDots}</span>
                )}
                <div style={{ minWidth: 0, flex: 1 }}>
                  {isRenaming ? (
                    <input autoFocus value={renaming.text}
                      onChange={(e) => setRenaming({ cat, text: e.target.value })}
                      onBlur={commitRename}
                      onKeyDown={(e) => { if (e.key === "Enter") commitRename(); if (e.key === "Escape") setRenaming(null); }}
                      style={{ fontSize: 18, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#0e0e10", border: "1px solid #0e0e10", borderRadius: 6, padding: "3px 8px", outline: "none", minWidth: 200 }} />
                  ) : (
                    <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
                      <span style={{ fontSize: 18, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: none ? "#71717a" : "#0e0e10", letterSpacing: "-.2px" }}>{cat}</span>
                      {sys && <span title="System-managed — collects every Team Required / Team Supplied item. It can move, but not be renamed or removed." style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".06em", color: "#52525b", background: "#f4f4f5", borderRadius: 999, padding: "2px 8px", cursor: "help" }}>SYSTEM</span>}
                    </div>
                  )}
                  <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 3 }}>{subtitle}</div>
                </div>
                {secMenuItems.length > 0 && (
                  <span style={{ position: "relative" }}>
                    <button onClick={() => setSecMenu(secMenu === cat ? null : cat)} aria-label="Section actions" style={{ width: 30, height: 30, border: 0, borderRadius: 6, background: secMenu === cat ? "#f4f4f5" : "none", cursor: "pointer", color: "#71717a", fontSize: 16, fontFamily: "inherit" }}>⋯</button>
                    {secMenu === cat && (
                      <div style={{ position: "absolute", right: 0, top: "calc(100% + 4px)", zIndex: 50, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 8, boxShadow: "0 8px 24px rgba(20,20,30,.14)", padding: 6, minWidth: 200, textAlign: "left" }}>
                        {secMenuItems.map((mi, i) => (
                          <button key={i} onClick={() => { setSecMenu(null); mi.act(); }} style={{ display: "block", width: "100%", textAlign: "left", background: "none", border: 0, borderRadius: 6, padding: "8px 10px", fontFamily: "inherit", fontSize: 12.5, fontWeight: 600, color: mi.danger ? "#b81d14" : "#27272a", cursor: "pointer" }}>
                            {mi.label}
                            {mi.note && <span style={{ display: "block", fontSize: 10.5, color: "#a1a1aa", fontWeight: 400, marginTop: 2 }}>{mi.note}</span>}
                          </button>
                        ))}
                      </div>
                    )}
                  </span>
                )}
              </div>
              {/* Groups: ungrouped first, then one block per subcategory */}
              {items.length === 0 && !subAdding && (
                <div style={{ padding: "18px 4px 4px", fontSize: 12.5, color: "#a1a1aa", lineHeight: 1.5 }}>
                  Nothing in {cat} yet. {none ? "Assign a category on each product to clear this bucket." : "Set this category on a product and it appears here."}
                </div>
              )}
              {groups.map(([sub, gItems], gi) => {
                if (!sub && gItems.length === 0) return null;
                return (
                  <div key={sub || "__none"} style={{ marginTop: gi > 0 && (gItems.length > 0 || sub) ? 20 : 0 }}
                    onDragOver={(e) => { if (drag && drag.kind === "sub" && drag.cat === cat && dragEnabled && sub) { e.preventDefault(); const si = subs.indexOf(sub); if (drag.i !== si && si >= 0) { reorderSubs(cat, drag.i, si); setDrag({ kind: "sub", cat, i: si }); } } }}>
                    {sub && (
                      <div style={{ display: "flex", alignItems: "center", gap: 8, marginBottom: 12, color: "#71717a" }}>
                        <span
                          draggable={dragEnabled}
                          onDragStart={() => setDrag({ kind: "sub", cat, i: subs.indexOf(sub) })}
                          onDragEnd={() => setDrag(null)}
                          title={dragEnabled ? "Drag to reorder this subcategory" : undefined}
                          style={{ cursor: dragEnabled ? "grab" : "default", display: "inline-flex" }}>{subcatDragIcon}</span>
                        <span style={{ fontSize: 13, fontWeight: 700, color: "#71717a", letterSpacing: ".01em" }}>{sub}</span>
                        {gItems.length > 0 && <span style={{ fontSize: 11, color: "#b6b6bd" }}>{gItems.length}</span>}
                        <button onClick={() => removeSub(cat, sub)} title={"Remove subcategory " + sub} style={{ background: "none", border: 0, padding: 2, cursor: "pointer", color: "#c4c4cb", display: "inline-flex" }}><IconX size={11} /></button>
                      </div>
                    )}
                    {sub && gItems.length === 0 && (
                      <div style={{ fontSize: 12, color: "#b6b6bd", margin: "0 0 4px 22px" }}>No products yet — assign this subcategory on a product page.</div>
                    )}
                    {gItems.length > 0 && (
                      <div style={grid}>
                        {gItems.map((p) => (
                          <ProductCatalogCard key={p.id} p={p} store={store}
                            selectMode={selMode} selChecked={isChecked(p)} selLocked={isLockedSel(p)} onSelToggle={() => toggleSel(p)}
                            pinned={!!cfg.pins[p.id]}
                            onOpen={onOpenDetail}
                            menuItems={cardMenu(p, cat)}
                            menuOpen={menuId === p.id}
                            onMenuToggle={(id) => setMenuId(menuId === id ? null : id)}
                            draggable={dragEnabled}
                            onDragStart={() => setDrag({ kind: "prod", cat, id: p.id })}
                            onDragOver={(e) => { if (drag && drag.kind === "prod" && drag.cat === cat && dragEnabled) { e.preventDefault(); e.stopPropagation(); if (drag.id !== p.id) moveProd(cat, storeCatProducts(store, products, cat), drag.id, p.id); } }}
                            onDragEnd={() => setDrag(null)} />
                        ))}
                      </div>
                    )}
                  </div>
                );
              })}
              {subAdding && subAdding.cat === cat && (
                <div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 16 }}>
                  <span style={{ color: "#b6b6bd", display: "inline-flex" }}>{subcatDragIcon}</span>
                  <input autoFocus value={subAdding.text}
                    onChange={(e) => setSubAdding({ cat, text: e.target.value })}
                    onBlur={commitAddSub}
                    onKeyDown={(e) => { if (e.key === "Enter") commitAddSub(); if (e.key === "Escape") setSubAdding(null); }}
                    placeholder="Subcategory name"
                    style={{ width: 220, height: 32, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 10px", fontSize: 12.5, fontFamily: "inherit", outline: "none" }} />
                  <span style={{ fontSize: 11.5, color: "#b6b6bd" }}>Enter to add</span>
                </div>
              )}
            </div>
          );
        })}
        {/* Add category */}
        {dragEnabled && (
          <div style={{ border: "1px dashed #d4d4d8", borderRadius: 12, padding: "16px 20px", display: "flex", alignItems: "center", gap: 10, background: "#fafafa" }}>
            <span style={{ fontSize: 12.5, fontWeight: 700, color: "#52525b" }}>New category</span>
            <input value={newCat} onChange={(e) => setNewCat(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") addCat(); }} placeholder="e.g. Spirit Wear"
              style={{ width: 220, height: 34, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 10px", fontSize: 12.5, fontFamily: "inherit", outline: "none", background: "#fff" }} />
            <button onClick={addCat} disabled={!newCat.trim()} style={{ background: newCat.trim() ? "#0e0e10" : "#f1f1f3", color: newCat.trim() ? "#fff" : "#a1a1aa", border: 0, borderRadius: 6, padding: "0 14px", height: 34, fontSize: 12.5, fontWeight: 700, cursor: newCat.trim() ? "pointer" : "default", fontFamily: "inherit" }}>Add</button>
            <span style={{ fontSize: 11.5, color: "#a1a1aa" }}>Categories become the storefront header tabs, in this order.</span>
          </div>
        )}
      </div>

      {selMode && selCount > 0 && (
        <div style={{ position: "fixed", left: "50%", transform: "translateX(-50%)", bottom: 24, zIndex: 60, background: "#0e0e10", color: "#fff", borderRadius: 12, boxShadow: "0 12px 32px rgba(20,20,30,.3)", padding: "12px 12px 12px 18px", display: "flex", alignItems: "center", gap: 14 }}>
          <span style={{ fontSize: 13, fontWeight: 700 }}>{selCount} selected</span>
          <button onClick={() => setTsel({})} style={{ background: "none", border: 0, color: "#a1a1aa", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Clear</button>
          <button onClick={() => { setTplName(""); setTplDialog(true); }} style={{ background: "#fff", color: "#0e0e10", border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Create Catalog Template</button>
        </div>
      )}
      {tplDialog && (
        <div onClick={() => setTplDialog(false)} style={{ position: "fixed", inset: 0, zIndex: 600, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", padding: 24 }}>
          <div onClick={(e) => e.stopPropagation()} role="dialog" aria-modal="true" aria-label="Create Catalog Template" style={{ width: "min(460px, 94vw)", background: "#fff", borderRadius: 12, boxShadow: "0 30px 80px rgba(9,9,11,.34)", padding: "24px 24px 22px" }}>
            <h3 style={{ margin: "0 0 4px", fontSize: 18, fontWeight: 800, color: "#0e0e10", fontFamily: "Archivo, sans-serif" }}>Create Catalog Template</h3>
            <div style={{ fontSize: 12.5, color: "#71717a", marginBottom: 16 }}>{selectedItems.length} item{selectedItems.length === 1 ? "" : "s"} from this store's catalog. Members keep this store's artwork.</div>
            <label style={{ display: "block", fontSize: 11, fontWeight: 800, letterSpacing: ".06em", textTransform: "uppercase", color: "#9a9aa1", marginBottom: 6 }}>Template name</label>
            <input autoFocus value={tplName} onChange={(e) => setTplName(e.target.value)} onKeyDown={(e) => { if (e.key === "Enter") saveTemplate(); }} placeholder="e.g. Grizzlies 2026 season"
              style={{ width: "100%", boxSizing: "border-box", height: 40, border: "1px solid #d4d4d8", borderRadius: 8, padding: "0 12px", fontSize: 13.5, fontFamily: "inherit", outline: "none", marginBottom: 14 }} />
            {lockedNames.length > 0 && (
              <div style={{ display: "flex", gap: 10, background: "#fafafa", border: "1px solid #e4e4e7", borderRadius: 8, padding: "12px 14px", marginBottom: 16 }}>
                <IconLock size={13} style={{ flexShrink: 0, marginTop: 2, color: "#71717a" }} />
                <div style={{ fontSize: 12.5, color: "#3f3f46", lineHeight: 1.55 }}>
                  These items keep this store's artwork wherever the template is used, and can't be re-branded later: <strong style={{ color: "#0e0e10", fontWeight: 700 }}>{lockedNames.join(", ")}</strong>. Everything else can be re-decorated in the destination store.
                </div>
              </div>
            )}
            <div style={{ display: "flex", justifyContent: "flex-end", gap: 10 }}>
              <button onClick={() => setTplDialog(false)} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "10px 18px", fontSize: 13, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Cancel</button>
              <button disabled={!tplName.trim()} onClick={saveTemplate} style={{ background: tplName.trim() ? "#0e0e10" : "#e4e4e7", color: tplName.trim() ? "#fff" : "#a1a1aa", border: 0, borderRadius: 6, padding: "10px 18px", fontSize: 13, fontWeight: 700, cursor: tplName.trim() ? "pointer" : "not-allowed", fontFamily: "inherit" }}>Create Catalog Template</button>
            </div>
          </div>
        </div>
      )}
      <ProductDeleteModal
        product={deleting}
        store={store}
        onClose={() => setDeleting(null)}
        onConfirm={() => { if (deleting) { onRemove(deleting.id); onToast && onToast(`"${deleting.title}" deleted`); } setDeleting(null); }}
        onGoToBundle={typeof onOpenDetail === "function" ? (bid) => { setDeleting(null); onOpenDetail(bid); } : undefined}
        onGoToOrders={() => { setDeleting(null); window.__goToPendingOrder && window.__goToPendingOrder(); }} />
    </div>
  );
}

// ── Product Detail sub-page — Product info / Sizes & color ways ──
// mode="edit"  : a product already in the catalog (replaces the slide-out)
// mode="vault" : a ProStock Vault item being configured for this store
const PDP_SIZES = ["YS", "YM", "YL", "S", "M", "L", "XL", "2XL"];
function ProductDetailPage({ mode = "edit", product, vaultItem, store, onBack, onSave, onRemove, onAddToCatalog, onOpenImages, onOpenEditor, onSendForApproval, onToast }) {
  const isVault = mode === "vault";
  const src = isVault ? vaultItem : product;
  const p = isVault ? null : product;
  const isBundle = !isVault && !!p.isBundle;
  const m = isVault ? null : pteMeta(p);
  const lock = isVault ? "none" : pteLockState(p, store);
  const fullLock = lock === "full";
  const fundOn = store?.fundraisingEnabled === true;
  const allProducts = store?.products || [];
  const hasActiveTM = !isVault && pteHasActiveTM(store);
  const stats = isVault ? null : productOrderStats(p, store);
  const cws = src.colorways || null;

  const [tab, setTab] = React.useState("info");
  const [name, setName] = React.useState(src.title || "");
  const [styleNo, setStyleNo] = React.useState(src.sku || "");
  const [manufacturer, setManufacturer] = React.useState((src.brand || "Prolook") + " Sports");
  const [prodCat, setProdCat] = React.useState("");
  const [prodSubcat, setProdSubcat] = React.useState("");
  const [retail, setRetail] = React.useState(isVault ? (src.retail || 0).toFixed(2) : "");
  const [base, setBase] = React.useState(isVault ? (src.base || 0).toFixed(2) : "");
  const [optRequired, setOptRequired] = React.useState(false);
  const [optSupplied, setOptSupplied] = React.useState(false);
  const [pricing, setPricing] = React.useState(defaultPricing());
  const [rosters, setRosters] = React.useState([]);
  const [catError, setCatError] = React.useState(false);
  const [teamError, setTeamError] = React.useState(false);
  const [deleting, setDeleting] = React.useState(false);
  const [builderOpen, setBuilderOpen] = React.useState(false);
  const [appSizes, setAppSizes] = React.useState(() => (!isVault && p && p.applicationSizes) ? p.applicationSizes.map((r) => ({ ...r })) : defaultApplicationSizes());
  // Gallery / colorway offering
  const [gSel, setGSel] = React.useState(0);
  const [gSide, setGSide] = React.useState("front");
  const [cwOn, setCwOn] = React.useState({});
  const [defCw, setDefCw] = React.useState(null);
  const [sizesOn, setSizesOn] = React.useState(() => Object.fromEntries(PDP_SIZES.map((s) => [s, true])));
  // Bundle state
  const [bundleRequired, setBundleRequired] = React.useState(false);
  const [bundleSupplied, setBundleSupplied] = React.useState(false);
  const [itemPricings, setItemPricings] = React.useState({});
  const [expandedItemId, setExpandedItemId] = React.useState(null);
  const [bundleProfit, setBundleProfit] = React.useState(defaultBundleProfit());
  const [bundleHidden, setBundleHidden] = React.useState(false);
  const [bundleHideRetail, setBundleHideRetail] = React.useState(false);
  const [bundleFreeShip, setBundleFreeShip] = React.useState(false);
  const [memberIds, setMemberIds] = React.useState(() => (isBundle && p ? p.bundleItemIds || [] : []));
  const [addingMembers, setAddingMembers] = React.useState(false);
  const bundleItems = isBundle ? memberIds.map((id) => allProducts.find((x) => x.id === id)).filter(Boolean) : [];

  React.useEffect(() => {
    setTab("info");
    setName(src.title || "");
    setStyleNo(src.sku || "");
    setManufacturer((src.brand || "Prolook") + " Sports");
    setCatError(false); setTeamError(false);
    if (cws) {
      const savedDef = (!isVault && p.colorway) ? cws.findIndex((c) => c.name === p.colorway) : cws.indexOf(hcmDefaultCw(src));
      const di = savedDef >= 0 ? savedDef : 0;
      setDefCw(cws[di].slug);
      setGSel(di);
      setGSide("front");
      const offered = !isVault && p.enabledColorways ? new Set(p.enabledColorways) : null;
      setCwOn(Object.fromEntries(cws.map((c) => [c.slug, offered ? offered.has(c.slug) : true])));
    }
    const offeredSizes = !isVault && p && p.offeredSizes ? new Set(p.offeredSizes) : null;
    setSizesOn(Object.fromEntries(PDP_SIZES.map((s) => [s, offeredSizes ? offeredSizes.has(s) : true])));
    setAppSizes((!isVault && p && p.applicationSizes) ? p.applicationSizes.map((r) => ({ ...r })) : defaultApplicationSizes());
    if (isVault) {
      setProdCat(""); setProdSubcat("");
      setRetail((src.retail || 0).toFixed(2)); setBase((src.base || 0).toFixed(2));
      setOptRequired(false); setOptSupplied(false);
      setRosters([]);
    } else {
      const ts = p.teamSetting || "fan-gear";
      setOptRequired(p.optRequired ?? ts === "team-required");
      setOptSupplied(p.optSupplied ?? ts === "team-supplied");
      setPricing(p.pricing || defaultPricing());
      setRosters(p.rosters || []);
      const tOn = (p.optRequired ?? ts === "team-required") || (p.optSupplied ?? ts === "team-supplied");
      setProdCat(tOn ? "Team Gear" : (p.storeCategory && p.storeCategory !== CAT_NONE ? p.storeCategory : ""));
      setProdSubcat(p.storeSubcategory || "");
      if (p.isBundle) {
        setBundleRequired(p.bundleRequired ?? ts === "team-required");
        setBundleSupplied(p.bundleSupplied ?? ts === "team-supplied");
        const init = {};
        (p.bundleItemIds || []).forEach((id) => { const it = allProducts.find((x) => x.id === id); if (it) init[id] = it.pricing || defaultPricing(); });
        Object.assign(init, p.itemPricings || {});
        setItemPricings(init);
        setBundleProfit(p.bundleProfit || defaultBundleProfit());
        setBundleHidden(!!p.hidden);
        setBundleHideRetail(!!p.hideRetail);
        setBundleFreeShip(!!p.freeShipping);
        setMemberIds(p.bundleItemIds || []);
        setExpandedItemId((p.bundleItemIds || [])[0] || null);
      }
    }
  }, [src?.id]);

  const teamOn = !isVault && (optRequired || optSupplied);
  const catOptions = storeCatList(store, allProducts).filter((c) => c !== CAT_SYS && c !== CAT_NONE);
  const catSubs = (() => {
    const scoped = (storeCatCfg(store).subs || {})[prodCat] || [];
    const ws = (((window.__prodCats || []).find((c) => c.name === prodCat)) || {}).subs || [];
    return Array.from(new Set(ws.concat(scoped)));
  })();
  const priceGridCols = fundOn ? "70px 110px minmax(120px, 150px) minmax(120px, 150px) minmax(120px, 150px) 90px" : "70px 110px minmax(120px, 150px) minmax(120px, 150px) 90px";
  const cwCount = cws ? cws.filter((c) => cwOn[c.slug]).length : 0;
  const sizeCount = PDP_SIZES.filter((s) => sizesOn[s]).length;
  const gCw = cws ? cws[gSel] : null;
  const mainPhoto = gCw ? gCw[gSide] : (p && p.photo);

  const updatePricing = (size, field, value) => {
    const next = { ...pricing, [size]: { ...pricing[size], [field]: value } };
    const baseNum = parseFloat(next[size].base) || 0;
    const dealerNum = parseFloat(next[size].dealer) || 0;
    const fundNum = parseFloat(next[size].fund) || 0;
    const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
    const fundAmt = next[size].fundUnit === "$" ? fundNum : baseNum * fundNum / 100;
    next[size].retail = (baseNum + dealerAmt + ((next.fundraisingOn ?? (store?.fundraisingEnabled === true)) ? fundAmt : 0)).toFixed(2);
    setPricing(next);
  };
  const updateItemPricing = (itemId, size, field, value) => {
    setItemPricings((prev) => {
      const cur = prev[itemId] || defaultPricing();
      const next = { ...cur, [size]: { ...cur[size], [field]: value } };
      const baseNum = parseFloat(next[size].base) || 0;
      const dealerNum = parseFloat(next[size].dealer) || 0;
      const dealerAmt = next[size].dealerUnit === "$" ? dealerNum : baseNum * dealerNum / 100;
      next[size].retail = (baseNum + dealerAmt).toFixed(2);
      return { ...prev, [itemId]: next };
    });
  };
  const updateBundleProfit = (field, value) => setBundleProfit((prev) => ({ ...prev, [field]: value }));

  const handleSave = () => {
    if (isVault) {
      const enabled = cws.filter((c) => cwOn[c.slug]);
      const list = enabled.length ? enabled : cws;
      const def = list.find((c) => c.slug === defCw) || list[0];
      onAddToCatalog({
        id: "vault-" + src.id + "-" + Date.now(),
        title: name.trim() || src.title,
        sku: styleNo || src.sku,
        brand: "Prolook", source: "ProStock", srcKind: "vault",
        category: src.vaultCategory,
        storeCategory: prodCat || "", storeSubcategory: prodSubcat || "",
        stock: "in", deco: "none", decoratable: true,
        photo: def.front, colorway: def.name, colorways: list,
        enabledColorways: list.map((c) => c.slug),
        offeredSizes: PDP_SIZES.filter((s) => sizesOn[s]),
        status: "live", teamSetting: "fan-gear",
        subtitle: (styleNo || src.sku) + " · Prolook ProStock",
        pricing: vaultPricing(base, retail),
      });
      return;
    }
    if (isBundle) {
      onSave({
        ...p,
        title: name.trim() || p.title,
        bundleRequired, bundleSupplied,
        teamSetting: bundleRequired ? "team-required" : bundleSupplied ? "team-supplied" : "fan-gear",
        itemPricings, bundleProfit,
        bundleItemIds: memberIds,
        // Incomplete bundles are held off the storefront (Epic J).
        hidden: memberIds.length < 2 ? true : bundleHidden,
        hideRetail: bundleHideRetail, freeShipping: bundleFreeShip,
        rosters,
        pricing: bundleCardPricing(bundleItems, itemPricings, bundleProfit, p.bundleType || "all", bundleSupplied, fundOn),
      });
      onToast && onToast("Bundle updated");
      onBack();
      return;
    }
    if (!teamOn && !prodCat) { setCatError(true); setActiveSec("essentials"); return; }
    if (teamOn && store?.storeType === "league" && rosters.length === 0) { setTeamError(true); setActiveSec("essentials"); return; }
    const patch = {
      ...p,
      title: name.trim() || p.title,
      sku: styleNo || p.sku,
      teamSetting: optRequired ? "team-required" : optSupplied ? "team-supplied" : "fan-gear",
      optRequired, optSupplied,
      storeCategory: teamOn ? "Team Gear" : prodCat, storeSubcategory: prodSubcat,
      rosters, pricing,
      offeredSizes: PDP_SIZES.filter((s) => sizesOn[s]),
    };
    if (isCustom) patch.applicationSizes = appSizes;
    if (cws) {
      const enabled = cws.filter((c) => cwOn[c.slug]);
      const list = enabled.length ? enabled : cws;
      const def = list.find((c) => c.slug === defCw) || list[0];
      patch.colorway = def.name;
      patch.photo = def.front;
      patch.enabledColorways = list.map((c) => c.slug);
    }
    onSave(patch);
    onToast && onToast("Product updated");
    onBack();
  };

  // ── styles ──
  const capLabel = { fontSize: 11, fontWeight: 800, letterSpacing: ".1em", color: "#9a9aa1", textTransform: "uppercase" };
  const fieldLabel = { display: "block", fontSize: 12.5, fontWeight: 600, color: "#3f3f46", marginBottom: 7 };
  const fieldInput = { width: "100%", height: 42, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 6, padding: "0 12px", fontSize: 13.5, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" };
  const fieldWrap = { marginBottom: 18 };
  const specRow = (l, v) => (
    <div key={l} style={{ display: "flex", alignItems: "center", justifyContent: "space-between", padding: "9px 0" }}>
      <span style={{ fontSize: 13, color: "#52525b" }}>{l}</span>
      <span style={{ fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>{v}</span>
    </div>
  );
  const settingBadge = {
    "team-required": { label: "TEAM REQUIRED", color: "#e8590c", bg: "#fff4e6" },
    "team-supplied": { label: "TEAM SUPPLIED", color: "#2944a8", bg: "#e9eefb" },
    "fan-gear": { label: "FAN GEAR", color: "#1f7a44", bg: "#e7f5ed" },
  }[optRequired ? "team-required" : optSupplied ? "team-supplied" : "fan-gear"];
  const headerThumb = mainPhoto || (cws && cws[0].front);

  const decorationPanel = (
    <div>
      <div style={{ ...capLabel, marginBottom: 14 }}>Decoration</div>
      {cws ? (
        <React.Fragment>
          <div style={{ display: "flex", alignItems: "center", gap: 14, border: "1px solid #ececf0", borderRadius: 10, padding: "14px 16px", marginBottom: 12 }}>
            <span style={{ width: 56, height: 56, borderRadius: 6, background: "#f7f7f8", display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 4, flexShrink: 0 }}>
              <CatalogPhoto photo={hcmDefaultCw(src).front} alt={name} />
            </span>
            <span style={{ minWidth: 0 }}>
              <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>{name || src.title}</span>
              <span style={{ display: "block", fontSize: 12, color: "#71717a", marginTop: 2 }}>{cws.length} color ways on file · 3D renders, front and back</span>
            </span>
          </div>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "6px 16px" }}>
            {specRow("Print method", "Screen print")}
            {specRow("Print location", "Front center chest")}
            {specRow("Print area", "12\u2033 \u00d7 8\u2033")}
            {specRow("Views on file", "Front, back")}
          </div>
        </React.Fragment>
      ) : (
        <div style={{ border: "1px solid #ececf0", borderRadius: 10, padding: "14px 16px" }}>
          <div style={{ display: "flex", alignItems: "center", gap: 10, marginBottom: 10 }}>
            {(() => { const dm = PTE_DECO[m.deco] || PTE_DECO.na; return m.deco === "na"
              ? <span style={{ fontSize: 12.5, color: "#71717a" }}>Made to order — decoration is part of the design.</span>
              : <span style={pteChip(dm.bg, dm.ink, { display: "inline-flex", alignItems: "center", gap: 5 })}><span style={{ width: 5, height: 5, borderRadius: 999, background: dm.dot, display: "inline-block" }} />{dm.label}</span>; })()}
          </div>
          <div style={{ display: "flex", gap: 8 }}>
            {(!pteRestricted(src) || m.decoratable) && <button onClick={onOpenEditor} style={{ background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "9px 14px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" }}>Customize</button>}
          </div>
        </div>
      )}
    </div>
  );

  // ── 3A/3B (exploration turn 3): chip nav + stacked always-open section cards ──
  const backBtn = { display: "inline-flex", alignItems: "center", gap: 6, background: "#fff", border: "1px solid #e4e4e7", borderRadius: 8, padding: "8px 14px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", marginBottom: 16 };
  const defName = cws ? ((cws.find((c) => c.slug === defCw) || cws[0] || {}).name || "") : "";
  const openBuilder = () => { if (!cwCount) { onToast && onToast("Pick at least one color way first"); return; } setBuilderOpen(true); };
  const awbKey = ((store && store.id) || "ws") + ":" + src.id;
  const awbPlaced = !!(window.__awbState && window.__awbState[awbKey] && window.__awbState[awbKey].baseline);
  const editLink = { background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 12, fontWeight: 600, color: "#0e0e10", textDecoration: "underline", textUnderlineOffset: 2, cursor: "pointer", whiteSpace: "nowrap" };
  const centerLink = { background: "none", border: 0, padding: 0, fontFamily: "inherit", fontSize: 11.5, color: "#9a9aa1", textAlign: "center", textDecoration: "underline", textUnderlineOffset: 2, cursor: "pointer" };
  const btnGhost = { background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, padding: "9px 14px", fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit" };
  const factRow = (l, v, c) => (
    <div key={l} style={{ display: "flex", justifyContent: "space-between", gap: 10, fontSize: 12 }}>
      <span style={{ color: "#6b6b72" }}>{l}</span>
      <span style={{ fontWeight: 700, color: c || "#0e0e10", textAlign: "right" }}>{v}</span>
    </div>
  );
  const miniToggle = (on, onClick, onLabel, offLabel) => (
    <button onClick={onClick} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: "none", border: 0, padding: 0, cursor: "pointer", fontFamily: "inherit", flexShrink: 0 }}>
      <span style={{ fontSize: 12, fontWeight: 700, color: on ? "#1f9d55" : "#71717a" }}>{on ? onLabel : offLabel}</span>
      <span style={{ width: 32, height: 18, borderRadius: 999, position: "relative", background: on ? "#1f9d55" : "#d4d4d8", transition: "background .14s", display: "inline-block" }}>
        <span style={{ position: "absolute", top: 2, left: on ? 16 : 2, width: 14, height: 14, borderRadius: 999, background: "#fff", transition: "left .14s" }} />
      </span>
    </button>
  );
  const isCustom = !isVault && !isBundle && !cws && (!m || m.deco === "na");
  const secRefs = React.useRef({});
  const [activeSec, setActiveSec] = React.useState("essentials");
  const jumpTo = (key) => {
    setActiveSec(key);
    const el = secRefs.current[key]; if (!el) return;
    let sp = el.parentElement;
    while (sp && sp !== document.body) {
      const o = window.getComputedStyle(sp).overflowY;
      if ((o === "auto" || o === "scroll") && sp.scrollHeight > sp.clientHeight + 4) break;
      sp = sp.parentElement;
    }
    if (sp && sp !== document.body) sp.scrollTo({ top: sp.scrollTop + el.getBoundingClientRect().top - sp.getBoundingClientRect().top - 16, behavior: "smooth" });
    else window.scrollTo({ top: window.scrollY + el.getBoundingClientRect().top - 16, behavior: "smooth" });
  };
  const secCard = (key, label, body, opts) => {
    opts = opts || {};
    return { key, label, dot: !!opts.dot, node: (
      <div key={key} ref={(el) => { secRefs.current[key] = el; }} style={{ border: "1px solid #e4e4e7", borderRadius: 8, background: "#fff", boxShadow: "0 1px 2px rgba(20,20,30,.03)", padding: "24px" }}>
        <div style={{ display: "flex", alignItems: "baseline", justifyContent: "space-between", gap: 12, marginBottom: 16 }}>
          <span style={{ fontSize: 12, fontWeight: 700, letterSpacing: ".03em", color: "#a1a1aa", textTransform: "uppercase" }}>{label}</span>
          {opts.locked
            ? <span style={{ display: "inline-flex", alignItems: "center", gap: 10 }}>{opts.rightWhenLocked || null}<span title={PTE_LOCK_TIPS.full} style={{ display: "inline-flex", alignItems: "center", gap: 5, fontSize: 11, fontWeight: 700, color: "#b06f1e", cursor: "help" }}><IconLock size={11} /> Locked</span></span>
            : (opts.right || null)}
        </div>
        <div style={opts.locked ? { opacity: 0.55, pointerEvents: "none" } : undefined}>{body}</div>
      </div>
    ) };
  };
  const APP_SIZE_OPTS = ["AS SHOWN", "XS", "S", "M", "L", "XL", "XXL"];
  const appTitle = (s) => String(s || "").toLowerCase().split(" ").map((w) => w ? w.charAt(0).toUpperCase() + w.slice(1) : w).join(" ");
  const setAppSize = (id, field, v) => setAppSizes((list) => list.map((r) => r.id === id ? { ...r, [field]: v } : r));
  const setAllShown = () => setAppSizes((list) => list.map((r) => ({ ...r, adultSize: "AS SHOWN", youthSize: "AS SHOWN" })));
  const appUnset = appSizes.filter((r) => !r.adultSize).length;
  const floatWrap = { position: "relative", minWidth: 0 };
  const floatLabel = { position: "absolute", top: -8, left: 12, background: "#fff", padding: "0 4px", fontSize: 12, lineHeight: "16px", color: "#3f3f46", whiteSpace: "nowrap", zIndex: 1, pointerEvents: "none" };
  const floatInput = { width: "100%", height: 48, boxSizing: "border-box", border: "1px solid #d4d4d8", borderRadius: 10, padding: "0 40px 0 14px", fontSize: 13.5, fontFamily: "inherit", outline: "none", background: "#fff", color: "#0e0e10" };
  const floatSelect = { ...floatInput, appearance: "none", WebkitAppearance: "none", cursor: "pointer" };
  const clearBtn = (onClick) => (
    <button type="button" onClick={onClick} aria-label="Clear" style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)", border: 0, background: "none", padding: 0, cursor: "pointer", color: "#9a9aa1", display: "inline-flex" }}>
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.6"><circle cx="12" cy="12" r="9" /><path d="M9.5 9.5l5 5M14.5 9.5l-5 5" strokeLinecap="round" /></svg>
    </button>
  );
  const chevAbs = (
    <span style={{ position: "absolute", right: 14, top: "50%", transform: "translateY(-50%)", color: "#9a9aa1", pointerEvents: "none", display: "inline-flex" }}>
      <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M6 9l6 6 6-6" /></svg>
    </span>
  );
  const linkAbs = (
    <span style={{ position: "absolute", right: 12, top: "50%", transform: "translateY(-50%)", color: "#52525b", display: "inline-flex" }}>
      <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"><path d="M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71" /><path d="M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71" /></svg>
    </span>
  );
  const appGridCols = "minmax(90px, 170px) minmax(80px, 120px) minmax(80px, 120px) minmax(130px, 170px) minmax(140px, 1fr)";
  const appHead = { fontSize: 12.5, fontWeight: 700, color: "#71717a", letterSpacing: ".01em" };
  const appSizingBody = (
    <div>
      <div style={{ display: "grid", gridTemplateColumns: appGridCols, gap: "0 16px", alignItems: "center", padding: "10px 0", borderTop: "1px solid #e4e4e7", borderBottom: "1px solid #e4e4e7" }}>
        <span style={appHead}>Item</span>
        <span style={appHead}>Item Type</span>
        <span style={appHead}>Location</span>
        <span style={appHead}>Select Size</span>
        <span style={appHead}>Notes</span>
      </div>
      <div style={{ display: "flex", flexDirection: "column", gap: 28, padding: "24px 0 4px" }}>
        {appSizes.map((r) => (
          <div key={r.id} style={{ display: "grid", gridTemplateColumns: appGridCols, gap: "0 16px", alignItems: "center" }}>
            <span style={{ fontFamily: "Archivo, sans-serif", fontWeight: 800, fontSize: 26, letterSpacing: ".06em", lineHeight: 1, color: "#0e0e10", textTransform: "uppercase", whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{(r.details && r.details.TEXT && r.details.TEXT !== "NONE") ? r.details.TEXT : appTitle(r.type)}</span>
            <span style={{ fontSize: 14, color: "#18181b" }}>{appTitle(r.type)}</span>
            <span style={{ fontSize: 14, color: "#18181b" }}>{r.placement === "BACK" ? "Back Body" : "Front Body"}</span>
            <div style={floatWrap}>
              <span style={{ ...floatLabel, color: r.adultSize ? "#3f3f46" : "#e8590c" }}>Adult</span>
              <select value={r.adultSize || ""} onChange={(e) => setAppSize(r.id, "adultSize", e.target.value)} style={{ ...floatSelect, border: "1px solid " + (r.adultSize ? "#d4d4d8" : "#e8590c"), color: r.adultSize ? "#0e0e10" : "#e8590c" }}>
                {!r.adultSize && <option value="">Select size</option>}
                {APP_SIZE_OPTS.map((o) => <option key={o} value={o}>{o === "AS SHOWN" ? "As Shown" : o}</option>)}
              </select>
              {chevAbs}
            </div>
            <div style={floatWrap}>
              <input value={r.notes || ""} placeholder="Add notes" onChange={(e) => setAppSize(r.id, "notes", e.target.value)} style={floatInput} />
              {linkAbs}
            </div>
          </div>
        ))}
      </div>
      <div style={{ marginTop: 20, fontSize: 12, color: "#9a9aa1", lineHeight: 1.5 }}>Sizes come from the saved design — confirm each application's print size before the product can publish. Garment sizes are chosen by shoppers at checkout.</div>
    </div>
  );

  const essentialsBody = (
    <div>
      <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "26px 24px", paddingTop: 8, marginBottom: 20 }}>
        <div style={floatWrap}>
          <span style={floatLabel}>Product Name</span>
          <input value={name} onChange={(e) => setName(e.target.value)} disabled={fullLock} style={floatInput} />
          {!fullLock && !!name && clearBtn(() => setName(""))}
        </div>
        {!isVault && (
          <div style={floatWrap}>
            <span style={floatLabel}>Product Settings</span>
            <select value={optRequired ? "req" : optSupplied ? "sup" : "fan"} disabled={fullLock} onChange={(e) => {
              const v = e.target.value;
              setOptRequired(v === "req"); setOptSupplied(v === "sup");
              if (v === "fan") { setProdCat(""); setProdSubcat(""); } else { setProdCat("Team Gear"); setProdSubcat(""); setCatError(false); }
              setTeamError(false);
            }} style={floatSelect}>
              <option value="fan">Fan Gear</option>
              <option value="req">Team Required</option>
              <option value="sup">Team Supplied</option>
            </select>
            {chevAbs}
          </div>
        )}
        <div style={floatWrap}>
          <span style={floatLabel}>Category{!isVault && !teamOn && <span style={{ color: "#e1251b" }}> *</span>}</span>
          {teamOn ? (
            <div style={{ ...floatInput, display: "flex", alignItems: "center", gap: 8, background: "#fafafb", fontSize: 13, fontWeight: 600 }}>
              Team Gear
              <span style={{ marginLeft: "auto", display: "inline-flex", color: "#9a9aa1" }}><IconLock size={13} /></span>
            </div>
          ) : (
            <React.Fragment>
              <select value={prodCat} onChange={(e) => { setProdCat(e.target.value); setProdSubcat(""); setCatError(false); }} style={{ ...floatSelect, border: "1px solid " + (catError ? "#e1251b" : "#d4d4d8") }}>
                <option value="">{isVault ? "Pick a category (optional)" : "Select a category"}</option>
                {catOptions.map((c) => <option key={c} value={c}>{c}</option>)}
              </select>
              {prodCat ? clearBtn(() => { setProdCat(""); setProdSubcat(""); }) : chevAbs}
            </React.Fragment>
          )}
        </div>
        <div style={floatWrap}>
          <span style={floatLabel}>Subcategory</span>
          <select value={prodSubcat} onChange={(e) => setProdSubcat(e.target.value)} disabled={!prodCat || catSubs.length === 0} style={floatSelect}>
            <option value="">{!prodCat ? "Pick a category first" : catSubs.length === 0 ? "No subcategories" : "No subcategory"}</option>
            {catSubs.map((sc) => <option key={sc} value={sc}>{sc}</option>)}
          </select>
          {prodSubcat ? clearBtn(() => setProdSubcat("")) : chevAbs}
        </div>
      </div>
      {catError && <div style={{ fontSize: 12, color: "#e1251b", marginBottom: 14, fontWeight: 600 }}>Select a category before saving — items no longer default to Fan Gear.</div>}
    </div>
  );

  const pricingBody = isVault ? (
    <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "26px 24px", maxWidth: 480, paddingTop: 8 }}>
      <div style={floatWrap}><span style={floatLabel}>Retail price</span><input value={retail} onChange={(e) => setRetail(e.target.value)} style={{ ...floatInput, padding: "0 14px" }} /></div>
      <div style={floatWrap}><span style={floatLabel}>Base price</span><input value={base} onChange={(e) => setBase(e.target.value)} style={{ ...floatInput, padding: "0 14px" }} /></div>
    </div>
  ) : (
    // Governing rule 3 — shared card body; the $ / % control mounts in the
    // section header (secCard right) so one mode governs the whole section.
    <PriceSettingsCard bare pricing={pricing} onChange={setPricing} fundOn={fundOn} locked={fullLock} covered={optSupplied} />
  );

    const sizesChips = (
    <div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}>
      {PDP_SIZES.map((sz) => {
        const on = !!sizesOn[sz];
        return (
          <button key={sz} type="button" onClick={() => setSizesOn((z) => ({ ...z, [sz]: !z[sz] }))} style={{
            minWidth: 46, padding: "9px 12px", borderRadius: 6, cursor: "pointer", fontFamily: "inherit",
            fontSize: 12.5, fontWeight: 700, border: "1px solid " + (on ? "#0e0e10" : "#e4e4e7"),
            background: on ? "#0e0e10" : "#fff", color: on ? "#fff" : "#a1a1aa", transition: "all .12s",
          }}>{sz}</button>
        );
      })}
    </div>
  );
  const sizesCwBody = (
    <div style={{ maxWidth: 640 }}>
      {cws && (
        <React.Fragment>
          <div style={{ fontSize: 12, color: "#9a9aa1", marginBottom: 10 }}>Pick the color ways this store offers. The default is what buyers see first on the storefront.</div>
          <div style={{ border: "1px solid #ececf0", borderRadius: 10, overflow: "hidden", marginBottom: 20, background: "#fff" }}>
            {cws.map((c, i) => {
              const on = !!cwOn[c.slug];
              const isDef = defCw === c.slug;
              const active = gSel === i;
              return (
                <div key={c.slug} onClick={() => setGSel(i)} style={{ display: "flex", alignItems: "center", gap: 12, padding: "10px 14px", borderBottom: i < cws.length - 1 ? "1px solid #f4f4f6" : 0, cursor: "pointer", background: active ? "#f4f4f6" : "#fff" }}>
                  <input type="checkbox" checked={on} onClick={(e) => e.stopPropagation()} onChange={() => {
                    setCwOn((sOn) => ({ ...sOn, [c.slug]: !sOn[c.slug] }));
                    if (isDef && on) { const nx = cws.find((x) => x.slug !== c.slug && cwOn[x.slug]); if (nx) setDefCw(nx.slug); }
                  }} style={{ width: 16, height: 16, accentColor: "#0e0e10", cursor: "pointer", flexShrink: 0 }} />
                  <span style={{ width: 22, height: 22, borderRadius: "50%", background: c.hex, border: c.slug === "white" ? "1px solid #d4d4d8" : "1px solid rgba(9,9,11,.15)", flexShrink: 0 }} />
                  <span style={{ fontSize: 13, fontWeight: 600, color: on ? "#0e0e10" : "#a1a1aa", minWidth: 0, flex: 1 }}>{c.name}</span>
                  {isDef ? (
                    <span style={{ fontSize: 9, fontWeight: 800, letterSpacing: ".06em", color: "#fff", background: "#0e0e10", borderRadius: 4, padding: "2px 8px", flexShrink: 0 }}>DEFAULT</span>
                  ) : on && (
                    <button onClick={(e) => { e.stopPropagation(); setDefCw(c.slug); setGSel(i); }} style={{ background: "none", border: 0, fontSize: 11, fontWeight: 700, color: "#9a9aa1", cursor: "pointer", fontFamily: "inherit", textDecoration: "underline", textUnderlineOffset: 2, flexShrink: 0 }}>Make default</button>
                  )}
                </div>
              );
            })}
          </div>
        </React.Fragment>
      )}
      <div style={{ fontSize: 12, color: "#9a9aa1", marginBottom: 10 }}>{sizeCount + " of " + PDP_SIZES.length + " sizes offered in this store."}</div>
      {sizesChips}
    </div>
  );

  const catText = teamOn ? "Team Gear" : (prodCat ? prodCat + (prodSubcat ? " · " + prodSubcat : "") : "No category");
  const sections = [];
  if (!isBundle) {
    sections.push(secCard("essentials", "Essentials", essentialsBody, {
      right: catError ? <span style={{ fontSize: 11.5, fontWeight: 700, color: "#e1251b" }}>Select a category</span> : null,
    }));
    if (!isVault && store?.storeType === "league") {
      sections.push(secCard("teams", "Assign to Teams", (
        <div>
          <RosterAssignField variant="dropdown" direction="down" value={rosters} onChange={(v) => { setRosters(v); setTeamError(false); }} accent="#e1251b" minOne={teamOn} store={store} />
          <div style={{ fontSize: 12, color: "#71717a", lineHeight: 1.5, marginTop: 12 }}>{teamOn ? "Team Required and Team Supplied items must stay assigned to at least one Team." : "Leave empty to make it available league-wide."}</div>
          {teamError && <div style={{ fontSize: 12, color: "#e1251b", marginTop: 8, fontWeight: 600 }}>Assign this item to at least one team or division before saving.</div>}
        </div>
      ), {
        right: teamError ? <span style={{ fontSize: 11.5, fontWeight: 700, color: "#e1251b" }}>Assign at least one team</span> : null,
      }));
    }
    sections.push(secCard("pricing", "Price Settings", pricingBody, {
      locked: fullLock,
      right: (isVault || optSupplied) ? null : <PSModeControl mode={pricingMode(pricing)} onChange={(m2) => { if (m2 !== pricingMode(pricing)) setPricing(switchPricingMode(pricing, m2, pricing.fundraisingOn ?? fundOn)); }} />,
      // Locked items keep the $ / % control visible but inert (spec: mode control inert).
      rightWhenLocked: (isVault || optSupplied) ? null : <PSModeControl locked mode={pricingMode(pricing)} onChange={() => {}} />,
    }));
    if (cws) {
      sections.push(secCard("sizes", "Sizes & color ways", sizesCwBody, { locked: fullLock }));
      sections.push({ key: "deco", label: "Decoration", node: (
        <div key="deco" ref={(el) => { secRefs.current.deco = el; }} style={{ border: "1px solid #e4e4e7", borderRadius: 8, background: "#fff", boxShadow: "0 1px 2px rgba(20,20,30,.03)", padding: "20px 24px", display: "flex", alignItems: "center", gap: 14 }}>
          <span style={{ width: 48, height: 52, borderRadius: 6, background: "#f7f7f8", display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 4, flexShrink: 0 }}><CatalogPhoto photo={hcmDefaultCw(src).front} alt={name || src.title} /></span>
          <span style={{ flex: 1, minWidth: 0 }}>
            <span style={{ display: "block", fontSize: 13, fontWeight: 700, color: "#0e0e10" }}>Decoration — Front center chest</span>
            <span style={{ display: "block", fontSize: 12, color: "#9a9aa1", marginTop: 2 }}>{"Screen print · 12″ × 8″ · " + (awbPlaced ? "logo placed" : "logo not placed yet — set it once, copy to every color way")}</span>
          </span>
          <button onClick={openBuilder} style={{ height: 36, padding: "0 16px", background: "#fff", border: "1px solid #d4d4d8", borderRadius: 6, fontSize: 12.5, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap", flexShrink: 0 }}>Artwork builder</button>
        </div>
      ) });
    } else if (isCustom) {
      sections.push(secCard("appsizing", "Application sizing", appSizingBody, {
        locked: fullLock, dot: appUnset > 0,
        right: <button onClick={setAllShown} style={editLink}>Set all to As Shown</button>,
      }));
    } else {
      sections.push(secCard("sizes", "Sizes", sizesCwBody, { locked: fullLock }));
      if (!isVault && m) {
        sections.push(secCard("deco", "Decoration", (
          <div>
            <div style={{ marginBottom: 10 }}>
              {m.deco === "na"
                ? <span style={{ fontSize: 12.5, color: "#71717a" }}>Made to order — decoration is part of the design.</span>
                : (() => { const dm = PTE_DECO[m.deco] || PTE_DECO.na; return <span style={pteChip(dm.bg, dm.ink, { display: "inline-flex", alignItems: "center", gap: 5 })}><span style={{ width: 5, height: 5, borderRadius: 999, background: dm.dot, display: "inline-block" }} />{dm.label}</span>; })()}
            </div>
            <div style={{ display: "flex", gap: 8 }}>
              {(!pteRestricted(src) || m.decoratable) && <button onClick={onOpenEditor} style={btnGhost}>Customize</button>}
            </div>
          </div>
        )));
      }
    }
    if (!isVault) {
      const sfRows = [];
      sfRows.push(["Visibility",
        p.hidden ? "Hidden — buyers can't see or order this item." : "Live on the storefront once the store launches.",
        miniToggle(!p.hidden, () => { onSave({ ...p, hidden: !p.hidden }); onToast && onToast(p.hidden ? '"' + p.title + '" is visible on the storefront' : '"' + p.title + '" hidden from the storefront'); }, "Visible", "Hidden")]);
      if (optRequired) sfRows.push(["Allow opt-out",
        "“I already own this” counts as satisfied in required-gear tracking.",
        miniToggle(!!p.optOut, () => { onSave({ ...p, optOut: !p.optOut }); onToast && onToast(p.optOut ? "Opt-out disabled" : "Opt-out enabled"); }, "On", "Off")]);
      sfRows.push(["Performance",
        stats ? (stats.orders + " orders · " + stats.units + " units · $" + stats.revenue.toFixed(0) + " revenue") : "No orders yet — stats appear once this product sells.",
        null]);
      sections.unshift(secCard("storefront", "Storefront", (
        <div>
          {sfRows.map((r, i) => (
            <div key={r[0]} style={{ display: "flex", alignItems: "center", gap: 16, paddingTop: i === 0 ? 0 : 10, paddingBottom: i === sfRows.length - 1 ? 0 : 10, borderTop: i > 0 ? "1px solid #f4f4f6" : 0 }}>
              <span style={{ width: 128, flexShrink: 0, fontSize: 12, fontWeight: 700, letterSpacing: ".02em", color: "#09090b" }}>{r[0]}</span>
              <span style={{ flex: 1, minWidth: 0, fontSize: 12, color: "#71717a" }}>{r[1]}</span>
              {r[2]}
            </div>
          ))}
        </div>
      )));
      // Stock items: surface the Artwork builder banner at the very top, above Storefront.
      if (cws) { const di = sections.findIndex((s) => s.key === "deco"); if (di > 0) sections.unshift(sections.splice(di, 1)[0]); }
    }
  }

  const badgeChip = (txt, fg, bg) => <span key={txt} style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".06em", color: fg, background: bg, borderRadius: 4, padding: "3px 8px" }}>{txt}</span>;
  const chips = [];
  if (isVault || (p && p.srcKind === "vault")) chips.push(badgeChip("PROSTOCK", "#6b6b72", "#f4f4f5"));
  if (!isVault && !isBundle) {
    chips.push(optRequired ? badgeChip("TEAM REQUIRED", "#e8590c", "#fff4e6") : optSupplied ? badgeChip("TEAM SUPPLIED", "#2944a8", "#e9eefb") : badgeChip("FAN GEAR", "#1f7a44", "#e7f5ed"));
  }

  const facts = [];
  facts.push(factRow("Retail", isVault ? "$" + retail : "$" + pricing.adult.retail));
  if (cws) {
    facts.push(factRow("Sizes", sizeCount + " of " + PDP_SIZES.length));
    facts.push(factRow("Color ways", cwCount + " · " + defName));
    facts.push(factRow("Artwork", awbPlaced ? "Placed" : "Not placed", awbPlaced ? "#1f7a44" : "#b06f1e"));
  } else if (isCustom) {
    facts.push(factRow("Category", catText));
    facts.push(factRow("App. sizing", appUnset ? appUnset + " to confirm" : "All confirmed", appUnset ? "#b06f1e" : "#1f7a44"));
    facts.push(factRow("SKU", styleNo || src.sku || "—"));
  } else {
    facts.push(factRow("Sizes", sizeCount + " of " + PDP_SIZES.length));
    facts.push(factRow("Category", catText));
  }

  if (isBundle) {
    return (
      <div style={{ paddingBottom: 60 }}>
        <button onClick={onBack} style={backBtn}><IconChevronLeft size={13} /> Back to Catalog</button>
        <div style={{ background: "#fff", border: "1px solid #ececf0", borderRadius: 12, boxShadow: "0 1px 3px rgba(20,20,30,.04)", overflow: "visible" }}>
          <div style={{ padding: "18px 28px 0" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 12, marginBottom: 14, flexWrap: "wrap" }}>
              <span style={{ width: 30, height: 30, borderRadius: 5, background: "#f7f7f8", display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden", padding: 2, flexShrink: 0 }}>
                {headerThumb ? <CatalogPhoto photo={headerThumb} alt={name} /> : (p && p.thumbnail) || <span style={{ fontSize: 12, fontWeight: 800, color: "#c4c4cb" }}>{(name || "P").slice(0, 1)}</span>}
              </span>
              <span style={{ fontSize: 14.5, fontWeight: 600, color: "#27272a" }}>{name || src.title}</span>
              <span style={{ display: "inline-flex", alignItems: "center", gap: 6, background: "#e7f0fc", color: "#2563eb", padding: "3px 10px", borderRadius: 999, fontSize: 10.5, fontWeight: 700 }}><BundleStackIcon size={11} color="#2563eb" /> Bundle</span>
              {fullLock && <span title={PTE_LOCK_TIPS.full} style={{ display: "inline-flex", alignItems: "center", gap: 5, fontSize: 10.5, fontWeight: 800, color: "#b06f1e", background: "#fff8ef", border: "1px solid #f2e2c9", borderRadius: 999, padding: "3px 10px", cursor: "help" }}><IconLock size={10} /> HAS ORDERS</span>}
            </div>
            <div style={{ borderBottom: "1px solid #ececf0" }} />
          </div>
          <div style={{ padding: "24px 28px 30px" }}>
            <BundleEditBody
              items={bundleItems} store={store} locked={fullLock} fundOn={fundOn}
              bundleType={p.bundleType || "all"}
              bundleRequired={bundleRequired} bundleSupplied={bundleSupplied}
              onBundleRequired={setBundleRequired} onBundleSupplied={setBundleSupplied}
              bundleProfit={bundleProfit} onBundleProfit={updateBundleProfit}
              bundleHidden={memberIds.length < 2 ? true : bundleHidden}
              onBundleHidden={(v) => { if (memberIds.length < 2) { onToast && onToast("Incomplete — the bundle needs a second item before it can go visible"); return; } setBundleHidden(v); }}
              hideRetail={bundleHideRetail} onHideRetail={setBundleHideRetail}
              freeShipping={bundleFreeShip} onFreeShipping={setBundleFreeShip}
              itemPricings={itemPricings} expandedId={expandedItemId}
              onToggleExpanded={(id) => setExpandedItemId((cur) => cur === id ? null : id)}
              onUpdateItemPricing={updateItemPricing}
              onRemoveMember={(id) => { setMemberIds((ids) => ids.filter((x) => x !== id)); onToast && onToast("Removed from bundle — save to apply"); }}
              onAddItems={() => setAddingMembers(true)}
              onDeleteBundle={() => setDeleting(true)} />
          </div>
          <div style={{ borderTop: "1px solid #ececf0", padding: "16px 28px", display: "flex", alignItems: "center", gap: 12, background: "#fff", borderBottomLeftRadius: 12, borderBottomRightRadius: 12 }}>
            <button onClick={() => setDeleting(true)} style={{ background: "#fff", color: "#e1251b", border: "1px solid #f1c4c2", borderRadius: 6, padding: "11px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", display: "inline-flex", alignItems: "center", gap: 7, fontFamily: "inherit" }}>
              <IconTrash size={13} /> Delete Bundle
            </button>
            <span style={{ fontSize: 12.5, color: "#71717a" }}>{bundleItems.length + " items in this bundle"}</span>
            <div style={{ flex: 1 }} />
            <button onClick={handleSave} style={{ background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "11px 24px", fontSize: 13, fontWeight: 700, cursor: "pointer", fontFamily: "inherit" }}>Save Changes</button>
          </div>
        </div>
        {addingMembers && (() => {
          const bundledElsewhere = new Set(allProducts.flatMap((b) => b.isBundle && b.id !== p.id ? b.bundleItemIds || [] : []));
          const eligible = allProducts.filter((x) => !x.isBundle && !memberIds.includes(x.id) && !bundledElsewhere.has(x.id));
          return (
            <Modal open onClose={() => setAddingMembers(false)} width={560}>
              <div style={{ padding: "24px 28px 28px" }}>
                <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 6 }}>
                  <h3 style={{ margin: 0, fontSize: 18, fontWeight: 800, color: "#0e0e10" }}>Add items to bundle</h3>
                  <button onClick={() => setAddingMembers(false)} style={{ background: "none", border: 0, cursor: "pointer", color: "#6b6b72", padding: 4 }}><IconX size={18} /></button>
                </div>
                <div style={{ fontSize: 12.5, color: "#6b6b72", marginBottom: 16 }}>Only un-bundled catalog items are eligible — anything else is added to the catalog first via Add Products.</div>
                <div style={{ display: "flex", flexDirection: "column", gap: 8, maxHeight: "50vh", overflowY: "auto" }}>
                  {eligible.length === 0 && <div style={{ padding: "28px 12px", textAlign: "center", fontSize: 12.5, color: "#9a9aa1", border: "1px dashed #e5e5e9", borderRadius: 8 }}>No un-bundled items left in this catalog.</div>}
                  {eligible.map((it) => (
                    <PickerCard key={it.id} product={it} store={store} onAdd={() => { setMemberIds((ids) => [...ids, it.id]); setAddingMembers(false); onToast && onToast('"' + it.title + '" added — save to apply'); }} />
                  ))}
                </div>
              </div>
            </Modal>
          );
        })()}
        {deleting && (
          <ProductDeleteModal
            product={p}
            store={store}
            onClose={() => setDeleting(false)}
            onConfirm={() => { setDeleting(false); onRemove(p.id); onToast && onToast('"' + p.title + '" deleted'); onBack(); }}
            onGoToOrders={() => { setDeleting(false); window.__goToPendingOrder && window.__goToPendingOrder(); }} />
        )}
      </div>
    );
  }

  return (
    <div style={{ paddingBottom: 60 }}>
      <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", gap: 12, marginBottom: 16 }}>
        <button onClick={onBack} style={{ ...backBtn, marginBottom: 0 }}>
          <IconChevronLeft size={13} /> {isVault ? "Back to Stock Vault" : "Back to Catalog"}
        </button>
        <div style={{ display: "flex", alignItems: "center", gap: 8 }}>
          {!isVault && <button onClick={() => setDeleting(true)} style={{ height: 36, padding: "0 22px", background: "#fff", border: "1px solid #b91c1c", borderRadius: 4, fontSize: 13, fontWeight: 600, color: "#b91c1c", cursor: "pointer", fontFamily: "inherit" }}>Delete Product</button>}
          <button onClick={handleSave} style={{ height: 36, padding: "0 22px", background: "#18181b", color: "#fff", border: 0, borderRadius: 4, fontSize: 13, fontWeight: 600, cursor: "pointer", fontFamily: "inherit" }}>{isVault ? "Add to Catalog" : "Save Changes"}</button>
        </div>
      </div>

      <div style={{ display: "grid", gridTemplateColumns: "280px minmax(0, 1fr)", gap: 24, alignItems: "start" }}>
        <div style={{ position: "sticky", top: 24, border: "1px solid #ececf0", borderRadius: 12, background: "#fff", boxShadow: "0 1px 3px rgba(20,20,30,.04)", padding: 16, display: "flex", flexDirection: "column", gap: 12 }}>
          <div style={{ position: "relative", height: 200, borderRadius: 8, background: "#f7f7f8", display: "flex", alignItems: "center", justifyContent: "center", padding: 12, boxSizing: "border-box" }}>
            {mainPhoto
              ? <div style={{ width: "100%", height: "100%", maxWidth: 170 }}><CatalogPhoto photo={mainPhoto} alt={name || src.title} /></div>
              : (p && p.thumbnail)
                ? <span style={{ width: 110, height: 150, display: "inline-flex", alignItems: "center", justifyContent: "center", overflow: "hidden" }}>{p.thumbnail}</span>
                : <span style={{ color: "#c7c7cd", fontSize: 12 }}>No preview</span>}
            {cws && (
              <span style={{ position: "absolute", bottom: 8, left: "50%", transform: "translateX(-50%)", display: "inline-flex", background: "#fff", border: "1px solid #e5e5e9", borderRadius: 999, padding: 2 }}>
                {["front", "back"].map((sd) => (
                  <button key={sd} type="button" onClick={() => setGSide(sd)} style={{
                    border: 0, borderRadius: 999, padding: "3px 12px", fontSize: 10, fontWeight: 800, letterSpacing: ".05em", cursor: "pointer", fontFamily: "inherit",
                    background: gSide === sd ? "#0e0e10" : "transparent", color: gSide === sd ? "#fff" : "#6b6b72", textTransform: "uppercase",
                  }}>{sd}</button>
                ))}
              </span>
            )}
          </div>
          <div>
            <div style={{ fontSize: 15, fontWeight: 700, lineHeight: 1.3, textWrap: "pretty" }}>{name || src.title}</div>
            <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 2 }}>{src.subtitle || styleNo || src.sku || "—"}</div>
          </div>
          {chips.length > 0 && <div style={{ display: "flex", alignItems: "center", gap: 6, flexWrap: "wrap" }}>{chips}</div>}
          {cws && (
            <div style={{ display: "flex", gap: 5, alignItems: "center", flexWrap: "wrap" }}>
              {cws.map((c, i) => (
                <button key={c.slug} type="button" title={c.name} onClick={() => setGSel(i)} style={{
                  width: 14, height: 14, borderRadius: "50%", background: c.hex, cursor: "pointer", padding: 0, flexShrink: 0,
                  border: c.slug === "white" ? "1px solid #d4d4d8" : "1px solid rgba(9,9,11,.15)",
                  opacity: cwOn[c.slug] ? 1 : .3,
                  boxShadow: defCw === c.slug ? "0 0 0 1.5px #0e0e10" : "none",
                }} />
              ))}
            </div>
          )}
          <div style={{ borderTop: "1px solid #f0f0f2", paddingTop: 12, display: "flex", flexDirection: "column", gap: 8 }}>{facts}</div>
          <div style={{ display: "flex", flexDirection: "column", gap: 8, marginTop: 2 }}>
            {cws && <span style={{ fontSize: 11, color: "#9a9aa1", textAlign: "center", lineHeight: 1.5 }}>Placement is set once in the artwork builder, then tuned per color way.</span>}
          </div>
        </div>

        <div style={{ display: "flex", flexDirection: "column", gap: 16, minWidth: 0 }}>
          {fullLock && (
            <div style={{ display: "flex", alignItems: "flex-start", gap: 10, border: "1px solid #f2e2c9", borderRadius: 12, padding: "12px 16px", fontSize: 12, color: "#6b5314", lineHeight: 1.5, background: "#fff8ef" }}>
              <IconLock size={13} style={{ flexShrink: 0, marginTop: 2, color: "#b06f1e" }} />
              <span>This item has orders, so design and pricing are locked. Team assignment, Category and Subcategory stay editable; cancelling every order re-enables the rest.</span>
            </div>
          )}
          {sections.map((s) => s.node)}
        </div>
      </div>

      {builderOpen && cws && (
        <ArtworkBuilderPage item={src} store={store} name={name || src.title} styleNo={styleNo || src.sku}
          cwList={cws.filter((c) => cwOn[c.slug])} sizeCount={sizeCount} sizeTotal={PDP_SIZES.length}
          onClose={() => setBuilderOpen(false)} />
      )}
      {deleting && p && (
        <ProductDeleteModal
          product={p}
          store={store}
          onClose={() => setDeleting(false)}
          onConfirm={() => { setDeleting(false); onRemove(p.id); onToast && onToast('"' + p.title + '" deleted'); onBack(); }}
          onGoToOrders={() => { setDeleting(false); window.__goToPendingOrder && window.__goToPendingOrder(); }} />
      )}
    </div>
  );
}

// ═══ Artwork Builder — colorway placement, ported 1:1 from the Colorway Placement reference ═══
const AWB_AREA = { x: 29, y: 28, w: 42, h: 26 };
const AWB_PL = { cx: 50, cy: 41, w: 26, rot: 0 };
const AWB_SWATCHES = ["#FFFFFF", "#09090B", "#EF4444", "#C7CACC", "#1E43A6", "#D9A83B", "#17402B"];
const AWB_CAP = { fontSize: 11, fontWeight: 800, letterSpacing: ".09em", textTransform: "uppercase", color: "#a1a1aa" };
const awbLink = { fontSize: 11, textDecoration: "underline", textUnderlineOffset: 2, cursor: "pointer", background: "none", border: 0, padding: 0, fontFamily: "inherit", color: "#09090b" };
function awbWord(store) {
  const raw = String((store && store.teamName) || "Team").trim();
  return (raw.split(/\s+/)[0] || "Team").toUpperCase();
}
function awbMarks(store) {
  const word = awbWord(store);
  return [
    { id: "wordmark", label: word.charAt(0) + word.slice(1).toLowerCase() + " wordmark", paths: 14, layers: [{ id: "letters", name: "Letters", def: "#FFFFFF" }, { id: "outline", name: "Outline", def: "#EF4444" }, { id: "bar", name: "Underbar", def: "#EF4444" }] },
    { id: "shield", label: "Shield crest", paths: 9, layers: [{ id: "letters", name: "Letter", def: "#FFFFFF" }, { id: "outline", name: "Shield edge", def: "#FFFFFF" }, { id: "bar", name: "Shield fill", def: "#EF4444" }] },
    { id: "monogram", label: word.slice(0, 2) + " monogram", paths: 6, layers: [{ id: "letters", name: "Monogram", def: "#FFFFFF" }, { id: "outline", name: "Ring", def: "#FFFFFF" }, { id: "bar", name: "Ring accent", def: "#EF4444" }] },
  ];
}
function awbArt(m, colors, mode, store) {
  if (m && m.src) {
    if (mode === "one") return <span style={{ display: "block", width: "100%", aspectRatio: String(m.aspect || 1), background: colors.print || "#FFFFFF", WebkitMaskImage: "url(" + m.src + ")", maskImage: "url(" + m.src + ")", WebkitMaskSize: "contain", maskSize: "contain", WebkitMaskRepeat: "no-repeat", maskRepeat: "no-repeat", WebkitMaskPosition: "center", maskPosition: "center" }} />;
    return <img src={m.src} alt={m.label} draggable={false} style={{ display: "block", width: "100%", height: "auto", pointerEvents: "none" }} />;
  }
  const word = awbWord(store);
  const fs = Math.min(46, Math.round(276 / Math.max(3, word.length)));
  if (m.id === "shield") return (
    <svg viewBox="0 0 200 130" width="100%" height="100%" style={{ display: "block", overflow: "visible" }}>
      <path d="M100 6 L182 30 V70 C182 100 145 120 100 126 C55 120 18 100 18 70 V30 Z" fill={colors.bar} stroke={colors.outline} strokeWidth={6} />
      <text x={100} y={88} textAnchor="middle" fill={colors.letters} style={{ font: "800 62px Archivo, sans-serif" }}>{word.slice(0, 1)}</text>
    </svg>
  );
  if (m.id === "monogram") return (
    <svg viewBox="0 0 200 130" width="100%" height="100%" style={{ display: "block", overflow: "visible" }}>
      <circle cx={100} cy={65} r={56} fill="none" stroke={colors.outline} strokeWidth={7} />
      <circle cx={100} cy={65} r={44} fill="none" stroke={colors.bar} strokeWidth={3} />
      <text x={100} y={84} textAnchor="middle" fill={colors.letters} style={{ font: "800 46px Archivo, sans-serif", letterSpacing: 2 }}>{word.slice(0, 2)}</text>
    </svg>
  );
  return (
    <svg viewBox="0 0 200 130" width="100%" height="100%" style={{ display: "block", overflow: "visible" }}>
      <text x={100} y={72} textAnchor="middle" fill="none" stroke={colors.outline} strokeWidth={9} strokeLinejoin="round" style={{ font: "800 " + fs + "px Archivo, sans-serif" }}>{word}</text>
      <text x={100} y={72} textAnchor="middle" fill={colors.letters} style={{ font: "800 " + fs + "px Archivo, sans-serif" }}>{word}</text>
      <rect x={34} y={88} width={132} height={9} rx={4} fill={colors.bar} />
    </svg>
  );
}
function ArtworkBuilderPage({ item, store, name, styleNo, cwList, sizeCount, sizeTotal, onClose, backLabel, actionLabel, onAction, onSaveChanges, pickedStores }) {
  const key = ((store && store.id) || "ws") + ":" + item.id;
  const marks = [];
  const freshCw = () => ({ placement: { ...AWB_PL }, copied: false, colors: null, visible: true, touched: false, printMode: null });
  const [st, setSt] = React.useState(() => {
    const saved = (window.__awbState || {})[key];
    const cws = {};
    cwList.forEach((c) => { cws[c.slug] = (saved && saved.cws && saved.cws[c.slug]) || freshCw(); });
    const first = cwList[0] && cwList[0].slug;
    if (saved) return { ...saved, cws, view: "front", applyModal: false, toast: "", activeId: cws[saved.activeId] ? saved.activeId : first, sourceId: cws[saved.sourceId] ? saved.sourceId : first };
    return { activeId: first, sourceId: first, view: "front", markId: null, uploads: [], cws, baseline: null, applyModal: false, toast: "" };
  });
  React.useEffect(() => { window.__awbState = window.__awbState || {}; window.__awbState[key] = st; }, [st, key]);
  React.useEffect(() => () => clearTimeout(tRef.current), []);
  const canvasRef = React.useRef(null);
  const tRef = React.useRef(null);
  const patch = (p) => setSt((s) => ({ ...s, ...(typeof p === "function" ? p(s) : p) }));
  const setCwP = (id, x) => setSt((s) => { const cws = { ...s.cws }; cws[id] = { ...cws[id], ...x }; return { ...s, cws }; });
  const flash = (msg) => { clearTimeout(tRef.current); patch({ toast: msg }); tRef.current = setTimeout(() => patch({ toast: "" }), 2400); };
  const allMarks = marks.concat(st.uploads);
  const mark = (id) => allMarks.find((mm) => mm.id === (id || st.markId)) || null;
  const hasMark = !!mark();
  const colorsFor = (id) => { const c = st.cws[id] || {}; const mm = mark() || {}; const out = {}; (mm.layers || []).forEach((l) => { out[l.id] = (c.colors && c.colors[l.id]) || l.def; }); return out; };
  const printModeFor = (id) => (st.cws[id] && st.cws[id].printMode) || "full";
  const numeric = (k, val) => { const cur = { ...((st.cws[st.activeId] || {}).placement || AWB_PL) }; cur[k] = val; setCwP(st.activeId, { placement: cur, touched: true }); };
  const beginDrag = (ev, dragMode) => {
    ev.preventDefault(); ev.stopPropagation();
    const box = canvasRef.current; if (!box) return;
    const r = box.getBoundingClientRect();
    const id = st.activeId;
    const start = { ...((st.cws[id] || {}).placement || AWB_PL) };
    const sx = ev.clientX, sy = ev.clientY;
    const move = (e) => {
      const dx = ((e.clientX - sx) / r.width) * 100;
      const dy = ((e.clientY - sy) / r.height) * 100;
      let npl;
      if (dragMode === "move") {
        const halfW = start.w / 2;
        const halfH = (start.w * 0.65 * (r.width / r.height)) / 2;
        npl = { cx: Math.min(AWB_AREA.x + AWB_AREA.w - halfW, Math.max(AWB_AREA.x + halfW, start.cx + dx)), cy: Math.min(AWB_AREA.y + AWB_AREA.h - halfH, Math.max(AWB_AREA.y + halfH, start.cy + dy)), w: start.w, rot: start.rot };
      } else {
        npl = { ...start, w: Math.min(40, Math.max(12, start.w + dx * 1.6)) };
      }
      setCwP(id, { placement: npl, touched: true });
    };
    const up = () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); };
    window.addEventListener("pointermove", move);
    window.addEventListener("pointerup", up);
  };
  const handleFiles = (ev) => {
    const files = Array.prototype.slice.call(ev.target.files || []);
    if (!files.length) return;
    files.forEach((file) => {
      const reader = new FileReader();
      reader.onload = () => {
        const src = reader.result;
        const probe = new Image();
        const add = (aspect) => {
          const id = "up-" + Date.now() + "-" + Math.round(Math.random() * 999);
          const isSvg = /svg/i.test(file.type) || /\.svg$/i.test(file.name);
          const up = { id, label: file.name.replace(/\.[^.]+$/, ""), src, aspect, isSvg, layers: [{ id: "print", name: "Print color", def: "#FFFFFF" }] };
          setSt((s) => ({ ...s, uploads: s.uploads.concat([up]), markId: id }));
          flash("\u201c" + up.label + "\u201d added");
        };
        probe.onload = () => add(probe.naturalWidth / probe.naturalHeight || 1);
        probe.onerror = () => add(1);
        probe.src = src;
      };
      reader.readAsDataURL(file);
    });
    ev.target.value = "";
  };
  if (!cwList.length) return null;
  const active = st.cws[st.activeId] || {};
  const m = mark();
  const upload = !!(m && m.src);
  const pmode = printModeFor(st.activeId);
  const colors = colorsFor(st.activeId);
  const applied = !!st.baseline;
  const srcId = st.baseline ? st.baseline.fromId : st.sourceId;
  const done = !hasMark ? 0 : cwList.filter((c) => { const x = st.cws[c.slug]; if (!x || !x.placement) return false; if (c.slug === srcId) return true; return !!x.colors || !!x.touched || x.visible === false; }).length;
  const pl = active.placement || AWB_PL;
  const activeCw = cwList.find((c) => c.slug === st.activeId) || cwList[0];
  const imgFor = (c, view) => "assets/products/" + (c[view] || c.front || c.back || "") + ".png";
  const activeStatus = (active.colors || active.printMode === "one") ? "Custom logo color" : active.copied ? "Placement copied" : "";
  const layerRows = (!hasMark || (upload && pmode === "full")) ? [] : (m.layers || []);
  const applyAll = () => {
    const srcPl = { ...(active.placement || AWB_PL) };
    const fromName = activeCw.name;
    setSt((s) => {
      const cws = { ...s.cws };
      cwList.forEach((c) => { cws[c.slug] = { ...cws[c.slug], placement: { ...srcPl }, copied: c.slug !== s.activeId, touched: false }; });
      return { ...s, cws, applyModal: false, baseline: { fromId: s.activeId, fromName, pl: srcPl } };
    });
    flash("Placement applied to all " + cwList.length + " color ways");
  };
  const pushAll = () => {
    const srcPl = { ...active.placement };
    setSt((s) => {
      const cws = { ...s.cws };
      cwList.forEach((c) => { cws[c.slug] = { ...cws[c.slug], placement: { ...srcPl }, touched: c.slug === s.activeId ? false : cws[c.slug].touched }; });
      return { ...s, cws, baseline: { ...s.baseline, pl: srcPl } };
    });
    flash("Placement pushed to all color ways");
  };
  return (
    <div data-screen-label="Artwork Builder" style={{ position: "fixed", inset: 0, zIndex: 700, background: "#f4f4f5", display: "flex", flexDirection: "column", color: "#09090b", userSelect: "none" }}>
      <style>{".awb-range{-webkit-appearance:none;appearance:none;height:2px;background:#d4d4d8;border-radius:2px;outline:none;width:100%}.awb-range::-webkit-slider-thumb{-webkit-appearance:none;width:14px;height:14px;border-radius:50%;background:#09090b;cursor:pointer}"}</style>
      <div style={{ height: 56, flexShrink: 0, background: "#fff", borderBottom: "1px solid #e4e4e7", display: "flex", alignItems: "center", justifyContent: "space-between", padding: "0 18px", gap: 20 }}>
        <div style={{ display: "flex", alignItems: "center", gap: 14, minWidth: 0 }}>
          <button onClick={onClose} style={{ display: "inline-flex", alignItems: "center", gap: 7, padding: "7px 12px", border: "1px solid #d4d4d8", borderRadius: 6, cursor: "pointer", fontSize: 12, fontWeight: 700, whiteSpace: "nowrap", background: "#fff", fontFamily: "inherit", color: "#09090b" }}>
            <IconChevronLeft size={13} /> {backLabel || "Product setup"}
          </button>
          <div style={{ minWidth: 0 }}>
            <div style={{ fontSize: 13, fontWeight: 700, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{name} · {styleNo}</div>
            <div style={{ fontSize: 11, color: "#71717a", whiteSpace: "nowrap" }}>Team Artwork builder · {sizeCount != null ? sizeCount + " of " + sizeTotal + " sizes" : cwList.length + " color way" + (cwList.length === 1 ? "" : "s")}</div>
          </div>
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 10, flexShrink: 0 }}>
          <div style={{ width: 104, height: 4, borderRadius: 999, background: "#e4e4e7", overflow: "hidden" }}>
            <div style={{ height: "100%", background: "#09090b", borderRadius: 999, width: Math.round((done / cwList.length) * 100) + "%", transition: "width 160ms cubic-bezier(0.2,0,0,1)" }} />
          </div>
          <span style={{ fontSize: 12, color: "#52525b", whiteSpace: "nowrap" }}>{done} of {cwList.length} color ways customized</span>
          {pickedStores && pickedStores.length > 0 && (
            <span title={pickedStores.map((s) => s.storeTitle || s.teamName).join(", ")} style={{ display: "inline-flex", alignItems: "center", gap: 7, background: "#f4f4f5", border: "1px solid #e4e4e7", borderRadius: 999, padding: "4px 12px 4px 5px", marginLeft: 6 }}>
              <span style={{ display: "inline-flex" }}>
                {pickedStores.slice(0, 3).map((s, i) => (
                  <span key={s.id} style={{ width: 20, height: 20, borderRadius: 6, background: (s.teamColors && s.teamColors[0]) || "#09090b", color: "#fff", display: "inline-flex", alignItems: "center", justifyContent: "center", fontSize: 8, fontWeight: 800, border: "1.5px solid #fff", marginLeft: i ? -6 : 0 }}>{String(s.storeTitle || s.teamName || "S").slice(0, 2).toUpperCase()}</span>
                ))}
              </span>
              <span style={{ fontSize: 11.5, fontWeight: 700, color: "#3f3f46", whiteSpace: "nowrap", maxWidth: 200, overflow: "hidden", textOverflow: "ellipsis" }}>
                {"Adding to " + (pickedStores.length === 1 ? (pickedStores[0].storeTitle || pickedStores[0].teamName) : pickedStores.length + " stores")}
              </span>
            </span>
          )}
          {actionLabel && (
            <button onClick={onAction} style={{ marginLeft: 6, background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" }}>{actionLabel}</button>
          )}
          {onSaveChanges && (
            <button onClick={onSaveChanges} style={{ marginLeft: 6, background: "#0e0e10", color: "#fff", border: 0, borderRadius: 6, padding: "9px 16px", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" }}>Save Changes</button>
          )}
        </div>
      </div>
      <div style={{ flex: 1, minHeight: 0, display: "flex" }}>
        <div style={{ width: 196, flexShrink: 0, background: "#fff", borderRight: "1px solid #e4e4e7", display: "flex", flexDirection: "column", minHeight: 0 }}>
          <div style={{ padding: "14px 14px 10px", display: "flex", alignItems: "baseline", justifyContent: "space-between" }}>
            <span style={AWB_CAP}>Color ways</span>
            <span style={{ fontSize: 11, color: "#a1a1aa" }}>{cwList.length} offered</span>
          </div>
          <div style={{ flex: 1, minHeight: 0, overflowY: "auto", padding: "0 12px 16px", display: "flex", flexDirection: "column", gap: 8 }}>
            {cwList.map((c) => {
              const x = st.cws[c.slug] || {};
              const on = c.slug === st.activeId;
              const tpl = x.placement || AWB_PL;
              let badge = "", bStyle = null;
              if (x.colors || x.printMode === "one") { badge = "Custom logo color"; bStyle = { color: "#52525b", background: "#f4f4f5" }; }
              else if (x.copied) { badge = "Placement copied"; bStyle = { color: "#166534", background: "#dcfce7" }; }
              else if (c.slug === srcId) { badge = "Source placement"; bStyle = { color: "#71717a", background: "#f4f4f5" }; }
              return (
                <div key={c.slug} onClick={() => patch({ activeId: c.slug })} style={{ display: "flex", alignItems: "center", gap: 10, padding: 7, border: "1px solid " + (on ? "#09090b" : "#e4e4e7"), borderRadius: 9, cursor: "pointer", background: on ? "#fafafa" : "#fff" }}>
                  <div style={{ position: "relative", width: 44, height: 48, borderRadius: 6, background: "#fafafa", flexShrink: 0, overflow: "hidden" }}>
                    <span style={{ position: "absolute", inset: 0, background: "url(" + imgFor(c, st.view) + ") center/contain no-repeat", display: "block" }} />
                    {hasMark && !!x.placement && x.visible !== false && st.view === "front" && (
                      <span style={{ position: "absolute", left: tpl.cx + "%", top: tpl.cy + "%", width: tpl.w + "%", transform: "translate(-50%,-50%) rotate(" + tpl.rot + "deg)", display: "block" }}>{awbArt(mark(), colorsFor(c.slug), printModeFor(c.slug), store)}</span>
                    )}
                  </div>
                  <div style={{ flex: 1, minWidth: 0, display: "flex", flexDirection: "column", gap: 3 }}>
                    <span style={{ fontSize: 12, fontWeight: 700, whiteSpace: "nowrap", overflow: "hidden", textOverflow: "ellipsis" }}>{c.name}</span>
                    {badge && <span style={{ fontSize: 10, borderRadius: 999, padding: "2px 7px", alignSelf: "flex-start", whiteSpace: "nowrap", ...bStyle }}>{badge}</span>}
                  </div>
                </div>
              );
            })}
          </div>
        </div>
        <div style={{ flex: 1, minWidth: 0, display: "flex", flexDirection: "column", minHeight: 0 }}>
          <div style={{ flexShrink: 0, display: "flex", alignItems: "center", justifyContent: "space-between", gap: 16, padding: "14px 22px 0" }}>
            <div style={{ display: "flex", alignItems: "center", gap: 10, minWidth: 0 }}>
              <span style={{ fontSize: 13, fontWeight: 700, whiteSpace: "nowrap" }}>{activeCw.name}</span>
              {activeStatus && <span style={{ fontSize: 11, borderRadius: 999, padding: "3px 9px", whiteSpace: "nowrap", ...(activeStatus === "Placement copied" ? { color: "#166534", background: "#dcfce7" } : { color: "#52525b", background: "#f4f4f5" }) }}>{activeStatus}</span>}
            </div>
            <div style={{ display: "flex", alignItems: "center", gap: 6, background: "#f4f4f5", borderRadius: 8, padding: 3 }}>
              {["front", "back"].map((v) => (
                <button key={v} onClick={() => patch({ view: v })} style={{ padding: "6px 16px", border: 0, borderRadius: 6, fontSize: 12, fontWeight: 700, cursor: "pointer", whiteSpace: "nowrap", fontFamily: "inherit", background: st.view === v ? "#fff" : "transparent", color: st.view === v ? "#09090b" : "#71717a", boxShadow: st.view === v ? "0 1px 2px rgba(9,9,11,.12)" : "none" }}>{v === "front" ? "Front" : "Back"}</button>
              ))}
            </div>
          </div>
          <div style={{ flex: 1, minHeight: 0, display: "flex", alignItems: "center", justifyContent: "center", padding: "14px 22px 22px" }}>
            <div ref={canvasRef} style={{ position: "relative", aspectRatio: "100/110", height: "100%", maxWidth: "100%", background: "#fff", border: "1px solid #e4e4e7", borderRadius: 12, overflow: "hidden" }}>
              <span style={{ position: "absolute", inset: 0, background: "url(" + imgFor(activeCw, st.view) + ") center/contain no-repeat", display: "block" }} />
              {st.view === "front" && (
                <div style={{ position: "absolute", left: AWB_AREA.x + "%", top: AWB_AREA.y + "%", width: AWB_AREA.w + "%", height: AWB_AREA.h + "%", border: "1.5px dashed #ef4444", borderRadius: 3, background: "rgba(239,68,68,.05)" }}>
                  <span style={{ position: "absolute", top: -9, left: 10, background: "#ef4444", color: "#fff", fontSize: 9, fontWeight: 800, letterSpacing: ".6px", textTransform: "uppercase", padding: "2px 6px", borderRadius: 3, whiteSpace: "nowrap" }}>Printable area</span>
                  <span style={{ position: "absolute", bottom: -9, right: 10, background: "#fff", border: "1px solid #e4e4e7", color: "#71717a", fontSize: 9, padding: "2px 6px", borderRadius: 3, whiteSpace: "nowrap" }}>{"12\u2033 \u00d7 8\u2033"}</span>
                </div>
              )}
              {hasMark && st.view === "front" && !!active.placement && active.visible !== false && (
                <div onPointerDown={(e) => beginDrag(e, "move")} style={{ position: "absolute", left: pl.cx + "%", top: pl.cy + "%", width: pl.w + "%", transform: "translate(-50%,-50%) rotate(" + pl.rot + "deg)", cursor: "grab", outline: "1px solid rgba(9,9,11,.35)", outlineOffset: 6 }}>
                  <span style={{ display: "block", pointerEvents: "none" }}>{awbArt(m, colors, pmode, store)}</span>
                  <span onPointerDown={(e) => beginDrag(e, "resize")} style={{ position: "absolute", right: -11, bottom: -11, width: 11, height: 11, borderRadius: 2, background: "#fff", border: "1.5px solid #09090b", cursor: "nwse-resize" }} />
                </div>
              )}
              {st.view === "back" && (
                <span style={{ position: "absolute", left: "50%", bottom: 18, transform: "translateX(-50%)", background: "#09090b", color: "#fff", fontSize: 11, padding: "6px 12px", borderRadius: 999, whiteSpace: "nowrap" }}>Front center chest is the only print location in this store</span>
              )}
            </div>
          </div>
        </div>
        <div style={{ width: 312, flexShrink: 0, background: "#fff", borderLeft: "1px solid #e4e4e7", display: "flex", flexDirection: "column", minHeight: 0 }}>
          <div style={{ flex: 1, minHeight: 0, overflowY: "auto" }}>
            <div style={{ padding: "16px 16px 14px", borderBottom: "1px solid #e4e4e7" }}>
              <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between", marginBottom: 12 }}>
                <span style={AWB_CAP}>Logo</span>
                <button onClick={() => setCwP(st.activeId, { visible: active.visible === false })} style={{ display: "inline-flex", alignItems: "center", gap: 7, fontSize: 11, color: "#71717a", cursor: "pointer", background: "none", border: 0, padding: 0, fontFamily: "inherit" }}>
                  <span>{active.visible !== false ? "Visible" : "Hidden"}</span>
                  <span style={{ width: 30, height: 17, borderRadius: 999, background: active.visible !== false ? "#09090b" : "#d4d4d8", display: "inline-flex", alignItems: "center", padding: 2, transition: "background 140ms", boxSizing: "border-box" }}>
                    <span style={{ width: 13, height: 13, borderRadius: 999, background: "#fff", transform: "translateX(" + (active.visible !== false ? "13px" : "0") + ")", transition: "transform 140ms cubic-bezier(0.2,0,0,1)" }} />
                  </span>
                </button>
              </div>
              <div style={{ display: allMarks.length ? "grid" : "none", gridTemplateColumns: "repeat(3, minmax(0,1fr))", gap: 8, marginBottom: 10 }}>
                {allMarks.map((mk) => {
                  const cols = {};
                  (mk.layers || []).forEach((l) => { cols[l.id] = l.def; });
                  return (
                    <div key={mk.id} onClick={() => patch({ markId: mk.id })} title={mk.label} style={{ height: 56, border: "1px solid " + (mk.id === st.markId ? "#09090b" : "#e4e4e7"), borderRadius: 8, background: "#27272a", display: "flex", alignItems: "center", justifyContent: "center", padding: 8, cursor: "pointer", overflow: "hidden", boxSizing: "border-box" }}>
                      <span style={{ display: "block", width: "100%", maxHeight: "100%" }}>{awbArt(mk, cols, mk.src ? "full" : "vector", store)}</span>
                    </div>
                  );
                })}
              </div>
              <label style={{ display: "flex", alignItems: "center", justifyContent: "center", gap: 8, height: 38, border: "1px dashed #d4d4d8", borderRadius: 8, fontSize: 12, fontWeight: 700, cursor: "pointer", background: "#fafafa" }}>
                <span>Upload logo</span>
                <span style={{ fontSize: 11, color: "#71717a", fontWeight: 400 }}>PNG, SVG</span>
                <input type="file" accept="image/png,image/svg+xml,image/jpeg,image/webp" multiple onChange={handleFiles} style={{ display: "none" }} />
              </label>
              <div style={{ marginTop: 10, fontSize: 11, color: "#71717a", lineHeight: 1.5 }}>
                {!hasMark ? "No logo in the print area yet. Upload artwork to place it." : upload ? "Uploaded " + (m.isSvg ? "vector" : "artwork") + " · " + m.label + " · one-color print available per color way" : "Vector · " + m.paths + " paths · " + m.layers.length + " editable fills"}
              </div>
              {upload && (
                <div style={{ marginTop: 12, display: "flex", gap: 6, background: "#f4f4f5", borderRadius: 8, padding: 3 }}>
                  {[["full", "Full color"], ["one", "One-color print"]].map(([k, l]) => (
                    <button key={k} onClick={() => setCwP(st.activeId, { printMode: k })} style={{ flex: 1, padding: "7px 10px", border: 0, borderRadius: 6, fontSize: 12, fontWeight: 700, textAlign: "center", cursor: "pointer", fontFamily: "inherit", background: pmode === k ? "#fff" : "transparent", color: pmode === k ? "#09090b" : "#71717a", boxShadow: pmode === k ? "0 1px 2px rgba(9,9,11,.12)" : "none" }}>{l}</button>
                  ))}
                </div>
              )}
            </div>
            <div style={{ padding: 16, borderBottom: "1px solid #e4e4e7", display: "flex", flexDirection: "column", gap: 14 }}>
              <span style={AWB_CAP}>Placement</span>
              <div style={{ display: "flex", flexDirection: "column", gap: 6 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12 }}><span style={{ color: "#52525b" }}>Print location</span><span style={{ fontWeight: 700 }}>Front center chest</span></div>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12 }}><span style={{ color: "#52525b" }}>Print area</span><span style={{ fontWeight: 700 }}>{"12\u2033 \u00d7 8\u2033"}</span></div>
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 7 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12 }}><span style={{ color: "#52525b" }}>Size</span><span style={{ fontFamily: "ui-monospace, Menlo, monospace", fontSize: 11, color: "#71717a" }}>{(pl.w / 42 * 12).toFixed(1) + "\u2033 wide"}</span></div>
                <input className="awb-range" type="range" min={12} max={40} step={1} value={Math.round(pl.w)} onChange={(e) => numeric("w", Number(e.target.value))} />
              </div>
              <div style={{ display: "flex", flexDirection: "column", gap: 7 }}>
                <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12 }}><span style={{ color: "#52525b" }}>Rotation</span><span style={{ fontFamily: "ui-monospace, Menlo, monospace", fontSize: 11, color: "#71717a" }}>{Math.round(pl.rot) + "\u00b0"}</span></div>
                <input className="awb-range" type="range" min={-30} max={30} step={1} value={Math.round(pl.rot)} onChange={(e) => numeric("rot", Number(e.target.value))} />
              </div>
              <div style={{ fontSize: 11, color: "#71717a", lineHeight: 1.5 }}>{applied ? "Drag to reposition. Changes here affect this color way only." : "Drag the logo on the preview to reposition. It stays inside the printable area."}</div>
              {applied && !!active.touched && (
                <div style={{ display: "flex", alignItems: "center", gap: 12 }}>
                  <button onClick={() => { setCwP(st.activeId, { placement: { ...st.baseline.pl }, touched: false }); flash("Copied placement restored"); }} style={awbLink}>Restore copied placement</button>
                  <button onClick={pushAll} style={awbLink}>Push to all color ways</button>
                </div>
              )}
            </div>
            <div style={{ padding: 16, borderBottom: "1px solid #e4e4e7", display: hasMark ? "flex" : "none", flexDirection: "column", gap: 14 }}>
              <div style={{ display: "flex", alignItems: "center", justifyContent: "space-between" }}>
                <span style={AWB_CAP}>Logo color</span>
                <span style={{ fontSize: 11, color: "#a1a1aa" }}>This color way only</span>
              </div>
              {layerRows.map((l) => (
                <div key={l.id} style={{ display: "flex", flexDirection: "column", gap: 8 }}>
                  <div style={{ display: "flex", justifyContent: "space-between", fontSize: 12 }}><span style={{ color: "#52525b" }}>{l.name}</span><span style={{ fontFamily: "ui-monospace, Menlo, monospace", fontSize: 11, color: "#a1a1aa" }}>{(colors[l.id] || l.def).toUpperCase()}</span></div>
                  <div style={{ display: "flex", gap: 7, flexWrap: "wrap" }}>
                    {AWB_SWATCHES.map((hex) => {
                      const on = (colors[l.id] || "").toLowerCase() === hex.toLowerCase();
                      return <span key={hex} onClick={() => { const next = { ...colorsFor(st.activeId) }; next[l.id] = hex; setCwP(st.activeId, { colors: next }); flash("Custom logo color · " + activeCw.name + " only"); }} style={{ width: 24, height: 24, borderRadius: 6, background: hex, border: "1px solid " + (on ? "#09090b" : "#d4d4d8"), boxShadow: on ? "0 0 0 2px #fff inset" : "none", cursor: "pointer", display: "block", boxSizing: "border-box" }} />;
                    })}
                  </div>
                </div>
              ))}
              {upload && (
                <div style={{ fontSize: 11, color: "#52525b", background: "#fafafa", border: "1px solid #e4e4e7", borderRadius: 8, padding: "9px 10px", lineHeight: 1.5 }}>
                  {pmode === "full" ? "Uploaded artwork prints as supplied. Switch to one-color print to set a single ink color per color way." : "One-color print uses the artwork silhouette, so it needs transparent PNG or SVG art."}
                </div>
              )}
              {!!active.colors && <button onClick={() => setCwP(st.activeId, { colors: null })} style={{ ...awbLink, alignSelf: "flex-start" }}>Reset to store colors</button>}
            </div>
          </div>
          <div style={{ flexShrink: 0, borderTop: "1px solid #e4e4e7", padding: "14px 16px", display: "flex", flexDirection: "column", gap: 9, background: "#fff" }}>
            {!applied && hasMark && !!active.placement && (
              <button onClick={() => patch({ applyModal: true })} style={{ width: "100%", height: 40, display: "flex", alignItems: "center", justifyContent: "center", textAlign: "center", background: "#09090b", color: "#fff", border: 0, borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", padding: "0 12px", lineHeight: 1.25, fontFamily: "inherit" }}>Apply placement to all color ways</button>
            )}
            {applied && (
              <div style={{ display: "flex", alignItems: "flex-start", gap: 8, background: "#fafafa", border: "1px solid #e4e4e7", borderRadius: 8, padding: "10px 11px" }}>
                <span style={{ width: 6, height: 6, borderRadius: 999, background: "#16a34a", marginTop: 5, flexShrink: 0 }} />
                <span style={{ fontSize: 11, color: "#52525b", lineHeight: 1.5 }}>{"Placement copied from " + st.baseline.fromName + " to " + Math.max(0, cwList.length - 1) + " color ways. Logo color stays per color way."}</span>
              </div>
            )}
            <span style={{ fontSize: 11, color: "#a1a1aa", textAlign: "center" }}>{applied ? "Open any color way to fine-tune its logo color." : "Set the placement once, then tune color per garment."}</span>
          </div>
        </div>
      </div>
      {st.applyModal && (
        <div onClick={() => patch({ applyModal: false })} style={{ position: "fixed", inset: 0, background: "rgba(9,9,11,.45)", display: "flex", alignItems: "center", justifyContent: "center", zIndex: 710 }}>
          <div onClick={(e) => e.stopPropagation()} style={{ width: 420, background: "#fff", borderRadius: 12, boxShadow: "0 24px 48px rgba(9,9,11,.22)", padding: 22 }}>
            <div style={{ fontSize: 16, fontWeight: 700, marginBottom: 12 }}>Apply this placement to all color ways?</div>
            <div style={{ fontSize: 13, color: "#52525b", lineHeight: 1.6, marginBottom: 6 }}>{"Copy the logo\u2019s position, size, rotation, print location, and print area."}</div>
            <div style={{ fontSize: 13, color: "#52525b", lineHeight: 1.6, marginBottom: 20 }}>Logo color will remain editable separately for each color way.</div>
            <div style={{ display: "flex", justifyContent: "flex-end", gap: 10 }}>
              <button onClick={() => patch({ applyModal: false })} style={{ padding: "10px 16px", border: "1px solid #d4d4d8", borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", background: "#fff", fontFamily: "inherit", color: "#09090b" }}>Cancel</button>
              <button onClick={applyAll} style={{ padding: "10px 16px", border: 0, borderRadius: 6, fontSize: 13, fontWeight: 700, cursor: "pointer", background: "#09090b", color: "#fff", fontFamily: "inherit" }}>Apply to all color ways</button>
            </div>
          </div>
        </div>
      )}
      {st.toast && <div style={{ position: "fixed", left: "50%", bottom: 26, transform: "translateX(-50%)", background: "#09090b", color: "#fff", fontSize: 12, padding: "10px 16px", borderRadius: 999, zIndex: 720, whiteSpace: "nowrap" }}>{st.toast}</div>}
    </div>
  );
}

Object.assign(window, { ProStockVaultPage, ProStockDeployModal, StoreVaultPage, ProStockItemCard, ProductsCatalogSections, ProductDetailPage, ProductCatalogCard, ArtworkBuilderPage });

/* ===== ASSET 33 : Store Customization & Design ===== */
// Settings Epic I + Wizard Epic H. Announcement Message (rich text, one store-
// level field written from the wizard and Settings alike) and Store Theme Color
// (team default → Prolook picker override, buttons & accents only). Both are
// EXEMPT from post-launch locking (Epic B) — no lock icons, ever.

const ANN_PLACEHOLDER = "e.g. Welcome to the 2026 season store! Orders close Friday, May 22 — jerseys arrive about 3 weeks after the store closes. Sizing runs small; most players go up one size. All items are custom-made to order, so no returns or exchanges on personalized gear. Everything ships free to the coach for handout at practice.";

function annPlain(html) { return String(html || "").replace(/<[^>]*>/g, " ").replace(/&nbsp;/g, " ").replace(/\s+/g, " ").trim(); }
function annIsEmpty(html) { return annPlain(html).length === 0; }
// Sanitizing renderer — rep-authored markup renders formatted; script/embed/
// handler content is stripped, not escaped-and-displayed.
function annSanitize(html) {
  return String(html || "")
    .replace(/<\s*(script|style|iframe|object|embed|link|meta)[^>]*>[\s\S]*?<\s*\/\s*\1\s*>/gi, "")
    .replace(/<\s*(script|style|iframe|object|embed|link|meta)[^>]*\/?>/gi, "")
    .replace(/\son\w+\s*=\s*("[^"]*"|'[^']*'|[^\s>]+)/gi, "")
    .replace(/javascript:/gi, "");
}

const CzIconMegaphone = ({ size = 20, style }) => (
  <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.9" strokeLinecap="round" strokeLinejoin="round" style={style}>
    <path d="M3 10v4a1 1 0 0 0 1 1h2l4 4.4V4.6L6 9H4a1 1 0 0 0-1 1z" /><path d="M13.5 8.6a4.6 4.6 0 0 1 0 6.8" /><path d="M16.8 5.8a9 9 0 0 1 0 12.4" />
  </svg>
);
const CzIconSearch = ({ size = 15, style }) => (
  <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" style={style}><circle cx="11" cy="11" r="7" /><path d="m21 21-4.3-4.3" /></svg>
);
const CzIconLink = ({ size = 13, style }) => (
  <svg width={size} height={size} viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round" style={style}><path d="M10 13a5 5 0 0 0 7.5.5l3-3a5 5 0 0 0-7-7l-1.7 1.7" /><path d="M14 11a5 5 0 0 0-7.5-.5l-3 3a5 5 0 0 0 7 7l1.7-1.7" /></svg>
);

const czOpenChip = { fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#8a5f10", background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 999, padding: "3px 8px", whiteSpace: "nowrap", flexShrink: 0 };

function AnnouncementRichText({ html, style }) {
  return (
    <div>
      <style>{".cz-rich p{margin:0 0 8px}.cz-rich p:last-child{margin-bottom:0}.cz-rich div{margin:0 0 6px}.cz-rich div:last-child{margin-bottom:0}.cz-rich ul,.cz-rich ol{margin:6px 0 8px;padding-left:22px}.cz-rich li{margin:3px 0}"}</style>
      <div className="cz-rich" style={{ fontSize: 13.5, lineHeight: 1.6, color: "#26262b", ...style }} dangerouslySetInnerHTML={{ __html: annSanitize(html) }} />
    </div>
  );
}

// Rich-text input (reference: Announcement message box). Bold / italic / lists
// are safe marks; LINKS are an open spec question — surfaced, not wired.
function AnnouncementEditor({ value, onChange, accent = "#e1251b", minHeight = 128 }) {
  const ref = React.useRef(null);
  const [focus, setFocus] = React.useState(false);
  React.useEffect(() => {
    const el = ref.current;
    if (el && !focus && el.innerHTML !== (value || "")) el.innerHTML = value || "";
  }, [value, focus]);
  const input = () => { if (ref.current) onChange(ref.current.innerHTML); };
  const exec = (cmd) => { if (ref.current) ref.current.focus(); document.execCommand(cmd); input(); };
  const empty = annIsEmpty(value);
  const chars = annPlain(value).length;
  const tbtn = (label, cmd, title, extra) => (
    <button type="button" title={title} onMouseDown={(e) => { e.preventDefault(); exec(cmd); }} style={{
      minWidth: 30, height: 28, padding: "0 8px", border: 0, borderRadius: 5, background: "transparent",
      color: "#3f3f46", fontSize: 12.5, fontWeight: 700, cursor: "pointer", fontFamily: "inherit",
      display: "inline-flex", alignItems: "center", justifyContent: "center", gap: 5, ...extra,
    }}
      onMouseEnter={(e) => e.currentTarget.style.background = "#ececf0"}
      onMouseLeave={(e) => e.currentTarget.style.background = "transparent"}>{label}</button>
  );
  return (
    <div>
      <div style={{ border: `1.5px solid ${focus ? "#0e0e10" : "#d4d4d8"}`, borderRadius: 8, overflow: "hidden", transition: "border-color .14s", background: "#fff" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 2, padding: "5px 8px", background: "#fafafb", borderBottom: "1px solid #ececf0" }}>
          {tbtn(<span style={{ fontWeight: 800 }}>B</span>, "bold", "Bold")}
          {tbtn(<span style={{ fontStyle: "italic", fontFamily: "Georgia, serif", fontWeight: 600 }}>I</span>, "italic", "Italic")}
          <span style={{ width: 1, height: 18, background: "#e5e5e9", margin: "0 4px" }} />
          {tbtn("• List", "insertUnorderedList", "Bulleted list")}
          {tbtn("1. List", "insertOrderedList", "Numbered list")}
          <span style={{ marginLeft: "auto", display: "inline-flex", alignItems: "center", gap: 6 }}>
            <button type="button" title="Links are an open spec decision — not enabled yet" style={{ height: 28, padding: "0 8px", border: 0, borderRadius: 5, background: "transparent", color: "#b0b0b6", cursor: "not-allowed", display: "inline-flex", alignItems: "center", gap: 5, fontSize: 12, fontWeight: 700, fontFamily: "inherit" }}>
              <CzIconLink /> Link
            </button>
          </span>
        </div>
        <div style={{ position: "relative" }}>
          {empty && !focus && (
            <div style={{ position: "absolute", inset: 0, padding: "13px 16px", fontSize: 13.5, lineHeight: 1.6, color: "#b0b0b6", pointerEvents: "none" }}>{ANN_PLACEHOLDER}</div>
          )}
          <style>{".cz-edit p{margin:0 0 8px}.cz-edit ul,.cz-edit ol{margin:6px 0 8px;padding-left:22px}.cz-edit li{margin:3px 0}.cz-edit:focus{outline:none}"}</style>
          <div ref={ref} className="cz-edit" contentEditable suppressContentEditableWarning
            onInput={input} onFocus={() => setFocus(true)} onBlur={() => { setFocus(false); input(); }}
            style={{ minHeight, padding: "13px 16px", fontSize: 13.5, lineHeight: 1.6, color: "#26262b", fontFamily: "inherit" }} />
        </div>
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 8, marginTop: 8, flexWrap: "wrap" }}>
        <span style={{ fontSize: 11.5, color: "#9a9aa1", lineHeight: 1.5 }}>{chars} characters</span>
      </div>
    </div>
  );
}

// ─────────── Store Theme Color ───────────
function teamDefaultThemeColor(store) {
  return (store && (store.primaryColor || (store.teamColors || [])[0])) || "#e1251b";
}
function resolvedThemeColor(store) {
  if (store && store.storeThemeSource === "custom" && store.storeThemeColor) return { hex: store.storeThemeColor, source: "custom" };
  return { hex: teamDefaultThemeColor(store), source: "team" };
}
function prolookColorName(hex) {
  const m = PROLOOK_COLORS.find((c) => c.hex.toLowerCase() === String(hex || "").toLowerCase());
  return m ? m.name : null;
}

// Prolook color picker (reference component), single-select. Includes the
// non-blocking contrast warning — a PROPOSAL, spec open question #1.
function SelectStoreColorModal({ open, store, onClose, onSave }) {
  const teamHex = teamDefaultThemeColor(store);
  const [hex, setHex] = React.useState(teamHex);
  const [source, setSource] = React.useState("team");
  React.useEffect(() => { if (open) { const c = resolvedThemeColor(store); setHex(c.hex); setSource(c.source); } }, [open]);
  if (!open) return null;
  const lightPick = source === "custom" && readableInk(hex) === "#0e0e10";
  return (
    <Modal open={open} onClose={onClose} width={780}>
      <div style={{ padding: "26px 30px 0", maxHeight: "calc(100vh - 200px)", overflowY: "auto" }}>
        <div style={{ fontSize: 16, fontWeight: 800, color: "#0e0e10" }}>Choose Your Store Color</div>
        <div style={{ fontSize: 13, color: "#9a9aa1", marginTop: 4, lineHeight: 1.5 }}>Applied to storefront buttons and accents only — the page background, header, navigation, and product photos don't change.</div>
        <button type="button" onClick={() => { setHex(teamHex); setSource("team"); }} style={{
          marginTop: 18, width: "100%", textAlign: "left", display: "flex", alignItems: "center", gap: 14,
          border: `1.5px solid ${source === "team" ? "#0e0e10" : "#e5e5e9"}`, background: source === "team" ? "#fafafb" : "#fff",
          borderRadius: 8, padding: "12px 14px", cursor: "pointer", fontFamily: "inherit",
        }}>
          <span style={{ width: 34, height: 34, borderRadius: 7, background: teamHex, border: "1px solid rgba(0,0,0,.1)", display: "inline-flex", alignItems: "center", justifyContent: "center", color: readableInk(teamHex), flexShrink: 0 }}>
            {source === "team" && <IconCheck size={16} style={{ strokeWidth: 3 }} />}
          </span>
          <span style={{ flex: 1, minWidth: 0 }}>
            <span style={{ display: "block", fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>Team default{prolookColorName(teamHex) ? ` — ${prolookColorName(teamHex)}` : ""}</span>
            <span style={{ display: "block", fontSize: 12, color: "#9a9aa1", marginTop: 2 }}>Follows the connected team's primary color — reconnecting the team keeps it in sync.</span>
          </span>
        </button>
        <div style={{ fontSize: 11, fontWeight: 800, letterSpacing: ".08em", color: "#9a9aa1", textTransform: "uppercase", margin: "18px 0 10px" }}>Or pick a custom color</div>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(11, 1fr)", gap: 10 }}>
          {PROLOOK_COLORS.map((c) => {
            const on = source === "custom" && hex.toLowerCase() === c.hex.toLowerCase();
            return (
              <button key={c.code} type="button" title={c.name} onClick={() => { setHex(c.hex); setSource("custom"); }} style={{
                aspectRatio: "1 / 1", borderRadius: 6, background: c.hex,
                border: c.hex.toLowerCase() === "#ffffff" ? "1px solid #d4d4d8" : "1px solid rgba(0,0,0,.06)",
                color: readableInk(c.hex), display: "inline-flex", alignItems: "center", justifyContent: "center",
                fontSize: 12, fontWeight: 800, letterSpacing: ".02em", cursor: "pointer",
                outline: on ? "2.5px solid #0e0e10" : "none", outlineOffset: 2,
              }}>
                {on ? <IconCheck size={16} style={{ strokeWidth: 3 }} /> : c.code}
              </button>
            );
          })}
        </div>
        {lightPick && (
          <div style={{ marginTop: 16, display: "flex", alignItems: "flex-start", gap: 10, background: "#fdf6e7", border: "1px solid #efdcae", borderRadius: 8, padding: "12px 14px" }}>
            <span style={czOpenChip}>PROPOSAL</span>
            <span style={{ fontSize: 12.5, color: "#6b5a1e", lineHeight: 1.55 }}>
              Light colors can leave white button text hard to read on the storefront. Whether to warn or block on poor contrast is an <b>open spec question</b> — this warning is non-blocking and doesn't stop you saving.
            </span>
          </div>
        )}
      </div>
      <div style={{ display: "flex", alignItems: "center", gap: 12, padding: "20px 30px 26px" }}>
        {source === "custom" && (
          <button type="button" onClick={() => { setHex(teamHex); setSource("team"); }} style={{ background: "none", border: 0, padding: 0, fontSize: 13, fontWeight: 700, color: "#6b6b72", cursor: "pointer", fontFamily: "inherit", textDecoration: "underline", textUnderlineOffset: 2 }}>
            Reset to team color
          </button>
        )}
        <span style={{ flex: 1 }} />
        <Button variant="secondary" onClick={onClose}>Cancel</Button>
        <Button variant="primary" onClick={() => onSave(source === "team" ? { storeThemeSource: "team", storeThemeColor: "" } : { storeThemeSource: "custom", storeThemeColor: hex })}>Save Changes</Button>
      </div>
    </Modal>
  );
}

function StoreThemeColorCard({ store, onUpdateStore, onToast }) {
  const [open, setOpen] = React.useState(false);
  const t = resolvedThemeColor(store);
  const pill = t.source === "custom"
    ? { label: "CUSTOM", background: "#0e0e10", color: "#fff" }
    : { label: "TEAM DEFAULT", background: "#f4f4f5", color: "#52525b" };
  return (
    <React.Fragment>
      <DetailCard title="Store Theme Color" onEdit={() => setOpen(true)}>
        <div style={{ display: "flex", alignItems: "flex-start", gap: 16 }}>
          <span style={{ width: 44, height: 44, borderRadius: 9, flexShrink: 0, background: t.hex, border: "1px solid rgba(0,0,0,.1)" }} />
          <div style={{ minWidth: 0, flex: 1 }}>
            <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
              <span style={{ fontSize: 14, fontWeight: 700, color: "#0e0e10" }}>{prolookColorName(t.hex) || t.hex.toUpperCase()}</span>
              <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", borderRadius: 999, padding: "3px 9px", background: pill.background, color: pill.color }}>{pill.label}</span>
            </div>
            <div style={{ fontSize: 12.5, color: "#6b6b72", marginTop: 5, lineHeight: 1.5 }}>
              Applied to storefront <b>buttons and accents only</b> — the page background, header, navigation, and product photos don't change.
            </div>
            <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 5, lineHeight: 1.5 }}>
              {t.source === "custom" ? "Overriding the team color — reset it from the editor anytime." : "Following the connected team's primary color."} Editable anytime, even on a live store.
            </div>
          </div>
        </div>
      </DetailCard>
      <SelectStoreColorModal open={open} store={store} onClose={() => setOpen(false)}
        onSave={(patch) => { onUpdateStore && onUpdateStore(patch); setOpen(false); onToast && onToast(patch.storeThemeSource === "team" ? "Theme color set to team default" : "Store theme color updated"); }} />
    </React.Fragment>
  );
}

// ─────────── Announcement Message (Settings card + shared edit modal) ───────────
function AnnouncementEditModal({ open, title, initial, note, onClose, onSave }) {
  const [draft, setDraft] = React.useState(initial || "");
  React.useEffect(() => { if (open) setDraft(initial || ""); }, [open]);
  return (
    <EditModalShell open={open} onClose={onClose} title={title} width={720} onSave={() => onSave(draft)}>
      <AnnouncementEditor value={draft} onChange={setDraft} minHeight={150} />
      <div style={{ display: "flex", alignItems: "flex-start", gap: 8, fontSize: 12.5, color: "#6b6b72", lineHeight: 1.5, marginTop: 14 }}>
        <IconInfo size={14} style={{ flexShrink: 0, marginTop: 1 }} />
        <span>{note || <React.Fragment>Leave it blank and the storefront shows <b>no banner at all</b>. Changes apply immediately — even on a live store.</React.Fragment>}</span>
      </div>
    </EditModalShell>
  );
}

function AnnouncementMessageCard({ store, onUpdateStore, onToast }) {
  const [open, setOpen] = React.useState(false);
  const empty = annIsEmpty(store.announcement);
  return (
    <React.Fragment>
      <DetailCard title="Announcement Message" onEdit={() => setOpen(true)}>
        {empty ? (
          <div style={{ display: "flex", gap: 14, alignItems: "flex-start" }}>
            <span style={{ width: 40, height: 40, borderRadius: 9, flexShrink: 0, background: "#f4f4f5", color: "#9a9aa1", display: "inline-flex", alignItems: "center", justifyContent: "center" }}>
              <CzIconMegaphone size={19} />
            </span>
            <div style={{ minWidth: 0 }}>
              <div style={{ fontSize: 13.5, fontWeight: 600, color: "#3f3f46" }}>No announcement message</div>
              <div style={{ fontSize: 12.5, color: "#9a9aa1", marginTop: 3, lineHeight: 1.55 }}>
                The storefront shows <b>no banner at all</b> while this is empty — that's the designed behavior, not a missing step. Write one whenever there's something buyers should know: deadlines, sizing, returns, shipping.
              </div>
            </div>
          </div>
        ) : (
          <React.Fragment>
            <AnnouncementRichText html={store.announcement} />
            <div style={{ fontSize: 12, color: "#9a9aa1", marginTop: 12, paddingTop: 12, borderTop: "1px solid #f1f1f3" }}>
              Shown at the top of the storefront to every buyer — content doesn't vary by role. Editable anytime, even on a live store.
            </div>
          </React.Fragment>
        )}
      </DetailCard>
      <AnnouncementEditModal open={open} title="EDIT ANNOUNCEMENT MESSAGE" initial={store.announcement}
        onClose={() => setOpen(false)}
        onSave={(html) => {
          const v = annIsEmpty(html) ? "" : html;
          onUpdateStore && onUpdateStore({ announcement: v });
          setOpen(false);
          onToast && onToast(v ? "Announcement message updated" : "Announcement cleared — the storefront shows no banner");
        }} />
    </React.Fragment>
  );
}

// ─────────── League-only override surface ───────────
// Granularity is DIVISION and STANDALONE TEAM only — teams inside a division
// inherit their division and get no row of their own. No color picker here:
// a division's color is its existing brand color, edited in My Team(s).
function LeagueAnnouncementOverrides({ store, org, onUpdateStore, onToast, onGoMyTeams }) {
  const [q, setQ] = React.useState("");
  const [sel, setSel] = React.useState({});
  const [edit, setEdit] = React.useState(null); // { ids, label, initial }
  const ov = store.annOverrides || {};
  const colorOn = store.divColorApplied || {};
  const groups = (org && org.groups) || [];
  const loose = (org && org.loose) || [];
  const all = [
    ...groups.map((g) => ({ id: g.id, name: g.name, kind: "division", color: g.color || "#9a9aa1", teams: (g.rosters || []).length })),
    ...loose.map((r) => ({ id: r.id, name: r.name, kind: "standalone" })),
  ];
  const rows = all.filter((r) => !q || r.name.toLowerCase().includes(q.toLowerCase()));
  const selIds = all.map((r) => r.id).filter((id) => sel[id]);
  const setOverride = (ids, html) => {
    const next = { ...ov };
    ids.forEach((id) => { if (html == null || annIsEmpty(html)) delete next[id]; else next[id] = html; });
    onUpdateStore && onUpdateStore({ annOverrides: next });
  };
  const storeEmpty = annIsEmpty(store.announcement);
  const smallBtn = { border: "1px solid #d4d4d8", borderRadius: 6, background: "#fff", padding: "6px 12px", fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", whiteSpace: "nowrap" };
  const check = (on, onClick) => (
    <span role="checkbox" aria-checked={!!on} onClick={onClick} style={{
      width: 17, height: 17, borderRadius: 4, flexShrink: 0, marginTop: 2, cursor: "pointer",
      border: `1.5px solid ${on ? "#0e0e10" : "#c9c9cf"}`, background: on ? "#0e0e10" : "#fff",
      display: "inline-flex", alignItems: "center", justifyContent: "center", color: "#fff",
    }}>{on && <IconCheck size={11} style={{ strokeWidth: 3.4 }} />}</span>
  );
  return (
    <div style={{ background: "#fff", border: "1px solid #e4e4e7", borderRadius: 10, overflow: "hidden" }}>
      <div style={{ padding: "18px 20px 14px" }}>
        <div style={{ display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap" }}>
          <span style={{ fontSize: 13, fontWeight: 800, letterSpacing: ".04em", color: "#0e0e10" }}>Division &amp; Team Messages</span>
          <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#6b6b72", background: "#f4f4f5", borderRadius: 999, padding: "3px 8px" }}>LEAGUE STORE ONLY</span>
        </div>
        <div style={{ fontSize: 12.5, color: "#6b6b72", marginTop: 6, lineHeight: 1.55 }}>
          A division or standalone team can carry its own announcement; everything else inherits the store-level message above. Teams <b>inside</b> a division always follow their division — there's no per-team override.
        </div>
        <div style={{ display: "flex", alignItems: "center", gap: 10, marginTop: 14, background: "#fafafb", border: "1px solid #ececf0", borderRadius: 6, padding: "9px 12px" }}>
          <CzIconSearch style={{ color: "#9a9aa1", flexShrink: 0 }} />
          <input value={q} onChange={(e) => setQ(e.target.value)} placeholder="Search divisions and standalone teams" style={{ flex: 1, border: 0, outline: 0, background: "none", fontSize: 13, fontFamily: "inherit", color: "#0e0e10" }} />
        </div>
      </div>
      {selIds.length > 0 && (
        <div style={{ display: "flex", alignItems: "center", gap: 12, flexWrap: "wrap", background: "#0e0e10", color: "#fff", padding: "10px 20px" }}>
          <span style={{ fontSize: 12.5, fontWeight: 700 }}>{selIds.length} selected</span>
          <button type="button" style={{ ...smallBtn, background: "#fff" }} onClick={() => setEdit({ ids: selIds, label: selIds.length + " selected", initial: "" })}>Set message for selected…</button>
          <button type="button" style={{ ...smallBtn, background: "transparent", color: "#fff", borderColor: "rgba(255,255,255,.4)" }} onClick={() => { setOverride(selIds, null); setSel({}); onToast && onToast(selIds.length + " set to inherit the store message"); }}>Revert to store message</button>
          <button type="button" onClick={() => setSel({})} style={{ background: "none", border: 0, color: "rgba(255,255,255,.65)", fontSize: 12, fontWeight: 700, cursor: "pointer", fontFamily: "inherit", marginLeft: "auto" }}>Clear</button>
        </div>
      )}
      {rows.length === 0 && (
        <div style={{ padding: "22px 20px", borderTop: "1px solid #f1f1f3", fontSize: 13, color: "#9a9aa1", textAlign: "center" }}>
          {all.length === 0 ? "No divisions or standalone teams yet — build them in My Team(s)." : "No match for “" + q + "”."}
        </div>
      )}
      {rows.map((r) => {
        const own = !annIsEmpty(ov[r.id]);
        return (
          <div key={r.id} style={{ display: "flex", gap: 12, padding: "14px 20px", borderTop: "1px solid #f1f1f3", alignItems: "flex-start" }}>
            {check(sel[r.id], () => setSel((s) => ({ ...s, [r.id]: !s[r.id] })))}
            <div style={{ flex: 1, minWidth: 0 }}>
              <div style={{ display: "flex", alignItems: "center", gap: 8, flexWrap: "wrap" }}>
                {r.kind === "division" && <span style={{ width: 10, height: 10, borderRadius: 3, background: r.color, flexShrink: 0, border: "1px solid rgba(0,0,0,.08)" }} />}
                <span style={{ fontSize: 13.5, fontWeight: 700, color: "#0e0e10" }}>{r.name}</span>
                <span style={{ fontSize: 11.5, color: "#9a9aa1" }}>{r.kind === "division" ? `Division · ${r.teams} team${r.teams === 1 ? "" : "s"}` : "Standalone team"}</span>
                {own
                  ? <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#fff", background: "#0e0e10", borderRadius: 999, padding: "3px 9px" }}>OWN MESSAGE</span>
                  : <span style={{ fontSize: 9.5, fontWeight: 800, letterSpacing: ".05em", color: "#52525b", background: "#f4f4f5", borderRadius: 999, padding: "3px 9px" }}>INHERITING · STORE MESSAGE</span>}
              </div>
              <div style={{ fontSize: 12.5, color: own ? "#3f3f46" : "#9a9aa1", fontStyle: own ? "normal" : "italic", marginTop: 5, lineHeight: 1.5, overflow: "hidden", textOverflow: "ellipsis", whiteSpace: "nowrap" }}>
                {own ? annPlain(ov[r.id]) : storeEmpty ? "Store message is empty — shoppers resolved here see no banner." : annPlain(store.announcement)}
              </div>
              {r.kind === "division" && (
                <div style={{ marginTop: 10, display: "flex", alignItems: "center", gap: 10, flexWrap: "wrap" }}>
                  <SettingsToggle value={!!colorOn[r.id]} onChange={(v) => { onUpdateStore && onUpdateStore({ divColorApplied: { ...colorOn, [r.id]: v } }); onToast && onToast(v ? r.name + "'s brand color now themes its shoppers' storefront" : r.name + " follows the store theme color"); }} />
                  <span style={{ fontSize: 12.5, color: "#3f3f46", display: "inline-flex", alignItems: "center", gap: 6 }}>
                    Use this division's brand color
                    <span style={{ width: 12, height: 12, borderRadius: 3, background: r.color, display: "inline-block", border: "1px solid rgba(0,0,0,.08)" }} />
                    for its shoppers
                  </span>
                  <button type="button" onClick={onGoMyTeams} style={{ background: "none", border: 0, padding: 0, fontSize: 12, fontWeight: 700, color: "#0e0e10", cursor: "pointer", fontFamily: "inherit", textDecoration: "underline", textUnderlineOffset: 2 }}>
                    Edit color in My Team(s)
                  </button>
                </div>
              )}
            </div>
            <div style={{ display: "flex", gap: 8, flexShrink: 0 }}>
              {own ? (
                <React.Fragment>
                  <button type="button" style={smallBtn} onClick={() => setEdit({ ids: [r.id], label: r.name, initial: ov[r.id] })}>Edit</button>
                  <button type="button" style={{ ...smallBtn, color: "#6b6b72" }} onClick={() => { setOverride([r.id], null); onToast && onToast(r.name + " now inherits the store message"); }}>Revert</button>
                </React.Fragment>
              ) : (
                <button type="button" style={smallBtn} onClick={() => setEdit({ ids: [r.id], label: r.name, initial: store.announcement || "" })}>Override</button>
              )}
            </div>
          </div>
        );
      })}
      <AnnouncementEditModal open={!!edit} title={edit ? "SET MESSAGE — " + edit.label.toUpperCase() : ""} initial={edit ? edit.initial : ""}
        note={edit ? <React.Fragment>Shoppers resolved to <b>{edit.label}</b> see this instead of the store-level message. Teams inside a division inherit it automatically. Saving it blank reverts to the store message.</React.Fragment> : null}
        onClose={() => setEdit(null)}
        onSave={(html) => {
          const ids = edit.ids;
          setOverride(ids, html);
          setEdit(null); setSel({});
          onToast && onToast(annIsEmpty(html) ? "Reverted to the store message" : (ids.length === 1 ? "Message set for " + edit.label : "Message applied to " + ids.length + " selections"));
        }} />
    </div>
  );
}

// Section shell — the seventh Settings section.
function StoreCustomizationSection({ store, org, onUpdateStore, onToast, onGoMyTeams }) {
  return (
    <React.Fragment>
      <h3 style={{ margin: "0 0 4px", fontSize: 17, fontWeight: 800, fontFamily: "Archivo, sans-serif", color: "#09090b" }}>Store Customization &amp; Design</h3>
      <p style={{ fontSize: 13, color: "#71717a", margin: "0 0 6px" }}>The storefront's accent color and the announcement buyers read at the top of the store.</p>
      <p style={{ fontSize: 12, color: "#9a9aa1", margin: "0 0 18px", lineHeight: 1.5 }}>Both settings stay editable after launch — they're exempt from the post-launch lock, so a moved deadline or a new color is a two-minute fix on a live store.</p>
      <StoreThemeColorCard store={store} onUpdateStore={onUpdateStore} onToast={onToast} />
      <div style={{ height: 16 }} />
      <AnnouncementMessageCard store={store} onUpdateStore={onUpdateStore} onToast={onToast} />
      {store.storeType === "league" && (
        <div style={{ marginTop: 16 }}>
          <LeagueAnnouncementOverrides store={store} org={org} onUpdateStore={onUpdateStore} onToast={onToast} onGoMyTeams={onGoMyTeams} />
        </div>
      )}
    </React.Fragment>
  );
}

Object.assign(window, { AnnouncementEditor, AnnouncementRichText, StoreCustomizationSection, SelectStoreColorModal, annSanitize, annIsEmpty, resolvedThemeColor });

