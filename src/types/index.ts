/**
 * Shared domain types.
 *
 * Empty until a feature needs one. Types are added alongside the service that
 * returns them, not speculatively — the entity shapes catalogued in
 * docs/architecture/data-inventory.md describe the PROTOTYPE's in-memory
 * objects, and CLAUDE.md forbids assuming they match the eventual backend.
 */
export {};
